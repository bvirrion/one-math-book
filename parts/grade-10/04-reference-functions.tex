\chapter{Reference Functions}\label{ch:g10:reffunc}

A handful of simple functions --- affine, square, inverse, square root,
cube --- appear everywhere, alone or combined. Knowing their graphs and
variations by heart turns many problems into a quick sketch. This chapter
studies each of them in turn and proves their variations with the
comparison technique of \cref{ch:g10:functions}.

\section{Affine functions}

\begin{definition}[Affine function]\label{def:g10:reffunc:affine}
An \emph{affine function}\index{affine function} is a function of the form
\[
f(x) = mx + p,
\]
where $m$ and $p$ are fixed real numbers. Its graph is a straight line:
$m$ is the \emph{slope}\index{slope} and $p$ the
\emph{$y$-intercept}\index{y-intercept@$y$-intercept} (the line crosses
the vertical axis at $(0, p)$). When $p = 0$ the function is
\emph{linear}: $f(x) = mx$ expresses proportionality.
\end{definition}

\begin{proposition}[Slope and variations]\label{prop:g10:reffunc:affine}
Let $f(x) = mx + p$.
\begin{enumerate}
  \item For any two distinct inputs $u \neq v$:
        $m = \dfrac{f(v) - f(u)}{v - u}$ (the slope is the change of the
        output per unit change of the input).
  \item If $m > 0$, $f$ is strictly increasing on $\R$; if $m < 0$,
        strictly decreasing; if $m = 0$, constant.
\end{enumerate}
\end{proposition}

\begin{proof}
1. Compute $f(v) - f(u) = (mv + p) - (mu + p) = m(v - u)$, then divide by
$v - u \neq 0$.

2. If $u < v$, then $f(v) - f(u) = m(v-u)$ has the sign of $m$, since
$v - u > 0$: positive $m$ gives $f(u) < f(v)$ (increasing), negative $m$
gives $f(u) > f(v)$ (decreasing).
\end{proof}

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis,
  width=7.8cm, height=5.6cm,
  xmin=-3.4, xmax=3.4, ymin=-2.8, ymax=4.4,
  xtick={-2,-1,1,2}, ytick={-1,1,2,3}]
  \addplot[omDef, thick, domain=-3:2.6] {0.5*x + 1};
  \addplot[omThm, thick, domain=-2.2:3] {-x + 1};
  \node[omDef, font=\small] at (axis cs:2.5,2.7) {$y = \tfrac12 x + 1$};
  \node[omThm, font=\small] at (axis cs:-2.0,2.6) {$y = -x + 1$};
  \draw[dashed] (axis cs:1,1.5) -- (axis cs:2,1.5) -- (axis cs:2,2);
  \node[font=\footnotesize, below] at (axis cs:1.5,1.5) {$1$};
  \node[font=\footnotesize, right] at (axis cs:2,1.75) {$m$};
\end{axis}
\end{tikzpicture}

{\small Two affine functions: increasing for $m = \frac12 > 0$, decreasing
for $m = -1 < 0$. Moving one unit to the right changes $y$ by $m$.}
\end{omfigure}

\begin{example}\label{ex:g10:reffunc:affine}
Find the affine function whose graph passes through $A(1, 3)$ and
$B(4, 9)$. The slope is
\[
m = \frac{9 - 3}{4 - 1} = \frac{6}{3} = 2 .
\]
Then $f(x) = 2x + p$, and $f(1) = 3$ gives $2 + p = 3$, so $p = 1$:
$f(x) = 2x + 1$. Check with $B$: $f(4) = 9$.
\end{example}

\section{The square function}

\begin{proposition}[Square function]\label{prop:g10:reffunc:square}
The function $f(x) = x^2$, defined on $\R$:
\begin{enumerate}
  \item is strictly decreasing on $\intoc{-\infty}{0}$ and strictly
        increasing on $\intco{0}{+\infty}$, with minimum $0$ at $x = 0$;
  \item satisfies $f(-x) = f(x)$ for all $x$: its graph, a
        \emph{parabola}\index{parabola}, is symmetric about the vertical
        axis.
\end{enumerate}
\end{proposition}

\begin{proof}
1. Let $0 \leq u < v$. Then
\[
f(v) - f(u) = v^2 - u^2 = (v - u)(v + u) > 0,
\]
since $v - u > 0$ and $v + u > 0$: $f$ is strictly increasing on
$\intco{0}{+\infty}$. If $u < v \leq 0$, then $v - u > 0$ but
$v + u < 0$, so $f(v) - f(u) < 0$: strictly decreasing. Finally
$x^2 \geq 0 = f(0)$ for all $x$.

2. $(-x)^2 = x^2$; so the points $(x, x^2)$ and $(-x, x^2)$, mirror images
across the vertical axis, are both on the graph.
\end{proof}

\begin{remark}[Squares and order]
Because the square function decreases on the negative side, taking squares
\emph{reverses} the order of negative numbers: $-3 < -2$ but $9 > 4$.
Never square both sides of an inequality without checking signs.
\end{remark}

\section{The inverse function}

\begin{proposition}[Inverse function]\label{prop:g10:reffunc:inverse}
The function $f(x) = \dfrac1x$, defined for $x \neq 0$:
\begin{enumerate}
  \item is strictly decreasing on $\intoo{-\infty}{0}$ and strictly
        decreasing on $\intoo{0}{+\infty}$;
  \item satisfies $f(-x) = -f(x)$: its graph, a
        \emph{hyperbola}\index{hyperbola}, is symmetric about the origin.
\end{enumerate}
\end{proposition}

\begin{proof}
1. Let $0 < u < v$. Then
\[
f(v) - f(u) = \frac1v - \frac1u = \frac{u - v}{uv} < 0,
\]
since $u - v < 0$ and $uv > 0$: strictly decreasing on
$\intoo{0}{+\infty}$. On $\intoo{-\infty}{0}$ the same quotient has
$u - v < 0$ and $uv > 0$ again (product of two negatives), so $f$
decreases there too.

2. $\frac{1}{-x} = -\frac1x$.
\end{proof}

\begin{remark}
Careful: $\frac1x$ is \emph{not} decreasing on its whole domain. From
$u = -1$ to $v = 1$ the value jumps from $-1$ up to $1$. The two branches
must be studied separately.
\end{remark}

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis,
  width=5.6cm, height=5.2cm,
  xmin=-3.2, xmax=3.2, ymin=-2.6, ymax=6.4,
  xtick={-2,-1,1,2}, ytick={1,4},
  title={$y = x^2$}]
  \addplot[omDef, thick, domain=-2.45:2.45, samples=90] {x^2};
\end{axis}
\end{tikzpicture}\hfill
\begin{tikzpicture}
\begin{axis}[omaxis,
  width=5.6cm, height=5.2cm,
  xmin=-3.4, xmax=3.4, ymin=-3.4, ymax=3.4,
  xtick={-1,1}, ytick={-1,1},
  title={$y = \frac1x$}]
  \addplot[omDef, thick, domain=0.3:3.1, samples=80] {1/x};
  \addplot[omDef, thick, domain=-3.1:-0.3, samples=80] {1/x};
\end{axis}
\end{tikzpicture}

{\small The parabola $y = x^2$ (symmetric about the vertical axis) and the
hyperbola $y = \frac1x$ (two branches, symmetric about the origin).}
\end{omfigure}

\section{Square root and cube}

\begin{proposition}[Square root function]\label{prop:g10:reffunc:sqrt}
The function $f(x) = \sqrt{x}$, defined on $\intco{0}{+\infty}$, is
strictly increasing.
\end{proposition}

\begin{proof}
Let $0 \leq u < v$. Multiply and divide by the \emph{conjugate}:
\[
\sqrt v - \sqrt u
= \frac{(\sqrt v - \sqrt u)(\sqrt v + \sqrt u)}{\sqrt v + \sqrt u}
= \frac{v - u}{\sqrt v + \sqrt u} > 0,
\]
since $v - u > 0$ and $\sqrt v + \sqrt u > 0$ (note $v > 0$, so
$\sqrt v > 0$).
\end{proof}

\begin{proposition}[Cube function]\label{prop:g10:reffunc:cube}
The function $f(x) = x^3$, defined on $\R$, is strictly increasing, and
its graph is symmetric about the origin.
\end{proposition}
\admitted

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis,
  width=5.6cm, height=5.0cm,
  xmin=-0.7, xmax=5.4, ymin=-0.7, ymax=2.9,
  xtick={1,4}, ytick={1,2},
  title={$y = \sqrt x$}]
  \addplot[omDef, thick, domain=0:5.1, samples=100] {sqrt(x)};
\end{axis}
\end{tikzpicture}\hfill
\begin{tikzpicture}
\begin{axis}[omaxis,
  width=5.6cm, height=5.0cm,
  xmin=-2.2, xmax=2.2, ymin=-3.4, ymax=3.4,
  xtick={-1,1}, ytick={-1,1},
  title={$y = x^3$}]
  \addplot[omDef, thick, domain=-1.48:1.48, samples=90] {x^3};
\end{axis}
\end{tikzpicture}

{\small The square root (defined for $x \geq 0$ only) and the cube, both
increasing.}
\end{omfigure}

\begin{proposition}[Comparing $x$, $x^2$ and $\sqrt x$]\label{prop:g10:reffunc:compare}
For $0 < x < 1$:\ \ $x^2 < x < \sqrt x$.\quad
For $x > 1$:\ \ $\sqrt x < x < x^2$.\quad
At $x = 0$ and $x = 1$ the three values are equal.
\end{proposition}

\begin{proof}
Suppose $0 < x < 1$. Multiplying $x < 1$ by $x > 0$ gives $x^2 < x$.
Next, $x < \sqrt x$: since both sides are positive and squaring is
increasing on positive numbers, this inequality is equivalent to
$x^2 < x$, which we just proved. For $x > 1$, multiplying $x > 1$ by $x$
gives $x^2 > x$, and $\sqrt x < x$ follows by the same squaring argument.
\end{proof}

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis,
  width=7.4cm, height=5.8cm,
  xmin=-0.35, xmax=2.5, ymin=-0.35, ymax=2.9,
  xtick={1,2}, ytick={1,2}]
  \addplot[omThm, thick, domain=0:1.65, samples=80] {x^2};
  \addplot[omDef, thick, domain=0:2.35] {x};
  \addplot[omMeth, thick, domain=0:2.35, samples=80] {sqrt(x)};
  \fill (axis cs:1,1) circle (1.5pt);
  \node[omThm, font=\small, right] at (axis cs:1.45,2.4) {$y = x^2$};
  \node[omDef, font=\small, right] at (axis cs:2.0,1.85) {$y = x$};
  \node[omMeth, font=\small, right] at (axis cs:2.0,1.28) {$y = \sqrt x$};
\end{axis}
\end{tikzpicture}

{\small The three curves cross at $(0,0)$ and $(1,1)$ and swap order
there: between $0$ and $1$, $x^2$ is the smallest and $\sqrt x$ the
largest; beyond $1$ the order is reversed.}
\end{omfigure}

\begin{method}[Comparing images]\label{met:g10:reffunc:compare}
To compare $f(a)$ and $f(b)$ for a reference function $f$:
\begin{enumerate}
  \item place $a$ and $b$ on an interval where the variations of $f$ are
        known;
  \item if $f$ increases there, images are in the same order as the
        inputs; if $f$ decreases, the order is reversed;
  \item if $a$ and $b$ are not in the same interval of monotonicity, deal
        with signs first (e.g.\ for the square: a negative and a positive
        input).
\end{enumerate}
\end{method}

\begin{example}
Compare $\dfrac{1}{2.3}$ and $\dfrac{1}{2.4}$: both inputs are in
$\intoo{0}{+\infty}$, where the inverse function decreases, and
$2.3 < 2.4$, so $\dfrac{1}{2.3} > \dfrac{1}{2.4}$. Compare $(-1.2)^2$
and $(-1.5)^2$: on $\intoc{-\infty}{0}$ the square decreases, and
$-1.5 < -1.2$, so $(-1.5)^2 > (-1.2)^2$ --- indeed $2.25 > 1.44$.
\end{example}

\section{Exercises}

\begin{exercise}[$\star$]\label{exo:g10:reffunc:1}
For each affine function, give its slope, its $y$-intercept, and say
whether it is increasing or decreasing:
\[
f(x) = 3x - 2, \qquad
g(x) = -\tfrac12 x + 4, \qquad
h(x) = 7, \qquad
k(x) = -x .
\]
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g10:reffunc:2}
Find the affine function whose graph passes through $(2, 1)$ and
$(5, 10)$; then the one through $(-1, 4)$ and $(3, -4)$.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g10:reffunc:3}
Without a calculator, compare:
\[
(3.1)^2 \text{ and } (3.2)^2; \qquad
(-2.7)^2 \text{ and } (-2.8)^2; \qquad
\frac{1}{5.1} \text{ and } \frac{1}{5.2}; \qquad
\sqrt{17} \text{ and } \sqrt{15}.
\]
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g10:reffunc:4}
Using the graph of the square function, solve $x^2 = 16$, then
$x^2 \leq 16$, then $x^2 > 9$.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g10:reffunc:5}
Let $x \in \intoo{0}{1}$. Order the numbers $x$, $x^2$, $x^3$ and
$\sqrt x$ from smallest to largest, and check your answer with
$x = 0.25$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g10:reffunc:6}
Solve graphically, then algebraically: $\dfrac1x = 2$, and
$\dfrac1x < 2$ for $x > 0$. What changes if we also allow $x < 0$?
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g10:reffunc:7}
Knowing that the square function decreases on $\intoc{-\infty}{0}$ and
increases on $\intco{0}{+\infty}$, frame $x^2$ when:
\[
\text{(a) } x \in \intcc{2}{5};
\qquad
\text{(b) } x \in \intcc{-3}{-1};
\qquad
\text{(c) } x \in \intcc{-2}{3}.
\]
(In case (c), be careful: the minimum of $x^2$ is not at an endpoint.)
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g10:reffunc:8}
A taxi company charges a fixed fee of $4$ plus $1.5$ per kilometer;
another charges no fee and $2$ per kilometer. Model each price by an
affine function of the distance $x$, plot both lines, and find from which
distance the first company is cheaper.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g10:reffunc:9}
Show that for all $a, b \geq 0$: $\sqrt{a + b} \leq \sqrt a + \sqrt b$.
(Hint: both sides are nonnegative, so compare their squares.) When is
there equality?
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:g10:reffunc:10}
Let $f(x) = \dfrac{2x + 1}{x - 1}$, defined for $x \neq 1$.
\begin{enumerate}
  \item Show that $f(x) = 2 + \dfrac{3}{x - 1}$ for all $x \neq 1$.
  \item Deduce the variations of $f$ on $\intoo{1}{+\infty}$ from those
        of the inverse function.
\end{enumerate}
\end{exercise}
