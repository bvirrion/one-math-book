\chapter{Rijen: een eerste cursus}\label{ch:g11:seq}

Een rij is een lijst getallen voortgebracht door een regel: de
opeenvolgende saldi van een spaarrekening, de groottes van een populatie
jaar na jaar. Dit hoofdstuk bestudeert de twee families die toepassingen
domineren --- \emph{rekenkundige} rijen, die met gelijke stappen groeien,
en \emph{meetkundige} rijen, die met gelijke verhoudingen groeien. De
rigoureuze theorie van limieten wordt ontwikkeld in \cref{ch:g12:seq}.

\section{Een rij definiëren}

\begin{definition}[Rij]\label{def:g11:seq:sequence}
Een \emph{rij}\index{rij} $(u_n)$ koppelt aan elk geheel getal $n \geq 0$
(of $n \geq 1$) een reëel getal $u_n$, haar \emph{term van index $n$}.
Een rij kan gegeven worden
\begin{itemize}
  \item \emph{expliciet}, door een formule voor $u_n$ in termen van $n$:
        bijv.\ $u_n = n^2 + 1$;
  \item \emph{recursief}, door haar eerste term en een regel om van elke
        term naar de volgende te gaan: bijv.\ $u_0 = 3$ en
        $u_{n+1} = 2u_n - 1$.
\end{itemize}
\end{definition}

\begin{example}
Voor $u_n = n^2 + 1$: $u_0 = 1$, $u_1 = 2$, $u_2 = 5$, en $u_{10} = 101$
direct. Voor $u_0 = 3$, $u_{n+1} = 2u_n - 1$: $u_1 = 5$, $u_2 = 9$,
$u_3 = 17$ --- elke term heeft de vorige nodig; $u_{10}$ bereiken kost
tien stappen (of een algemene formule, zie \cref{exo:g11:seq:11}).
\end{example}

\section{Rekenkundige rijen}

\begin{definition}[Rekenkundige rij]\label{def:g11:seq:arithmetic}
Een rij is \emph{rekenkundig}\index{rekenkundige rij} met
\emph{gemeenschappelijk verschil} $d$ als elke term uit de vorige
verkregen wordt door $d$ op te tellen:
\[
u_{n+1} = u_n + d \quad \text{voor alle } n.
\]
Equivalent: het verschil $u_{n+1} - u_n$ is constant, gelijk aan $d$.
\end{definition}

\begin{theorem}[Algemene term]\label{thm:g11:seq:arithgeneral}
Als $(u_n)$ rekenkundig is met eerste term $u_0$ en gemeenschappelijk
verschil $d$, dan
\[
u_n = u_0 + n\,d \quad \text{voor alle } n \geq 0,
\qquad\text{en algemener } u_n = u_p + (n - p)\,d .
\]
\end{theorem}

\begin{proof}
Om van $u_0$ naar $u_n$ te gaan wordt de regel ``tel $d$ op'' $n$ keer
toegepast: één stap geeft $u_1 = u_0 + d$, twee stappen geven
$u_2 = u_0 + 2d$, en na $n$ stappen heeft elke toepassing één $d$
bijgedragen, dus $u_n = u_0 + nd$. (Dit ``enzovoort'' wordt rigoriseerd
door inductie in \cref{ch:g12:seq}.) De algemene formule volgt door de
$n - p$ stappen van $u_p$ naar $u_n$ te tellen.
\end{proof}

\begin{theorem}[Som van opeenvolgende gehele getallen]\label{thm:g11:seq:intsum}
Voor elk geheel getal $n \geq 1$:
\[
1 + 2 + \dots + n = \frac{n(n+1)}{2}.
\]
Algemener is een som van opeenvolgende termen van een rekenkundige rij
gelijk aan
\[
(\text{aantal termen}) \times
\frac{\text{eerste term} + \text{laatste term}}{2}.
\]
\end{theorem}

\begin{proof}
Schrijf de som $S$ twee keer, de tweede keer in omgekeerde volgorde, en
tel kolom voor kolom op:
\[
\begin{array}{ccccccccc}
S & = & 1 & + & 2 & + & \dots & + & n\\
S & = & n & + & (n-1) & + & \dots & + & 1\\
\hline
2S & = & (n+1) & + & (n+1) & + & \dots & + & (n+1)
\end{array}
\]
Er zijn $n$ kolommen, elk sommerend tot $n + 1$, dus $2S = n(n+1)$. Voor
een algemene rekenkundige rij werkt hetzelfde paren: eerste $+$ laatste
$=$ tweede $+$ voorlaatste $= \dots$, omdat één stap vooruit aan het
linkereind ($+d$) gecompenseerd wordt door één stap achteruit aan het
rechtereind ($-d$).
\end{proof}

\begin{example}
$1 + 2 + \dots + 100 = \frac{100 \times 101}{2} = 5050$. De som van de
oneven getallen $1 + 3 + \dots + 99$ ($50$ termen) is
$50 \times \frac{1 + 99}{2} = 2500$.
\end{example}

\section{Meetkundige rijen}

\begin{definition}[Meetkundige rij]\label{def:g11:seq:geometric}
Een rij is \emph{meetkundig}\index{meetkundige rij} met
\emph{gemeenschappelijke verhouding} $q \neq 0$ als elke term uit de
vorige verkregen wordt door met $q$ te vermenigvuldigen:
\[
u_{n+1} = q\,u_n \quad \text{voor alle } n.
\]
Equivalent, wanneer geen term verdwijnt: de verhouding
$\frac{u_{n+1}}{u_n}$ is constant, gelijk aan $q$.
\end{definition}

\begin{theorem}[Algemene term]\label{thm:g11:seq:geomgeneral}
Als $(u_n)$ meetkundig is met eerste term $u_0$ en verhouding $q$, dan
\[
u_n = u_0\, q^n \quad \text{voor alle } n \geq 0,
\qquad\text{en algemener } u_n = u_p\, q^{\,n-p} .
\]
\end{theorem}

\begin{proof}
Zelfde stappentelling als in \cref{thm:g11:seq:arithgeneral}: van $u_0$
naar $u_n$ wordt de regel ``vermenigvuldig met $q$'' $n$ keer toegepast,
wat een factor $q^n$ bijdraagt.
\end{proof}

\begin{theorem}[Meetkundige som]\label{thm:g11:seq:geomsum}
Voor elke reële $q \neq 1$ en geheel getal $n \geq 0$:
\[
1 + q + q^2 + \dots + q^n = \frac{1 - q^{\,n+1}}{1 - q}.
\]
\end{theorem}

\begin{proof}
Laat $S = 1 + q + \dots + q^n$. Vermenigvuldig met $q$:
$qS = q + q^2 + \dots + q^{n+1}$. Trek af:
\[
S - qS = \bigl(1 + q + \dots + q^n\bigr)
- \bigl(q + q^2 + \dots + q^{n+1}\bigr) = 1 - q^{\,n+1},
\]
omdat elke tussenterm één keer in elke som voorkomt en wegvalt. Dus
$(1 - q)S = 1 - q^{\,n+1}$, en delen door $1 - q \neq 0$ geeft de
formule.
\end{proof}

\begin{example}\label{ex:g11:seq:chessboard}
$1 + 2 + 4 + \dots + 2^{10} = \frac{1 - 2^{11}}{1 - 2} = 2^{11} - 1 =
2047$: rijstkorrels verdubbelen op de velden van een schaakbord overweldigt
elke graanschuur ruim vóór het $64$ste veld, waar het totaal
$2^{64} - 1 \approx 1.8 \times 10^{19}$ is.
\end{example}

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis,
  width=10cm, height=5.6cm,
  xlabel={$n$}, ylabel={$u_n$},
  xmin=-0.5, xmax=10.8, ymin=0, ymax=13,
  xtick={5,10}, ytick={2,6}]
  \addplot[omDef, only marks, mark=*, mark size=1.5pt,
    samples at={0,...,10}] {2 + 0.9*x};
  \addplot[omThm, only marks, mark=*, mark size=1.5pt,
    samples at={0,...,10}] {2*1.2^x};
  \node[omDef, font=\small, anchor=east] at (axis cs:9.7,10.9)
    {rekenkundig};
  \node[omThm, font=\small, anchor=west] at (axis cs:8.6,7.1)
    {meetkundig};
\end{axis}
\end{tikzpicture}

{\small Gelijke stappen versus gelijke verhoudingen: een rekenkundige rij
($u_{n+1} = u_n + 0.9$, blauw) volgt een lijn, een meetkundige rij
($u_{n+1} = 1.2\,u_n$, rood) volgt een exponentiële kromme die haar
uiteindelijk overgroeit.}
\end{omfigure}

\begin{method}[Het type van een rij herkennen]\label{met:g11:seq:recognize}
Bereken $u_{n+1} - u_n$ en vereenvoudig. Als het resultaat een constante
$d$ is, is de rij rekenkundig. Anders bereken $\frac{u_{n+1}}{u_n}$
(termen niet-nul) en vereenvoudig: een constante $q$ betekent meetkundig.
Als geen van beide constant is, is de rij van geen van beide types ---
concludeer nooit alleen uit de eerste paar termen.
\end{method}

\begin{example}
Voor $u_n = 3 \times 5^n$:
$\frac{u_{n+1}}{u_n} = \frac{3 \times 5^{n+1}}{3 \times 5^n} = 5$ voor
alle $n$: meetkundig met verhouding $5$. Voor $u_n = n^2$:
$u_1 - u_0 = 1$ maar $u_2 - u_1 = 3$, en $\frac{u_1}{u_0}$ is zelfs niet
gedefinieerd --- noch rekenkundig noch meetkundig.
\end{example}

\section{Monotonie}

\begin{definition}[Monotone rij]\label{def:g11:seq:monotone}
Een rij $(u_n)$ is \emph{stijgend} als $u_{n+1} \geq u_n$ voor alle $n$,
en \emph{dalend} als $u_{n+1} \leq u_n$ voor alle $n$.
\end{definition}

\begin{method}[Monotonie bestuderen]\label{met:g11:seq:monotonicity}
Bestudeer het teken van $u_{n+1} - u_n$. Voor rijen met positieve termen
mag men in plaats daarvan $\frac{u_{n+1}}{u_n}$ met $1$ vergelijken.
\end{method}

\begin{example}
Een rekenkundige rij is stijgend wanneer $d \geq 0$
($u_{n+1} - u_n = d$), dalend wanneer $d \leq 0$. Een meetkundige rij
met $u_0 > 0$ en $q > 1$ is stijgend:
$u_{n+1} - u_n = u_0 q^n (q - 1) > 0$; met $u_0 > 0$ en $0 < q < 1$ is ze
dalend.
\end{example}

\section{Lange-termijngedrag, informeel}

Wat gebeurt er met $u_n$ wanneer $n$ zeer groot wordt? Voor een
rekenkundige rij met $d > 0$ overschrijden de termen $u_0 + nd$
uiteindelijk elk vast getal. Voor een meetkundige rij met $0 < q < 1$
krimpen de termen $u_0 q^n$ naar $0$: herhaaldelijk vermenigvuldigen met
$0.9$, zeg, erodeert elke startwaarde. En voor $q > 1$ exploderen de
termen, zoals in \cref{ex:g11:seq:chessboard}.

\begin{remark}
Deze uitspraken kunnen perfect precies gemaakt worden --- ``de termen
blijven uiteindelijk binnen elke gegeven afstand van $0$'' --- en
bewezen. Dat is de theorie van \emph{limieten}, het openings thema van
\cref{ch:g12:seq}.
\end{remark}

\section{Oefeningen}

\begin{exercise}[$\star$]\label{exo:g11:seq:1}
Bereken voor elke rij $u_1$, $u_2$, $u_3$:
\[
u_n = \frac{n}{n+1}; \qquad
u_0 = 5,\ u_{n+1} = 3u_n - 2; \qquad
u_n = (-1)^n\,n .
\]
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g11:seq:2}
$(u_n)$ is rekenkundig met $u_0 = 7$ en $d = -3$. Bereken $u_{10}$ en
$u_{25}$. $(v_n)$ is rekenkundig met $v_3 = 11$ en $v_8 = 26$. Vind het
gemeenschappelijk verschil en $v_0$.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g11:seq:3}
$(u_n)$ is meetkundig met $u_0 = 5$ en $q = 2$. Bereken $u_8$. $(v_n)$ is
meetkundig met positieve termen, $v_2 = 12$ en $v_4 = 48$. Vind de
verhouding en $v_0$.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g11:seq:4}
Bereken
\[
1 + 2 + 3 + \dots + 500, \qquad
4 + 7 + 10 + \dots + 61, \qquad
1 + \frac12 + \frac14 + \dots + \frac{1}{2^{10}} .
\]
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g11:seq:5}
Bepaal of elke rij rekenkundig, meetkundig, of geen van beide is:
\[
u_n = 4n - 1; \qquad
v_n = \frac{2^n}{3^{n+1}}; \qquad
w_n = n^2 + n .
\]
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g11:seq:6}
Een theater heeft $20$ rijen: $16$ stoelen in de eerste rij, en elke rij
heeft $2$ stoelen meer dan de vorige. Hoeveel stoelen staan in de laatste
rij? In het hele theater?
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g11:seq:7}
Een bacteriepopulatie verdubbelt elk uur; om twaalf uur zijn er $500$
bacteriën. Hoeveel zijn er om 20u? Na hoeveel volle uren overschrijdt de
populatie voor het eerst één miljoen? (Los op door opeenvolgende machten
van $2$ te proberen.)
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g11:seq:8}
Elke maand stort een spaarder $100$ euro op een rekening die $0.2\%$
rente per maand betaalt over het bestaande saldo (rente wordt bijgeschreven
vlak vóór de storting). Laat $c_n$ het saldo zijn vlak na de $n$-de
storting, dus $c_1 = 100$ en $c_{n+1} = 1.002\,c_n + 100$. Bereken $c_2$
en $c_3$, en leg uit waarom $(c_n)$ noch rekenkundig noch meetkundig is.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g11:seq:9}
Bestudeer de monotonie van de rijen
\[
u_n = n^2 - 8n \ (n \geq 0), \qquad
v_n = \frac{3^n}{n!}\ (n \geq 1),
\]
waar $n! = 1 \times 2 \times \dots \times n$. (Voor $(v_n)$, vergelijk
$\frac{v_{n+1}}{v_n}$ met $1$.)
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g11:seq:10}
De som van de eerste $n$ termen van een rekenkundige rij met $u_0 = 3$
en $d = 4$ is gelijk aan $903$. Vind $n$. (Stel een kwadratische
vergelijking in $n$ op en gebruik \cref{ch:g11:quad}.)
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:g11:seq:11}
Laat $u_0 = 3$ en $u_{n+1} = 2u_n - 1$.
\begin{enumerate}
  \item Bereken $u_1, u_2, u_3$ en vermoed een formule voor $u_n$.
  \item Laat $v_n = u_n - 1$. Toon aan dat $(v_n)$ meetkundig is, geef
        haar verhouding en eerste term.
  \item Leid een expliciete formule voor $u_n$ af en verifieer je
        vermoeden.
\end{enumerate}
\end{exercise}
