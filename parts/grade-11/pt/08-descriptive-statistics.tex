\chapter{Estatística Descritiva}\label{ch:g11:stat}

As estatísticas compactam uma lista de números em alguns resumos significativos:
onde estão os dados (\omterm{def:g11:stat:mean}{média}, \omterm{def:g11:stat:median}{mediana}) e quão espalhado ele é (quartis,
\omterm{def:g11:stat:variance}{variância}, \omterm{def:g11:stat:variance}{variância}). Este capítulo resolve tudo de uma só vez
exemplo de execução. Os mesmos resumos reaparecem para variáveis aleatórias em
\cref{ch:g11:prob}, e suas leis probabilísticas em \cref{ch:g12:sums}.

Ao longo do capítulo, nosso conjunto de dados em execução são as marcas (de $20$) de
uma aula de $12$ alunos:
\[
8,\ 9,\ 10,\ 10,\ 11,\ 12,\ 12,\ 12,\ 13,\ 14,\ 16,\ 17 .
\]

\section{Média e mediana}

\begin{definition}[Média]\label{def:g11:stat:mean}
A \emph{média}\index{média} dos valores $ x_1, x_2, \dots, x_n $ é
\[
\bar x = \frac{x_1 + x_2 + \dots + x_n}{n}.
\]
Se o valor $ x_i $ aparece com \omterm{def:g10:stats:series}{frequência} $ n_i $ (com
$ n_1 + \dots + n_k = n $), então
$\bar x = \frac{n_1 x_1 + \dots + n_k x_k}{n}$.
\end{definition}

\begin{definition}[Mediana]\label{def:g11:stat:median}
UM \emph{mediana}\index{mediana} de um conjunto de dados classificado é um valor que divide
pela metade: pelo menos metade dos valores são $\leq $ isso, e pelo menos metade são
$\geq $ isto. Na prática, classifique o $ n $ valores; se $ n $ é \omterm{def:g11:func:parity}{ímpar}, a mediana é
o valor médio; se $ n $ é par, pegue o \omterm{prop:g10:coordgeom:midpoint}{ponto médio} dos dois médios
valores.
\end{definition}

\begin{example}\label{ex:g11:stat:meanmedian}
Para o conjunto de dados em execução, a soma é $144$, então $\bar x = \frac{144}{12} =
12$. Os dois valores intermediários ($6$ o e $7$ da lista ordenada) são ambos
$12$, então a \omterm{def:g11:stat:median}{mediana} é $12$ também. \omterm{def:g11:stat:mean}{média} e \omterm{def:g11:stat:median}{mediana} não precisa coincidir: o
\omterm{def:g11:stat:median}{mediana} ignora \emph{quão longe} os valores extremos são, a \omterm{def:g11:stat:mean}{média} não.
Substituindo a marca superior $17$ por $20$ move a \omterm{def:g11:stat:mean}{média} para $12.25$ mas vai embora
a \omterm{def:g11:stat:median}{mediana} no $12$.
\end{example}

\section{Quartis}

\begin{definition}[Quartis, intervalo interquartil]\label{def:g11:stat:quartiles}
O \emph{primeiro quartil}\index{primeiro quartil} $ Q_1$ é o menor valor tal
que pelo menos um quarto dos dados seja $\leq Q_1$; o \emph{terceiro
quartil} $ Q_3$ é o menor valor tal que pelo menos três quartos da
os dados são $\leq Q_3$. O \emph{intervalo interquartil}\index{intervalo interquartil} é $ Q_3 - Q_1$: o
largura da metade central dos dados.
\end{definition}

\begin{example}
$ n = 12$: um quarto é $3$, então $ Q_1$ é o $3$ Terceiro valor classificado:
$Q_1 = 10$. Três quartos é $9$, então $ Q_3$ é o $9$ o valor:
$Q_3 = 13$. O \omterm{def:g10:stats:quartiles}{amplitude} interquartil é $3$: a metade central da classe
senta-se dentro $3$ marcas.
\end{example}

\begin{omfigure}
\begin{tikzpicture}[x=0.62cm]
  \draw[->] (7,0) -- (18.4,0) node[right, font=\small] {mark};
  \foreach \x in {8,10,12,14,16} {
    \draw (\x,0) -- (\x,-0.12) node[below, font=\small] {\x};
  }
  \fill[omDef] (8,0.25) circle (2pt);
  \fill[omDef] (9,0.25) circle (2pt);
  \fill[omDef] (10,0.25) circle (2pt);
  \fill[omDef] (10,0.55) circle (2pt);
  \fill[omDef] (11,0.25) circle (2pt);
  \fill[omDef] (12,0.25) circle (2pt);
  \fill[omDef] (12,0.55) circle (2pt);
  \fill[omDef] (12,0.85) circle (2pt);
  \fill[omDef] (13,0.25) circle (2pt);
  \fill[omDef] (14,0.25) circle (2pt);
  \fill[omDef] (16,0.25) circle (2pt);
  \fill[omDef] (17,0.25) circle (2pt);
  \draw[omThm, thick] (12,-0.35) -- (12,1.15)
    node[above, font=\small] {$\bar x = 12$};
  \draw[omProp, thick] (10,1.55) -- (10,2.35) -- (13,2.35) -- (13,1.55)
    -- cycle;
  \draw[omProp, thick] (12,1.55) -- (12,2.35);
  \draw[omProp, thick] (8,1.95) -- (10,1.95);
  \draw[omProp, thick] (13,1.95) -- (17,1.95);
  \draw[omProp, thick] (8,1.75) -- (8,2.15);
  \draw[omProp, thick] (17,1.75) -- (17,2.15);
  \node[omProp, font=\small, above] at (10,2.35) {$ Q_1$};
  \node[omProp, font=\small, above] at (13,2.35) {$ Q_3$};
\end{tikzpicture}

{\small O conjunto de dados em execução como um \omterm{def:g10:functions:graph}{gráfico} de pontos, é \omterm{def:g11:stat:mean}{média} (vermelho) e o
resumo de cinco números acima (laranja): \omterm{def:g10:functions:extrema}{mínimo}, $ Q_1$, \omterm{def:g11:stat:median}{mediana}, $ Q_3$,
\omterm{def:g10:functions:extrema}{máximo}. A caixa contém a metade central dos dados.}
\end{omfigure}

\section{Variância e desvio padrão}

\begin{definition}[Variância, desvio padrão]\label{def:g11:stat:variance}
O \emph{variância}\index{variância} do conjunto de dados é a \omterm{def:g11:stat:mean}{média} do
desvios quadrados do \omterm{def:g11:stat:mean}{média}:
\[
V = \frac{1}{n}\sum_{i=1}^{n} (x_i - \bar x)^2,
\]
e o \emph{desvio padrão}\index{desvio padrão} é
$\sigma = \sqrt{V}$, expresso na mesma unidade dos dados.
\end{definition}

\begin{proposition}[Fórmula de atalho]\label{prop:g11:stat:shortcut}
A \omterm{def:g11:stat:variance}{variância} é a \omterm{def:g11:stat:mean}{média} dos quadrados menos o quadrado do \omterm{def:g11:stat:mean}{média}:
\[
V = \frac{1}{n}\sum_{i=1}^{n} x_i^2 \;-\; \bar x^{\,2}.
\]
\end{proposition}

\begin{proof}
Expanda cada desvio quadrático:
$(x_i - \bar x)^2 = x_i^2 - 2\bar x\, x_i + \bar x^2$. Resumindo e
dividindo por $ n $:
\[
V = \frac1n \sum x_i^2 - 2\bar x \cdot \underbrace{\frac1n \sum
x_i}_{=\ \bar x} + \frac1n \cdot n\bar x^2
= \frac1n\sum x_i^2 - 2\bar x^2 + \bar x^2
= \frac1n\sum x_i^2 - \bar x^2 .
\]
A mesma identidade, para variáveis aleatórias, é
\cref{prop:g11:prob:konig}.
\end{proof}

\begin{example}\label{ex:g11:stat:variance}
Para o conjunto de dados em execução, os desvios de $\bar x = 12$ são $-4$,
$-3$, $-2$, $-2$, $-1$, $0$, $0$, $0$, $1$, $2$, $4$, $5$; seus quadrados
soma para
\[
16 + 9 + 4 + 4 + 1 + 0 + 0 + 0 + 1 + 4 + 16 + 25 = 80 .
\]
Por isso
\[
V = \frac{80}{12} = \frac{20}{3} \approx 6.67,
\qquad
\sigma = \sqrt{20/3} \approx 2.6 \text{ marks}.
\]
Grosso modo, uma marca típica fica $2.6$ marcas longe do
\omterm{def:g11:stat:mean}{média}.
\end{example}

\begin{proposition}[Mudança de unidades]\label{prop:g11:stat:affine}
Se todo valor for transformado por $ y_i = a x_i + b $, então
\[
\bar y = a \bar x + b,
\qquad
V_y = a^2\, V_x,
\qquad
\sigma_y = \abs{a}\,\sigma_x .
\]
\end{proposition}

\begin{proof}
$\bar y = \frac1n \sum (a x_i + b) = a\left(\frac1n\sum x_i\right) +
\frac1n \cdot nb = a\bar x + b $. Então
$ y_i - \bar y = a(x_i - \bar x)$: cada desvio é dimensionado por $a$
(a mudança $ b $ cancela), então cada desvio quadrático é escalonado por $ a^2$,
e assim é o deles \omterm{def:g11:stat:mean}{média}. Tirar raízes quadradas dá
$\sigma_y = \abs a\,\sigma_x $.
\end{proof}

\begin{example}
Temperaturas registradas em graus Celsius com \omterm{def:g11:stat:mean}{média} $20$ e \omterm{def:g11:stat:variance}{variância} $3$ converter para Fahrenheit por $ F = 1.8\,C + 32$: a \omterm{def:g11:stat:mean}{média}
torna-se $1.8 \times 20 + 32 = 68$, o \omterm{def:g11:stat:variance}{variância}
$1.8 \times 3 = 5.4$. A mudança de um conjunto de dados não altera sua distribuição;
redimensionar redimensiona o spread.
\end{example}

\begin{method}[Resumindo e comparando conjuntos de dados]\label{met:g11:stat:compare}
Para comparar dois conjuntos de dados, coloque lado a lado: \omterm{def:g11:stat:mean}{média} (ou \omterm{def:g11:stat:median}{mediana}) para
\emph{posição}, \omterm{def:g11:stat:variance}{variância} (ou \omterm{def:g10:stats:quartiles}{amplitude} interquartil) para
\emph{espalhar}. O par (\omterm{def:g11:stat:median}{mediana}, \omterm{def:g10:stats:quartiles}{amplitude} interquartil) é robusto para
valores extremos; o par (\omterm{def:g11:stat:mean}{média}, \omterm{def:g11:stat:variance}{variância}) usa todos os valores e
alimenta \omterm{def:g10:proba:distribution}{probabilidade} teoria.
\end{method}

\begin{example}
Dois arqueiros disparam cada $10$ flechas; \omterm{def:g11:stat:mean}{média} de pontuação $8.5$ para ambos, mas
o \omterm{def:g11:stat:variance}{variância} são $0.5$ e $2.1$. Mesmo nível em \omterm{def:g11:stat:mean}{média} --- mas
o primeiro arqueiro é muito mais consistente.
\end{example}

\section{Exercícios}

\begin{exercise}[$\star $]\label{exo:g11:stat:1}
Calcule a \omterm{def:g11:stat:mean}{média}, \omterm{def:g11:stat:median}{mediana}, quartis e \omterm{def:g10:stats:quartiles}{amplitude} interquartil do
conjunto de dados
\[
5,\ 7,\ 7,\ 8,\ 9,\ 10,\ 11,\ 14 .
\]
\end{exercise}

\begin{exercise}[$\star $]\label{exo:g11:stat:2}
Calcule a \omterm{def:g11:stat:variance}{variância} e \omterm{def:g11:stat:variance}{variância} do conjunto de dados
$2,\ 4,\ 4,\ 4,\ 5,\ 5,\ 7,\ 9$ (\omterm{def:g11:stat:mean}{média} primeiro, depois a fórmula de atalho de
\cref{prop:g11:stat:shortcut}).
\end{exercise}

\begin{exercise}[$\star $]\label{exo:g11:stat:3}
Um dado é lançado $50$ vezes; os resultados são resumidos por \omterm{def:g10:stats:series}{frequências}:
\begin{center}
\begin{tabular}{c|cccccc}
outcome & 1 & 2 & 3 & 4 & 5 & 6\\
\hline
\omterm{def:g10:stats:series}{frequência} & 6 & 9 & 8 & 10 & 9 & 8
\end{tabular}
\end{center}
Calcule a \omterm{def:g11:stat:mean}{média} resultado e a \omterm{def:g11:stat:median}{mediana}.
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g11:stat:4}
Uma aula de $15$ os alunos têm \omterm{def:g11:stat:mean}{média} marca $11.2$. UM $16$ o aluno ingressa e
pontuações $18$. Qual é a nova classe \omterm{def:g11:stat:mean}{média}?
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g11:stat:5}
A \omterm{def:g11:stat:mean}{média} do conjunto de dados $6, 8, x, 12, 14$ é $10$. Encontrar $x$, então calcule
o \omterm{def:g11:stat:variance}{variância} do conjunto de dados concluído.
\end{exercise}

\begin{exercise}[$\star $]\label{exo:g11:stat:6}
Marcas com \omterm{def:g11:stat:mean}{média} $12.4$ e \omterm{def:g11:stat:variance}{variância} $2.5$ são redimensionados por
$ y = \frac{x}{2} + 4$. Dê o novo \omterm{def:g11:stat:mean}{média} e o novo \omterm{def:g11:stat:variance}{variância}.
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g11:stat:7}
Grupo A ($20$ valores) tem \omterm{def:g11:stat:mean}{média} $10$; grupo B ($30$ valores) tem \omterm{def:g11:stat:mean}{média} $15$.
Calcule a \omterm{def:g11:stat:mean}{média} do $50$ valores reunidos. É o \omterm{prop:g10:coordgeom:midpoint}{ponto médio}
$12.5$? Por que não?
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g11:stat:8}
Duas linhas de produção preenchem $1$-kg sacos de açúcar. \omterm{def:g10:proba:sample}{amostras} dar:
linha 1: \omterm{def:g11:stat:mean}{média} $1.002$ kg, \omterm{def:g11:stat:variance}{variância} $0.006$ kg; linha 2: \omterm{def:g11:stat:mean}{média}
$1.001$ kg, \omterm{def:g11:stat:variance}{variância} $0.019$ kg. Qual linha deve ser
recalibrado primeiro e por quê?
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g11:stat:9}
Um conjunto de dados de $ n = 10$ valores tem $\sum x_i = 70$ e
$\sum x_i^2 = 540$. Calcule seu \omterm{def:g11:stat:mean}{média}, \omterm{def:g11:stat:variance}{variância} e \omterm{def:g11:stat:variance}{variância}.
\end{exercise}

\begin{exercise}[$\star\star\star $]\label{exo:g11:stat:10}
O conjunto de dados $1,\ 2,\ 2,\ 3,\ 3,\ 3,\ 4,\ 4,\ 5,\ 23$ contém um
\emph{atípico}.
\begin{enumerate}
  \item Calcule a \omterm{def:g11:stat:mean}{média} e \omterm{def:g11:stat:median}{mediana} com e sem valor $23$.
  \item Calcule o \omterm{def:g10:stats:quartiles}{amplitude} interquartil com e sem $23$.
  \item Quais resumos são robustos ao outlier? Conclua com um
        recomendação.
\end{enumerate}
\end{exercise}

\section{Problema: O escore z, uma régua universal}

\begin{problem}[{Problema de fim de semana --- comparação de desvios padrão
maçãs com laranjas, a desigualdade de Chebyshev vira $\sigma $ em um
garantia, e a média ganha sua própria medalha}]
\label{pb:g11:stat:1}
É $15/20$ em um exame difícil melhor do que $13/20$ em um fácil? É
um $23$ em uma lista de pequenos números, um acidente ou uma exceção? Cru
os números não podem dizer --- mas divididos pela direita \omterm{def:g11:stat:variance}{variância} todos falam uma língua, a \emph{\omterm{pb:g11:stat:1}{pontuação z}}. Isto
problema domina a \omterm{def:g11:stat:variance}{variância} maquinaria
(\cref{def:g11:stat:variance},
\cref{prop:g11:stat:shortcut}), constrói o governante universal, e
prova a notável desigualdade que torna $\sigma $ de um
número descritivo em uma garantia.

\medskip
\noindent\textbf{Parte I --- \omterm{def:g11:stat:variance}{variância} mechanics.}
\begin{enumerate}
  \item Para o conjunto de dados $4, 8, 6, 5, 2$: calcular a \omterm{def:g11:stat:mean}{média}, então
        a \omterm{def:g11:stat:variance}{variância} direto da definição, então
        $\sigma $.
  \item Recalcular a \omterm{def:g11:stat:variance}{variância} com a fórmula de atalho
        (\cref{prop:g11:stat:shortcut}) --- \omterm{def:g11:stat:mean}{média} de quadrados
        menos quadrado de \omterm{def:g11:stat:mean}{média} --- e verifique o acordo.
  \item Com \omterm{def:g10:stats:series}{frequências}: marcas $8$, $10$, $14$ obtido por
        $3$, $5$, $2$ estudantes. Calcular \omterm{def:g11:stat:mean}{média}, \omterm{def:g11:stat:variance}{variância} e
        $\sigma $ (duas casas decimais).
  \item As temperaturas têm \omterm{def:g11:stat:mean}{média} $20$ e $\sigma = 3$ (graus
        Celsius). Dê a \omterm{def:g11:stat:mean}{média} e $\sigma $ em Fahrenheit
        ($ F = 1.8\,C + 32$; \cref{prop:g11:stat:affine}).
  \item Prove as duas regras que acabamos de usar: adicionar uma constante $ b $ para
        cada valor muda a \omterm{def:g11:stat:mean}{média} por $ b $ e folhas $\sigma $
        inalterado; multiplicando por $a$ multiplica a \omterm{def:g11:stat:mean}{média} por $a$
        e $\sigma $ por $\abs a $.
\end{enumerate}

\medskip
\noindent\textbf{Parte II --- O governante universal.}
O \emph{pontuação z}\index{pontuação z} de um valor $x$ em um conjunto de dados de
\omterm{def:g11:stat:mean}{média} $\bar x $ e \omterm{def:g11:stat:variance}{variância} $\sigma $ é
$ z = \dfrac{x - \bar x}{\sigma}$: o número de \omterm{def:g11:stat:variance}{variância} separando $x$ do \omterm{def:g11:stat:mean}{média}.
\begin{enumerate}[resume]
  \item O exame A tem \omterm{def:g11:stat:mean}{média} $12$ e $\sigma = 2$; exame B tem \omterm{def:g11:stat:mean}{média}
        $8$ e $\sigma = 4$. Alice marcou $15$ em A, Bob $13$
        em B. Calcule ambos pontuações z: cujo desempenho se destaca
        mais acima da multidão?
  \item Calcule os cinco pontuações z do conjunto de dados da pergunta 1,
        e verifique se eles têm \omterm{def:g11:stat:mean}{média} $0$ e \omterm{def:g11:stat:variance}{variância} $1$. Explique a partir da pergunta 5 por que
        padronização \emph{sempre} produz \omterm{def:g11:stat:mean}{média} $0$ e
        $\sigma = 1$.
  \item Um teste de aptidão tem \omterm{def:g11:stat:mean}{média} $100$ e $\sigma = 15$. O que
        pontuação bruta tem $ z = 2$? Qual é o \omterm{pb:g11:stat:1}{pontuação z} de $76$?
  \item Um pediatra registra o peso de um bebê como
        ``$ z = -2.5$''. Diga o que isso significa e por que o crescimento é
        rastreado em pontuações z em vez de quilogramas como a criança
        idades.
  \item O estranho de \cref{exo:g11:stat:10} (conjunto de dados
        $1, 2, 2, 3, 3, 3, 4, 4, 5, 23$): calcule a \omterm{def:g11:stat:mean}{média},
        $\sigma $, e o \omterm{pb:g11:stat:1}{pontuação z} de $23$. O alarme clássico
        limite é $\abs z \geq 3$: veredicto?
  \item Recalcular $\sigma $ \emph{sem} o valor $23$ e
        o \omterm{pb:g11:stat:1}{pontuação z} que $23$ teria contra o limpo
        dados \omterm{def:g11:stat:mean}{média} e $\sigma $. O que o outlier fez
        o próprio governante pretendia medi-lo, e o que significa
        \cref{exo:g11:stat:10} sugerir como contramedida?
\end{enumerate}

\medskip
\noindent\textbf{Parte III --- Garantia de Chebyshev.}
\begin{enumerate}[resume]
  \item Prove o Bienaymé-Desigualdade de Chebyshev para um
        conjunto de dados: se uma proporção $ p $ dos valores satisfazem
        $\abs{x - \bar x} \geq k\sigma $, então, mantendo apenas
        esses termos no \omterm{def:g11:stat:variance}{variância} soma,
        \[
        \sigma^2 \geq p \cdot (k\sigma)^2,
        \qquad\text{hence}\qquad
        p \leq \frac{1}{k^2} .
        \]
  \item O que a desigualdade garante sobre a proporção
        de qualquer conjunto de dados além $2\sigma $ do seu \omterm{def:g11:stat:mean}{média}? Além
        $3\sigma $? Verifique o $2\sigma $ declaração sobre o conjunto de dados
        da questão 1.
  \item Uma fábrica produz parafusos com \omterm{def:g11:stat:mean}{média} $50$ milímetros e
        $\sigma = 0.1$ mm. O que Chebyshev garante sobre
        a proporção de parafusos fora
        $\intcc{49.7}{50.3}$? (A produção real é muito melhor
        --- a curva do sino da 12ª série torna isso cauteloso
        $\frac19$ em uma fração de um por cento.)
  \item Dois fundos retornam $5\,\%$ em \omterm{def:g11:stat:mean}{média} por ano, com
        $\sigma = 2\,\%$ para o fundo A e $\sigma = 12\,\%$ para
        fundo B. Para cada fundo, o que Chebyshev diz sobre
        a proporção probabilística de anos com retornos
        abaixo $-19\,\%$? O que é $\sigma $ nome financeiro?
\end{enumerate}

\medskip
\noindent\textbf{Parte IV --- O \omterm{def:g11:stat:mean}{média}'s medal, and a warning.}
\begin{enumerate}[resume]
  \item Seja $f(c)$ seja a \omterm{def:g11:stat:mean}{média} dos desvios quadrados
        $(x_i - c)^2$ de um centro arbitrário $ c $. Prove o
        identidade
        \[
        f(c) = \sigma^2 + (\bar x - c)^2 ,
        \]
        e concluo: a \omterm{def:g11:stat:mean}{média} é o único minimizador de \omterm{def:g11:stat:mean}{média}
        desvio quadrado --- sua medalha, correspondendo ao medianade
        minimização de desvios absolutos em
        \cref{pb:g10:stats:1}.
  \item Verifique a identidade no conjunto de dados da pergunta 1 com
        $ c = 6$: calcular $ f(6)$ diretamente e por meio da fórmula.
  \item Mesmos resumos, dados diferentes: verifique isso
        $ P = \{3, 5, 7, 9, 11\}$ e $ Q = \{1, 7, 7, 7, 13\}$
        compartilhe o mesmo \omterm{def:g11:stat:mean}{média} mas não é o mesmo \omterm{def:g11:stat:variance}{variância}; então
        construir um conjunto de dados do formulário
        $\{7 - a, 7, 7, 7, 7 + a\}$ exatamente com a \omterm{def:g11:stat:mean}{média} e
        \omterm{def:g11:stat:variance}{variância} de $ P $ --- ainda com uma forma própria.
  \item Prove isso $\sigma = 0$ força todos os valores iguais. (O que
        deve ser todo termo de uma soma zero de quadrados?)
  \item Finale --- a escada do estatístico, uma linha por
        degrau: centros (\omterm{def:g11:stat:mean}{média}, \omterm{def:g11:stat:median}{mediana}), spreads ($\sigma $, AIQ),
        o governante universal ($ z$), a garantia (Chebyshev),
        e a advertência da questão 18: resumos compactados,
        e a compressão perde --- com a curva em forma de sino do grau
        12 esperando para virar pontuações z em \omterm{def:g10:proba:distribution}{probabilidades} exatas.
\end{enumerate}
\end{problem}
