\chapter{Calcul différentiel}\label{ch:b2:diffcalc}

Le calcul à deux variables du volume de Licence~1 mûrit en le calcul
différentiel des applications entre espaces normés~: la
\emph{\omterm{def:b2:diffcalc:differential}{différentielle}} comme meilleure approximation linéaire, la règle
de la chaîne en toute généralité, le théorème de symétrie de Schwarz
\emph{démontré}, les formules de Taylor, et l'analyse complète du
second \omterm{def:b2:structures:generated}{ordre} des extrema. Le théorème d'inversion locale, couronnement
de la théorie, est énoncé avec sa stratégie de démonstration --- un
point fixe de Banach --- rendue explicite.

Dans tout ce chapitre, $U$ est un \omterm{def:b2:metric:topology}{ouvert} de $\R^n$ (ou d'un espace
normé~; le cas de dimension finie porte toutes les idées), $f \colon U
\to \R^m$.

\section{La différentielle}

\begin{definition}\label{def:b2:diffcalc:differential}
$f$ est \emph{différentiable}\index{différentiable} en $a$ lorsqu'il
existe une application linéaire (\omterm{def:b2:metric:continuity}{continue}) $\dd f_a \colon \R^n \to
\R^m$ telle que
\[
f(a + h) = f(a) + \dd f_a(h) + o(\norm h)
\qquad (h \to 0).
\]
L'application $\dd f_a$, la \emph{différentielle}\index{différentielle}
de $f$ en $a$, est unique~; sa matrice dans les bases canoniques est
la \emph{matrice jacobienne}\index{matrice jacobienne} $J_f(a) =
\bigl(\frac{\partial f_i}{\partial x_j}(a)\bigr)$.
La différentiabilité entraîne la continuité et l'existence de toutes
les dérivées directionnelles $\dd f_a(v) = \lim_{t\to0}\frac{f(a + tv)
- f(a)}{t}$~; la réciproque est fausse
(\cref{exo:b2:diffcalc:2}). Pour $m = 1$, $\dd f_a(h) = \langle
\nabla f(a), h\rangle$~: le gradient de Licence~1, désormais compris
comme le vecteur représentant la différentielle.
\end{definition}

\begin{theorem}[Critère $C^1$]\label{thm:b2:diffcalc:c1}
Si toutes les dérivées partielles de $f$ existent sur $U$ et sont
\omterm{def:b2:metric:continuity}{continues} en $a$, alors $f$ est \omterm{def:b2:diffcalc:differential}{différentiable} en $a$. «~$C^1$ sur
$U$~» --- dérivées partielles \omterm{def:b2:metric:continuity}{continues} --- entraîne donc la
différentiabilité partout, avec \omterm{def:b2:diffcalc:differential}{différentielle} \omterm{def:b2:metric:continuity}{continue}.
\end{theorem}

\begin{proof}
Composante par composante ($m = 1$ suffit). La démonstration de
Licence~1 pour deux variables --- déplacer une coordonnée à la fois,
appliquer le théorème des accroissements finis à une variable sur
chaque segment, et utiliser la continuité des dérivées partielles en
$a$ --- se généralise mot pour mot à $n$ segments~:
\[
f(a + h) - f(a) = \sum_{j=1}^{n} \bigl(f(a + h^{(j)}) - f(a +
h^{(j-1)})\bigr)
= \sum_j h_j\,\frac{\partial f}{\partial x_j}(\xi_j),
\]
où $h^{(j)}$ fige les $j$ premières coordonnées de $h$ et
$\xi_j$ appartient au $j$-ème segment~; la continuité transforme chaque
$\frac{\partial f}{\partial x_j}(\xi_j)$ en $\frac{\partial
f}{\partial x_j}(a) + o(1)$, et l'erreur est un $o(\norm h)$.
\end{proof}

\begin{theorem}[Règle de la chaîne]\label{thm:b2:diffcalc:chainrule}
Si $f$ est \omterm{def:b2:diffcalc:differential}{différentiable} en $a$ et $g$ en $f(a)$, alors $g \circ f$
est \omterm{def:b2:diffcalc:differential}{différentiable} en $a$ avec
\[
\dd(g \circ f)_a = \dd g_{f(a)} \circ \dd f_a ,
\qquad
J_{g\circ f}(a) = J_g\bigl(f(a)\bigr)\,J_f(a) :
\]
les jacobiennes se multiplient.\index{règle de la chaîne}
\end{theorem}

\begin{proof}
Écrivons $f(a + h) = f(a) + \dd f_a(h) + \norm h\,\varepsilon_1(h)$
et $g(b + k) = g(b) + \dd g_b(k) + \norm k\,\varepsilon_2(k)$ avec
$b = f(a)$, $k = k(h) = \dd f_a(h) + \norm h \varepsilon_1(h)$.
En substituant,
\[
g(f(a+h)) = g(b) + \dd g_b\bigl(\dd f_a(h)\bigr)
+ \norm h\,\dd g_b(\varepsilon_1(h)) + \norm{k}\,\varepsilon_2(k),
\]
et les deux termes d'erreur sont des $o(\norm h)$~: le premier car
$\dd g_b$ est \omterm{def:b2:metric:continuity}{continue} et $\varepsilon_1 \to 0$~; le second car
$\norm k \leq C\norm h$ (application linéaire bornée plus terme petit)
et $\varepsilon_2(k) \to 0$ quand $h \to 0$.
\end{proof}

\begin{example}[Fonctions radiales, une fois pour toutes]\label{ex:b2:diffcalc:radial}
Soit $r(x) = \norm x_2$ sur $\R^n\setminus\{0\}$ et $f = g \circ
r$ avec $g$ une fonction $C^1$ d'une variable. D'abord, $r$ est
\omterm{def:b2:diffcalc:differential}{différentiable} en dehors de $0$~: de $r^2 = \sum x_i^2$,
\[
\frac{\partial r}{\partial x_i} = \frac{x_i}{r},
\qquad\text{c.-à-d.}\qquad
\nabla r(x) = \frac{x}{\norm x} ,
\]
le vecteur radial \omterm{def:b2:hermitian:adjoint}{unitaire} (dériver $r^2$ et diviser --- ou
appliquer la règle de la chaîne à $\sqrt{\cdot}$). Alors la règle de
la chaîne donne, pour toute fonction radiale,
\[
\nabla f(x) = g'\bigl(\norm x\bigr)\,\frac{x}{\norm x} .
\]
Cas travaillé~: $g(r) = \frac1r$ donne $\nabla\frac{1}{\norm
x} = -\frac{x}{\norm x^3}$, le champ en carré inverse de la
gravitation et de l'électrostatique --- de direction radiale, de
\omterm{def:b2:nvs:norm}{norme} $\frac{1}{\norm x^2}$. Idée à retenir~: les gradients des
fonctions radiales sont radiaux parce que les ensembles de niveau
sont des sphères et que le gradient est orthogonal aux ensembles de
niveau~; en $x = 0$, en revanche, $r$ n'est \emph{pas}
\omterm{def:b2:diffcalc:differential}{différentiable} (aucune application linéaire candidate ne correspond
à $\norm h$ dans toutes les directions) --- les profils radiaux
lisses ont besoin de $g'(0) = 0$ pour franchir l'origine avec grâce.
\end{example}

\begin{theorem}[Inégalité des accroissements finis]\label{thm:b2:diffcalc:mvi}
Soit $f$ \omterm{def:b2:diffcalc:differential}{différentiable} sur $U$ et supposons que le segment
$\intcc{a}{b} = \{a + t(b-a)\}$ est inclus dans $U$. Alors
\[
\norm{f(b) - f(a)} \leq \norm{b - a}\,
\sup_{x \in \intcc{a}{b}} \vertiii{\dd f_x} .
\]
En particulier, une application \omterm{def:b2:diffcalc:differential}{différentiable} de \omterm{def:b2:diffcalc:differential}{différentielle}
nulle sur un \omterm{def:b2:metric:topology}{ouvert} \emph{\omterm{def:b2:metric:connected}{connexe}} est constante.
\end{theorem}

\begin{proof}
La fonction $\varphi(t) = f(a + t(b-a))$ est \omterm{def:b2:diffcalc:differential}{différentiable} sur
$\intcc{0}{1}$ avec $\varphi'(t) = \dd f_{a + t(b-a)}(b - a)$
(règle de la chaîne), de \omterm{def:b2:nvs:norm}{norme} $\leq M\norm{b-a}$ où $M$ est le sup
affiché. Pour $f$ à valeurs dans $\R$, l'inégalité des accroissements
finis à une variable conclut~; pour des valeurs vectorielles,
l'appliquer à $t \mapsto \langle u, \varphi(t)\rangle$ avec $u$ le
vecteur \omterm{def:b2:hermitian:adjoint}{unitaire} dirigé selon $f(b) - f(a)$. Constance~: localement
constante (segments dans les boules) plus connexité (l'ensemble où
$f$ vaut une valeur donnée est à la fois \omterm{def:b2:metric:topology}{ouvert} et fermé~:
\cref{ch:b2:metric}).
\end{proof}

\begin{example}[Une constante de Lipschitz issue de l'inégalité des accroissements finis]\label{ex:b2:diffcalc:lipschitz}
La fonction $f(x, y) = \sin x\,\sin y$ est-elle \omterm{def:b2:metric:continuity}{lipschitzienne} sur
$\R^2$, et avec quelle constante~? Son gradient est $\nabla f =
(\cos x\sin y,\ \sin x\cos y)$, de \omterm{def:b2:nvs:norm}{norme} au carré
\[
\cos^2x\sin^2y + \sin^2x\cos^2y
\leq \sin^2 y + \cos^2y\cdot 1 = 1
\]
(majorer $\cos^2x$ et $\sin^2x$ par $1$ séparément), donc
$\vertiii{\dd f_{(x,y)}} = \norm{\nabla f} \leq 1$ partout,
et le \cref{thm:b2:diffcalc:mvi} appliqué au segment entre deux
points quelconques donne
\[
\abs{f(b) - f(a)} \leq \norm{b - a}_2 :
\]
$f$ est $1$-lipschitzienne, et la constante est optimale (près de
l'origine, $f(x, \tfrac\pi2) = \sin x$ a une pente $1$). Idée à
retenir~: l'inégalité des accroissements finis convertit une
majoration ponctuelle de la \omterm{def:b2:diffcalc:differential}{différentielle} en un module de
continuité global --- la voie standard vers les estimations de
Lipschitz en toute dimension, et le moteur de
l'\cref{exo:b2:diffcalc:12}.
\end{example}

\section{Dérivées secondes}

\begin{theorem}[Schwarz]\label{thm:b2:diffcalc:schwarz}
Si $f$ est $C^2$ sur $U$ (toutes les dérivées partielles secondes
existent et sont \omterm{def:b2:metric:continuity}{continues}), alors pour tous $i, j$~:\index{théorème de Schwarz}
\[
\frac{\partial^2 f}{\partial x_i\,\partial x_j}
= \frac{\partial^2 f}{\partial x_j\,\partial x_i} .
\]
\end{theorem}

\begin{proof}
Deux variables suffisent ($x = x_i$, $y = x_j$, les autres figées).
Considérons la différence seconde
\[
\Delta(h) = f(a + h, b + h) - f(a + h, b) - f(a, b + h) + f(a,b) .
\]
Fixons $h$ et posons $\varphi(x) = f(x, b+h) - f(x, b)$~: alors
$\Delta(h) = \varphi(a + h) - \varphi(a)$, et deux applications du
théorème des accroissements finis donnent
\[
\Delta(h) = h\,\varphi'(\xi)
= h\Bigl(\frac{\partial f}{\partial x}(\xi, b+h) -
\frac{\partial f}{\partial x}(\xi, b)\Bigr)
= h^2\,\frac{\partial^2 f}{\partial y\,\partial x}(\xi, \eta),
\]
avec $\xi \in \intoo{a}{a+h}$, $\eta \in \intoo{b}{b+h}$. Par
continuité, $\frac{\Delta(h)}{h^2} \to \frac{\partial^2 f}{\partial
y\partial x}(a, b)$ quand $h \to 0$. Le même calcul en échangeant les
rôles des variables (figer d'abord la seconde variable) donne
$\frac{\Delta(h)}{h^2} \to \frac{\partial^2 f}{\partial x
\partial y}(a,b)$~: les deux limites de la même quantité coïncident.
\end{proof}

\begin{example}[Pourquoi $C^2$ est nécessaire~: le contre-exemple de Peano]\label{ex:b2:diffcalc:peano}
Soit $f(x, y) = \dfrac{xy(x^2 - y^2)}{x^2 + y^2}$, $f(0,0) = 0$.
En dehors de l'origine $f$ est $C^\infty$~; à l'origine toutes les
dérivées partielles premières et secondes existent, mais les dérivées
croisées diffèrent. Calculons le long des axes~: $f(x, 0) = f(0, y) =
0$, et pour $y \neq 0$,
\[
\frac{\partial f}{\partial x}(0, y)
= \lim_{x\to0}\frac{f(x,y)}{x}
= \frac{y(0 - y^2)}{y^2} = -y ,
\qquad\text{symétriquement}\qquad
\frac{\partial f}{\partial y}(x, 0) = x .
\]
D'où
\[
\frac{\partial^2 f}{\partial y\,\partial x}(0,0)
= \frac{\dd}{\dd y}\Bigl[\frac{\partial f}{\partial
x}(0,y)\Bigr]_{y=0} = -1,
\qquad
\frac{\partial^2 f}{\partial x\,\partial y}(0,0) = +1 :
\]
les deux dérivées croisées existent et diffèrent. Aucune
contradiction avec le \cref{thm:b2:diffcalc:schwarz}~: les dérivées
partielles secondes de $f$ ne sont pas \omterm{def:b2:metric:continuity}{continues} en $0$ (tester le
long de $y = tx$). Idée à retenir~: le théorème de Schwarz est un
vrai théorème de \emph{continuité}, non une identité formelle --- et
l'hypothèse «~$C^2$~» dans le Taylor--Young ci-dessous fait un
travail réel.
\end{example}

\begin{theorem}[Taylor--Young à l'ordre 2]\label{thm:b2:diffcalc:taylor}
Soit $f \colon U \to \R$ de classe $C^2$ et $a \in U$. Alors, quand
$h \to 0$,
\[
f(a + h) = f(a) + \langle\nabla f(a), h\rangle
+ \frac12\, \langle H_a h,\, h\rangle + o\bigl(\norm h^2\bigr),
\]
où $H_a = \bigl(\frac{\partial^2 f}{\partial x_i\partial
x_j}(a)\bigr)$ est la \emph{matrice hessienne}\index{matrice hessienne}
(\omterm{def:b2:quadratic:adjoint}{symétrique}, d'après Schwarz).
\end{theorem}

\begin{proof}
Appliquons le théorème de Taylor--Young à une variable (volume de
Licence~1) à $\varphi(t) = f(a + th)$ sur $\intcc{0}{1}$~: par la
règle de la chaîne, $\varphi'(t) = \langle \nabla f(a + th),
h\rangle$ et $\varphi''(t) = \langle H_{a+th}h, h\rangle$, toutes
deux \omterm{def:b2:metric:continuity}{continues} en $t$. Alors $\varphi(1) = \varphi(0) + \varphi'(0)
+ \frac12\varphi''(\theta)$ (Taylor--Lagrange) avec $\theta \in
\intoo{0}{1}$, et la continuité des dérivées partielles secondes
transforme $\varphi''(\theta) = \varphi''(0) + o(1)\cdot\norm h^2$
de manière uniforme~: le développement affiché.
\end{proof}

\begin{example}[Un développement de deux façons]\label{ex:b2:diffcalc:taylortwoways}
Développons $f(x, y) = \eu^x\cos y$ à l'origine à l'\omterm{def:b2:structures:generated}{ordre} $2$.
\emph{Par composition de développements à une variable~:}
\[
\eu^x\cos y = \Bigl(1 + x + \frac{x^2}{2} +
o(x^2)\Bigr)\Bigl(1 - \frac{y^2}{2} + o(y^2)\Bigr)
= 1 + x + \frac{x^2 - y^2}{2} + o\bigl(\norm{(x,y)}^2\bigr) .
\]
\emph{Par dérivées partielles~:} $f_x = \eu^x\cos y$, $f_y =
-\eu^x\sin y$, donc $\nabla f(0) = (1, 0)$~; et $f_{xx} = f$,
$f_{yy} = -f$, $f_{xy} = -\eu^x\sin y$ donnent $H_0 =
\operatorname{diag}(1, -1)$~: le \cref{thm:b2:diffcalc:taylor}
reproduit $1 + x + \frac12(x^2 - y^2)$. Les deux calculs concordent,
et la voie par composition était plus rapide --- aucune dérivée
partielle seconde. Idée à retenir~: l'origine n'est \emph{pas} un
point critique ($\nabla f \neq 0$), donc malgré la hessienne
indéfinie il n'y a pas de point col à déclarer~: le terme linéaire
domine, et le test du second \omterm{def:b2:structures:generated}{ordre} ne parle jamais qu'aux points
critiques.
\end{example}

\begin{theorem}[Test d'extremum du second ordre, démontré]\label{thm:b2:diffcalc:extrema}
Soit $f$ de classe $C^2$ au voisinage d'un point critique $a$
($\nabla f(a) = 0$), de hessienne $H = H_a$.
\begin{enumerate}
  \item Si $H$ est définie positive, $a$ est un minimum local strict
        (définie négative~: maximum).
  \item Si $H$ a des \omterm{def:b2:reduction:eigen}{valeurs propres} des deux signes, $a$ est un
        point col~: pas d'extremum.
  \item Si $H$ est singulière (et semi-définie), aucune conclusion.
\end{enumerate}
Le test «~$rt - s^2$~» de Licence~1 est le cas $n = 2$~: $\det H = rt
- s^2$, le signe de $\operatorname{tr}$ se lit sur $r$.
\end{theorem}

\begin{proof}
D'après le théorème spectral (\cref{thm:b2:quadratic:spectral}), la
\omterm{def:b2:quadratic:def}{forme quadratique} de $H$ est encadrée par ses \omterm{def:b2:reduction:eigen}{valeurs propres}
extrêmes~: $\lambda_{\min}\norm h^2 \leq \langle Hh, h\rangle \leq
\lambda_{\max}\norm h^2$.

(1) Si $\lambda_{\min} > 0$~: Taylor--Young donne
\[
f(a + h) - f(a) \geq \frac{\lambda_{\min}}{2}\norm h^2 -
o(\norm h^2) > 0
\]
pour $h \neq 0$ petit~: minimum local strict.

(2) Le long d'un vecteur propre $v_+$ avec $\lambda_+ > 0$~: $f(a +
tv_+) - f(a) = \frac{\lambda_+}{2}t^2 + o(t^2) > 0$ pour $t$ petit~;
le long de $v_-$ avec $\lambda_- < 0$ la différence est négative~:
les deux signes apparaissent dans tout voisinage.

(3) $f(x,y) = x^2 + y^4$, $x^2 - y^4$, $x^2 + y^3$ partagent la même
hessienne singulière semi-définie en $0$ avec trois comportements
différents.
\end{proof}

\begin{example}[Une classification complète, le global compris]\label{ex:b2:diffcalc:quarticrun}
Classifions tous les extrema de $f(x, y) = x^4 + y^4 - 4xy$ sur
$\R^2$. \emph{Points critiques~:} $\nabla f = (4x^3 - 4y,\ 4y^3 -
4x) = 0$ donne $y = x^3$ et $x = y^3 = x^9$, donc $x(x^8 - 1) = 0$~:
les solutions réelles sont $(0,0)$, $(1,1)$, $(-1,-1)$.
\emph{Hessiennes~:} $H = \begin{pmatrix} 12x^2 & -4\\ -4 &
12y^2\end{pmatrix}$. En $(\pm1, \pm1)$~: $\begin{pmatrix} 12 &
-4\\ -4 & 12\end{pmatrix}$, \omterm{def:b2:reduction:eigen}{valeurs propres} $8$ et $16$~: définie
positive, minima locaux stricts avec $f = -2$. En $(0,0)$~:
$\begin{pmatrix} 0 & -4\\ -4 & 0\end{pmatrix}$, \omterm{def:b2:reduction:eigen}{valeurs propres}
$\pm4$~: un point col. \emph{Globalité~:} de $2\abs{xy} \leq x^2 +
y^2$,
\[
f(x, y) \geq x^4 + y^4 - 2(x^2 + y^2)
= (x^2 - 1)^2 + (y^2 - 1)^2 + x^2 + y^2 - 2
\xrightarrow[\norm{(x,y)}\to\infty]{} +\infty :
\]
$f$ est coercive, donc elle atteint un minimum global (compacité des
ensembles de sous-niveau), nécessairement en un point critique~: la
valeur $-2$, atteinte en $(1,1)$ et $(-1,-1)$, est le minimum
global~; il n'y a pas de maximum ($f$ non majorée). Idée à retenir~:
le test local classifie les candidats, mais seul un argument de
croissance transforme le «~local~» en «~global~» --- le schéma en
deux temps de toute démonstration d'optimisation de ce livre.
\end{example}

\begin{method}[Classifier les extrema de $f \colon \R^n \to \R$]\label{met:b2:diffcalc:extrema}
\begin{enumerate}
  \item Résoudre $\nabla f = 0$ (tous les points critiques~; sur un
        domaine à bord, traiter le bord séparément comme dans
        l'\cref{exo:b2:diffcalc:7}).
  \item En chaque point critique, calculer la hessienne et les signes
        de ses \omterm{def:b2:reduction:eigen}{valeurs propres} --- en dimension $2$, juste $\det H$
        et $\operatorname{tr} H$~: $\det < 0$ point col~; $\det >
        0$ extremum, du type donné par le signe de la trace~; $\det =
        0$~: le test est muet, étudier $f$ le long de courbes.
  \item Pour des énoncés globaux, ajouter un argument de compacité ou
        de coercivité ($f \to +\infty$ à l'infini, ou un ensemble de
        contraintes \omterm{def:b2:metric:compact}{compact}), puis comparer les valeurs critiques.
\end{enumerate}
\end{method}

\section{Le théorème d'inversion locale}

\begin{theorem}[Théorème d'inversion locale]\label{thm:b2:diffcalc:inverse}
Soit $f \colon U \to \R^n$ de classe $C^1$ et $a \in U$ avec $\dd f_a$
\emph{inversible}. Alors il existe des voisinages \omterm{def:b2:metric:topology}{ouverts} $V \ni a$,
$W \ni f(a)$ tels que $f \colon V \to W$ est une bijection de
réciproque $C^1$, et\index{théorème d'inversion locale}
\[
\dd (f^{-1})_{f(x)} = (\dd f_x)^{-1} \qquad (x \in V).
\]
\end{theorem}
\admitted

\begin{remark}[Pourquoi c'est vrai~: la stratégie du point fixe]
Résoudre $f(x) = y$ près de $a$ se réécrit comme l'équation de point
fixe $x = x + \dd f_a^{-1}\bigl(y - f(x)\bigr) =: \Phi_y(x)$~; l'application
$\Phi_y$ a pour \omterm{def:b2:diffcalc:differential}{différentielle} $\mathrm{id} - \dd f_a^{-1}\dd f_x$,
petite près de $a$ par continuité de $\dd f$, donc $\Phi_y$ est
contractante sur une petite boule fermée et le théorème du point
fixe de Banach (\cref{thm:b2:metric:banach}) fournit l'unique
solution locale $x = f^{-1}(y)$. La continuité et la différentiabilité
de la réciproque découlent ensuite des estimations de la contraction.
Le décompte \omterm{def:b2:metric:complete}{complet} est mené en Licence~3~; la stratégie --- et
l'énoncé --- sont utilisés librement désormais. Le \emph{théorème des
fonctions implicites} associé (résoudre $F(x, y) = 0$ en $y(x)$
lorsque $\frac{\partial F}{\partial y}$ est inversible) s'en déduit
en appliquant le théorème à $(x, y) \mapsto (x, F(x,y))$.
\end{remark}

\begin{example}[Coordonnées polaires]\label{ex:b2:diffcalc:polar}
$\Phi(r, \theta) = (r\cos\theta, r\sin\theta)$ a pour jacobienne
\[
J_\Phi = \begin{pmatrix} \cos\theta & -r\sin\theta\\ \sin\theta &
r\cos\theta \end{pmatrix},
\qquad \det J_\Phi = r :
\]
inversible pour $r \neq 0$, donc $\Phi$ est un difféomorphisme local
$C^1$ en dehors de l'origine --- le permis de «~passer en
coordonnées polaires~», renouvelé pour les intégrales multiples du
\cref{ch:b2:multint}.
\end{example}

\begin{example}[Local partout, global nulle part]\label{ex:b2:diffcalc:localnotglobal}
Soit $f(x, y) = \bigl(\eu^x\cos y,\ \eu^x\sin y\bigr)$ sur
$\R^2$. Sa jacobienne,
\[
J_f = \begin{pmatrix}
\eu^x\cos y & -\eu^x\sin y\\
\eu^x\sin y & \eu^x\cos y
\end{pmatrix},
\qquad
\det J_f = \eu^{2x} > 0 ,
\]
ne s'annule jamais~: d'après le \cref{thm:b2:diffcalc:inverse}, $f$
est un difféomorphisme local $C^1$ en \emph{tout} point du plan.
Pourtant $f$ est loin d'être injective~: $f(x, y + 2\pi) = f(x, y)$,
donc toute valeur est prise une infinité de fois~; et elle n'est pas
non plus surjective, puisque $\norm{f(x, y)} = \eu^x > 0$ manque
l'origine. Idée à retenir~: le théorème d'inversion locale est
irréductiblement \emph{local} --- l'inversibilité de chaque $\dd f_a$
fournit un patchwork de réciproques locales qui n'ont pas à
s'assembler en une seule. (Les lecteurs qui connaissent les nombres
complexes reconnaîtront $z \mapsto \eu^z$~; le patchwork est la
famille des branches du logarithme.) Comparer avec
l'\cref{exo:b2:diffcalc:12}, où une hypothèse globale quantitative
force bel et bien une réciproque globale unique.
\end{example}

\begin{remark}[Pièges classiques]
\emph{(i) Les dérivées directionnelles sont bon marché, pas les
\omterm{def:b2:diffcalc:differential}{différentielles}~:} toutes les dérivées directionnelles peuvent
exister --- et même ne pas dépendre linéairement de la direction ---
sans différentiabilité (\cref{exo:b2:diffcalc:2})~; seul le critère
$C^1$ (\cref{thm:b2:diffcalc:c1}) promeut les dérivées partielles en
une \omterm{def:b2:diffcalc:differential}{différentielle}. \emph{(ii) Critique ne signifie pas extrémal~:}
les points cols (\cref{ex:b2:diffcalc:quarticrun}) et le cas
singulier muet (\cref{thm:b2:diffcalc:extrema}~(3)) se cachent tous
deux derrière $\nabla f = 0$. \emph{(iii) Pas d'égalité des
accroissements finis à valeurs vectorielles~:} seule l'\emph{inégalité}
du \cref{thm:b2:diffcalc:mvi} subsiste
(\cref{exo:b2:diffcalc:9})~; ne jamais écrire $f(b) - f(a) = \dd
f_c(b-a)$ pour $f$ à valeurs dans $\R^m$, $m \geq 2$.
\emph{(iv) L'inversibilité locale n'est pas l'injectivité~:}
\cref{ex:b2:diffcalc:localnotglobal}. \emph{(v) Le gradient
appartient au produit scalaire~:} $\nabla f$ est le vecteur
représentant $\dd f_a$ dans un produit scalaire choisi~; changer le
produit (comme dans l'exemple pondéré du chapitre sur les \omterm{def:b2:quadratic:def}{formes
quadratiques}) et le gradient tourne, tandis que la \omterm{def:b2:diffcalc:differential}{différentielle} ---
l'objet intrinsèque --- ne bouge pas.
\end{remark}

\begin{remark}[Où cela sert]
Tout ce qui suit ce chapitre est du calcul différentiel appliqué~: le
chapitre sur les équations \omterm{def:b2:diffcalc:differential}{différentielles} linéarise les flots et
utilise la formule du \omterm{def:b2:linalg:det}{déterminant} de Liouville (démontrée dans le
problème du week-end de ce chapitre)~; les chapitres sur les courbes
et les surfaces étudient les ensembles de niveau et les paramétrages
via le théorème des fonctions implicites~; les intégrales multiples
changent de variables via les jacobiennes. Le problème du week-end
développe le calcul \emph{sur l'espace des matrices lui-même} ---
\omterm{def:b2:diffcalc:differential}{différentielle} du \omterm{def:b2:linalg:det}{déterminant}, de l'inverse, l'\omterm{ex:b2:nvs:matrixexp}{exponentielle de
matrice}, et le groupe orthogonal comme ensemble de niveau lisse ---
l'ombre en Licence~2 de ce que le volume de Licence~3 formalise sous
le nom de variétés et de groupes de Lie.
\end{remark}

\section{Exercices}

\begin{exercise}[$\star$]\label{exo:b2:diffcalc:1}
Calculer les \omterm{def:b2:diffcalc:differential}{matrices jacobiennes} de $f(x,y) = (x^2 - y^2,\, 2xy)$ et
de $\Phi(r,\theta,z) = (r\cos\theta, r\sin\theta, z)$~; où les
\omterm{def:b2:diffcalc:differential}{différentielles} sont-elles inversibles~?
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b2:diffcalc:2}
Soit $f(x,y) = \frac{x^3}{x^2 + y^2}$ ($f(0,0) = 0$). Démontrer que
toutes les dérivées directionnelles de $f$ en $0$ existent, mais que
$f$ n'est pas \omterm{def:b2:diffcalc:differential}{différentiable} en $0$ \emph{(l'application $v \mapsto$
dérivée directionnelle n'est pas linéaire)}.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b2:diffcalc:3}
Trouver et classifier les points critiques de $f(x, y) = x^3 + y^3 -
3xy$ à l'aide du \cref{thm:b2:diffcalc:extrema}, et de $g(x,y) = x^4 +
y^4 - 2(x - y)^2$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:diffcalc:4}
Soit $f \colon \R^n \to \R$ de classe $C^1$ et \emph{homogène de degré
$p$}~: $f(tx) = t^pf(x)$ pour $t > 0$. Démontrer l'identité d'Euler
\[
\langle \nabla f(x), x\rangle = p\,f(x) ,
\]
et sa réciproque pour les fonctions $C^1$ sur $\R^n\setminus\{0\}$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:diffcalc:5}
Soit $A$ \omterm{def:b2:quadratic:adjoint}{symétrique} et $f(x) = \frac12\langle Ax, x\rangle -
\langle b, x\rangle$. Calculer $\nabla f$ et $H_f$~; quand $f$ est-elle
convexe~? En supposant $A$ définie positive, montrer que $f$ a un
unique minimum global au point solution de $Ax = b$ --- la raison
d'être de la descente de gradient.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:diffcalc:6}
(Multiplicateur de Lagrange, une contrainte, démontré à la main) Soit
$f, g$ de classe $C^1$ sur $\R^2$, et supposons que $f$ atteint, en
$a$, un extremum local sur l'ensemble de niveau $\{g = 0\}$, avec
$\nabla g(a) \neq 0$. Démontrer que $\nabla f(a) = \lambda\nabla g(a)$
pour un certain $\lambda$. \emph{(Paramétrer l'ensemble de niveau près
de $a$ par le théorème des fonctions implicites et dériver $t \mapsto
f(\gamma(t))$.)} Application~: extrema de $f(x,y) = xy$ sur le cercle
$x^2 + y^2 = 1$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:diffcalc:7}
Déterminer les extrema de $f(x, y) = x^2 + y^2 - xy + x - y$ sur
$\R^2$, puis son maximum et son minimum sur le triangle fermé de
sommets $(0,0)$, $(1,0)$, $(0,1)$ \emph{(points critiques intérieurs,
puis les trois arêtes, puis les sommets)}.
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b2:diffcalc:8}
Soit $f \colon \R^2 \to \R^2$, $f(x, y) = (x + y^2,\; y + x^2)$.
Montrer que $f$ est un difféomorphisme local près de $0$, calculer
$\dd(f^{-1})_{(0,0)}$, et trouver le plus grand $r$ tel que $\dd f$
soit inversible sur la boule $\norm{(x,y)}_2 < r$ \emph{(calculer
$\det J_f$)}.
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b2:diffcalc:9}
(Rolle échoue, les accroissements finis survivent) Donner $f \colon
\R \to \R^2$, $C^1$, avec $f(0) = f(2\pi)$ mais $f'(t) \neq 0$ pour
tout $t$ (pas de Rolle à valeurs vectorielles). Puis vérifier sur
votre exemple l'\emph{inégalité} des accroissements finis du
\cref{thm:b2:diffcalc:mvi}.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b2:diffcalc:10}
Calculer la \omterm{def:b2:diffcalc:differential}{différentielle} et le gradient de $f(x) = \norm
x_2^2$ et de $g(x) = \langle Ax, x\rangle$ sur $\R^n$ ($A$ une
matrice carrée, non supposée \omterm{def:b2:quadratic:adjoint}{symétrique}), et la hessienne de chacune.
Pour quels $A$ la fonction $g$ est-elle convexe~?
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:diffcalc:11}
Soit $f \colon \R^n \to \R$ de classe $C^2$. Démontrer que $f$ est
convexe si et seulement si sa hessienne $H_x$ est semi-définie
positive en tout $x$ \emph{(se ramener à une variable~: $t \mapsto
f(a + t(b-a))$~; utiliser Taylor--Lagrange dans une direction, et
pour la réciproque évaluer $\varphi''$)}.
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b2:diffcalc:12}
(Un théorème d'inversion globale) Soit $g \colon \R^n \to \R^n$ de
classe $C^1$ avec $\vertiii{\dd g_x} \leq k < 1$ pour tout $x$, et $f =
\mathrm{id} + g$.
\begin{enumerate}
  \item Montrer $\norm{f(x) - f(y)} \geq (1 - k)\norm{x - y}$~: $f$
        est injective, de réciproque \omterm{def:b2:metric:continuity}{continue} sur son image.
  \item Montrer que pour chaque $y \in \R^n$ l'application $x \mapsto
        y - g(x)$ est une contraction de l'espace \omterm{def:b2:metric:complete}{complet} $\R^n$,
        et conclure par le théorème du point fixe de Banach
        (\cref{thm:b2:metric:banach}) que $f$ est surjective.
  \item Conclure que $f$ est une bijection de $\R^n$ de réciproque
        $(1-k)^{-1}$-lipschitzienne --- un pendant \emph{global} du
        \cref{thm:b2:diffcalc:inverse} (qui, par contraste, est
        purement local).
\end{enumerate}
\end{exercise}

\section{Problème~: Le calcul des matrices --- Jacobi,
exponentielle, et le groupe orthogonal}

\begin{problem}\label{pb:b2:diffcalc:1}
Le terrain de jeu le plus propre du calcul différentiel est l'espace
$\mathcal M_n(\R) \simeq \R^{n^2}$ lui-même~: ses applications les
plus naturelles --- produit, inverse, \omterm{def:b2:linalg:det}{déterminant}, exponentielle ---
ont des \omterm{def:b2:diffcalc:differential}{différentielles} d'une élégance frappante. Ce problème les
calcule toutes~: la \emph{série de Neumann}, la \omterm{def:b2:diffcalc:differential}{différentielle} de
l'inverse, la \emph{formule de Jacobi}\index{formule de Jacobi} pour
le \omterm{def:b2:linalg:det}{déterminant} avec la \emph{formule de Liouville} en dividende,
l'\emph{exponentielle de matrice}\index{exponentielle de matrice}
avec $\det\eu^A = \eu^{\operatorname{tr}A}$, et enfin le groupe
orthogonal $O_n$ comme ensemble de niveau lisse avec les matrices
antisymétriques comme espace tangent --- de la géométrie
\omterm{def:b2:diffcalc:differential}{différentielle} en germe. Dans tout ce qui suit, $\vertiii\cdot$ est
la \omterm{thm:b2:nvs:continuouslinear}{norme d'opérateur} subordonnée à $\norm\cdot_2$, et $\langle X,
Y\rangle = \operatorname{tr}(X^{\mathsf T}Y)$ le produit scalaire de
Frobenius.

\medskip
\noindent\textbf{Partie I --- La série de Neumann.}
\begin{enumerate}
  \item Démontrer la sous-multiplicativité, $\vertiii{AB} \leq
        \vertiii A\,\vertiii B$, et en déduire que les applications
        polynomiales de $A$ (produits de matrices, \omterm{def:b2:linalg:det}{déterminant},
        trace) sont \omterm{def:b2:metric:continuity}{continues} sur $\mathcal M_n(\R)$.
  \item Pour $\vertiii X < 1$, montrer que $\sum_{k\geq0}X^k$
        \omterm{def:b2:integration:improper}{converge} \omterm{def:b2:series:def}{absolument} dans $\mathcal M_n(\R)$
        (\cref{thm:b2:nvs:absoluteconvergence}), que sa somme
        est $(I - X)^{-1}$, et que
        \[
        \vertiii{(I - X)^{-1}} \leq \frac{1}{1 - \vertiii X},
        \qquad
        (I - X)^{-1} = I + X + O\bigl(\vertiii X^2\bigr) .
        \]
  \item En déduire que $GL_n(\R)$ est \emph{\omterm{def:b2:metric:topology}{ouvert}}~: si $A$ est
        inversible et $\vertiii H <
        \frac{1}{\vertiii{A^{-1}}}$, alors $A + H$ est
        inversible. En déduire aussi que $GL_n(\R)$ est \emph{dense}
        dans $\mathcal M_n(\R)$ \emph{(perturber $A$ par
        $\varepsilon I$~: $\det(A + \varepsilon I)$ est un polynôme
        non nul en $\varepsilon$)}.
  \item Montrer que l'application d'inversion $\Phi(A) = A^{-1}$ est
        \omterm{def:b2:metric:continuity}{continue} sur $GL_n(\R)$.
\end{enumerate}

\medskip
\noindent\textbf{Partie II --- Premières \omterm{def:b2:diffcalc:differential}{différentielles}.}
\begin{enumerate}[resume]
  \item Montrer que l'application de mise au carré $A \mapsto A^2$ est
        \omterm{def:b2:diffcalc:differential}{différentiable} de \omterm{def:b2:diffcalc:differential}{différentielle} $H \mapsto AH + HA$,
        et plus généralement que $A \mapsto A^k$ a pour
        \omterm{def:b2:diffcalc:differential}{différentielle} $H \mapsto \sum_{i=0}^{k-1}
        A^iHA^{k-1-i}$. Pourquoi ne peut-on \emph{pas} écrire
        $kA^{k-1}H$ en général~?
  \item Démontrer que $\Phi(A) = A^{-1}$ est \omterm{def:b2:diffcalc:differential}{différentiable} sur
        $GL_n(\R)$ avec
        \[
        \dd\Phi_A(H) = -A^{-1}HA^{-1}
        \]
        \emph{(écrire $(A + H)^{-1} = (I + A^{-1}H)^{-1}A^{-1}$
        et développer par la question 2)}. Vérifier la formule sur
        le cas scalaire $n = 1$.
  \item Pour une courbe $C^1$ $t \mapsto A(t) \in GL_n(\R)$, en
        déduire $\bigl(A(t)^{-1}\bigr)' = -A^{-1}A'A^{-1}$, et
        développer $t \mapsto (I + tB)^{-1}$ à l'\omterm{def:b2:structures:generated}{ordre} 1 en $t = 0$.
  \item Calculer la \omterm{def:b2:diffcalc:differential}{différentielle} de $f(A) =
        \operatorname{tr}(A^k)$ et identifier son gradient pour
        le produit scalaire de Frobenius~:
        \[
        \dd f_A(H) = k\operatorname{tr}\bigl(A^{k-1}H\bigr),
        \qquad
        \nabla f(A) = k\,\bigl(A^{k-1}\bigr)^{\mathsf T} .
        \]
  \item Mêmes questions pour $f(A) = \operatorname{tr}
        (A^{\mathsf T}A) = \norm A_F^2$~: \omterm{def:b2:diffcalc:differential}{différentielle},
        gradient, et la hessienne (constante)~; conclure que
        $\norm\cdot_F^2$ est strictement convexe.
\end{enumerate}

\medskip
\noindent\textbf{Partie III --- La formule de Jacobi.}
\begin{enumerate}[resume]
  \item Démontrer
        \[
        \det(I + H) = 1 + \operatorname{tr}H +
        O\bigl(\vertiii H^2\bigr)
        \]
        \emph{(développer $\det(e_1 + h_1, \dots, e_n + h_n)$ par
        multilinéarité en les colonnes~: les termes ayant au moins
        deux colonnes $h$ sont des $O(\vertiii H^2)$)}~: $\dd(\det)_I =
        \operatorname{tr}$.
  \item Pour $A$ inversible, en déduire
        \[
        \dd(\det)_A(H) = \det(A)\,
        \operatorname{tr}\bigl(A^{-1}H\bigr) .
        \]
  \item Montrer que pour \emph{toute} matrice $A$ (inversible ou
        non), $\frac{\partial\det}{\partial a_{ij}}(A) = C_{ij}$, le
        cofacteur d'indice $(i,j)$ \emph{(développement de Laplace
        selon la ligne $i$)}, de sorte qu'avec la comatrice
        \omterm{def:b2:linalg:transpose}{transposée} $\operatorname{adj}A =
        \operatorname{com}(A)^{\mathsf T}$~:
        \[
        \dd(\det)_A(H) =
        \operatorname{tr}\bigl(\operatorname{adj}(A)\,H\bigr),
        \qquad
        \nabla(\det)(A) = \operatorname{com}(A) ,
        \]
        retrouvant la question 11 lorsque $A$ est inversible
        ($\operatorname{adj}A = \det(A)A^{-1}$). C'est la
        \emph{formule de Jacobi}~: $\bigl(\det
        A(t)\bigr)' = \operatorname{tr}\bigl(
        \operatorname{adj}(A(t))\,A'(t)\bigr)$.
  \item (Formule de Liouville) Soit $A(t)$ une courbe $C^1$ de
        matrices satisfaisant l'équation \omterm{def:b2:diffcalc:differential}{différentielle} linéaire
        $A'(t) = M(t)A(t)$. Démontrer
        \[
        \bigl(\det A(t)\bigr)' =
        \operatorname{tr}\bigl(M(t)\bigr)\,\det A(t),
        \qquad\text{d'où}\qquad
        \det A(t) = \det A(0)\,
        \exp\Bigl(\int_0^t\operatorname{tr}M\Bigr)
        \]
        \emph{(utiliser $\operatorname{adj}(A)\,A = \det(A)I$ et
        l'invariance \omterm{def:b2:structures:generated}{cyclique} de la trace)} --- l'identité du
        wronskien que le chapitre sur les équations \omterm{def:b2:diffcalc:differential}{différentielles}
        utilisera constamment.
  \item Montrer que $SL_n(\R) = \{\det = 1\}$ est un ensemble de
        niveau lisse~: en tout $A \in SL_n(\R)$ la \omterm{def:b2:diffcalc:differential}{différentielle}
        $\dd(\det)_A$ est une application linéaire \emph{surjective}
        sur $\R$ \emph{(l'évaluer en $H = \frac1nA$)}.
\end{enumerate}

\medskip
\noindent\textbf{Partie IV --- L'\omterm{ex:b2:nvs:matrixexp}{exponentielle de matrice}.}
\begin{enumerate}[resume]
  \item Montrer que $\eu^A = \sum_{k\geq0}\frac{A^k}{k!}$
        \omterm{def:b2:integration:improper}{converge} \omterm{def:b2:series:def}{absolument} pour toute matrice $A$, \omterm{def:b2:funcseq:series}{normalement} sur
        toute boule, avec $\vertiii{\eu^A} \leq
        \eu^{\vertiii A}$~; et que $\eu^A$ dépend
        continûment de $A$.
  \item Démontrer que $AB = BA$ implique $\eu^{A+B} =
        \eu^A\eu^B$ \emph{(\omterm{thm:b2:series:fubini}{produit de Cauchy}, légitime par
        convergence absolue)}~; en déduire que $\eu^A$ est toujours
        inversible, d'inverse $\eu^{-A}$~: $\exp$ envoie
        $\mathcal M_n(\R)$ dans $GL_n(\R)$.
  \item Montrer que $t \mapsto \eu^{tA}$ est $C^1$ (et même
        $C^\infty$) avec
        \[
        \frac{\dd}{\dd t}\,\eu^{tA} = A\,\eu^{tA} =
        \eu^{tA}A
        \]
        \emph{(dériver la série terme à terme sur les segments~: la
        série dérivée \omterm{def:b2:integration:improper}{converge} \omterm{def:b2:funcseq:series}{normalement})}.
  \item Démontrer l'identité
        \[
        \det\bigl(\eu^{A}\bigr) = \eu^{\operatorname{tr}A}
        \]
        \emph{(appliquer la formule de Liouville, question 13, à
        $A(t) = \eu^{tA}$)}. Vérifications de bon sens~: $n = 1$~;
        $A$ nilpotente~; et les matrices de trace nulle atterrissent
        dans $SL_n(\R)$.
  \item Montrer $\eu^H = I + H + O(\vertiii H^2)$, donc $\exp$ est
        \omterm{def:b2:diffcalc:differential}{différentiable} en $0$ avec $\dd(\exp)_0 =
        \mathrm{id}$~; conclure avec le théorème d'inversion
        locale (\cref{thm:b2:diffcalc:inverse}) que $\exp$
        est un difféomorphisme $C^1$ d'un voisinage de $0$
        sur un voisinage de $I$~: toute matrice proche de
        l'identité a un logarithme.
  \item Montrer que $\exp$ envoie les matrices \omterm{def:b2:quadratic:adjoint}{symétriques} sur les
        matrices \omterm{def:b2:quadratic:adjoint}{symétriques} \emph{définies positives}, de façon
        bijective \emph{(diagonaliser~; l'inverse est le logarithme
        spectral)}.
\end{enumerate}

\medskip
\noindent\textbf{Partie V --- Le groupe orthogonal comme ensemble de
niveau.}
\begin{enumerate}[resume]
  \item Soit $F(A) = A^{\mathsf T}A$, de $\mathcal M_n(\R)$ vers
        les matrices \omterm{def:b2:quadratic:adjoint}{symétriques} $S_n$. Calculer $\dd F_A(H) =
        A^{\mathsf T}H + H^{\mathsf T}A$ et montrer qu'en tout
        $A \in O_n = F^{-1}(I)$ cette \omterm{def:b2:diffcalc:differential}{différentielle} est
        \emph{surjective} sur $S_n$ \emph{(étant donné $S \in S_n$,
        essayer $H = \frac12 AS$)}~: $O_n$ est un ensemble de niveau
        lisse, de dimension $n^2 - \frac{n(n+1)}2 = \frac{n(n-1)}2$.
  \item Montrer que toute courbe $C^1$ $A(t) \in O_n$ avec $A(0) =
        I$ a une vitesse $A'(0)$ \emph{antisymétrique}, et
        réciproquement que pour $K$ antisymétrique la courbe
        $\eu^{tK}$ reste dans $O_n$~: l'espace tangent de $O_n$
        en $I$ est exactement les matrices antisymétriques.
  \item Montrer $\det\eu^{K} = 1$ pour $K$ antisymétrique (question
        18)~: la courbe exponentielle vit dans le groupe des
        rotations $SO_n$. La calculer complètement pour $n = 2$~:
        avec $J = \begin{pmatrix}0 & -1\\ 1 & 0\end{pmatrix}$,
        démontrer
        \[
        \eu^{\theta J} = \begin{pmatrix} \cos\theta &
        -\sin\theta\\ \sin\theta & \cos\theta\end{pmatrix} :
        \]
        l'\omterm{ex:b2:nvs:matrixexp}{exponentielle de matrice} \emph{est} la rotation d'angle
        $\theta$, et les définitions en série du cosinus et du sinus
        réapparaissent à l'intérieur d'une matrice.
  \item (L'astuce de densité) En utilisant la densité de $GL_n(\R)$
        (question 3) et la continuité, étendre des matrices
        inversibles à toutes les matrices l'identité
        \[
        \operatorname{adj}(AB) =
        \operatorname{adj}(B)\operatorname{adj}(A)
        \]
        \emph{(pour $A, B$ inversibles les deux membres valent
        $\det(AB)(AB)^{-1}$~; les deux membres sont polynomiaux en
        les coefficients)}.
  \item Synthèse. En une phrase chacune~: (i) quels chapitres
        antérieurs ont fourni le moteur de chaque Partie
        (complétude et \omterm{def:b2:structures:algebra}{algèbres} normées~; le théorème spectral~;
        le théorème d'inversion locale)~; (ii) quelle formule de ce
        problème le chapitre sur les équations \omterm{def:b2:diffcalc:differential}{différentielles}
        exploitera, et où~; (iii) ce que $\dd(\det)_I =
        \operatorname{tr}$ et $\det\eu^A =
        \eu^{\operatorname{tr}A}$ disent de la trace et du
        \omterm{def:b2:linalg:det}{déterminant} comme «~volume infinitésimal et global~»~;
        (iv) ce que le volume de Licence~3 fait des questions
        21--23 (groupes de Lie et leurs \omterm{def:b2:structures:algebra}{algèbres} de Lie).
\end{enumerate}
\end{problem}
