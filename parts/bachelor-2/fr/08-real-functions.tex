\chapter{Fonctions d'une variable réelle}\label{ch:b2:realfun}

Avant que l'analyse ne passe aux fonctions de fonctions
(le~\cref{ch:b2:funcseq}), il est utile de connaître le paysage à une
variable plus finement que ne l'exigeait la première année~: à quel
point une fonction monotone peut être discontinue, à quel point une
fonction convexe doit être régulière, et quelles propriétés
particulières possèdent les dérivées (Darboux). Ces résultats
structurels sont courts, tranchants, et chers aux examinateurs.

\section{Fonctions monotones}

\begin{theorem}[Régularité des fonctions monotones]\label{thm:b2:realfun:monotone}
Soit $f \colon I \to \R$ croissante sur un intervalle.
\begin{enumerate}
  \item En tout point intérieur $a$, les limites latérales existent~:
        \[
        f(a^-) = \sup_{x < a} f(x) \;\leq\; f(a) \;\leq\;
        f(a^+) = \inf_{x > a} f(x) ;
        \]
        toute discontinuité est un \emph{saut}.
  \item L'ensemble des points de discontinuité de $f$ est au plus
        \omterm{def:b2:structures:countable}{dénombrable}.\index{fonction monotone!discontinuités}
\end{enumerate}
\end{theorem}

\begin{proof}
(1) L'ensemble $\{f(x) : x < a\}$ est non vide, majoré par $f(a)$~:
sa borne supérieure $s$ vérifie $f(x) \to s$ quand $x \to a^-$ (étant
donné $\varepsilon$, il existe $f(x_0) > s - \varepsilon$, et la
monotonie piège $f(x) \in \intoc{s - \varepsilon}{s}$ pour $x \in
\intoo{x_0}{a}$). Symétriquement à droite.

(2) À chaque discontinuité $a$ on associe l'intervalle \omterm{def:b2:metric:topology}{ouvert} non vide
$J_a = \intoo{f(a^-)}{f(a^+)}$ (un véritable saut). Pour des
discontinuités $a < b$, $J_a$ et $J_b$ sont disjoints~: $f(a^+) \leq
f(c) \leq f(b^-)$ pour tout $c$ entre les deux. Chaque $J_a$ contient
un rationnel~; des discontinuités distinctes reçoivent des rationnels
distincts~: c'est une injection de l'ensemble des discontinuités dans
$\Q$, qui est \omterm{def:b2:structures:countable}{dénombrable}
(la~\cref{prop:b2:structures:countablestable}).
\end{proof}

\begin{example}
La borne est optimale~: fixons une énumération $(r_n)$ de $\Q \cap
\intoo{0}{1}$ et posons $f(x) = \sum_{n : r_n \leq x} 2^{-n}$
(une définition par \omterm{def:b2:series:summable}{famille sommable},
la~\cref{def:b2:series:summable}). Alors $f$ est croissante sur
$\intcc{0}{1}$ et discontinue exactement en tout rationnel de
$\intoo{0}{1}$ (saut $2^{-n}$ en $r_n$)~: une fonction monotone
\emph{peut} être discontinue sur un \omterm{def:b2:structures:countable}{ensemble dénombrable} dense.
\end{example}

\begin{example}[Les sauts ne peuvent l'emporter sur la montée]\label{ex:b2:realfun:jumpbudget}
Pour $f$ croissante sur $\intcc{a}{b}$, les sauts disposent d'un
budget~: si $a < c_1 < \dots < c_m < b$ sont des discontinuités de
sauts $s_i = f(c_i^+) - f(c_i^-) > 0$, alors en choisissant des
points intercalés $a < c_1 < t_1 < c_2 < \dots$ et en utilisant la
monotonie sur chaque morceau,
\[
\sum_{i=1}^{m} s_i \;\leq\; f(b) - f(a) :
\]
la montée totale majore la somme des sauts. Conséquence~: pour
chaque $k$, au plus $k\,\bigl(f(b) - f(a)\bigr)$ discontinuités
ont un saut $\geq \frac1k$ --- un raffinement quantitatif du
\cref{thm:b2:realfun:monotone}~(2), puisque l'ensemble des
discontinuités est la réunion \omterm{def:b2:structures:countable}{dénombrable} sur $k$ de ces ensembles
finis. Sur la fonction à sauts rationnels ci-dessus, le budget est
dépensé exactement~: les sauts $2^{-n}$ ont pour somme $1 = f(1^+) -
f(0^-)$ au sens étendu évident. Les fonctions monotones peuvent
sauter de manière dense, mais seulement dans les limites d'une
allocation stricte.
\end{example}

\section{Fonctions convexes}

\begin{lemma}[Inégalité des pentes]\label{lem:b2:realfun:slopes}
Soit $f$ convexe sur $I$ et $x < y < z$ dans $I$. Alors
\[
\frac{f(y) - f(x)}{y - x}
\;\leq\; \frac{f(z) - f(x)}{z - x}
\;\leq\; \frac{f(z) - f(y)}{z - y} :
\]
les pentes des cordes croissent en chacune des deux
extrémités.\index{fonction convexe!inégalité des pentes}
\end{lemma}

\begin{proof}
Écrivons $y = \frac{z - y}{z - x}\,x + \frac{y - x}{z - x}\,z$~: une
combinaison convexe, puisque les deux coefficients sont positifs et
de somme $1$. La convexité donne
\[
f(y) \;\leq\; \frac{z-y}{z-x}\,f(x) + \frac{y-x}{z-x}\,f(z).
\]
Pour l'inégalité de gauche, retranchons $f(x)$ des deux membres, en
utilisant $\frac{z-y}{z-x} - 1 = -\frac{y-x}{z-x}$~:
\[
f(y) - f(x) \leq \frac{y - x}{z - x}\bigl(f(z) - f(x)\bigr),
\]
et divisons par $y - x > 0$. Pour l'inégalité de droite, retranchons
plutôt de $f(z)$~:
\[
f(z) - f(y) \geq f(z) - \frac{z-y}{z-x}f(x) -
\frac{y-x}{z-x}f(z)
= \frac{z - y}{z - x}\bigl(f(z) - f(x)\bigr),
\]
et divisons par $z - y > 0$. Les deux étapes affichées sont la même
identité barycentrique lue par rapport à une extrémité différente.
\end{proof}

\begin{theorem}[Régularité des fonctions convexes]\label{thm:b2:realfun:convexreg}
Soit $f$ convexe sur un intervalle $I$.
\begin{enumerate}
  \item En tout point intérieur, $f$ possède des dérivées latérales
        finies $f'_g \leq f'_d$~; toutes deux sont des fonctions
        croissantes du point~; en particulier $f$ est \omterm{def:b2:metric:continuity}{continue} sur
        l'intérieur de $I$ (mais peut-être pas aux extrémités).
  \item $f$ est au-dessus de chaque \emph{droite d'appui}~: pour $a$
        intérieur et tout $m \in \intcc{f'_g(a)}{f'_d(a)}$,
        \[
        f(x) \geq f(a) + m(x - a) \qquad (x \in I).
        \]
  \item (Jensen, pondérée) Pour $x_i \in I$ et des poids $\lambda_i
        \geq 0$, $\sum\lambda_i = 1$~:
        \[
        f\Bigl(\sum_i \lambda_i x_i\Bigr) \leq \sum_i \lambda_i
        f(x_i) .\index{inégalité de Jensen}
        \]
\end{enumerate}
\end{theorem}

\begin{proof}
(1) Fixons $a$ intérieur. D'après le~\cref{lem:b2:realfun:slopes}, la
pente $\tau(h) = \frac{f(a + h) - f(a)}{h}$ est une fonction
croissante de $h$ (des deux côtés, et $\tau(h_-) \leq \tau(h_+)$ pour
$h_- < 0 < h_+$). Donc $\tau$ admet une limite finie quand $h \to
0^-$ (croissante, majorée par n'importe quelle pente à droite) ---
c'est $f'_g(a)$ --- et quand $h \to 0^+$ ($f'_d(a)$), avec $f'_g(a)
\leq f'_d(a)$. Des dérivées latérales finies forcent la continuité en
$a$. Monotonie en le point~: pour $a < b$ intérieurs, $f'_d(a) \leq
\frac{f(b) - f(a)}{b - a} \leq f'_g(b)$, de nouveau par l'inégalité
des pentes.

(2) Pour $x > a$~: $\frac{f(x) - f(a)}{x - a} \geq f'_d(a) \geq m$~;
pour $x < a$~: $\frac{f(a) - f(x)}{a - x} \leq f'_g(a) \leq m$. Les
deux se réarrangent en l'assertion.

(3) Récurrence sur le nombre de points exactement comme dans le
volume de première année (le cas à deux points est la définition) ---
ou d'un seul coup~: appliquons (2) en $a = \sum\lambda_i x_i$ et
moyennons les inégalités de droite d'appui aux points $x_i$ avec les
poids $\lambda_i$~: $\sum_i \lambda_i f(x_i) \geq f(a) +
m\sum_i\lambda_i(x_i - a) = f(a)$.
\end{proof}

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis, width=8.2cm, height=5.4cm,
    xmin=-2.4, xmax=2.5, ymin=-0.9, ymax=4.5,
    xtick={-1.5, 0.5, 2}, ytick=\empty]
  \addplot[omDef, very thick, domain=-2.1:2.15, samples=80]
    {x^2};
  \addplot[omThm, thick, domain=-1.5:2, samples=2]
    {0.5*x + 3};
  \addplot[omProp, thick, domain=-1.1:2.2, samples=2]
    {x - 0.25};
  \node[font=\small, omThm] at (axis cs:-1.05,3.15)
    {corde};
  \node[font=\small, omProp] at (axis cs:1.75,0.95)
    {droite d'appui};
  \node[font=\small, omDef] at (axis cs:-1.95,2.6) {$f$};
\end{axis}
\end{tikzpicture}

{\small La convexité en une image~: entre $-1.5$ et $2$, le graphe
de $f(x) = x^2$ reste en dessous de sa corde (la définition) et
au-dessus de la droite d'appui en $x = 0.5$
(le~\cref{thm:b2:realfun:convexreg}~(2)) --- toute inégalité du
problème du week-end de ce chapitre est un réarrangement de ces deux
positions.}
\end{omfigure}

\begin{example}[Discontinuité au bord]
Sur $\intcc{0}{1}$, la fonction $f(0) = 1$, $f(x) = 0$ pour $x > 0$
est convexe mais discontinue à l'extrémité $0$~: l'assertion (1) est
optimale.
\end{example}

\begin{example}[Coins et le faisceau de droites d'appui]\label{ex:b2:realfun:corner}
Pour $f(x) = \abs x$ en $a = 0$~: les dérivées latérales sont
$f'_g(0) = -1$ et $f'_d(0) = +1$, et
le~\cref{thm:b2:realfun:convexreg}~(2) fournit une droite d'appui
pour \emph{chaque} pente $m \in \intcc{-1}{1}$~:
\[
\abs x \geq m\,x \qquad (x \in \R,\ -1 \leq m \leq 1),
\]
chacune étant une égalité exactement sur une demi-droite ou en $0$.
Une fonction convexe est dérivable en $a$ précisément lorsque le
faisceau se réduit à une seule droite ($f'_g(a) = f'_d(a)$)~; les
coins portent tout un intervalle de tangentes. Ce faisceau est le
germe en dimension finie du \emph{sous-différentiel} de
l'optimisation convexe --- et la raison pour laquelle les fonctions
convexes sont si robustes~: même là où la dérivée fait défaut, la
géométrie d'appui subsiste, et c'est tout ce dont s'est servie la
démonstration de Jensen.
\end{example}

\begin{example}[Inégalité des moyennes de puissances]\label{ex:b2:realfun:powermeans}
Pour $0 < p < q$ et des $x_i$ positifs de poids $\lambda_i$ de somme
$1$, en appliquant Jensen à la fonction convexe $t \mapsto t^{q/p}$
aux points $x_i^p$~:
\[
\Bigl(\sum \lambda_i x_i^{p}\Bigr)^{1/p}
\leq \Bigl(\sum \lambda_i x_i^{q}\Bigr)^{1/q} :
\]
les moyennes de puissances croissent avec l'exposant --- englobant
l'inégalité entre moyenne arithmétique et moyenne quadratique, et, à
la limite $p \to 0$ (l'\cref{exo:b2:realfun:6}), de nouveau
l'inégalité arithmético-géométrique.
\end{example}

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis, width=8.6cm, height=5.6cm,
    xmin=-8.5, xmax=8.5, ymin=0.5, ymax=4.5,
    xtick={-4,-1,1,2,4}, ytick={1,2,4},
    xlabel={$p$}, ylabel={$M_p$}]
  \addplot[omDef, very thick, domain=-8:-0.4, samples=90]
    {((1/3)*(1^x + 2^x + 4^x))^(1/x)};
  \addplot[omDef, very thick, domain=0.4:8, samples=90]
    {((1/3)*(1^x + 2^x + 4^x))^(1/x)};
  \addplot[omThm, dashed, domain=-8.5:8.5, samples=2] {4};
  \addplot[omThm, dashed, domain=-8.5:8.5, samples=2] {1};
  \node[font=\small, omDef] at (axis cs:-5.4,1.55)
    {$M_p(1,2,4)$};
  \node[font=\small, omThm] at (axis cs:-6.4,3.75)
    {$\max = 4$};
  \node[font=\small, omThm] at (axis cs:6.3,1.25)
    {$\min = 1$};
  \node[font=\small, omProp] at (axis cs:0.2,1.83) {$2$};
\end{axis}
\end{tikzpicture}

{\small La moyenne de puissance $M_p$ des valeurs $1, 2, 4$ (poids
égaux), en fonction de l'exposant $p$~: croissante de $\min = 1$
(quand $p \to -\infty$) à $\max = 4$ (quand $p \to +\infty$), en
passant par les moyennes harmonique ($p = -1$), géométrique (le trou
en $p = 0$, valeur $2$), arithmétique ($p = 1$) et quadratique ($p
= 2$). Toute la chaîne des inégalités classiques entre moyennes est
une seule courbe croissante --- démontrée dans le problème du
week-end de ce chapitre, Partie III.}
\end{omfigure}

\begin{example}[Entropie maximale]\label{ex:b2:realfun:entropy}
Pour un vecteur de probabilité $(p_1, \dots, p_n)$ (positif, de somme
$1$), l'entropie $H(p) = -\sum_i p_i\ln p_i$ vérifie
\[
H(p) \leq \ln n ,
\qquad\text{avec égalité si et seulement si } p_i = \frac1n
\text{ pour tout } i .
\]
Démonstration par Jensen (le~\cref{thm:b2:realfun:convexreg}~(3))
appliqué à la fonction \emph{concave} $\ln$ avec les poids $p_i$ aux
points $\frac{1}{p_i}$~:
\[
H(p) = \sum_i p_i\ln\frac{1}{p_i}
\leq \ln\Bigl(\sum_i p_i\,\frac1{p_i}\Bigr) = \ln n ,
\]
l'égalité forçant tous les points $\frac1{p_i}$ à être égaux
(concavité stricte), c'est-à-dire $p$ uniforme. De façon
équivalente, c'est l'\cref{exo:b2:realfun:7} avec $q$ uniforme.
L'incertitude est maximisée par l'ignorance uniformément répartie ---
le principe variationnel derrière le codage, la mécanique
statistique, et les apparitions de l'entropie du~\cref{ch:b2:randomvar}.
\end{example}

\begin{method}[Trouver la fonction convexe derrière une inégalité]\label{met:b2:realfun:spotting}
La plupart des inégalités classiques sont du Jensen déguisé~; pour le
déshabiller~: (1) normaliser pour qu'apparaisse une \emph{moyenne
pondérée} (poids positifs, de somme $1$ --- diviser par une masse
totale si nécessaire)~; (2) regarder quelle fonction est appliquée à
l'intérieur plutôt qu'à l'extérieur de la moyenne~: l'assertion
«~$f(\text{moyenne}) \leq$ moyenne de $f$~» nomme la fonction convexe
$f$~; (3) certifier la convexité par la dérivée seconde, et traiter
l'égalité par la stricte convexité~; (4) si aucune moyenne n'est
visible, prendre d'abord des logarithmes --- produits et puissances
deviennent des moyennes, et la concavité de $\ln$ porte l'inégalité
arithmético-géométrique, Young et leurs parentes (le problème du
week-end de ce chapitre déroule les étapes 1--4 sur chacune d'elles).
Si même les logarithmes ne révèlent aucune moyenne, essayer de lire
l'inégalité comme une monotonie des pentes
(le~\cref{lem:b2:realfun:slopes}) --- les énoncés de sur-additivité
comme l'\cref{exo:b2:realfun:9} vivent là.
\end{method}

\begin{remark}[Pièges classiques]\label{rem:b2:realfun:pitfalls}
(i) La convexité n'est pas préservée par produit~: $x$ et $(x - 1)^2$
sont convexes sur $\intcc{0}{2}$, mais leur produit $x(x-1)^2$ a pour
dérivée seconde $6x - 4$, négative sur
$\intco{0}{\frac23}$ --- non convexe~; la convexité n'est pas non
plus préservée par composition sans monotonie
(l'\cref{exo:b2:realfun:10}). (ii) Jensen se renverse pour les
fonctions concaves~: la moitié des inégalités classiques sont la
version concave en $\ln$~; appliquer la forme convexe à $\ln$ est le
moyen le plus rapide de démontrer l'inégalité arithmético-géométrique
\emph{à l'envers}. (iii) La convexité au milieu seule n'implique pas
la convexité --- il faut la continuité (ou seulement le caractère
borné) (l'\cref{exo:b2:realfun:8})~; les contre-exemples
pathologiques vivent au-delà des axiomes de ce livre. (iv) Une
fonction convexe sur un intervalle \emph{\omterm{def:b2:metric:topology}{ouvert}} est \omterm{def:b2:metric:continuity}{continue}, et
même localement \omterm{def:b2:metric:continuity}{lipschitzienne} (l'\cref{exo:b2:realfun:12})~; aux
extrémités, rien n'est gratuit. (v) Les dérivées obéissent à Darboux
mais ne sont pas nécessairement \omterm{def:b2:metric:continuity}{continues}
(l'\cref{ex:b2:realfun:oscillation})~: «~$f'$ n'a pas de saut~» ne
signifie jamais «~$f'$ est \omterm{def:b2:metric:continuity}{continue}~».
\end{remark}

\section{La propriété de Darboux}

\begin{theorem}[Darboux]\label{thm:b2:realfun:darboux}
Soit $f$ dérivable sur un intervalle $I$. Alors $f'$ prend toute
valeur comprise entre deux quelconques de ses valeurs --- bien que
$f'$ ne soit pas nécessairement \omterm{def:b2:metric:continuity}{continue}.\index{théorème de Darboux}
\end{theorem}

\begin{proof}
Soient $a < b$ dans $I$ et $v$ strictement compris entre $f'(a)$ et
$f'(b)$, disons $f'(a) < v < f'(b)$. La fonction $g(x) = f(x) - vx$
est dérivable avec $g'(a) < 0 < g'(b)$~: son minimum sur
$\intcc{a}{b}$ (atteint~: continuité sur un \omterm{def:b2:metric:compact}{compact}) n'est pas en $a$
(juste après $a$, $g$ décroît en dessous de $g(a)$) ni en $b$ (juste
avant $b$, $g$ est en dessous de $g(b)$)~: il est intérieur, et là
$g'(c) = 0$, c'est-à-dire $f'(c) = v$. (C'était un exercice étoilé de
première année~; sa place dans la théorie est ici.)
\end{proof}

\begin{example}[Quelles fonctions sont des dérivées~?]\label{ex:b2:realfun:whichderivatives}
Le théorème de Darboux est une machine à non-existence. La fonction
partie entière $\lfloor x\rfloor$ n'est la dérivée d'aucune fonction
sur $\R$~: elle prend les valeurs $0$ et $1$ mais saute $\frac12$ sur
$\intcc{0}{1}$, ce que le~\cref{thm:b2:realfun:darboux} interdit aux
dérivées. Le même verdict frappe toute fonction ayant un saut ---
signe, Heaviside, toutes les fonctions en escalier --- si innocentes
qu'elles paraissent~; leurs «~primitives~» ($\abs x$ pour le signe,
etc.) n'existent qu'en dehors du saut et s'y nouent en un coin. Par
contraste~: la $f'$ sauvagement discontinue de
l'\cref{ex:b2:realfun:oscillation} \emph{est} une dérivée --- sa
discontinuité est une oscillation, que Darboux tolère. La frontière
entre les deux comportements est exactement le corollaire d'absence
de saut ci-dessous.
\end{example}

\begin{corollary}\label{cor:b2:realfun:nojumps}
Une dérivée n'a pas de discontinuité de saut~: si $f'(a^-)$ et
$f'(a^+)$ existent, elles valent $f'(a)$. Les discontinuités d'une
dérivée sont toujours de type oscillation (la dérivée de
$x^2\sin\frac1x$ en $0$, volume de première année).
\end{corollary}

\begin{proof}
Si $f'(a^+) = \lim_{x\to a^+} f'(x)$ existe et diffère de $f'(a)$,
les valeurs strictement comprises entre les deux seraient sautées par
$f'$ sur un voisinage à droite --- contredisant Darboux sur les
intervalles $\intcc{a}{a + h}$. (Autrement~: le théorème des
accroissements finis force $f'(a) = \lim_{h\to0^+}
\frac{f(a+h)-f(a)}{h} = f'(a^+)$, le taux d'accroissement étant une
valeur de $f'$ en un point intermédiaire.) De même à gauche.
\end{proof}

\begin{example}[La dérivée oscillante canonique]\label{ex:b2:realfun:oscillation}
Soit $f(x) = x^2\sin\frac1x$ pour $x \neq 0$ et $f(0) = 0$. En
$0$~: $\bigl|\frac{f(h) - f(0)}{h}\bigr| = \abs{h\sin\frac1h}
\leq \abs h \to 0$, donc $f'(0) = 0$ existe. En dehors de $0$,
\[
f'(x) = 2x\sin\frac1x - \cos\frac1x ,
\]
dont le premier terme tend vers $0$ tandis que $\cos\frac1x$ oscille
dans $\intcc{-1}{1}$ sur tout intervalle $\intoo{0}{\delta}$~: la
limite $f'(0^+)$ n'existe pas. Ainsi $f'$ est définie partout mais
discontinue en $0$ --- et, exactement comme le prédit
le~\cref{cor:b2:realfun:nojumps}, la discontinuité est une
oscillation, non un saut~: sur chaque $\intoo{0}{\delta}$, $f'$
balaie encore tout un intervalle autour de $0$. Les dérivées peuvent
être sauvages, mais seulement de la manière compatible avec Darboux.
\end{example}

\begin{remark}[Où ce chapitre est utilisé]
La convexité est le moteur de l'industrie des inégalités~: le
problème du week-end de ce chapitre en fabrique Young, Hölder,
Minkowski et la chaîne des moyennes de puissances, que la théorie des
\omterm{def:b2:nvs:norm}{normes} du~\cref{ch:b2:nvs} et les estimations intégrales
du~\cref{ch:b2:integration} consomment~; Jensen reparaît en
probabilité sous la forme des inégalités de moments
du~\cref{ch:b2:randomvar}. La régularité des fonctions monotones
revient au~\cref{ch:b2:integration} (les fonctions monotones sont
intégrables) et, dans le volume de troisième année, sous la forme de
la dérivabilité presque partout des fonctions monotones --- où «~un
\omterm{def:b2:structures:countable}{ensemble dénombrable} de sauts~» devient le premier pas de la théorie
de Lebesgue.
\end{remark}

\section{Exercices}

\begin{exercise}[$\star$]\label{exo:b2:realfun:1}
Déterminer les ensembles de discontinuité et les tailles des sauts~:
$\lfloor x \rfloor$~; $\;x - \lfloor x\rfloor$~; $\;\lfloor x \rfloor
+ \sqrt{x - \lfloor x\rfloor}$~; la fonction de l'exemple suivant
le~\cref{thm:b2:realfun:monotone} restreinte aux rationnels dyadiques
$r_n$.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b2:realfun:2}
Démontrer qu'une fonction croissante $f \colon I \to \R$ ayant la
propriété des valeurs intermédiaires (l'image de tout sous-intervalle
est un intervalle) est \omterm{def:b2:metric:continuity}{continue}.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b2:realfun:3}
Lesquelles des fonctions suivantes sont convexes sur leur domaine~?
$x \mapsto x\ln x$ ($x > 0$)~; $\;x \mapsto \ln(1 + \eu^x)$~; $\;x
\mapsto \sqrt{1 + x^2}$~; $\;x \mapsto x^3$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:realfun:4}
Soit $f$ convexe sur $\R$ et majorée. Démontrer que $f$ est
constante. \emph{(Si $f(a) \neq f(b)$, l'inégalité des pentes propage
la pente de corde non nulle~: au-delà du point de plus grande valeur,
$f$ croît au moins linéairement --- ce qui contredit le caractère
borné. Traiter les deux signes de la pente.)} En déduire qu'une
fonction convexe sur $\R$ admettant une asymptote aux deux bouts est
affine.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:realfun:5}
Soit $f$ dérivable sur $I$ avec $f'$ monotone. Démontrer que $f'$ est
\omterm{def:b2:metric:continuity}{continue} \emph{(combiner le~\cref{thm:b2:realfun:monotone} et
le~\cref{cor:b2:realfun:nojumps})}.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:realfun:6}
(Moyenne géométrique comme limite) Pour des $x_i$ positifs et des
poids $\lambda_i$ de somme $1$, démontrer
\[
\lim_{p \to 0^+} \Bigl(\sum_i \lambda_i x_i^p\Bigr)^{1/p}
= \prod_i x_i^{\lambda_i} ,
\]
via $x_i^p = \eu^{p\ln x_i} = 1 + p\ln x_i + O(p^2)$, et en déduire
l'inégalité arithmético-géométrique pondérée à partir de
l'\cref{ex:b2:realfun:powermeans}.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:realfun:7}
(Inégalité d'entropie) À l'aide de la stricte convexité de $t \mapsto
t\ln t$, démontrer que pour des $p_i, q_i$ positifs avec $\sum p_i =
\sum q_i = 1$~:
\[
\sum_i p_i \ln\frac{p_i}{q_i} \geq 0 ,
\]
avec égalité si et seulement si $p = q$. \emph{(Écrire le membre de
gauche sous la forme $\sum q_i\, \varphi\bigl(\frac{p_i}{q_i}\bigr)$
avec $\varphi(t) = t\ln t$ et appliquer Jensen avec les poids
$q_i$.)}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b2:realfun:8}
(Convexité au milieu) $f \colon I \to \R$ est \emph{convexe au milieu}
lorsque $f\bigl(\frac{x+y}{2}\bigr) \leq \frac{f(x) + f(y)}{2}$
toujours. Démontrer qu'une fonction \emph{\omterm{def:b2:metric:continuity}{continue}} convexe au milieu
est convexe. \emph{(Établir l'inégalité de convexité pour les poids
dyadiques $\frac{k}{2^m}$ par récurrence sur $m$, puis passer à la
limite en utilisant la densité et la continuité.)}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b2:realfun:9}
Soit $f$ convexe sur $\intco{0}{+\infty}$ avec $f(0) \leq 0$.
Démontrer que $x \mapsto \frac{f(x)}{x}$ est croissante sur
$\intoo{0}{+\infty}$, et en déduire que pour $f$ convexe avec $f(0) =
0$~: $f(x + y) \geq f(x) + f(y)$ pour $x, y \geq 0$
(sur-additivité).
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b2:realfun:10}
Soit $f$ convexe sur $I$ et $g$ convexe \emph{croissante} sur un
intervalle contenant $f(I)$. Démontrer que $g \circ f$ est convexe,
et montrer par un contre-exemple que la monotonie de $g$ ne peut être
abandonnée.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:realfun:11}
(Hermite--Hadamard) Soit $f$ convexe et \omterm{def:b2:metric:continuity}{continue} sur
$\intcc{a}{b}$. Démontrer
\[
f\Bigl(\frac{a+b}{2}\Bigr) \;\leq\; \frac{1}{b -
a}\int_a^b f(t)\,\dd t \;\leq\; \frac{f(a) + f(b)}{2} .
\]
\emph{(Gauche~: intégrer une droite d'appui au milieu. Droite~:
majorer $f$ par la corde.)}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b2:realfun:12}
Démontrer qu'une fonction convexe sur un intervalle \emph{\omterm{def:b2:metric:topology}{ouvert}} $I$
est localement \omterm{def:b2:metric:continuity}{lipschitzienne}~: pour tout segment $\intcc{a}{b}
\subseteq I$ et toute marge $\delta > 0$ avec $\intcc{a - \delta}{b +
\delta} \subseteq I$, la restriction de $f$ à $\intcc{a}{b}$ est
\omterm{def:b2:metric:continuity}{lipschitzienne}, de constante
$\max\Bigl(\bigl|\frac{f(a) - f(a - \delta)}{\delta}\bigr|,
\bigl|\frac{f(b + \delta) - f(b)}{\delta}\bigr|\Bigr)$
\emph{(piéger toute pente de corde entre ces deux par l'inégalité des
pentes)}.
\end{exercise}

\section{Problème~: la boîte à outils de la convexité}

Une seule définition --- la corde au-dessus du graphe --- engendre
toute la boîte à outils des inégalités classiques. Ce problème du
week-end la construit dans l'\omterm{def:b2:structures:generated}{ordre} logique~: critères de convexité et
Jensen strict, puis Young, Hölder et Minkowski (les actes de
naissance des $p$-normes), la chaîne complète des moyennes de
puissances du minimum au maximum, et deux dividendes de couronne ---
l'inégalité de Carleman, et Hölder lu comme une dualité. Tout est
démontré~; rien n'est importé.

\begin{problem}[{Problème du week-end --- Young, Hölder, Minkowski,
et la chaîne des moyennes de puissances}]
\label{pb:b2:realfun:1}
Partout, $p, q > 1$ sont des \emph{exposants conjugués}~: $\frac1p +
\frac1q = 1$~; les vecteurs sont $a = (a_1, \dots, a_n) \in \R^n$~;
les poids $\lambda_i > 0$ vérifient $\sum_i\lambda_i = 1$.

\medskip
\noindent\textbf{Partie I --- Critères et Jensen strict.}
\begin{enumerate}
  \item Soit $f$ dérivable sur un intervalle $I$. Démontrer que
        $f$ est convexe si et seulement si $f'$ est croissante
        \emph{(un sens en passant à la limite dans l'inégalité des
        pentes (le~\cref{lem:b2:realfun:slopes})~; l'autre par le
        théorème des accroissements finis)}. En déduire le critère
        $C^2$ $f'' \geq 0$.
  \item Supposons $f''> 0$ sur $I$. Démontrer que $f$ est
        \emph{strictement} convexe (inégalité stricte pour $x \neq
        y$ et $\lambda \in \intoo01$), et qu'une fonction
        strictement convexe vérifie l'inégalité de Jensen
        (le~\cref{thm:b2:realfun:convexreg}~(3)) avec égalité
        \emph{seulement} lorsque tous les $x_i$ coïncident.
  \item Certifier les matières premières de la boîte à outils~:
        $-\ln$ est strictement convexe sur $\intoo{0}{+\infty}$~; $t
        \mapsto t^r$ y est strictement convexe pour $r > 1$ et
        strictement concave pour $0 < r < 1$~; $\exp$ est strictement
        convexe sur $\R$.
  \item (Inégalité de Young) Pour $a, b \geq 0$, démontrer
        \[
        ab \;\leq\; \frac{a^p}{p} + \frac{b^q}{q},
        \]
        avec égalité si et seulement si $a^p = b^q$ \emph{(appliquer
        la concavité de $\ln$ aux deux points $a^p, b^q$
        avec les poids $\frac1p, \frac1q$)}.
  \item Redémontrer l'inégalité arithmético-géométrique pondérée en
        une ligne à partir de la concavité de $\ln$~:
        \[
        \prod_i x_i^{\lambda_i} \leq \sum_i\lambda_ix_i \qquad
        (x_i > 0),
        \]
        avec le cas d'égalité~; comparer avec la voie par limite de
        l'\cref{exo:b2:realfun:6}.
\end{enumerate}

\medskip
\noindent\textbf{Partie II --- Hölder et Minkowski.} Notons
$\norm{a}_p = \bigl(\sum_i \abs{a_i}^p\bigr)^{1/p}$ et
$\norm{a}_\infty = \max_i\abs{a_i}$.
\begin{enumerate}[resume]
  \item (Hölder) Démontrer
        \[
        \sum_{i=1}^{n}\abs{a_ib_i} \;\leq\;
        \norm a_p\,\norm b_q ,
        \]
        avec égalité si et seulement si les vecteurs $(\abs{a_i}^p)$
        et $(\abs{b_i}^q)$ sont proportionnels \emph{(normaliser
        $\norm a_p = \norm b_q = 1$ et appliquer Young terme à
        terme)}.
  \item Identifier les cas particuliers~: $p = q = 2$
        (Cauchy--Schwarz), et le couple extrême $(p, q) = (1,
        \infty)$~: énoncer et démontrer $\sum\abs{a_ib_i} \leq
        \norm a_1\norm b_\infty$.
  \item (Minkowski) Pour $p \geq 1$, démontrer
        \[
        \norm{a + b}_p \leq \norm a_p + \norm b_p
        \]
        \emph{(écrire $\abs{a_i + b_i}^p \leq \abs{a_i +
        b_i}^{p-1}(\abs{a_i} + \abs{b_i})$ et appliquer Hölder
        à chaque produit)}. Conclure~: $\norm\cdot_p$ est une \omterm{def:b2:nvs:norm}{norme}
        sur $\R^n$ pour tout $p \in \intco{1}{+\infty}$,
        complétant le tableau du~\cref{ch:b2:nvs}.
  \item Versions intégrales~: pour $f, g$ \omterm{def:b2:metric:continuity}{continues} sur
        $\intcc{a}{b}$, énoncer et démontrer Hölder et Minkowski
        pour $\norm f_p = \bigl(\int_a^b\abs
        f^p\bigr)^{1/p}$ \emph{(mêmes démonstrations, avec la stricte
        positivité de l'intégrale pour la discussion de l'égalité)}.
  \item Démontrer la monotonie $\norm a_q \leq \norm a_p$ pour $1
        \leq p \leq q$, la limite $\norm a_p \to
        \norm a_\infty$ quand $p \to \infty$, et la comparaison
        inverse avec la constante optimale~:
        \[
        \norm a_p \leq n^{\frac1p - \frac1q}\,\norm a_q
        \]
        \emph{(Hölder contre le vecteur constant)}. Identifier
        les vecteurs réalisant chaque égalité.
  \item (Interpolation) Pour $1 \leq p < r < q$ et $\theta \in
        \intoo01$ avec $\frac1r = \frac\theta p +
        \frac{1-\theta}q$, démontrer
        \[
        \norm a_r \leq \norm a_p^{\theta}\,
        \norm a_q^{1-\theta}
        \]
        \emph{(appliquer Hölder avec les exposants $\frac{p}{\theta
        r}$ et $\frac{q}{(1-\theta)r}$ à $\abs{a_i}^{\theta
        r}\abs{a_i}^{(1-\theta)r}$)}.
\end{enumerate}

\medskip
\noindent\textbf{Partie III --- La chaîne des moyennes de puissances,
complète.} Pour $p \neq 0$ posons $M_p = \bigl(\sum_i\lambda_i
x_i^p\bigr)^{1/p}$ ($x_i > 0$), et $M_0 = \prod_i
x_i^{\lambda_i}$.
\begin{enumerate}[resume]
  \item Démontrer que $p \mapsto M_p$ est croissante sur tout
        $\R^*$~: traiter $p < q < 0$ par l'identité réciproque
        $M_{-p}(x) = M_p(1/x)^{-1}$, et faire le pont à travers $0$
        en montrant $M_p \leq M_0 \leq M_q$ pour $p < 0 < q$
        \emph{(appliquer la concavité de $\ln$ à $x_i^q$, et
        l'inégalité renversée pour les exposants négatifs)}.
  \item Démontrer les limites $M_p \to \max_i x_i$ quand $p \to
        +\infty$ et $M_p \to \min_i x_i$ quand $p \to -\infty$.
  \item Écrire la chaîne $\min \leq \mathrm{HM} \leq
        \mathrm{GM} \leq \mathrm{AM} \leq \mathrm{QM} \leq
        \max$ pour des poids égaux, et démontrer la conséquence
        classique~: pour des $a_1, \dots, a_n$ positifs,
        \[
        \Bigl(\sum_i a_i\Bigr)\Bigl(\sum_i\frac1{a_i}\Bigr)
        \geq n^2 .
        \]
  \item Relier les moyennes aux \omterm{def:b2:nvs:norm}{normes}~: pour des poids égaux
        $\lambda_i = \frac1n$, $M_p(x) = n^{-1/p}\norm x_p$.
        Concilier les deux monotonies --- les moyennes
        \emph{croissent} avec $p$ tandis que les \omterm{def:b2:nvs:norm}{normes}
        \emph{décroissent} (question 10) --- en une phrase sur le
        facteur $n^{-1/p}$.
  \item Déterminer les cas d'égalité tout au long de la chaîne de
        la question 14 (poids positifs)~: l'égalité en un point
        quelconque force tous les $x_i$ égaux --- la stricte
        convexité paie.
\end{enumerate}

\medskip
\noindent\textbf{Partie IV --- Dividendes.}
\begin{enumerate}[resume]
  \item (Young avec un réglage) Pour $a, b \geq 0$ et $\varepsilon >
        0$, démontrer
        \[
        ab \leq \varepsilon\,\frac{a^p}{p} +
        \varepsilon^{-q/p}\,\frac{b^q}{q},
        \]
        et le cas de travail $ab \leq \varepsilon a^2 +
        \frac{b^2}{4\varepsilon}$~: l'astuce d'absorption utilisée
        dans toute l'analyse.
  \item (Vers Carleman) Soit $c_k = \frac{(k+1)^k}{k^{k-1}}$.
        Démontrer l'identité télescopique $\prod_{k=1}^{n}c_k =
        (n+1)^n$, et en déduire, par l'inégalité
        arithmético-géométrique appliquée aux nombres $c_ka_k$,
        \[
        (a_1a_2\cdots a_n)^{1/n} \leq
        \frac{1}{n(n+1)}\sum_{k=1}^{n} c_k a_k
        \qquad (a_k > 0).
        \]
  \item (Inégalité de Carleman) Sommer sur $n$, échanger l'\omterm{def:b2:structures:generated}{ordre}
        de sommation (\omterm{def:b2:series:summable}{familles sommables} positives,
        le~\cref{thm:b2:series:fubini}), et utiliser $\sum_{n \geq
        k}\frac{1}{n(n+1)} = \frac1k$ et $c_k/k = \bigl(1 +
        \frac1k\bigr)^k < \eu$ pour conclure~: pour toute série
        $\sum a_k$ convergente à termes positifs,
        \[
        \sum_{n=1}^{\infty}(a_1a_2\cdots a_n)^{1/n}
        \;\leq\; \eu\sum_{k=1}^{\infty}a_k .
        \]
  \item Pour $f$ \omterm{def:b2:metric:continuity}{continue} et positive sur $\intcc{0}{1}$,
        démontrer
        \[
        \Bigl(\int_0^1 f\Bigr)\Bigl(\int_0^1\frac1f\Bigr)
        \geq 1,
        \]
        avec égalité si et seulement si $f$ est constante
        \emph{(Cauchy--Schwarz sur $\sqrt f\cdot\frac1{\sqrt
        f}$)}.
  \item (Géométrie des boules) À l'aide du cas d'égalité de
        Minkowski, montrer que pour $1 < p < \infty$ la sphère
        unité de $\norm\cdot_p$ ne contient aucun segment (la \omterm{def:b2:nvs:norm}{norme}
        est strictement convexe au sens du~\cref{pb:b2:nvs:1}),
        tandis que pour $p = 1$ et $p = \infty$ elle en contient~:
        exhiber les morceaux plats.
\end{enumerate}

\medskip
\noindent\textbf{Partie V --- Dualité et synthèse.}
\begin{enumerate}[resume]
  \item (Hölder comme dualité) Démontrer que pour tout $a \in
        \R^n$,
        \[
        \norm a_p = \max_{\norm b_q \leq 1}\ \sum_i a_ib_i ,
        \]
        en exhibant explicitement un $b$ maximisant. (La $p$-norme
        est la duale de la $q$-norme --- le germe en dimension finie
        de la dualité $L^p$.)
  \item (Moments) Soit $X$ une variable aléatoire prenant un nombre
        fini de valeurs positives $x_i$ avec les probabilités
        $\lambda_i$. Reformuler la question 12 ainsi~: $r \mapsto
        \E[X^r]^{1/r}$ est croissante --- l'inégalité des moments
        (Lyapunov), à réutiliser au~\cref{ch:b2:randomvar}.
  \item Résoudre avec des outils nommés, en deux lignes chacun~: (i)
        pour $a, b, c$ positifs~: $a^3 + b^3 + c^3 \geq
        \frac{(a+b+c)^3}{9}$~; (ii) pour $x_1, \dots,
        x_n$ positifs~: $\bigl(\sum_i\sqrt{x_i}\bigr)^2 \leq
        n\sum_i x_i$.
  \item (Synthèse) Tracer la généalogie en cinq phrases~: de la
        définition par corde au lemme des pentes~; des pentes aux
        droites d'appui à Jensen~; de la concavité de $\ln$ à Young
        à Hölder à Minkowski aux $p$-normes~; de Jensen à la chaîne
        des moyennes de puissances aux moments~; de l'inégalité
        arithmético-géométrique à Carleman. Nommer les sommets
        (Hölder--Minkowski~; Carleman), et dire où se dirige la
        boîte à outils~: les espaces $L^p$ du volume de troisième
        année, dont les axiomes sont exactement les questions 6 et 8.
\end{enumerate}
\end{problem}
