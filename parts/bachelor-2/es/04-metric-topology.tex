\chapter{Topología de espacios métricos}\label{ch:b2:metric}

La topología de la línea real (volumen del año 1) se generaliza, casi
sin cambiar una palabra, a cualquier conjunto equipado con una distancia. el
la ganancia es enorme: secuencias de funciones, matrices, curvas --- todo
se convierten en puntos de \omterm{def:b2:metric:def}{espacios métricos}, y los tres pilares demostrados aquí
--- \omterm{def:b2:metric:complete}{lo completo} con el teorema del punto fijo de Banach, \omterm{def:b2:metric:compact}{compacidad},
\omterm{def:b2:metric:connected}{conectividad} --- se aplican a ellos de manera uniforme. Este capítulo es el
columna vertebral de toda la mitad del análisis del libro.

\section{Espacios métricos}

\begin{definition}\label{def:b2:metric:def}
Un \emph{espacio métrico}\index{espacio métrico} es un conjunto $X$ con un mapa $d
\colon X \times X \to \R_+$tal que, para todo $x, y, z$:
\[
d(x,y) = 0 \iff x = y,
\qquad
d(x,y) = d(y,x),
\qquad
d(x,z) \leq d(x,y) + d(y,z).
\]
Bolas: $B(a, r) = \{x : d(a,x) < r\}$ (\omterm{def:b2:metric:topology}{abierto}), $\overline B(a,r) =
\{x : d(a,x) \leq r\}$(cerrada). Un subconjunto $A \subseteq X$ se convierte en un
espacio métrico con la distancia inducida.
\end{definition}

\begin{example}\label{ex:b2:metric:examples}
$\R$ con $\abs{x - y}$; $\R^n$ con cualquiera de
\[
d_1(x,y) = \sum_i \abs{x_i - y_i},
\quad
d_2(x,y) = \Bigl(\sum_i (x_i - y_i)^2\Bigr)^{1/2},
\quad
d_\infty(x,y) = \max_i \abs{x_i - y_i};
\]
el conjunto $C(\intcc{a}{b})$ de \omterm{def:b2:metric:continuity}{continuo} funciona con el
\emph{distancia de hundimiento} $d_\infty(f, g) = \sup_{\intcc{a}{b}} \abs{f -
g}$(finito: $f - g$ está acotado); cualquier conjunto con el \emph{discreto}
distancia ($d(x,y) = 1$ para $x \neq y$). Distancias provenientes de normas.
son el tema de \cref{ch:b2:nvs}.
\end{example}

\begin{definition}[Topología de un espacio métrico]\label{def:b2:metric:topology}
$U \subseteq X$ es \emph{abierto}\index{conjunto abierto!en un espacio métrico}
cuando cada punto de $U$ es el centro de una bola contenida en $U$;
$F$ es \emph{cerrado} cuando su complemento está abierto. barrios,
interior, cierre, densidad, límite se definen exactamente como en el
línea real (volumen del año 1), con bolas reemplazando intervalos, y el
declaraciones probadas allí --- uniones/intersecciones de conjuntos abiertos,
Caracterizaciones de interior y cierre, cierre como menor.
superconjunto cerrado --- continuar con las mismas pruebas. Las bolas abiertas son
las bolas abiertas y cerradas están cerradas (desigualdad triangular).
\end{definition}

\begin{example}[Interior, cierre, delimitación en un conjunto]\label{ex:b2:metric:intclosure}
En $\R$, deje $A = \intoc{0}{1} \cup \{2\}$. Interior:
$\intoo{0}{1}$ --- alrededor de cualquier $x \in \intoo01$ una bolita
permanece en $A$; alrededor de $1$, cada bola $\intoo{1-r}{1+r}$ gotea
fuera de $A$ a la derecha, por lo que $1$ no es interior; y el
aislado $2$ tampoco es interior. Cierre:
$\intcc{0}{1} \cup \{2\}$ (el punto $0$ es un límite de $A$,
no se añade nada más). Límite (cierre menos interior):
$\{0, 1, 2\}$. Nótense las asimetrías que vale la pena recordar: una
El punto final puede pertenecer a un conjunto sin ser interior ($1$), puede
ser adherente sin pertenencia ($0$), y un punto aislado es
su propio límite ($2$). La misma contabilidad aparece textualmente en
cualquier \omterm{def:b2:metric:def}{espacio métrico}, con bolas en lugar de intervalos.
\end{example}

\begin{definition}[Límites, continuidad]\label{def:b2:metric:continuity}
$x_n \to x$ en $X$ cuando $d(x_n, x) \to 0$. Un mapa $f \colon X \to Y$
entre \omterm{def:b2:metric:def}{espacios métricos} es \emph{continuo}\index{continuo} en $a$ cuando
\[
\forall \varepsilon > 0,\ \exists\delta > 0,\quad
d_X(x, a) \leq \delta \implies d_Y\bigl(f(x), f(a)\bigr) \leq
\varepsilon ;
\]
equivalentemente (la misma prueba que en $\R$), $f(x_n) \to f(a)$ para cada
secuencia $x_n \to a$. $f$ es \emph{Lipschitz}\index{Lipschitz} con constante $k$ cuando
$d_Y(f(x), f(y)) \leq k\, d_X(x, y)$ siempre --- luego uniformemente
continuo, por lo tanto continuo.
\end{definition}

\begin{theorem}[Caracterización global de la continuidad]\label{thm:b2:metric:globalcontinuity}
$f \colon X \to Y$ es \omterm{def:b2:metric:continuity}{continuo} (en cada punto) si y sólo si el
La preimagen de cada conjunto \omterm{def:b2:metric:topology}{abierto} es \omterm{def:b2:metric:topology}{abierto} --- si la preimagen de cada
El conjunto cerrado está cerrado.
\end{theorem}

\begin{proof}
($\Rightarrow$) Sea $V \subseteq Y$ \omterm{def:b2:metric:topology}{abierto} y $a \in f^{-1}(V)$:
alguna pelota $B(f(a), \varepsilon) \subseteq V$; \omterm{def:b2:metric:continuity}{continuidad} en $a$
proporciona $\delta$ con $f(B(a, \delta)) \subseteq B(f(a),
\varepsilon)$, por lo que $B(a, \delta) \subseteq f^{-1}(V)$.

($\Leftarrow$) Dados $a$ y $\varepsilon$: $f^{-1}\bigl(B(f(a),
\varepsilon)\bigr)$es \omterm{def:b2:metric:topology}{abierto} y contiene $a$, por lo tanto contiene una bola.
$B(a, \delta)$: esa es la definición de \omterm{def:b2:metric:continuity}{continuidad} en $a$. Cerrado
conjuntos: complementos (\cref{prop:b2:structures:images}).
\end{proof}

\section{Espacios completos}

\begin{definition}\label{def:b2:metric:complete}
Una secuencia $(x_n)$ es \emph{cauchy} cuando $\sup_{p, q \geq N} d(x_p,
x_q) \to 0$ es $N \to \infty$. Un \omterm{def:b2:metric:def}{espacio métrico} es
\emph{completo}\index{espacio métrico completo} cuando cada Cauchy
la secuencia converge. Convergente $\Rightarrow$ Cauchy siempre; cerrado
Los subconjuntos de espacios completos son completos y los subconjuntos completos de cualquier
los espacios están cerrados (las mismas pruebas que en $\R$: Volumen Año 1).
\end{definition}

\begin{example}[Cauchy sin límite]\label{ex:b2:metric:cauchynolimit}
En $X = \Q$ con la distancia habitual, los truncamientos decimales de
$\sqrt2$,
\[
x_0 = 1,\quad x_1 = 1.4,\quad x_2 = 1.41,\quad x_3 = 1.414,
\quad\dots
\]
satisfacer $\abs{x_p - x_q} \leq 10^{-\min(p,q)}$: Cauchy en $\Q$.
Un límite en $\Q$ también sería el límite en $\R$, es decir
$\sqrt2 \notin \Q$: no existe límite en $X$. Lo incompleto es
la presencia de tales "límites fantasmas"; \omterm{def:b2:metric:complete}{lo completo} de $\R$
fue diseñado en el volumen del Año 1 precisamente para dar a cada
Cauchy secuencia una casa.
\end{example}

\begin{theorem}\label{thm:b2:metric:rncomplete}
$\R^n$ (cualquiera de las tres distancias de
\cref{ex:b2:metric:examples}) y $\bigl(C(\intcc{a}{b}),
d_\infty\bigr)$ son \omterm{def:b2:metric:complete}{completo}.
\end{theorem}

\begin{proof}
$\R^n$: una secuencia de Cauchy es Cauchy en cada coordenada (cada
$\abs{x_i - y_i} \leq d(x,y)$ para las tres distancias), por lo que cada
la coordenada converge (\omterm{def:b2:metric:complete}{lo completo} de $\R$, volumen del año 1), y
la convergencia coordinada implica convergencia para $d_\infty$
(finitos coordenadas), por lo tanto, para los tres (los tres
las distancias se dominan entre sí dentro de factores constantes: $d_\infty
\leq d_2 \leq d_1 \leq n\,d_\infty$).

$C(\intcc{a}{b})$: sea $(f_n)$ $d_\infty$-Cauchy. Para cada $x$,
$(f_n(x))$ es Cauchy en $\R$ ($\abs{f_p(x) - f_q(x)} \leq
d_\infty(f_p, f_q)$): converge con algún $f(x)$. Pasando a la
límite en $\abs{f_p(x) - f_q(x)} \leq \varepsilon$ (válido para $p, q
\geq N_\varepsilon$, todo $x$) como $q \to \infty$: $\abs{f_p(x) -
f(x)} \leq \varepsilon $para todo $x$, es decir $d_\infty(f_p, f) \leq
\varepsilon$: convergencia uniforme. El límite es \omterm{def:b2:metric:continuity}{continuo}: dado
$\varepsilon$, elija $p$ con $\sup\abs{f_p - f} \leq \varepsilon$,
luego use \omterm{def:b2:metric:continuity}{continuidad} de $f_p$ en $a$ y la división de tres términos
\[
\abs{f(x) - f(a)} \leq \abs{f(x) - f_p(x)} + \abs{f_p(x) - f_p(a)}
+ \abs{f_p(a) - f(a)} \leq 3\varepsilon
\]
para $x$ cerca de $a$. (Este ``argumento $3\varepsilon$'' devuelve como
el teorema del límite uniforme de \cref{ch:b2:funcseq}.)
\end{proof}

\begin{example}[Conjuntos abiertos y cerrados reconocidos por continuidad]\label{ex:b2:metric:recognize}
La caracterización global
(\cref{thm:b2:metric:globalcontinuity}) es la herramienta diaria para
contabilidad topológica. En $\R^2$: el conjunto $\{(x, y) : x^2 + y^2
< 1,\ y > x^3\}$ es \omterm{def:b2:metric:topology}{abierto} --- es $g^{-1}(\intoo{-\infty}{1})
\cap h^{-1}(\intoo{0}{+\infty})$para \omterm{def:b2:metric:continuity}{continuo}$g(x,y) =
x^2 + y^2$y $h(x, y) = y - x^3$, una intersección de dos \omterm{def:b2:metric:topology}{abierto}
preimágenes. En $\bigl(C(\intcc01), d_\infty\bigr)$: el conjunto de
funciones con $f(0) = f(1)$ y $\int_0^1 f = 0$ están cerradas ---
la preimagen de $\{(0,0)\}$ bajo el mapa \omterm{def:b2:metric:continuity}{continuo} $f \mapsto
\bigl(f(0) - f(1),\ \int_0^1 f\bigr)$en $\R^2$ (cada
La coordenada es $1$-Lipschitz, como en \cref{exo:b2:metric:3}). el
El método nunca hace un dibujo: exhiba un mapa \omterm{def:b2:metric:continuity}{continuo}, lea el
establecido como preimagen, cite el teorema.
\end{example}

\begin{example}[Un conjunto cerrado definido por infinitas condiciones]\label{ex:b2:metric:lipclosed}
En $\bigl(C(\intcc01), d_\infty\bigr)$, el conjunto
\[
L = \{f : \abs{f(x) - f(y)} \leq \abs{x - y}
\ \text{for all } x, y\}
\]
de funciones $1$-Lipschitz está cerrado, aunque está cortado por
innumerables condiciones: para cada par fijo $(x, y)$, el
mapa $f \mapsto \abs{f(x) - f(y)} - \abs{x - y}$ es \omterm{def:b2:metric:continuity}{continuo}
(las evaluaciones son $1$-Lipschitz), por lo que cada condición
define un conjunto cerrado, y $L$ es el \emph{intersección} de
esta familia --- una intersección arbitraria de conjuntos cerrados es
cerrado. La misma plantilla certifica el cierre para monótonos.
funciones, funciones convexas, funciones acotadas por un $g$ fijo:
Los límites uniformes heredan toda propiedad expresable como familia.
de restricciones puntuales cerradas. ¿Qué hacen los límites uniformes?
\emph{no} hereda automáticamente --- diferenciabilidad, por
uno --- es exactamente por lo que debe trabajar \cref{ch:b2:funcseq}.
\end{example}

\begin{theorem}[Teorema del punto fijo de Banach]\label{thm:b2:metric:banach}
Sea $X$ un \omterm{def:b2:metric:complete}{espacio métrico completo} no vacío y $f \colon X \to X$ un
\emph{contracción}: \omterm{def:b2:metric:continuity}{Lipschitz} con constante $k < 1$. Entonces $f$ tiene un
punto fijo único $\ell$, y cada órbita $x_{n+1} = f(x_n)$
converge a $\ell$, con\index{Teorema del punto fijo de Banach}
\[
d(x_n, \ell) \leq \frac{k^n}{1 - k}\, d(x_1, x_0) .
\]
\end{theorem}

\begin{proof}
Unicidad: dos puntos fijos están a una distancia $\leq k$ veces ella misma.
Existencia: $d(x_{n+1}, x_n) \leq k^n d(x_1, x_0)$ por inducción, por lo que
para $q > p$,
\[
d(x_q, x_p) \leq \sum_{j=p}^{q-1} d(x_{j+1}, x_j)
\leq d(x_1, x_0) \sum_{j \geq p} k^j
= \frac{k^p}{1-k}\, d(x_1, x_0) \xrightarrow[p\to\infty]{} 0 :
\]
Cauchy, por lo tanto convergente a algún $\ell$; \omterm{def:b2:metric:continuity}{continuidad} de $f$ pasa
$x_{n+1} = f(x_n)$ hasta el límite: $\ell = f(\ell)$. El error obligado
es la estimación mostrada con $q \to \infty$.
\end{proof}

\begin{example}[Una ecuación integral]\label{ex:b2:metric:integralequation}
En $X = C(\intcc{0}{1})$ (\omterm{def:b2:metric:complete}{completo},
\cref{thm:b2:metric:rncomplete}), considere $T(f)(x) = 1 + \frac12
\int_0^x f(t)\,\dd t$. Para $f, g \in X$:
\[
\abs{T(f)(x) - T(g)(x)} \leq \frac12 \int_0^x \abs{f - g} \leq
\frac12\, d_\infty(f, g),
\]
entonces $T$ es una contracción $\frac12$: tiene un \omterm{def:b2:metric:continuity}{continuo} único fijo
punto --- la solución de $f' = \frac f2$, $f(0) = 1$, es decir
$\eu^{x/2}$. Este esquema, industrializado, se convierte en el
Teorema de Cauchy--Lipschitz de \cref{ch:b2:diffeq}.
\end{example}

\begin{example}[Un punto fijo numérico: $x = \cos x$]\label{ex:b2:metric:cosfix}
En el \omterm{def:b2:metric:complete}{completo} $X = \intcc{0}{1}$, el mapa $f = \cos$ envía $X$
en $\intcc{\cos 1}{1} \subseteq X$ y es una contracción: por el
desigualdad del valor medio,
\[
\abs{\cos x - \cos y} \leq \bigl(\sup_{\intcc01}\abs{\sin}\bigr)
\abs{x - y} = (\sin 1)\abs{x - y},
\qquad \sin 1 \approx 0.841 < 1 .
\]
Banach: una solución única de $x = \cos x$ en $\intcc01$ (de ahí en
$\R$: cualquier punto fijo real se encuentra en $\intcc{-1}{1}$, luego en
$\intcc{\cos 1}{1}$ después de una aplicación), y la iteración
$x_{n+1} = \cos x_n$ converge a él desde cualquier inicio: $x_\infty
\approx 0.739085$, el famoso número que se obtiene martillando el
Tecla coseno de una calculadora. El límite de error predice
Decaimiento de $(\sin1)^n/(1 - \sin1)$ --- aproximadamente un dígito por $13$
prensas; El límite a posteriori del fin de semana de este capítulo.
El problema (pregunta 14) certifica cada paso sobre la marcha.
\end{example}

\section{Compacidad}

\begin{definition}\label{def:b2:metric:compact}
A \omterm{def:b2:metric:def}{espacio métrico} $X$ es \emph{compacto}\index{espacio métrico compacto}
cuando cada secuencia en $X$ tiene una subsecuencia convergente \emph{in}
$X$ (la propiedad Bolzano-Weierstrass). Un subconjunto es compacto cuando
lo mismo ocurre con la distancia inducida.
\end{definition}

\begin{theorem}[Primeras propiedades]\label{thm:b2:metric:compactprops}
\begin{enumerate}
 \item Un subconjunto \omterm{def:b2:metric:compact}{compacto} está cerrado y acotado; un subconjunto cerrado de un
 El espacio \omterm{def:b2:metric:compact}{compacto} es \omterm{def:b2:metric:compact}{compacto}.
 \item En $\R^n$, se mantiene lo contrario: \omterm{def:b2:metric:compact}{compacto} $\iff$ cerrado y
 acotado.
 \item Una imagen \omterm{def:b2:metric:continuity}{continuo} de un espacio \omterm{def:b2:metric:compact}{compacto} es \omterm{def:b2:metric:compact}{compacto}; un
 \omterm{def:b2:metric:continuity}{continuo} función real en un espacio \omterm{def:b2:metric:compact}{compacto} no vacío es
 limitado y alcanza sus límites.
 \item (Heine) Un mapa \omterm{def:b2:metric:continuity}{continuo} en un espacio \omterm{def:b2:metric:compact}{compacto} es uniformemente
 continuo.
 Productos \item: si $X, Y$ son \omterm{def:b2:metric:compact}{compacto}, también lo son $X \times Y$ (con
 $d\bigl((x,y),(x',y')\bigr) = d(x,x') + d(y,y')$).
\end{enumerate}
\end{theorem}

\begin{proof}
(1) Los mismos argumentos que en la línea (volumen del año 1): un escape al%
la secuencia infinita o convergente exterior no tiene subsecuencia
convergiendo en el interior; para la segunda reivindicación, extraer en el ambiente
\omterm{def:b2:metric:compact}{compacto} y uso cerrado.

(2) Las secuencias acotadas en $\R^n$ tienen componentes convergentes
subsecuencias: extraer en la primera coordenada
(Bolzano--Weierstrass en $\R$), luego, a partir de esa subsecuencia, en el
segundo, y así sucesivamente ($n$ extracciones sucesivas); la cerrazón mantiene
el límite interior.

(3) Dado $(f(x_n))$, extraiga $x_{\varphi(n)} \to x \in X$;
\omterm{def:b2:metric:continuity}{continuidad} da $f(x_{\varphi(n)}) \to f(x) \in f(X)$. Caso real:
\omterm{def:b2:metric:compact}{compacidad} de $f(X) \subseteq \R$ lo hace cerrado y acotado, y
$\sup f(X) \in f(X)$ (el sup de un conjunto es adherente a él, y
$f(X)$ está cerrado).

(4) La prueba del Año 1 se transfiere palabra por palabra; Aquí está, en métrico.
vestido. Supongamos que $f \colon X \to Y$ \omterm{def:b2:metric:continuity}{continuo} en \omterm{def:b2:metric:compact}{compacto} $X$
pero no uniformemente continuo: algunos $\varepsilon > 0$ admiten, por
cada $n$, puntos con
\[
d_X(x_n, y_n) \leq \frac{1}{n+1}
\qquad\text{and}\qquad
d_Y\bigl(f(x_n), f(y_n)\bigr) > \varepsilon .
\]
Extracto $x_{\varphi(n)} \to a \in X$; luego $y_{\varphi(n)} \to
a $también (las distancias mutuas tienden a $0$). \omterm{def:b2:metric:continuity}{Continuidad} en $a$
envía ambas secuencias de imágenes a $f(a)$, por lo que
$d_Y\bigl(f(x_{\varphi(n)}), f(y_{\varphi(n)})\bigr) \to 0$ ---
contradiciendo la brecha uniforme $> \varepsilon$. \omterm{def:b2:metric:compact}{Compacidad}
proporcionó exactamente una cosa: el punto del grupo $a$ en el que
aplique \omterm{def:b2:metric:continuity}{continuidad} liso.

(5) Extraiga las coordenadas $X$, luego nuevamente las
Coordenadas $Y$.
\end{proof}

\begin{example}[Teorema de Heine, con y sin compacidad]\label{ex:b2:metric:heinework}
En $\intcc{0}{1}$, la función $x \mapsto x^2$ es uniformemente
continuo --- Heine lo dice sin ningún cálculo, pero el directo
La estimación es instructiva:
\[
\abs{x^2 - y^2} = \abs{x + y}\,\abs{x - y} \leq 2\abs{x - y},
\]
entonces $\delta = \varepsilon/2$ funciona \emph{para cada punto en
una vez}. En $\R$ la misma función no es uniformemente continua:
con $x_n = n$ y $y_n = n + \frac1n$, la brecha $\abs{x_n - y_n}
= \frac1n \to 0$ mientras que $\abs{x_n^2 - y_n^2} = 2 + \frac1{n^2}
\geq 2$: ningún $\delta $sirve a $\varepsilon = 1$. el
El mecanismo es visible: la constante local \omterm{def:b2:metric:continuity}{Lipschitz} $\abs{x + y}$.
está acotado en un \omterm{def:b2:metric:compact}{compacto} y no acotado en $\R$ --- Heine
El teorema es exactamente la afirmación de que \omterm{def:b2:metric:compact}{compacidad} limita tales
constantes locales de manera uniforme.
\end{example}

\begin{method}[Demostrar que un conjunto es compacto]\label{met:b2:metric:compactness}
Tres rutas, en orden de frecuencia. (1) \emph{ambiente
reconocimiento:} en $\R^n$ (o cualquier norma de dimensión finita
espacio, \cref{ch:b2:nvs}), verificar cerrado --- normalmente como
preimagen, \cref{ex:b2:metric:recognize} --- y acotada. (2)
\emph{Herencia:} es un subconjunto cerrado de un espacio \omterm{def:b2:metric:compact}{compacto} conocido.
\omterm{def:b2:metric:compact}{compacto}; una unión finita o un producto de \omterm{def:b2:metric:compact}{compactos} es \omterm{def:b2:metric:compact}{compacto}; un
La imagen \omterm{def:b2:metric:continuity}{continuo} de un \omterm{def:b2:metric:compact}{compacto} es \omterm{def:b2:metric:compact}{compacto}. (3) \emph{desnudo
manos:} extrae una subsecuencia convergente de un arbitrario
secuencia --- generalmente por extracciones sucesivas coordinadas por
coordinar. Para demostrar la compacidad \emph{no}, un testigo
basta: una secuencia sin subsecuencia convergente, la mayoría de las veces
puntos a distancia mutua $\geq \varepsilon$.
\end{method}

\begin{example}[Distancias entre conjuntos: la compacidad se gana la vida]\label{ex:b2:metric:setdistance}
Sea $K$ \omterm{def:b2:metric:compact}{compacto}, $F$ cerrado, $K \cap F = \emptyset$ en un
\omterm{def:b2:metric:def}{espacio métrico}. entonces
\[
d(K, F) = \inf\,\{d(x, y) : x \in K,\ y \in F\} > 0 :
\]
la función $x \mapsto d(x, F)$ es \omterm{def:b2:metric:continuity}{continuo}
(\cref{exo:b2:metric:11}) y positivo en $K$ ($d(x, F) = 0$
pondría $x \in \overline F = F$), por lo que alcanza un resultado positivo
mínimo en el \omterm{def:b2:metric:compact}{compacto} $K$
(\cref{thm:b2:metric:compactprops}~(3)). \omterm{def:b2:metric:compact}{Compacidad} no es
decorativo: para dos juegos \emph{cerrado} el mínimo puede desaparecer
sin ser alcanzado --- en $\R^2$, la hipérbola $F_1 = \{xy
= 1\}$y el eje $F_2 = \{y = 0\}$ son conjuntos cerrados disjuntos
con $d(F_1, F_2) = 0$ (los puntos $(n, \frac1n)$ se acercan al
eje). Escape al infinito es exactamente lo que \omterm{def:b2:metric:compact}{compacidad} prohíbe.
\end{example}

\begin{theorem}[Borel--Lebesgue]\label{thm:b2:metric:borellebesgue}
A \omterm{def:b2:metric:def}{espacio métrico} $X$ es \omterm{def:b2:metric:compact}{compacto} si y sólo si cada portada de $X$ por
Los conjuntos \omterm{def:b2:metric:topology}{abierto} tienen una subcubierta \emph{finito}.\index{Borel--Lebesgue
teorema}
\end{theorem}

\begin{proof}
($\Leftarrow$) Supongamos que $(x_n)$ no tiene una subsecuencia convergente. Nosotros
afirma que cada $x \in X$ tiene una bola $B(x, r_x)$ que contiene $x_n$
sólo para un número finito de índices $n$: de lo contrario, cada bola
$B(x, \frac1{k+1})$ contendría infinitos términos, y
elegir índices
\[
\varphi(0) < \varphi(1) < \varphi(2) < \cdots
\quad\text{with}\quad
x_{\varphi(k)} \in B\Bigl(x, \frac{1}{k+1}\Bigr)
\]
(posible en cada paso precisamente porque hay infinitos
quedan candidatos) construiría una subsecuencia que convergería a $x$.
Las bolas $B(x, r_x)$ cubren $X$; si un número finito de ellos
cubierto $X$, el conjunto de índices $\N$ sería una unión finita de
conjuntos finitos: absurdos.

($\Rightarrow$) Dos pasos. \emph{Número de Lebesgue:} para una funda \omterm{def:b2:metric:topology}{abierto}
$(U_i)$ de un \omterm{def:b2:metric:compact}{compacto} $X$, existe $\rho > 0$ tal que cada bola
de radio $\rho$ se encuentra en algún $U_i$. De lo contrario, para cada selección $n$
$x_n$ con $B(x_n, \frac{1}{n+1})$ en el número $U_i$; extraer
$x_{\varphi(n)} \to x \in U_{i_0} \supseteq B(x, r)$; para $n$ grande,
$B(x_{\varphi(n)}, \frac{1}{\varphi(n)+1}) \subseteq B(x, r)
\subseteq U_{i_0}$: contradicción. \emph{Limitación total:} para
cada $\rho > 0$, un número finito de bolas de radio $\rho$ cubren $X$.
De lo contrario, elija inductivamente $x_{n+1}$ fuera de $B(x_0, \rho) \cup
\dots \cup B(x_n, \rho)$: la secuencia tiene distancias por pares $\geq
\rho$, por lo que no hay Cauchy --- por lo tanto no hay subsecuencia --- convergente:
contradicción. Combinando: cubrir $X$ por un número finito de bolas de radio
$\rho$ (el número de Lebesgue), cada uno dentro de algún $U_i$: un finito
subcubierta.
\end{proof}

\begin{example}[Una roja $\varepsilon$, contada]\label{ex:b2:metric:epsnet}
Limitación total (de la prueba de
\cref{thm:b2:metric:borellebesgue}) es muy concreto en
$\intcc{0}{1}$: para $\varepsilon > 0$, las bolas $\lceil
\frac{1}{2\varepsilon}\rceil $centradas en $\varepsilon,
3\varepsilon, 5\varepsilon, \dots $de radio $\varepsilon$
cúbrelo --- alrededor de bolas $\frac1{2\varepsilon}$, y sin tapa
puede funcionar con menos de $\frac{1}{2\varepsilon}$ de ellos (cada
longitud de las cubiertas de bolas como máximo $2\varepsilon$). En $\intcc01^2$
el conteo de cuadrados para ordenar $\varepsilon^{-2}$: cubriendo números
crecer como $\varepsilon^{-d}$ en la dimensión $d$ --- una cuantitativa
cara de \omterm{def:b2:metric:compact}{compacidad}, y la razón por la que el universo de dimensión infinita
bolas unitarias de \cref{ch:b2:nvs} (donde no hay finitos
$\frac13$-net existe) no puede ser \omterm{def:b2:metric:compact}{compacto}.
\end{example}

\begin{example}[Compacidad de lectura en portadas]\label{ex:b2:metric:covers}
El intervalo \omterm{def:b2:metric:topology}{medio abierto} $\intoc{0}{1}$ está cubierto por los conjuntos \omterm{def:b2:metric:topology}{abierto}.
$U_n = \intoo{\frac1n}{2}$, $n \geq 1$; cualquier subfamilia finita tiene un
índice más grande $N$ y falta $\intoc{0}{\frac1N}$: no finito
subcubierta, por lo que $\intoc{0}{1}$ no es \omterm{def:b2:metric:compact}{compacto} --- que el
La definición secuencial ve a través de $x_n = \frac1n$, cuyo límite
$0$ se escapa. Por otro lado, añadiendo el punto único $0$
repara ambos diagnósticos a la vez: en $\intcc{0}{1}$ cada una de esas coberturas
debe contener un conjunto que contenga $0$, que se traga un conjunto inicial completo
segmento, y un número finito de conjuntos terminan el resto. los dos idiomas
de \cref{thm:b2:metric:borellebesgue} siempre falla o tiene éxito
juntos --- las cubiertas detectan el escape exactamente donde lo hacen las secuencias.
\end{example}

\begin{remark}[Perspectivas dentro de este volumen]\label{rem:b2:metric:perspectives}
Este capítulo es el muro de carga del volumen; mira donde
cada pilar lleva peso. \emph{\omterm{def:b2:metric:complete}{Lo completo}}: el Cauchy
El criterio se convierte en la prueba de convergencia para series en Banach.
espacios (\cref{ch:b2:series}), convergencia uniforme en
\cref{ch:b2:funcseq} es exactamente convergencia en el \omterm{def:b2:metric:complete}{completo}
$\bigl(C, d_\infty\bigr)$ y Cauchy--Lipschitz
(\cref{ch:b2:diffeq}) es el teorema del punto fijo de Banach que lleva
una ecuación integral. \emph{\omterm{def:b2:metric:compact}{Compacidad}}: prueba la
equivalencia de normas (\cref{ch:b2:nvs}), la consecución de
extremos para la optimización de \cref{ch:b2:diffcalc}, y el
existencia de mejores aproximaciones (fin de semana de \cref{ch:b2:nvs}
problema). \emph{\omterm{def:b2:metric:connected}{Conectividad}}: globaliza declaraciones locales
--- unicidad de las soluciones de ecuaciones diferenciales, la
teorema del valor intermedio en curvas (\cref{ch:b2:curves}), y
los dos componentes de $GL_n(\R)$ esa teoría de la orientación
(\cref{ch:b2:multint}) se mantendrán separados.
\end{remark}

\begin{remark}[Errores comunes]\label{rem:b2:metric:pitfalls}
(i) "Cerrado y acotado implica \omterm{def:b2:metric:compact}{compacto}" es un teorema sobre
$\R^n$, no sobre \omterm{def:b2:metric:def}{espacios métricos}: un conjunto infinito con el
la métrica discreta está cerrada y limitada en sí misma pero no es \omterm{def:b2:metric:compact}{compacto}
(\cref{exo:b2:metric:4}), y la bola unitaria cerrada de
$C(\intcc01)$ también falla (\cref{ch:b2:nvs}). (ii) \omterm{def:b2:metric:complete}{Lo completo} es
una propiedad del \emph{distancia}, no de la topología: $\R$
con $d(x,y) = \abs{\arctan x - \arctan y}$ tiene el habitual
secuencias convergentes pero incompletas
(\cref{exo:b2:metric:1}). (iii) No es necesario una biyección \omterm{def:b2:metric:continuity}{continuo}
ser un homeomorfismo --- la parametrización circular de
\cref{exo:b2:metric:7}; \omterm{def:b2:metric:compact}{compacidad} de la fuente lo repara.
(iv) El teorema de Banach necesita $k < 1$ \emph{uniformemente}: el
La condición $d(f(x), f(y)) < d(x,y)$ por sí sola no garantiza nada en un
\omterm{def:b2:metric:compact}{no compacto} espacio (\cref{exo:b2:metric:5}). (v) \omterm{def:b2:metric:connected}{Conectado} no
no implica \omterm{def:b2:metric:connected}{conectado por camino} en general --- sino para \omterm{def:b2:metric:topology}{abierto}
subconjuntos de espacios normados se encuentran en este libro, los dos están de acuerdo
(\cref{ch:b2:nvs}).
\end{remark}

\begin{remark}[Dónde se utiliza este capítulo]
En todas partes en la mitad del análisis. \omterm{def:b2:metric:complete}{Lo completo} de
$C(\intcc{a}{b})$ potencia los teoremas de convergencia de
\cref{ch:b2:funcseq} y la teoría de Cauchy-Lipschitz de
\cref{ch:b2:diffeq} (el problema del fin de semana de este capítulo demuestra la
Picard local--Lindelöf teorema ya); \omterm{def:b2:metric:compact}{compacidad} da la
equivalencia de normas en dimensión finita (\cref{ch:b2:nvs}) y
la existencia de extremos en \cref{ch:b2:diffcalc}; \omterm{def:b2:metric:connected}{conectividad}
subyace a los argumentos de valor intermedio de \cref{ch:b2:realfun}
y la globalización de unicidad para ecuaciones diferenciales. en
el volumen del año 3, \omterm{def:b2:metric:compact}{compacidad} en espacios funcionales (el
Arzelà--teorema de Ascoli) y el teorema de la categoría de Baire
(\cref{exo:b2:metric:12} aquí) se convierten en herramientas de uso diario.
\end{remark}

\begin{omfigure}
\begin{tikzpicture}[xscale=6.4, yscale=0.8]
 \draw[omDef, line width=3pt] (0,3) -- (1,3);
 \draw[omDef, line width=3pt] (0,2) -- (0.3333,2);
 \draw[omDef, line width=3pt] (0.6667,2) -- (1,2);
 \foreach \a in {0, 0.6667} {
 \draw[omDef, line width=3pt] (\a,1) -- (\a + 0.1111,1);
 \draw[omDef, line width=3pt]
 (\a + 0.2222,1) -- (\a + 0.3333,1);
 }
 \foreach \a in {0, 0.2222, 0.6667, 0.8889} {
 \draw[omThm, line width=3pt] (\a,0) -- (\a + 0.037,0);
 \draw[omThm, line width=3pt]
 (\a + 0.0741,0) -- (\a + 0.1111,0);
 }
 \node[left, font=\small] at (0,3) {$C_0$};
 \node[left, font=\small] at (0,2) {$C_1$};
 \node[left, font=\small] at (0,1) {$C_2$};
 \node[left, font=\small] at (0,0) {$C_3$};
\end{tikzpicture}

{\small Las primeras etapas del set de Cantor
(\cref{exo:b2:metric:8}): cada nivel elimina el medio \omterm{def:b2:metric:topology}{abierto}
tercio de cada segmento. La intersección $C = \bigcap_n C_n$ es
\omterm{def:b2:metric:compact}{compacto}, tiene interior vacío y longitud cero, pero es \omterm{def:b2:structures:countable}{equipotente}
a $\R$ --- y regresa como \emph{punto fijo} de un
contracción en conjuntos en el problema de fin de semana de este capítulo (pregunta
22).}
\end{omfigure}

\section{Conectividad}

\begin{definition}\label{def:b2:metric:connected}
$X$ es \emph{conectado}\index{espacio conectado} cuando no admite
partición en dos subconjuntos \omterm{def:b2:metric:topology}{abierto} no vacíos --- de manera equivalente, cuando su
sólo los subconjuntos \omterm{def:b2:metric:topology}{abierto} y cerrados son $\emptyset$ y $X$. $X$ es
\emph{conectado por camino} cuando dos puntos cualesquiera están unidos por un \omterm{def:b2:metric:continuity}{continuo}
mapa $\gamma \colon \intcc{0}{1} \to X$.
\end{definition}

\begin{theorem}\label{thm:b2:metric:connectedness}
\begin{enumerate}
 \item Los subconjuntos \omterm{def:b2:metric:connected}{conectado} de $\R$ son exactamente los intervalos.
 \item Una imagen \omterm{def:b2:metric:continuity}{continuo} de un \omterm{def:b2:metric:connected}{espacio conectado} es \omterm{def:b2:metric:connected}{conectado} ---
 de donde el teorema general del valor intermedio: a \omterm{def:b2:metric:continuity}{continuo}
 La función real en un \omterm{def:b2:metric:connected}{espacio conectado} toma cada valor entre
 dos de sus valores.
 \item \omterm{def:b2:metric:connected}{Conectado por camino} $\Rightarrow$ \omterm{def:b2:metric:connected}{conectado}. (Lo contrario falla
 en general; es válido para \omterm{def:b2:metric:topology}{abierto} subconjuntos de espacios normados,
 \cref{ch:b2:nvs}.)
\end{enumerate}
\end{theorem}

\begin{proof}
(1) Un $A$ sin intervalo pierde algo de $z$ entre dos de sus puntos:
$A = (A \cap \intoo{-\infty}{z}) \cup (A \cap \intoo{z}{+\infty})$
lo divide en dos partes \omterm{def:b2:metric:topology}{abierto} (en $A$) no vacías. Por el contrario, dejemos
$I$ sea un intervalo y $I = U \cup V$ una partición no vacía
relativamente conjuntos \omterm{def:b2:metric:topology}{abierto}; elija $a \in U$, $b \in V$, diga $a < b$ y
establezca $s = \sup\,(U \cap \intcc{a}{b})$, un punto de $\intcc{a}{b}
\subseteq I$. Si $s \in U$: entonces $s \neq b$ y apertura relativa
de $U$ pone un intervalo completo alrededor de $s$ (se cruza con $I$)
dentro de $U$ --- entonces los puntos de $U \cap \intcc{a}{b}$ exceden $s$,
contradiciendo al supremo. Si $s \in V$: apertura relativa de $V$
pone un intervalo $\intoo{s - r}{s + r} \cap I$ dentro de $V$; pero el
supremum es adherente a $U \cap \intcc{a}{b}$, que debe cumplir con eso
intervalo --- contradicción con $U \cap V = \emptyset$. (Esto es
el argumento clopen del año 1 para $\R$, ejecútelo dentro de $I$).

(2) Si $f(X) = U' \cup V'$ se divide en relativamente \omterm{def:b2:metric:topology}{abierto} no vacío
establece, luego $X = f^{-1}(U') \cup f^{-1}(V')$ divide $X$
(\cref{thm:b2:metric:globalcontinuity}). ITV: $f(X) \subseteq \R$
es \omterm{def:b2:metric:connected}{conectado}, por lo tanto un intervalo de (1).

(3) Supongamos que $X = U \cup V$, ambos \omterm{def:b2:metric:topology}{abierto} no vacíos, y se unen a $a \in U$
a $b \in V$ por un camino $\gamma$: luego $\gamma^{-1}(U),
\gamma^{-1}(V)$divide $\intcc{0}{1}$, contradiciendo (1).
\end{proof}

\begin{example}\label{ex:b2:metric:glnr}
$GL_n(\R)$ no es \omterm{def:b2:metric:connected}{conectado}: $\det$ es \omterm{def:b2:metric:continuity}{continuo} (un polinomio en
las entradas) en $\R^*$, que no es \omterm{def:b2:metric:connected}{conectado}; las preimágenes de
$\R_+^*$ y $\R_-^*$ dividen $GL_n(\R)$. (Cada pieza es de hecho
\omterm{def:b2:metric:connected}{conectado por camino} --- un ejercicio placentero más allá de nuestras necesidades.) Por
contraste $GL_n(\C)$ \emph{is} \omterm{def:b2:metric:connected}{conectado por camino}:
\cref{exo:b2:metric:10}.
\end{example}

\begin{example}[Un punto fijo solo desde la conexión]\label{ex:b2:metric:ivtfixed}
Cada \omterm{def:b2:metric:continuity}{continuo} $f \colon \intcc01 \to \intcc01$ tiene un fijo
punto --- sin hipótesis de contracción, sin iteración. considerar
$g(x) = f(x) - x$, \omterm{def:b2:metric:continuity}{continuo} en \omterm{def:b2:metric:connected}{conectado} $\intcc01$:
\[
g(0) = f(0) \geq 0,
\qquad
g(1) = f(1) - 1 \leq 0 ,
\]
y el teorema del valor intermedio
(\cref{thm:b2:metric:connectedness}~(2)) entrega un cero de
$g$, es decir\ un punto fijo de $f$. Contraste con Banach
(\cref{thm:b2:metric:banach}): aquí la existencia es topológica
y gratis, pero la unicidad y el algoritmo se pierden --- $f =
\mathrm{id}$ tiene todos los puntos fijos y la iteración de un
$f$ que no se contrae puede circular para siempre. Los dos puntos fijos
Los teoremas de este capítulo responden a diferentes preguntas con
diferentes monedas.
\end{example}

\begin{example}[$\R$ y $\R^2$ no son homeomórficos]\label{ex:b2:metric:rnotr2}
\omterm{def:b2:metric:connected}{Conectividad} es una huella digital topológica. Supongamos que $h \colon
\R^2 \to \R$ fuera un homeomorfismo (una biyección \omterm{def:b2:metric:continuity}{continuo} con
\omterm{def:b2:metric:continuity}{continuo} inversa). Eliminar un punto $a \in \R^2$: el
La restricción $h \colon \R^2\setminus\{a\} \to
\R\setminus\{h(a)\}$ sigue siendo un homeomorfismo. Pero $\R^2$ menos
un punto es \omterm{def:b2:metric:connected}{conectado por camino} --- une dos puntos cualesquiera mediante un segmento,
desviarse a lo largo de un segundo segmento a través de un punto auxiliar si
$a$ bloquea el directo --- por lo tanto \omterm{def:b2:metric:connected}{conectado}
(\cref{thm:b2:metric:connectedness}~(3)); mientras que $\R$ menos un
el punto se divide en dos medias líneas \omterm{def:b2:metric:topology}{abierto} no vacías: no \omterm{def:b2:metric:connected}{conectado}.
\omterm{def:b2:metric:connected}{Conectividad} se conserva en los mapas \omterm{def:b2:metric:continuity}{continuo}: contradicción.
El plano y la línea son realmente diferentes \emph{como
espacios topológicos} --- un hecho que la cardinalidad por sí sola
(¡Las biyecciones de estilo \cref{exo:b2:structures:3} existen!) también lo es
tosco de ver.
\end{example}

\section{Ceremonias}

\begin{exercise}[$\star$]\label{exo:b2:metric:1}
En $\R$, verifique que $\delta(x, y) = \min(1, \abs{x - y})$ y
$d(x,y) = \abs{\arctan x - \arctan y}$ son distancias. cual
¿Las secuencias convergen para cada una? ¿Es $(\R, d)$ \omterm{def:b2:metric:complete}{completo}?
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b2:metric:2}
En un \omterm{def:b2:metric:def}{espacio métrico}, demuestre que una secuencia convergente es Cauchy y
acotado, y que una secuencia de Cauchy con una subsecuencia convergente
converge. Deduzca nuevamente que \omterm{def:b2:metric:compact}{espacios métricos compactos} son \omterm{def:b2:metric:complete}{completo}.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b2:metric:3}
En $\bigl(C(\intcc{0}{1}), d_\infty\bigr)$, calcule la distancia
entre $f(x) = x$ y $g(x) = x^2$; describir la bola cerrada
$\overline B(0, 1)$; y demostrar que el conjunto $\{f : f(0) = 0\}$ es
cerrado mientras $\{f : f(0) > 0\}$ es \omterm{def:b2:metric:topology}{abierto}.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:metric:4}
Demuestre que el discreto \omterm{def:b2:metric:def}{espacio métrico} $X$ (cualquier conjunto) es \omterm{def:b2:metric:complete}{completo}, y
que es \omterm{def:b2:metric:compact}{compacto} si y sólo si $X$ es finito. ¿Qué subconjuntos son?
\omterm{def:b2:metric:connected}{conectado}?
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:metric:5}
Sea $X$ \omterm{def:b2:metric:compact}{compacto} y $f \colon X \to X$ con
\[
d\bigl(f(x), f(y)\bigr) < d(x, y) \quad \text{for all } x \neq y .
\]
Demuestre que $f$ tiene un punto fijo único \emph{(minimize $x \mapsto
d(x, f(x))$)} y dé un ejemplo sobre $X = \intco{1}{+\infty}$
(no \omterm{def:b2:metric:compact}{compacto}) sin punto fijo.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:metric:6}
(Anidado \omterm{def:b2:metric:compact}{compactos}) Sea $(K_n)$ una secuencia decreciente de valores no vacíos.
\omterm{def:b2:metric:compact}{compacto} subconjuntos de un \omterm{def:b2:metric:def}{espacio métrico}. Demuestre que $\bigcap_n K_n \neq
\emptyset $\emph{(pick $x_n \in K_n$ and extract)}. Mostrar con el ejemplo
que los conjuntos \emph{cerrado} anidados no vacíos en $\R$ pueden tener valores vacíos
intersección.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:metric:7}
Sean $K$ \omterm{def:b2:metric:compact}{compacto} y $f \colon K \to Y$ \omterm{def:b2:metric:continuity}{continuo} y biyectivo.
Demuestre que $f^{-1}$ es \omterm{def:b2:metric:continuity}{continuo} \emph{(use closed sets:
\cref{thm:b2:metric:globalcontinuity} and
\cref{thm:b2:metric:compactprops})}. Da un contraejemplo sin
\omterm{def:b2:metric:compact}{compacidad} ($\gamma(t) = (\cos t, \sin t)$ en
$\intco{0}{2\pi}$).
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:metric:8}
El \emph{conjunto de cantantes} $C$ se obtiene de $\intcc{0}{1}$ por
eliminando repetidamente \omterm{def:b2:metric:topology}{abierto} tercios medios. Demuestre que $C$ es \omterm{def:b2:metric:compact}{compacto},
tiene interior vacío, y es infinito --- de hecho \omterm{def:b2:structures:countable}{equipotente} a
$\{0,1\}^{\N}$\emph{(ternary expansions with digits $0,2$;
\cref{exo:b2:structures:3})}.
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b2:metric:9}
Sea $X$ un \emph{\omterm{def:b2:metric:compact}{compacto}} \omterm{def:b2:metric:def}{espacio métrico} y $f \colon X \to X$ un
isometría: $d(f(x), f(y)) = d(x, y)$. Demuestre que $f$ es sobreyectivo.
\emph{Hint: if $a \notin f(X)$, then $\varepsilon = d(a, f(X)) > 0$
(why?); study the orbit $a, f(a), f^2(a), \dots$ and show its
points are pairwise $\geq \varepsilon$ apart --- contradiction with
\omterm{def:b2:metric:compact}{compacidad}.}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b2:metric:10}
Demuestre que $GL_n(\C)$ es \omterm{def:b2:metric:connected}{conectado por camino}. \emph{Hint: given $A, B$
invertible, consider $p(z) = \det\bigl((1 - z)A + zB\bigr)$ for $z
\in \C$: a polynomial in $z$, not identically zero, so it has
finitely many roots; join $0$ to $1$ in $\C$ by a path avoiding
them.}
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b2:metric:11}
Para $\emptyset \neq A \subseteq X$, configure $d(x, A) = \inf_{a \in A}
d(x, a)$. Demuestre que $x \mapsto d(x, A)$es $1$-Lipschitz, que
$d(x, A) = 0$ si $x \in \overline A$, y eso para disjuntos
\emph{cerrado} no vacío establece $A, B$ la función
\[
\varphi(x) = \frac{d(x, A)}{d(x, A) + d(x, B)}
\]
está bien definido, \omterm{def:b2:metric:continuity}{continuo}, igual a $0$ exactamente en $A$ y a
$1$ exactamente en $B$ --- un ``interruptor'' \omterm{def:b2:metric:continuity}{continuo} que separa dos
conjuntos cerrados disjuntos.
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b2:metric:12}
(Baire) Sea $X$ un \omterm{def:b2:metric:complete}{espacio métrico completo} y $(U_n)_{n\geq1}$ un
secuencia de subconjuntos densos \omterm{def:b2:metric:topology}{abierto}. Demuestre que $\bigcap_n U_n$ es
denso en $X$ \emph{(inside any ball, build nested closed balls
$\overline B(x_n, r_n) \subseteq U_n$ with $r_n \to 0$ and use
\omterm{def:b2:metric:complete}{lo completo})}. Deducir que $\R$ no es una unión \omterm{def:b2:structures:countable}{contable} de
conjuntos cerrados con interior vacío, y recuperar --- otra vez --- eso
$\R$ es incontable.
\end{exercise}

\section{Problema: iteración de Picard}

\omterm{def:b2:metric:complete}{Lo completo} plus contracción es una máquina de resolución: aliméntala
ecuación escrita como un problema de punto fijo y devuelve
existencia, unicidad, un algoritmo y barras de error. este fin de semana
El problema ejecuta la máquina a máxima potencia en la ecuación $y' = f(t,
y)$: probamos el \emph{Picard--Lindelöf theorem} local (el
corazón no lineal de Cauchy--Lipschitz de \cref{ch:b2:diffeq}
teoría), observe cómo cada hipótesis se mantiene
contraejemplos y cobrar dividendos puramente métricos ---
\omterm{def:b2:metric:continuity}{continuo} dependencia de los datos, la ecuación de Kepler y la
autosimilitud del conjunto de Cantor.

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis, width=8.6cm, height=5.4cm,
 xmin=-0.05, xmax=1.45, ymin=0, ymax=4.2,
 xtick={0,1}, ytick={1,2,3}]
 \addplot[omDef!45, thick, domain=0:1.4, samples=2] {1};
 \addplot[omDef!65, thick, domain=0:1.4, samples=2] {1 + x};
 \addplot[omDef, thick, domain=0:1.4, samples=60]
 {1 + x + x^2/2};
 \addplot[omThm, very thick, domain=0:1.4, samples=120]
 {exp(x)};
 \node[font=\small, omDef] at (axis cs:1.28,0.8) {$y_0$};
 \node[font=\small, omDef] at (axis cs:1.28,2.15) {$y_1$};
 \node[font=\small, omDef] at (axis cs:1.28,2.95) {$y_2$};
 \node[font=\small, omThm] at (axis cs:1.17,3.85)
 {$\eu^{\,t}$};
\end{axis}
\end{tikzpicture}

{\small Picard itera para $y' = y$, $y(0) = 1$: cada pasada
hasta $T(y)(t) = 1 + \int_0^t y$ agrega un término de Taylor, y el
la contracción aprieta toda la secuencia uniformemente sobre
$\eu^{\,t}$.}
\end{omfigure}

\begin{problem}[{Problema del fin de semana --- el Picard--Lindelöf
teorema}]
\label{pb:b2:metric:1}
En todo momento, $t_0 \in \R$, $y_0 \in \R$, $a, b > 0$ y $f$ son
Función \omterm{def:b2:metric:continuity}{continuo} en el rectángulo $R = \intcc{t_0 - a}{t_0 +
a} \times \intcc{y_0 - b}{y_0 + b}$, delimitada por $M =
\sup_R\,\abs f $y \emph{$L$-Lipschitz en su segunda variable}:
$\abs{f(t, y) - f(t, z)} \leq L\abs{y - z}$ siempre que ambos puntos
se encuentran en $R$. conjunto
\[
h = \min\Bigl(a, \frac bM\Bigr) \quad (\text{with } h = a
\text{ if } M = 0), \qquad I = \intcc{t_0 - h}{t_0 + h}.
\]

\medskip
\noindent\textbf{Parte I --- The \omterm{def:b2:metric:complete}{completo} stage.}
\begin{enumerate}
 \item Demuestre las dos afirmaciones citadas en
 \cref{def:b2:metric:complete}: un subconjunto cerrado de un
 \omterm{def:b2:metric:complete}{espacio métrico completo} es \omterm{def:b2:metric:complete}{completo} y un subconjunto \omterm{def:b2:metric:complete}{completo}
 de cualquier \omterm{def:b2:metric:def}{espacio métrico} está cerrado. Deducir que cada cerrado
 El subconjunto de $\bigl(C(I), d_\infty\bigr)$ es un \omterm{def:b2:metric:complete}{completo
 espacio métrico}.
 \item Muestre que el mapa $C(I) \to C(I)$, $y \mapsto \bigl(t
 \mapsto \int_{t_0}^{t}y(s)\,\dd s\bigr)$, es
 $h$-Lipschitz para $d_\infty$.
 \item (Los puntos fijos se mueven menos que los mapas) Sea $g \colon X
 \to X$una contracción $k$ de un \omterm{def:b2:metric:def}{espacio métrico} con puntos fijos
 punto $\ell_g$ y $\widetilde g \colon X \to X$
 Mapa \emph{cualquier} con punto fijo $\ell_{\widetilde g}$.
 probar
 \[
 d(\ell_g, \ell_{\widetilde g}) \leq
 \frac{d\bigl(g(\ell_{\widetilde g}),
 \widetilde g(\ell_{\widetilde g})\bigr)}{1 - k}
 \leq \frac{\sup_{x \in X} d\bigl(g(x), \widetilde
 g(x)\bigr)}{1 - k}.
 \]
 \item (El truco de iteración) Sea $X$ \omterm{def:b2:metric:complete}{completo} no vacío y $g
 \colon X \to X$ un mapa --- no se supone \omterm{def:b2:metric:continuity}{continuo} ---
 de modo que alguna iteración $g^m$ sea una contracción $k$.
 Demuestre que $g$ tiene un punto fijo único $\ell$ y que
 La órbita \emph{cada} $x_{n+1} = g(x_n)$ converge a
 $\ell$. \emph{(Fixed points of $g$ are fixed points of
 $g^m$; conversely $g(\ell)$ is a fixed point of $g^m$;
 split the orbit along residues mod $m$.)}
\end{enumerate}

\medskip
\noindent\textbf{Parte II --- The Picard--Lindelöf theorem.}
\begin{enumerate}[resume]
 \item Mostrar que una función $y \colon I \to \intcc{y_0 - b}{y_0
 + b}$ es $C^1$ con $y(t_0) = y_0$y $y' = f(t, y)$ activadas
 $I$ si y sólo si es \omterm{def:b2:metric:continuity}{continuo} y satisface los
 ecuación integral
 \[
 y(t) = y_0 + \int_{t_0}^{t} f\bigl(s, y(s)\bigr)\dd s
 \qquad (t \in I).
 \]
 \item Dejemos que $X_h = \{y \in C(I) : \abs{y(t) - y_0} \leq b
 \text{ on } I\}$y $T$estén definidos por $T(y)(t) =
 y_0 + \int_{t_0}^{t}f(s, y(s))\dd s$. Demuestre que $X_h$ es
 un subconjunto cerrado no vacío de $C(I)$, por lo tanto \omterm{def:b2:metric:complete}{completo}, y
 que $T$ asigna $X_h$ a $X_h$ --- aquí es donde funciona $h \leq
 b/M$.
 \item Mostrar que $d_\infty\bigl(T(y), T(z)\bigr) \leq
 Lh\,d_\infty(y, z)$en $X_h$: si $Lh < 1$, Banach's
 El teorema ya concluye. Quitamos esta pequeñez
 condición siguiente.
 \item Demostrar por inducción en $n$:
 \[
 \abs{T^n(y)(t) - T^n(z)(t)} \leq
 \frac{\bigl(L\abs{t - t_0}\bigr)^n}{n!}\,
 d_\infty(y, z) \qquad (y, z \in X_h,\ t \in I),
 \]
 entonces alguna iteración de $T$ es una contracción. Concluir con
 pregunta 4 (\emph{the Picard--Lindelöf theorem}): la
 El problema de Cauchy $y' = f(t,y)$, $y(t_0) = y_0$ tiene exactamente
 una solución en $I = \intcc{t_0 - h}{t_0 + h}$ con
 valores en $\intcc{y_0 - b}{y_0 + b}$.
 \item Mostrar que la restricción ``con valores en $\intcc{y_0 -
 b}{y_0 + b}$'' es automática: cualquier solución del Cauchy
 problema definido en $I$ cuyo gráfico comienza en $R$ se queda
 en $\intcc{y_0 - b}{y_0 + b}$ \emph{(consider the first
 exit time and bound $\abs{y(t) - y_0}$ by $M\abs{t -
 t_0}$)}. Por lo tanto, la unicidad se mantiene entre todas las soluciones en
 $I$.
 \item Ejecute la máquina en $y' = y$, $y(0) = 1$, comenzando desde
 la constante $y^{(0)} \equiv 1$: calcula el Picard
 itera $y^{(n)}$, identifíquelos y describa el
 convergencia.
\end{enumerate}

\medskip
\noindent\textbf{Parte III --- Every hypothesis earns its keep.}
\begin{enumerate}[resume]
 \item (\omterm{def:b2:metric:continuity}{Lipschitz} falla, la unicidad falla) Para $y' =
 2\sqrt{\abs y}$, $y(0) = 0$: verifique que $y \equiv 0$
 y, para cada $c \geq 0$, la función $y_c(t) = 0$ para
 $t \leq c$, $y_c(t) = (t - c)^2$ para $t > c$, son todos
 Soluciones $C^1$ en $\R$. ¿Dónde exactamente $y \mapsto
 2\sqrt{\abs y}$ no es \omterm{def:b2:metric:continuity}{Lipschitz}?
 \item (La localidad es real) Para $y' = y^2$, $y(0) = 1$: resolver
 explícitamente, dar el intervalo máximo de existencia, y
 Calcule el mejor $h$ que el teorema puede certificar sobre todos
 opciones del rectángulo ($a$ grande, $b$ gratis): mostrar
 $h_{\max} = \sup_{b>0} \frac{b}{(1+b)^2} = \frac14$,
 mientras que la verdadera solución vive en $\intoo{-\infty}{1}$.
 \item (\omterm{def:b2:metric:complete}{Lo completo} no es decoración) En $X = \Q \cap
 \intcc{1}{2}$ con la distancia habitual, deje $g(x) =
 \frac x2 + \frac1x$. Mostrar $g(X) \subseteq X$, ese $g$
 es una contracción $\frac12$ \emph{(valor medio
 desigualdad)}, y que $g$ no tiene un punto fijo en $X$.
 ¿Qué hipótesis del teorema de Banach falla y cuál es?
 ¿El punto fijo en la finalización?
 \item (barras de error) Para una contracción $k$ $g$ en un \omterm{def:b2:metric:complete}{completo}
 espacio, demuestre la estimación \emph{a posteriori} $d(x_n,
 \ell) \leq \frac{k}{1-k}\,d(x_n, x_{n-1})$. Para garza
 mapa $g(x) = \frac x2 + \frac 1x$ en $\intcc{1}{2}$
 (punto fijo $\sqrt2$), a partir de $x_0 = \frac32$:
 ¿Cuántos pasos enlaza el \emph{a priori}?
 $\frac{k^n}{1-k}d(x_1, x_0)$ exigencia de precisión
 $10^{-6}$, ¿y cuántos pasos son suficientes en realidad?
 (Calcule $x_1, x_2, x_3$ y sus errores; el
 El límite de contracción es honesto pero pesimista.
 Heron converge cuadráticamente.)
\end{enumerate}

\medskip
\noindent\textbf{Parte IV --- \omterm{def:b2:metric:continuity}{Continuo} dependence.} En este
parte $Lh < 1$, por lo que $T$ en sí es una contracción de $X_h$ (pregunta
7); escriba $y[\,y_0\,]$ para la solución con valor inicial
$y_0$.
\begin{enumerate}[resume]
 \item (Dependencia del valor inicial) Sea $z_0$ otro
 valor inicial con $\abs{z_0 - y_0}$ lo suficientemente pequeño como para
 ambos problemas caben en el rectángulo. Usando la pregunta 3,
 probar
 \[
 d_\infty\bigl(y[y_0], y[z_0]\bigr) \leq
 \frac{\abs{y_0 - z_0}}{1 - Lh}.
 \]
 \item (Intervalos largos por encadenamiento) Supongamos que existen las soluciones.
 en un segmento largo cortado en $m$ trozos consecutivos en
 cada uno de los cuales se aplica el límite anterior con $Lh \leq
 \frac12$. Muestre que la desviación crece como máximo en un factor
 $2$ por pieza, por lo tanto $d_\infty \leq 2^m\abs{y_0 - z_0}$
 general --- un límite de longitud exponencial, el discreto
 sombra del $\eu^{L\abs{t - t_0}}$ del lema de Gronwall
 (\cref{ch:b2:diffeq}).
 \item (Dependencia del campo) Sea $g$ otro campo en
 $R$, también $L$-Lipschitz en $y$, con $\sup_R \abs{f - g}
 \leq \varepsilon$. Demostrar que el correspondiente
 las soluciones satisfacen $d_\infty \leq
 \frac{\varepsilon h}{1 - Lh}$: error de modelado
 se propaga linealmente.
 \item (Parámetros) Si una familia $f_\lambda$ de campos es
 $L$-Lipschitz en $y$ uniformemente y $\sup_R\abs{f_\lambda
 - f_\mu} \leq C\abs{\lambda - \mu}$, deduce que
 $\lambda \mapsto y_\lambda$ es \omterm{def:b2:metric:continuity}{Lipschitz} del
 espacio de parámetros en $\bigl(C(I), d_\infty\bigr)$.
 \item (Los sistemas no cuestan nada) Explique por qué las Partes I, II y IV
 mantenga textualmente $y$ con valores en $\R^n$ (sup
 distancias construidas sobre cualquiera de las distancias de
 \cref{ex:b2:metric:examples}), luego calcula todos los Picard
 itera para el sistema $y' = Ay$, $y(0) = (c_1,
 c_2)$, $A = \begin{psmallmatrix} 0 & 1\\ 0 &
 0\end{psmallmatrix}$: muestra que la iteración se convierte
 estacionario en la solución exacta después de un paso.
\end{enumerate}

\medskip
\noindent\textbf{Part V --- Metric dividends and synthesis.}
\begin{enumerate}[resume]
 \item (Perturbación de la identidad) Sea $X$ un \omterm{def:b2:metric:complete}{completo}
 etapa similar al espacio normado: tome $X = C(I)$ o $\R^n$. si
 $\eta \colon X \to X$ es $k$-Lipschitz con $k < 1$,
 demostrar que $x \mapsto x + \eta(x)$ es una biyección de $X$
 cuyo inverso es $\frac1{1-k}$-Lipschitz \emph{(for each
 $y$, apply Banach to $x \mapsto y - \eta(x)$)}. esto es
 el corazón métrico del teorema de la función inversa
 (\cref{ch:b2:diffcalc}).
 \item (ecuación de Kepler) Para $0 \leq e < 1$ y $m \in \R$,
 demostrar que $x = m + e\sin x$ tiene exactamente una solución,
 que la iteración $x_{n+1} = m + e\sin x_n$ converge
 desde cualquier inicio, y calcule: para $e = \frac12$,
 $m = 1$, ¿cuántas iteraciones garantizan un error $\leq
 10^{-3}$ por el límite a priori? (La solución es $x
 \approx 1.4987$).
 \item (El conjunto de Cantor es un punto fijo) Dejemos $S_1(x) = \frac
 x3$y $S_2(x) = \frac x3 + \frac23$ en $\R$, y dejemos
 $C$ será el conjunto de Cantor de \cref{exo:b2:metric:8}. probar
 $C = S_1(C) \cup S_2(C)$, y explícalo en una frase.
 ¿Por qué ningún conjunto \emph{otro} no vacío \omterm{def:b2:metric:compact}{compacto} satisface esto?
 ecuación (el mapa $A \mapsto S_1(A) \cup S_2(A)$ es una
 contracción para una distancia entre conjuntos \omterm{def:b2:metric:compact}{compacto} --- el
 distancia de Hausdorff, sincerada en el volumen del Año 3).
 \item (\omterm{def:b2:metric:connected}{Conectividad} globaliza la unicidad) Deje que $f$ sea local
 \omterm{def:b2:metric:continuity}{Lipschitz} en $y$ en un conjunto \omterm{def:b2:metric:topology}{abierto}, y sean $y, z$ dos
 soluciones de $y' = f(t, y)$ en un intervalo común $J$
 con $y(t_0) = z(t_0)$. Demuestre $y = z$ en $J$: demuestre que
 $\{t \in J : y(t) = z(t)\}$ no está vacío, cerrado en $J$,
 y \omterm{def:b2:metric:topology}{abierto} en $J$ (por unicidad local), y utilice el
 \omterm{def:b2:metric:connected}{conectividad} de intervalos
 (\cref{thm:b2:metric:connectedness}).
 \item (Sin pequeñez para ecuaciones lineales) Para $y' =
 \alpha(t)y + \beta(t)$con $\alpha, \beta$ \omterm{def:b2:metric:continuity}{continuo}
 en un segmento $\intcc{A}{B}$, adapte la pregunta 8 para mostrar
 el límite factorial se mantiene en el segmento \emph{entero},
 entonces la existencia y la unicidad son globales allí --- el
 caso escalar de Cauchy--Lipschitz de \cref{ch:b2:diffeq}
 teorema, sin restricción en la longitud $B - A$.
 \item (Síntesis) Una frase cada uno: qué \omterm{def:b2:metric:complete}{lo completo}
 contribuido; qué contribuyó la contracción; que
 truco iterado comprado en comparación con Banach simple; donde
 \omterm{def:b2:metric:connected}{conectividad} entró; y qué contraejemplo de la parte
 III guarda qué hipótesis. Nombra el teorema de la cumbre,
 y diga qué reemplaza la contracción cuando $f$ es simplemente
 \omterm{def:b2:metric:continuity}{continuo} (teorema de Peano, vía \omterm{def:b2:metric:compact}{compacidad} en función
 espacios --- el volumen del año 3 Arzelà--Ascoli).
\end{enumerate}
\end{problem}