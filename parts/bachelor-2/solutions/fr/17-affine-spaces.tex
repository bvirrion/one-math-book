\section*{Chapitre \ref{ch:b2:affine} --- Espaces affines}

\begin{solution}{exo:b2:affine:1}
$\{x + y + z = 1\}$~: plan \omterm{def:b2:affine:subspace}{affine}, de direction le plan vectoriel $\{x
+ y + z = 0\}$, dimension $2$. En ajoutant $x - z = 3$~: une droite
\omterm{def:b2:affine:subspace}{affine} (deux équations indépendantes), de direction $\{x + y + z = 0,\
x = z\} = \operatorname{Vect}\bigl((1, -2, 1)\bigr)$, dimension $1$.
$\{x^2 + y^2 = 1\}$~: un cylindre --- non stable par \omterm{def:b2:affine:barycenter}{barycentres} (le
milieu de $(1,0,0)$ et $(-1,0,0)$ est l'origine, hors du
cylindre)~: non \omterm{def:b2:affine:subspace}{affine}. Un système compatible $MX = B$~: sous-espace
\omterm{def:b2:affine:subspace}{affine} $X_0 + \ker M$ de dimension $\dim\ker M$, comme rappelé
à la~\cref{def:b2:affine:subspace}.
\end{solution}

\begin{solution}{exo:b2:affine:2}
$C = \operatorname{bar}(A, 1-t; B, t)$ signifie $\vect{AC} =
t\,\vect{AB}$~: l'existence d'un tel $t$ est exactement la liaison de
$\vect{AC}$ avec $\vect{AB} \neq 0$, c'est-à-dire l'alignement.
Positions~: $t = \frac12$~: milieu~; $t = 2$~: au-delà de $B$, à la
distance de $B$ à celui-ci ($\vect{AC} = 2\vect{AB}$)~; $t = -1$~: le
\omterm{def:b2:quadratic:adjoint}{symétrique} de $B$ par rapport à $A$.
\end{solution}

\begin{solution}{exo:b2:affine:3}
$M$ est la matrice de projection sur $\operatorname{Vect}(1,1)$
parallèlement à $(1,-1)$ (vérifier $M^2 = M$). Image de $f$~: $\{MX +
C\} = C + \operatorname{im} M$~: la droite \omterm{def:b2:affine:subspace}{affine} passant par $(1,0)$
dirigée par $(1,1)$. Points fixes~: $X = MX + C$, c'est-à-dire $(I -
M)X = C$~; mais $C = (1, 0)^{\mathsf T}$ et $\operatorname{im}(I - M) =
\operatorname{Vect}(1,-1)$~; $(1,0)$ y est-il~? $(1, 0) =
\alpha(1,-1)$ force $\alpha = 1$ et $0 = -1$~: non. Pas de point fixe.
Et
\[
f(f(X)) = M(MX + C) + C = MX + MC + C = f(X) + MC,
\qquad MC = \tfrac12(1,1)^{\mathsf T} :
\]
$f\circ f$ est $f$ suivie d'une translation le long de la droite image
--- $f$ est une «~projection glissée~»~: projection sur la droite
composée avec un glissement.
\end{solution}

\begin{solution}{exo:b2:affine:4}
$I = \operatorname{bar}(B, 2; C, 1)$ (car $\vect{BI} =
\frac13\vect{BC}$ place $I$ plus près de $B$~: poids $2$ sur $B$,
$1$ sur $C$ --- vérification~: $\vect{BI} = \frac{1}{3}\vect{BC}$).
De même $J = \operatorname{bar}(C, 2; A, 1)$, $K =
\operatorname{bar}(A, 2; B, 1)$. En sommant les trois systèmes
pondérés, le \omterm{def:b2:affine:barycenter}{barycentre} de $(I, 1; J, 1; K, 1)$ (chacun de poids
total $3$, donc remplacer $I$ par son système, etc.) est
\[
\operatorname{bar}\bigl(A, 1 + 2;\ B, 2 + 1;\ C, 1 + 2\bigr)
= \operatorname{bar}(A, 1; B, 1; C, 1) = G ,
\]
le centre de gravité de $ABC$~: le triangle $IJK$ a le même centre de
gravité --- prévisible, car la construction traite $A, B, C$
cycliquement et le centre de gravité est l'unique point fixe de la
symétrie \omterm{def:b2:structures:generated}{cyclique} des poids.
\end{solution}

\begin{solution}{exo:b2:affine:5}
Posons $g(u) = f(O + u) - f(O)$ (en travaillant dans $\R^n$
vectorialisé en $O$), $g(0) = 0$.

\emph{Additivité~:} $\frac{(O + u) + (O + v)}{2} = O + \frac{u +
v}{2}$, donc la conservation des milieux donne
$g\bigl(\frac{u+v}{2}\bigr) = \frac{g(u) + g(v)}{2}$~; avec $v = 0$~:
$g(u/2) = g(u)/2$~; en combinant, $g(u + v) =
2g\bigl(\frac{u+v}{2}\bigr) = g(u) + g(v)$.

\emph{$\Q$-homogénéité~:} l'additivité donne $g(nu) = ng(u)$ ($n \in
\N$, par récurrence), puis $g(-u) = -g(u)$ (en ajoutant), puis
$g(\frac pq u) = \frac pq g(u)$ (appliquer $q$, utiliser
l'injectivité de la mise à l'échelle).

\emph{$\R$-homogénéité~:} pour $t \in \R$, prenons des rationnels $t_n
\to t$~: $g(t_nu) = t_ng(u)$, et la continuité de $g$ (héritée de $f$)
passe à la limite~: $g(tu) = tg(u)$. Donc $g$ est linéaire et $f =
f(O) + g$~: \omterm{def:b2:affine:subspace}{affine}.
\end{solution}

\begin{solution}{exo:b2:affine:6}
Partie linéaire $\varphi(x,y) = (y, x)$~: la réflexion d'axe la
diagonale $y = x$ (orthogonale, \omterm{def:b2:linalg:det}{déterminant} $-1$). Points fixes~: $(x,
y) = (y + 1, x - 1)$ revient à l'unique équation $y = x - 1$ (les deux
composantes sont \omterm{def:b2:nvs:equivalent}{équivalentes})~: tout point de la droite $y = x - 1$
est fixe. Donc $f$ fixe cette droite point par point~: $f$
est la \emph{réflexion} d'axe cette droite (une isométrie ayant une
droite de points fixes et une partie linéaire qui est une réflexion).
De façon cohérente, $f \circ f(x,y) = f(y+1, x-1) = (x - 1 + 1, y + 1
- 1) = (x, y)$~: une involution, comme une réflexion se doit de
l'être.
\end{solution}

\begin{solution}{exo:b2:affine:7}
Les $n + 1$ vecteurs $\vect{A_1A_i}$ ($i \geq 2$) sont liés en
dimension $n$~: il existe des $\mu_i$, non tous nuls, avec
$\sum_{i\geq2} \mu_i\vect{A_1A_i} = 0$~; posons $\mu_1 = -\sum_{i \geq
2}\mu_i$, de sorte que $\sum_{i}\mu_i = 0$ et $\sum_i \mu_i\,\vect{OA_i}
= 0$ pour tout $O$, les $\mu_i$ n'étant pas tous nuls. Séparons les
indices~: $P = \{i : \mu_i > 0\}$, $N = \{i : \mu_i < 0\}$, tous deux
non vides (les $\mu_i$ sont de somme nulle et non tous nuls). Avec $s =
\sum_{i\in P}\mu_i = -\sum_{i \in N}\mu_i > 0$~:
\[
\operatorname{bar}\bigl(A_i, \tfrac{\mu_i}{s}\bigr)_{i \in P}
= \operatorname{bar}\bigl(A_i, \tfrac{-\mu_i}{s}\bigr)_{i \in N},
\]
(les deux membres sont égaux au point $X$ tel que $\vect{OX} =
\frac1s\sum_{i\in P}\mu_i\vect{OA_i}$, par la relation)~: un point
commun aux deux \omterm{def:b2:affine:convex}{enveloppes convexes}, avec des groupes d'indices
disjoints.
\end{solution}

\begin{solution}{exo:b2:affine:8}
\emph{Image = points fixes~:} pour $Y = f(X)$, $f(Y) = f(f(X)) =
f(X) = Y$~: tout point image est fixe~; réciproquement les points fixes
sont des images. Donc $\mathcal{F} = \operatorname{im} f =
\operatorname{Fix}(f)$ est non vide, et c'est un sous-espace \omterm{def:b2:affine:subspace}{affine}
(image d'une \omterm{def:b2:affine:subspace}{application affine}), de direction $\operatorname{im}\vec
f$.

\emph{Structure de projection~:} $\vec f$ est idempotente ($\vec{f\circ
f} = \vec f^{\,2} = \vec f$), donc $E = \operatorname{im}\vec f
\oplus \ker \vec f$ (\cref{ex:b2:reduction:projections}). Pour tout
point $X$, considérons le vecteur $\vect{f(X)\,X}$~; en appliquant
$\vec f$~:
\[
\vec f\bigl(\vect{f(X)\,X}\bigr) = \vect{f(f(X))\,f(X)} = 0
\qquad (f \circ f = f),
\]
donc $\vect{f(X)\,X} \in \ker\vec f$. Ainsi $X = f(X) +
\vect{f(X)X}$ présente $X$ comme un point de $\mathcal{F}$ translaté
par un vecteur de $\ker\vec f$~: $f$ est exactement la projection sur
$\mathcal{F}$ parallèlement à $\ker\vec f$. Réciproquement de telles
projections vérifient clairement $f \circ f = f$.
\end{solution}

\begin{solution}{exo:b2:affine:9}
Soit $M = \operatorname{bar}(B, 2;\ C, 3)$, de poids total $5$.
L'associativité donne $G = \operatorname{bar}(A, 1;\ M, 5)$, donc
$\vect{AG} = \frac56\,\vect{AM}$~: $G$ est sur le segment
$\intcc AM$ aux cinq sixièmes depuis $A$. Comme $A \notin
(BC)$, la droite $(AG) = (AM)$ rencontre $(BC)$ au seul point
$M$, avec $\vect{BM} = \frac35\,\vect{BC}$. De même, avec $N =
\operatorname{bar}(C, 3;\ A, 1)$ (poids total $4$, $\vect{CN}
= \frac14\,\vect{CA}$), l'associativité donne $G =
\operatorname{bar}(B, 2;\ N, 4)$~: la droite $(BG)$ rencontre $(CA)$
en $N$, et $\vect{BG} = \frac46\,\vect{BN} =
\frac23\,\vect{BN}$.
\end{solution}

\begin{solution}{exo:b2:affine:10}
Vectorialisons en une origine $O$ et écrivons les points comme des
vecteurs~: $h_{\Omega, \lambda}(x) = \omega + \lambda(x - \omega)$ avec
$\omega = \vect{O\Omega}$. La composée $g = h_{\Omega',
\mu} \circ h_{\Omega, \lambda}$ est \omterm{def:b2:affine:subspace}{affine} de partie linéaire
$\mu\lambda\,\mathrm{id}$. Si $\lambda\mu \neq 1$~: $1 \notin
\operatorname{Sp}(\lambda\mu\,\mathrm{id})$, donc
la~\cref{prop:b2:affine:fixedpoint} fournit un unique point fixe
$\Omega''$ et, vectorialisée là, $g =
\lambda\mu\,\mathrm{id}$~: l'homothétie $h_{\Omega'',
\lambda\mu}$. Si $\lambda\mu = 1$ la partie linéaire est
l'identité, donc $g$ est une translation~; en développant,
\[
g(x) = \omega' + \mu\bigl(\omega + \lambda(x - \omega) -
\omega'\bigr) = x + (1 - \mu)\,\omega' + \mu(1 -
\lambda)\,\omega .
\]
Pour $\lambda = \mu = -1$ (symétries centrales) le vecteur est
$2\omega' - 2\omega = 2\,\vect{\Omega\Omega'}$~: la composée des
symétries centrales de centre $\Omega$ puis $\Omega'$ est la
translation de vecteur $2\,\vect{\Omega\Omega'}$.
\end{solution}

\begin{solution}{exo:b2:affine:11}
$\vect{A'B} = \alpha\,\vect{A'C}$ dit exactement $1\cdot
\vect{A'B} - \alpha\,\vect{A'C} = 0$, c'est-à-dire $A' =
\operatorname{bar}(B, 1;\ C, -\alpha)$ (poids total $1 -
\alpha \neq 0$ car $B \neq C$)~; de même $B' =
\operatorname{bar}(C, 1;\ A, -\beta)$ et $C' =
\operatorname{bar}(A, 1;\ B, -\gamma)$.

\emph{Le critère d'alignement.} Donnons à chaque point $P$ sa
ligne barycentrique normalisée $p = (p_A, p_B, p_C)$, $p_A + p_B +
p_C = 1$, relativement à $(A, B, C)$. Si $\sum_i c_i p_i = 0$
avec $(c_1, c_2, c_3) \neq 0$ pour trois points $P_1, P_2,
P_3$, alors la somme des entrées donne $\sum c_i = 0$, et
$\sum_i c_i \vect{OP_i} = \sum_j \bigl(\sum_i
c_ip_{ij}\bigr)\vect{OV_j} = 0$~: les $P_i$ sont affinement
dépendants, c'est-à-dire alignés. Réciproquement une dépendance \omterm{def:b2:affine:subspace}{affine}
$(t_i)$ donne $w = \sum t_ip_i$ dont les entrées sont de somme $0$
et $\sum_j w_j\vect{OV_j} = 0$~; en développant à partir de $A$, $w_B
\vect{AB} + w_C\vect{AC} = 0$, donc $w = 0$ par indépendance
\omterm{def:b2:affine:subspace}{affine} de $(A, B, C)$~: les lignes sont linéairement dépendantes.
Ainsi l'alignement équivaut à l'annulation d'un \omterm{def:b2:linalg:det}{déterminant} $3 \times
3$, et multiplier les lignes par les facteurs non nuls $1 - \alpha$, $1
- \beta$, $1 - \gamma$ ne change rien~:
\[
\det\begin{pmatrix} 0 & 1 & -\alpha\\ -\beta & 0 & 1\\ 1 &
-\gamma & 0\end{pmatrix}
= 1 - \alpha\beta\gamma .
\]
Donc $A', B', C'$ sont alignés si et seulement si $\alpha\beta\gamma =
1$~: le théorème de Ménélaüs.
\end{solution}

\begin{solution}{exo:b2:affine:12}
Soit $\Delta = \{\lambda \in \R^{n+1} : \lambda_i \geq 0,\
\sum\lambda_i = 1\}$~: fermé et borné dans $\R^{n+1}$, donc
\omterm{def:b2:metric:compact}{compact}, et $K^{n+1}$ est \omterm{def:b2:metric:compact}{compact} comme produit fini. L'application
\[
\Phi \colon \Delta \times K^{n+1} \to \R^n, \qquad
\Phi(\lambda, x_0, \dots, x_n) = \sum_{i=0}^n \lambda_i x_i
\]
est \omterm{def:b2:metric:continuity}{continue}, et le~\cref{thm:b2:affine:caratheodory} dit
précisément que $\operatorname{conv}(K) = \Phi(\Delta \times
K^{n+1})$~: image \omterm{def:b2:metric:continuity}{continue} d'un \omterm{def:b2:metric:compact}{compact}
(\cref{thm:b2:metric:compactprops}), donc compacte.

Pour un fermé~: prenons $S = (\R \times \{0\}) \cup \{(0,
1)\}$, fermé dans $\R^2$. Une combinaison \omterm{def:b2:affine:convex}{convexe} mettant le poids
$t$ sur $(0,1)$ et $1 - t$ sur des points de l'axe a pour seconde
coordonnée $t$, donc
\[
\operatorname{conv}(S) = \bigl(\R \times \intco01\bigr) \cup
\{(0,1)\}
\]
(pour $0 \leq t < 1$, $(x, t) = t\,(0,1) + (1-t)\,\bigl(\tfrac
x{1-t}, 0\bigr)$). Le point $(1, 1) = \lim_{t \to 1}(1, t)$
est adhérent mais pas dans l'enveloppe~: non fermée.
\end{solution}

\begin{solution}{pb:b2:affine:1}
\textbf{1.} Rebasons en $A_j$~: pour $i \neq j$, $\vect{A_jA_i} =
\vect{A_0A_i} - \vect{A_0A_j}$. Si $\sum_{i \neq j}
c_i\vect{A_jA_i} = 0$, le développement donne $\sum_{i \notin \{0,
j\}} c_i\,\vect{A_0A_i} - \bigl(\sum_{i\neq j}
c_i\bigr)\vect{A_0A_j} = 0$~; l'indépendance des
$\vect{A_0A_i}$ force $c_i = 0$ pour $i \notin \{0, j\}$, puis
$c_0 = 0$~: indépendance en $A_j$. Pour l'équivalence~: deux
familles de poids $(\lambda_i)$, $(\lambda_i')$ de somme $1$
ayant le même \omterm{def:b2:affine:barycenter}{barycentre} donnent, avec $\nu = \lambda -
\lambda'$~: $\sum\nu_i = 0$ et (origine $A_0$) $\sum_{i \geq
1}\nu_i\,\vect{A_0A_i} = 0$, donc $\nu = 0$ sous l'hypothèse
d'indépendance. Réciproquement une relation non triviale
$\sum_{i\geq1}\mu_i \vect{A_0A_i} = 0$, complétée par $\mu_0 =
-\sum_{i\geq1} \mu_i$, permet d'ajouter $t(\mu_i)$ à toute famille de
poids sans déplacer le \omterm{def:b2:affine:barycenter}{barycentre}~: non-unicité.

\textbf{2.} $(\vect{AB}, \vect{AC})$ est une base de $\R^2$~:
écrivons $\vect{AM} = \beta\,\vect{AB} + \gamma\,\vect{AC}$
(de manière unique) et posons $\alpha = 1 - \beta - \gamma$~; la
condition de \omterm{def:b2:affine:barycenter}{barycentre} à l'origine $A$ s'écrit exactement $\vect{AM} =
\beta\,\vect{AB} + \gamma\,\vect{AC}$. L'unicité est la question
1.

\textbf{3.} De $\alpha\vect{MA} + \beta\vect{MB} +
\gamma\vect{MC} = 0$ et Chasles, $\vect{MA} =
-\beta\,\vect{AB} - \gamma\,\vect{AC}$. Avec $D =
\det(\vect{AB}, \vect{AC})$~:
\begin{align*}
\det(\vect{MB}, \vect{MC})
&= \det(\vect{MA} + \vect{AB},\ \vect{MA} + \vect{AC})\\
&= \det(\vect{MA}, \vect{AC}) + \det(\vect{AB}, \vect{MA}) +
D = -\beta D - \gamma D + D = \alpha D,
\end{align*}
en utilisant la bilinéarité et $\det(\vect{MA}, \vect{AC}) = -\beta D$,
$\det(\vect{AB}, \vect{MA}) = -\gamma D$. Les deux autres
formules s'obtiennent par le même calcul en permutant cycliquement les
rôles.

\textbf{4.} Par définition $\operatorname{conv}\{A, B, C\}$ est
l'ensemble des \omterm{def:b2:affine:barycenter}{barycentres} à poids positifs~; en normalisant
les poids à somme $1$ et en invoquant l'unicité (question 2),
$M \in \operatorname{conv}\{A,B,C\}$ si et seulement si $\alpha, \beta,
\gamma \geq 0$. Chaque coordonnée est une fonction \omterm{def:b2:affine:subspace}{affine} de $M$
(question 3~: un \omterm{def:b2:linalg:det}{déterminant} $2\times2$ avec une colonne \omterm{def:b2:affine:subspace}{affine}
en $M$), donc chaque condition de signe ouverte définit un
demi-plan \omterm{def:b2:metric:topology}{ouvert}. La configuration $(-,-,-)$ contredit $\alpha + \beta
+ \gamma = 1$~; chacune des sept configurations restantes est
réalisée~: mettre à l'échelle un triplet respectant les signes avec au
moins une entrée $+$ de sorte que la somme (positive) soit $1$ ---
par exemple $(-1, 1, 1)$, $(3, -1, -1)$, $(\frac13, \frac13, \frac13)$,
et permutations.

\textbf{5.} Une \omterm{def:b2:affine:subspace}{application affine} conserve les \omterm{def:b2:affine:barycenter}{barycentres}
(\cref{def:b2:affine:subspace}), donc $u(M) = \alpha u(A) +
\beta u(B) + \gamma u(C)$. En écrivant $u(x, y) = ax + by + c$
avec $(a, b) \neq (0,0)$~: $\{u = c'\}$ est une droite, et toute
droite $ax + by = c'$ est une telle ligne de niveau. Si $u(M), u(N) \geq
c$ et $t \in \intcc01$, alors $u\bigl(\operatorname{bar}(M,
1-t; N, t)\bigr) = (1-t)u(M) + tu(N) \geq c$~: les demi-plans sont
\omterm{def:b2:affine:convex}{convexes}.

\textbf{6.} Les conditions $\sum\mu_i = 0$, $3\mu_2 + \mu_4 =
0$, $3\mu_3 + \mu_4 = 0$ donnent (en prenant $\mu_4 = 3$) la
dépendance $(\mu_1, \mu_2, \mu_3, \mu_4) = (-1, -1, -1, 3)$.
Les signes se répartissent en $\{A_4\} \mid \{A_1, A_2, A_3\}$, et
en normalisant chaque membre par $3$~:
\[
A_4 = \operatorname{bar}\bigl(A_1, \tfrac13;\ A_2, \tfrac13;\
A_3, \tfrac13\bigr) = (1,1),
\]
le centre de gravité du triangle~: le point de Radon est $A_4$
lui-même, qui est bien à l'intérieur du triangle $A_1A_2A_3$.

\textbf{7.} L'application linéaire $\Phi \colon \R^{n+2} \to \R
\times \R^n$, $\mu \mapsto (\sum\mu_i,\
\sum\mu_i\vect{OA_i})$, est de rang $\leq n + 1$, donc $\dim\ker
\Phi \geq 1$. Si deux dépendances indépendantes $\mu, \mu'$
existaient, une combinaison convenable $\nu = \mu'_{n+2}\mu -
\mu_{n+2}\mu'$ (ou $\mu$ elle-même si les deux derniers coefficients
s'annulent) serait une dépendance \emph{non nulle} avec $\nu_{n+2}
= 0$~; en restreignant à $A_1, \dots, A_{n+1}$ et en rebasant en
$A_1$, un certain $\nu_i \neq 0$ avec $i \geq 2$ (un unique
poids non nul ne peut être de somme nulle), donnant une relation non
triviale $\sum_{i\geq2}\nu_i\vect{A_1A_i} = 0$~: les $n+1$ points
seraient affinement dépendants, contre la position générale. Donc
$\dim\ker \Phi = 1$. Le même argument de restriction montre qu'une
dépendance non nulle n'a aucun coefficient nul.

\textbf{8.} Soit $\mu \neq 0$ une dépendance, $P = \{i :
\mu_i > 0\}$ et $N = \{i : \mu_i < 0\}$~: tous deux non vides
($\sum\mu_i = 0$, $\mu \neq 0$) et exhaustifs (aucun coefficient
nul). La construction de Radon (\cref{exo:b2:affine:7})
produit le point commun des enveloppes à partir exactement de cette
partition. Comme la dépendance est unique à un scalaire non nul près
(question 7), la paire non ordonnée $\{P, N\}$ --- donc la
partition de Radon --- est unique.

\textbf{9.} Les blocs sont non vides, donc le type est $(1,3)$ ou
$(2,2)$. Type $(1,3)$, bloc $\{j\}$~: le point de Radon est dans
$\operatorname{conv}\{A_j\} = \{A_j\}$, donc $A_j \in
\operatorname{conv}$ des trois autres~; il ne peut être sur un
côté (trois des points seraient alignés, contre la position
générale), donc $A_j$ est intérieur au triangle. Type
$(2,2)$, blocs $\{i,j\} \mid \{k,l\}$~: le point de Radon $z$
est sur $\intcc{A_i}{A_j} \cap \intcc{A_k}{A_l}$, et $z$ n'est
pas une extrémité (cela alignerait trois points)~: les deux
segments se croisent en un point intérieur. De plus aucun point n'est
dans l'enveloppe des autres~: une telle appartenance $A_l =
\operatorname{bar}(A_i, \lambda_i)_{i \neq l}$ avec
$\lambda_i \geq 0$ est une dépendance \omterm{def:b2:affine:subspace}{affine} de configuration de signes
$(+,+,+,-)$, qui par unicité (question 8) rendrait la
partition $(1,3)$. Donc dans le cas $(2,2)$ les quatre points sont
en position \omterm{def:b2:affine:convex}{convexe} et les segments qui se croisent sont les
diagonales.

\textbf{10.} Les équations $\mu_2 + \mu_3 = 0$, $\mu_3 +
\mu_4 = 0$, $\sum\mu_i = 0$ donnent la dépendance $(1, -1, 1,
-1)$~: partition $\{(0,0), (1,1)\} \mid \{(1,0), (0,1)\}$, et
\[
\operatorname{bar}\bigl((0,0), \tfrac12;\ (1,1),
\tfrac12\bigr) = \bigl(\tfrac12, \tfrac12\bigr) =
\operatorname{bar}\bigl((1,0), \tfrac12;\ (0,1),
\tfrac12\bigr) :
\]
le point de Radon est le centre du carré, où les deux
diagonales se croisent --- type $(2,2)$, comme la figure le prédit.

\textbf{11.} Radon appliqué à $x_1, \dots, x_4$ donne des blocs
$I \mid J$ et un point $z \in \operatorname{conv}\{x_i : i
\in I\} \cap \operatorname{conv}\{x_j : j \in J\}$. Fixons $k
\in \{1, \dots, 4\}$, disons $k \in I$. Tout $j \in J$
vérifie $j \neq k$, donc $x_j \in C_k$ par le choix $x_j \in
\bigcap_{l \neq j}C_l$~; comme $C_k$ est \omterm{def:b2:affine:convex}{convexe}, $z \in
\operatorname{conv}\{x_j : j \in J\} \subseteq C_k$. Comme $k$
était quelconque, $z \in C_1 \cap C_2 \cap C_3 \cap C_4$.

\textbf{12.} Récurrence sur $m$. Pour $m = 3$ l'hypothèse est
la conclusion~; $m = 4$ est la question 11. Soit $m \geq 4$,
supposons l'énoncé pour $m$ ensembles, et prenons $C_1, \dots,
C_{m+1}$ avec la propriété d'intersection triple. Posons $C_m' =
C_m \cap C_{m+1}$, \omterm{def:b2:affine:convex}{convexe}. La famille $C_1, \dots, C_{m-1},
C_m'$ a $m$ membres~; un triplet évitant $C_m'$ s'intersecte par
hypothèse, et un triplet $\{C_i, C_j, C_m'\}$ a
pour intersection $C_i \cap C_j \cap C_m \cap C_{m+1}$, non vide
par la question 11 appliquée à $C_i, C_j, C_m, C_{m+1}$ (trois
quelconques d'entre eux se rencontrent, par hypothèse). L'hypothèse
de récurrence fournit alors un point commun à la nouvelle famille,
c'est-à-dire à tous les $m+1$ ensembles.

\textbf{13.} (a) Les côtés fermés $\intcc PQ$, $\intcc QR$,
$\intcc RP$ d'un triangle non dégénéré~: deux quelconques partagent un
sommet, mais un point commun aux trois serait dans $\intcc
PQ \cap \intcc RP = \{P\}$ et dans $\intcc QR$, qui exclut
$P$. (b) Trois quelconques des ensembles $S_i = \{x_1, \dots, x_4\}
\setminus \{x_i\}$ omettent trois des quatre points, laissant
exactement un point commun~; l'intersection totale omet tout
point. Les $S_i$ sont finis, non \omterm{def:b2:affine:convex}{convexes}~: la convexité est
essentielle. (c) Un nombre fini de $H_k = \intco{k}{+\infty} \times
\R$ s'intersectent en $\intco{k_{\max}}{+\infty} \times \R \neq
\emptyset$, pourtant aucun point n'a $x \geq k$ pour tout $k \in \N$~:
pour les familles infinies, la compacité est essentielle.

\textbf{14.} Supposons $\bigcap_{i \in I}K_i = \emptyset$ et
fixons $i_0$. Tout $x \in K_{i_0}$ échappe à un certain $K_i$, donc
$K_{i_0} \subseteq \bigcup_{i \in I}(\R^2 \setminus K_i)$, un
recouvrement par des \omterm{def:b2:metric:topology}{ouverts} ($K_i$ est \omterm{def:b2:metric:compact}{compact}, donc fermé). Par
Borel--Lebesgue (\cref{thm:b2:metric:borellebesgue}) un nombre fini
suffit~: $K_{i_0} \cap K_{i_1} \cap \dots \cap K_{i_N} =
\emptyset$. Mais trois membres quelconques de cette famille finie de
\omterm{def:b2:affine:convex}{convexes} s'intersectent, donc la question 12 rend l'intersection
non vide~: contradiction.

\textbf{15.} Posons $D_p = \overline D(p, r)$ pour $p \in S$~:
des \omterm{def:b2:affine:convex}{convexes} \omterm{def:b2:metric:compact}{compacts}. Pour $p, q, s \in S$, l'hypothèse
donne un disque fermé $\overline D(z, r)$ contenant $p, q, s$~;
alors $\norm{\vect{zp}}, \norm{\vect{zq}}, \norm{\vect{zs}}
\leq r$, c'est-à-dire $z \in D_p \cap D_q \cap D_s$. Par Helly
(question 12~; la famille est finie) il existe $c \in
\bigcap_{p \in S}D_p$~: tout $p \in S$ vérifie
$\norm{\vect{cp}} \leq r$, donc $S \subseteq \overline D(c,
r)$.

\textbf{16.} Soit $c = x_{\lceil n/2\rceil}$. Une demi-droite
fermée contenant $c$ est $\intoc{-\infty}{t}$ avec $t \geq
c$ ou $\intco{t}{+\infty}$ avec $t \leq c$. La première
contient $x_1, \dots, x_{\lceil n/2\rceil}$~: au moins
$\lceil n/2\rceil \geq n/2$ points. La seconde contient
$x_{\lceil n/2\rceil}, \dots, x_n$~: exactement $n - \lceil
n/2\rceil + 1 = \floor{n/2} + 1 > n/2$ points.

\textbf{17.} $\abs{A \cap B} = \abs A + \abs B - \abs{A \cup
B} \geq \abs A + \abs B - n > \tfrac{4n}3 - n = \tfrac n3$,
puis
\[
\abs{A \cap B \cap C} \geq \abs{A \cap B} + \abs C - n >
\tfrac n3 + \tfrac{2n}3 - n = 0 .
\]

\textbf{18.} Notons $m > 2n/3$. Pour $T_1, T_2, T_3 \subseteq S$
de cardinal $m$, la question 17 fournit un point $x \in T_1
\cap T_2 \cap T_3$~; alors $x \in \operatorname{conv}(T_i)$ pour
chaque $i$~: trois membres quelconques de $\mathcal F$ se rencontrent.
La famille est finie (un nombre fini de parties de $S$) et formée de
\omterm{def:b2:affine:convex}{convexes}, donc Helly (question 12) donne $c \in
\bigcap_{\abs T = m}\operatorname{conv}(T)$.

\textbf{19.} Supposons qu'un demi-plan fermé $H \ni c$
contienne moins de $n/3$ points de $S$. Son complémentaire $U$
est un demi-plan \omterm{def:b2:metric:topology}{ouvert}, \omterm{def:b2:affine:convex}{convexe}, avec $\abs{S \cap U} > 2n/3$,
donc $\abs{S \cap U} \geq \floor{2n/3} + 1 = m$~; choisissons $T
\subseteq S \cap U$ avec $\abs T = m$. Alors
$\operatorname{conv}(T) \subseteq U$ par convexité de $U$, donc
$c \in \operatorname{conv}(T) \subseteq U$~: contradiction
avec $c \in H$. Donc tout demi-plan fermé contenant $c$
contient au moins $n/3$ points~: $c$ est un point central.

\textbf{20.} Prenons le triangle équilatéral de côté $L$ et
$\varepsilon = L/100$. Soit $c$ un point quelconque~; nous exhibons un
demi-plan fermé contenant $c$ et au plus $k$ points.

\emph{Cas 1~: $c$ est à moins de $L/10$ d'un sommet, disons $B$.}
Les directions de $c$ vers $A$ et vers $C$ s'écartent des
directions $B \to A$, $B \to C$ d'au plus
$\arcsin\bigl(\tfrac{L/10}{9L/10}\bigr) = \arcsin\tfrac19$,
donc elles font un angle $\leq \tfrac\pi3 + 2\arcsin\tfrac19 <
\tfrac{2\pi}3$. \emph{Cas 2~: $c$ est à distance $> L/10$
de tous les sommets.} Si $c$ est dans le triangle, les trois
écarts angulaires entre les directions $u_A, u_B, u_C$ de $c$
vers les sommets somment à $2\pi$, donc un écart est $\leq 2\pi/3$~;
si $c$ est à l'extérieur, les trois directions sont dans un demi-plan
\omterm{def:b2:metric:topology}{ouvert} de directions et deux d'entre elles font un angle $<
\pi/2$. Dans tous les cas deux directions, disons vers $X$ et $Y$,
font un angle $\leq 2\pi/3$~; soit $w$ leur bissectrice \omterm{def:b2:hermitian:adjoint}{unitaire},
de sorte que $\langle u_X, w\rangle, \langle u_Y, w\rangle \geq
\cos\tfrac\pi3 = \tfrac12$. Pour tout point $b$ du disque
autour de $X$~:
\[
\langle \vect{cb}, w\rangle \geq
\tfrac12\norm{\vect{cX}} - \varepsilon > 0,
\]
car $\norm{\vect{cX}} \geq L/10 > 2\varepsilon$ (et
de même pour $Y$)~: le demi-plan \omterm{def:b2:metric:topology}{ouvert} $\{\langle \vect{cx},
w\rangle > 0\}$ avale les deux amas. Son complémentaire fermé
contient $c$ et au plus les $k$ points du troisième amas. Donc
aucun point du plan ne dépasse $n/3$~: avec la question 19, la
constante du point central est exactement $1/3$.

\textbf{21.} Ordonnons les angles~; le plus grand, $\theta$,
vérifie $\theta \geq \pi/3$ (les trois somment à $\pi$). Si
$\theta \geq \pi/2$, disons en $R$, soit $M$ le milieu du
côté opposé $\intcc PQ$. La formule de la médiane ($\vect{RM}
= \tfrac12(\vect{RP} + \vect{RQ})$, développer et éliminer
$\langle\vect{RP}, \vect{RQ}\rangle$ avec la loi des cosinus) donne
\[
\norm{\vect{RM}}^2 = \tfrac12\norm{\vect{RP}}^2 +
\tfrac12\norm{\vect{RQ}}^2 - \tfrac14\norm{\vect{PQ}}^2 \leq
\tfrac12\norm{\vect{PQ}}^2 - \tfrac14\norm{\vect{PQ}}^2 =
\tfrac14\norm{\vect{PQ}}^2,
\]
en utilisant $\norm{\vect{PQ}}^2 = \norm{\vect{RP}}^2 +
\norm{\vect{RQ}}^2 - 2\langle\vect{RP}, \vect{RQ}\rangle \geq
\norm{\vect{RP}}^2 + \norm{\vect{RQ}}^2$ (le produit scalaire
est $\leq 0$). Donc le disque de diamètre $\intcc PQ$, de rayon
$\leq \tfrac12 < \tfrac1{\sqrt3}$, contient les trois points
(les triplets alignés dégénérés relèvent de $\theta = \pi$). Si
$\theta < \pi/2$ le triangle est acutangle~; par la loi des sinus
le rayon du cercle circonscrit est $R_c = a/(2\sin\theta)$ avec $a \leq 1$
le côté opposé à $\theta$, et $\theta \in
\intco{\pi/3}{\pi/2}$ donne $\sin\theta \geq \sqrt3/2$, donc
$R_c \leq 1/\sqrt3$~: le disque circonscrit fait l'affaire.

\textbf{22.} Pour $p \in S$ soit $K_p = \overline D(p,
1/\sqrt3)$~: \omterm{def:b2:affine:convex}{convexe} \omterm{def:b2:metric:compact}{compact}. Trois points quelconques $p, q, s$ de $S$
sont deux à deux à distance $\leq 1$, donc la question 21 donne un
disque de rayon $1/\sqrt3$ les contenant~: son centre est dans
$K_p \cap K_q \cap K_s$. Par Helly \omterm{def:b2:metric:compact}{compact} (question 14,
familles quelconques autorisées) il existe $c \in \bigcap_{p\in
S}K_p$~: tout $p \in S$ est à distance $\leq 1/\sqrt3$ de $c$,
c'est-à-dire $S \subseteq \overline D(c, 1/\sqrt3)$. C'est le théorème
de Jung dans le plan.

\textbf{23.} Avec $G$ le centre de gravité, $\sum_i\vect{GA_i} = 0$,
donc
\[
\sum_i\norm{\vect{OA_i}}^2 = \sum_i\norm{\vect{OG} +
\vect{GA_i}}^2 = 3\norm{\vect{OG}}^2 + 2\Bigl\langle
\vect{OG}, \sum_i\vect{GA_i}\Bigr\rangle +
\sum_i\norm{\vect{GA_i}}^2,
\]
et le terme central s'annule~: l'identité de Leibniz. Pour le
triangle équilatéral de côté $1$, $\norm{\vect{GA_i}} =
1/\sqrt3$ (les deux tiers de la hauteur $\sqrt3/2$), donc
$\sum_i\norm{\vect{GA_i}}^2 = 1$. Si $\overline D(O, r)$
contient les sommets, alors $3r^2 \geq
\sum_i\norm{\vect{OA_i}}^2 = 3\norm{\vect{OG}}^2 + 1 \geq 1$~:
$r \geq 1/\sqrt3$, l'égalité forçant $O = G$ et les trois
distances égales à $r$ --- le disque circonscrit. La constante de
Jung $1/\sqrt3$ est optimale.

\textbf{24.} \emph{Helly dans $\R^n$~: si $C_1, \dots, C_m$
($m \geq n + 1$) sont des parties \omterm{def:b2:affine:convex}{convexes} de $\R^n$ et que $n + 1$
quelconques d'entre elles s'intersectent, alors toutes s'intersectent.}
Cas de base $m = n + 2$~: choisir $x_i \in \bigcap_{j\neq i}C_j$~;
le lemme de Radon (\cref{exo:b2:affine:7}) partage $x_1, \dots,
x_{n+2}$ en blocs $I \mid J$ avec un point commun d'enveloppe $z$,
et pour chaque $k$, le bloc ne contenant pas $k$ est formé de
points de $C_k$, donc $z \in C_k$ par convexité, exactement comme à la
question 11. Étape de récurrence pour $m \geq n + 2$~: remplacer
$C_m, C_{m+1}$ par $C_m \cap C_{m+1}$~; un $(n+1)$-uplet de la
nouvelle famille contenant le membre intersecté revient à $n + 2$
des anciens ensembles, traité par le cas de base, et les autres
uplets sont couverts par hypothèse. Conclure par l'hypothèse de
récurrence.

\textbf{25.} L'\omterm{def:b2:structures:algebra}{algèbre} linéaire entre une seule fois~: $n + 2$
vecteurs dans l'espace de dimension $n+1$ des couples (poids total,
position pondérée) doivent être dépendants --- c'est la dépendance
\omterm{def:b2:affine:subspace}{affine}. Les signes de ses coefficients répartissent les points
dans les deux blocs de Radon et transforment une relation linéaire en
une égalité de deux \omterm{def:b2:affine:barycenter}{barycentres} à poids positifs. La convexité est
utilisée exactement deux fois~: à l'étape de Helly (l'enveloppe des
points de $C_k$ reste dans $C_k$) et dans les applications (les
demi-plans et les disques sont \omterm{def:b2:affine:convex}{convexes}). La dimension du plan n'est
entrée que par le nombre $4 = 2 + 2$ de points fournis à Radon,
c'est-à-dire le «~$3 = 2 + 1$~» de l'hypothèse de Helly~; tout le
reste était indépendant de la dimension, comme le confirme la
question 24. Dans $\R^n$ les constantes deviennent~: nombre de Helly
$n + 1$~; constante du point central $\frac1{n+1}$ (tout ensemble
fini a un point tel que tout demi-espace fermé le contenant contient
une fraction $\geq \frac1{n+1}$ de l'ensemble)~; rayon de Jung
$\sqrt{\frac{n}{2(n+1)}}$ pour les ensembles de diamètre $1$ --- égal
à $1/\sqrt3$ quand $n = 2$.
\end{solution}
