\section*{Chapitre \ref{ch:b2:multint} --- Intégrales curvilignes et intégrales multiples}

\begin{solution}{exo:b2:multint:1}
(a) Segment $\gamma(t) = (t, t)$, $t \in [0,1]$~:
\[
\int_\gamma y^2\dd x + x\dd y
= \int_0^1 (t^2 + t)\,\dd t = \frac13 + \frac12 = \frac56 .
\]
(b) Parabole $\gamma(t) = (t, t^2)$~:
\[
\int_0^1 \bigl(t^4\cdot 1 + t\cdot 2t\bigr)\dd t
= \frac15 + \frac23 = \frac{13}{15} .
\]
Les deux valeurs diffèrent, donc l'intégrale dépend du chemin~: la forme
n'est \emph{pas} \omterm{def:b2:multint:exact}{exacte} --- en cohérence, $P_y = 2y \neq 1 = Q_x$, donc
elle n'est même pas \omterm{def:b2:multint:exact}{fermée}.
\end{solution}

\begin{solution}{exo:b2:multint:2}
$P = 2xy + y^3$, $Q = x^2 + 3xy^2 + 1$~: $P_y = 2x + 3y^2 = Q_x$,
\omterm{def:b2:multint:exact}{fermée} sur $\R^2$. Cherchons $f$ avec $f_x = P$~: $f = x^2y + xy^3
+ g(y)$~; puis $f_y = x^2 + 3xy^2 + g'(y) = Q$ force $g'(y) = 1$,
disons $g(y) = y$. Donc
\[
f(x, y) = x^2y + xy^3 + y
\]
est un \omterm{def:b2:multint:exact}{potentiel} ($\R^2$ est étoilé, donc un \omterm{def:b2:multint:exact}{potentiel} devait
exister par le lemme de Poincaré --- mais l'exhiber est plus rapide). Par
le~\cref{thm:b2:multint:ftc}, pour tout arc de $(0,0)$ à $(1,2)$~:
\[
\int_\gamma\omega = f(1, 2) - f(0, 0) = 2 + 8 + 2 = 12 .
\]
\end{solution}

\begin{solution}{exo:b2:multint:3}
Le domaine est $\{0 \leq x \leq 1,\ x^2 \leq y \leq x\}$.
$y$ d'abord~:
\[
\int_0^1\!\int_{x^2}^{x}(x + y)\,\dd y\,\dd x
= \int_0^1\Bigl(x(x - x^2)
+ \frac{x^2 - x^4}{2}\Bigr)\dd x
= \int_0^1\Bigl(\frac{3x^2}{2} - x^3 - \frac{x^4}{2}\Bigr)\dd x
= \frac12 - \frac14 - \frac1{10} = \frac{3}{20} .
\]
$x$ d'abord~: la tranche à hauteur $y \in [0, 1]$ est $y \leq x \leq
\sqrt y$, donc
\[
\int_0^1\!\int_{y}^{\sqrt y}(x + y)\,\dd x\,\dd y
= \int_0^1\Bigl(\frac{y - y^2}{2}
+ y(\sqrt y - y)\Bigr)\dd y
= \frac14 - \frac16 + \frac25 - \frac13 = \frac{3}{20} .
\]
\end{solution}

\begin{solution}{exo:b2:multint:4}
\emph{Première intégrale.} Sur le disque $D_R$, en coordonnées polaires~:
\[
\iint_{D_R}\frac{\dd x\,\dd y}{(1 + x^2 + y^2)^2}
= \int_0^{2\pi}\!\!\int_0^R
\frac{\rho\,\dd\rho\,\dd\alpha}{(1 + \rho^2)^2}
= 2\pi\Bigl[-\frac{1}{2(1 + \rho^2)}\Bigr]_0^R
= \pi\Bigl(1 - \frac{1}{1 + R^2}\Bigr)
\xrightarrow[R \to \infty]{} \pi .
\]
\emph{Deuxième intégrale.} Le quart de disque est $0 \leq \alpha \leq
\frac\pi2$, $0 \leq \rho \leq 1$, et $xy =
\rho^2\cos\alpha\sin\alpha$~:
\[
\iint_{D'}xy\,\dd x\,\dd y
= \int_0^{\pi/2}\!\!\cos\alpha\sin\alpha\,\dd\alpha
\int_0^1 \rho^3\,\dd\rho
= \frac12\cdot\frac14 = \frac18 .
\]
\end{solution}

\begin{solution}{exo:b2:multint:5}
Par le~\cref{cor:b2:multint:area} avec $x = \cos^3 t$, $y = \sin^3 t$~:
$x' = -3\cos^2 t\sin t$, $y' = 3\sin^2 t\cos t$, donc
\[
xy' - yx' = 3\cos^4 t\sin^2 t + 3\sin^4 t\cos^2 t
= 3\sin^2 t\cos^2 t = \frac{3}{4}\sin^2 2t
= \frac{3}{8}(1 - \cos 4t) .
\]
D'où
\[
A = \frac12\int_0^{2\pi}\frac38(1 - \cos 4t)\,\dd t
= \frac{3}{16}\cdot 2\pi = \frac{3\pi}{8} .
\]
(L'\omterm{pb:b2:curves:1}{astroïde} tient dans le disque unité d'\omterm{def:b2:multint:domain}{aire} $\pi$~; trois huitièmes de
$\pi$ est plausible pour sa forme d'étoile à quatre pointes.)
\end{solution}

\begin{solution}{exo:b2:multint:6}
\emph{Empilement~:} au-dessus de chaque $(x, y)$ du disque unité $D$, $z$
va de $x^2 + y^2$ à $1$~:
\[
V = \iint_D \bigl(1 - x^2 - y^2\bigr)\dd x\,\dd y
= \int_0^{2\pi}\!\!\int_0^1 (1 - \rho^2)\rho\,\dd\rho\,\dd\alpha
= 2\pi\Bigl(\frac12 - \frac14\Bigr) = \frac\pi2 .
\]
\emph{Tranches~:} la tranche à hauteur $z \in [0, 1]$ est le disque
$x^2 + y^2 \leq z$, d'\omterm{def:b2:multint:domain}{aire} $\pi z$~:
\[
V = \int_0^1 \pi z\,\dd z = \frac\pi2 .
\]
\end{solution}

\begin{solution}{exo:b2:multint:7}
En coordonnées sphériques, l'élément de volume est
$r^2\cos\varphi\,\dd r\,\dd\theta\,\dd\varphi$
(\cref{ex:b2:multint:spherical}), et la partie angulaire s'intègre
en $4\pi$ ($2\pi$ pour $\theta$, $\int_{-\pi/2}^{\pi/2}\cos = 2$).
Le volume de la coquille est
\[
\int_a^b 4\pi r^2\,\dd r = \frac43\pi\bigl(b^3 - a^3\bigr).
\]
Pour la seconde intégrale, l'intégrande $1/r$ ne dépend que de $r$~:
\[
\iiint_B \frac{\dd x\,\dd y\,\dd z}{r}
= \int_0^R 4\pi r^2\cdot\frac1r\,\dd r
= 4\pi\,\frac{R^2}{2} = 2\pi R^2 .
\]
(L'intégrande explose à l'origine, mais sans dommage~: $r^2/r = r$
est \omterm{def:b2:metric:continuity}{continue} --- l'intégrale sur les coquilles $\varepsilon \leq r
\leq R$ \omterm{def:b2:integration:improper}{converge} lorsque $\varepsilon \to 0$, ce qui est le sens précis
de l'énoncé. Ce genre de calcul est le premier pas vers le théorème de
Newton, selon lequel une boule homogène attire comme une masse
ponctuelle en son centre.)
\end{solution}

\begin{solution}{exo:b2:multint:8}
L'intégrande $g(t; x, y) = xP(tx, ty) + yQ(tx, ty)$ est
$\mathcal{C}^1$ en $(x, y)$, \omterm{def:b2:metric:continuity}{continu} en $t$, avec des dérivées
partielles \omterm{def:b2:metric:continuity}{continues} sur $[0,1] \times U$~; la dérivation sous le signe
intégral (\cref{ch:b2:integration}, appliquée sur l'intervalle \omterm{def:b2:metric:compact}{compact}
en $t$ $[0,1]$, la domination y étant automatique)
donne
\[
f_x(x, y)
= \int_0^1 \bigl(P(tx, ty) + tx\,P_x(tx, ty)
+ ty\,Q_x(tx, ty)\bigr)\dd t .
\]
En utilisant le caractère fermé $Q_x = P_y$~:
\[
tx\,P_x(tx, ty) + ty\,P_y(tx, ty)
= t\,\frac{\dd}{\dd t}\bigl[P(tx, ty)\bigr] ,
\]
donc l'intégrande est $P(tx, ty) + t\frac{\dd}{\dd t}P(tx, ty) =
\frac{\dd}{\dd t}\bigl[t\,P(tx, ty)\bigr]$ et
\[
f_x(x, y) = \Bigl[t\,P(tx, ty)\Bigr]_0^1 = P(x, y) .
\]
Symétriquement $f_y = Q$ (même calcul avec $P_y = Q_x$ utilisé
dans l'autre sens). Notons où l'hypothèse intervient~: $f$ est définie
en intégrant le long du segment $[0, M]$, qui est inclus dans $U$
précisément parce que $U$ est étoilé.
\end{solution}

\begin{solution}{exo:b2:multint:9}
Sur la bande $[0, A] \times [0, \infty)$, la fonction $(x, y)
\mapsto e^{-xy}\sin x$ n'est pas \omterm{def:b2:series:def}{absolument} intégrable jusqu'à $y =
\infty$ \omterm{def:b2:funcseq:def}{uniformément} au sens naïf, mais chaque intégrale itérée
\omterm{def:b2:integration:improper}{converge} et leur égalité découle de Fubini sur $[0, A]
\times [0, B]$ plus un passage à la limite $B \to \infty$ (la queue
$\int_0^A\int_B^\infty e^{-xy}\abs{\sin x}\,\dd y\,\dd x \leq
\int_0^A \frac{e^{-Bx}\abs{\sin x}}{x}\dd x \leq \int_0^A e^{-Bx}
\dd x\to 0$, en utilisant $\abs{\sin x} \leq x$).

\emph{$y$ d'abord~:} $\int_0^\infty e^{-xy}\,\dd y = \frac1x$ pour $x
> 0$, donc la première intégrale est $\int_0^A \frac{\sin x}{x}\,\dd x$.

\emph{$x$ d'abord~:} deux intégrations par parties (ou en prenant la
partie imaginaire de $\int_0^A e^{(i - y)x}\dd x$) donnent
\[
\int_0^A e^{-xy}\sin x\,\dd x
= \frac{1 - e^{-Ay}(y\sin A + \cos A)}{1 + y^2} .
\]
En intégrant en $y$ sur $[0, \infty)$, le terme $\int_0^\infty
\frac{\dd y}{1 + y^2} = \frac\pi2$ se détache~:
\[
\int_0^A \frac{\sin x}{x}\,\dd x
= \frac{\pi}{2}
- \int_0^\infty e^{-Ay}\,\frac{y\sin A + \cos A}{1 + y^2}\,\dd y .
\]
Le reste est majoré par $\int_0^\infty e^{-Ay}\frac{y +
1}{1 + y^2}\dd y \leq \int_0^\infty e^{-Ay}\cdot\frac{1+y}{1+y^2}
\,\dd y \to 0$ lorsque $A \to \infty$ (convergence dominée, ou la
majoration grossière $\frac{1 + y}{1 + y^2} \leq \frac32$ donnant
$\frac{3}{2A}$). Donc $\int_0^\infty\frac{\sin x}{x}\dd x =
\frac\pi2$ --- la même valeur obtenue au
\cref{ch:b2:integration} en dérivant une \omterm{thm:b2:integration:continuity}{intégrale à paramètre}~;
ici c'est Fubini qui fait le travail.
\end{solution}

\begin{solution}{exo:b2:multint:10}
Paramétrons par l'\omterm{def:b2:curves:length}{abscisse curviligne} $s \in [0, 2\pi]$, de sorte que
$x'^2 + y'^2 = 1$, et translatons pour que $\int_0^{2\pi} x(s)\,\dd s = 0$. Par
le~\cref{cor:b2:multint:area},
\[
2A = \oint x\,\dd y - y\,\dd x
= \int_0^{2\pi}\bigl(xy' - yx'\bigr)\dd s .
\]
En intégrant $\oint y\,\dd x$ par parties sur la période (les termes de
bord s'annulent par périodicité), $-\int yx' = \int y'x$, donc en fait
$2A = 2\int_0^{2\pi}xy'\,\dd s$. Alors $2xy' \leq x^2 + y'^2$
donne
\[
2A \leq \int_0^{2\pi}\bigl(x^2 + y'^2\bigr)\dd s
= \int_0^{2\pi} x^2 + \int_0^{2\pi}\bigl(1 - x'^2\bigr)
= 2\pi - \int_0^{2\pi}\bigl(x'^2 - x^2\bigr)\dd s .
\]
L'inégalité de Wirtinger (exercices du \cref{ch:b2:fourier}~: pour une
fonction $\mathcal{C}^1$ $2\pi$-périodique de moyenne nulle,
$\int x^2 \leq \int x'^2$) rend la dernière intégrale positive~:
$A \leq \pi$. L'égalité requiert l'égalité dans Wirtinger ($x(s) =
a\cos s + b\sin s$) et dans $2xy' \leq x^2 + y'^2$ ($y' = x$
ponctuellement), ce qui force $y = a\sin s - b\cos s + c$~: la courbe est
le cercle unité (convenablement centré). Puisqu'une courbe de \omterm{def:b2:curves:length}{longueur} $L$
se ramène par homothétie à la \omterm{def:b2:curves:length}{longueur} $2\pi$, l'énoncé général est $A \leq
\frac{L^2}{4\pi}$~: parmi toutes les courbes \omterm{def:b2:multint:exact}{fermées} de périmètre donné, le
cercle délimite la plus grande \omterm{def:b2:multint:domain}{aire}.
\end{solution}

\begin{solution}{exo:b2:multint:11}
En coordonnées sphériques $z = r\sin\varphi$ et $\dd x\,\dd
y\,\dd z = r^2\cos\varphi\,\dd r\,\dd\theta\,\dd\varphi$~:
\[
\iiint_B z^2
= \int_0^R r^4\,\dd r\int_0^{2\pi}\dd\theta
\int_{-\pi/2}^{\pi/2}\sin^2\varphi\cos\varphi\,\dd\varphi
= \frac{R^5}5\cdot2\pi\cdot
\Bigl[\frac{\sin^3\varphi}3\Bigr]_{-\pi/2}^{\pi/2}
= \frac{4\pi R^5}{15}.
\]
Par la symétrie de la boule sous permutation des coordonnées,
$\iiint_B x^2 = \iiint_B y^2 = \iiint_B z^2$, donc $\iiint_B(x^2
+ y^2 + z^2) = 3\cdot\frac{4\pi R^5}{15} = \frac{4\pi R^5}5$.
Vérification en coquilles~: $\int_0^R r^2\cdot 4\pi r^2\,\dd r = \frac{4\pi
R^5}5$ --- l'intégrande $r^2$ est constant sur la sphère de
rayon $r$, d'\omterm{def:b2:multint:domain}{aire} $4\pi r^2$.
\end{solution}

\begin{solution}{exo:b2:multint:12}
Paramétrons l'arête de $(x_i, y_i)$ à $(x_{i+1}, y_{i+1})$
par $\gamma(t) = \bigl((1-t)x_i + tx_{i+1},\ (1-t)y_i +
ty_{i+1}\bigr)$. Sa contribution à
$\frac12\oint(x\,\dd y - y\,\dd x)$ est
\[
\frac12\int_0^1\Bigl(\bigl((1-t)x_i +
tx_{i+1}\bigr)(y_{i+1} - y_i) - \bigl((1-t)y_i +
ty_{i+1}\bigr)(x_{i+1} - x_i)\Bigr)\dd t ,
\]
et puisque $\int_0^1\bigl((1-t)u + tv\bigr)\dd t = \frac{u +
v}2$, ceci vaut
\[
\frac14\Bigl((x_i + x_{i+1})(y_{i+1} - y_i) - (y_i +
y_{i+1})(x_{i+1} - x_i)\Bigr)
= \frac12\bigl(x_iy_{i+1} - x_{i+1}y_i\bigr),
\]
les termes croisés s'annulant. En sommant sur les $m$ arêtes on obtient
la formule du lacet, par le~\cref{cor:b2:multint:area}. Triangle
$(0,0), (1,0), (0,1)$~: $\frac12\bigl((0\cdot0 - 1\cdot0) +
(1\cdot1 - 0\cdot0) + (0\cdot0 - 0\cdot1)\bigr) = \frac12$,
l'\omterm{def:b2:multint:domain}{aire} correcte.
\end{solution}

\begin{solution}{pb:b2:multint:1}
\textbf{1.} La boule $B_n(R)$ est décrite par des bornes itérées
$-R \leq x_n \leq R$, puis $\abs{x_{n-1}} \leq \sqrt{R^2 -
x_n^2}$, et ainsi de suite~; substituer $x_i = Ru_i$ dans chacune des
$n$ intégrales à une variable multiplie chacune par $R$ et envoie
les bornes sur celles de $B_n(1)$~: $v_n(R) = R^n\,v_n(1) =
V_nR^n$.

\textbf{2.} En tranchant selon $x_n = t$~: la tranche de $B_n(1)$ est
la boule $B_{n-1}\bigl(\sqrt{1 - t^2}\bigr)$, donc, par la question
1,
\[
V_n = \int_{-1}^1 v_{n-1}\bigl(\sqrt{1 - t^2}\bigr)\,\dd t
= V_{n-1}\int_{-1}^{1}(1 - t^2)^{\frac{n-1}2}\,\dd t .
\]

\textbf{3.} Avec $t = \sin\theta$, $\dd t =
\cos\theta\,\dd\theta$ et $(1 - t^2)^{\frac{n-1}2} =
\cos^{n-1}\theta$ sur $\intcc{-\pi/2}{\pi/2}$~:
\[
\int_{-1}^1(1 - t^2)^{\frac{n-1}2}\dd t
= \int_{-\pi/2}^{\pi/2}\cos^n\theta\,\dd\theta
= 2\int_0^{\pi/2}\cos^n\theta\,\dd\theta = 2W_n,
\]
et $\theta \mapsto \frac\pi2 - \theta$ échange les formes en sinus
et en cosinus de $W_n$.

\textbf{4.} Écrivons $\sin^n = \sin^{n-2}(1 - \cos^2)$ et
intégrons $\int\sin^{n-2}\cos\cdot\cos$ par parties ($v =
\frac{\sin^{n-1}}{n-1}$)~:
\[
W_n = W_{n-2} - \frac{W_n}{n-1}
\quad\Longrightarrow\quad
W_n = \frac{n-1}{n}W_{n-2}.
\]
D'où $nW_nW_{n-1} = (n-1)W_{n-1}W_{n-2}$~: la suite
$(nW_nW_{n-1})$ est constante, égale à $1\cdot W_1W_0 =
1\cdot\frac\pi2$, donc $W_nW_{n-1} = \frac{\pi}{2n}$.

\textbf{5.} Les questions 2 et 3 donnent $V_n = 2W_nV_{n-1}$, deux fois~:
\[
V_n = 2W_n\cdot 2W_{n-1}\,V_{n-2}
= 4\,\frac{\pi}{2n}\,V_{n-2} = \frac{2\pi}n\,V_{n-2}.
\]

\textbf{6.} À partir de $V_0 = 1$~: $V_{2k} = \frac{2\pi}{2k}V_{2k-2}
= \frac\pi kV_{2k-2}$, donc $V_{2k} = \frac{\pi^k}{k!}$ par
récurrence. À partir de $V_1 = 2$~: $V_{2k+1} =
\frac{2\pi}{2k+1}V_{2k-1}$, donc
\[
V_{2k+1} = 2\prod_{j=1}^k\frac{2\pi}{2j+1}
= \frac{2^{k+1}\pi^k}{1\cdot3\cdot5\cdots(2k+1)} .
\]

\textbf{7.} $V_1 = 2$, $V_2 = \pi \approx 3.142$, $V_3 =
\frac{4\pi}3 \approx 4.189$, $V_4 = \frac{\pi^2}2 \approx
4.935$, $V_5 = \frac{8\pi^2}{15} \approx 5.264$, $V_6 =
\frac{\pi^3}6 \approx 5.168$, $V_7 = \frac{16\pi^3}{105}
\approx 4.725$. Le rapport à un pas est $V_n/V_{n-1} = 2W_n$,
et $(W_n)$ est décroissante ($\sin^n \leq \sin^{n-1}$
ponctuellement). Or $2W_5 = 2\cdot\frac45\cdot\frac23 =
\frac{16}{15} > 1$ tandis que $2W_6 =
2\cdot\frac56\cdot\frac34\cdot\frac12\cdot\frac\pi2 =
\frac{5\pi}{16} < 1$~: les rapports dépassent $1$ jusqu'à $n = 5$ et
sont inférieurs à $1$ à partir de $n = 6$ --- $(V_n)$ croît jusqu'à son
maximum $V_5$ puis décroît.

\textbf{8.} Pour $n \geq 13 > 4\pi$~: $V_n/V_{n-2} = 2\pi/n <
\tfrac12$, donc $V_{n} \leq C\cdot 2^{-n/2}$ avec une constante
fixe~; mieux, pour tout $q > 0$, $2\pi/n < q^2$ pour $n$ grand,
donc $V_n/q^n \to 0$~: la décroissance bat toute suite géométrique.
La convergence de $\sum V_n$ découle du rapport
$V_n/V_{n-2} \to 0$ (comparaison avec une série géométrique à partir
d'un certain rang).

\textbf{9.} $\sum_{k\geq0}V_{2k}x^{2k} =
\sum_{k\geq0}\frac{(\pi x^2)^k}{k!} = \eu^{\pi x^2}$, la
série exponentielle (\cref{ch:b2:powerseries}), convergente pour
tout $x$. En $x = 1$~: $\sum_kV_{2k} = \eu^\pi \approx
23.14$.

\textbf{10.} Sur le cube $\intcc{-R}R^n$, l'intégrande est le
produit $\prod_i\eu^{-x_i^2}$, donc l'intégrale itérée se
factorise~: $\bigl(\int_{-R}^R\eu^{-t^2}\dd t\bigr)^n$.
En faisant $R \to \infty$ et en utilisant
$\int_\R\eu^{-t^2}\dd t = \sqrt\pi$
(\cref{ex:b2:multint:polar})~: $I_n = \pi^{n/2}$.

\textbf{11.} $t = u^2$ donne $\Gamma(\tfrac12) =
\int_0^\infty t^{-1/2}\eu^{-t}\dd t =
2\int_0^\infty\eu^{-u^2}\dd u = \sqrt\pi$. En itérant
$\Gamma(s+1) = s\Gamma(s)$~: $\Gamma(k+1) = k!\,\Gamma(1) =
k!$, et
\[
\Gamma\Bigl(k + \frac32\Bigr)
= \Bigl(k + \frac12\Bigr)\Bigl(k - \frac12\Bigr)\cdots
\frac12\cdot\Gamma\Bigl(\frac12\Bigr)
= \frac{(2k+1)(2k-1)\cdots1}{2^{k+1}}\,\sqrt\pi .
\]

\textbf{12.} Posons $F_n = \pi^{n/2}/\Gamma(\frac n2 + 1)$.
Puisque $\Gamma(\frac n2 + 1) = \frac n2\,\Gamma(\frac n2) =
\frac n2\,\Gamma(\frac{n-2}2 + 1)$, on obtient $F_n =
\frac{2\pi}nF_{n-2}$~: la même récurrence que $V_n$ (question
5). Bases~: $F_1 = \sqrt\pi/\Gamma(\frac32) =
\sqrt\pi/(\frac{\sqrt\pi}2) = 2 = V_1$ et $F_2 =
\pi/\Gamma(2) = \pi = V_2$. Par récurrence $V_n = F_n$ pour tout
$n$~; la question 11 la retraduit en les deux expressions explicites
de la question 6.

\textbf{13.} Avec $r = \sqrt t$~: $\int_0^\infty
\eu^{-r^2}r^{n-1}\dd r = \frac12\int_0^\infty
t^{\frac n2 - 1}\eu^{-t}\dd t = \frac12\Gamma(\frac n2)$.
D'où
\[
n\,V_n\int_0^\infty\eu^{-r^2}r^{n-1}\dd r
= V_n\cdot\frac n2\,\Gamma\Bigl(\frac n2\Bigr)
= V_n\,\Gamma\Bigl(\frac n2 + 1\Bigr) = \pi^{n/2} = I_n .
\]
Les deux membres étant prouvés, l'identité peut se \emph{lire} comme
la décomposition en coquilles de l'intégrale gaussienne~: la sphère
de rayon $r$ porte l'\omterm{def:b2:multint:domain}{aire} $nV_nr^{n-1}$, et le poids
gaussien $\eu^{-r^2}$ est intégré sur les coquilles.

\textbf{14.} $s_0 = V_1 = 2$ (la $0$-sphère est deux points),
$s_1 = 2V_2 = 2\pi$, $s_2 = 3V_3 = 4\pi$, $s_3 = 4V_4 =
2\pi^2$~; et $\int_0^R s_{n-1}r^{n-1}\dd r = V_nR^n =
v_n(R)$~: l'\omterm{def:b2:multint:domain}{aire} est la dérivée radiale du volume.

\textbf{15.} Pour $n = 2k$, Stirling
(\cref{thm:b2:comparison:stirling}) donne $k! \sim
\sqrt{2\pi k}\,(k/\eu)^k$, donc
\[
V_{2k} = \frac{\pi^k}{k!}
\sim \frac{(\pi\eu/k)^k}{\sqrt{2\pi k}}
= \frac1{\sqrt{\pi n}}\Bigl(\frac{2\pi\eu}n\Bigr)^{n/2}
\qquad (n = 2k).
\]
Pour $n$ impair~: $V_{2k+1} = 2W_{2k+1}V_{2k} \leq 2V_{2k}$, donc
les mêmes bornes de décroissance super-géométrique valent (à un facteur
$2$ et un décalage de un dans l'exposant près) --- pour tout $q >
0$, $V_n = o(q^n)$.

\textbf{16.} $V_2/4 = \pi/4 \approx 0.785$~; $V_3/8 = \pi/6
\approx 0.524$~; $V_{10}/2^{10} = \frac{\pi^5}{120\cdot1024}
\approx 0.0025$. En général $\frac{V_n/2^n}{V_{n-2}/2^{n-2}}
= \frac{2\pi}{4n} = \frac{\pi}{2n} \to 0$~: le rapport tend vers
$0$ (super-géométriquement). La boule inscrite occupe une
fraction négligeable~: le volume du cube migre vers ses
coins.

\textbf{17.} Par la question 1 la boule intérieure de rayon $1 -
\varepsilon$ a pour volume $V_n(1-\varepsilon)^n$, donc la coquille
extérieure porte la fraction $1 - (1 - \varepsilon)^n \to 1$.
Pour $n = 100$, $\varepsilon = 0.01$~: $(0.99)^{100} =
\eu^{100\ln0.99} \approx \eu^{-1.005} \approx 0.366$~: environ
$63\%$ de la boule se trouve à moins de $1\%$ de sa surface.

\textbf{18.} $(W_n)$ décroît, donc $W_n \leq W_{n-1} \leq
W_{n-2} = \frac{n}{n-1}W_n$~: par encadrement, $W_{n-1}/W_n \to 1$.
En multipliant par $W_nW_{n-1} = \frac\pi{2n}$~: $W_n^2 \sim
\frac\pi{2n}$, c.-à-d.\ $W_n \sim \sqrt{\pi/(2n)}$. Minoration~:
$W_n^2 \geq W_nW_{n+1} = \frac{\pi}{2(n+1)}$, donc $W_n \geq
\sqrt{\pi/(2(n+1))}$ pour tout $n$.

\textbf{19.} Numérateur~: $1 - x^2 \leq \eu^{-x^2}$ donne $(1 -
x^2)^{\frac{n-1}2} \leq \eu^{-(n-1)x^2/2}$, et avec $a =
\frac{n-1}2$,
\[
\int_\delta^1(1 - x^2)^{\frac{n-1}2}\dd x
\leq \int_\delta^\infty\eu^{-ax^2}\dd x
\leq \int_\delta^\infty\frac x\delta\,\eu^{-ax^2}\dd x
= \frac{\eu^{-a\delta^2}}{2a\delta}
= \frac{\eu^{-(n-1)\delta^2/2}}{(n-1)\delta}.
\]
Dénominateur~: $2W_n \geq \sqrt{2\pi/(n+1)}$ par la question 18.
En divisant, on obtient la borne affichée. Pour $\delta =
s/\sqrt{n-1}$ elle devient
$\sqrt{\tfrac{n+1}{2\pi(n-1)}}\;\eu^{-s^2/2}/s = O\bigl(
\eu^{-s^2/2}/s\bigr)$, \omterm{def:b2:funcseq:def}{uniformément} en $n$~: en dehors de la dalle
$\abs{x_1} \leq s/\sqrt{n-1}$ il n'y a presque pas de volume, pour
$s$ modérément grand --- et par symétrie il en va de même pour
toute direction.

\textbf{20.} Les deux affirmations coexistent parce qu'elles décrivent
des coordonnées différentes du même point. Presque tout point
de $B_n(1)$ a une \omterm{def:b2:nvs:norm}{norme} proche de $1$ (question 17~: concentration
radiale près de la sphère), et pourtant chacune de ses $n$
coordonnées est petite, d'\omterm{def:b2:structures:generated}{ordre} $1/\sqrt n$ (question 19),
ce qui est cohérent puisque $n$ coordonnées de taille $1/\sqrt n$
ont une \omterm{def:b2:nvs:norm}{norme} d'\omterm{def:b2:structures:generated}{ordre} $1$. Le volume en grande dimension se concentre
là où toutes les coordonnées se partagent également le budget de \omterm{def:b2:nvs:norm}{norme}
--- près de la sphère, mais loin de tout pôle d'axe de coordonnée.

\textbf{21.} Tranchons $\Delta_n$ en $x_n = t \in \intcc01$~: la
tranche est $\{x' \in \R^{n-1} : x_i \geq 0,\ \sum x_i \leq 1 -
t\} = (1-t)\Delta_{n-1}$, de volume
$(1-t)^{n-1}\operatorname{vol}(\Delta_{n-1})$ par homogénéité.
Donc
\[
\operatorname{vol}(\Delta_n) =
\operatorname{vol}(\Delta_{n-1})\int_0^1(1 - t)^{n-1}\dd t =
\frac{\operatorname{vol}(\Delta_{n-1})}{n}
\quad\Longrightarrow\quad
\operatorname{vol}(\Delta_n) = \frac1{n!}\,.
\]

\textbf{22.} Les $2^n$ orthants de signes découpent $C_n$ en $2^n$
copies de $\Delta_n$ (avec des recouvrements négligeables sur les
hyperplans de coordonnées)~: $\operatorname{vol}(C_n) = \frac{2^n}{n!}$. Si
$\sum\abs{x_i} \leq 1$ alors $\sum x_i^2 \leq
\bigl(\sum\abs{x_i}\bigr)^2 \leq 1$~: $C_n \subseteq B_n(1)$~;
et $B_n(1) \subseteq \intcc{-1}1^n$ puisque $\abs{x_i} \leq
\norm x$. D'où $\frac{2^n}{n!} \leq V_n \leq 2^n$ ---
en cohérence avec la question 15, qui place $V_n$ entre les échelles
factorielle et géométrique.

\textbf{23.} Pour $(x, y)$ dans le disque unité, la tranche de
$B_4(1)$ est le disque de rayon $\sqrt{1 - x^2 - y^2}$ dans le
plan des $(z, w)$, d'\omterm{def:b2:multint:domain}{aire} $\pi(1 - x^2 - y^2)$. En polaires~:
\[
V_4 = \iint_{x^2+y^2\leq1}\pi(1 - x^2 - y^2)\,\dd x\,\dd y
= \pi\int_0^{2\pi}\!\!\int_0^1(1 - \rho^2)\rho\,
\dd\rho\,\dd\alpha
= \pi\cdot2\pi\cdot\frac14 = \frac{\pi^2}2 ,
\]
en accord avec la question 6.

\textbf{24.} La probabilité est le rapport de volumes
$\dfrac{V_{20}}{2^{20}} = \dfrac{\pi^{10}}{10!\cdot2^{20}}
\approx \dfrac{0.0258}{1\,048\,576} \approx
2.5\cdot10^{-8}$. Le nombre de tirages jusqu'au premier succès est
d'\omterm{def:b2:structures:generated}{ordre} l'inverse, environ $4\cdot10^7$~: un échantillonneur par rejet
qui fonctionnait à merveille pour le disque ($\pi/4$ de succès) est
inutile en dimension $20$ --- le fléau de la dimension en
une ligne.

\textbf{25.} La voie une (Parties I--II) a utilisé~: le découpage en
tranches de type Fubini de l'intégrale itérée, la substitution à une
variable dans chaque coordonnée (homogénéité), et les \omterm{pb:b2:multint:1}{intégrales de Wallis}
--- pur calcul à une variable plus récurrence. La voie deux
(Partie III) a utilisé~: Fubini pour la structure de produit de $I_n$,
le changement de variables polaire à travers l'intégrale de Gauss de
\cref{ex:b2:multint:polar}, et l'équation fonctionnelle de la fonction
$\Gamma$. Elles se rejoignent en $V_n =
\pi^{n/2}/\Gamma(\frac n2 + 1)$, avec Stirling
(\cref{thm:b2:comparison:stirling}) convertissant la formule
en asymptotique. Le volume de Licence~3 reconstruit tout ceci sur
l'intégrale de Lebesgue~: là, Fubini et le changement de variables
sont des théorèmes pour des fonctions intégrables générales, les
coordonnées sphériques existent en toute dimension, et les mêmes volumes
de boule réapparaissent comme les dividendes travaillés des
problèmes de mesure produit et de Stirling --- avec la convergence
dominée remplaçant nos encadrements faits main.
\end{solution}
