\section*{Chapitre \ref{ch:b2:quadratic} --- Formes quadratiques}

\begin{solution}{exo:b2:quadratic:1}
$q_1 = (x + 2y)^2 - 3y^2$~: rang $2$, signature $(1, 1)$ (une
forme de type hyperbolique).

$q_2$~: on complète le carré en $x$~: $q_2 = (x + y)^2 + y^2 + 2yz +
3z^2 = (x+y)^2 + (y + z)^2 + 2z^2$~: rang $3$, signature $(3, 0)$
--- définie positive.
\end{solution}

\begin{solution}{exo:b2:quadratic:2}
\omterm{def:b2:reduction:eigen}{Valeurs propres} $2$ (sur $\operatorname{Vect}(1,1,1)$) et $-1$ (sur
le plan $x + y + z = 0$). Orthonormalisation~: $e_1 =
\frac{1}{\sqrt3}(1,1,1)$~; dans le plan, Gram--Schmidt sur $(1,-1,0),
(1,0,-1)$ donne $e_2 = \frac{1}{\sqrt2}(1,-1,0)$, $e_3 =
\frac{1}{\sqrt6}(1,1,-2)$. Alors $P = (e_1\ e_2\ e_3)$ est
orthogonale avec $P^{\mathsf T}AP = \operatorname{diag}(2,-1,-1)$.

La forme $q = 2xy + 2yz + 2zx$ a pour matrice $A$~: dans les
coordonnées tournées $q = 2X^2 - Y^2 - Z^2$ --- axes principaux~;
signature $(1,2)$, en accord avec l'\cref{ex:b2:quadratic:gaussexample}
(la même forme~!).
\end{solution}

\begin{solution}{exo:b2:quadratic:3}
$u^{**} = u$~: $\langle u^{**}x, y\rangle = \langle x, u^*y\rangle =
\langle ux, y\rangle$ pour tout $y$. $(uv)^* = v^*u^*$~: $\langle
uvx, y\rangle = \langle vx, u^*y\rangle = \langle x,
v^*u^*y\rangle$. Noyau~: $y \in \ker u^* \iff \langle x, u^*y
\rangle = 0\ \forall x \iff \langle u(x), y\rangle = 0\ \forall x
\iff y \perp \operatorname{im} u$. Rangs~: $\dim\ker u^* = n -
\operatorname{rk} u$ (supplémentaire orthogonal), donc
$\operatorname{rk} u^* = \operatorname{rk} u$ par le théorème du rang
--- l'avatar euclidien du théorème sur le rang de la \omterm{def:b2:linalg:transpose}{transposée}.
\end{solution}

\begin{solution}{exo:b2:quadratic:4}
Spectral~: $A = PDP^{\mathsf T}$, $D$ diagonale à coefficients
$\lambda_i$ vérifiant $\lambda_i^3 = \lambda_i$~: $\lambda_i \in
\{-1, 0, 1\}$. Alors $A^2 = PD^2P^{\mathsf T}$ avec $D^2$ diagonale
à coefficients $0/1$~: $A^2$ est \omterm{def:b2:quadratic:adjoint}{symétrique} et idempotente
($(A^2)^2 = A^4 = A\cdot A^3 = A^2$) --- \omterm{def:b2:quadratic:adjoint}{symétrique} idempotente
$=$ projection orthogonale (c'est la projection sur
$\ker(A^2 - I) = \ker(A-I)\oplus\ker(A+I)$ parallèlement à $\ker A$,
et ceux-ci sont orthogonaux par le théorème spectral).

En général, $P(A) = P\!\left(\text{diag}\right)$~: $P(A)$ a les
mêmes vecteurs propres, pour \omterm{def:b2:reduction:eigen}{valeurs propres} $P(\lambda_i)$ ---
l'«~image spectrale~» au niveau \omterm{def:b2:reduction:diag}{diagonalisable}.
\end{solution}

\begin{solution}{exo:b2:quadratic:5}
Fermé~: $O(n) = g^{-1}(\{I\})$ pour l'application \omterm{def:b2:metric:continuity}{continue} $g(P) =
P^{\mathsf T}P$ (coefficients polynomiaux). Borné~: chaque colonne
de $P \in O(n)$ est un vecteur unitaire, donc tous les coefficients
sont dans $\intcc{-1}{1}$. Fermé et borné dans $\mathcal{M}_n(\R)
\simeq \R^{n^2}$~: \omterm{def:b2:metric:compact}{compact} (\cref{thm:b2:metric:compactprops}~(2)).

Non \omterm{def:b2:metric:connected}{connexe}~: $\det$ prend les deux valeurs $\pm1$ sur $O(n)$, et
une surjection \omterm{def:b2:metric:continuity}{continue} sur $\{-1, 1\}$ scinde l'espace
(l'argument de l'\cref{ex:b2:metric:glnr}).
\end{solution}

\begin{solution}{exo:b2:quadratic:6}
\emph{Existence~:} $A = PDP^{\mathsf T}$ avec $D =
\operatorname{diag}(\lambda_i)$, $\lambda_i \geq 0$~; posons $B =
P\sqrt D P^{\mathsf T}$ avec $\sqrt D =
\operatorname{diag}(\sqrt{\lambda_i})$~: \omterm{def:b2:quadratic:adjoint}{symétrique}, semi-définie
positive, $B^2 = A$.

\emph{Unicité~:} soit $B$ \omterm{def:b2:quadratic:adjoint}{symétrique} semi-définie positive avec $B^2
= A$. $B$ commute avec $A$~; donc $B$ préserve chaque sous-espace
propre $E_\lambda(A)$ (pour $Ax = \lambda x$~: $A(Bx) = BAx =
\lambda Bx$). Sur $E_\lambda(A)$, la restriction de $B$ est
\omterm{def:b2:quadratic:adjoint}{symétrique} semi-définie positive de carré $\lambda\,\mathrm{id}$~;
ses \omterm{def:b2:reduction:eigen}{valeurs propres} $\mu$ vérifient $\mu^2 = \lambda$, $\mu \geq 0$~:
$\mu = \sqrt\lambda$ --- donc la restriction, étant \omterm{def:b2:reduction:diag}{diagonalisable}
de seule \omterm{def:b2:reduction:eigen}{valeur propre} $\sqrt\lambda$, \emph{est}
$\sqrt\lambda\,\mathrm{id}$. Comme $E = \bigoplus E_\lambda(A)$, $B$
est déterminée~: $B = \sqrt A$.
\end{solution}

\begin{solution}{exo:b2:quadratic:7}
Dans une base propre orthonormée, $\norm{Ax}_2^2 = \sum \lambda_i^2
x_i^2 \leq (\max_i \lambda_i^2)\norm x_2^2$, avec égalité au
vecteur propre correspondant~: $\vertiii A_2 = \max\abs{\lambda_i}$.
Pour la matrice donnée~: \omterm{def:b2:reduction:eigen}{valeurs propres} $3, -1$
(la jumelle de l'\cref{ex:b2:quadratic:spectralexample})~:
$\vertiii A_2 = 3$.
\end{solution}

\begin{solution}{exo:b2:quadratic:8}
($\Rightarrow$) Le bloc $k \times k$ en haut à gauche $A_k$ est la
matrice de la restriction de la forme (définie) au sous-espace
engendré par les $k$ premiers vecteurs de base~: définie positive,
donc ses \omterm{def:b2:reduction:eigen}{valeurs propres} sont positives et $\Delta_k = \det A_k >
0$.

($\Leftarrow$) Récurrence sur $n$~; $n = 1$ clair. Supposons tous
les $\Delta_k > 0$. Par récurrence, $A_{n-1}$ est définie positive~:
la forme $q$ restreinte à $F = \operatorname{Vect}(e_1, \dots,
e_{n-1})$ est définie. Diagonalisons $q|_F$ (Gauss)~: coordonnées
$y_1, \dots, y_{n-1}$ avec $q|_F = \sum y_i^2$. Dans l'espace
entier, en complétant le carré en la dernière variable,
\[
q = \sum_{i=1}^{n-1} \bigl(y_i + c_i x_n\bigr)^2 + c\,x_n^2
\]
pour des constantes convenables (regrouper les termes croisés dans
les carrés). La réduction exhibe une signature $(n-1 + \epsilon,
\cdot)$ où $\epsilon$ est la contribution de signe de $c$~; et le
\omterm{def:b2:linalg:det}{déterminant} conserve le signe du produit des coefficients diagonaux
par \omterm{def:b2:quadratic:def}{congruence} ($\det(P^{\mathsf T}AP) = (\det P)^2\det A$)~:
$\Delta_n > 0$ force $c > 0$. Ainsi $q$ est une somme de $n$ carrés
de formes indépendantes~: définie positive.
\end{solution}

\begin{solution}{exo:b2:quadratic:9}
Soit $(e_1, \dots, e_n)$ une base propre orthonormée pour $\lambda_1
\geq \dots \geq \lambda_n$.

\emph{$\lambda_2 \leq$ le min-max~:} pour tout hyperplan $H$, le
sous-espace $V = \operatorname{Vect}(e_1, e_2)$ de dimension $2$
vérifie $\dim(H \cap V) \geq 1$ (Grassmann)~: on choisit un $x \in H
\cap V$ unitaire, $x = ae_1 + be_2$, $a^2 + b^2 = 1$~:
\[
\langle u(x), x\rangle = \lambda_1 a^2 + \lambda_2 b^2 \geq
\lambda_2 :
\]
le maximum de tout hyperplan est $\geq \lambda_2$.

\emph{$\geq$~:} pour $H = e_1^{\perp}$, tout $x = \sum_{i\geq2}
x_ie_i \in H$ unitaire vérifie $\langle u(x), x\rangle = \sum_{i
\geq 2} \lambda_i x_i^2 \leq \lambda_2$, atteint en $e_2$~: le
maximum de cet hyperplan est exactement $\lambda_2$. Le minimum sur
$H$ est donc $\lambda_2$.
\end{solution}

\begin{solution}{exo:b2:quadratic:10}
\emph{Voie algébrique~:} $2q = \bigl(\sum x_i\bigr)^2 - \sum
x_i^2$. Sur l'hyperplan $H : \sum x_i = 0$ (dimension $n -
1$), $q = -\frac12\sum x_i^2$ est définie négative~; sur la droite
$\R(1, \dots, 1)$, $q(t, \dots, t) = \binom n2 t^2 > 0$. Un
sous-espace où $q$ est définie positive rencontre $H$ trivialement,
donc est de dimension $\leq 1$~: par Sylvester
(\cref{thm:b2:quadratic:sylvester}), $s = 1$, et $t \geq n-1$
grâce à $H$~; le rang $\leq n$ force la signature $(1, n-1)$, rang
$n$.

\emph{Voie spectrale~:} la matrice est $\frac12(J - I)$~; $J$ a pour
\omterm{def:b2:reduction:eigen}{valeurs propres} $n$ (sur $(1,\dots,1)$) et $0$ (sur $H$), donc
$\frac12(J-I)$ a pour \omterm{def:b2:reduction:eigen}{valeurs propres} $\frac{n-1}{2}$ (une fois) et
$-\frac12$ ($n-1$ fois)~: une positive, $n-1$ négatives --- la même
signature, d'après le~\cref{cor:b2:quadratic:principalaxes}.
\end{solution}

\begin{solution}{exo:b2:quadratic:11}
Écrivons $B = C^{\mathsf T}C$ (\cref{exo:b2:quadratic:6} via $C =
\sqrt B$). Alors, avec $c_1, \dots, c_n$ les colonnes de
$C^{\mathsf T}$~:
\[
\operatorname{tr}(AB) = \operatorname{tr}(AC^{\mathsf T}C)
= \operatorname{tr}(CAC^{\mathsf T})
= \sum_{i=1}^n \langle A c_i, c_i\rangle .
\]
D'après le~\cref{cor:b2:quadratic:principalaxes}~(2), chaque terme
est compris entre $\lambda_{\min}(A)\norm{c_i}^2$ et
$\lambda_{\max}(A)\norm{c_i}^2$, et $\sum\norm{c_i}^2 =
\operatorname{tr}(C^{\mathsf T}C)^{\vphantom1} =
\operatorname{tr} B$~: la double inégalité s'ensuit. Si $A$ est
aussi semi-définie positive, $\lambda_{\min}(A) \geq 0$~:
$\operatorname{tr}(AB) \geq 0$.
\end{solution}

\begin{solution}{exo:b2:quadratic:12}
\begin{enumerate}
  \item $\varphi(M, N) = \operatorname{tr}(MN)$ est bilinéaire et
        \omterm{def:b2:quadratic:adjoint}{symétrique} ($\operatorname{tr}(MN) =
        \operatorname{tr}(NM)$), et $\varphi(M, M) = q(M)$~:
        $q$ est la \omterm{def:b2:quadratic:def}{forme quadratique} de $\varphi$.
  \item Pour $S$ \omterm{def:b2:quadratic:adjoint}{symétrique} et $K$ antisymétrique~:
        $\operatorname{tr}(SK) =
        \operatorname{tr}\bigl((SK)^{\mathsf T}\bigr) =
        \operatorname{tr}(K^{\mathsf T}S^{\mathsf T}) =
        -\operatorname{tr}(KS) = -\operatorname{tr}(SK)$, donc
        $\varphi(S, K) = 0$~: les deux sous-espaces sont
        $\varphi$-orthogonaux. Coefficient par coefficient,
        $\operatorname{tr} (M^2) = \sum_{i,j} m_{ij}m_{ji}$~: pour
        $M$ \omterm{def:b2:quadratic:adjoint}{symétrique} cela vaut $\sum m_{ij}^2 > 0$ ($M \neq 0$)~;
        pour $M$ antisymétrique cela vaut $-\sum m_{ij}^2 < 0$.
  \item $\mathcal M_n(\R) = S_n \oplus A_n$ de dimensions
        $\frac{n(n+1)}2$ et $\frac{n(n-1)}2$~; une \omterm{thm:b2:quadratic:gauss}{réduction de
        Gauss} adaptée à cette décomposition $\varphi$-orthogonale
        écrit $q$ comme $\frac{n(n+1)}2$ carrés positifs et
        $\frac{n(n-1)}2$ carrés négatifs~: signature
        $\bigl(\frac{n(n+1)}2, \frac{n(n-1)}2\bigr)$
        (Sylvester), rang $n^2$~: la forme est non dégénérée.
\end{enumerate}
\end{solution}

\begin{solution}{pb:b2:quadratic:1}
\textbf{1.} $G$ est \omterm{def:b2:quadratic:adjoint}{symétrique} par symétrie du produit scalaire,
et
\[
X^{\mathsf T}GX = \sum_{i,j}x_ix_j\langle v_i, v_j\rangle
= \Bigl\|\sum_i x_iv_i\Bigr\|^2 \geq 0 ,
\]
avec égalité ssi $\sum x_iv_i = 0$~: $G$ est définie ssi la
seule combinaison nulle est triviale, c'est-à-dire ssi la famille
est indépendante.

\textbf{2.} Avec $B = \sqrt A$ (\cref{exo:b2:quadratic:6})~: $A
= B^2 = B^{\mathsf T}B$, la matrice de Gram des colonnes de $B$~;
prendre $C = B$. Et $X^{\mathsf T}AX = \norm{CX}^2$, donc $A$ est
définie ssi $CX \neq 0$ pour $X \neq 0$, ssi $C$ est inversible.

\textbf{3.} La restriction de la forme à
$\operatorname{Vect}(e_i, e_j)$ a pour matrice
$\begin{pmatrix} a_{ii} & a_{ij}\\ a_{ij} & a_{jj}
\end{pmatrix}$, encore semi-définie positive~: son \omterm{def:b2:linalg:det}{déterminant}
(produit de ses \omterm{def:b2:reduction:eigen}{valeurs propres} positives ou nulles) est $\geq 0$~:
$a_{ij}^2 \leq a_{ii}a_{jj}$. C'est Cauchy--Schwarz pour les
vecteurs $v_i, v_j$ d'une réalisation de Gram.

\textbf{4.} \emph{Existence~:} écrire $A$ comme la matrice de Gram
d'une famille indépendante $(v_1, \dots, v_n)$ (questions~1--2).
Gram--Schmidt produit une famille orthonormée $(e_1, \dots, e_n)$
avec
\[
v_k = \sum_{i \leq k} t_{ik}\,e_i,
\qquad t_{kk} = \bigl\| v_k - \operatorname{proj}_{k-1}v_k
\bigr\| > 0 ,
\]
donc $T = (t_{ik})$ est triangulaire supérieure à diagonale
positive, et
\[
a_{jk} = \langle v_j, v_k\rangle
= \sum_i t_{ij}t_{ik} = (T^{\mathsf T}T)_{jk} .
\]
\emph{Unicité~:} si $T_1^{\mathsf T}T_1 = T_2^{\mathsf T}T_2$
alors $R = T_1T_2^{-1}$ vérifie $R^{\mathsf T}R = I$~: $R$ est
orthogonale, et aussi triangulaire supérieure à diagonale positive
(produit de telles matrices). Alors $R^{-1} = R^{\mathsf T}$ est
simultanément triangulaire supérieure (inverse d'une supérieure) et
inférieure (\omterm{def:b2:linalg:transpose}{transposée} d'une supérieure)~: diagonale~; une matrice
diagonale orthogonale a des coefficients $\pm1$, et la positivité
force $R = I$~: $T_1 = T_2$.

\textbf{5.} Pour $i, j \leq k$, $(T^{\mathsf T}T)_{ij} = \sum_m
t_{mi}t_{mj}$ ne fait intervenir que $m \leq \min(i,j) \leq k$~: le
bloc $k\times k$ dominant de $A$ est $T_k^{\mathsf T}T_k$ où
$T_k$ est le bloc dominant de $T$. Ainsi $\Delta_k = (\det T_k)^2
= (t_{11}\cdots t_{kk})^2$. Or la \omterm{thm:b2:quadratic:gauss}{réduction de Gauss} d'une
forme définie positive dans l'\omterm{def:b2:structures:generated}{ordre} naturel ne rencontre jamais un
coefficient de carré nul (les pivots sont les coefficients
diagonaux des blocs définis positifs successivement réduits)~: elle
produit $q = \sum_k d_k\ell_k^2$ avec $\ell_k = x_k + (\text{termes
en } x_{k+1}, \dots)$, c'est-à-dire $A = L^{\mathsf T}DL$ avec $L$
triangulaire unipotente~; alors $T = \sqrt D\,L$ est un facteur de
Cholesky, donc par unicité $t_{kk}^2 = d_k$ et
\[
d_k = \frac{(t_{11}\cdots t_{kk})^2}
{(t_{11}\cdots t_{k-1,k-1})^2}
= \frac{\Delta_k}{\Delta_{k-1}} .
\]
Sur l'\cref{ex:b2:quadratic:tworoads}~: $\Delta_1, \Delta_2,
\Delta_3 = 2, 3, 4$ et les pivots étaient $2, \frac32, \frac43$.

\textbf{6.} Chaque $a_{ii} = e_i^{\mathsf T}Ae_i > 0$. Posons $D =
\operatorname{diag}(a_{ii}^{-1/2})$ et $B = DAD$~: définie
positive (\omterm{def:b2:quadratic:def}{congruence}), avec $b_{ii} = 1$, donc $\operatorname{tr}
B = n$. Ses \omterm{def:b2:reduction:eigen}{valeurs propres} $\mu_i > 0$ vérifient, par l'inégalité
arithmético-géométrique,
\[
\det B = \prod_i\mu_i
\leq \Bigl(\frac{\sum\mu_i}{n}\Bigr)^{\!n} = 1 ,
\]
et $\det B = (\det D)^2\det A = \dfrac{\det A}{\prod
a_{ii}}$~: $\det A \leq \prod a_{ii}$.

\textbf{7.} L'inégalité arithmético-géométrique est une égalité ssi
tous les $\mu_i$ sont égaux (à $1$)~; une matrice \omterm{def:b2:quadratic:adjoint}{symétrique} de
seule \omterm{def:b2:reduction:eigen}{valeur propre} $1$ est $PIP^{\mathsf T} = I$. Donc égalité ssi
$B = I$ ssi $a_{ij} = 0$ pour $i \neq j$~: $A$ diagonale.

\textbf{8.} Si $M$ est singulière, les deux membres sont $\geq 0 =
\abs{\det M}$. Sinon $A = M^{\mathsf T}M$ est définie positive avec
$a_{ii} = \norm{c_i}^2$ et $\det A = (\det M)^2$~: la question~6
donne $(\det M)^2 \leq \prod\norm{c_i}^2$. Égalité ssi $A =
M^{\mathsf T}M$ est diagonale (question~7), c'est-à-dire ssi les
colonnes sont deux à deux orthogonales.

\textbf{9.} $\abs{\det M}$ est le volume du parallélépipède
engendré par les colonnes~: le volume est au plus le produit des
longueurs d'arêtes, avec égalité exactement pour les pavés
rectangles. Si $\abs{m_{ij}} \leq 1$ alors $\norm{c_i} \leq \sqrt
n$, donc $\abs{\det M} \leq n^{n/2}$. (L'atteindre force des
colonnes orthogonales à coefficients $\pm1$~: une matrice de
Hadamard.)

\textbf{10.} $X^{\mathsf T}A^{\mathsf T}AX = \norm{AX}^2 > 0$
pour $X \neq 0$ ($A$ inversible)~: $A^{\mathsf T}A$ est définie
positive. Sa racine carrée $S$ est définie positive (\omterm{def:b2:reduction:eigen}{valeurs propres}
$\sqrt{\lambda_i} > 0$), donc inversible, et $Q = AS^{-1}$
vérifie
\[
Q^{\mathsf T}Q = S^{-1}A^{\mathsf T}AS^{-1}
= S^{-1}S^2S^{-1} = I :
\]
$A = QS$ avec $Q$ orthogonale, $S$ définie positive.

\textbf{11.} Si $A = QS = Q'S'$ alors $S'^{\,2} =
S'^{\mathsf T}Q'^{\mathsf T}Q'S' = A^{\mathsf T}A = S^2$~; deux
matrices semi-définies positives de même carré coïncident
(\cref{exo:b2:quadratic:6})~: $S' = S$, puis $Q' = AS^{-1} =
Q$.

\textbf{12.} $\det(A + \varepsilon I)$ est un polynôme non nul
en $\varepsilon$~: il a un nombre fini de racines, donc une suite
$\varepsilon_k \to 0$ les évite. Écrire $A + \varepsilon_kI =
Q_kS_k$ (question~10). $O(n)$ est \omterm{def:b2:metric:compact}{compact}
(\cref{exo:b2:quadratic:5})~: une sous-suite donne $Q_{\varphi(k)}
\to Q \in O(n)$. Alors
\[
S_{\varphi(k)} = Q_{\varphi(k)}^{\mathsf T}
\bigl(A + \varepsilon_{\varphi(k)}I\bigr)
\longrightarrow Q^{\mathsf T}A =: S,
\]
\omterm{def:b2:quadratic:adjoint}{symétrique} semi-définie positive comme limite de telles matrices
(conditions fermées), et $A = QS$. De plus $S^2 = S^{\mathsf T}S =
A^{\mathsf T}QQ^{\mathsf T}A = A^{\mathsf T}A$, donc $S =
\sqrt{A^{\mathsf T}A}$ par unicité. Pour $A$ singulière, $S$ est
singulière et $Q$ n'est pas unique~: il peut être modifié
arbitrairement sur $(\operatorname{im} S)^{\perp}$ --- cas extrême
$A = 0$, où tout $Q$ orthogonal convient.

\textbf{13.} Diagonaliser $S = P\Sigma P^{\mathsf T}$ (théorème
spectral), $\Sigma = \operatorname{diag}(\sigma_i)$ avec
$\sigma_i \geq 0$ les \omterm{def:b2:reduction:eigen}{valeurs propres} de $S =
\sqrt{A^{\mathsf T}A}$. Alors
\[
A = QS = (QP)\,\Sigma\,P^{\mathsf T} = U\Sigma V^{\mathsf T},
\qquad U = QP,\ V = P \in O(n) .
\]

\textbf{14.} $\norm{Ax}^2 = x^{\mathsf T}S^2x \leq
\sigma_{\max}^2\norm x^2$ avec égalité en un vecteur propre dominant
de $S$~: $\vertiii A_2 = \sigma_{\max}$ --- pour $A$ \omterm{def:b2:quadratic:adjoint}{symétrique},
$S = \sqrt{A^2}$ a pour \omterm{def:b2:reduction:eigen}{valeurs propres} $\abs{\lambda_i}$, ce qui
retrouve l'\cref{exo:b2:quadratic:7}. \omterm{def:b2:linalg:det}{Déterminant}~: $\abs{\det A} =
\abs{\det U}\det\Sigma\abs{\det V} = \sigma_1\cdots\sigma_n$.
Sphère~: en écrivant $x = Vy$ avec $\norm y = 1$, $Ax = U\Sigma y$
a pour coordonnées $z_i = \sigma_iy_i$ dans le repère orthonormé des
colonnes de $U$~: l'image est $\{\sum z_i^2/\sigma_i^2 =
1\}$, un ellipsoïde de demi-axes $\sigma_i$.

\textbf{15.} Une rotation vers les axes principaux
(\cref{cor:b2:quadratic:principalaxes}) transforme $f$ en
$\lambda_1X^2 + \lambda_2Y^2 + \beta_1X + \beta_2Y + c$, avec
$\lambda_1\lambda_2 = \det A$. Si $\det A \neq 0$, absorber les
termes linéaires par la translation $X \mapsto X -
\frac{\beta_1}{2\lambda_1}$ (et de même pour $Y$)~: $\lambda_1X'^2
+ \lambda_2Y'^2 = c'$. Pour $\det A > 0$ (signes égaux)~: une
ellipse ($c'$ du bon signe), un point, ou l'ensemble vide. Pour
$\det A < 0$~: une hyperbole ($c' \neq 0$) ou deux droites
sécantes. Si $\det A = 0$ de rang $1$ (disons $\lambda_2 = 0 \neq
\lambda_1$)~: $\lambda_1X'^2 + \beta_2Y + c''$, une parabole
lorsque $\beta_2 \neq 0$~; sinon deux droites parallèles, une
droite, ou l'ensemble vide. Formes non dégénérées~: ellipse,
hyperbole, parabole, gouvernées par le signe de $\det A$.

\textbf{16.} La partie quadratique $x^2 + 4xy + y^2$ a pour matrice
$\begin{pmatrix}1 & 2\\ 2 & 1\end{pmatrix}$, \omterm{def:b2:reduction:eigen}{valeurs propres} $3$
et $-1$ de directions orthonormées $\frac{1}{\sqrt2}(1,1)$,
$\frac{1}{\sqrt2}(1,-1)$. Dans les coordonnées tournées $u =
\frac{x+y}{\sqrt2}$, $v = \frac{x-y}{\sqrt2}$~: $x^2 + y^2 = u^2
+ v^2$, $2xy = u^2 - v^2$, donc la forme est $3u^2 - v^2$, et $2x
- 2y = 2\sqrt2\,v$. L'équation devient
\[
3u^2 - v^2 + 2\sqrt2\,v = 4
\quad\Longleftrightarrow\quad
3u^2 - \bigl(v - \sqrt2\bigr)^2 = 2 :
\]
une hyperbole, de centre $(u, v) = (0, \sqrt2)$, c'est-à-dire $(x,
y) = (1, -1)$, d'axes selon le repère tourné.

\textbf{17.} $f(x) = (x - x_0)^{\mathsf T}A(x - x_0) + f(x_0)$
dès que $Ax_0 = -\frac b2$, c'est-à-dire $x_0 = -\frac12A^{-1}b$~:
le gradient de $f$ s'annule exactement là ($x_0$ est le centre de
symétrie). Avec $M = \begin{pmatrix} I & x_0\\ 0 &
1\end{pmatrix}$~:
\[
M^{\mathsf T}\widetilde QM
= \begin{pmatrix}
A & Ax_0 + \frac b2\\[2pt]
\bigl(Ax_0 + \frac b2\bigr)^{\mathsf T} &
x_0^{\mathsf T}Ax_0 + b^{\mathsf T}x_0 + c
\end{pmatrix}
= \begin{pmatrix} A & 0\\ 0 & f(x_0)\end{pmatrix},
\]
et $\det M = 1$~: $\det\widetilde Q = f(x_0)\det A$. L'équation
centrée s'écrit $q(X) = -f(x_0)$~: pour $f(x_0) = 0$ elle
dégénère en $q(X) = 0$ (deux droites passant par le centre si la
signature est $(1,1)$, le point unique $x_0$ si $q$ est
définie)~; pour $f(x_0) \neq 0$ la conique est une véritable
ellipse ou hyperbole.

\textbf{18.} $A^{-1} = -\frac13\begin{pmatrix} 1 & -2\\ -2 &
1\end{pmatrix}$, $\frac b2 = (1, -1)$~: $x_0 =
-A^{-1}\frac b2 = (1, -1)$, comme trouvé à la question~16. $f(x_0) =
q(1,-1) + 2 + 2 - 4 = (1 - 4 + 1) + 0 = -2 \neq 0$, et
$\det\widetilde Q = f(x_0)\det A = (-2)(-3) = 6 \neq 0$~:
non dégénérée~; $\det A = -3 < 0$~: une hyperbole --- et en effet
l'équation centrée $3u^2 - (v - \sqrt2)^2 = -f(x_0) = 2$
correspond à la question~16.

\textbf{19.} La matrice est $(1-\lambda)I + \lambda J$~:
\omterm{def:b2:reduction:eigen}{valeurs propres} $1 + 2\lambda$ (direction $(1,1,1)$) et $1 -
\lambda$ (double, sur $x + y + z = 0$). Cas~:
\begin{itemize}
  \item $-\frac12 < \lambda < 1$~: toutes les \omterm{def:b2:reduction:eigen}{valeurs propres}
        positives~: un ellipsoïde de révolution autour de
        $(1,1,1)$ (une sphère pour $\lambda = 0$)~;
  \item $\lambda = 1$~: $q = (x+y+z)^2$~: l'équation donne les
        deux plans parallèles $x + y + z = \pm1$~;
  \item $\lambda = -\frac12$~: \omterm{def:b2:reduction:eigen}{valeurs propres} $0, \frac32,
        \frac32$~: un cylindre de révolution d'axe $(1,1,1)$~;
  \item $\lambda > 1$~: signature $(1, 2)$~: un hyperboloïde à
        deux nappes~;
  \item $\lambda < -\frac12$~: signature $(2, 1)$~: un
        hyperboloïde à une nappe.
\end{itemize}

\textbf{20.} La forme polaire de $q$ est un produit scalaire
$\langle\cdot,\cdot\rangle_q$ sur $E$. Pour $x$ fixé, $y \mapsto
\varphi'(x, y)$ (forme polaire de $q'$) est linéaire, donc égale
$\langle z_x, y\rangle_q$ pour un unique $z_x$~; $u(x) := z_x$ est
linéaire (unicité), et $\langle u(x), y\rangle_q =
\varphi'(x,y) = \varphi'(y,x) = \langle u(y), x\rangle_q$~: $u$
est \omterm{def:b2:quadratic:adjoint}{symétrique} dans l'espace euclidien $(E,
\langle\cdot,\cdot\rangle_q)$. Le théorème spectral
(\cref{thm:b2:quadratic:spectral}) donne une base propre
$q$-orthonormée $(\varepsilon_i)$, $u(\varepsilon_i) =
\mu_i\varepsilon_i$~: dans celle-ci $q(x) = \sum x_i^2$ et $q'(x) =
\langle u(x), x\rangle_q = \sum\mu_ix_i^2$.

\textbf{21.} Soit $P$ la matrice de cette base~: la \omterm{def:b2:quadratic:def}{congruence}
donne $P^{\mathsf T}AP = I$ et $P^{\mathsf T}BP =
\operatorname{diag}(\mu_i)$. Alors
\[
\det(B - \mu A) = \det(P^{-\mathsf T})
\det\bigl(\operatorname{diag}(\mu_i) - \mu I\bigr)
\det(P^{-1})
= (\det P)^{-2}\prod_i(\mu_i - \mu) :
\]
les $\mu_i$ sont les racines du faisceau $\det(B - \mu A)$.

\textbf{22.} $\det(B - \mu A) =
\det\begin{pmatrix} -2\mu & 1-\mu\\ 1-\mu & -\mu\end{pmatrix}
= 2\mu^2 - (1-\mu)^2 = \mu^2 + 2\mu - 1$~: racines $\mu_\pm = -1
\pm \sqrt2$. En résolvant $(B - \mu_\pm A)v = 0$~: $v_\pm = (1 -
\mu_\pm,\ 2\mu_\pm)$ (l'identité de la deuxième ligne $(1-\mu)^2 =
2\mu^2$ aux racines le confirme). Les $A$-normes se calculent sans
peine~: $q_A(v_\pm) = 2(1 + \mu_\pm^2)$, et on vérifie
$\varphi_A(v_+, v_-) = 0$ en utilisant $\mu_+ + \mu_- = -2$,
$\mu_+\mu_- = -1$. La base $\Bigl(\frac{v_+}{\sqrt{2(1 +
\mu_+^2)}}, \frac{v_-}{\sqrt{2(1+\mu_-^2)}}\Bigr)$ est
orthonormée pour $A$ et diagonalise $B$ avec coefficients
$\mu_\pm$.

\textbf{23.} Si un $P$ inversible diagonalisait les deux formes,
le calcul de la question~21 donnerait $\det(B - \mu A) = (\det
P)^{-2}\prod(d_{2i} - \mu d_{1i})$, un polynôme réel scindé
en facteurs linéaires réels. Mais ici
\[
\det(B - \mu A) = \det\begin{pmatrix} -\mu & 1\\ 1 &
\mu\end{pmatrix} = -\mu^2 - 1 ,
\]
de degré $2$ sans racine réelle~: contradiction. (Le
$A$ de signature de Lorentz admet des «~rotations~» pour $B$ sans
axes réels.)

\textbf{24.} $A^{-1}B = A^{-1/2}\bigl(A^{-1/2}BA^{-1/2}\bigr)
A^{1/2}$ avec $A^{1/2} = \sqrt A$ définie positive
(\cref{exo:b2:quadratic:6})~: $A^{-1}B$ est semblable à la matrice
\emph{\omterm{def:b2:quadratic:adjoint}{symétrique}} $A^{-1/2}BA^{-1/2}$, donc \omterm{def:b2:reduction:diag}{diagonalisable} à
\omterm{def:b2:reduction:eigen}{valeurs propres} réelles. Et $\det(B - \mu A) = \det A\cdot
\det(A^{-1}B - \mu I)$~: les racines du faisceau $\mu_i$ de la
question~21 sont exactement les \omterm{def:b2:reduction:eigen}{valeurs propres} de $A^{-1}B$.

\textbf{25.} (i) Chaque partie s'est appuyée sur le théorème
spectral~: via la racine carrée (Cholesky, polaire), les bornes
sur les \omterm{def:b2:reduction:eigen}{valeurs propres} (Hadamard), les axes principaux (coniques),
et la version adaptée à $q$ (réduction simultanée). (ii) Les
réalisations de Gram, Hadamard et la \omterm{pb:b2:quadratic:1}{décomposition polaire}
survivent dans le monde semi-défini~; l'unicité de Cholesky, la
formule des pivots et la réduction simultanée exigent la définitude
(les questions~12 et~23 montrent exactement comment elles
échouent). (iii) Petites oscillations couplées~: l'énergie
cinétique (définie positive) et l'énergie potentielle sont deux
\omterm{def:b2:quadratic:def}{formes quadratiques}~; la base des questions~20--22 constitue les
\emph{modes propres} du système, et les $\mu_i$ sont les carrés des
pulsations.
\end{solution}
