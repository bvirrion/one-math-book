\section*{Capítulo \ref{ch:b2:realfun} --- Functions of a Real Variable}

\begin{solution}{exo:b2:realfun:1}
$\lfloor x\rfloor$: saltos de tamaño $1$ en cada número entero. $x -
\lfloor x\rfloor$: saltos de tamaño$-1$ en números enteros (límite izquierdo $1$,
valor $0$). $\lfloor x\rfloor + \sqrt{x - \lfloor x\rfloor}$: en un
entero $n$, límite izquierdo $(n - 1) + 1 = n$ y valor $n$:
\emph{\omterm{def:b2:metric:continuity}{continuo}} en todas partes (la raíz cuadrada repara el salto),
aunque no diferenciable en números enteros. La función de salto racional:
restringiendo la construcción a una enumeración de las diádicas,
salta por $2^{-n}$ exactamente en el $n$-ésimo racional diádico y es
\omterm{def:b2:metric:continuity}{continuo} en otro lugar.
\end{solution}

\begin{solution}{exo:b2:realfun:2}
Supongamos que el aumento de $f$ tiene una discontinuidad en el interior de $a$: entonces
$f(a^-) < f(a^+)$ (\cref{thm:b2:realfun:monotone}) y la imagen
de $I$ pierde el intervalo \omterm{def:b2:metric:topology}{abierto} no vacío
$\intoo{f(a^-)}{f(a^+)}$ excepto posiblemente el valor único $f(a)$:
la imagen de cualquier subintervalo que contenga $a$ en su interior no es
un intervalo (tiene un hueco en al menos un lado de $f(a)$). esto
contradice la propiedad del valor intermedio. Punto final
Las discontinuidades se excluyen del mismo modo en el caso de huecos unilaterales.
\end{solution}

\begin{solution}{exo:b2:realfun:3}
$x\ln x$: segunda derivada $\frac1x > 0$: convexa. $\ln(1 +
\eu^x)$: derivada $\frac{\eu^x}{1 + \eu^x} = 1 - \frac{1}{1 +
\eu^x}$, creciente: convexa.$\sqrt{1 + x^2}$: segunda derivada
$(1 + x^2)^{-3/2} > 0$: convexo. $x^3$: no convexo en $\R$ ($f'' =
6x $cambia de signo); convexo solo en $\R_+$.
\end{solution}

\begin{solution}{exo:b2:realfun:4}
Supongamos $f(a) \neq f(b)$, digamos $f(b) > f(a)$ con $a < b$ (el caso
$f(b) < f(a)$ es simétrico, mirando hacia la izquierda). Para $x > b$, la pendiente
desigualdad (\cref{lem:b2:realfun:slopes}) en $a < b < x$ da
\[
\frac{f(x) - f(a)}{x - a} \geq \frac{f(b) - f(a)}{b - a} = m > 0
\quad\Longrightarrow\quad
f(x) \geq f(a) + m(x - a) \xrightarrow[x\to+\infty]{} +\infty,
\]
limitación contradictoria arriba. Por tanto, $f$ es constante.

Asíntotas: si $f(x) - (\alpha x + \beta) \to 0$ en $+\infty$ y
$f(x) - (\alpha' x + \beta') \to 0$ en $-\infty$, el convexo
la función $g(x) = f(x) - (\alpha x + \beta)$ está limitada arriba cerca
$+\infty$; convexidad más una asíntota en $-\infty$ (que fuerza
$\alpha' \leq \alpha$ y luego $\alpha' = \alpha$ comparando pendientes en
$\mp\infty$: pendientes de una función convexa aumentan) hace $g$
acotado arriba en todo $\R$, por lo tanto constante $= 0$ en el límite: $f$
es afín.
\end{solution}

\begin{solution}{exo:b2:realfun:5}
$f'$ es monótono, por lo que \cref{thm:b2:realfun:monotone} es el único
posibles discontinuidades son saltos, existiendo límites unilaterales
en todas partes. Por \cref{cor:b2:realfun:nojumps}, un derivado no tiene
discontinuidades de salto. Por lo tanto, $f'$ no tiene ninguna discontinuidad:
\omterm{def:b2:metric:continuity}{continuo}.
\end{solution}

\begin{solution}{exo:b2:realfun:6}
Toma logaritmos:
\[
\frac1p \ln\Bigl(\sum_i \lambda_i x_i^p\Bigr)
= \frac1p \ln\Bigl(1 + p\sum_i \lambda_i \ln x_i +
O(p^2)\Bigr)
= \sum_i \lambda_i \ln x_i + O(p)
\xrightarrow[p \to 0^+]{} \sum_i \lambda_i \ln x_i ,
\]
utilizando $\sum\lambda_i = 1$ y $\ln(1 + u) = u + O(u^2)$.
La potenciación da la media geométrica. Ahora, para cada $p \in
\intoo{0}{1}$, la desigualdad de media potencia
(\cref{ex:b2:realfun:powermeans}, exponentes $p < 1$) da
\[
\Bigl(\sum_i \lambda_i x_i^p\Bigr)^{1/p} \leq \sum_i\lambda_i x_i ;
\]
dejando $p \to 0^+$ a la izquierda se obtiene $\prod_i x_i^{\lambda_i}
\leq \sum_i \lambda_i x_i$: la desigualdad AM-GM ponderada.
\end{solution}

\begin{solution}{exo:b2:realfun:7}
Con $\varphi(t) = t\ln t$ (convexo: $\varphi'' = \frac1t > 0$) y
pesos $q_i$ en los puntos $t_i = \frac{p_i}{q_i}$:
\[
\sum_i p_i \ln\frac{p_i}{q_i}
= \sum_i q_i\, \varphi\Bigl(\frac{p_i}{q_i}\Bigr)
\;\geq\; \varphi\Bigl(\sum_i q_i \frac{p_i}{q_i}\Bigr)
= \varphi(1) = 0 ,
\]
por Jensen (\cref{thm:b2:realfun:convexreg}~(3)). Igualdad en
Jensen para una función convexa \emph{estrictamente} fuerza todos los puntos
$t_i$ para que coincida: $\frac{p_i}{q_i}$ constante, y sumatoria, la
La constante es $1$: $p = q$. (Esta cantidad --- el Kullback--Leibler
divergencia --- retornos en el mundo de \cref{ch:b2:randomvar}.)
\end{solution}

\begin{solution}{exo:b2:realfun:8}
\emph{Pesos diádicos.} Por inducción sobre $m$: el caso $m = 1$ es el
hipótesis. Para el peso $\lambda = \frac{k}{2^{m+1}}$ (impar $k$),
escriba $\lambda = \frac12(\lambda_1 + \lambda_2)$ con $\lambda_j =
\frac{k \mp 1}{2^{m+1}}$, ambos del denominador $2^m$ después
simplificación; entonces
\[
f\bigl(\lambda x + (1{-}\lambda)y\bigr)
= f\Bigl(\tfrac{u + v}{2}\Bigr)
\leq \frac{f(u) + f(v)}{2}
\leq \lambda f(x) + (1 - \lambda) f(y),
\]
donde $u = \lambda_1 x + (1 - \lambda_1)y$ y $v = \lambda_2 x +
(1-\lambda_2)y$, usando la convexidad del punto medio y luego la inducción
hipótesis sobre $u, v$.

\emph{Paso al límite.} Para $\lambda \in
\intcc{0}{1}$ arbitrario, tome diádicas $\lambda_n \to \lambda$: \omterm{def:b2:metric:continuity}{continuidad} de
$f$ y de los mapas afines pasa la desigualdad
$f(\lambda_n x + (1-\lambda_n)y) \leq \lambda_n f(x) +
(1-\lambda_n)f(y)$al límite: $f$ es convexo.
\end{solution}

\begin{solution}{exo:b2:realfun:9}
Para $0 < x < y$: la desigualdad de pendiente
(\cref{lem:b2:realfun:slopes}) en los puntos $0 < x < y$ da
\[
\frac{f(x) - f(0)}{x} \leq \frac{f(y) - f(0)}{y},
\qquad\text{i.e.}\qquad
\frac{f(x)}{x} \leq \frac{f(y)}{y} +
f(0)\Bigl(\frac1x - \frac1y\Bigr).
\]
Desde $f(0) \leq 0$ y $\frac1x - \frac1y > 0$, el último término es
$\leq 0$: $\frac{f(x)}{x} \leq \frac{f(y)}{y}$. Entonces $x \mapsto
\frac{f(x)}x$ aumenta.

Superaditividad para $f(0) = 0$: para $x, y > 0$ (los casos con una
variable cero son triviales),
\[
f(x) = x\,\frac{f(x)}{x} \leq x\,\frac{f(x+y)}{x+y},
\qquad
f(y) \leq y\,\frac{f(x+y)}{x+y},
\]
por la monotonicidad recién demostrada; agregando da $f(x) + f(y) \leq
f(x+y)$.
\end{solution}

\begin{solution}{exo:b2:realfun:10}
Para $x, y \in I$ y $\lambda \in \intcc01$: convexidad de $f$,
luego monotonicidad de $g$, luego convexidad de $g$:
\[
g\bigl(f(\lambda x + (1{-}\lambda)y)\bigr)
\leq g\bigl(\lambda f(x) + (1{-}\lambda)f(y)\bigr)
\leq \lambda\,g(f(x)) + (1{-}\lambda)\,g(f(y)).
\]
Contraejemplo sin monotonicidad: $g(t) = -t$ es convexo
(afín) pero decreciente, $f(x) = x^2$ es convexo y $g \circ f =
-x^2$ es estrictamente cóncavo.
\end{solution}

\begin{solution}{exo:b2:realfun:11}
\emph{Desigualdad izquierda:} deja a $m = \frac{a+b}2$ y toma un soporte.
línea en $m$ (\cref{thm:b2:realfun:convexreg}~(2)): $f(t) \geq
f(m) + \mu(t - m)$para todos los $t \in \intcc ab$. Integrando sobre
$\intcc{a}{b}$: el término lineal se integra a $\mu\int_a^b(t -
m)\dd t = 0$(simetría alrededor de $m$), por lo que $\int_a^b f \geq (b -
a)f(m)$.

\emph{Desigualdad correcta:} en $\intcc ab$, límites de convexidad $f$ por
su acorde: $f(t) \leq f(a) + \frac{f(b) - f(a)}{b - a}(t - a)$.
Integrando: $\int_a^b f \leq (b-a)f(a) + \frac{f(b) - f(a)}{b -
a}\cdot\frac{(b-a)^2}2 = (b - a)\,\frac{f(a) + f(b)}2$. dividir por
$b - a$.
\end{solution}

\begin{solution}{exo:b2:realfun:12}
Dejemos $a - \delta < a \leq x < y \leq b < b + \delta$, todo en $I$.
Dos aplicaciones de la desigualdad de la pendiente.
(\cref{lem:b2:realfun:slopes}), primero a $a - \delta < a \leq x <
y$, luego a $x < y \leq b < b + \delta$:
\[
\frac{f(a) - f(a - \delta)}{\delta}
\leq \frac{f(y) - f(x)}{y - x}
\leq \frac{f(b + \delta) - f(b)}{\delta}
\]
(Las pendientes de las cuerdas aumentan cuando ambos puntos finales se mueven hacia la derecha). Por lo tanto
cada pendiente de cuerda dentro de $\intcc ab$ está atrapada entre dos fijos
números, y
\[
\abs{f(y) - f(x)} \leq K\,\abs{y - x},
\qquad
K = \max\Bigl(\Bigl|\frac{f(a) - f(a-\delta)}{\delta}\Bigr|,
\Bigl|\frac{f(b+\delta) - f(b)}{\delta}\Bigr|\Bigr):
\]
$f$ es \omterm{def:b2:metric:continuity}{Lipschitz} en $\intcc ab$. Cada punto del \omterm{def:b2:metric:topology}{abierto} $I$ tiene
un segmento con margen a su alrededor: localmente \omterm{def:b2:metric:continuity}{Lipschitz}, por lo tanto
(nuevamente) \omterm{def:b2:metric:continuity}{continuo} en $I$.
\end{solution}

\begin{solution}{pb:b2:realfun:1}
\textbf{1.} \emph{Convex $\Rightarrow$ $f'$ increasing:} para $a <
b$, la desigualdad de pendiente da, para $h > 0$ pequeño, $\frac{f(a+h)
- f(a)}h \leq \frac{f(b) - f(a)}{b-a} \leq \frac{f(b) -
f(b-h)}{h}$; dejando $h \to 0$: $f'(a) \leq \frac{f(b) -
f(a)}{b - a} \leq f'(b)$. \emph{En cambio}, si $f'$ aumenta
y $x < y < z$: el teorema del valor medio da $c_1 \in
\intoo{x}{y}$, $c_2 \in \intoo yz$ con
\[
\frac{f(y) - f(x)}{y - x} = f'(c_1) \leq f'(c_2)
= \frac{f(z) - f(y)}{z - y},
\]
y esta desigualdad de pendiente de tres puntos, aplicada con $y = \lambda
x + (1 - \lambda)z$, se reorganiza en la desigualdad de convexidad.
Para $C^2$: $f'' \geq 0$ si $f'$ aumenta.

\textbf{2.} Si $f'' > 0$, $f'$ es estrictamente creciente y el
El cálculo del valor medio anterior da una desigualdad \emph{estricto}
entre las dos pendientes de la cuerda: convexidad estricta. \emph{estricto
soporte:} en un $a$ interior con pendiente de apoyo $m$, si $f(x_0) =
f(a) + m(x_0 - a)$para algún $x_0 \neq a$, entonces en el segmento
de $a$ a $x_0$ coinciden la línea de apoyo y la cuerda, y
La convexidad estricta en el punto medio da $f\bigl(\frac{a +
x_0}2\bigr) < f(a) + m\,\frac{x_0 - a}2$, contradiciendo la
apoyar la desigualdad. Entonces $f(x) > f(a) + m(x - a)$ para todos los $x \neq
a$. \emph{Jensen estricto:} con $a = \sum\lambda_ix_i$, promediando
las desigualdades de soporte dan $\sum\lambda_if(x_i) \geq f(a)$,
con igualdad si cada término es una igualdad, es decir, si cada $x_i
= a$.

\textbf{3.}$(-\ln)'' = \frac1{t^2} > 0$; $(t^r)'' = r(r -
1)t^{r-2}$, positivo para $r > 1$, negativo para $0 < r < 1$;
$\exp'' = \exp > 0$. Todo estricto por la pregunta 2.

\textbf{4.} Los casos $ab = 0$ son triviales. Para $a, b > 0$,
concavidad de $\ln$ en los puntos $a^p, b^q$ con pesas
$\frac1p, \frac1q$:
\[
\ln\Bigl(\frac{a^p}p + \frac{b^q}q\Bigr) \geq
\frac1p\ln(a^p) + \frac1q\ln(b^q) = \ln(ab),
\]
y $\ln$ aumenta: $ab \leq \frac{a^p}p + \frac{b^q}q$.
Igualdad si los dos puntos coinciden (concavidad estricta): $a^p =
b^q$.

\textbf{5.} Concavidad de $\ln$ con pesas $\lambda_i$:
$\ln\bigl(\sum\lambda_ix_i\bigr) \geq \sum\lambda_i\ln x_i =
\ln\prod x_i^{\lambda_i}$; exponenciar. Igualdad si todo $x_i$
igual (pregunta 2). La ruta de \cref{exo:b2:realfun:6}
obtuvo la misma desigualdad que un límite de poder significa; aqui esta
es una aplicación de Jensen --- la caja de herramientas tiene redundancia
incorporado.

\textbf{6.} Si $a = 0$ o $b = 0$ la desigualdad es trivial.
Normalizar: reemplazando $a$ por $a/\norm a_p$ y $b$ por $b/\norm
b_q$, podemos asumir $\norm a_p = \norm b_q = 1$ y debemos mostrar
$\sum\abs{a_ib_i} \leq 1$. Jóvenes por trimestre:
\[
\sum_i\abs{a_i}\abs{b_i} \leq
\sum_i\Bigl(\frac{\abs{a_i}^p}{p} +
\frac{\abs{b_i}^q}{q}\Bigr) = \frac1p + \frac1q = 1 .
\]
Igualdad si cada desigualdad de Young es estricta: $\abs{a_i}^p =
\abs{b_i}^q $para todos $i$ --- después de deshacer la normalización,
$(\abs{a_i}^p)$ proporcional a $(\abs{b_i}^q)$.

\textbf{7.} $p = q = 2$ es Cauchy--Schwarz con el mismo
caso de igualdad (proporcionalidad). Punto final: $\sum\abs{a_ib_i}
\leq \bigl(\max_i\abs{b_i}\bigr)\sum_i\abs{a_i} =
\norm a_1\norm b_\infty$, término inmediato.

\textbf{8.} Para $p = 1$ es la desigualdad del triángulo término a término.
Para $p > 1$, con conjugado $q$:
\[
\norm{a+b}_p^p = \sum_i\abs{a_i + b_i}^p
\leq \sum_i\abs{a_i+b_i}^{p-1}\abs{a_i} +
\sum_i\abs{a_i+b_i}^{p-1}\abs{b_i},
\]
y Hölder en cada suma, anotando $(p - 1)q = p$:
\[
\sum_i\abs{a_i+b_i}^{p-1}\abs{a_i} \leq
\Bigl(\sum_i\abs{a_i+b_i}^{p}\Bigr)^{1/q}\norm a_p
= \norm{a + b}_p^{p/q}\,\norm a_p ,
\]
lo mismo con $b$. Por lo tanto $\norm{a+b}_p^p \leq \norm{a +
b}_p^{p/q}\bigl(\norm a_p + \norm b_p\bigr)$; si $a + b \neq 0$,
divida por $\norm{a+b}_p^{p/q}$ y use $p - \frac pq = 1$. con
homogeneidad y separación (claro), $\norm\cdot_p$ es un \omterm{def:b2:nvs:norm}{norma} en
$\R^n$.

\textbf{9.} Para \omterm{def:b2:metric:continuity}{continuo} $f, g$ en $\intcc ab$: Hölder
\[
\int_a^b\abs{fg} \leq \Bigl(\int_a^b\abs
f^p\Bigr)^{1/p}\Bigl(\int_a^b\abs g^q\Bigr)^{1/q}
\]
por la misma normalización más puntualmente Young, integrado; y
Minkowski $\norm{f + g}_p \leq \norm f_p + \norm g_p$ por el
misma división, Hölder en cada pieza. Separación del \omterm{def:b2:nvs:norm}{norma}
utiliza positividad estricta: un \omterm{def:b2:metric:continuity}{continuo} $\abs f^p$ con cero
la integral desaparece de forma idéntica (volumen del año 1).

\textbf{10.} \emph{Monotonicidad:} podemos suponer $\norm a_p = 1$;
luego cada $\abs{a_i} \leq 1$, entonces $\abs{a_i}^q \leq \abs{a_i}^p$
y $\norm a_q^q \leq 1$: $\norm a_q \leq 1 = \norm a_p$.
La igualdad requiere $\abs{a_i}^q = \abs{a_i}^p$ por cada $i$,
es decir,\ cada $\abs{a_i} \in \{0, 1\}$; con $\sum\abs{a_i}^p = 1$
esto deja exactamente una coordenada del módulo $1$: igualdad iff
$a$ tiene como máximo una coordenada distinta de cero. \emph{Límite:} $\norm a_\infty \leq
\norm a_p \leq n^{1/p}\norm a_\infty $y$n^{1/p} \to 1$.
\emph{Comparación inversa:} Hölder con exponentes $\frac
qp $y su conjugado $\frac{q}{q-p}$, aplicado a
$\abs{a_i}^p\cdot 1$:
\[
\norm a_p^p = \sum_i\abs{a_i}^p\cdot 1 \leq
\Bigl(\sum_i\abs{a_i}^{q}\Bigr)^{p/q}\,n^{1 - p/q}
= \norm a_q^{p}\; n^{1-p/q},
\]
de donde $\norm a_p \leq n^{\frac1p - \frac1q}\norm a_q$, con
igualdad si todos los $\abs{a_i}$ son iguales (la igualdad mayor H\"older
caso contra el vector constante).

\textbf{11.} Escriba $\abs{a_i}^r = \abs{a_i}^{\theta
r}\,\abs{a_i}^{(1-\theta)r}$ y aplique Hölder con el
exponentes conjugados $\frac{p}{\theta r}$ y
$\frac{q}{(1-\theta)r}$ (conjugado precisamente porque $\frac{\theta
r}p + \frac{(1-\theta)r}q = 1$):
\[
\norm a_r^r = \sum_i \abs{a_i}^{\theta r}\abs{a_i}^{(1-\theta)r}
\leq \Bigl(\sum_i\abs{a_i}^{p}\Bigr)^{\theta r/p}
\Bigl(\sum_i\abs{a_i}^{q}\Bigr)^{(1-\theta)r/q}
= \norm a_p^{\theta r}\,\norm a_q^{(1-\theta)r} .
\]
Tome las raíces $r$-ésima: las normas $p$ son log-convexas en $\frac1p$.

\textbf{12.} \emph{Ambos negativos:} si $p < q < 0$ entonces $0 < -q <
-p $y$M_{-q}(y) \leq M_{-p}(y)$ para los exponentes positivos
(caso del curso, \cref{ex:b2:realfun:powermeans}) aplicado a $y =
(1/x_i)$; invirtiendo la identidad $M_p(x) = M_{-p}(1/x)^{-1}$
invierte la desigualdad en $M_p(x) \leq M_q(x)$.
\emph{Puente:} para $q > 0$, la concavidad de $\ln$ da $\ln M_q =
\frac1q\ln\bigl(\sum\lambda_ix_i^q\bigr) \geq
\frac1q\sum\lambda_i\ln x_i^q = \ln M_0$; para $p < 0$, lo mismo
la concavidad da $\ln\bigl(\sum\lambda_ix_i^p\bigr) \geq
p\sum\lambda_i\ln x_i$, y al dividir por $p < 0$ se voltea: $\ln M_p
\leq \ln M_0$. Por lo tanto $M_p \leq M_0 \leq M_q $siempre que $p < 0 <
q$: con los dos casos del mismo signo, $M$ aumenta en todos
$\R^*$ (y hasta $0$).

\textbf{13.} Vamos $x_{\max} = \max x_i$, alcanzado en $i^*$. Para
$p > 0$:
\[
\lambda_{i^*}^{1/p}\,x_{\max} \leq M_p \leq x_{\max},
\]
y $\lambda_{i^*}^{1/p} \to 1$: $M_p \to x_{\max}$. Para $p \to
-\infty$: $M_p(x) = M_{-p}(1/x)^{-1} \to
\bigl(\max_i\frac1{x_i}\bigr)^{-1} = \min_ix_i$.

\textbf{14.} Con $\lambda_i = \frac1n$, la cadena $M_{-\infty}
\leq M_{-1} \leq M_0 \leq M_1 \leq M_2 \leq M_{+\infty}$ lee
\[
\min \leq \frac{n}{\sum\frac1{a_i}} \leq \Bigl(\prod
a_i\Bigr)^{1/n} \leq \frac{\sum a_i}{n} \leq
\sqrt{\frac{\sum a_i^2}{n}} \leq \max .
\]
AM--HM ($M_{-1} \leq M_1$) se reorganiza directamente en
$\bigl(\sum a_i\bigr)\bigl(\sum\frac1{a_i}\bigr) \geq n^2$.

\textbf{15.} Con pesos iguales, $M_p(x) =
\bigl(\frac1n\sum\abs{x_i}^p\bigr)^{1/p} = n^{-1/p}\norm x_p$.
A medida que $p$ crece, $\norm x_p$ disminuye (pregunta 10) pero el
El normalizador $n^{-1/p}$ aumenta más rápido y el producto
aumentos (pregunta 12): significa promedio, \omterm{def:b2:nvs:norm}{normas} acumula, y
el factor $n^{-1/p}$ es exactamente el tipo de cambio entre los
Dos convenciones contables.

\textbf{16.} Cada enlace es una instancia de Jensen estricto.
(pregunta 2) con las funciones estrictamente convexas/cóncavas de
pregunta 3 ($t^{q/p}$, $\ln$), por lo que la igualdad en cualquier vínculo fuerza
todos los $x_i$ iguales; y $\min = M_p$ o $M_p = \max$ igualmente
obliga a todos los valores a ser iguales al extremo común. la cadena es
estricto tan pronto como dos $x_i$ difieren.

\textbf{17.} Aplica Joven (pregunta 4) a la pareja
$\varepsilon^{1/p}a$ y $\varepsilon^{-1/p}b$:
\[
ab = (\varepsilon^{1/p}a)(\varepsilon^{-1/p}b)
\leq \varepsilon\,\frac{a^p}p +
\varepsilon^{-q/p}\,\frac{b^q}q .
\]
Para $p = q = 2$, reemplazando $\varepsilon$ por $2\varepsilon$:
$ab \leq \varepsilon a^2 + \frac{b^2}{4\varepsilon}$ --- el
desigualdad de absorción: un producto se comercializa por un pequeño múltiplo
de un cuadrado más un múltiplo grande del otro.

\textbf{18.} Telescópico:
\[
\prod_{k=1}^{n}c_k = \frac{\prod_{k=1}^n(k+1)^k}
{\prod_{k=1}^{n}k^{k-1}}
= \frac{2^1\,3^2\cdots(n+1)^n}{1^0\,2^1\cdots n^{n-1}}
= (n+1)^n,
\]
cada factor $(k+1)^k$ del numerador cancelándose contra el
siguiente término del denominador. AM--GM en los números $n$ $c_ka_k$:
\[
(a_1\cdots a_n)^{1/n} =
\frac{\bigl(\prod_k c_ka_k\bigr)^{1/n}}{(n+1)}
\leq \frac{1}{n+1}\cdot\frac1n\sum_{k=1}^{n}c_ka_k .
\]

\textbf{19.} Sumando sobre $n$ e intercambiando las dos sumas
(todos los términos positivos: \cref{thm:b2:series:fubini}):
\[
\sum_{n\geq1}(a_1\cdots a_n)^{1/n}
\leq \sum_{n\geq1}\frac{1}{n(n+1)}\sum_{k=1}^{n}c_ka_k
= \sum_{k\geq1}c_ka_k\sum_{n\geq k}\frac1{n(n+1)}
= \sum_{k\geq1}\frac{c_ka_k}{k},
\]
utilizando el telescópico $\sum_{n\geq k}\bigl(\frac1n -
\frac1{n+1}\bigr) = \frac1k$. Finalmente $\frac{c_k}k =
\frac{(k+1)^k}{k^k} = \bigl(1 + \frac1k\bigr)^k < \eu$
(secuencia creciente con límite $\eu$, volumen Año 1):
\[
\sum_{n\geq1}(a_1\cdots a_n)^{1/n} \leq
\eu\sum_{k\geq1}a_k :
\]
La desigualdad de Carleman. (La constante $\eu$ es óptima, aunque
no lo pruebes.)

\textbf{20.} Cauchy--Schwarz (pregunta 9, $p = q = 2$) aplicada
a $\sqrt f$ y $\frac1{\sqrt f}$:
\[
1 = \Bigl(\int_0^1\sqrt f\cdot\frac{1}{\sqrt f}\Bigr)^{2}
\leq \Bigl(\int_0^1 f\Bigr)\Bigl(\int_0^1\frac1f\Bigr).
\]
Igualdad si $\sqrt f$ y $\frac1{\sqrt f}$ son proporcionales,
es decir, \ $f^2$ constante, es decir, \ $f$ constante ($f > 0$ \omterm{def:b2:metric:continuity}{continuo}).

\textbf{21.} Vamos $1 < p < \infty$, $\norm a_p = \norm b_p = 1$,
$a \neq b$, y supongamos $\bigl\Vert\frac{a+b}2\bigr\Vert_p = 1$,
es decir,\ Minkowski es una igualdad para $a, b$. Seguimiento de las preguntas 8
prueba, la igualdad fuerza la igualdad en ambas aplicaciones Hölder
y en las desigualdades del triángulo termino: $(\abs{a_i}^p)$ y
$(\abs{b_i}^p)$ ambos proporcionales a $(\abs{a_i + b_i}^p)$, y
$a_i, b_i$ del mismo signo --- por lo tanto $b = ta$ para algunos $t \geq
0$, y $\norm b_p = \norm a_p $da $t = 1$: $b = a$,
contradicción. Entonces la esfera $p$ no contiene ningún punto medio de
puntos de esfera distintos: sin segmento. Para $p = \infty$ en $\R^2$:
todos $(1, t)$, $\abs t \leq 1$, se encuentran en la esfera unitaria --- un plano
borde; para $p = 1$: el segmento $(t, 1 - t)$, $t \in \intcc01$,
lo hace.

\textbf{22.} Para $a = 0$ ambos lados desaparecen. De lo contrario Hölder
limita cada $\sum a_ib_i$ por $\norm a_p\norm b_q \leq \norm
a_p$. Logro: tomar
\[
b_i = \frac{\operatorname{sign}(a_i)\,\abs{a_i}^{p-1}}
{\norm a_p^{p/q}} :
\qquad
\norm b_q^q = \frac{\sum_i\abs{a_i}^{(p-1)q}}{\norm a_p^{p}}
= \frac{\norm a_p^p}{\norm a_p^p} = 1,
\quad
\sum_ia_ib_i = \frac{\norm a_p^p}{\norm a_p^{p/q}} =
\norm a_p ,
\]
utilizando $(p-1)q = p$ y $p - \frac pq = 1$. Entonces el supremo es un
máximo, igual a $\norm a_p$: cada norma $p$ es la \omterm{def:b2:linalg:dual}{dual} \omterm{def:b2:nvs:norm}{norma} de
su conjugado --- el germen de la dualidad $L^p$--$L^q$.

\textbf{23.} $\E[X^r] = \sum_i\lambda_ix_i^r$, entonces
$\E[X^r]^{1/r} = M_r(x; \lambda)$, aumentando en $r$ en
pregunta 12 (y hasta $r \to 0, \pm\infty$ por preguntas
12--13): La desigualdad del momento de Lyapunov, puramente una afirmación sobre
medios de potencia ponderados. Devuelve para variables aleatorias genuinas.
en \cref{ch:b2:randomvar}.

\textbf{24.} (i) Potencia significa $M_1 \leq M_3$ con pesos iguales:
$\frac{a+b+c}3 \leq \bigl(\frac{a^3+b^3+c^3}3\bigr)^{1/3}$;
cubo y multiplicar por $3$: $a^3 + b^3 + c^3 \geq
\frac{(a+b+c)^3}9$. (ii) Cauchy--Schwarz contra la constante
vector: $\sum_i\sqrt{x_i}\cdot1 \leq
\bigl(\sum_ix_i\bigr)^{1/2}n^{1/2}$; cuadrado.

\textbf{25.} La definición de cuerda produce el lema de pendiente en uno
reordenamiento algebraico; pendientes apretadas en un punto producen
derivados unilaterales y líneas de soporte, cuyo promedio ponderado
Es Jensen. Aplicado a $-\ln$, Jensen se vuelve Joven, lo cual
sumado contra vectores normalizados es Hölder, que divide y
reabsorbido es Minkowski --- y las normas $p$ de
\cref{ch:b2:nvs} nacen, con su dualidad (pregunta 22)
y su geometría (pregunta 21). Jensen aplicó a lo largo del
escala de poderes encadena todos los medios desde $\min$ hasta $\max$
(preguntas 12 a 14), que se lee en variables aleatorias es la
desigualdad de momento (pregunta 23). Y AM-GM, ponderado por uno
truco telescópico, produce el límite de Carleman con su
constante irreducible $\eu$ (preguntas 18--19). Cumbres:
Hölder: Minkowski y Carleman. Destino: el $L^p$
espacios del volumen del Año 3, cuyos axiomas fundamentales son exactamente
preguntas 6 y 8 con integrales en lugar de sumas.
\end{solution}