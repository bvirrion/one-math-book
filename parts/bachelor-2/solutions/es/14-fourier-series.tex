\section*{Capítulo \ref{ch:b2:fourier} --- Fourier Series}

\begin{solution}{exo:b2:fourier:1}
La rareza mata al $a_n$. Para $n \geq 1$:
\[
b_n = \frac{2}{\pi}\int_0^{\pi} \sin nt\,\dd t
= \frac{2}{\pi}\cdot\frac{1 - (-1)^n}{n}
= \begin{cases} \frac{4}{\pi n} & n \text{ odd},\\ 0 & n
\text{ even}. \end{cases}
\]
Entonces $S(f)(t) = \frac{4}{\pi}\sum_{k\geq0} \frac{\sin\bigl((2k+1)t
\bigr)}{2k+1}$. Dirichlet en $t = \frac\pi2$ (un punto \omterm{def:b2:metric:continuity}{continuidad},
valor $1$): $\sin\bigl((2k+1)\frac\pi2\bigr) = (-1)^k$, dando
\[
1 = \frac4\pi \sum_{k\geq0}\frac{(-1)^k}{2k+1}
\quad\Longrightarrow\quad
\sum_{k\geq0}\frac{(-1)^k}{2k+1} = \frac{\pi}{4}
\quad\text{(Leibniz)}.
\]
En el salto $t = 0$: la serie suma $0 = \frac{f(0^+) +
f(0^-)}{2}$, como prescribe Dirichlet (todos los términos desaparecen:
consistente).
\end{solution}

\begin{solution}{exo:b2:fourier:2}
La uniformidad mata al $b_n$; $a_0 = \frac{1}{\pi}\int_{-\pi}^\pi\abs
t\,\dd t = \pi $y para $n \geq 1$:
\[
a_n = \frac{2}{\pi}\int_0^\pi t\cos nt\,\dd t
= \frac{2}{\pi}\cdot\frac{(-1)^n - 1}{n^2}
= \begin{cases} -\frac{4}{\pi n^2} & n \text{ odd},\\ 0 & n
\text{ even}, \end{cases}
\]
(una integración por partes). Por lo tanto
\[
\abs t = \frac{\pi}{2} - \frac{4}{\pi}\sum_{k\geq0}
\frac{\cos\bigl((2k+1)t\bigr)}{(2k+1)^2} ,
\]
con convergencia \emph{normal} ($\sum (2k+1)^{-2} < \infty$) --- como
\cref{thm:b2:fourier:parseval}~(1) predice para este \omterm{def:b2:metric:continuity}{continuo}
Función por partes-$C^1$. En $t = 0$:
\[
0 = \frac\pi2 - \frac4\pi\sum_{k\geq0}\frac{1}{(2k+1)^2}
\quad\Longrightarrow\quad
\sum_{k\geq0}\frac{1}{(2k+1)^2} = \frac{\pi^2}{8} .
\]
Dividiendo $\sum \frac{1}{n^2}$ en partes pares e impares: $S =
\frac{\pi^2}{8} + \frac S4$, entonces $S = \frac{\pi^2}{6}$: Basilea nuevamente.
\end{solution}

\begin{solution}{exo:b2:fourier:3}
Uniformidad: $b_n = 0$; $a_0 = \frac{1}{\pi}\int_{-\pi}^{\pi} t^2 =
\frac{2\pi^2}{3}$; dos integraciones por partes dan $a_n =
\frac{4(-1)^n}{n^2}$($n \geq 1$). Por lo tanto
\[
t^2 = \frac{\pi^2}{3} + 4\sum_{n\geq1}
\frac{(-1)^n}{n^2}\cos nt
\qquad (\abs t \leq \pi),
\]
\omterm{def:b2:funcseq:series}{normalmente} convergente. En $t = 0$: $0 = \frac{\pi^2}{3} +
4\sum\frac{(-1)^n}{n^2}$, es decir $\sum_{n\geq1}
\frac{(-1)^{n+1}}{n^2} = \frac{\pi^2}{12}$. Parseval:
\[
\frac{1}{2\pi}\int_{-\pi}^{\pi} t^4\,\dd t = \frac{\pi^4}{5}
= \frac{a_0^2}{4} + \frac12\sum_{n\geq1} a_n^2
= \frac{\pi^4}{9} + 8\sum_{n\geq1}\frac{1}{n^4} ,
\]
Entonces $\sum \frac{1}{n^4} = \frac18\bigl(\frac{\pi^4}{5} -
\frac{\pi^4}{9}\bigr) = \frac{\pi^4}{90}$.
\end{solution}

\begin{solution}{exo:b2:fourier:4}
Si además $f$ es $C^1$ por partes: Parseval (probado) da
$\norm f_2^2 = \sum\abs{c_n}^2 = 0$, y estricta positividad del
integral de las fuerzas \omterm{def:b2:metric:continuity}{continuo} $\abs f^2$ $f = 0$.

Para simplemente \omterm{def:b2:metric:continuity}{continuo} $f$, admita Parseval general: la misma línea
prueba. (Esquema de la ruta de densidad: Fejér/Weierstrass-tipo
La aproximación trigonométrica muestra que los polinomios trigonométricos son
$\norm\cdot_2$-denso entre \omterm{def:b2:metric:continuity}{continuo} funciones periódicas; ya que $f
\perp $todos ellos, $\norm f_2^2 = \langle f, f - P\rangle \leq
\norm f_2\norm{f - P}_2$ para aproximantes $P$, forzando a $\norm f_2 =
0$.)
\end{solution}

\begin{solution}{exo:b2:fourier:5}
Uniformidad: $b_n = 0$;
\[
a_n = \frac{2}{\pi}\int_0^\pi \cos(\alpha t)\cos(nt)\,\dd t
= \frac{2}{\pi}\cdot
\frac{(-1)^n\,\alpha\sin(\pi\alpha)}{\alpha^2 - n^2}
\]
(producto a suma, luego integrar; $a_0 =
\frac{2\sin(\pi\alpha)}{\pi\alpha}$). Dirichlet en $t = \pi$ (un
\omterm{def:b2:metric:continuity}{continuidad} punto de la extensión periódica, cuyos valores unilaterales
de acuerdo por equidad):
\[
\cos(\pi\alpha)
= \frac{\sin(\pi\alpha)}{\pi\alpha}
+ \sum_{n\geq1} \frac{2\alpha\sin(\pi\alpha)}{\pi(\alpha^2 -
n^2)}\,(-1)^n\cos(n\pi)
= \frac{\sin(\pi\alpha)}{\pi}\Bigl(\frac{1}{\alpha} +
\sum_{n\geq1}\frac{2\alpha}{\alpha^2 - n^2}\Bigr),
\]
utilizando $(-1)^n\cos n\pi = 1$. Dividiendo por $\sin(\pi\alpha)/\pi$
da la expansión cotangente.
\end{solution}

\begin{solution}{exo:b2:fourier:6}
Iterando $c_n(f') = \iu n\,c_n(f)$ ($k$ veces, integración por
partes a través de las piezas $C^{k}$ con valores límite coincidentes):
$c_n(f^{(k)}) = (\iu n)^k c_n(f)$. Los coeficientes de la
por partes \omterm{def:b2:metric:continuity}{continuo} $f^{(k)}$ están acotados (de hecho, $\to 0$, Bessel):
\[
\abs{c_n(f)} = \frac{\abs{c_n(f^{(k)})}}{\abs n^k}
= O\bigl(\abs n^{-k}\bigr) .
\]
\end{solution}

\begin{solution}{exo:b2:fourier:7}
Parseval para $f$ y para $f'$ (ambos legítimos: $f$ es $C^1$, $f'$
por partes \omterm{def:b2:metric:continuity}{continuo} --- de hecho \omterm{def:b2:metric:continuity}{continuo}):
\[
\frac{1}{2\pi}\int \abs f^2 = \sum_{n\neq0} \abs{c_n}^2
\quad (c_0 = 0 \text{ by the mean-zero hypothesis}),
\qquad
\frac{1}{2\pi}\int \abs{f'}^2 = \sum_{n\neq0} n^2\abs{c_n}^2 .
\]
Por términos, $n^2\abs{c_n}^2 \geq \abs{c_n}^2$ para $\abs n \geq 1$:
la desigualdad sigue. Fuerzas de igualdad $(n^2 - 1)\abs{c_n}^2 = 0$
para todos $n$, es decir,\ $c_n = 0$ para $\abs n \geq 2$: $f(t) =
c_1\eu^{\iu t} + c_{-1}\eu^{-\iu t} = a\cos t + b\sin t$ (real
forma); por el contrario, tales $f$ dan igualdad.
\end{solution}

\begin{solution}{exo:b2:fourier:8}
De \cref{exo:b2:fourier:1}, $S_{2N-1}(x) =
\frac4\pi\sum_{k=0}^{N-1}\frac{\sin((2k+1)x)}{2k+1}$. En $x_N =
\frac{\pi}{2N}$, con $u_k = (2k+1)x_N = \frac{(2k+1)\pi}{2N}$:
\[
S_{2N-1}(x_N) = \frac{4}{\pi}\sum_{k=0}^{N-1}\frac{\sin u_k}{2k+1}
= \frac{4}{\pi}\sum_{k=0}^{N-1}\frac{\sin u_k}{u_k}\cdot
\frac{u_k}{2k+1}
= \frac{2}{\pi}\sum_{k=0}^{N-1}\frac{\sin u_k}{u_k}\cdot
\frac{\pi}{N},
\]
desde $\frac{u_k}{2k+1} = \frac{\pi}{2N}$. Los puntos $u_k$ son los
puntos medios de los subintervalos $N$ de $\intcc{0}{\pi}$ de longitud
$\frac{\pi}{N}$: la suma es una suma de Riemann en el punto medio de la
\omterm{def:b2:metric:continuity}{continuo} $u \mapsto \frac{\sin u}{u}$, por lo tanto converge a
\[
\frac{2}{\pi}\int_0^\pi \frac{\sin u}{u}\,\dd u
\approx \frac{2}{\pi}\times 1.8519 \approx 1.179 .
\]
Las sumas parciales cerca del salto sobrepasan para siempre el valor $1$ por $\approx
18\%$ del medio salto: el fenómeno de Gibbs, cuantificado.
\end{solution}

\begin{solution}{exo:b2:fourier:9}
De $\cos 3t = 4\cos^3t - 3\cos t$:
\[
\cos^3 t = \frac{3\cos t + \cos 3t}{4},
\qquad
\sin^2t\,\cos t = \cos t - \cos^3 t
= \frac{\cos t - \cos 3t}{4} .
\]
Cada uno es un polinomio trigonométrico, por lo tanto igual a su propio
Serie de Fourier (unicidad de los coeficientes: dos expansiones
diferiría por un polinomio trigonométrico con todos
coeficientes cero). Para $\cos^3t$: $a_1 = \frac34$, $a_3 =
\frac14$, todos los demás $a_n $y todos $b_n $cero; $c_{\pm1} =
\frac38$, $c_{\pm3} = \frac18$. Para $\sin^2t\cos t$: $a_1 =
\frac14$, $a_3 = -\frac14$; $c_{\pm1} = \frac18$, $c_{\pm3} =
-\frac18$.
\end{solution}

\begin{solution}{exo:b2:fourier:10}
Cálculo directo:
\[
c_n = \frac{1}{2\pi}\int_{-\pi}^{\pi}\eu^{(a - \iu n)t}\dd t
= \frac{\eu^{(a-\iu n)\pi} - \eu^{-(a - \iu n)\pi}}
{2\pi(a - \iu n)}
= \frac{(-1)^n\sinh(a\pi)}{\pi(a - \iu n)} ,
\]
utilizando $\eu^{\pm\iu n\pi} = (-1)^n$. En $t = \pi$ el periódico
la extensión salta de $\eu^{a\pi}$ a $\eu^{-a\pi}$; Dirichlet
(\omterm{def:b2:quadratic:adjoint}{simétrico} sumas parciales) da
\[
\cosh(a\pi) = \sum_{n\in\Z}(-1)^n c_n\,
= \frac{\sinh(a\pi)}{\pi}\Bigl(\frac1a +
\sum_{n\geq1}\Bigl(\frac{1}{a - \iu n} +
\frac{1}{a + \iu n}\Bigr)\Bigr)
= \frac{\sinh(a\pi)}{\pi}\Bigl(\frac1a +
\sum_{n\geq1}\frac{2a}{a^2 + n^2}\Bigr) ,
\]
las partes imaginarias de los términos emparejados se cancelan. dividir por
$\sinh(a\pi)$:
\[
\coth(\pi a) = \frac{1}{\pi a} +
\sum_{n\geq1}\frac{2a}{\pi(a^2 + n^2)} ,
\]
el gemelo hiperbólico de \cref{exo:b2:fourier:5}.
\end{solution}

\begin{solution}{exo:b2:fourier:11}
Conmutatividad: sustituir $s = x - t$ y utilizar la periodicidad de
el integrando. Para $c_n(f * g)$, el integrando $(x, t) \mapsto
f(x-t)g(t)\eu^{-\iu nx}$ es \omterm{def:b2:metric:continuity}{continuo}; los dos iterados
integrales concuerdan (ambas, como funciones del límite superior de la
variable exterior, desaparece en el punto final izquierdo y tiene el mismo
derivado --- \omterm{def:b2:metric:continuity}{continuidad} más
\cref{thm:b2:integration:continuity} justificar la diferenciación
la integral iterada). Por lo tanto
\[
c_n(f*g) = \frac{1}{2\pi}\int_{-\pi}^{\pi} g(t)\,\eu^{-\iu nt}
\Bigl(\frac{1}{2\pi}\int_{-\pi}^{\pi}
f(x-t)\,\eu^{-\iu n(x-t)}\dd x\Bigr)\dd t
= c_n(f)\,c_n(g),
\]
la integral interna es $c_n(f)$ para cada $t$ (sustitución
y periodicidad). Finalmente \cref{lem:b2:fourier:kernel} dice
$S_N(f)(x) = \frac{1}{2\pi}\int f(x+u)D_N(u)\dd u$; el
sustitución $u \mapsto -t$ y la uniformidad de $D_N$ convierten esto
en $(f * D_N)(x)$.
\end{solution}

\begin{solution}{exo:b2:fourier:12}
\begin{enumerate}
 \item Con $w = r\eu^{\iu t}$:
 \[
 P_r(t) = 1 + 2\,\Re\frac{w}{1 - w}
 = \Re\frac{1 + w}{1 - w}
 = \frac{1 - \abs w^2}{\abs{1 - w}^2}
 = \frac{1 - r^2}{1 - 2r\cos t + r^2} > 0 .
 \]
 Media $1$: integración término a término del \omterm{def:b2:funcseq:series}{normalmente}
 La serie convergente mantiene solo $n = 0$.
 \item Para $\delta \leq \abs t \leq \pi$: $\cos t \leq
 \cos\delta$, entonces $P_r(t) \leq \frac{1 - r^2}{1 -
 2r\cos\delta + r^2}$, cuyo denominador tiende a $2 -
 2\cos\delta > 0$ mientras que el numerador tiende a $0$:
 \omterm{def:b2:funcseq:def}{convergencia uniforme} a $0$ en cualquier entorno de
 $0$.
 \item Integración término por término (\omterm{def:b2:funcseq:series}{convergencia normal} en $t$)
 da $(f * P_r)(x) = \sum_n r^{\abs
 n}c_n(f)\eu^{\iu nx}$. La identidad aproximada
 argumento: con media $1$ y positividad,
 \[
 \abs{(f*P_r)(x) - f(x)}
 \leq \frac{1}{2\pi}\int_{-\pi}^{\pi}
 \abs{f(x-t) - f(x)}\,P_r(t)\,\dd t ,
 \]
 dividir en $\abs t = \delta$: como máximo $\varepsilon$
 (Heine) más $2\norm f_\infty\sup_{\delta\leq\abs
 t\leq\pi}P_r \to \varepsilon$: \omterm{def:b2:funcseq:def}{convergencia uniforme} como
 $r \to 1^-$. Este es \omterm{thm:b2:series:abel}{resumen de abel} del Fourier.
 serie --- el gemelo de Fourier de la serie de potencias
 La teoría de límites del capítulo.
\end{enumerate}
\end{solution}

\begin{solution}{pb:b2:fourier:1}
\textbf{1.} Promediando \cref{lem:b2:fourier:kernel} sobre $n = 0,
\dots, N-1$(linealidad de la integral) se obtiene $\sigma_N(f)(x) =
\frac{1}{2\pi}\int f(x+u)F_N(u)\dd u$; cada $D_n$ tiene media
$1$, por lo que $F_N$ significa $1$.

\textbf{2.} Con $\eu^{\iu u} - 1 = 2\iu\,\eu^{\iu
u/2}\sin\frac u2$:
\[
\sum_{n=0}^{N-1}\sin\Bigl(\Bigl(n + \frac12\Bigr)u\Bigr)
= \Im\Bigl[\eu^{\iu u/2}\,\frac{\eu^{\iu Nu} - 1}{\eu^{\iu u}
- 1}\Bigr]
= \Re\,\frac{1 - \eu^{\iu Nu}}{2\sin\frac u2}
= \frac{1 - \cos Nu}{2\sin\frac u2}
= \frac{\sin^2\frac{Nu}2}{\sin\frac u2} .
\]
Dividiendo por $N\sin\frac u2$:
\[
F_N(u) = \frac1N\sum_{n=0}^{N-1}
\frac{\sin\bigl((n+\frac12)u\bigr)}{\sin\frac u2}
= \frac{1}{N}\,
\frac{\sin^2\frac{Nu}{2}}{\sin^2\frac u2} \geq 0 .
\]

\textbf{3.} En $\delta \leq \abs u \leq \pi$: $\sin^2\frac u2
\geq \sin^2\frac\delta2$y $\sin^2\frac{Nu}2 \leq 1$: $F_N
\leq \frac{1}{N\sin^2(\delta/2)} \to 0$, \omterm{def:b2:funcseq:def}{uniformemente} allí.

\textbf{4.} Por unidad media, $\sigma_Nf(x) - f(x) =
\frac{1}{2\pi}\int\bigl(f(x+u) - f(x)\bigr)F_N(u)\dd u$. dado
$\varepsilon$, Heine produce $\delta$ con $\abs{f(x+u) - f(x)}
\leq \varepsilon $para $\abs u \leq \delta$, uniformemente en $x$.
Luego, usando $F_N \geq 0$ y su media unitaria,
\[
\abs{\sigma_Nf(x) - f(x)}
\leq \varepsilon +
2\norm f_\infty\cdot\frac{1}{2\pi}
\int_{\delta\leq\abs u\leq\pi}F_N
\leq \varepsilon + \frac{2\norm
f_\infty}{N\sin^2\frac\delta2}
\leq 2\varepsilon
\]
para $N$ grande, uniformemente en $x$: teorema de Fejér.

\textbf{5.} $F_N$ es par con la media $1$: cada mitad
$\intcc{0}{\pi}$, $\intcc{-\pi}{0}$ lleva la media $\frac12$.
entonces
\[
\sigma_Nf(x) - \frac{f(x^+)+f(x^-)}{2}
= \frac{1}{2\pi}\int_0^\pi\bigl(f(x+u) -
f(x^+)\bigr)F_N\,\dd u
+ \frac{1}{2\pi}\int_{-\pi}^0\bigl(f(x+u) -
f(x^-)\bigr)F_N\,\dd u ;
\]
en cada mitad, dividido en $\abs u = \delta$ donde el unilateral
El límite es $\varepsilon$-close y deja que la pregunta 3 acabe con el límite
parte: ambas integrales tienden a $0$. El cota: $F_N \geq 0$
da $\abs{\sigma_Nf(x)} \leq
\frac{1}{2\pi}\int\abs{f(x+u)}F_N \leq \norm f_\infty$.

\textbf{6.} Cada $\sigma_N(f)$ es un polinomio trigonométrico
(un promedio de $S_n(f)$, $n < N$) y $\sigma_N(f) \to f$
\omterm{def:b2:funcseq:def}{uniformemente}: el teorema trigonométrico de Weierstrass.

\textbf{7.} $c_n(f) = 0$ para todos los $n$ hace cada $S_n(f) = 0$,
de ahí cada $\sigma_N(f) = 0$; por Fejér, $f = \lim\sigma_Nf =
0$. Aplicando esto a una diferencia: \omterm{def:b2:metric:continuity}{continuo} periódica
Las funciones están determinadas por su \omterm{def:b2:fourier:coefficients}{coeficientes de Fourier}.

\textbf{8.} $S_Nf$ es la proyección ortogonal de $f$ sobre
$\mathcal T_N$ (\cref{prop:b2:fourier:bessel}), por lo que minimiza
$\norm{f - P}_2$ sobre $P \in \mathcal T_N$; desde $\sigma_Nf
\in \mathcal T_{N-1} \subseteq \mathcal T_N$:
\[
\norm{f - S_Nf}_2 \leq \norm{f - \sigma_Nf}_2
\leq \norm{f - \sigma_Nf}_\infty \to 0
\]
(la desigualdad media porque la media de $\abs\cdot^2$ está en
la mayoría del sup al cuadrado). Pitágoras $\norm f_2^2 = \sum_{\abs
n\leq N}\abs{c_n}^2 + \norm{f - S_Nf}_2^2$ pasa luego al
límite: Parseval para cada \omterm{def:b2:metric:continuity}{continuo} periódico $f$.

\textbf{9.} Sean $t_1, \dots, t_p$ los saltos de $f$ en un
período, $M = \norm f_\infty$. Para $\eta$ pequeño, defina $g = f$
fuera de los intervalos $\intoo{t_j - \eta}{t_j + \eta}$ y por
el acorde afín en cada uno de estos intervalos: $g$ es \omterm{def:b2:metric:continuity}{continuo},
periódico, $\norm g_\infty \leq M$, y
\[
\norm{f - g}_2^2 \leq \frac{1}{2\pi}\,p\cdot(2M)^2\cdot2\eta
\leq \varepsilon^2
\]
para $\eta$ pequeño. Bessel hace $S_N$ una contracción para
$\norm\cdot_2$, entonces
\[
\norm{f - S_Nf}_2
\leq \norm{f - g}_2 + \norm{g - S_Ng}_2 + \norm{S_N(g -
f)}_2
\leq 2\varepsilon + \norm{g - S_Ng}_2 ,
\]
y la pregunta 8 da $\limsup_N\norm{f - S_Nf}_2 \leq
2\varepsilon $por cada $\varepsilon$: $\norm{f - S_Nf}_2 \to
0$, y Pitágoras da Parseval por cada trozo
\omterm{def:b2:metric:continuity}{continuo} $f$: el ``admitido'' en
\cref{thm:b2:fourier:parseval} es ahora un teorema.

\textbf{10.} La onda cuadrada tiene $\norm f_\infty = 1$, entonces
la pregunta 5 da $\abs{\sigma_N(f)} \leq 1$ en todas partes y para
cada $N$ --- sin exceso, nunca --- mientras
\cref{exo:b2:fourier:8} muestra $\sup_xS_{2N-1}(f)(x) \to
\approx 1.179$. La única propiedad responsable: $F_N \geq 0$, entonces
$\sigma_Nf(x)$ es un \emph{promedio} ponderado de valores de $f$
y nunca podrá salir de $\intcc{\min f}{\max f}$; $D_N$ toma
valores negativos, por lo que $S_N$ puede hacerlo.

\textbf{11.} $\abs{\sin N\theta} \leq N\abs{\sin\theta}$ por
inducción ($\abs{\sin(N{+}1)\theta} \leq
\abs{\sin N\theta}\abs{\cos\theta} +
\abs{\cos N\theta}\abs{\sin\theta} \leq
(N+1)\abs{\sin\theta}$): con $\theta = \frac u2$,
\[
F_N(u) = \frac{\sin^2\frac{Nu}2}{N\sin^2\frac u2}
\leq \frac{N^2\sin^2\frac u2}{N\sin^2\frac u2} = N .
\]
La concavidad de $\sin$ en $\intcc{0}{\frac\pi2}$ da
$\sin\frac u2 \geq \frac{u}{\pi}$ para $0 \leq u \leq \pi$, entonces
$F_N(u) \leq \frac{1}{N(u/\pi)^2} = \frac{\pi^2}{Nu^2}$.

\textbf{12.} Por equidad y división en $\frac1N$:
\[
\frac{1}{2\pi}\int_{-\pi}^{\pi}\abs uF_N
= \frac1\pi\int_0^\pi uF_N
\leq \frac1\pi\Bigl(\int_0^{1/N}uN\,\dd u +
\int_{1/N}^{\pi}\frac{\pi^2}{Nu}\,\dd u\Bigr)
= \frac{1}{2\pi N} + \frac{\pi\ln(\pi N)}{N} .
\]
Para $N \geq 2$: $\ln(\pi N) \leq \bigl(1 +
\frac{\ln\pi}{\ln2}\bigr)\ln N \leq 2.66\ln N$ y
$\frac{1}{2\pi N} \leq \frac{\ln N}{N}$, entonces el momento es
$\leq \frac{9\ln N}{N}$.

\textbf{13.} Para $L$-Lipschitz $f$:
\[
\abs{\sigma_Nf(x) - f(x)}
\leq \frac{1}{2\pi}\int\abs{f(x+u) - f(x)}F_N(u)\dd u
\leq L\cdot\frac{1}{2\pi}\int\abs uF_N
\leq \frac{9L\ln N}{N},
\]
uniformemente en $x$.

\textbf{14.} $c_0(e_1) = 0$ da $S_0(e_1) = 0$, mientras
$S_n(e_1) = e_1$ para $n \geq 1$: $\sigma_N(e_1) =
\frac{N-1}{N}e_1$, entonces $\norm{\sigma_Ne_1 - e_1}_\infty =
\frac1N$. Incluso para toda esta señal de banda limitada, la velocidad es
$\frac1N$: Fejér satura, exactamente como el operador de Bernstein
satura en $\frac1n$ (Voronovskaya, en el
problema de fin de semana del capítulo de secuencias de funciones).

\textbf{15.} Si $f$ desaparece en $\intoo{x-\delta}{x+\delta}$,
luego $\sigma_Nf(x) = \frac{1}{2\pi}\int_{\delta \leq \abs u
\leq \pi}f(x+u)F_N(u)\dd u$, de valor absoluto como máximo
$\frac{\norm f_\infty}{N\sin^2(\delta/2)} \to 0$: el Cesàro
significa que en $x$ solo se ve $f$ cerca de $x$.

\textbf{16.} Dados $\intcc ab$ y $\varepsilon$, elija
\omterm{def:b2:metric:continuity}{continuo} $1$-periódico afín por partes $\varphi^\pm$ con
$\varphi^- \leq \mathbf 1_{\intcc ab} \leq \varphi^+$ y
$\int_0^1(\varphi^+ - \varphi^-) \leq \varepsilon$ (trapecios
con pendientes en intervalos de longitud total $\varepsilon$).
entonces
\[
\limsup_N\frac{\#\{n \leq N : x_n \in \intcc ab\}}{N}
\leq \lim_N\frac1N\sum_{n\leq N}\varphi^+(x_n)
= \int_0^1\varphi^+ \leq b - a + \varepsilon ,
\]
y simétricamente $\liminf \geq b - a - \varepsilon$: el
la proporción tiende a $b - a$.

\textbf{17.} Para $f = \eu^{2\iu\pi k\cdot}$ con $k \neq 0$ el
la hipótesis da el límite $0 = \int_0^1f$; para $k = 0$ ambos
los lados son $1$; la linealidad maneja cada trigonometría
polinomio. Para \omterm{def:b2:metric:continuity}{continuo} $1$-periódico $f$ y $\varepsilon >
0$, la pregunta 6 (transportada por $t = 2\pi x$) proporciona una
polinomio trigonométrico $P$ con $\norm{f - P}_\infty \leq
\varepsilon$:
\[
\Bigl|\frac1N\sum_{n\leq N}f(x_n) - \int_0^1f\Bigr|
\leq 2\varepsilon +
\Bigl|\frac1N\sum_{n\leq N}P(x_n) - \int_0^1P\Bigr|
\longrightarrow 2\varepsilon .
\]
Con la pregunta 16: $(x_n)$ está equidistribuido.

\textbf{18.} Para $k \neq 0$ e irracional $\alpha$, $w =
\eu^{2\iu\pi k\alpha} \neq 1$:
\[
\Bigl|\sum_{n=1}^{N}w^n\Bigr|
= \Bigl|\frac{w(w^N - 1)}{w - 1}\Bigr|
\leq \frac{2}{\abs{1 - w}} ,
\]
un límite independiente de $N$; al dividir entre $N$ se obtiene Weyl
criterio, y la pregunta 17 concluye: $(\{n\alpha\})$ es
equidistribuido.

\textbf{19.} Cada subintervalo recibe proporción asintótica
igual a su longitud, en particular infinitos puntos:
$(\{n\alpha\})$ es denso. La equidistribución es más fuerte: a
La secuencia puede ser densa y pasar casi todo el tiempo en una.
esquina (la densidad dice dónde está la secuencia \emph{va},
la equidistribución dice \emph{con qué frecuencia}).

\textbf{20.} $2^n$ tiene el dígito inicial $1$ iff $10^m \leq 2^n <
2\cdot10^m $para algunos $m$, es decir,\ iff $\{n\log_{10}2\} \in
\intco{0}{\log_{10}2}$. Irracionalidad: $\log_{10}2 =
\frac pq $daría $2^q = 10^p = 2^p5^p$, imposible para $p
\geq 1$ por factorización única. Teorema de Weyl (pregunta 18
con $\alpha = \log_{10}2$; el intervalo \omterm{def:b2:metric:topology}{medio abierto} está comprimido
entre cerrados de longitudes cercanas) da la proporción
$\log_{10}2 \approx 0.301$: siguen los primeros dígitos de $2^n$
La ley de Benford.

\textbf{21.} $z$ es $C^1$, por lo que su serie de Fourier converge
\omterm{def:b2:funcseq:series}{normalmente} con suma $z$ (\cref{thm:b2:fourier:parseval}~(1)),
y $c_n(z') = \iu n\,c_n$. Desde $\abs{z'} = \frac{L}{2\pi}$
es constante,
\[
\int_0^{2\pi}\abs{z'}^2\dd t
= 2\pi\Bigl(\frac{L}{2\pi}\Bigr)^{\!2}
= \frac{L^2}{2\pi} ,
\]
y Parseval aplicado al \omterm{def:b2:metric:continuity}{continuo} $z'$ da
$\frac{1}{2\pi}\int_0^{2\pi}\abs{z'}^2 =
\sum_n\abs{\iu nc_n}^2$, es decir $\frac{L^2}{2\pi} = 2\pi\sum_n
n^2\abs{c_n}^2$.

\textbf{22.} El Parseval polarizado $\frac{1}{2\pi}\int\conj
fg = \sum\conj{c_n(f)}c_n(g)$ se deriva del Parseval aplicado a
$f + g$ y $f + \iu g$ (\omterm{def:b2:quadratic:def}{identidad de polarización}), ambos
\omterm{def:b2:metric:continuity}{continuo}. Con $f = z$, $g = z'$:
\[
A = \frac12\,\Im\int_0^{2\pi}\conj z\,z'
= \pi\,\Im\sum_n\conj{c_n}(\iu n c_n)
= \pi\sum_n n\abs{c_n}^2 .
\]

\textbf{23.} Combinando las preguntas 21--22:
\[
L^2 - 4\pi A
= 4\pi^2\sum_n n^2\abs{c_n}^2 -
4\pi^2\sum_n n\abs{c_n}^2
= 4\pi^2\sum_{n\in\Z}(n^2 - n)\abs{c_n}^2 \geq 0 ,
\]
desde $n^2 - n = n(n-1) \geq 0$ para cada número entero. Igualdad
fuerza $c_n = 0$ para todo $n \notin \{0, 1\}$: $z(t) = c_0 +
c_1\eu^{\iu t}$, un círculo de centro $c_0$ y radio
$\abs{c_1} = \frac{L}{2\pi}$ (velocidad constante). La prueba de Hurwitz
de la desigualdad isoperimétrica: $A \leq \frac{L^2}{4\pi}$,
círculo solamente.

\textbf{24.} Círculo de radio $R$: $L = 2\pi R$, $A = \pi
R^2$: $L^2 = 4\pi^2R^2 = 4\pi A$: igualdad. cuadrado de lado
$a$: $L^2 = 16a^2 > 4\pi a^2 = 4\pi A$ (como $16 > 4\pi \approx
12.57$). Para $n$negativo, $n^2 - n = n(n - 1)$ es un producto
de dos números enteros negativos: positivo --- tan al revés
Los modos cuestan el área dos veces. Velocidad constante ingresada en la pregunta 21,
convirtiendo $\int\abs{z'}^2$ en $\frac{L^2}{2\pi}$; por un
velocidad no constante, Cauchy-Schwarz da $\int\abs{z'}^2 \geq
\frac{(\int\abs{z'})^2}{2\pi} = \frac{L^2}{2\pi}$, por lo que el
La desigualdad sobrevive y el círculo sigue siendo la única igualdad.
caso.

\textbf{25.} (i) Todo fluye desde $F_N \geq 0$ (con unidad
media y concentración); $D_N$ tiene unidad media y concentración
de oscilación pero no de positividad, y Gibbs es el precio.
(ii) Cesàro \omterm{def:b2:series:summable}{sumabilidad} de la serie de Fourier implica su
Abel \omterm{def:b2:series:summable}{sumabilidad} con la misma suma (Frobenius, probado en el
problema del fin de semana del capítulo de la serie de potencias) --- el núcleo de Poisson
La ruta de \cref{exo:b2:fourier:12} es exactamente el método de Abel.
(iii) El teorema de Weyl sólo necesitaba una aproximación uniforme
(pregunta 6); la desigualdad isoperimétrica necesaria Parseval
mismo (preguntas 8, 21-22). (iv) El volumen del año 3 demuestra
\omterm{def:b2:metric:complete}{lo completo}: las exponenciales forman una base de Hilbert de $L^2$,
Parseval se convierte en una isometría de los espacios de Hilbert, y Fejér
El teorema se convierte en la afirmación de que esta isometría es computable.
por promedios positivos.
\end{solution}