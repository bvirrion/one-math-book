\section*{Capítulo \ref{ch:b2:reduction} --- Reduction of Endomorphisms}

\begin{solution}{exo:b2:reduction:1}
$A$: $\chi_A = X^2 - 2X - 3 = (X - 3)(X + 1)$. \omterm{def:b2:reduction:eigen}{Vectores propios}: para
$3$: $(1,1)$; para $-1$: $(1,-1)$. Entonces $P = \begin{pmatrix} 1 & 1\\ 1
& -1\end{pmatrix}$ da $P^{-1}AP = \operatorname{diag}(3, -1)$.

$B = J - I$ donde $J$ es la matriz de todos unos. $J$ tiene el rango $1$ con
$Jv = 3v$ para $v = (1,1,1)$ y $Jw = 0$ en el avión $x + y + z =
0$: \omterm{def:b2:reduction:eigen}{espectro} de $B$es $\{2, -1\}$ con \omterm{def:b2:reduction:eigen}{espacios propios}
$\operatorname{Vect}(1,1,1)$ (dimensión $1$) y $\{x + y + z = 0\}$
(dimensión $2$, base $(1,-1,0), (1,0,-1)$). $P$ con estos tres
columnas dan $P^{-1}BP = \operatorname{diag}(2, -1, -1)$.
\end{solution}

\begin{solution}{exo:b2:reduction:2}
\emph{\omterm{def:b2:reduction:eigen}{Espacios propios}:} $\chi_C = (X-1)^2$, soltero \omterm{def:b2:reduction:eigen}{valor propio} $1$;
$\ker(C - I) = \ker\begin{pmatrix} 0&1\\ 0&0\end{pmatrix}$ es el
línea $\operatorname{Vect}(e_1)$: dimensión $1 < 2 = m_1$, por lo que no
\omterm{def:b2:reduction:diag}{diagonalizable} (\cref{thm:b2:reduction:diagcrit}).

\emph{\omterm{def:b2:reduction:polyu}{Polinomio mínimo}:} $\mu_C$ divide $(X-1)^2$ y $C \neq I$,
entonces $\mu_C = (X-1)^2$: una raíz doble, entonces no \omterm{def:b2:reduction:diag}{diagonalizable}
(\cref{cor:b2:reduction:minpolycrit}).
\end{solution}

\begin{solution}{exo:b2:reduction:3}
$X^2 - 5X + 6 = (X-2)(X-3)$: dividido con raíces simples, por lo que $u$ es
\omterm{def:b2:reduction:diag}{diagonalizable} (\cref{cor:b2:reduction:minpolycrit}), con
$\operatorname{Sp}(u) \subseteq \{2, 3\}$. Posibles espectros:
$\{2\}$ ($u = 2\,\mathrm{id}$), $\{3\}$ ($u = 3\,\mathrm{id}$), o
$\{2, 3\}$.

Poderes: buscar $u^k = a_k\,\mathrm{id} + b_k\,u$. En el \omterm{def:b2:reduction:eigen}{espacios propios},
esto dice $2^k = a_k + 2b_k$ y $3^k = a_k + 3b_k$: resolviendo, $b_k
= 3^k - 2^k$, $a_k = 3\cdot2^k - 2\cdot 3^k$:
\[
u^k = (3\cdot 2^k - 2\cdot 3^k)\,\mathrm{id} + (3^k - 2^k)\, u .
\]
(Válido para los tres espectros: las identidades se mantienen en cuanto a valores propios).
\end{solution}

\begin{solution}{exo:b2:reduction:4}
$u$ \omterm{def:b2:reduction:diag}{diagonalizable}: $P = \prod_{\lambda}(X - \lambda)$ sobre el
\omterm{def:b2:reduction:eigen}{espectro} aniquila $u$, escisiones, raíces simples. Entonces $P(u|_F) =
P(u)|_F = 0$: la restricción es aniquilada por un polinomio dividido
con raíces simples, por lo tanto \omterm{def:b2:reduction:diag}{diagonalizable}
(\cref{cor:b2:reduction:minpolycrit}).
\end{solution}

\begin{solution}{exo:b2:reduction:5}
$\chi_A = X^2 - X - 1$, raíces $\varphi$ y $\psi$ (distintas):
\omterm{def:b2:reduction:diag}{diagonalizable}, con \omterm{def:b2:reduction:eigen}{vectores propios} $(\varphi, 1)$ y $(\psi, 1)$.
La recurrencia da $\begin{pmatrix} F_{n+1}\\ F_n \end{pmatrix} =
A^n \begin{pmatrix}1\\ 0\end{pmatrix}$. Descomponer $(1, 0)$ en el
\omterm{def:b2:reduction:eigen}{vectores propios}: $(1,0) = \frac{1}{\varphi - \psi}\bigl((\varphi, 1) -
(\psi, 1)\bigr)$con $\varphi - \psi = \sqrt5$. Aplicando $A^n$
multiplica cada componente propio por su $n$-ésima potencia de \omterm{def:b2:reduction:eigen}{valor propio};
leyendo la segunda coordenada:
\[
F_n = \frac{\varphi^n - \psi^n}{\sqrt 5} .
\]
(Compruebe: $n = 1$ da $\frac{\varphi - \psi}{\sqrt5} = 1$).
\end{solution}

\begin{solution}{exo:b2:reduction:6}
Deja que $P = \prod_i (X - \mu_i)$ aniquile a $u^2$, divídelo con simple
raíces $\mu_i$ (el \omterm{def:b2:reduction:eigen}{espectro} de $u^2$). Dado que $u$ es invertible, $0$
no es un \omterm{def:b2:reduction:eigen}{valor propio} de $u^2$ ($\det u^2 = (\det u)^2 \neq 0$), por lo que
todos $\mu_i \neq 0$. entonces
\[
Q(X) = \prod_i (X^2 - \mu_i) = \prod_i (X - \sqrt{\mu_i})(X +
\sqrt{\mu_i})
\]
aniquila $u$: $\;Q(u) = \prod_i (u^2 - \mu_i\,\mathrm{id}) =
P(u^2) = 0$. Sus raíces $\pm
\sqrt{\mu_i}$ (raíces cuadradas complejas) son distintas por pares porque
los $\mu_i$ son distintos y distintos de cero ($\sqrt{\mu_i} = -\sqrt{\mu_j}$
daría $\mu_i = \mu_j$). División + raíces simples: $u$ es
\omterm{def:b2:reduction:diag}{diagonalizable}.

Contraejemplo sin invertibilidad: $u = \begin{pmatrix} 0 & 1\\
0 & 0\end{pmatrix}$: $u^2 = 0$ es \omterm{def:b2:reduction:diag}{diagonalizable}, $u$ no lo es.
\end{solution}

\begin{solution}{exo:b2:reduction:7}
$A = 2I + N$ con $N$ el turno ($N e_2 = e_1$, $Ne_3 = e_2$),
$N^3 = 0$, $N^2 = E_{13}$: este \emph{is} el \omterm{thm:b2:reduction:dunford}{Descomposición de Dunford}
($2I$ diagonal, $N$ nilpotentes, conmutan; la unicidad lo hace
el uno). Binomio con términos de desplazamiento:
\[
A^k = 2^k I + k 2^{k-1} N + \binom k2 2^{k-2} N^2
= \begin{pmatrix}
2^k & k2^{k-1} & \binom k2 2^{k-2}\\
0 & 2^k & k2^{k-1}\\
0 & 0 & 2^k
\end{pmatrix},
\]
\[
\eu^{tA} = \eu^{2t}\Bigl(I + tN + \frac{t^2}{2}N^2\Bigr)
= \eu^{2t}\begin{pmatrix}
1 & t & t^2/2\\
0 & 1 & t\\
0 & 0 & 1
\end{pmatrix}.
\]
\end{solution}

\begin{solution}{exo:b2:reduction:8}
$X^k - 1$ aniquila a $A$ y se divide en $\C$ con el $k$ distinto
raíces $\eu^{2\iu\pi j/k}$: $A$ es \omterm{def:b2:reduction:diag}{diagonalizable}
(\cref{cor:b2:reduction:minpolycrit}) y su \omterm{def:b2:reduction:eigen}{valores propios}, raíces de
$X^k - 1$, son $k$-ésimas raíces de la unidad.

Si además $A$ es similar a una matriz triangular con unidad
diagonal: todos \omterm{def:b2:reduction:eigen}{valores propios} son iguales a $1$ y $A$, \omterm{def:b2:reduction:diag}{diagonalizable} con
único \omterm{def:b2:reduction:eigen}{valor propio} $1$, es $P\,I\,P^{-1} = I$.
\end{solution}

\begin{solution}{exo:b2:reduction:9}
Escribe $E = \bigoplus_\lambda E_\lambda(u)$
(\cref{thm:b2:reduction:diagcrit}). Cada $E_\lambda(u)$ es
$v$-estable: para $x \in E_\lambda$, $u(v(x)) = v(u(x)) = \lambda
v(x)$. La restricción de $v$a $E_\lambda(u)$ es \omterm{def:b2:reduction:diag}{diagonalizable}
(\cref{exo:b2:reduction:4}): elige una base de $E_\lambda(u)$ hecha
de $v$-vectores propios. Concatenando estas bases sobre todo $\lambda$
da una base de $E$ cuyos vectores son \omterm{def:b2:reduction:eigen}{vectores propios} de \emph{ambos}
$u$ (por membresía en $E_\lambda(u)$) y $v$ (por construcción).
\end{solution}

\begin{solution}{exo:b2:reduction:10}
($\Rightarrow$) Sea $u$ \omterm{def:b2:reduction:diag}{diagonalizable} y $F$ estable. entonces
$u|_F$ es \omterm{def:b2:reduction:diag}{diagonalizable} (\cref{exo:b2:reduction:4}): $F$ tiene un
base de \omterm{def:b2:reduction:eigen}{vectores propios}, que se extiende, dentro de cada \omterm{def:b2:reduction:eigen}{espacio propio} global
$E_\lambda$, a una base de $E_\lambda$ (teorema de base incompleta
dentro de $E_\lambda$, a partir de la parte de la base de $F$ que se encuentra
allí --- nota $F = \bigoplus_\lambda (F \cap E_\lambda)$ desde
$u|_F$ es \omterm{def:b2:reduction:diag}{diagonalizable}). Los vectores agregados abarcan un establo.
suplemento (cada uno se encuentra en algún $E_\lambda$, por lo que su intervalo es
$u$-estable).

($\Leftarrow$) Dejemos que $F = \sum_\lambda E_\lambda(u)$ (un establo
subspace) y $G$ un suplemento estable. Si $G \neq \{0\}$: $\chi_{u|_G}$
se divide sobre $\C$, por lo que $u|_G$ tiene un \omterm{def:b2:reduction:eigen}{vector propio} $x \in G$
(\cref{thm:b2:reduction:trigonalization} o directamente la existencia
de una raíz); pero cada \omterm{def:b2:reduction:eigen}{vector propio} de $u$ se encuentra en $F$, por lo que $x \in F
\cap G = \{0\}$: contradicción. Por lo tanto $G = \{0\}$y $E = F$: el
\omterm{def:b2:reduction:eigen}{espacios propios} llena $E$, es decir, \ $u$ es \omterm{def:b2:reduction:diag}{diagonalizable}.
\end{solution}

\begin{solution}{exo:b2:reduction:11}
Trigonalizar: $A = PTP^{-1}$, $T = \begin{pmatrix} \lambda & c\\ 0 &
\mu\end{pmatrix}$, $\abs\lambda, \abs\mu < 1$. Entonces $A^k =
PT^kP^{-1}$, y basta con que $T^k \to 0$.

\emph{Distinct \omterm{def:b2:reduction:eigen}{valores propios}:} da inducción
\[
T^k = \begin{pmatrix}
\lambda^k & c\,\dfrac{\lambda^k - \mu^k}{\lambda - \mu}\\[4pt]
0 & \mu^k
\end{pmatrix},
\]
y cada entrada tiende a $0$ ($\abs{\lambda}^k, \abs\mu^k \to 0$).

\emph{Equal \omterm{def:b2:reduction:eigen}{valores propios} ($\mu = \lambda$):}$T = \lambda I + cE_{12}$
y $T^k = \lambda^k I + k\lambda^{k-1}cE_{12}$; la entrada
$k\lambda^{k-1} \to 0$ desde $\abs\lambda < 1$ (ritmos geométricos
polinomio). En ambos casos $T^k \to 0$ de entrada, por lo tanto $A^k =
PT^kP^{-1} \to 0$(la multiplicación de matrices por $P, P^{-1}$ fijo es
continuo en las entradas --- cada entrada del producto es un valor fijo
combinación lineal).
\end{solution}

\begin{solution}{exo:b2:reduction:12}
$\ker u$ tiene la dimensión $n - 1$ (rango--nulidad), por lo que $0$ es un
\omterm{def:b2:reduction:eigen}{valor propio} de \omterm{def:b2:reduction:charpoly}{multiplicidad geométrica} $n - 1$, y $\chi_u$ es
divisible por $X^{n-1}$
(\cref{def:b2:reduction:charpoly}: geométrico $\leq$ algebraico).
Escribe $\chi_u = X^{n-1}(X - \alpha)$; el coeficiente de
Siendo $X^{n-1}$ $-\operatorname{tr} u$, obtenemos $\alpha =
\operatorname{tr} u$: $\chi_u = X^{n-1}(X - \operatorname{tr}
u)$.

Si $\operatorname{tr} u \neq 0$: el \omterm{def:b2:reduction:eigen}{valor propio}
$\operatorname{tr} u$ es una raíz de $\chi_u$, por lo que lleva una
\omterm{def:b2:reduction:eigen}{vector propio}; el \omterm{def:b2:reduction:eigen}{espacios propios} para $0$ y $\operatorname{tr} u$
tienen dimensiones $n - 1$ y $\geq 1$, sumando $\geq n$:
complete $E$ y $u$ es \omterm{def:b2:reduction:diag}{diagonalizable}
(\cref{thm:b2:reduction:diagcrit}). Si $\operatorname{tr} u = 0$:
por \cref{exo:b2:linalg:5}, $u^2 = (\operatorname{tr} u)u = 0$
con $u \neq 0$: $u$ es un nilpotente distinto de cero y un \omterm{def:b2:reduction:diag}{diagonalizable}
nilpotente es cero (\cref{prop:b2:reduction:nilpotent}): no
\omterm{def:b2:reduction:diag}{diagonalizable}.
\end{solution}

\begin{solution}{pb:b2:reduction:1}
\textbf{1.} Las primeras coordenadas $k - 1$ de $Cv_n$ son $u_{n+1},
\dots, u_{n+k-1}$ (los desplazamientos superdiagonales), y la última es
$a_0 u_n + \dots + a_{k-1}u_{n+k-1}$. Entonces $v_{n+1} = Cv_n$ se mantiene
para todos $n$ si las últimas coordenadas coinciden con todos $n$, es decir,\ si
$(\mathcal R)$ se mantiene. Iterando, $v_n = C^nv_0$.

\textbf{2.} Expande $D_k(X) = \det(XI_k - C)$ a lo largo de la primera
columna: las dos entradas distintas de cero son $X$ (posición $(1,1)$) y
$-a_0$ (posición $(k,1)$). El primer menor tiene forma de $D_{k-1}$.
para los coeficientes $a_1, \dots, a_{k-1}$; el segundo menor es
triangular superior con diagonal $-1$: \omterm{def:b2:linalg:det}{determinante} $(-1)^{k-1}$,
con signo $(-1)^{k+1}$ de la posición. Inducción en $k$
(base $k = 1$: $X - a_0$) da
\[
D_k(X) = X\bigl(X^{k-1} - a_{k-1}X^{k-2} - \dots - a_1\bigr) -
a_0 = P(X).
\]
Para $\mu_C$: desde $Q(C^{\mathsf T}) = Q(C)^{\mathsf T}$ para cualquier
polinomio, $C$ y $C^{\mathsf T}$ tienen el mismo \omterm{def:b2:linalg:annihilator}{aniquiladores},
de ahí el mismo \omterm{def:b2:reduction:polyu}{polinomio mínimo}. Para $C^{\mathsf T}$: el
las columnas dicen $C^{\mathsf T}e_1 = e_2$, \dots, $C^{\mathsf
T}e_{k-1} = e_k$, por lo que $(e_1, C^{\mathsf T}e_1, \dots, (C^{\mathsf
T})^{k-1}e_1)$es la base canónica: gratis. Un polinomio $Q
\neq 0$ de grado$< k $tiene entonces $Q(C^{\mathsf T})e_1 \neq 0$
(es una combinación no trivial de vectores base): $\deg\mu \geq
k$. Como $\mu \mid \chi = P$con $\deg P = k$: $\mu_C = P$.

\textbf{3.} Para $v = (1, \lambda, \dots, \lambda^{k-1})^{\mathsf
T}$: las filas $1$a $k-1$ de $Cv $dan $\lambda, \lambda^2, \dots,
\lambda^{k-1}$, es decir, $\lambda $multiplicadas por las primeras entradas $k - 1$
de $v$; la última fila da $\sum_m a_m\lambda^m = \lambda^k -
P(\lambda) = \lambda^k = \lambda\cdot\lambda^{k-1}$. Entonces $Cv =
\lambda v$. Por el contrario, las ecuaciones $(Cx)_i = \lambda x_i$ para
$i < k$ lee $x_{i+1} = \lambda x_i$: cualquier \omterm{def:b2:reduction:eigen}{vector propio} es
proporcional a $v$ --- cada \omterm{def:b2:reduction:eigen}{espacio propio} tiene una dimensión exacta
$1$. \omterm{def:b2:reduction:diag}{Diagonalizable} si las dimensiones \omterm{def:b2:reduction:eigen}{espacio propio} suman $k$
(\cref{thm:b2:reduction:diagcrit}) si hay $k$ distintos
\omterm{def:b2:reduction:eigen}{valores propios} si $P$ tiene raíces distintas $k$ (las \omterm{def:b2:reduction:eigen}{valores propios} son
las raíces de $\chi_C = P$).

\textbf{4.} Cada $(\lambda_i^n)_n$ resuelve $(\mathcal R)$:
$\lambda_i^{n+k} = \lambda_i^n\,\lambda_i^k =
\lambda_i^n\sum_m a_m\lambda_i^m$. La libertad: una desaparición
combinación $\sum_i c_i\lambda_i^n = 0$ para $n = 0, \dots, k-1$
es un sistema Vandermonde (\cref{exo:b2:linalg:11}) en el $c_i$:
todos $c_i = 0$. El espacio de solución tiene dimensión $k$ (pregunta 6,
cuya prueba es elemental e independiente): $k$ soluciones gratuitas
forman una base y las coordenadas son únicas.

\textbf{5.} $P = X^2 - X - 6 = (X - 3)(X + 2)$: solución general
$u_n = A\,3^n + B(-2)^n$. Condiciones iniciales: $A + B = 1$, $3A -
2B = 8$: $A = 2$, $B = -1$:
\[
u_n = 2\cdot 3^n - (-2)^n .
\]
(Consulte: $u_2 = u_1 + 6u_0 = 14$ y $2\cdot9 - 4 = 14$).

\textbf{6.} $P(S)\bigl((u_n)\bigr)$ es la secuencia $n \mapsto
u_{n+k} - a_{k-1}u_{n+k-1} - \dots - a_0u_n$: desaparece si y así
$(\mathcal R)$ se cumple, por lo que el conjunto de soluciones es $\ker P(S)$, un
subespacio. El mapa $\ker P(S) \to \C^k$, $u \mapsto (u_0, \dots,
u_{k-1})$, es lineal, inyectivo (la recurrencia determina
$u_{k}, u_{k+1}, \dots$ a partir de los primeros valores de $k$, por inducción)
y sobreyectiva (defina $u_n$ recursivamente a partir de cualquier dato inicial):
dimensión $k$.

\textbf{7.} La prueba de usos \cref{thm:b2:reduction:kernels}
sólo: la identidad de Bézout en $\C[X]$, y el hecho de que
polinomios en un endomorfismo fijo conmutan. Ninguno menciona
la dimensión del espacio ambiental: el lema se cumple palabra por palabra para
$S \in \mathcal{L}(\mathcal{S})$. Por lo tanto
\[
\ker P(S) = \bigoplus_{i=1}^{r}
\ker\,(S - \lambda_i\,\mathrm{id})^{m_i}.
\]

\textbf{8.} Para $Q \in \C[X]$: $(S -
\lambda)\bigl(Q(n)\lambda^n\bigr)_n $tiene $n$-ésimo término
$Q(n{+}1)\lambda^{n+1} - \lambda Q(n)\lambda^n =
\lambda^{n+1}(\Delta Q)(n)$, con $\Delta Q = Q(X{+}1) - Q(X)$
de grado $\deg Q - 1$ (los términos principales se cancelan). iterando,
$(S - \lambda)^m\bigl(Q(n)\lambda^n\bigr) =
\bigl(\lambda^{n+m}(\Delta^m Q)(n)\bigr)_n $y$\Delta^m Q = 0$
cuando $\deg Q \leq m - 1$: el conjunto de la derecha está contenido en el
núcleo. Es un subespacio de dimensión $m$: las secuencias
$(n^j\lambda^n)_n$, $0 \leq j < m$, son libres, ya que
$\sum_j c_j n^j\lambda^n = 0$ para todas las fuerzas $n$ (dividiendo por
$\lambda^n \neq 0$) el polinomio $\sum_j c_jX^j$ desaparecerá en
cada $n \in \N$, por lo tanto, cero. Por el contrario $\dim\ker(S -
\lambda)^m \leq m$: al expandir $(S - \lambda)^m = \sum_j
\binom mj(-\lambda)^{m-j}S^j$, la ecuación $(S - \lambda)^m u =
0$ es una recurrencia lineal de orden $m$ (coeficiente principal
$1$), por lo que $u$ está determinado por $u_0, \dots, u_{m-1}$ como en
pregunta 6. Concluye igualdad de dimensiones.

\textbf{9.} Combine las preguntas 7 y 8: cada solución se descompone
únicamente como una suma de elementos de $\ker(S -
\lambda_i)^{m_i}$, es decir, $u_n = \sum_i Q_i(n)\lambda_i^n$ con
$\deg Q_i \leq m_i - 1$; el $Q_i$ son únicos porque el
La descomposición es directa y, dentro de cada suma, la
Los coeficientes de $Q_i$ son coordenadas en la base.
$(n^j\lambda_i^n)_j$ (pregunta 8). Verificación de cordura en las dimensiones:
$\sum_i m_i = k$.

\textbf{10.} $P = X^2 - 4X + 4 = (X - 2)^2$: soluciones $(a +
bn)2^n$. Datos iniciales: $a = 1$, $2(a + b) = 0$, entonces $b = -1$:
\[
u_n = (1 - n)\,2^n .
\]
Verifique: $u_2 = 4u_1 - 4u_0 = -4$ y $(1 - 2)\cdot4 = -4$.

\textbf{11.} Escribe $u_n = \lambda_1^n\bigl(c_1 + \sum_{i\geq2}
c_i(\lambda_i/\lambda_1)^n\bigr)$; cada relación tiene módulo$< 1$,
por lo que el corchete tiende a $c_1 \neq 0$: $u_n \sim c_1\lambda_1^n$.
En particular $u_n \neq 0$ para $n$ grande, y
\[
\frac{u_{n+1}}{u_n} =
\lambda_1\,\frac{c_1 + o(1)}{c_1 + o(1)} \longrightarrow
\lambda_1 .
\]

\textbf{12.} Calcular:
\[
q(a_{n+1}, b_{n+1}) = (a_n + 2b_n)^2 - 2(a_n + b_n)^2
= -a_n^2 + 2b_n^2 = -q(a_n, b_n).
\]
Con $q(a_0, b_0) = 1 - 2 = -1$: $a_n^2 - 2b_n^2 = (-1)^{n+1}$.
Estructuralmente: $q(a, b) = (a - \sqrt2\,b)(a + \sqrt2\,b)$ y
el mapa lineal $M$ multiplica el factor $a + \sqrt2 b$ por $1 +
\sqrt2$y el factor $a - \sqrt2 b $por $1 - \sqrt2$ (calcular:
$a_{n+1} + \sqrt2 b_{n+1} = (1 + \sqrt2)(a_n + \sqrt2 b_n)$);
el producto se multiplica por $(1 + \sqrt2)(1 - \sqrt2) = -1 =
\det M$ en cada paso.

\textbf{13.} Desde $a_n^2 - 2b_n^2 = (a_n - \sqrt2 b_n)(a_n +
\sqrt2 b_n) = (-1)^{n+1}$,
\[
\Bigl|\frac{a_n}{b_n} - \sqrt2\Bigr|
= \frac{\abs{a_n^2 - 2b_n^2}}{b_n(a_n + \sqrt2 b_n)}
= \frac{1}{b_n(a_n + \sqrt2 b_n)} \leq \frac1{2b_n^2},
\]
usando $a_n \geq b_n \geq 1$ (inducción: ambos aumentan) entonces $a_n +
\sqrt2 b_n \geq (1 + \sqrt2)b_n \geq 2b_n$. \omterm{def:b2:reduction:eigen}{Valores propios} de $M$:
$\chi_M = X^2 - 2X - 1$, raíces $1 \pm \sqrt2$; desde $(a_0, b_0)$
tiene un componente distinto de cero en el \omterm{def:b2:reduction:eigen}{vector propio} dominante (todas las entradas
positivo), $b_n \sim c(1 + \sqrt2)^n$ con $c > 0$ (pregunta
11). Por tanto, el error es $\asymp (1 + \sqrt2)^{-2n} = (3 +
2\sqrt2)^{-n}$: decaimiento geométrico con relación $1/(3 + 2\sqrt2) = 3
- 2\sqrt2 \approx 0.172$.

\textbf{14.} (a) De la pregunta 9: $\abs{u_n} \leq \sum_i
\abs{Q_i(n)}\abs{\lambda_i}^n \leq \bigl(\sum_i
\abs{Q_i(n)}\bigr)\rho^n$, y cada $\abs{Q_i(n)} \leq C_i
n^{m_i - 1} \leq C_i n^{m-1}$ para $n \geq 1$: sume las constantes.
(b) Sean $\rho' = \max_{i \geq 2}\abs{\lambda_i} <
\abs{\lambda_1}$y $d = \deg Q_1$, coeficiente principal $c
\neq 0$. Luego $u_n = Q_1(n)\lambda_1^n + R_n $con $\abs{R_n}
\leq Cn^{m-1}\rho'^n$, y
\[
\frac{R_n}{Q_1(n)\lambda_1^n} = O\Bigl(n^{m-1-d}
\bigl(\rho'/\abs{\lambda_1}\bigr)^n\Bigr) \longrightarrow 0
\]
(polinomio de tiempos geométricos). entonces
\[
u_n \sim Q_1(n)\,\lambda_1^n \sim c\,n^d\lambda_1^n,
\qquad
\frac{u_{n+1}}{u_n} \longrightarrow \lambda_1
\quad\text{(since } Q_1(n{+}1)/Q_1(n) \to 1\text{)}.
\]
Verifique la pregunta 10: para $u_n = (1-n)2^n$ la relación es
\[
\frac{(-n)2^{n+1}}{(1-n)2^n} = 2\,\frac{-n}{1-n}
\longrightarrow 2 = \lambda_1 .
\]

\textbf{15.} Inducción en $n$. Para $n = 1$, $A_{ij}$ cuenta
paseos de longitud $1$. Paso: un recorrido de longitud $n + 1$ desde $i$ hasta
$j$ es un paseo de longitud $n$ desde $i$ hasta algún vértice $\ell$
seguido de una arista $\ell j$:
\[
\#\{\text{walks}\} = \sum_{\ell} (A^n)_{i\ell}A_{\ell j} =
(A^{n+1})_{ij}.
\]

\textbf{16.} $J = 3\Pi$ donde $\Pi = J/3$ es la proyección sobre
$\operatorname{Vect}(1,1,1)$ a lo largo del avión $x + y + z = 0$
($\Pi^2 = \Pi$ desde $J^2 = 3J$). Entonces $A = J - I = 2\Pi - (I -
\Pi)$, y como $\Pi $y$I - \Pi$ son complementarios
proyecciones,
\[
A^n = 2^n\,\Pi + (-1)^n (I - \Pi),
\qquad\text{i.e.}\qquad
(A^n)_{ij} = \frac{2^n}3 + (-1)^n\Bigl(\delta_{ij} -
\frac13\Bigr),
\]
que da las dos fórmulas mostradas. En $n = 2$: diagonal
$(4 + 2)/3 = 2$ (camina $i \to \ell \to i$ para los dos vecinos
$\ell$); $(4 - 1)/3 = 1$ fuera de la diagonal (el recorrido único $i \to
\ell \to j$ a través del tercer vértice).

\textbf{17.} Deje que $w_n^{(0)}, w_n^{(1)}$ cuente las palabras admisibles de
longitud $n$ que termina en $0$, resp.\ $1$. Adjuntando una letra: a $0$
puede seguir cualquier cosa, un $1$ solo un $0$:
\[
\begin{pmatrix} w_{n+1}^{(0)}\\ w_{n+1}^{(1)}\end{pmatrix}
= \begin{pmatrix} 1 & 1\\ 1 & 0\end{pmatrix}
\begin{pmatrix} w_n^{(0)}\\ w_n^{(1)}\end{pmatrix}.
\]
Sumando, $w_{n+2} = w_{n+1} + w_n$ (o: condición en el primer
carta). Con $w_1 = 2$, $w_2 = 3$: $w_n = F_{n+2}$ por inducción
($F_3 = 2$, $F_4 = 3$, misma recurrencia). Crecimiento: las raíces de
$X^2 - X - 1$ son $\varphi > \abs\psi$
(\cref{exo:b2:reduction:5}), y el componente $\varphi$ es
distinto de cero (los $w_n$ son positivos y $\psi^n \to 0$), así que pregunte
11 da $w_{n+1}/w_n \to \varphi = \frac{1 + \sqrt5}2$.

\textbf{18.} $A = \begin{psmallmatrix} 0&1&0\\ 1&0&1\\
0&1&0\end{psmallmatrix}$. Comprobar:
\[
A(1, \pm\sqrt2, 1)^{\mathsf T}
= (\pm\sqrt2, 2, \pm\sqrt2)^{\mathsf T}
= \pm\sqrt2\,(1, \pm\sqrt2, 1)^{\mathsf T},
\qquad
A(1, 0, -1)^{\mathsf T} = 0 :
\]
\omterm{def:b2:reduction:eigen}{valores propios} $\sqrt2, -\sqrt2, 0$ ($= 2\cos\frac\pi4,
2\cos\frac{3\pi}4, 2\cos\frac\pi2$). Descomponer $e_1$ en el
base propia y leer la tercera coordenada, o usar simetría: con
$v_\pm = (1, \pm\sqrt2, 1)$, $v_0 = (1, 0, -1)$, uno marca $e_1
= \frac14 v_+ + \frac14 v_- + \frac12 v_0$, entonces para $n \geq 1$
\[
(A^n)_{13} = \Bigl(\tfrac14(\sqrt2)^n v_+ +
\tfrac14(-\sqrt2)^n v_- + 0\Bigr)_{\!3}
= \frac{(\sqrt2)^n + (-\sqrt2)^n}{4},
\]
cero para $n$ impar (gráfico bipartito: los extremos están a una distancia par),
y $2\cdot 2^{n/2}/4 = 2^{n/2 - 1}$ para incluso $n$. En $n = 4$:
$2^{1} = 2$, haciendo coincidir los dos paseos $1\,2\,1\,2\,3$ y
$1\,2\,3\,2\,3$.

\textbf{19.} Los paseos cerrados de longitud $n$ desde $i$ son
$(A^n)_{ii}$; sumando $i$ da $\operatorname{tr}(A^n)$.
Trigonalizando $A$ (sobre $\C$), $A^n$ es triangular con diagonal
$\lambda_i^n$: $\operatorname{tr}(A^n) = \sum_i\lambda_i^n$.
Triángulo: $\operatorname{tr}(A^n) = 3\,\frac{2^n + 2(-1)^n}3 =
2^n + 2(-1)^n = 2^n + (-1)^n + (-1)^n$: el \omterm{def:b2:reduction:eigen}{espectro}$\{2, -1,
-1\}$, consistente con la pregunta 16.

\textbf{20.} Las columnas de $W^{\mathsf T}$: $W^{\mathsf T}e_i =
e_{i-1}$ para $i \geq 1$y $W^{\mathsf T}e_0 = e_{k-1}$;
reetiquetado en el orden $e_0, e_1, \dots$ este es exactamente el
matriz complementaria de $X^k - 1$ ($a_0 = 1$, otra $a_m = 0$).
Pregunta 2: $\chi_{W} = \chi_{W^{\mathsf T}} = X^k - 1 =
\mu_{W}$. Las raíces $\omega^j $($j = 0, \dots, k-1$) son las $k$
distintas raíces de unidad $k$: $W$ es \omterm{def:b2:reduction:diag}{diagonalizable} (pregunta
3, o \cref{exo:b2:reduction:8}: $W^k = I$). \omterm{def:b2:reduction:eigen}{Vectores propios}: $Wf_j
= \sum_m \omega^{-jm}e_{m+1} = \sum_{m'}\omega^{-j(m'-1)}e_{m'}
= \omega^j f_j$.

\textbf{21.} Los circulantes son polinomios en $W$ y los polinomios
en una matriz fija conmutan entre sí. Cada $f_j$ es un
\omterm{def:b2:reduction:eigen}{vector propio} de cada potencia: $W^m f_j = \omega^{jm}f_j$, entonces
\[
Cf_j = \sum_m c_m\omega^{jm} f_j = \widehat c(\omega^j)\,f_j :
\]
la base $(f_0, \dots, f_{k-1})$ (gratis: Vandermonde en el
distinta $\omega^{-j}$, \cref{exo:b2:linalg:11}) diagonaliza
cada circulante a la vez, con el indicado \omterm{def:b2:reduction:eigen}{valores propios}.

\textbf{22.} El \omterm{def:b2:linalg:det}{determinante} es el producto del \omterm{def:b2:reduction:eigen}{valores propios}
(diagonalizar): $\det C = \prod_{j}\widehat c(\omega^j)$. Para $k
= 3$, $c_0 = a$, $c_1 = b$, $c_2 = c $y$\omega = j =
\eu^{2\iu\pi/3}$:
\[
\det C = (a + b + c)(a + bj + cj^2)(a + bj^2 + cj^4),
\]
y $j^4 = j$: exactamente la factorización de
\cref{exo:b2:linalg:8}.

\textbf{23.} $M = \frac12(W + W^{-1})$ es un circulante ($W^{-1} =
W^{k-1}$), con \omterm{def:b2:reduction:eigen}{valores propios}$\frac12(\omega^j + \omega^{-j}) =
\cos\frac{2\pi j}k $en la misma base $f_j$. Coordenadas: escribir
$x^{(0)} = \sum_j \alpha_j f_j$. Las coordenadas de $f_j$ suman
$\sum_m \omega^{-jm}$, que es $k$ para $j = 0$ y $0$
en caso contrario (suma geométrica con relación $\omega^{-j} \neq 1$).
Sumando las coordenadas de $x^{(0)}$: $\sum_m x^{(0)}_m =
\alpha_0\,k$, entonces $\alpha_0 = \frac1k\sum_m x^{(0)}_m$, la media.

\textbf{24.}$x^{(n)} = M^nx^{(0)} = \sum_j
\alpha_j\cos^n\bigl(\tfrac{2\pi j}k\bigr)f_j$. Para $k$ impar,
$\abs{\cos(2\pi j/k)} < 1$ por cada $j \neq 0$ (el ángulo es
nunca $0$ o $\pi$), por lo que todos los términos excepto $j = 0$ tienden a $0$:
$x^{(n)} \to \alpha_0 f_0$, el vector constante igual al
media --- promediar en un anillo impar iguala. Para $k = 4$ el
\omterm{def:b2:reduction:eigen}{valores propios} son $1, 0, -1, 0$: el término $j = 2$
$\alpha_2(-1)^nf_2$ con $f_2 = (1, -1, 1, -1)^{\mathsf T}$
oscila para siempre. La obstrucción es la media alterna:
multiplicando las coordenadas de $x^{(0)}$ por $(-1)^m$ y
sumando, el mismo cálculo de suma geométrica da $\sum_m
(-1)^mx^{(0)}_m = 4\alpha_2$: el proceso converge si y solo
$x^{(0)}_0 - x^{(0)}_1 + x^{(0)}_2 - x^{(0)}_3 = 0$, y luego
converge a la media.

\textbf{25.} La matriz complementaria convierte una recurrencia escalar
de orden $k$ en una recurrencia vectorial de primer orden, de modo que
las fórmulas cerradas se convierten en declaraciones sobre $C^n$ --- reducciones
terreno de origen (preguntas 1 a 5). El lema de descomposición del núcleo es
álgebra polinómica pura (Bézout más conmutación), por lo que se divide
$\ker P(S)$ aunque $\mathcal{S}$ es de dimensión infinita
(preguntas 7--9). Los dominantes \omterm{def:b2:reduction:eigen}{valores propios} gobiernan el crecimiento porque
cualquier otra contribución es geométricamente insignificante después
normalización --- que es también la razón por la que el error Pell decae en el
cuadrado de la raíz dominante (preguntas 11--14). poderes de la
el recuento de matrices de adyacencia camina porque las sumas de multiplicación de matrices
sobre vértices intermedios, por lo que los espectros cuentan recorridos cerrados
(preguntas 15--19). Las matrices de conmutación comparten una base propia, y
una base de Fourier luego diagonaliza toda el álgebra circulante
de un solo golpe (preguntas 20-24). Cumbres: lo fundamental
teorema de recurrencias lineales (pregunta 9); y para no negativo
matrices, la razón por la que las raíces dominantes como $\varphi$ o $1 +
\sqrt2$ son automáticamente reales, positivas y simples es la
Teorema de Perron-Frobenius, demostrado en el volumen del año 3.
\end{solution}