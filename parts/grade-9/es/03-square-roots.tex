\chapter{Raíces cuadradas}\label{ch:g9:sqrt}

¿Cuánto mide la diagonal de un cuadrado de lado? $1$? El teorema de Pitágoras
respuestas $\sqrt 2$, un número cuyo cuadrado es $2$ --- y cual resulta
no ser un \omterm{def:g9:fractions:fraction}{fracción}. Este capítulo define \omterm{def:g9:sqrt:def}{raíces cuadradas}, establece el
reglas para calcular con ellos y enseña cómo simplificar expresiones
me gusta $\sqrt{75}$.

\section{Definición y primeras propiedades.}

\begin{definition}[Raíz cuadrada]\label{def:g9:sqrt:def}
Sea $ a \geq 0$. El \emph{raíz cuadrada}\index{raíz cuadrada} de $ a $, escrito
$\sqrt a $, es el único \emph{no negativo} número cuyo cuadrado es $ a $:
\[
\sqrt a \geq 0
\qquad\text{y}\qquad
\left(\sqrt a\right)^2 = a .
\]
Los números negativos no tienen raíz cuadrada, ya que todo cuadrado no es negativo.
\end{definition}

\begin{example}
$\sqrt{49} = 7$, $\sqrt 0 = 0$, $\sqrt 1 = 1$,
$\sqrt{2.25} = 1.5$. La primera \omterm{def:g9:sqrt:def}{raíces cuadradas} saber de memoria son los de
el \emph{cuadrados perfectos}: $1, 4, 9, 16, 25, 36, 49, 64, 81, 100, 121,
144$.
\end{example}

\begin{omfigure}
\begin{tikzpicture}[scale=1.35]
  \draw[thick, fill=omDef!8] (0,0) rectangle (2,2);
  \draw[omThm, thick] (0,0) -- (2,2);
  \node[below, font=\small] at (1,0) {$1$};
  \node[left, font=\small] at (0,1) {$1$};
  \node[omThm, above left=-2pt, font=\small, rotate=45] at (1.08,0.92)
    {$\sqrt2$};
  \draw[thin] (1.75,0) -- (1.75,0.25) -- (2,0.25);
\end{tikzpicture}

{\small La diagonal de un cuadrado unitario tiene longitud. $\sqrt{1^2 + 1^2} =
\sqrt2 \approx 1.414$, por el teorema de Pitágoras
(\cref{ch:g9:trig} lo recuerda). Este número es irracional: sus decimales
nunca repetir.}
\end{omfigure}

\begin{proposition}[Raíz cuadrada de un cuadrado]\label{prop:g9:sqrt:square}
Por cada número real $ x $ (positivo o no):
\[
\sqrt{x^2} = \abs{x} =
\begin{cases}
x & \text{if } x \geq 0,\\
-x & \text{if } x < 0 .
\end{cases}
\]
\end{proposition}

\begin{proof}
el número $\abs{x}$ es no negativo y su cuadrado es $ x^2$ (un número y
su opuesto tiene el mismo cuadrado). Siendo el número único no negativo
de cuadrado $ x^2$, es $\sqrt{x^2}$.
\end{proof}

\begin{example}
$\sqrt{(-5)^2} = \sqrt{25} = 5 = \abs{-5}$: el \omterm{def:g9:sqrt:def}{raíz cuadrada} ``olvida''
la señal, no la restaura. En particular $\sqrt{x^2} = x $ es
\emph{equivocado} por negativo $ x $.
\end{example}

\section{Productos y cocientes de raíces cuadradas.}

\begin{theorem}[Reglas de multiplicación y división.]\label{thm:g9:sqrt:rules}
A pesar de $ a \geq 0$ y $ b \geq 0$:
\[
\sqrt{a b} = \sqrt a \times \sqrt b,
\qquad\text{and for } b > 0:\quad
\sqrt{\frac ab} = \frac{\sqrt a}{\sqrt b} .
\]
\end{theorem}

\begin{proof}
el número $\sqrt a \times \sqrt b $ no es negativo (\omterm{def:g2:mult:def}{producto} de
números no negativos), y su cuadrado es
\[
\left(\sqrt a \times \sqrt b\right)^2
= \left(\sqrt a\right)^2 \times \left(\sqrt b\right)^2
= a b .
\]
Por la unicidad del número no negativo cuyo cuadrado es $ ab $, es igual
$\sqrt{ab}$. El \omterm{def:g3:division:remainder}{cociente} La regla se demuestra de la misma manera.
\end{proof}

\begin{remark}[¡No existe tal regla para las sumas!]
$\sqrt{a + b}$ es \emph{no} $\sqrt a + \sqrt b $ en general:
\[
\sqrt{9 + 16} = \sqrt{25} = 5,
\qquad\text{but}\qquad
\sqrt 9 + \sqrt{16} = 3 + 4 = 7 .
\]
\end{remark}

\begin{method}[Simplificando $\sqrt n $]\label{met:g9:sqrt:simplify}
Para simplificar el \omterm{def:g9:sqrt:def}{raíz cuadrada} de un número entero:
\begin{enumerate}
  \item encontrar el cuadrado perfecto más grande dividiendo $ n $ (factor $ n $ si
        necesario);
  \item dividir con el \omterm{def:g2:mult:def}{producto} regla y extrae el cuadrado:
        $\sqrt{k^2 m} = k\sqrt m $;
  \item comprueba que no hay un cuadrado perfecto \omterm{def:g9:arith:divisor}{divide} lo que queda debajo del
        raíz.
\end{enumerate}
\end{method}

\begin{example}\label{ex:g9:sqrt:simplify}
Simplificar $\sqrt{75}$: desde $75 = 25 \times 3$,
\[
\sqrt{75} = \sqrt{25 \times 3} = \sqrt{25} \times \sqrt 3 = 5\sqrt3 .
\]
Simplificar $\sqrt{72}$: desde $72 = 36 \times 2$,
$\sqrt{72} = 6\sqrt2$. y un \omterm{def:g3:division:remainder}{cociente}:
$\sqrt{\dfrac{49}{16}} = \dfrac{\sqrt{49}}{\sqrt{16}} = \dfrac74$.
\end{example}

\begin{example}[Sumar raíces cuadradas]\label{ex:g9:sqrt:add}
\omterm{def:g1:addition:def}{sumas} de \omterm{def:g9:sqrt:def}{raíces cuadradas} simplificar sólo cuando las raíces son \emph{similar}. Calcular $\sqrt{12} + \sqrt{27} - \sqrt{48}$, simplificando cada término primero:
\begin{align*}
\sqrt{12} &= \sqrt{4 \times 3} = 2\sqrt3, \\
\sqrt{27} &= \sqrt{9 \times 3} = 3\sqrt3, \\
\sqrt{48} &= \sqrt{16 \times 3} = 4\sqrt3,
\end{align*}
entonces el \omterm{def:g1:addition:def}{suma} es $2\sqrt3 + 3\sqrt3 - 4\sqrt3 = (2 + 3 - 4)\sqrt3 = \sqrt3$.
\end{example}

\begin{example}[Expandiendo con raíces cuadradas]\label{ex:g9:sqrt:expand}
Las identidades del álgebra se aplican a \omterm{def:g9:sqrt:def}{raíces cuadradas}. Expandir
$\left(\sqrt5 + 2\right)^2$ con $(a+b)^2 = a^2 + 2ab + b^2$:
\[
\left(\sqrt5 + 2\right)^2 = 5 + 2 \times 2\sqrt5 + 4 = 9 + 4\sqrt5 .
\]
Y con la tercera identidad, $(a+b)(a-b) = a^2 - b^2$:
\[
\left(\sqrt7 + \sqrt3\right)\left(\sqrt7 - \sqrt3\right) = 7 - 3 = 4 :
\]
el \omterm{def:g2:mult:def}{producto} de dos números irracionales puede ser un número entero.
\end{example}

\section{La ecuacion \texorpdfstring{$ x^2 = a $}{x2 = a}}

\begin{theorem}[resolviendo $ x^2 = a $]\label{thm:g9:sqrt:equation}
\begin{enumerate}
  \item Si $ a > 0$, el \omterm{def:g8:equations:def}{ecuación} $ x^2 = a $ tiene exactamente dos soluciones:
        $\sqrt a $ y $-\sqrt a $;
  \item si $ a = 0$, la única solución es $0$;
  \item si $ a < 0$, no hay solución.
\end{enumerate}
\end{theorem}

\begin{proof}
Volver a escribir $ x^2 = a $ como $ x^2 - \left(\sqrt a\right)^2 = 0$ cuando $ a \geq 0$,
y factorizar como \omterm{ex:g1:subtraction:difference}{diferencia} de cuadrados:
\[
\left(x - \sqrt a\right)\left(x + \sqrt a\right) = 0 ,
\]
entonces $ x = \sqrt a $ o $ x = -\sqrt a $ (estos coinciden cuando $ a = 0$). Cuando
$ a < 0$ no hay solución, ya que $ x^2 \geq 0 > a $ por cada $ x $.
\end{proof}

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[omaxis,
  width=7.6cm, height=5.4cm,
  xmin=-3.1, xmax=3.1, ymin=-1.6, ymax=5.4,
  xtick={-1.732,1.732}, xticklabels={$-\sqrt3$,$\sqrt3$},
  ytick={3}, yticklabels={$3$}]
  \addplot[omDef, thick, domain=-2.25:2.25, samples=90] {x^2};
  \addplot[omThm, thick, domain=-2.9:2.9] {3};
  \fill (axis cs:-1.732,3) circle (1.5pt);
  \fill (axis cs:1.732,3) circle (1.5pt);
  \draw[dashed] (axis cs:-1.732,3) -- (axis cs:-1.732,0);
  \draw[dashed] (axis cs:1.732,3) -- (axis cs:1.732,0);
\end{axis}
\end{tikzpicture}

{\small resolviendo $ x^2 = 3$ en la parábola $ y = x^2$: la línea horizontal
$ y = 3$ lo corta en las dos abscisas $\pm\sqrt3$.}
\end{omfigure}

\begin{example}
$ x^2 = 16$: soluciones $4$ y $-4$. \quad
$ x^2 = 5$: soluciones $\sqrt5$ y $-\sqrt5$ (valores exactos; $\sqrt5
\approx 2.236$). \quad
$ x^2 = -9$: sin solución. \quad
$3x^2 = 21$: dividir por 3 primero, $ x^2 = 7$, soluciones $\pm\sqrt7$.
\end{example}

\section{Ejercicios}

\begin{exercise}[$\star $]\label{exo:g9:sqrt:1}
Calcular sin calculadora:
\[
\sqrt{64}, \qquad \sqrt{100}, \qquad \sqrt{0.09}, \qquad
\sqrt{\tfrac{1}{25}}, \qquad \left(\sqrt{13}\right)^2, \qquad
\sqrt{(-4)^2} .
\]
\end{exercise}

\begin{exercise}[$\star $]\label{exo:g9:sqrt:2}
Simplificar:
\[
\sqrt{50}, \qquad \sqrt{45}, \qquad \sqrt{98}, \qquad \sqrt{300} .
\]
\end{exercise}

\begin{exercise}[$\star $]\label{exo:g9:sqrt:3}
Calcula y simplifica:
\[
\sqrt2 \times \sqrt{18}, \qquad
\frac{\sqrt{75}}{\sqrt3}, \qquad
\sqrt5 \times \sqrt{20}, \qquad
\frac{\sqrt{8}}{\sqrt{50}} .
\]
\end{exercise}

\begin{exercise}[$\star $]\label{exo:g9:sqrt:4}
Resolver: $ x^2 = 36$; \quad $ x^2 = 11$; \quad $ x^2 + 4 = 0$; \quad
$2x^2 = 50$.
\end{exercise}

\begin{exercise}[$\star $]\label{exo:g9:sqrt:5}
Reducir a un solo término: $\sqrt{20} + \sqrt{45}$, entonces
$3\sqrt8 - \sqrt{18} + \sqrt2$.
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g9:sqrt:6}
Ampliar y simplificar:
\[
\left(\sqrt3 + 1\right)^2, \qquad
\left(2\sqrt5 - 3\right)^2, \qquad
\left(\sqrt6 + \sqrt2\right)\left(\sqrt6 - \sqrt2\right).
\]
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g9:sqrt:7}
Un campo cuadrado tiene \omterm{def:g6:measure:area}{área} $6400$ metro $^2$. ¿Cuál es la longitud de su lado? de
¿Su diagonal (valor exacto, luego redondeado al metro más cercano)?
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g9:sqrt:8}
mostrar eso $\dfrac{1}{\sqrt2} = \dfrac{\sqrt2}{2}$ (multiplicar \omterm{def:g4:fractions:def}{numerador} y
\omterm{def:g4:fractions:def}{denominador} por $\sqrt2$). Usa el mismo truco para escribir. $\dfrac{6}{\sqrt3}$
y $\dfrac{10}{\sqrt5}$ sin un \omterm{def:g9:sqrt:def}{raíz cuadrada} en el \omterm{def:g4:fractions:def}{denominador}.
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g9:sqrt:9}
¿Verdadero o falso? Justifique con una prueba o un contraejemplo.
\begin{enumerate}
  \item A pesar de $ a, b \geq 0$: $\sqrt{ab} = \sqrt a \sqrt b $.
  \item A pesar de $ a, b \geq 0$: $\sqrt{a+b} = \sqrt a + \sqrt b $.
  \item A pesar de $ x $: $\sqrt{x^2} = x $.
  \item $\left(3\sqrt2\right)^2 = 18$.
\end{enumerate}
\end{exercise}

\begin{exercise}[$\star\star\star $]\label{exo:g9:sqrt:10}
Sea $ x = \sqrt{7 + 4\sqrt3}$. Calcular
$\left(2 + \sqrt3\right)^2$, y deducir una expresión más simple de $ x $.
La misma pregunta para $\sqrt{9 - 4\sqrt5}$ (apunta a un cuadrado de la forma
$\left(\sqrt5 - 2\right)^2$, y tenga cuidado con la señal).
\end{exercise}

\section{Problema: El número que no es una fracción.}

\begin{problem}[{Problema de fin de semana --- la prueba legendaria de que $\sqrt2$ es irracional, sus muchos primos y la increíblemente buena receta de Heron para aproximarlo}]
\label{pb:g9:sqrt:1}
La diagonal de un cuadrado unitario es una longitud perfectamente real.
lo dibujó \cref{ex:g8:pythagoras:diagonal} --- y sin embargo, como el
Los pitagóricos descubrieron con horror unos veinticinco
Hace siglos, \emph{No \omterm{def:g9:fractions:fraction}{fracción} lo que} lo mide. Leyenda
Afirma que el descubrimiento fue castigado con ahogamiento. Este problema camina
a través de la prueba inmortal (cuatro preguntas y es tuya)
de por vida), multiplica las víctimas, y termina con la receta
Los ingenieros utilizaron durante dos mil años para domar lo indomable.
número.

\medskip
\noindent\textbf{Parte I --- The proof.}
\begin{enumerate}
  \item Primero cazarlo: comprueba que
        $1.4 < \sqrt2 < 1.5$, entonces eso $1.41 < \sqrt2 < 1.42$,
        elevando al cuadrado los límites. Un dígito más: entre los cuales
        los números tres decimales sí $\sqrt2$ ¿mentir?
  \item Ahora supongamos, en aras de la contradicción, que
        $\sqrt2 = \frac ab $ para algunos \omterm{def:g9:fractions:fraction}{fracción} \emph{en el más bajo
        términos} (\cref{met:g9:arith:simplify}). Cuadrar ambos lados
        y demostrar que $ a^2 = 2 b^2$. ¿Cuál es la paridad de
        $ a^2$?
  \item Los hechos de paridad de \cref{pb:g8:pythagoras:1} decir: \omterm{def:g3:numbers:evenodd}{impar
números} tener \omterm{def:g3:numbers:evenodd}{extraño} cuadrícula. deducir eso $ a $ es \omterm{def:g3:numbers:evenodd}{incluso}, escribir
        $ a = 2k $, sustituye --- y demuestra que $ b $ debe ser \omterm{def:g3:numbers:evenodd}{incluso}
        también.
  \item ¿Dónde está la contradicción? Concluya y exponga la
        teorema completo: \emph{$\sqrt2$ es irracional --- es
        no \omterm{def:g3:division:remainder}{cociente} de números enteros}.
  \item La prueba se apoyó una vez, discretamente, en las palabras "en
        términos más bajos". Señale el paso exacto que
        colapsar sin ellos, y explicar por qué asumir el nivel más bajo
        términos eran legítimos en primer lugar.
\end{enumerate}

\medskip
\noindent\textbf{Parte II --- The victims multiply.}
\begin{enumerate}[resume]
  \item Un segundo estilo de prueba, mediante factorizaciones primas.
        (\cref{thm:g9:arith:factorization}): en $ a^2 = 3b^2$,
        comparar la paridad de \emph{\omterm{def:g8:powers:def}{exponente} de $3$} en cada uno
        lado (\cref{pb:g9:arith:1}: cuadrados llevar \omterm{def:g3:numbers:evenodd}{incluso}
        \omterm{def:g8:powers:def}{exponentes}). Concluir que $\sqrt3$ es irracional.
  \item ejecuta lo mismo \omterm{def:g8:powers:def}{exponente} argumento sobre $ a^2 = n b^2$ por un
        número entero general $ n $: para el cual $ n $ ¿Produce?
        una contradicción, y ¿en qué falla? Indique el
        resultado completo: $\sqrt n $ es irracional exactamente
        cuando\,\dots
  \item Demuestre el lema del escudo: un racional distinto de cero multiplicado por un
        irracional es irracional. (Suponer $ r x = q $ con $ r, q $
        racional, $ r \neq 0$y resolver para $ x $.) Deducir que
        $2\sqrt2$ y $\frac{\sqrt2}{2}$ son irracionales.
  \item demostrar que $1 + \sqrt2$ es irracional. entonces la linda
        uno: suponiendo $ s = \sqrt2 + \sqrt3$ eran racionales,
        calcular $ s^2$, aislar $\sqrt6$, y encontrar el
        contradicción.
  \item Templar el entusiasmo: dar dos números irracionales cuyo
        \emph{\omterm{def:g1:addition:def}{suma}} es racional, y dos cuyo \emph{\omterm{def:g2:mult:def}{producto}} es
        racional. (La irracionalidad no se preserva mediante la aritmética.
        --- cada caso necesita su propia prueba.)
\end{enumerate}

\medskip
\noindent\textbf{Parte III --- Heron's recipe.}
Dos mil años antes de los calculadores, Garza de Alejandría
aproximado $\sqrt2$ como esto: \emph{adivinar $ x $; reemplazar el
adivinar por el promedio de $ x $ y $\frac2x $; repetir.}
\begin{enumerate}[resume]
  \item Empezar desde la suposición $ x = 1$ y calcular los dos siguientes
        conjeturas tan exactas \omterm{def:g9:fractions:fraction}{fracciones}.
  \item Calcule la tercera suposición, nuevamente como una cifra exacta. \omterm{def:g9:fractions:fraction}{fracción}, y
        su valor decimal. Comparar con la pregunta 1: ¿cuántos
        decimales de $\sqrt2$ ¿Ya son correctos?
  \item La idea detrás de la receta: un rectángulo de \omterm{def:g6:measure:area}{área} $2$ con
        un lado $ x $ tiene el otro lado $\frac2x $. mostrar eso
        $\sqrt2$ siempre miente \emph{entre} $ x $ y $\frac2x $
        (considere los dos casos $ x^2 > 2$ y $ x^2 < 2$), entonces
        promediar los dos lados aprieta el rectángulo hacia
        la plaza.
  \item las conjeturas $\frac32$, $\frac{17}{12}$,
        $\frac{577}{408}$ esconder una joya: calcular $3^2 - 2 \times
        2^2$, entonces $17^2 - 2 \times 12^2$, entonces
        $577^2 - 2 \times 408^2$. Parte que probé
        $ a^2 - 2b^2 = 0$ imposible --- ¿qué tan cerca están los de Heron?
        \omterm{def:g9:fractions:fraction}{fracciones} llegar a lo imposible?
  \item Final, en dos frases: la diagonal de la unidad.
        El cuadrado existe en el papel, no. \omterm{def:g9:fractions:fraction}{fracción} lo mide, y su
        la escritura decimal nunca puede repetirse
        (\cref{pb:g9:fractions:1}). ¿Qué clase de "nuevos números"
        Por lo tanto, la recta numérica debe contener --- y en qué lugar
        ¿En esta serie se cuenta su historia completa?
\end{enumerate}
\end{problem}
