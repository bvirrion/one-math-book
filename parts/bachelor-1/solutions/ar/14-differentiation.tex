\section*{الفصل \ref{ch:b1:derivative} --- الاشتقاق}

\begin{solution}{exo:b1:derivative:1}
$x^x = \eu^{x\ln x}$ على $\intoo{0}{+\infty}$: \omterm{def:b1:derivative:def}{المشتقة} $(\ln x +
1)\,x^x$.

$\ln(x + \sqrt{x^2+1})$ على $\R$ (لأن المتغير دائمًا $> 0$):
\omterm{def:b1:derivative:def}{المشتقة} $\frac{1}{\sqrt{x^2+1}}$ (محسوبة في
\cref{prop:b1:functions:invhyp} --- وهي
$\operatorname{arsinh}$).

$\arctan\frac1x$ على $\R^*$: \omterm{def:b1:derivative:def}{المشتقة} $\frac{-1/x^2}{1 + 1/x^2} =
\frac{-1}{1 + x^2}$ (وهذا متسق مع
\cref{prop:b1:functions:arcidentities}~(2): فالدالة
$\pm\frac\pi2 - \arctan x$ على كل نصف مستقيم).

$\sqrt{1 + \eu^{2x}}$ على $\R$: \omterm{def:b1:derivative:def}{المشتقة}
$\frac{\eu^{2x}}{\sqrt{1 + \eu^{2x}}}$.
\end{solution}

\begin{solution}{exo:b1:derivative:2}
عند $0$: $\bigl|\frac{f(h) - 0}{h}\bigr| = \abs{h \sin\frac1h} \leq
\abs h \to 0$، ومنه $f'(0) = 0$. ومن أجل $x \neq 0$، تعطي القواعد المعتادة
$f'(x) = 2x\sin\frac1x - \cos\frac1x$. وعلى $x_n = \frac{1}{2\pi
n}$: $f'(x_n) = 0 - 1 \to -1$؛ وعلى $y_n = \frac{1}{(2n+1)\pi}$:
$f'(y_n) = 0 + 1 \to 1$. أي متتاليتان تؤولان إلى $0$ بنهايتين مختلفتين
للمقدار $f'$: فلا نهاية (\cref{thm:b1:continuity:seqchar})، ومنه \omterm{def:b1:derivative:def}{فالمشتقة} $f'$
ليست \omterm{def:b1:continuity:continuous}{متصلة} عند $0$ وتكون $f$ \omterm{def:b1:derivative:def}{قابلة للاشتقاق} دون أن تكون من الصنف
$C^1$.
\end{solution}

\begin{solution}{exo:b1:derivative:3}
$\ln(1+x) < x$ من أجل $x > 0$: بمتراجحة مماس التقعّر عند $0$
(وهي قطعية بعيدًا عن نقطة التماسّ لأن $\ln$ مقعّرة قطعًا؛
أو طبّق مبرهنة التزايدات المنتهية: $\ln(1+x) = \frac{x}{1+c}$ من أجل
$c \in \intoo{0}{x}$ ما، و $\frac{x}{1+c} < x$). وتعطي متطابقة التزايدات
المنتهية نفسها الحاصرَ الأدنى: $\frac{x}{1+c} > \frac{x}{1+x}$.

والنتيجة: مع $x/n$ بدل $x$،
\[
\frac{x/n}{1 + x/n} < \ln\Bigl(1 + \frac xn\Bigr) < \frac xn
\quad\implies\quad
\frac{x}{1 + x/n} < n \ln\Bigl(1 + \frac xn\Bigr) < x .
\]
ويؤول الطرف الأيسر إلى $x$: ومنه، بالحصر، $n\ln(1 + \frac xn)
\to x$، وباتصال $\exp$، $\bigl(1 + \frac xn\bigr)^n =
\eu^{n\ln(1 + x/n)} \to \eu^x$.
\end{solution}

\begin{solution}{exo:b1:derivative:4}
ليكن $x_1 < x_2 < \dots < x_k$ جذورًا متمايزة للمقدار $P$. وعلى كل
$\intcc{x_i}{x_{i+1}}$، تنتج مبرهنة رول (\cref{thm:b1:derivative:rolle})
عددًا $c_i \in \intoo{x_i}{x_{i+1}}$ يحقق $P'(c_i) = 0$: أي
$k - 1$ جذرًا للمقدار $P'$، متمايزة لأن الفترات \omterm{def:b1:topology:open}{المفتوحة}
منفصلة --- ومتداخلة بحكم البناء.

وإذا كانت كل جذور $P$ (من الدرجة $n$) حقيقية، فاكتبها مع
تضاعفاتها $m_1 + \dots + m_k = n$. وكل جذر تضاعفه
$m_i \geq 2$ جذرٌ للمقدار $P'$ تضاعفه $m_i - 1$
(\cref{prop:b1:poly:multiplicity})، فيسهم بالمقدار $\sum (m_i - 1) = n
- k$؛ وتسهم رول بعدد $k - 1$ آخر، وكلها متمايزة عن هذه. والمجموع
$\geq n - 1 = \deg P'$: ومنه فكل جذور $P'$ حقيقية.
\end{solution}

\begin{solution}{exo:b1:derivative:5}
ثبّت $\varepsilon > 0$ و $A$ يحقق $\abs{f'(t) - \ell} \leq
\varepsilon$ من أجل $t \geq A$. ومن أجل $x > A$، تعطي مبرهنة التزايدات المنتهية على
$\intcc{A}{x}$ عددًا $c \in \intoo{A}{x}$ يحقق
\[
f(x) = f(A) + f'(c)(x - A),
\qquad\text{ومنه}\qquad
\Bigl|\frac{f(x)}{x} - \ell\Bigr|
\leq \frac{\abs{f(A)} + \abs\ell A}{x} + \abs{f'(c) - \ell}
\cdot\frac{x - A}{x} \leq \frac{C_A}{x} + \varepsilon .
\]
ومن أجل $x$ كبير، $\frac{C_A}{x} \leq \varepsilon$: ومنه
$\frac{f(x)}{x} \to \ell$.

نعم: $f(x+1) - f(x) = f'(c_x)$ مع $c_x \in \intoo{x}{x+1}$ (بمبرهنة
التزايدات المنتهية على $\intcc{x}{x+1}$)، و $c_x \to +\infty$، ومنه
$f(x+1) - f(x) \to \ell$.
\end{solution}

\begin{solution}{exo:b1:derivative:6}
بالاستقراء على $n$. من أجل $n = 1$: رول. وإذا صحّ الادعاء من أجل $n - 1$:
فإن $f$ تنعدم عند $n+1$ نقطة، ومنه، برول مطبَّقة على الفجوات $n$،
تنعدم $f'$ عند $n$ نقطة متمايزة؛ ويجعل فرض الاستقراء
مطبَّقًا على $f'$ (وهي \omterm{def:b1:derivative:def}{قابلة للاشتقاق} $n-1$ مرة ولها $n$ جذرًا)
$(f')^{(n-1)} = f^{(n)}$ ينعدم في موضع ما.

وتطبيقًا: إذا انعدم $P$ من الدرجة $\leq n$ عند $n+1$ نقطة، فإن
$P^{(n)}$، وهو ثابت يساوي $n!$ مضروبًا في المعامل المهيمن،
ينعدم: ومنه فالمعامل المهيمن هو $0$، ويُختم باستقراء
نازل (أو مباشرةً: تنعدم كل المعاملات).
\end{solution}

\begin{solution}{exo:b1:derivative:7}
بمبرهنة التزايدات المنتهية على $\intcc{a}{x_0}$ وعلى
$\intcc{x_0}{b}$:
\[
f'(c_1) = \frac{f(x_0) - f(a)}{x_0 - a} = \frac{f(x_0)}{x_0 - a} > 0,
\qquad
f'(c_2) = \frac{f(b) - f(x_0)}{b - x_0} = \frac{-f(x_0)}{b - x_0} < 0,
\]
مع $c_1 < x_0 < c_2$. ثم تعطي مبرهنة التزايدات المنتهية مطبَّقةً على $f'$
على $\intcc{c_1}{c_2}$ عددًا $c$ يحقق
\[
f''(c) = \frac{f'(c_2) - f'(c_1)}{c_2 - c_1} < 0 . \qedhere
\]
\end{solution}

\begin{solution}{exo:b1:derivative:8}
$f'(x) = \frac{1 - \ln x}{x^2}$: تتزايد $f$ على $\intoc{0}{\eu}$، و
تتناقص على $\intco{\eu}{+\infty}$، بقيمة عظمى $f(\eu) =
\frac1\eu$؛ والنهايتان $-\infty$ عند $0^+$ و $0$ عند $+\infty$ (\omterm{prop:b1:functions:powerrules}{بمقارنة
النمو}).

ومن أجل $\eu \leq a < b$: يعطي تناقص $f$ القطعي هناك أن
$\frac{\ln a}{a} > \frac{\ln b}{b}$، أي $b \ln a > a \ln b$،
أي $a^b > b^a$.

ومع $a = \eu < b = \pi$: $\eu^\pi > \pi^\eu$.

والأزواج الصغيرة: $(2, 3)$: $2^3 = 8 < 9 = 3^2$ --- أي معكوسًا! والسبب:
$2 < \eu$، وعلى $\intoo{0}{\eu}$ تكون الدالة $f$
\emph{متزايدة}، ومنه تنقلب المقارنة حين يقع العددان
دون $\eu$، وتكون غير قابلة للتنبؤ عبر $\eu$ (ويفسّر $f(2) = f(4)$
التعادلَ $2^4 = 4^2 = 16$).
\end{solution}

\begin{solution}{exo:b1:derivative:9}
\emph{ينسن من أجل $\ln$، بالاستقراء على $n$.} الادعاء: من أجل
$x_i$ موجبة وأوزان $\lambda_i > 0$ مع $\sum \lambda_i = 1$:
$\ln\bigl(\sum \lambda_i x_i\bigr) \geq \sum \lambda_i \ln x_i$. ومن أجل
$n = 2$ هذا هو التقعّر. والخطوة: مع $\Lambda = \lambda_1 + \dots +
\lambda_{n-1} = 1 - \lambda_n$ و $y = \sum_{i<n}
\frac{\lambda_i}{\Lambda} x_i$،
\[
\ln\Bigl(\sum_{i \leq n} \lambda_i x_i\Bigr)
= \ln\bigl(\Lambda y + \lambda_n x_n\bigr)
\geq \Lambda \ln y + \lambda_n \ln x_n
\geq \Lambda \sum_{i<n} \frac{\lambda_i}{\Lambda}\ln x_i
+ \lambda_n \ln x_n,
\]
باستعمال التقعّر ($n = 2$) ثم فرض الاستقراء.

ومع $\lambda_i = \frac 1n$ و $x_i = a_i$: $\ln\frac{\sum a_i}{n}
\geq \frac 1n \sum \ln a_i = \ln\sqrt[n]{a_1\cdots a_n}$؛
ثم خذ الأسّي. وأمّا المساواة: فالدالة $\ln$ مقعّرة \emph{قطعًا} ($\ln'' < 0$)،
ومنه تفرض المساواة عند كل خطوة تطابقَ النقاط الممتوسطة ---
أي تساوي كل $a_i$؛ وإذا تساوت كلها، فالمساواة جلية.
\end{solution}

\begin{solution}{exo:b1:derivative:10}
لتكن $g(x) = f(x) - vx$: فهي \omterm{def:b1:derivative:def}{قابلة للاشتقاق}، مع $g'(a) = f'(a) - v < 0$
و $g'(b) = f'(b) - v > 0$. وحسب مبرهنة القيم الحدّية، تبلغ $g$
قيمتها الصغرى على $\intcc{a}{b}$ عند $c$ ما. وهي ليست عند $a$:
لأن $g'(a) < 0$، فللنقاط يمين $a$ مباشرةً $g < g(a)$. وهي
ليست عند $b$: لأن $g'(b) > 0$، فللنقاط يسار $b$ مباشرةً $g <
g(b)$. ومنه فالنقطة $c$ داخلية، ويعطي \cref{prop:b1:derivative:fermat}
أن $g'(c) = 0$، أي $f'(c) = v$.
\end{solution}

\begin{solution}{exo:b1:derivative:11}
\emph{الوحدانية:} نقطتان صامدتان $\ell \neq \ell'$ ستعطيان
$\abs{\ell - \ell'} = \abs{f(\ell) - f(\ell')} \leq k\abs{\ell -
\ell'} < \abs{\ell - \ell'}$، وهذا محال.

\emph{الوجود:} تحقق $g(x) = f(x) - x$، بمتراجحة التزايدات
المنتهية، $f(x) \leq f(0) + k\abs x$؛ ومنه من أجل $x \geq
\frac{\abs{f(0)}}{1 - k}$، $g(x) \leq f(0) + kx - x \leq 0$، و
بالتناظر $g(-x) \geq 0$ من أجل $x$ كبير. وتعطي مبرهنة القيم
الوسطى جذرًا $\ell$ للمقدار $g$: أي نقطة صامدة.

\emph{التقارب:} بمتراجحة التزايدات المنتهية مرة أخرى:
\[
\abs{u_{n+1} - \ell} = \abs{f(u_n) - f(\ell)} \leq k\abs{u_n - \ell},
\]
ومنه بالاستقراء $\abs{u_n - \ell} \leq k^n \abs{u_0 - \ell} \to 0$.
\end{solution}

\begin{solution}{exo:b1:derivative:12}
\begin{enumerate}
  \item إذا كان $g(b) = g(a)$، لأعطت رول جذرًا داخليًا للمقدار
        $g'$: وهذا مستبعد. ضع $\lambda = \frac{f(b) - f(a)}{g(b) -
        g(a)}$ و $h = f - \lambda g$: فالدالة $h$ \omterm{def:b1:continuity:continuous}{متصلة} على
        $\intcc{a}{b}$ \omterm{def:b1:derivative:def}{وقابلة للاشتقاق} بالداخل، و $h(b) - h(a)
        = f(b) - f(a) - \lambda(g(b) - g(a)) = 0$. وتعطي رول
        عددًا $c$ يحقق $h'(c) = 0$، أي $f'(c) =
        \lambda\,g'(c)$؛ فاقسم على $g'(c) \neq 0$.
  \item من أجل $x > a$ قريب من $a$، يعطي البند (1) على $\intcc{a}{x}$
        (حيث $g' \neq 0$) أن $g(x) \neq 0$ وأن $c_x \in
        \intoo{a}{x}$ يحقق
        \[
        \frac{f(x)}{g(x)} = \frac{f(x) - f(a)}{g(x) - g(a)}
        = \frac{f'(c_x)}{g'(c_x)} .
        \]
        وعندما $x \to a^+$، يكون $c_x \to a^+$ (بالحصر)، ومنه يؤول الطرف
        الأيمن إلى $\ell$: أي $\frac{f}{g} \to \ell$.
  \item $\frac{f(x)}{g(x)} = x\sin\frac1x \to 0$، بينما
        $\frac{f'(x)}{g'(x)} = 2x\sin\frac1x - \cos\frac1x$ لا
        نهاية له عند $0$ (\cref{exo:b1:derivative:2}):
        فقاعدة لوبيتال لا تنقل المعلومة إلا من
        $\frac{f'}{g'}$ إلى $\frac fg$، لا بالعكس أبدًا.
\end{enumerate}
\end{solution}

\begin{solution}{pb:b1:derivative:1}
\textbf{1.} $\bigl|\frac ab - \frac pq\bigr| = \frac{\abs{aq -
bp}}{bq}$، و $aq - bp$ عدد صحيح غير معدوم عندما يختلف
الكسران: ومنه فالمسافة $\geq \frac{1}{bq}$. ومنه فلا عدد ناطق
غير $x$ نفسه يدخل \omterm{prop:b1:reals:intervals}{الفترة} المثقوبة ذات نصف القطر
$\frac{1}{bq}$ حول $x = \frac ab$.

\textbf{2.} إذا كان $\bigl|\sqrt2 - \frac pq\bigr| \geq 1 \geq
\frac{1}{4q^2}$، فقد انتهينا. وإلا فإن $\frac pq \in \intoo{\sqrt2 -
1}{\sqrt2 + 1}$، ومنه $0 < \sqrt2 + \frac pq < 2\sqrt2 + 1 < 4$.
ولأن $\sqrt 2 \notin \Q$، يكون $p^2 - 2q^2$ عددًا صحيحًا غير معدوم، و
\[
\Bigl|\sqrt2 - \frac pq\Bigr|
= \frac{\abs{2q^2 - p^2}}{q^2\,\bigl(\sqrt2 + \frac pq\bigr)}
\geq \frac{1}{4q^2} .
\]

\textbf{3.} $(p + 2q)^2 - 2(p + q)^2 = -(p^2 - 2q^2)$: فتنتشر القيمة
$\pm1$. وانطلاقًا من $(1,1)$:
\[
(3,2),\ (7,5),\ (17,12),\ (41,29),\ (99,70),
\]
مع تناوب $p^2 - 2q^2$ بين $-1, +1, \dots$
ومن أجل هذه، $\frac pq \geq 1$، ومنه $\sqrt2 + \frac pq > 2$ و
\[
\Bigl|\sqrt2 - \frac pq\Bigr| =
\frac{1}{q^2(\sqrt2 + p/q)} < \frac{1}{2q^2} ,
\]
مع $q \to \infty$: أي تقريبات من الرتبة $2$ لا تُحصى.
ومع السؤال 2، يكون الأُسّ $2$ مضبوطًا من أجل $\sqrt 2$.

\textbf{4.} الأعداد $N + 1$ $kx - \lfloor kx\rfloor$ ($0
\leq k \leq N$) تقع في الأدراج $N$ $\intco{\frac
jN}{\frac{j+1}{N}}$: فيتشارك اثنان درجًا
(\cref{cor:b1:counting:pigeonhole})، ولنقل من أجل $i < j$. ومع $q =
j - i \leq N$ و $p = \lfloor jx\rfloor - \lfloor ix\rfloor$:
$\abs{qx - p} < \frac1N$، ومنه $\bigl|x - \frac pq\bigr| <
\frac{1}{Nq} \leq \frac{1}{q^2}$. وبجعل $N \to \infty$: ولأن
$x$ أصمّ، فلكل كسر مثبَّت مسافة موجبة إلى
$x$، بينما يفرض $\frac{1}{Nq} \leq \frac 1N \to 0$ ظهور كسور
جديدة: ومنه أعداد ناطقة متمايزة $\frac pq$ لا تُحصى تحقق
$\bigl|x - \frac pq\bigr| < \frac{1}{q^2}$.

\textbf{5.} ابدأ من أيّ \omterm{def:b1:poly:def}{كثير حدود} صحيح غير معدوم $P_0$ يحقق $P_0(x) =
0$. فإذا كان للمقدار $P_0$ جذر ناطق $\frac ab$، فإن مبرهنة العامل
(\cref{thm:b1:poly:factor}) تكتب $P_0 = \bigl(X - \frac
ab\bigr)Q$ مع $Q \in \Q[X]$؛
ولأن $x \neq \frac ab$ (فالعدد $x$ أصمّ)، يكون $Q(x) = 0$، و
تعطي إزالة المقامات \omterm{def:b1:poly:def}{كثير حدود} \emph{صحيحًا} غير معدوم
من درجة أصغر ينعدم عند $x$. وتهبط الدرجة عند كل
خطوة، ومنه تتوقف العملية: فنبلغ $P \in \Z[X]$ مع $P(x) = 0$،
بلا جذر ناطق، من درجة $d$ ما. وإذا كان $d \leq 1$، لجعل $P =
uX + v$ العددَ $x = -\frac vu$ ناطقًا: ومنه $d \geq 2$.

\textbf{6.} المقدار $q^d\,P\bigl(\frac pq\bigr) = a_d p^d + a_{d-1}
p^{d-1} q + \dots + a_0 q^d$ عدد صحيح، وهو غير معدوم
لأن $P$ ليس له جذر ناطق: $\bigl|P\bigl(\frac
pq\bigr)\bigr| \geq q^{-d}$.

\textbf{7.} ولاحظ $M > 0$: فالمقدار $P'$ \omterm{def:b1:poly:def}{كثير حدود} غير معدوم ($d \geq
2$)، ومنه لا يمكن أن ينعدم تمامًا على
$\intcc{x-1}{x+1}$. فإذا كان $\bigl|x - \frac pq\bigr| > 1$، فإنه
يتجاوز $\frac{C}{q^d}$ بداهةً. وإلا فإن $\frac pq \in
\intcc{x-1}{x+1}$ وتعطي مبرهنة التزايدات المنتهية
(\cref{thm:b1:derivative:mvt}) عددًا $c$ بين $x$ و
$\frac pq$ يحقق
\[
\Bigl|P\Bigl(\frac pq\Bigr)\Bigr|
= \Bigl|P\Bigl(\frac pq\Bigr) - P(x)\Bigr|
= \abs{P'(c)}\,\Bigl|x - \frac pq\Bigr|
\leq M\,\Bigl|x - \frac pq\Bigr| ,
\]
ومنه، مع السؤال 6: $\bigl|x - \frac pq\bigr| \geq
\frac{1}{Mq^d} \geq \frac{C}{q^d}$.

\textbf{8.} افترض أن $x = \frac ab$ عدد ليوفيل. اختر $n$ يحقق
$2^{n-1} > b$ والكسرَ المقابل $\frac pq$ مع $q \geq 2$:
\[
0 < \Bigl|x - \frac pq\Bigr| < \frac{1}{q^n}
= \frac{1}{q^{n-1}\,q} \leq \frac{1}{2^{n-1} q} <
\frac{1}{bq} ,
\]
وهذا يناقض السؤال 1. ومنه فأعداد ليوفيل صمّاء.

\textbf{9.} لو كان عدد ليوفيل $x$ جبريًا: فهو أصمّ
(السؤال 8)، ومنه تعطي الأسئلة 5--7 عددين $d \geq 2$ و $C > 0$
يحققان $\bigl|x - \frac pq\bigr| \geq \frac{C}{q^d}$ دائمًا. ومن أجل
كل $n$، يعطي مقرّب ليوفيل أن $\frac{C}{q^d} <
q^{-n}$، أي $C < q^{d-n} \leq 2^{d-n}$ (لأن $q \geq 2$). ومن أجل
$n$ كبير، $2^{d-n} < C$: وهذا تناقض. ومنه فأعداد ليوفيل
متسامية.

\textbf{10.} آحاد في المواضع $1, 2, 6, 24$؛ وكل الأرقام الأخرى
بين الأرقام $25$ الأولى معدومة:
\[
L = 0.1100010000\,0000000000\,00010\dots
\]

\textbf{11.} من أجل $m > k$، تكون المواضع $n!$ ذات $n > k$
أعدادًا صحيحة متمايزة $\geq (k+1)!$، ومنه يعطي المجموع الهندسي المنتهي
\[
t_m - t_k = \sum_{n=k+1}^{m} 10^{-n!}
\leq \sum_{j = (k+1)!}^{m!} 10^{-j}
< 10^{-(k+1)!}\,\frac{1}{1 - \frac1{10}}
= \frac{10}{9}\,10^{-(k+1)!} ;
\]
وبأخذ \omterm{def:b1:reals:bounds}{الحد الأعلى} على $m$: $L - t_k \leq
\frac{10}{9}10^{-(k+1)!} < 2\cdot10^{-(k+1)!}$. والحاصر الأدنى: $L
\geq t_{k+1} = t_k + 10^{-(k+1)!}$.

\textbf{12.} $p_k = 10^{k!}\,t_k \in \N$ و $q_k = 10^{k!}$، ويعطي
$(k+1)! = (k+1)\,k!$ أن $10^{-(k+1)!} = q_k^{-(k+1)}$:
فيُقرأ السؤال 11
\[
0 < L - \frac{p_k}{q_k} < \frac{2}{q_k^{\,k+1}} .
\]
ومن أجل $n$ معطى: من أجل $k \geq n$، $2\,q_k^{-(k+1)} \leq q_k^{-n}$
(فعلًا $q_k^{\,k+1-n} \geq q_k \geq 10 > 2$)، و $q_k \geq 2$:
فيتحقق تعريف السؤال 8. ومنه فالعدد $L$ عدد ليوفيل.

\textbf{13.} حسب السؤال 9، يكون $L$ متساميًا --- وهو أول
عدد في التاريخ بُرهن على تساميه (ليوفيل، 1844).
وللتحقق بالأرقام: في السلسلة آحاد لا تُحصى بفجوات
متتالية $(k+1)! - k! = k\cdot k! \to \infty$، ومنه فهي
ليست دورية في النهاية، و $L \notin \Q$ حسب محك
الدورية في \cref{pb:b1:reals:1} (السؤال 18) --- وهذا متسق.

\textbf{14.} مع أرقام $d_n \in \intint{1}{9}$ في
المواضع العاملية: يُضرب حاصر الذيل في السؤال 11 بعامل
$9$ على الأكثر: $0 < L' - t'_k \leq 9\cdot\frac{10}{9}\,10^{-(k+1)!} =
10\,q_k^{-(k+1)}$ (والإيجابية لأن الرقم في الموضع
$(k+1)!$ غير معدوم). ومن أجل $k \geq n$: $10\,q_k^{-(k+1)} \leq
q_k^{-n}$ لأن $q_k^{\,k+1-n} \geq 10$: أي عدد ليوفيل
مرة أخرى، ومنه متسامٍ. وهذه القيم متمايزة
مثنى مثنى من أجل اختيارات أرقام متمايزة (فالسلاسل سليمة ---
إذ تكثر فيها الأصفار --- والسلاسل السليمة تحدّد قيمتها،
\cref{pb:b1:reals:1}، السؤال 10). ومن أجل أيّ قائمة $k \mapsto
x_k$ منها، اختر الرقم العاملي $k$ في
$\intint{1}{9}$ مختلفًا عن رقم $x_k$: فتحصل على عدد
من الشكل نفسه غائب عن القائمة. أي أعداد متسامية صريحة
لا تُحصى عددًا غير قابل للعدّ.

\textbf{15.} أولًا مبرهنة مساعدة: \emph{إذا كان $\bigl|x - \frac pq\bigr|
\geq \frac{C}{q^s}$ من أجل كل $\frac pq \neq x$، فإن $x$ ليس
قابلًا للتقريب من أيّ رتبة $\mu > s$.} فعلًا، لو وُجد عدد لا يُحصى من
$\frac pq \neq x$ يحقق $\bigl|x - \frac pq\bigr| <
\frac{c}{q^\mu}$ لفُرض $\frac{C}{q^s} < \frac{c}{q^\mu}$،
أي $q^{\mu - s} < \frac cC$: فتكون $q$ محدودة، و
عددٌ محدود من الكسور يقع على مسافة $1$ من $x$ ---
أي عدد منته من المرشّحين، لا عددًا لا يُحصى. والآن
التراتب: الأعداد الناطقة قابلة للتقريب من الرتبة $1$ ($\frac
pq$ مع $p = \lfloor qx\rfloor + 1$ يعطي خطأ $\leq \frac1q <
\frac2q$) ولا من أيّ رتبة $\mu > 1$ (فالسؤال 1 يعطي
فرض المبرهنة المساعدة مع $s = 1$ و $C = \frac1b$)؛ و $\sqrt2$:
الرتبة $2$ (السؤال 3) ولا أكثر (السؤال 2 مع المبرهنة المساعدة)؛
وكل عدد أصمّ: الرتبة $2$ على الأقل (السؤال 4)؛ \omterm{pb:b1:logic:1}{والعدد الجبري} من الدرجة
$d$: $d$ على الأكثر (السؤال 7 مع المبرهنة المساعدة)؛ وأعداد ليوفيل:
كل رتبة (معروضة السؤال 12، مع $c = 2$).

\textbf{16.} مع $r = \frac ab$: $\frac{p_k}{q_k} + \frac ab =
\frac{b p_k + a q_k}{b q_k} =: \frac{P_k}{Q_k}$ و $Q_k = b q_k
\geq 2$، و
\[
\Bigl|(L + r) - \frac{P_k}{Q_k}\Bigr| = L - \frac{p_k}{q_k}
< 2\,q_k^{-(k+1)} = 2\,b^{\,k+1} Q_k^{-(k+1)} .
\]
ومن أجل $n$ معطى: من أجل $k$ كبير، $Q_k^{\,k+1-n} \geq Q_k = b\,10^{k!}
\geq 2\,b^{\,k+1}$ (لأن العاملي يسحق القوة)، ومنه يكون
الخطأ $< Q_k^{-n}$: أي إن $L + r$ عدد ليوفيل. ولأن $\Q$ \omterm{def:b1:topology:dense}{كثيفة}
وكل $L + r$ متسامٍ، تكون الأعداد المتسامية
\omterm{def:b1:topology:dense}{كثيفة} في $\R$.

\textbf{17.} من أجل $h \geq 1$ يوجد عدد منته من $P \in
\Z[X]$ يحقق $\deg P + \sum_i \abs{a_i} \leq h$ (فالدرجة $\leq h$
وكل معامل في $\intint{-h}{h}$: أي
$(2h+1)^{h+1}$ على الأكثر). ولكل \omterm{def:b1:poly:def}{كثير حدود} صحيح غير معدوم مثلُ هذا
الارتفاع، وله $\deg P$ جذرًا حقيقيًا على الأكثر: ومنه تكوّن الأعداد
الجبرية اتحادًا قابلًا للعدّ (على $h$) لمجموعات منتهية، ومنه
يمكن تعدادها متتاليةً واحدة. ولو أمكن تعداد الأعداد المتسامية
كذلك، لكان تشبيك القائمتين يعدّد $\R$،
مناقضًا \cref{pb:b1:reals:1} (السؤال 22). ومنه تكوّن
الأعداد المتسامية \omterm{def:b1:logic:sets}{مجموعة} غير قابلة للعدّ. والمقارنة:
يبرهن كانتور على أن \emph{أكثر} الأعداد الحقيقية متسامية دون أن يظهر
واحدًا؛ ويظهر ليوفيل واحدًا، بثوابت فعّالة (الجزء 5)
--- أي الوجود بالوفرة إزاء الوجود بالبناء.

\textbf{18.} من السؤال 2، يتحقق فرض المبرهنة المساعدة مع $s
= 2$ و $C = \frac14$. ولو كان $\sqrt2$ عدد ليوفيل، لكان من أجل $n =
3$: يفرض $\frac{1}{4q^2} < q^{-3}$ أن $q < 4$، ومنه $q \in \{2,
3\}$؛ ولا يقع إلا عدد منته من $\frac pq$ ذات هذه المقامات $q$ على بعد
$1$ من $\sqrt2$، وكلٌّ على مسافة موجبة
$\geq \varepsilon_0$ (لأن $\sqrt2$ أصمّ)؛ واختيار $n$ يحقق
$2^{-n} < \varepsilon_0$ لا يترك أيّ $\frac pq$ مقبولًا أصلًا:
وهذا تناقض. والحجة نفسها مع $\frac{C}{q^d}$ تبيّن أن لا
\omterm{pb:b1:logic:1}{عدد جبري} هو عدد ليوفيل --- أي السؤال 9 في ثياب
فعّالة.

\textbf{19.} $99^2 - 2\cdot70^2 = 9801 - 9800 = 1$. ومنه
\[
\sqrt2 - \frac{99}{70} =
\frac{2 - (99/70)^2}{\sqrt2 + 99/70}
= \frac{-1}{4900\,\bigl(\sqrt2 + \tfrac{99}{70}\bigr)} ,
\qquad
\Bigl|\sqrt2 - \frac{99}{70}\Bigr|
= \frac{1}{4900 \times 2.8284\dots} \approx 7.2\cdot10^{-5} :
\]
$\frac{99}{70} = 1.414285\dots$ إزاء $\sqrt2 =
1.414213\dots$ --- أي خمسة أرقام صحيحة.

\textbf{20.} اختبار الجذر الناطق من أجل $P = X^3 - 2$: المرشّحون
$\pm1, \pm2, \pm\frac12$، ولا واحد منهم جذر. ومنه $d = 3$ وينطبق الجزء 2
على $x = 2^{1/3} = 1.2599\dots$ وعلى $\intcc{x - 1}{x + 1}
\subseteq \intcc{0.25}{2.26}$: $\abs{P'(t)} = 3t^2 \leq 3\,(1 +
2^{1/3})^2 < 3\times(2.26)^2 = 15.32 < 16$، ومنه $M < 16$ و $C
\geq \frac{1}{16}$:
\[
\Bigl|2^{1/3} - \frac pq\Bigr| \geq \frac{1}{16\,q^3}
\qquad\text{من أجل كل الأعداد الناطقة.}
\]

\textbf{21.} إذا كان $\bigl|2^{1/3} - \frac pq\bigr| < 10^{-6}$، فإن
$\frac{1}{16 q^3} < 10^{-6}$، أي $q^3 > \frac{10^6}{16} =
62\,500$؛ ولأن $39^3 = 59\,319 < 62\,500 \leq 64\,000 = 40^3$:
يكون $q \geq 40$.

\textbf{22.} نفّذ الجزء 3 في الأساس $2$: $B = \sup_k \sum_{n\leq
k} 2^{-n!}$ و $q_k = 2^{k!}$، ويعطي الذيل الهندسي (بالأساس
$\frac12$) أن $0 < B - \frac{p_k}{q_k} \leq 2\cdot2^{-(k+1)!}
= 2\,q_k^{-(k+1)} \leq q_k^{-n}$ من أجل $k \geq n$. ومنه فالعدد $B$
عدد ليوفيل، ومنه متسامٍ: فلا شيء في الحجة
عشري.

\textbf{23.} مع آحاد في المواضع $3^k$: $q_k = 10^{3^k}$ و
يعطي حاصر الذيل $0 < x^\dagger - \frac{p_k}{q_k} <
2\cdot10^{-3^{k+1}} = 2\,q_k^{-3}$ (لأن $3^{k+1} = 3\cdot3^k$):
أي تقريبات من الرتبة $3$ لا تُحصى. وحسب المبرهنة المساعدة في
السؤال 15: تستبعد الرتبة $3 > 1$ النطقية، وتستبعد الرتبة $3 >
2$ كونه عددًا أصمّ تربيعيًا (فمتراجحة ليوفيل له
لها $s = d = 2$). لكن \omterm{pb:b1:logic:1}{العدد الجبري} من الدرجة
$\geq 3$ لا يُنافَر إلا من الرتبة $d \geq 3$: ومنه فطريقة
ليوفيل لا تستطيع فصل $x^\dagger$ عن التكعيبيات. وتُسدّ الفجوة
بمبرهنة روث --- إذ لكل عدد أصمّ جبري
رتبة تقريب تساوي $2$ بالضبط --- وهي نتيجة من القرن العشرين
أبعد بكثير من هذا المجلّد؛ وبقبولها، يكون $x^\dagger$ كذلك
متساميًا.

\textbf{24.} يوجد $(2H+1)^{d+1}$ على الأكثر من الثنائيات $(a_0,
\dots, a_d)$ ذات المركبات في $\intint{-H}{H}$، ولكل \omterm{def:b1:poly:def}{كثير حدود}
غير معدوم بينها $d$ جذرًا حقيقيًا على الأكثر: ومنه ينشأ
$d\,(2H+1)^{d+1}$ عددًا جبريًا على الأكثر --- وهو التناهي
الذي أتاح للسؤال 17 تعدادها كلها.

\textbf{25.} (أ) المكوّن التحليلي الوحيد هو مبرهنة
التزايدات المنتهية، في السؤال 7، وهي تحوّل الانعدام $P(x) =
0$ إلى التنافر الليبشيتزي $\abs{P(p/q)} \leq M\abs{x -
p/q}$. (ب) والتوتر: تدفع الصحيحية $\abs{P(p/q)}$ صعودًا إلى
$q^{-d}$، وتسحبها الملاسة نزولًا إلى $M\abs{x - p/q}$ --- ومنه فالعدد
الناطق القريب جدًا من $x$ سيُسحق بين الاثنين.
(ج) ويبني ليوفيل عددًا متساميًا واحدًا بثوابت
فعّالة؛ ويبيّن كانتور أن كل الأعداد الحقيقية تقريبًا متسامية
دون أن يسمّي واحدًا: أي البناء إزاء القوة العددية. (د) و
تبرهن مسألة نهاية الأسبوع في \cref{ch:b1:integration} على
صمم $\pi$ بالحصر نفسه --- أي تكامل
سيكون عددًا صحيحًا موجبًا ومع ذلك يُحبس في $\intoo{0}{1}$ ---
بالتكامل بدل الاشتقاق نصفًا
تحليليًا.
\end{solution}
