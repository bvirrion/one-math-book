\section*{Capítulo \ref{ch:b1:logic} --- Lógica, conjuntos y aplicaciones}
\begin{solution}{exo:b1:logic:1}
Negaciones, empujando a $\lnot$ a través de cada cuantificador
(\cref{prop:b1:logic:negquant}) y usando $\lnot(P \implies Q) \iff
P \land \lnot Q $:
\begin{enumerate}
 \item $\exists x \in \R,\ \forall y \in \R,\ x + y \leq 0$;
 \item $\forall x \in \R,\ \exists y \in \R,\ xy \neq 0$;
 \item $\exists \varepsilon > 0,\ \forall \delta > 0,\ \exists x \in
 \R,\ \abs{x} \leq \delta \text{ and } \abs{f(x)} >
 \varepsilon $.
\end{enumerate}
\omterm{def:b1:logic:statement}{Enunciado} (1) es verdadero: dado $x$, tome $ y = -x + 1$; luego $x + y = 1 >
0$. \omterm{def:b1:logic:statement}{Enunciado} (2) es verdadero:$x = 0$ satisface $ xy = 0$ para todos los $ y $.
\end{solution}

\begin{solution}{exo:b1:logic:2}
Tabla de verdad, escribiendo V/F para los cuatro casos $(P, Q)$:
\begin{center}
\begin{tabular}{cc|c|c|c|c}
$P$&$Q$&$P \implies Q$&$\lnot(P \implies Q)$&$\lnot Q$ &
$P \land \lnot Q$\\
\hline
T & T & T & F & F & F \\
T & F & F & T & T & T \\
F & T & T & F & F & F \\
F & F & T & F & T & F \\
\end{tabular}
\end{center}
Las columnas $4$ y $6$ coinciden, demostrando la equivalencia. La negación de
"Si una función es derivable entonces es continua" es por lo tanto:
"Existe una función que es diferenciable y no continua".
(un \omterm{def:b1:logic:statement}{enunciado} falso, da la casualidad: la implicación original es verdadera,
ver \cref{ch:b1:derivative}).
\end{solution}

\begin{solution}{exo:b1:logic:3}
\emph{Contraposición.} Supongamos $x < 1$. Entonces $x^3 < 1$(el cubo
la función está aumentando) y $x < 1$, por lo que $x^3 + x < 2$. Esto prueba la
contrapositivo, de ahí el \omterm{def:b1:logic:statement}{enunciado}.

\emph{Contradicción.} Supongamos que $a > 0$ es el valor estrictamente positivo más pequeño.
real. Entonces $a/2$ es estrictamente positivo y $a/2 < a $(desde $a > 0$),
minimalismo contradictorio. Por lo tanto, no existe tal $a$.
\end{solution}

\begin{solution}{exo:b1:logic:4}
\begin{enumerate}
 \item Caso base $n = 0$:$2^0 = 1 = 2^1 - 1$. Paso: asumir el
 identidad para $n$,
 \[
 \sum_{k=0}^{n+1} 2^k = (2^{n+1} - 1) + 2^{n+1}
 = 2 \cdot 2^{n+1} - 1 = 2^{n+2} - 1 .
 \]
 \item Caso base $n = 0$:$4^0 + 5 = 6 = 3 \times 2$. Paso: si
 $4^n + 5 = 3m$, entonces
 \[
 4^{n+1} + 5 = 4(4^n + 5) - 15 = 3(4m - 5),
 \]
 divisible por $3$.
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:logic:5}
El paso inductivo supone silenciosamente que los dos grupos (``el primero
$n$'' y ``el último $n$'') se superponen, de modo que los lápices compartidos llevan
el color de un grupo al otro. Para $n + 1 = 2$ los dos grupos
son $\{$primer lápiz $\}$ y $\{$segundo lápiz $\}$: son disjuntos,
y el argumento se rompe. Entonces $P(1) \implies P(2)$ nunca fue probado, y
la inducción colapsa --- aunque $P(n) \implies P(n+1)$ es
válido para cada $n \geq 2$.
\end{solution}

\begin{solution}{exo:b1:logic:6}
\begin{enumerate}
 \item $x \in A \setminus B \iff x \in A \land x \notin B \iff x \in
 A \land x \in \overline{B} \iff x \in A \cap \overline{B}$.
 \item Usando (1) y distributividad
 (\cref{prop:b1:logic:setalgebra}):
 $(A \cup B) \cap \overline{C} = (A \cap \overline{C}) \cup
 (B \cap \overline{C})$.
 \item Supongamos $A \subseteq B$. Entonces $A \cup B \subseteq B$(ambos
 piezas se encuentran en $B$) y $B \subseteq A \cup B$ siempre, por lo que
 $A \cup B = B$. Supongamos $A \cup B = B$: entonces $A \cap B
 \subseteq A $siempre, y $A \subseteq A \cup B = B $ da
 $A \subseteq A \cap B$, entonces $A \cap B = A$. Supongamos $A \cap B =
 A $: luego $A = A \cap B \subseteq B $. Las tres condiciones son
 por lo tanto equivalente (probamos un ciclo de implicaciones).
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:logic:7}
\begin{enumerate}
 \item $f$ es \omterm{def:b1:logic:inj}{inyectivo} ($n + 1 = m + 1 \implies n = m$) pero no
 \omterm{def:b1:logic:inj}{sobreyectivo}: $0$ no tiene \omterm{def:b1:logic:map}{imagen inversa} en $\N$.
 \item $g$ es \omterm{def:b1:logic:inj}{biyectivo}:$n \mapsto n - 1$ es una inversa de dos caras en
 $\Z$.
 \item $h$ es \omterm{def:b1:logic:inj}{inyectivo}:$\frac{x+1}{x-1} = \frac{x'+1}{x'-1}$ da
 $(x+1)(x'-1) = (x'+1)(x-1)$, es decir,$xx' - x + x' - 1 = xx' -
 x' + x - 1$, entonces $2x' = 2x $. No es \omterm{def:b1:logic:inj}{sobreyectivo} en $\R$:
 resolver $ y = \frac{x+1}{x-1}$ da $x(y - 1) = y + 1$, que
 no tiene solución cuando $ y = 1$(la ecuación dice $0 = 2$).
 Con el codominio $\R \setminus \{1\}$, el mismo cálculo da
 el único \omterm{def:b1:logic:map}{imagen inversa} $x = \frac{y+1}{y-1}$, por lo que
 $h \colon \R \setminus \{1\} \to \R \setminus \{1\}$ es
 \omterm{def:b1:logic:inj}{biyectivo} y $h^{-1}(y) = \frac{y+1}{y-1} = h(y)$:$h$ es su
 propia inversa.
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:logic:8}
\begin{enumerate}
 \item $x \in f^{-1}(B \cap B') \iff f(x) \in B \cap B' \iff f(x) \in
 B \land f(x) \in B' \iff x \in f^{-1}(B) \cap f^{-1}(B')$.
 Para imágenes: $ y \in f(A \cup A')$ y si $ y = f(x)$ para algunas $x$
 en $A$ o en $A'$, si es $ y \in f(A)$ o $ y \in f(A')$.
 \item Si $ y \in f(A \cap A')$, entonces $ y = f(x)$ con $x \in A$ y
 $x \in A'$, entonces $ y \in f(A)$ y $ y \in f(A')$. Rigor:
 tomar $f \colon \R \to \R$,$x \mapsto x^2$,$A = \{-1\}$,
 $A' = \{1\}$: luego $f(A \cap A') = f(\emptyset) = \emptyset $
 pero $f(A) \cap f(A') = \{1\}$.
 \item ($\Leftarrow$) Con $A = \{x\}$,$A' = \{x'\}$ para $x \neq
 x'$: si $f(x) = f(x')$, entonces $f(A) \cap f(A') = \{f(x)\}$
 mientras que $f(A \cap A') = \emptyset$, contradiciendo lo supuesto
 igualdad; entonces $f$ es \omterm{def:b1:logic:inj}{inyectivo}. ($\Rightarrow$) Sea $f$\omterm{def:b1:logic:inj}{inyectivo} y $ y \in f(A) \cap f(A')$:$ y = f(x) = f(x')$ con
 $x \in A$,$x' \in A'$; la inyectividad da $x = x' \in A \cap
 A'$, por lo que $ y \in f(A \cap A')$. Con (2), se cumple la igualdad.
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:logic:9}
$g \circ f = \mathrm{id}_E$ es \omterm{def:b1:logic:inj}{inyectivo} y \omterm{def:b1:logic:inj}{sobreyectivo}, por lo que por
\cref{prop:b1:logic:comp}~(2), $f$ es \omterm{def:b1:logic:inj}{inyectivo} y $g$ es \omterm{def:b1:logic:inj}{sobreyectivo}.
Ejemplo: $E = \N$,$F = \Z$,$f$ la inclusión $n \mapsto n$, y
$g \colon \Z \to \N$,$g(n) = n$ para $n \geq 0$ y $g(n) = 0$ para
$n < 0$. Entonces $g(f(n)) = n$ para todos los $n \in \N$, pero $f$ no lo es.
\omterm{def:b1:logic:inj}{sobreyectivo} y $g$ no es \omterm{def:b1:logic:inj}{inyectivo}.
\end{solution}

\begin{solution}{exo:b1:logic:10}
$x^2 - y^2 = x - y \iff (x - y)(x + y) = x - y \iff (x - y)(x + y - 1)
= 0 \iff y = x $ o $ y = 1 - x$. \emph{Reflexivo:}$ y = x$ funciona.
\emph{Simétrico:} la condición ``$ y = x$ o $ y = 1 - x$'' es
simétrico en $x$ y $ y $(si $ y = 1 - x$ entonces $x = 1 - y $).
\emph{Transitivo:} supongamos $x \mathbin{\mathcal{R}} y $ y $ y
\mathbin{\mathcal{R}} z $; pasando por los cuatro casos,$z $ es igual a$x $
o $1 - x$ cada vez (por ejemplo, $ y = 1 - x$ y $z = 1 - y $ dan $z = x$).
Entonces $\mathcal{R}$ es \omterm{def:b1:logic:equiv}{relación de equivalencia} y $\mathrm{cl}(x) =
\{x,\, 1 - x\}$. Esta clase tiene un elemento exactamente cuando $x = 1 - x $,
es decir,\ para $x = \frac12$.
\end{solution}

\begin{solution}{exo:b1:logic:11}
Sea $f \colon E \to \mathcal{P}(E)$ cualquier \omterm{def:b1:logic:map}{aplicación} y establezca
$D = \{x \in E : x \notin f(x)\} \in \mathcal{P}(E)$. Supongamos $D =
f(a)$ para algunos $a \in E $. Si$a \in D $, entonces por definición de $D $,
$a \notin f(a) = D$: contradicción. Si$a \notin D$, entonces $a \notin
f(a)$, por definición de $D $,$a \in D$: contradicción. Por lo tanto $D$ es
no en la imagen de $f$ y $f$ no es \omterm{def:b1:logic:inj}{sobreyectivo}. (En particular no
\omterm{def:b1:logic:sets}{conjunto} está en biyección con su \omterm{def:b1:logic:sets}{conjunto de potencia}: hay "más" subconjuntos de
$\N$ que los números enteros.)
\end{solution}

\begin{solution}{exo:b1:logic:12}
\begin{enumerate}
 \item ($\Rightarrow$) Sea $f$\omterm{def:b1:logic:inj}{sobreyectivo} y $\Phi(B) =
 \Phi(B')$. Para$ y \in B $, elija $x $ con $f(x) = y $; luego $x
 \in f^{-1}(B) = f^{-1}(B')$, entonces $ y = f(x) \in B'$. Por lo tanto $B
 \subseteq B'$, y simétricamente $B' \subseteq B $:$\Phi$ es
 \omterm{def:b1:logic:inj}{inyectivo}. ($\Leftarrow$) Si$f$ no es \omterm{def:b1:logic:inj}{sobreyectivo}, seleccione $y_0
 \in F $fuera de la imagen; entonces $f^{-1}(\{y_0\}) = \emptyset =
 f^{-1}(\emptyset)$ con $\{y_0\} \neq \emptyset $, por lo que $\Phi$ es
 no \omterm{def:b1:logic:inj}{inyectivo}.
 \item ($\Rightarrow$) Sea $f$\omterm{def:b1:logic:inj}{inyectivo} y $A \subseteq E$. conjunto
 $B = f(A)$; luego $f^{-1}(B) = \{x : f(x) \in f(A)\}$, y
 la inyectividad da $f(x) \in f(A) \iff x \in A$, por lo que $\Phi(B) =
 A $:$\Phi$ es \omterm{def:b1:logic:inj}{sobreyectivo}. ($\Leftarrow$) Si$f $ no es
 \omterm{def:b1:logic:inj}{inyectivo}, tome $x \neq x'$ con $f(x) = f(x')$. cada
 \omterm{def:b1:logic:map}{imagen inversa} \omterm{def:b1:logic:sets}{conjunto} $f^{-1}(B)$ contiene $x$ si y sólo si
 contiene $x'$; por lo tanto $\{x\}$ no tiene la forma $\Phi(B)$, y
 $\Phi$ no es \omterm{def:b1:logic:inj}{sobreyectivo}.
\end{enumerate}
\end{solution}

\begin{solution}{pb:b1:logic:1}
\textbf{1.} \emph{Reflexivo:} $\mathrm{id}_E$ es una biyección de $E$
sobre sí mismo. \emph{Simétrico:} si $f \colon E \to F$ es \omterm{def:b1:logic:inj}{biyectivo},
\cref{thm:b1:logic:inverse} proporciona $f^{-1} \colon F \to E$, en sí
\omterm{def:b1:logic:inj}{biyectivo}. \emph{Transitivo:} si $f \colon E \to F$ y $g \colon F
\to G $ son biyección, \cref{prop:b1:logic:comp}~(1) dice $g \circ f
\colon E \to G $ es una biyección. (Esto es sólo ``como'' un \omterm{def:b1:logic:equiv}{equivalencia
relación}: la colección de todos los \omterm{def:b1:logic:sets}{conjuntos} no es en sí misma un \omterm{def:b1:logic:sets}{conjunto}, según
paradojas que insinúa \cref{exo:b1:logic:11}; las tres propiedades
son lo que importa.)

\textbf{2.} Si $f \colon E \to F$ y $g \colon F \to G$ son
\omterm{def:b1:logic:inj}{inyectivo}, $g \circ f$ es \omterm{def:b1:logic:inj}{inyectivo} por \cref{prop:b1:logic:comp}~(1):
$E \preceq G$. Para el segundo punto, restrinja $f$ a su imagen: el
\omterm{def:b1:logic:map}{aplicación} $\tilde f \colon E \to f(E)$,$x \mapsto f(x)$, es \omterm{def:b1:logic:inj}{sobreyectivo} por
construcción de $f(E)$ y \omterm{def:b1:logic:inj}{inyectivo} porque $f$ es, por lo tanto, \omterm{def:b1:logic:inj}{biyectivo}:
$E \approx f(E)$.

\textbf{3.} ($\Rightarrow$) Sea $f \colon E \to F$\omterm{def:b1:logic:inj}{inyectivo} y
arreglar $a \in E$($E \neq \emptyset$). Defina $s \colon F \to E$ por:
$s(y)$ es el único $x$ con $f(x) = y $ cuando $ y \in f(E)$
(singularidad por inyectividad), y $s(y) = a$ en caso contrario. Por cada $x
\in E $,$s(f(x)) = x$, por lo que se alcanza cada $x$: $s$ es \omterm{def:b1:logic:inj}{sobreyectivo}.
($\Leftarrow$) Sea $s \colon F \to E$\omterm{def:b1:logic:inj}{sobreyectivo}. Para cada $x \in
E $, elija un $y_x \in F $ con $s(y_x) = x $ y configure $u(x) = y_x $. si
$u(x) = u(x')$ luego $x = s(u(x)) = s(u(x')) = x'$:$u \colon E \to F$
es \omterm{def:b1:logic:inj}{inyectivo}.

\textbf{4.} $n \mapsto n + 1$\omterm{def:b1:logic:map}{aplicaciones}$\N$ en $\N^*$, es \omterm{def:b1:logic:inj}{inyectivo}
($n + 1 = m + 1 \implies n = m$) y \omterm{def:b1:logic:inj}{sobreyectivo} (cada $m \geq 1$ es
$(m - 1) + 1$ con $m - 1 \in \N$). Para $\sigma$: \omterm{def:b1:logic:map}{aplicaciones} incluso
números $0, 2, 4, \dots $a $0, 1, 2, \dots$ y números impares $1, 3,
5, \dots $a$-1, -2, -3, \dots$ Inyectividad: las entradas pares aterrizan en
$\N$($\sigma(n) = n/2 \geq 0$) y las entradas impares aterrizan en el
enteros estrictamente negativos ($\sigma(n) = -(n+1)/2 \leq -1$), por lo que un
La colisión debe ocurrir dentro de una clase de paridad, donde $\sigma$ es
estrictamente monótono ($n/2 = m/2$ o $(n+1)/2 = (m+1)/2$ fuerza a$n =
m $). Sobreyectividad:$k \geq 0$ es $\sigma(2k)$;$k \leq -1$ es
$\sigma(-2k - 1)$ con $-2k - 1 \geq 1$ impar. Entonces $\N \approx \N^*$ y
$\N \approx \Z$.

\textbf{5.} $C_0 = E \setminus g(F) \subseteq C$, entonces $x \notin C$
implica $x \notin C_0$, es decir $x \in g(F)$: algunos$ y \in F$ satisfacen
$g(y) = x$. Si también es $g(y') = x$, la inyectividad de $g$ da $ y' = y $.
Por lo tanto, la segunda cláusula de la definición de $h$ selecciona un único,
elemento bien definido $g^{-1}(x)$.

\textbf{6.} Imágenes directas conmutan con sindicatos
(\cref{exo:b1:logic:8}~(1), aplicado a $f$ y luego a$g$):
\[
g\bigl(f(C)\bigr)
= g\Bigl(f\Bigl(\bigcup_{n \in \N} C_n\Bigr)\Bigr)
= \bigcup_{n \in \N} g\bigl(f(C_n)\bigr)
= \bigcup_{n \in \N} C_{n+1}
= \bigcup_{n \geq 1} C_n \subseteq C .
\]

\textbf{7.} Sea $x \neq x'$ en $E$. Si ambos se encuentran en $C$, entonces $h(x) =
f(x) \neq f(x') = h(x')$ por inyectividad de $f $. Si ninguno de los dos se encuentra en
$C$, luego $g(h(x)) = x \neq x' = g(h(x'))$, entonces $h(x) \neq h(x')$. si
$x \in C$ y $x' \notin C$(el caso mixto, hasta intercambiar nombres):
supongamos $h(x) = h(x')$, es decir,$f(x) = g^{-1}(x')$. Aplicando $g$:
$x' = g(f(x)) \in g(f(C))$, y la pregunta 6 da $x' \in C$ ---
contradicción. Entonces $h(x) \neq h(x')$ en todos los casos:$h$ es \omterm{def:b1:logic:inj}{inyectivo}.

\textbf{8.} Vamos $ y \in F$. \emph{Case 1:$g(y) \notin C$.} Entonces
$h(g(y)) = g^{-1}(g(y)) = y $: el elemento $g(y)$ es un \omterm{def:b1:logic:map}{imagen inversa}.
\emph{Case 2: $g(y) \in C$}, digamos $g(y) \in C_n$. Desde $g(y) \in
g(F)$, tenemos $g(y) \notin C_0 = E \setminus g(F)$, entonces $n \geq 1$
y $g(y) \in C_n = g(f(C_{n-1}))$: hay $x \in C_{n-1}$ con
$g(y) = g(f(x))$. La inyectividad de $g$ da $ y = f(x)$ y $x \in
C_{n-1} \subseteq C $, por lo que $h(x) = f(x) = y $. En ambos casos$ y $ es
obtenido: $h$ es \omterm{def:b1:logic:inj}{sobreyectivo}, por lo tanto \omterm{def:b1:logic:inj}{biyectivo}.

\textbf{9.} Si $E \preceq F$ y $F \preceq E$, elija las inyecciones $f
\colon E \to F $ y $g \colon F \to E$; preguntas 5--8 construir un
biyección $h \colon E \to F$, entonces $E \approx F$. El \omterm{def:b1:logic:statement}{enunciado} es
no trivial porque las dos inyecciones administradas no están relacionadas --- ninguna
necesita ser \omterm{def:b1:logic:inj}{sobreyectivo}, y ninguna fórmula que combine $f$ y $g$ define ingenuamente
a \omterm{def:b1:logic:map}{aplicación}: todo el contenido es el \omterm{thm:b1:logic:partition}{dividir} de $E$ en la región $C$
(donde se copia $f$) y su complemento (donde se ejecuta $g$
hacia atrás).

\textbf{10.} (a) La inclusión $\intoo01 \to \intcc01$ es \omterm{def:b1:logic:inj}{inyectivo};
y $x \mapsto \frac{x + 1}3$\omterm{def:b1:logic:map}{aplicaciones}$\intcc01$\omterm{def:b1:logic:inj}{inyectivamente} en
$\intcc{\frac13}{\frac23} \subseteq \intoo01$(es afín con
pendiente distinta de cero). Por pregunta 9, $\intcc01 \approx \intoo01$ --- a
biyección que es bastante desagradable de escribir explícitamente.
(b) \emph{Inyectividad.} Supongamos que $2^p(2q + 1) = 2^{p'}(2q' + 1)$ con,
digamos, $p \leq p'$. Dividiendo por $2^p$:$2q + 1 = 2^{p' - p}(2q' + 1)$.
Si $p' > p$ el lado derecho es par y el lado izquierdo impar ---
imposible; entonces $p = p'$, luego $2q + 1 = 2q' + 1$ y $q = q'$.
\emph{Surjetividad.} Demostramos por inducción fuerte que todo número entero
$m \geq 1$ tiene la forma $2^p(2q + 1)$. Para $m = 1$:$p = q = 0$.
Sea $m \geq 1$ y asuma el reclamo para todos los números enteros de
$\intint1m$. Si$m + 1$ es impar,$m + 1 = 2q + 1$ con $p = 0$. si
$m + 1$ es par,$m + 1 = 2m'$ con $1 \leq m' \leq m$; por
hipótesis $m' = 2^p(2q + 1)$, entonces $m + 1 = 2^{p+1}(2q + 1)$. Por lo tanto
$\varphi(p, q) = 2^p(2q + 1) - 1$ llega a cada $n \in \N$, y
$\varphi$ es una biyección $\N \times \N \to \N$.

\textbf{11.} Dado que $A$ es infinito,$A \setminus \{\varphi(0), \dots,
\varphi(n - 1)\}$ nunca está vacío y la propiedad de elemento mínimo de
$\N$(usado para probar \cref{thm:b1:logic:induction}) hace el
Definición recursiva legítima. \emph{Estrictamente creciente:}
$\varphi(n + 1)$ pertenece a$A \setminus \{\varphi(0), \dots,
\varphi(n)\} \subseteq A \setminus \{\varphi(0), \dots, \varphi(n -
1)\}$, cuyo mínimo es $\varphi(n)$; entonces $\varphi(n + 1) \geq
\varphi(n)$, y se excluye la igualdad, de donde $\varphi(n+1) >
\varphi(n)$. \emph{$\varphi(n) \geq n$:} por inducción, $\varphi(0)
\geq 0$ y $\varphi(n + 1) \geq \varphi(n) + 1 \geq n + 1$.
\emph{Inyectividad} se deriva de una estricta monotonicidad.
\emph{Sobreyectividad sobre $A$:} supongamos que algo de $a \in A$ nunca lo es
alcanzado. Desde $\varphi(a + 1) \geq a + 1 > a$, el \omterm{def:b1:logic:sets}{conjunto} de $n$ con
$\varphi(n) > a$ no está vacío; sea $n$ su elemento mínimo. por cada
$k < n$,$\varphi(k) \leq a$, por lo tanto $\varphi(k) < a $($a$ no es
alcanzado). Entonces $a$ se encuentra en $A \setminus \{\varphi(0), \dots,
\varphi(n - 1)\}$ y $ a < \varphi(n)$, contradiciendo la minimalidad
definiendo $\varphi(n)$. Entonces $\varphi$ es una biyección $\N \to A$, y
$A$ es contable.

\textbf{12.} Dejar $E \preceq \N$ mediante una inyección $f$; luego $E \approx
f(E)$(pregunta 2). Si$f(E)$ es finito,$E $ es finito; si $f(E)$ es
infinita, la pregunta 11 da $f(E) \approx \N$, entonces $E \approx \N$ por
transitividad (pregunta 1). Por el contrario finitos \omterm{def:b1:logic:sets}{conjuntos} y \omterm{pb:b1:logic:1}{conjuntos contables}
obviamente inyectar en $\N$. El atajo:$E \preceq \N$ y $\N
\preceq E $ dan $E \approx \N $ directamente por
Cantor--Schröder--Bernstein --- no se necesita ningún argumento de enumeración.

\textbf{13.} Sean $f \colon E \to \N$ y $g \colon F \to \N$
inyecciones. Entonces $(x, y) \mapsto \varphi\bigl(f(x), g(y)\bigr)$ es un
inyección $E \times F \to \N$: si las imágenes coinciden, inyectividad de
$\varphi $(pregunta 10) da $f(x) = f(x')$ y $g(y) = g(y')$,
luego $x = x'$,$ y = y'$. Para $\Z \times \N^*$: ambos factores son
contable (pregunta 4), entonces $\Z \times \N^* \preceq \N$; es
infinito (contiene $\{0\} \times \N^*$), por lo tanto contable por
pregunta 12.

\textbf{14.} Todo racional $r$ tiene una representación única $r =
p/q $ con $p \in \Z $,$q \in \N^*$ y la fracción en términos más bajos
(la unicidad se demuestra en \cref{ch:b1:arith}; para $r = 0$ tome
$0/1$). La \omterm{def:b1:logic:map}{aplicación}$r \mapsto (p, q)$ es entonces \omterm{def:b1:logic:inj}{inyectivo}: el par
determina $r = p/q$. Por lo tanto $\Q \preceq \Z \times \N^* \preceq \N$
por la pregunta 13. Dado que $\N \subseteq \Q$ da $\N \preceq \Q$,
La pregunta 12 (o Cantor--Schröder--Bernstein directamente) muestra $\Q
\approx \N $: los racionales son contables.

\textbf{15.} Para cada $n$ fijar una inyección $f_n \colon E_n \to \N$.
Para $x \in \bigcup_n E_n$, sea $n(x)$ el \emph{el menos}$n$ con
$x \in E_n$ y establezca $u(x) = \varphi\bigl(n(x), f_{n(x)}(x)\bigr)
\in \N $. Si$u(x) = u(x')$, la inyectividad de $\varphi$ da $ n(x) =
n(x') = n $ y $f_n(x) = f_n(x')$, por lo tanto $x = x'$ por inyectividad de
$f_n$. Entonces la unión se inyecta en $\N$: es, como mucho, contable.

\textbf{16.} Sea $\Psi(F) = \sum_{i \in F} 2^i$ para $F \subseteq \N$
finito ($\Psi(\emptyset) = 0$). Supongamos $F \neq F'$ y sea $m$
el elemento más grande en el que difieren, digamos $m \in F \setminus
F'$(intercambie nombres si es necesario). Los elementos$> m $ pertenecen a ambos o
ninguno de los dos, por lo que contribuyen igualmente a ambas sumas; comparando el
Aportes de elementos $\leq m$:
\[
\sum_{i \in F,\, i \leq m} 2^i \geq 2^m
> 2^m - 1 = \sum_{k=0}^{m-1} 2^k
\geq \sum_{i \in F',\, i \leq m} 2^i ,
\]
utilizando la suma geométrica de \cref{exo:b1:logic:4}. Por lo tanto $\Psi(F)
\neq \Psi(F')$:$\Psi$ es \omterm{def:b1:logic:inj}{inyectivo} y \omterm{def:b1:logic:sets}{conjunto} de subconjuntos finitos.
de $\N$ es como máximo contable; es infinito (contiene todo
singletons), por lo tanto contables.

\textbf{17.} Enviar $A \subseteq \N$a su indicador $\mathbf 1_A
\colon \N \to \{0,1\}$,$\mathbf 1_A(n) = 1$ si $ n \in A $ y $0$
de lo contrario; envíe $u \in \{0,1\}^{\N}$a$A_u = \{n \in \N : u(n) =
1\}$. Los dos \omterm{def:b1:logic:map}{aplicaciones} son mutuamente inversos:$A_{\mathbf 1_A} = A $ y
$\mathbf 1_{A_u} = u $(verificar el valor en cada $n$). Por
\cref{thm:b1:logic:inverse}, cada uno es una biyección: $\mathcal{P}(\N)
\approx \{0,1\}^{\N}$.

\textbf{18.} Por cada $n$,$d(n) = 1 - \Phi(n)(n) \neq \Phi(n)(n)$,
por lo que las secuencias $d$ y $\Phi(n)$ difieren en el índice $n$:$d \neq
\Phi(n)$. Por lo tanto ningún $\Phi$ es \omterm{def:b1:logic:inj}{sobreyectivo}, y según la pregunta 3 hay
tampoco hay inyección $\{0,1\}^{\N} \to \N$:$\{0,1\}^{\N}$ no está en
más contable. A través del diccionario de la pregunta 17, una \omterm{def:b1:logic:map}{aplicación} $\Phi
\colon \N \to \{0,1\}^{\N}$ es una \omterm{def:b1:logic:map}{aplicación}$f \colon \N \to
\mathcal{P}(\N)$, y $d $corresponde al \omterm{def:b1:logic:sets}{conjunto}$D = \{n : n
\notin f(n)\}$(efectivamente $d(n) = 1 \iff \Phi(n)(n) = 0 \iff n \notin
f(n)$): el argumento diagonal \emph{is} Prueba de Cantor
\cref{exo:b1:logic:11} para $E = \N$.

\textbf{19.} Escriba $x_n = 0.d_1(n)\,d_2(n)\,d_3(n)\dots$ en forma adecuada.
forme y defina $\delta_n = 5$ si $d_n(n) \neq 5$,$\delta_n = 6$
si $d_n(n) = 5$, entonces $x = 0.\delta_1\delta_2\delta_3\dots $ Este
La expansión utiliza sólo los dígitos $5$ y $6$, por lo que no termina en
todos los $9$: es la expansión adecuada de un $x \in \intco01$ real.
Para cada $n$, los dígitos $n$ de $x$ y $x_n$ difieren
($\delta_n \neq d_n(n)$ por construcción); desde expansiones adecuadas
son únicos, $x \neq x_n$. Por lo tanto ninguna secuencia agota $\intco01$: por
Pregunta 3 nuevamente, $\intco01$ no es contable como máximo.

\textbf{20.} $\intco01 \subseteq \R$, entonces una inyección $\R \to \N$
se limitaría a uno en $\intco01$, contradiciendo la pregunta 19:
$\R$ es incontable. Si$\R \setminus \Q$ fueran como máximo contables,
entonces $\R = \Q \cup (\R \setminus \Q)$ sería una unión de dos en
la mayoría \omterm{pb:b1:logic:1}{conjuntos contables}, por lo tanto, como máximo contable según la pregunta 15 (tome
$E_0 = \Q$,$E_n = \R \setminus \Q$ para $n \geq 1$) ---
contradicción. Entonces los irracionales son incontables. Precisamente:
dentro de $\R$, los racionales forman un \omterm{pb:b1:logic:1}{conjunto contable} mientras que sus
el complemento es incontable; ninguna biyección puede coincidir jamás con $\R
\setminus \Q $ con $\Q$ --- hay estrictamente "más" irracionales
que los racionales, aunque ambos son infinitos y densos.

\textbf{21.} $p/q $(con $q \neq 0$) es una raíz de $qX - p$, una
polinomio distinto de cero con coeficientes enteros. $\sqrt 2$ es una raíz
de $X^2 - 2$. Para $x = \sqrt 2 + \sqrt 3$:$x^2 = 5 + 2\sqrt 6$, entonces
$x^2 - 5 = 2\sqrt 6$ y $(x^2 - 5)^2 = 24$, es decir \[
x^4 - 10x^2 + 1 = 0 :
\]
$\sqrt 2 + \sqrt 3$ es una raíz de $X^4 - 10X^2 + 1$.

\textbf{22.} Aplicación $P = a_0 + a_1X + \dots + a_nX^n $(grado $\leq n$,
coeficientes enteros) a $(a_0, \dots, a_n) \in \Z^{n+1}$: esto es
\omterm{def:b1:logic:inj}{inyectivo}, ya que un polinomio está determinado por sus coeficientes. Por
inducción en $n$:$\Z^1 = \Z$ es contable (pregunta 4), y
$\Z^{n+2} \approx \Z^{n+1} \times \Z$ es contable como máximo por
pregunta 13. Entonces, cada \omterm{def:b1:logic:sets}{conjunto} de polinomios enteros de grado acotado es
como máximo contable; es infinito (contiene las constantes), por lo tanto
contable por la pregunta 12.

\textbf{23.} El \omterm{def:b1:logic:sets}{conjunto} de todos los
polinomios enteros es
\[
\bigcup_{n \in \N}
\{P : \deg P \leq n,\ P \text{ de coeficientes enteros}\},
\]
unión contable de \omterm{pb:b1:logic:1}{conjuntos contables}: a lo
sumo contable por la pregunta~15, e infinito, luego contable.

\textbf{24.} Para cada polinomio entero distinto de cero $P$, la raíz \omterm{def:b1:logic:sets}{conjunto}
$R_P = \{x \in \R : P(x) = 0\}$ es finito (como máximo $\deg P$
elementos, admitidos). Por la pregunta 23 los polinomios enteros distintos de cero
se puede enumerar $P_0, P_1, P_2, \dots$; entonces $\mathcal{A} =
\bigcup_{n \in \N} R_{P_n}$ es una unión contable de finitos (por lo tanto
como máximo contable) \omterm{def:b1:logic:sets}{conjuntos}: como máximo contable según la pregunta 15.
contiene $\Q$(pregunta 21), por lo que es infinito:$\mathcal{A}$ es
contable.

\textbf{25.} Si $\R \setminus \mathcal{A}$ fueran como máximo contables,
$\R = \mathcal{A} \cup (\R \setminus \mathcal{A})$ sería como máximo
contable (pregunta 15), contradiciendo la pregunta 20. Por lo tanto
\omterm{pb:b1:logic:1}{números trascendentales} existen e incluso forman un \omterm{def:b1:logic:sets}{conjunto} incontable,
mientras que el \omterm{pb:b1:logic:1}{números algebraicos} --- que incluye todos los números construidos
de números enteros por radicales --- forman un mero esqueleto contable
dentro de $\R$. Resumen de la arquitectura: preguntas 1--3 \omterm{def:b1:logic:sets}{conjunto} hasta el
lenguaje de comparación; Cantor--Schröder--Bernstein (preguntas
5--9) probemos la equipotencia mediante dos inyecciones fáciles en lugar de
una biyección inteligente, y se usó para $\intcc01 \approx \intoo01$,
para $\Q$ y en toda la Parte V; la biyección de emparejamiento (pregunta
10) productos motorizados y uniones contables (preguntas 13, 15),
que a su vez impulsó $\Q$, los polinomios enteros y
$\mathcal{A}$; el argumento diagonal (preguntas 18--19) proporcionado
la única desigualdad estricta $\N \prec \R$ que constituye toda la historia
no trivial. La conclusión de Cantor es filosóficamente sorprendente: la
La prueba no muestra ningún \omterm{pb:b1:logic:1}{numero trascendental} en absoluto, pero muestra que en
el sentido de equipotencia \emph{casi todos} número real es
trascendental. Nombrar un trascendental específico --- $\pi$ o
$\eu$ --- requirió matemáticas completamente diferentes y décadas más
trabajo.
\end{solution}