\section*{Chapitre \ref{ch:b1:logic} --- Logique, ensembles et applications}

\begin{solution}{exo:b1:logic:1}
Négations, en faisant passer $\lnot$ à travers chaque quantificateur
(\cref{prop:b1:logic:negquant}) et en utilisant $\lnot(P \implies Q)
\iff P \land \lnot Q$~:
\begin{enumerate}
  \item $\exists x \in \R,\ \forall y \in \R,\ x + y \leq 0$~;
  \item $\forall x \in \R,\ \exists y \in \R,\ xy \neq 0$~;
  \item $\exists \varepsilon > 0,\ \forall \delta > 0,\ \exists x \in
        \R,\ \abs{x} \leq \delta \text{ et } \abs{f(x)} >
        \varepsilon$.
\end{enumerate}
L'\omterm{def:b1:logic:statement}{assertion} (1) est vraie~: étant donné $x$, prendre $y = -x + 1$~;
alors $x + y = 1 > 0$. L'\omterm{def:b1:logic:statement}{assertion} (2) est vraie~: $x = 0$ vérifie
$xy = 0$ pour tout $y$.
\end{solution}

\begin{solution}{exo:b1:logic:2}
Table de vérité, en écrivant V/F pour les quatre cas $(P, Q)$~:
\begin{center}
\begin{tabular}{cc|c|c|c|c}
$P$ & $Q$ & $P \implies Q$ & $\lnot(P \implies Q)$ & $\lnot Q$ &
$P \land \lnot Q$ \\
\hline
V & V & V & F & F & F \\
V & F & F & V & V & V \\
F & V & V & F & F & F \\
F & F & V & F & V & F \\
\end{tabular}
\end{center}
Les colonnes $4$ et $6$ coïncident, ce qui démontre l'équivalence. La
négation de «~si une fonction est dérivable alors elle est continue~»
est donc~: «~il existe une fonction qui est dérivable et non continue~»
(une \omterm{def:b1:logic:statement}{assertion} fausse, en l'occurrence~: l'implication de départ est
vraie, voir le \cref{ch:b1:derivative}).
\end{solution}

\begin{solution}{exo:b1:logic:3}
\emph{Contraposition.} Supposons $x < 1$. Alors $x^3 < 1$ (la fonction
cube est croissante) et $x < 1$, donc $x^3 + x < 2$. Cela démontre la
contraposée, donc l'\omterm{def:b1:logic:statement}{assertion}.

\emph{Absurde.} Supposons que $a > 0$ soit le plus petit réel
strictement positif. Alors $a/2$ est strictement positif et $a/2 < a$
(puisque $a > 0$), ce qui contredit la minimalité. Donc un tel $a$
n'existe pas.
\end{solution}

\begin{solution}{exo:b1:logic:4}
\begin{enumerate}
  \item Initialisation $n = 0$~: $2^0 = 1 = 2^1 - 1$. Hérédité~: en
        supposant l'identité au rang $n$,
        \[
        \sum_{k=0}^{n+1} 2^k = (2^{n+1} - 1) + 2^{n+1}
        = 2 \cdot 2^{n+1} - 1 = 2^{n+2} - 1 .
        \]
  \item Initialisation $n = 0$~: $4^0 + 5 = 6 = 3 \times 2$. Hérédité~:
        si $4^n + 5 = 3m$, alors
        \[
        4^{n+1} + 5 = 4(4^n + 5) - 15 = 3(4m - 5),
        \]
        qui est divisible par $3$.
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:logic:5}
L'hérédité suppose en douce que les deux groupes («~les $n$ premiers~»
et «~les $n$ derniers~») se recoupent, de sorte que les crayons communs
transportent la couleur d'un groupe à l'autre. Pour $n + 1 = 2$, les
deux groupes sont $\{$premier crayon$\}$ et $\{$second crayon$\}$~: ils
sont disjoints, et l'argument s'effondre. Ainsi $P(1) \implies P(2)$ n'a
jamais été démontré, et la récurrence s'écroule --- même si
$P(n) \implies P(n+1)$ est valable pour tout $n \geq 2$.
\end{solution}

\begin{solution}{exo:b1:logic:6}
\begin{enumerate}
  \item $x \in A \setminus B \iff x \in A \land x \notin B \iff x \in
        A \land x \in \overline{B} \iff x \in A \cap \overline{B}$.
  \item En utilisant (1) et la distributivité
        (\cref{prop:b1:logic:setalgebra})~:
        $(A \cup B) \cap \overline{C} = (A \cap \overline{C}) \cup
        (B \cap \overline{C})$.
  \item Supposons $A \subseteq B$. Alors $A \cup B \subseteq B$ (les
        deux morceaux sont dans $B$) et $B \subseteq A \cup B$ toujours,
        donc $A \cup B = B$. Supposons $A \cup B = B$~: alors $A \cap B
        \subseteq A$ toujours, et $A \subseteq A \cup B = B$ donne
        $A \subseteq A \cap B$, donc $A \cap B = A$. Supposons
        $A \cap B = A$~: alors $A = A \cap B \subseteq B$. Les trois
        conditions sont donc équivalentes (on a démontré un cycle
        d'implications).
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:logic:7}
\begin{enumerate}
  \item $f$ est \omterm{def:b1:logic:inj}{injective} ($n + 1 = m + 1 \implies n = m$) mais non
        \omterm{def:b1:logic:inj}{surjective}~: $0$ n'a pas d'antécédent dans $\N$.
  \item $g$ est \omterm{def:b1:logic:inj}{bijective}~: $n \mapsto n - 1$ en est un inverse à
        gauche et à droite sur $\Z$.
  \item $h$ est \omterm{def:b1:logic:inj}{injective}~: $\frac{x+1}{x-1} = \frac{x'+1}{x'-1}$ donne
        $(x+1)(x'-1) = (x'+1)(x-1)$, c'est-à-dire $xx' - x + x' - 1 =
        xx' - x' + x - 1$, donc $2x' = 2x$. Elle n'est pas \omterm{def:b1:logic:inj}{surjective}
        sur $\R$~: résoudre $y = \frac{x+1}{x-1}$ donne
        $x(y - 1) = y + 1$, qui n'a pas de solution quand $y = 1$
        (l'équation s'écrit $0 = 2$). Avec l'\omterm{def:b1:logic:sets}{ensemble} d'arrivée
        $\R \setminus \{1\}$, le même calcul donne l'unique antécédent
        $x = \frac{y+1}{y-1}$, donc
        $h \colon \R \setminus \{1\} \to \R \setminus \{1\}$ est
        \omterm{def:b1:logic:inj}{bijective} et $h^{-1}(y) = \frac{y+1}{y-1} = h(y)$~: $h$ est sa
        propre réciproque.
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:logic:8}
\begin{enumerate}
  \item $x \in f^{-1}(B \cap B') \iff f(x) \in B \cap B' \iff f(x) \in
        B \land f(x) \in B' \iff x \in f^{-1}(B) \cap f^{-1}(B')$.
        Pour les images~: $y \in f(A \cup A')$ si et seulement si
        $y = f(x)$ pour un $x$ de $A$ ou de $A'$, si et seulement si
        $y \in f(A)$ ou $y \in f(A')$.
  \item Si $y \in f(A \cap A')$, alors $y = f(x)$ avec $x \in A$ et
        $x \in A'$, donc $y \in f(A)$ et $y \in f(A')$. Stricte
        inclusion~: prendre $f \colon \R \to \R$, $x \mapsto x^2$,
        $A = \{-1\}$, $A' = \{1\}$~: alors $f(A \cap A') =
        f(\emptyset) = \emptyset$ tandis que $f(A) \cap f(A') = \{1\}$.
  \item ($\Leftarrow$) Avec $A = \{x\}$, $A' = \{x'\}$ pour $x \neq
        x'$~: si $f(x) = f(x')$, alors $f(A) \cap f(A') = \{f(x)\}$
        tandis que $f(A \cap A') = \emptyset$, ce qui contredit
        l'égalité supposée~; donc $f$ est \omterm{def:b1:logic:inj}{injective}. ($\Rightarrow$)
        Soit $f$ \omterm{def:b1:logic:inj}{injective} et $y \in f(A) \cap f(A')$~: $y = f(x) =
        f(x')$ avec $x \in A$, $x' \in A'$~; l'\omterm{def:b1:logic:inj}{injectivité} donne
        $x = x' \in A \cap A'$, donc $y \in f(A \cap A')$. Avec (2),
        il y a égalité.
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:logic:9}
$g \circ f = \mathrm{id}_E$ est \omterm{def:b1:logic:inj}{injective} et \omterm{def:b1:logic:inj}{surjective}, donc d'après
la \cref{prop:b1:logic:comp}~(2), $f$ est \omterm{def:b1:logic:inj}{injective} et $g$ est \omterm{def:b1:logic:inj}{surjective}.
Exemple~: $E = \N$, $F = \Z$, $f$ l'inclusion $n \mapsto n$, et
$g \colon \Z \to \N$, $g(n) = n$ pour $n \geq 0$ et $g(n) = 0$ pour
$n < 0$. Alors $g(f(n)) = n$ pour tout $n \in \N$, mais $f$ n'est pas
\omterm{def:b1:logic:inj}{surjective} et $g$ n'est pas \omterm{def:b1:logic:inj}{injective}.
\end{solution}

\begin{solution}{exo:b1:logic:10}
$x^2 - y^2 = x - y \iff (x - y)(x + y) = x - y \iff (x - y)(x + y - 1)
= 0 \iff y = x$ ou $y = 1 - x$. \emph{Réflexive~:} $y = x$ convient.
\emph{Symétrique~:} la condition «~$y = x$ ou $y = 1 - x$~» est
symétrique en $x$ et $y$ (si $y = 1 - x$ alors $x = 1 - y$).
\emph{Transitive~:} supposons $x \mathbin{\mathcal{R}} y$ et $y
\mathbin{\mathcal{R}} z$~; en parcourant les quatre cas, $z$ vaut $x$
ou $1 - x$ à chaque fois (par exemple $y = 1 - x$ et $z = 1 - y$
donnent $z = x$). Donc $\mathcal{R}$ est une \omterm{def:b1:logic:equiv}{relation d'équivalence} et
$\mathrm{cl}(x) = \{x,\, 1 - x\}$. Cette classe a un seul élément
exactement lorsque $x = 1 - x$, c'est-à-dire pour $x = \frac12$.
\end{solution}

\begin{solution}{exo:b1:logic:11}
Soit $f \colon E \to \mathcal{P}(E)$ une \omterm{def:b1:logic:map}{application} quelconque et
posons $D = \{x \in E : x \notin f(x)\} \in \mathcal{P}(E)$. Supposons
$D = f(a)$ pour un certain $a \in E$. Si $a \in D$, alors par définition
de $D$, $a \notin f(a) = D$~: contradiction. Si $a \notin D$, alors
$a \notin f(a)$, donc par définition de $D$, $a \in D$~: contradiction.
Ainsi $D$ n'est pas dans l'image de $f$, et $f$ n'est pas \omterm{def:b1:logic:inj}{surjective}.
(En particulier aucun \omterm{def:b1:logic:sets}{ensemble} n'est en \omterm{def:b1:logic:inj}{bijection} avec son \omterm{def:b1:logic:sets}{ensemble des
parties}~: il y a «~plus~» de parties de $\N$ que d'entiers.)
\end{solution}

\begin{solution}{exo:b1:logic:12}
\begin{enumerate}
  \item ($\Rightarrow$) Soit $f$ \omterm{def:b1:logic:inj}{surjective} et $\Phi(B) = \Phi(B')$.
        Pour $y \in B$, choisissons $x$ tel que $f(x) = y$~; alors $x
        \in f^{-1}(B) = f^{-1}(B')$, donc $y = f(x) \in B'$. Ainsi $B
        \subseteq B'$, et symétriquement $B' \subseteq B$~: $\Phi$ est
        \omterm{def:b1:logic:inj}{injective}. ($\Leftarrow$) Si $f$ n'est pas \omterm{def:b1:logic:inj}{surjective},
        choisissons $y_0 \in F$ hors de l'image~; alors
        $f^{-1}(\{y_0\}) = \emptyset = f^{-1}(\emptyset)$ avec
        $\{y_0\} \neq \emptyset$, donc $\Phi$ n'est pas \omterm{def:b1:logic:inj}{injective}.
  \item ($\Rightarrow$) Soit $f$ \omterm{def:b1:logic:inj}{injective} et $A \subseteq E$. Posons
        $B = f(A)$~; alors $f^{-1}(B) = \{x : f(x) \in f(A)\}$, et
        l'\omterm{def:b1:logic:inj}{injectivité} donne $f(x) \in f(A) \iff x \in A$, donc
        $\Phi(B) = A$~: $\Phi$ est \omterm{def:b1:logic:inj}{surjective}. ($\Leftarrow$) Si $f$
        n'est pas \omterm{def:b1:logic:inj}{injective}, prenons $x \neq x'$ avec $f(x) = f(x')$.
        Toute \omterm{def:b1:logic:map}{image réciproque} $f^{-1}(B)$ contient $x$ si et seulement
        si elle contient $x'$~; donc $\{x\}$ n'est pas de la forme
        $\Phi(B)$, et $\Phi$ n'est pas \omterm{def:b1:logic:inj}{surjective}.
\end{enumerate}
\end{solution}

\begin{solution}{pb:b1:logic:1}
\textbf{1.} \emph{Réflexive~:} $\mathrm{id}_E$ est une \omterm{def:b1:logic:inj}{bijection} de $E$
sur lui-même. \emph{Symétrique~:} si $f \colon E \to F$ est \omterm{def:b1:logic:inj}{bijective},
le \cref{thm:b1:logic:inverse} fournit $f^{-1} \colon F \to E$, elle-même
\omterm{def:b1:logic:inj}{bijective}. \emph{Transitive~:} si $f \colon E \to F$ et $g \colon F
\to G$ sont des \omterm{def:b1:logic:inj}{bijections}, la \cref{prop:b1:logic:comp}~(1) dit que
$g \circ f \colon E \to G$ est une \omterm{def:b1:logic:inj}{bijection}. (Ce n'est qu'une
\omterm{def:b1:logic:equiv}{relation d'équivalence} «~au sens large~»~: la collection de tous les
\omterm{def:b1:logic:sets}{ensembles} n'est pas elle-même un \omterm{def:b1:logic:sets}{ensemble}, à cause des paradoxes
qu'évoque \cref{exo:b1:logic:11}~; ce sont les trois propriétés qui
comptent.)

\textbf{2.} Si $f \colon E \to F$ et $g \colon F \to G$ sont
\omterm{def:b1:logic:inj}{injectives}, $g \circ f$ est \omterm{def:b1:logic:inj}{injective} d'après
la \cref{prop:b1:logic:comp}~(1)~: $E \preceq G$. Pour le second point,
corestreignons $f$ à son image~: l'\omterm{def:b1:logic:map}{application} $\tilde f \colon E \to
f(E)$, $x \mapsto f(x)$, est \omterm{def:b1:logic:inj}{surjective} par construction de $f(E)$ et
\omterm{def:b1:logic:inj}{injective} parce que $f$ l'est, donc \omterm{def:b1:logic:inj}{bijective}~: $E \approx f(E)$.

\textbf{3.} ($\Rightarrow$) Soit $f \colon E \to F$ \omterm{def:b1:logic:inj}{injective} et fixons
$a \in E$ ($E \neq \emptyset$). Définissons $s \colon F \to E$ par~:
$s(y)$ est l'unique $x$ tel que $f(x) = y$ lorsque $y \in f(E)$
(unicité par \omterm{def:b1:logic:inj}{injectivité}), et $s(y) = a$ sinon. Pour tout $x \in E$,
$s(f(x)) = x$, donc tout $x$ est atteint~: $s$ est \omterm{def:b1:logic:inj}{surjective}.
($\Leftarrow$) Soit $s \colon F \to E$ \omterm{def:b1:logic:inj}{surjective}. Pour chaque $x \in
E$, choisissons un $y_x \in F$ tel que $s(y_x) = x$, et posons
$u(x) = y_x$. Si $u(x) = u(x')$ alors $x = s(u(x)) = s(u(x')) = x'$~:
$u \colon E \to F$ est \omterm{def:b1:logic:inj}{injective}.

\textbf{4.} $n \mapsto n + 1$ envoie $\N$ dans $\N^*$, est \omterm{def:b1:logic:inj}{injective}
($n + 1 = m + 1 \implies n = m$) et \omterm{def:b1:logic:inj}{surjective} (tout $m \geq 1$ vaut
$(m - 1) + 1$ avec $m - 1 \in \N$). Pour $\sigma$~: elle envoie les
nombres pairs $0, 2, 4, \dots$ sur $0, 1, 2, \dots$ et les nombres
impairs $1, 3, 5, \dots$ sur $-1, -2, -3, \dots$ \omterm{def:b1:logic:inj}{Injectivité}~: les
entrées paires arrivent dans $\N$ ($\sigma(n) = n/2 \geq 0$) et les
entrées impaires arrivent dans les entiers strictement négatifs
($\sigma(n) = -(n+1)/2 \leq -1$), donc une collision ne peut se
produire qu'à l'intérieur d'une classe de parité, où $\sigma$ est
strictement monotone ($n/2 = m/2$ ou $(n+1)/2 = (m+1)/2$ force
$n = m$). \omterm{def:b1:logic:inj}{Surjectivité}~: $k \geq 0$ vaut $\sigma(2k)$~; $k \leq -1$
vaut $\sigma(-2k - 1)$ avec $-2k - 1 \geq 1$ impair. Donc $\N \approx
\N^*$ et $\N \approx \Z$.

\textbf{5.} $C_0 = E \setminus g(F) \subseteq C$, donc $x \notin C$
entraîne $x \notin C_0$, c'est-à-dire $x \in g(F)$~: un certain $y \in
F$ vérifie $g(y) = x$. Si de plus $g(y') = x$, l'\omterm{def:b1:logic:inj}{injectivité} de $g$
donne $y' = y$. Ainsi la seconde clause de la définition de $h$ désigne
un élément unique et bien défini $g^{-1}(x)$.

\textbf{6.} Les images directes commutent aux réunions
(\cref{exo:b1:logic:8}~(1), appliquée à $f$ puis à $g$)~:
\[
g\bigl(f(C)\bigr)
= g\Bigl(f\Bigl(\bigcup_{n \in \N} C_n\Bigr)\Bigr)
= \bigcup_{n \in \N} g\bigl(f(C_n)\bigr)
= \bigcup_{n \in \N} C_{n+1}
= \bigcup_{n \geq 1} C_n \subseteq C .
\]

\textbf{7.} Soient $x \neq x'$ dans $E$. Si les deux sont dans $C$,
alors $h(x) = f(x) \neq f(x') = h(x')$ par \omterm{def:b1:logic:inj}{injectivité} de $f$. Si aucun
n'est dans $C$, alors $g(h(x)) = x \neq x' = g(h(x'))$, donc $h(x) \neq
h(x')$. Si $x \in C$ et $x' \notin C$ (le cas mixte, quitte à échanger
les noms)~: supposons $h(x) = h(x')$, c'est-à-dire $f(x) = g^{-1}(x')$.
En appliquant $g$~: $x' = g(f(x)) \in g(f(C))$, et la question 6 donne
$x' \in C$ --- contradiction. Donc $h(x) \neq h(x')$ dans tous les
cas~: $h$ est \omterm{def:b1:logic:inj}{injective}.

\textbf{8.} Soit $y \in F$. \emph{Cas 1~: $g(y) \notin C$.} Alors
$h(g(y)) = g^{-1}(g(y)) = y$~: l'élément $g(y)$ est un antécédent.
\emph{Cas 2~: $g(y) \in C$}, disons $g(y) \in C_n$. Comme $g(y) \in
g(F)$, on a $g(y) \notin C_0 = E \setminus g(F)$, donc $n \geq 1$
et $g(y) \in C_n = g(f(C_{n-1}))$~: il existe $x \in C_{n-1}$ tel que
$g(y) = g(f(x))$. L'\omterm{def:b1:logic:inj}{injectivité} de $g$ donne $y = f(x)$, et $x \in
C_{n-1} \subseteq C$, donc $h(x) = f(x) = y$. Dans les deux cas $y$ est
atteint~: $h$ est \omterm{def:b1:logic:inj}{surjective}, donc \omterm{def:b1:logic:inj}{bijective}.

\textbf{9.} Si $E \preceq F$ et $F \preceq E$, choisissons des
\omterm{def:b1:logic:inj}{injections} $f \colon E \to F$ et $g \colon F \to E$~; les questions
5 à 8 construisent une \omterm{def:b1:logic:inj}{bijection} $h \colon E \to F$, donc $E \approx
F$. L'énoncé est non trivial parce que les deux \omterm{def:b1:logic:inj}{injections} données
n'ont aucun lien --- aucune n'a besoin d'être \omterm{def:b1:logic:inj}{surjective}, et aucune
formule mélangeant naïvement $f$ et $g$ ne définit une \omterm{def:b1:logic:map}{application}~:
tout le contenu réside dans la \omterm{thm:b1:logic:partition}{partition} de $E$ en la région $C$ (où
l'on recopie $f$) et son complémentaire (où l'on remonte $g$).

\textbf{10.} (a) L'inclusion $\intoo01 \to \intcc01$ est \omterm{def:b1:logic:inj}{injective}~;
et $x \mapsto \frac{x + 1}3$ envoie $\intcc01$ injectivement dans
$\intcc{\frac13}{\frac23} \subseteq \intoo01$ (c'est une \omterm{def:b1:logic:map}{application}
affine de pente non nulle). D'après la question 9, $\intcc01 \approx
\intoo01$ --- une \omterm{def:b1:logic:inj}{bijection} qu'il serait fort désagréable d'écrire
explicitement.
(b) \emph{\omterm{def:b1:logic:inj}{Injectivité}.} Supposons $2^p(2q + 1) = 2^{p'}(2q' + 1)$ avec,
disons, $p \leq p'$. En divisant par $2^p$~: $2q + 1 = 2^{p' - p}(2q' +
1)$. Si $p' > p$, le membre de droite est pair et celui de gauche
impair --- impossible~; donc $p = p'$, puis $2q + 1 = 2q' + 1$ et
$q = q'$. \emph{\omterm{def:b1:logic:inj}{Surjectivité}.} Montrons par récurrence forte que tout
entier $m \geq 1$ est de la forme $2^p(2q + 1)$. Pour $m = 1$~:
$p = q = 0$. Soit $m \geq 1$ et supposons l'\omterm{def:b1:logic:statement}{assertion} vraie pour tous
les entiers de $\intint1m$. Si $m + 1$ est impair, $m + 1 = 2q + 1$
avec $p = 0$. Si $m + 1$ est pair, $m + 1 = 2m'$ avec $1 \leq m' \leq
m$~; par hypothèse $m' = 2^p(2q + 1)$, donc $m + 1 = 2^{p+1}(2q + 1)$.
Ainsi $\varphi(p, q) = 2^p(2q + 1) - 1$ atteint tout $n \in \N$, et
$\varphi$ est une \omterm{def:b1:logic:inj}{bijection} $\N \times \N \to \N$.

\textbf{11.} Comme $A$ est infinie, $A \setminus \{\varphi(0), \dots,
\varphi(n - 1)\}$ n'est jamais vide, et la propriété du plus petit
élément dans $\N$ (utilisée pour démontrer
\cref{thm:b1:logic:induction}) rend la définition par récurrence
légitime. \emph{Strictement croissante~:} $\varphi(n + 1)$ appartient
à $A \setminus \{\varphi(0), \dots, \varphi(n)\} \subseteq A \setminus
\{\varphi(0), \dots, \varphi(n - 1)\}$, dont le minimum est
$\varphi(n)$~; donc $\varphi(n + 1) \geq \varphi(n)$, et l'égalité est
exclue, d'où $\varphi(n+1) > \varphi(n)$. \emph{$\varphi(n) \geq n$~:}
par récurrence, $\varphi(0) \geq 0$, et $\varphi(n + 1) \geq \varphi(n)
+ 1 \geq n + 1$. \emph{L'\omterm{def:b1:logic:inj}{injectivité}} découle de la stricte
monotonie. \emph{\omterm{def:b1:logic:inj}{Surjectivité} sur $A$~:} supposons qu'un certain
$a \in A$ ne soit jamais atteint. Comme $\varphi(a + 1) \geq a + 1 >
a$, l'\omterm{def:b1:logic:sets}{ensemble} des $n$ tels que $\varphi(n) > a$ est non vide~; soit
$n$ son plus petit élément. Pour tout $k < n$, $\varphi(k) \leq a$,
donc $\varphi(k) < a$ ($a$ n'est pas atteint). Alors $a$ appartient à
$A \setminus \{\varphi(0), \dots, \varphi(n - 1)\}$ et $a < \varphi(n)$,
ce qui contredit la minimalité définissant $\varphi(n)$. Donc $\varphi$
est une \omterm{def:b1:logic:inj}{bijection} $\N \to A$, et $A$ est dénombrable.

\textbf{12.} Soit $E \preceq \N$ via une \omterm{def:b1:logic:inj}{injection} $f$~; alors $E
\approx f(E)$ (question 2). Si $f(E)$ est finie, $E$ est fini~; si
$f(E)$ est infinie, la question 11 donne $f(E) \approx \N$, donc
$E \approx \N$ par transitivité (question 1). Réciproquement, les
\omterm{def:b1:logic:sets}{ensembles} finis et les \omterm{pb:b1:logic:1}{ensembles dénombrables} s'injectent évidemment
dans $\N$. Le raccourci~: $E \preceq \N$ et $\N \preceq E$ donnent
$E \approx \N$ directement par
Cantor--Schröder--Bernstein --- aucun argument d'énumération n'est
nécessaire.

\textbf{13.} Soient $f \colon E \to \N$ et $g \colon F \to \N$ des
\omterm{def:b1:logic:inj}{injections}. Alors $(x, y) \mapsto \varphi\bigl(f(x), g(y)\bigr)$ est
une \omterm{def:b1:logic:inj}{injection} $E \times F \to \N$~: si les images coïncident,
l'\omterm{def:b1:logic:inj}{injectivité} de $\varphi$ (question 10) donne $f(x) = f(x')$ et
$g(y) = g(y')$, puis $x = x'$, $y = y'$. Pour $\Z \times \N^*$~: les
deux facteurs sont dénombrables (question 4), donc $\Z \times \N^*
\preceq \N$~; cet \omterm{def:b1:logic:sets}{ensemble} est infini (il contient $\{0\} \times \N^*$),
donc dénombrable d'après la question 12.

\textbf{14.} Tout rationnel $r$ admet une unique représentation $r =
p/q$ avec $p \in \Z$, $q \in \N^*$ et la fraction irréductible
(l'unicité est démontrée au \cref{ch:b1:arith}~; pour $r = 0$ prendre
$0/1$). L'\omterm{def:b1:logic:map}{application} $r \mapsto (p, q)$ est alors \omterm{def:b1:logic:inj}{injective}~: le
couple détermine $r = p/q$. Ainsi $\Q \preceq \Z \times \N^* \preceq \N$
d'après la question 13. Comme $\N \subseteq \Q$ donne $\N \preceq \Q$,
la question 12 (ou Cantor--Schröder--Bernstein directement) montre que
$\Q \approx \N$~: les rationnels sont dénombrables.

\textbf{15.} Pour chaque $n$, fixons une \omterm{def:b1:logic:inj}{injection} $f_n \colon E_n \to
\N$. Pour $x \in \bigcup_n E_n$, soit $n(x)$ le \emph{plus petit} $n$
tel que $x \in E_n$, et posons $u(x) = \varphi\bigl(n(x),
f_{n(x)}(x)\bigr) \in \N$. Si $u(x) = u(x')$, l'\omterm{def:b1:logic:inj}{injectivité} de
$\varphi$ donne $n(x) = n(x') = n$ et $f_n(x) = f_n(x')$, donc $x = x'$
par \omterm{def:b1:logic:inj}{injectivité} de $f_n$. La réunion s'injecte donc dans $\N$~: elle est
au plus dénombrable.

\textbf{16.} Posons $\Psi(F) = \sum_{i \in F} 2^i$ pour $F \subseteq \N$
finie ($\Psi(\emptyset) = 0$). Supposons $F \neq F'$ et soit $m$ le
plus grand élément sur lequel elles diffèrent, disons $m \in F \setminus
F'$ (quitte à échanger les noms). Les éléments $> m$ appartiennent aux
deux ou à aucune, donc ils contribuent également aux deux sommes~; en
comparant les contributions des éléments $\leq m$~:
\[
\sum_{i \in F,\, i \leq m} 2^i \geq 2^m
> 2^m - 1 = \sum_{k=0}^{m-1} 2^k
\geq \sum_{i \in F',\, i \leq m} 2^i ,
\]
en utilisant la somme géométrique de l'\cref{exo:b1:logic:4}. Donc
$\Psi(F) \neq \Psi(F')$~: $\Psi$ est \omterm{def:b1:logic:inj}{injective} et l'\omterm{def:b1:logic:sets}{ensemble des
parties} finies de $\N$ est au plus dénombrable~; il est infini (il
contient tous les singletons), donc dénombrable.

\textbf{17.} Envoyons $A \subseteq \N$ sur son indicatrice $\mathbf 1_A
\colon \N \to \{0,1\}$, $\mathbf 1_A(n) = 1$ si $n \in A$ et $0$
sinon~; envoyons $u \in \{0,1\}^{\N}$ sur $A_u = \{n \in \N : u(n) =
1\}$. Les deux \omterm{def:b1:logic:map}{applications} sont réciproques l'une de l'autre~:
$A_{\mathbf 1_A} = A$ et $\mathbf 1_{A_u} = u$ (vérifier la valeur en
chaque $n$). D'après le \cref{thm:b1:logic:inverse}, chacune est une
\omterm{def:b1:logic:inj}{bijection}~: $\mathcal{P}(\N) \approx \{0,1\}^{\N}$.

\textbf{18.} Pour tout $n$, $d(n) = 1 - \Phi(n)(n) \neq \Phi(n)(n)$,
donc les suites $d$ et $\Phi(n)$ diffèrent à l'indice $n$~: $d \neq
\Phi(n)$. Ainsi aucune $\Phi$ n'est \omterm{def:b1:logic:inj}{surjective}, et d'après la question 3
il n'existe pas non plus d'\omterm{def:b1:logic:inj}{injection} $\{0,1\}^{\N} \to \N$~:
$\{0,1\}^{\N}$ n'est pas au plus dénombrable. À travers le dictionnaire
de la question 17, une \omterm{def:b1:logic:map}{application} $\Phi \colon \N \to \{0,1\}^{\N}$ est
une \omterm{def:b1:logic:map}{application} $f \colon \N \to \mathcal{P}(\N)$, et $d$ correspond à
l'\omterm{def:b1:logic:sets}{ensemble} $D = \{n : n \notin f(n)\}$ (en effet $d(n) = 1 \iff
\Phi(n)(n) = 0 \iff n \notin f(n)$)~: l'argument diagonal \emph{est} la
démonstration de Cantor de l'\cref{exo:b1:logic:11} pour $E = \N$.

\textbf{19.} Écrivons $x_n = 0.d_1(n)\,d_2(n)\,d_3(n)\dots$ sous forme
propre et définissons $\delta_n = 5$ si $d_n(n) \neq 5$, $\delta_n = 6$
si $d_n(n) = 5$, puis $x = 0.\delta_1\delta_2\delta_3\dots$ Ce
développement n'utilise que les chiffres $5$ et $6$, donc il ne se
termine pas par une infinité de $9$~: c'est le développement propre d'un
réel $x \in \intco01$. Pour chaque $n$, les $n$-ièmes décimales de $x$
et de $x_n$ diffèrent ($\delta_n \neq d_n(n)$ par construction)~; comme
les développements propres sont uniques, $x \neq x_n$. Ainsi aucune
suite n'épuise $\intco01$~: d'après la question 3 de nouveau,
$\intco01$ n'est pas au plus dénombrable.

\textbf{20.} $\intco01 \subseteq \R$, donc une \omterm{def:b1:logic:inj}{injection} $\R \to \N$
se restreindrait en une \omterm{def:b1:logic:inj}{injection} de $\intco01$, ce qui contredit la
question 19~: $\R$ n'est pas dénombrable. Si $\R \setminus \Q$ était au
plus dénombrable, alors $\R = \Q \cup (\R \setminus \Q)$ serait une
réunion de deux \omterm{def:b1:logic:sets}{ensembles} au plus dénombrables, donc au plus dénombrable
d'après la question 15 (prendre $E_0 = \Q$, $E_n = \R \setminus \Q$ pour
$n \geq 1$) --- contradiction. Donc les irrationnels ne forment pas un
\omterm{pb:b1:logic:1}{ensemble dénombrable}. Précisément~: dans $\R$, les rationnels forment un
\omterm{pb:b1:logic:1}{ensemble dénombrable} tandis que leur complémentaire ne l'est pas~;
aucune \omterm{def:b1:logic:inj}{bijection} ne pourra jamais apparier $\R \setminus \Q$ avec $\Q$
--- il y a strictement «~plus~» d'irrationnels que de rationnels, bien
que les deux \omterm{def:b1:logic:sets}{ensembles} soient infinis et tous deux denses.

\textbf{21.} $p/q$ (avec $q \neq 0$) est racine de $qX - p$, un
polynôme non nul à coefficients entiers. $\sqrt 2$ est racine de
$X^2 - 2$. Pour $x = \sqrt 2 + \sqrt 3$~: $x^2 = 5 + 2\sqrt 6$, donc
$x^2 - 5 = 2\sqrt 6$ et $(x^2 - 5)^2 = 24$, c'est-à-dire
\[
x^4 - 10x^2 + 1 = 0 :
\]
$\sqrt 2 + \sqrt 3$ est racine de $X^4 - 10X^2 + 1$.

\textbf{22.} Envoyons $P = a_0 + a_1X + \dots + a_nX^n$ (de degré
$\leq n$, à coefficients entiers) sur $(a_0, \dots, a_n) \in
\Z^{n+1}$~: c'est \omterm{def:b1:logic:inj}{injectif}, puisqu'un polynôme est déterminé par ses
coefficients. Par récurrence sur $n$~: $\Z^1 = \Z$ est dénombrable
(question 4), et $\Z^{n+2} \approx \Z^{n+1} \times \Z$ est au plus
dénombrable d'après la question 13. Chaque \omterm{def:b1:logic:sets}{ensemble} de polynômes
entiers de degré borné est donc au plus dénombrable~; il est infini (il
contient les constantes), donc dénombrable d'après la question 12.

\textbf{23.} L'\omterm{def:b1:logic:sets}{ensemble} de tous les polynômes entiers est $\bigcup_{n
\in \N} \{P : \deg P \leq n,\ P \text{ à coefficients entiers}\}$, une
réunion dénombrable d'\omterm{pb:b1:logic:1}{ensembles dénombrables}~: il est au plus
dénombrable d'après la question 15, infini, donc dénombrable.

\textbf{24.} Pour chaque polynôme entier non nul $P$, l'\omterm{def:b1:logic:sets}{ensemble} des
racines $R_P = \{x \in \R : P(x) = 0\}$ est fini (au plus $\deg P$
éléments, admis). D'après la question 23, les polynômes entiers non nuls
peuvent être énumérés $P_0, P_1, P_2, \dots$~; alors $\mathcal{A} =
\bigcup_{n \in \N} R_{P_n}$ est une réunion dénombrable d'\omterm{def:b1:logic:sets}{ensembles}
finis (donc au plus dénombrables)~: il est au plus dénombrable d'après
la question 15. Il contient $\Q$ (question 21), donc il est infini~:
$\mathcal{A}$ est dénombrable.

\textbf{25.} Si $\R \setminus \mathcal{A}$ était au plus dénombrable,
$\R = \mathcal{A} \cup (\R \setminus \mathcal{A})$ serait au plus
dénombrable (question 15), ce qui contredit la question 20. Donc les
\omterm{pb:b1:logic:1}{nombres transcendants} existent et forment même un \omterm{def:b1:logic:sets}{ensemble} non
dénombrable, tandis que les \omterm{pb:b1:logic:1}{nombres algébriques} --- qui comprennent
tout nombre construit à partir des entiers par radicaux --- ne forment
qu'un maigre squelette dénombrable à l'intérieur de $\R$. Bilan de
l'architecture~: les questions 1 à 3 mettent en place le langage de la
comparaison~; Cantor--Schröder--Bernstein (questions 5 à 9) permet de
démontrer l'équipotence au moyen de deux \omterm{def:b1:logic:inj}{injections} faciles au lieu
d'une \omterm{def:b1:logic:inj}{bijection} astucieuse, et a servi pour $\intcc01 \approx
\intoo01$, pour $\Q$ et dans toute la partie V~; la \omterm{def:b1:logic:inj}{bijection} de
couplage (question 10) a alimenté les produits et les réunions
dénombrables (questions 13 et 15), qui ont à leur tour alimenté $\Q$,
les polynômes entiers et $\mathcal{A}$~; l'argument diagonal (questions
18 et 19) a fourni l'unique inégalité stricte $\N \prec \R$ qui rend
toute l'histoire non triviale. La conclusion de Cantor est
philosophiquement frappante~: la démonstration n'exhibe aucun \omterm{pb:b1:logic:1}{nombre
transcendant}, et pourtant elle montre qu'au sens de l'équipotence
\emph{presque tout} nombre réel est \omterm{pb:b1:logic:1}{transcendant}. Nommer un
\omterm{pb:b1:logic:1}{transcendant} précis --- $\pi$ ou $\eu$ --- a exigé des mathématiques
entièrement différentes et des décennies de travail supplémentaire.
\end{solution}
