\section*{Chapitre \ref{ch:b1:reals} --- Nombres réels}

\begin{solution}{exo:b1:reals:1}
$A$~: tout élément est $\leq 1$ et $1 \in A$~: $\sup A = \max A = 1$.
Minorants~: $0$ minore~; pour $\varepsilon > 0$, Archimède fournit
$n$ avec $\frac 1n < \varepsilon$, donc aucun nombre strictement
positif ne minore $A$~: $\inf A = 0$, non atteinte (pas de min).

$B$~: les termes $0, -\frac12, \frac23, -\frac34, \frac45, \dots$ Les
termes d'indice pair $\frac{n}{n+1}$ ($n$ pair) croissent vers $1$
sans l'atteindre~; ceux d'indice impair $-\frac{n}{n+1}$ décroissent
vers $-1$. Donc $\sup B = 1$ et $\inf B = -1$, ni l'une ni l'autre
atteinte~: pas de max, pas de min. (Bornes~: $\abs{b}
< 1$ pour tout $b \in B$~; et $\frac{n}{n+1} = 1 - \frac{1}{n+1} > 1 -
\varepsilon$ pour $n$ grand, de même en dessous.)

$C = \intoo{-\sqrt 3}{\sqrt 3}$~: $\sup C = \sqrt 3$, $\inf C =
-\sqrt 3$, ni l'une ni l'autre atteinte.
\end{solution}

\begin{solution}{exo:b1:reals:2}
Écrivons $x = \lfloor x \rfloor + u$, $y = \lfloor y \rfloor + v$ avec
$u, v \in \intco{0}{1}$. Alors $x + y = \lfloor x \rfloor + \lfloor y
\rfloor + (u + v)$ avec $u + v \in \intco{0}{2}$. Si $u + v < 1$,
$\lfloor x + y\rfloor = \lfloor x\rfloor + \lfloor y \rfloor$~; si
$1 \leq u + v < 2$, $\lfloor x+y \rfloor = \lfloor x \rfloor +
\lfloor y \rfloor + 1$. Les deux cas se produisent~: $(x, y) = (0.2,\,
0.3)$ donne l'égalité de gauche, $(0.7,\, 0.8)$ celle de droite.
\end{solution}

\begin{solution}{exo:b1:reals:3}
Posons $k = \lfloor x \rfloor$, de sorte que $k \leq x < k + 1$. En
multipliant par $n$~: $nk \leq nx < nk + n$, et en prenant les \omterm{thm:b1:reals:floor}{parties
entières} (opération croissante, du côté des entiers)~: $nk \leq
\lfloor nx \rfloor \leq nk + n - 1$. En divisant par $n$~: $k \leq
\frac{\lfloor nx \rfloor}{n} < k + 1$, donc la \omterm{thm:b1:reals:floor}{partie entière}
extérieure vaut $k$.
\end{solution}

\begin{solution}{exo:b1:reals:4}
Tout élément de $A$ est dans $B$, donc $\sup B$ majore $A$~: par
conséquent $\sup A \leq \sup B$ ($\sup A$ est le \emph{plus petit}
des \omterm{def:b1:reals:bounds}{majorants}). Symétriquement $\inf B \leq \inf A$. Enfin $\inf A
\leq \sup A$ car $A$ est non vide~: tout $a \in A$ se place entre les
deux.
\end{solution}

\begin{solution}{exo:b1:reals:5}
Posons $s = \sup A$, $t = \sup B$. Tout $a + b \leq s + t$~:
\omterm{def:b1:reals:bounds}{majorant}. Pour $\varepsilon > 0$, choisissons $a > s -
\frac\varepsilon2$ et $b > t - \frac\varepsilon2$
(\cref{prop:b1:reals:epsilon})~: alors $a + b > s + t - \varepsilon$.
D'après la caractérisation par $\varepsilon$, $\sup(A+B) = s + t$.

Pour $-A$~: $m$ majore $-A$ $\iff$ $-m$ minore $A$~; le plus petit
des \omterm{def:b1:reals:bounds}{majorants} de $-A$ correspond donc au plus grand des minorants de
$A$~: $\sup(-A) = -\inf A$.
\end{solution}

\begin{solution}{exo:b1:reals:6}
Pour tout $x$~: $f(x) + g(x) \leq \sup f + \sup g$~; en prenant la
\omterm{def:b1:reals:bounds}{borne supérieure} du membre de gauche, on obtient l'inégalité. Exemple
strict~: $E = \{0, 1\}$, $f = \mathbf{1}_{\{0\}}$ (valeur $1$ en $0$,
$0$ ailleurs), $g = \mathbf{1}_{\{1\}}$~: $\sup(f + g) = 1 < 2 = \sup
f + \sup g$.

Pas de contradiction avec l'\cref{exo:b1:reals:5}~: là-bas, $a \in A$
et $b \in B$ varient \emph{indépendamment}~; ici le même $x$ alimente
$f$ et $g$ --- l'\omterm{def:b1:logic:sets}{ensemble} $\{f(x) + g(x) : x \in E\}$ est plus petit
que l'\omterm{def:b1:logic:sets}{ensemble} $\{f(x) + g(y) : x, y \in E\}$.
\end{solution}

\begin{solution}{exo:b1:reals:7}
$\sqrt 6$ est irrationnel~: $6 = 2 \times 3$ n'est pas un carré
parfait, et $v_2(6q^2) = 1 + 2v_2(q)$ impair empêche $6q^2 = r^2$
(comme dans l'\cref{exo:b1:arith:7}). Supposons maintenant $x = \sqrt
2 + \sqrt 3 \in \Q$. Alors $x^2 = 5 + 2\sqrt 6 \in \Q$, donc $\sqrt 6
= \frac{x^2 - 5}{2} \in \Q$~: contradiction. Donc $\sqrt 2 + \sqrt 3
\notin \Q$.
\end{solution}

\begin{solution}{exo:b1:reals:8}
Soit $x < y$. Choisissons $n \in \N$ avec $2^n > \frac{1}{y - x}$
(Archimède~: $2^n \geq n + 1$ par une récurrence facile, donc une
puissance de $2$ dépasse tout réel). Alors, comme dans la
démonstration du \cref{thm:b1:reals:density} avec $2^n$ à la place de
$n$~: $m = \lfloor 2^n x \rfloor + 1$ vérifie $x < \frac{m}{2^n} < y$.
Donc $D$ est dense.
\end{solution}

\begin{solution}{exo:b1:reals:9}
\begin{enumerate}
  \item Supposons $\alpha > 0$. D'abord, $\alpha \in G$. Sinon~: par
        la caractérisation par $\varepsilon$ de la \omterm{def:b1:reals:bounds}{borne inférieure}
        avec $\varepsilon = \alpha$, il existe $g \in G$ avec $\alpha
        < g < 2\alpha$ (strict à gauche puisque $\alpha \notin G$)~;
        puis, avec $\varepsilon = g - \alpha$, il existe $h \in G$
        avec $\alpha < h < g$. Alors $g - h \in G$ et $0 < g - h < g -
        \alpha < \alpha$~: un élément de $G \cap \intoo{0}{+\infty}$
        en dessous de sa \omterm{def:b1:reals:bounds}{borne inférieure}, absurde. Donc $\alpha \in
        G$, et $\alpha\Z \subseteq G$ ($G$ est un \omterm{def:b1:structures:group}{groupe}).
        Réciproquement, pour $x \in G$, posons $k = \lfloor x/\alpha
        \rfloor$~: alors $x - k\alpha \in G$ et $0 \leq x - k\alpha <
        \alpha$, et la définition de $\alpha$ force $x - k\alpha = 0$.
        Donc $G = \alpha\Z$.
  \item Supposons $\alpha = 0$, et soit $x < y$. Il existe $g \in G$
        avec $0 < g < y - x$. Le multiple $kg$ avec $k = \lfloor x/g
        \rfloor + 1$ vérifie $x < kg \leq x + g < y$, et $kg \in G$~:
        densité.
\end{enumerate}
$G = \Z + \sqrt 2\,\Z$ est un \omterm{def:b1:structures:subgroup}{sous-groupe} de $(\R, +)$. Il n'est pas
de la forme $\alpha\Z$~: sinon $1 = p\alpha$ et $\sqrt 2 = q\alpha$
($p, q \in \Z$) donneraient $\sqrt 2 = \frac qp \in \Q$,
contradiction. D'après la dichotomie, $G$ est dense dans $\R$.
\end{solution}

\begin{solution}{exo:b1:reals:10}
Posons $s = \sup A > 0$, $t = \sup B > 0$. Pour $a \in A$, $b \in B$~:
$ab \leq st$ (produit d'inégalités entre nombres \emph{positifs}).
Pour $0 < \varepsilon < \min(s, t)$~: choisissons $a > s - \varepsilon$
et $b > t - \varepsilon$~; alors
\[
ab > (s - \varepsilon)(t - \varepsilon)
= st - \varepsilon(s + t) + \varepsilon^2
> st - \varepsilon (s + t),
\]
et $\varepsilon(s+t)$ peut être rendu arbitrairement petit~: d'après
la caractérisation par $\varepsilon$ (sous la forme~: aucun nombre $<
st$ ne majore $AB$), $\sup AB = st$.

La positivité est essentielle~: $A = B = \{-1, 0\}$ donne $AB = \{0,
1\}$, $\sup AB = 1$, alors que $\sup A \cdot \sup B = 0 \times 0 = 0$.
\end{solution}

\begin{solution}{exo:b1:reals:11}
Écrivons $s = \sup A$, $i = \inf A$. Pour $a, a' \in A$~: $a \leq
s$ et $a' \geq i$ donnent $a - a' \leq s - i$~; par symétrie
$\abs{a - a'} \leq s - i$, donc $s - i$ majore l'\omterm{def:b1:logic:sets}{ensemble} des
écarts. Pour $\varepsilon > 0$, choisissons $a > s -
\frac\varepsilon2$ et $a' < i + \frac\varepsilon2$
(\cref{prop:b1:reals:epsilon} et son miroir pour la \omterm{def:b1:reals:bounds}{borne
inférieure})~: alors $\abs{a - a'} \geq a - a' > s - i -
\varepsilon$. D'après la caractérisation par $\varepsilon$,
$\operatorname{diam} A = s - i$.

Tout $a \in A$ vérifie $i \leq a \leq s$, donc $A \subseteq
\intcc{i}{s}$, \omterm{prop:b1:reals:intervals}{intervalle} fermé de longueur $\operatorname{diam}
A$. Si un \omterm{prop:b1:reals:intervals}{intervalle} fermé $\intcc{u}{v}$ contient $A$, alors $v$
est un \omterm{def:b1:reals:bounds}{majorant} et $u$ un minorant de $A$, donc $u \leq i$ et $v
\geq s$~: $\intcc{i}{s} \subseteq \intcc{u}{v}$. Ainsi
$\intcc{i}{s}$ est le plus petit.
\end{solution}

\begin{solution}{exo:b1:reals:12}
$0 \in E$, donc $E \neq \emptyset$. Si $x > \max(1, y)$ alors $x^2
> x > y$, donc $E$ est majoré par $\max(1, y)$~: $s = \sup E$
existe (\cref{thm:b1:reals:sup}), et $s \geq \min(1, y) > 0$
car $\min(1, y) \in E$~: en effet si $y \geq 1$ alors $1^2 = 1
\leq y$, et si $y < 1$ alors $y^2 < y$.

\emph{$s^2 < y$ est impossible.} Choisissons $0 < h < 1$ avec $h <
\frac{y - s^2}{2s + 1}$. Alors
\[
(s + h)^2 = s^2 + 2sh + h^2 \leq s^2 + (2s + 1)h < y ,
\]
donc $s + h \in E$, ce qui contredit le fait que $s$ majore $E$.

\emph{$s^2 > y$ est impossible.} Choisissons $0 < h < s$ avec $h <
\frac{s^2 - y}{2s}$. Alors $(s - h)^2 = s^2 - 2sh + h^2 > s^2 -
2sh > y$~; tout $x \in E$ vérifie $x^2 \leq y < (s - h)^2$, donc
$x < s - h$ (les deux sont $\geq 0$)~: $s - h$ est un \omterm{def:b1:reals:bounds}{majorant} de
$E$ plus petit que $s$, ce qui contredit la minimalité.

Par conséquent $s^2 = y$. Unicité~: si $0 < s < s'$ alors $s^2 <
s'^2$, donc deux racines positives distinctes ne peuvent avoir le
même carré $y$. Croissance~: si $0 < y < y'$, alors $\sqrt y \neq
\sqrt{y'}$, et $\sqrt y > \sqrt{y'}$ donnerait $y > y'$ en élevant
au carré~: donc $\sqrt y < \sqrt{y'}$.
\end{solution}

\begin{solution}{pb:b1:reals:1}
\textbf{1.} Pour $\frac 18$~: $10 = 8 \cdot 1 + 2$, $20 = 8 \cdot 2
+ 4$, $40 = 8 \cdot 5 + 0$~; chiffres $1, 2, 5$, reste $0$, puis
que des zéros~: $\frac 18 = 0.125$. Pour $\frac 17$~: $10 = 7 \cdot
1 + 3$, $30 = 7 \cdot 4 + 2$, $20 = 7 \cdot 2 + 6$, $60 = 7 \cdot 8
+ 4$, $40 = 7 \cdot 5 + 5$, $50 = 7 \cdot 7 + 1$~: chiffres $1, 4,
2, 8, 5, 7$, restes $3, 2, 6, 4, 5, 1$. Le reste est revenu à $r =
1$, donc les six étapes se répètent à l'identique indéfiniment~:
$\frac 17 = 0.(142857)$, les restes parcourant le cycle $1, 3, 2,
6, 4, 5$.

\textbf{2.} $\frac 13$ en base $2$ ($r_0 = 1$)~: $2 = 3 \cdot 0 +
2$, $4 = 3 \cdot 1 + 1$, et $r = 1$ réapparaît~: $\frac 13 =
(0.\overline{01})_2$. $\frac{5}{16}$ en base $2$~: $10 = 16 \cdot
0 + 10$, $20 = 16 \cdot 1 + 4$, $8 = 16 \cdot 0 + 8$, $16 = 16
\cdot 1 + 0$~: $\frac{5}{16} = (0.0101)_2$, développement fini.
$\frac 12$ en base $3$~: $3 = 2 \cdot 1 + 1$, et $r = 1$ réapparaît
aussitôt~: $\frac 12 = (0.\overline{1})_3$.

\textbf{3.} Le reste de l'algorithme après $N$ étapes est $r_N =
b^N p \bmod q$ (démontré formellement à la question 14~; ici c'est
l'observation que chaque étape multiplie le reste par $b$ et le
réduit modulo $q$). Tous les chiffres suivants sont nuls si et
seulement si un certain $r_N = 0$, c'est-à-dire $q \mid b^N p$~;
comme $\gcd(p, q) = 1$, le \omterm{thm:b1:arith:gauss}{lemme de Gauss} donne $q \mid b^N$. Si $q
\mid b^N$, tout facteur premier de $q$ \omterm{def:b1:arith:divides}{divise} $b^N$, donc \omterm{def:b1:arith:divides}{divise}
$b$ (primalité). Réciproquement, si tout premier de $q = p_1^{a_1}
\cdots p_r^{a_r}$ \omterm{def:b1:arith:divides}{divise} $b$, alors avec $A = \max_i a_i$ chaque
$p_i^{a_i}$ \omterm{def:b1:arith:divides}{divise} $b^A$, et les $p_i^{a_i}$ sont \omterm{cor:b1:arith:bezout}{premiers entre
eux} deux à deux, donc $q \mid b^A$. Pour $q = 20 = 2^2 \cdot 5$~:
les deux premiers \omterm{def:b1:arith:divides}{divisent} $10$ ($\frac{1}{20} = 0.05$), mais $2
\nmid 3$, donc $\frac{1}{20}$ se répète indéfiniment en base $3$.

\textbf{4.} $1^2 = 1 < 2 < 4 = 2^2$ donne $s_0 = 1$. Puis $1.4^2
= 1.96 < 2 < 2.25 = 1.5^2$~: $\lfloor 10\sqrt 2 \rfloor = 14$,
$s_1 = 1.4$. Ensuite $1.41^2 = 1.9881 < 2 < 2.0164 = 1.42^2$~: $s_2
= 1.41$~; $1.414^2 = 1.999396 < 2 < 2.002225 = 1.415^2$~: $s_3 =
1.414$~; $1.4142^2 = 1.99996164 < 2 < 2.00024449 = 1.4143^2$~: $s_4
= 1.4142$. Dans chaque cas les inégalités affichées disent
exactement $s_n \leq \sqrt 2 < s_n + 10^{-n}$, ce qui est la
définition de la \omterm{thm:b1:reals:floor}{partie entière} de $10^n \sqrt 2$.

\textbf{5.} $A_0 = \lfloor x \rfloor = 0$ puisque $0 \leq x < 1$.
De $A_{n-1} \leq b^{n-1} x < A_{n-1} + 1$, en multipliant par $b$~:
\[
b\,A_{n-1} \leq b^n x < b\,A_{n-1} + b .
\]
L'entier $b\,A_{n-1}$ est $\leq b^n x$, donc $b\,A_{n-1} \leq
A_n$~; et $b^n x < b\,A_{n-1} + b$ avec $b\,A_{n-1} + b$ entier
force $A_n \leq b\,A_{n-1} + b - 1$. Donc $0 \leq d_n =
A_n - b\,A_{n-1} \leq b - 1$~: c'est bien un chiffre.

\textbf{6.} Télescopage~: $d_k b^{-k} = A_k b^{-k} - A_{k-1}
b^{-(k-1)}$, donc
\[
\sum_{k=1}^n d_k b^{-k} = A_n b^{-n} - A_0 = s_n .
\]
En divisant $A_n \leq b^n x < A_n + 1$ par $b^n$, on obtient $s_n
\leq x < s_n + b^{-n}$.

\textbf{7.} Récurrence~: $b^0 = 1 \geq 1$, et $b^{n+1} = b \cdot
b^n \geq 2(n + 1) \geq n + 2$. Croissance~: $s_n - s_{n-1} = d_n
b^{-n} \geq 0$. Chaque $s_n \leq x$ (question 6)~: $x$ est un
\omterm{def:b1:reals:bounds}{majorant} de $\{s_n\}$. Pour $\varepsilon > 0$, la \omterm{thm:b1:reals:archimedes}{propriété
d'Archimède} fournit $n$ avec $n + 1 > \frac1\varepsilon$, donc
$b^{-n} < \varepsilon$, et alors $s_n > x - b^{-n} > x -
\varepsilon$ d'après la question 6. D'après la
\cref{prop:b1:reals:epsilon}, $x = \sup_n s_n$.

\textbf{8.} Supposons $d_k = b - 1$ pour tout $k > N$. Pour $n >
N$, la somme géométrique finie donne
\[
s_n = s_N + (b - 1)\sum_{k=N+1}^{n} b^{-k}
= s_N + b^{-N} - b^{-n} .
\]
Donc $x \geq s_n = s_N + b^{-N} - b^{-n}$ pour tout $n$~; en
laissant le dernier terme descendre en dessous de tout
$\varepsilon$ (question 7), $x \geq s_N + b^{-N}$. Mais la question
6 au rang $N$ dit $x < s_N + b^{-N}$~: contradiction. La suite
$(d_n)$ est propre.

\textbf{9.} Majoration~: $t_n \leq (b-1)\sum_{k=1}^n b^{-k} = 1 -
b^{-n} < 1$, et $(t_n)$ est croissante au sens large, donc $y =
\sup t_n$ existe avec $0 \leq y \leq 1$. Fixons $n$. Pour
l'encadrement~: $t_n \leq y$ est clair. Par propreté, choisissons
$m > n$ avec $e_m \leq b - 2$. Pour $p \geq m$~:
\[
t_p - t_n = \sum_{k=n+1}^{p} e_k b^{-k}
\leq (b^{-n} - b^{-p}) - b^{-m} < b^{-n} - b^{-m},
\]
la somme centrale perdant au moins $b^{-m}$ face au maximum
tout-en-$(b-1)$~; pour $p \leq m$, on a aussi $t_p \leq t_m \leq
t_n + b^{-n} - b^{-m}$ (croissance plus le cas $p = m$). Donc tout
$t_p \leq t_n + b^{-n} - b^{-m}$, d'où $y \leq t_n + b^{-n} -
b^{-m} < t_n + b^{-n}$. (Avec $n = 0$~: $y < 1$, donc $y \in
\intco{0}{1}$.) Enfin $b^n t_n = \sum_{k \leq n} e_k b^{n-k}$ est
un entier, et $b^n t_n \leq b^n y < b^n t_n + 1$~: donc $\lfloor
b^n y \rfloor = b^n t_n$. Enfin les chiffres de $y$~: $d_n(y) = b^n
t_n - b \cdot b^{n-1} t_{n-1} = b^n(t_n - t_{n-1}) = e_n$.

\textbf{10.} La question 9 dit~: (valeur d'une suite) a pour
chiffres (la suite)~; les questions 5 à 8 disent~: (les chiffres de
$x$) forment une suite propre dont les troncatures ont pour \omterm{def:b1:reals:bounds}{borne
supérieure} $x$ (question 7). Les deux \omterm{def:b1:logic:map}{applications} se composent
donc en l'identité dans les deux sens~: ce sont des \omterm{def:b1:logic:inj}{bijections}
réciproques l'une de l'autre entre $\intco{0}{1}$ et les suites
propres. Si deux suites propres avaient la même valeur, appliquer
l'\omterm{def:b1:logic:map}{application} «~chiffres~» les rendrait égales~: unicité. C'est le
\emph{théorème du développement en base $b$}.

\textbf{11.} Soit $e_n = b - 1$ pour $n > M$, avec $M \geq 0$
minimal. Comme à la question 8, $t_n = t_M + b^{-M} - b^{-n}$ pour
$n \geq M$, donc la valeur est $\sup t_n = t_M + b^{-M}$. Si $M =
0$ la valeur est $0 + 1 = 1$~: en base $10$, $0.999\dots = 1$
exactement --- et non approximativement. Si $M \geq 1$, la
minimalité donne $e_M \leq b - 2$, et la valeur est
\[
t_M + b^{-M} = \frac{b^M t_M + 1}{b^M} \in \intoo{0}{1},
\]
une fraction $b$-adique, dont le développement \emph{propre} est
$e_1 \dots e_{M-1}\,(e_M + 1)\,000\dots$ (une suite qui s'arrête
est propre, et sa valeur est le même nombre). Réciproquement, un
réel ayant deux représentations doit en avoir une impropre (la
propreté fixe la représentation, question 10), donc être de cette
forme. Et chaque $m/b^N \in \intoo{0}{1}$, écrit avec un dernier
chiffre non nul $d_N$, possède bien le jumeau impropre $d_1 \dots
d_{N-1}(d_N - 1)(b-1)(b-1)\dots$~: ce sont exactement les fractions
$b$-adiques qui portent deux noms, tous les autres réels un seul.

\textbf{12.} Disons que les suites coïncident jusqu'à $m - 1$, avec
la troncature commune $P = s_{m-1}$, et que $d_m < e_m$. D'après la
question 9 (majoration stricte au rang $m$), $x < P + d_m b^{-m} +
b^{-m} = P + (d_m + 1)b^{-m} \leq P + e_m b^{-m} \leq y$, la
dernière étape parce que $P + e_m b^{-m}$ est la troncature $t_m
\leq y$ de $y$. Donc $d_m < e_m \implies x < y$~; en échangeant les
rôles, $e_m < d_m \implies y < x$~; et comme les suites diffèrent
en $m$, l'un des deux cas se produit. Les deux sens s'ensuivent.

\textbf{13.} Posons $z = bx - A_1 \in \intco{0}{1}$ (en effet $A_1
\leq bx < A_1 + 1$). Pour $n \geq 0$~: $b^n z = b^{n+1} x - b^n
A_1$ avec $b^n A_1 \in \Z$, donc d'après $\lfloor u - K \rfloor =
\lfloor u \rfloor - K$ ($K$ entier),
\[
A_n(z) = A_{n+1}(x) - b^n A_1(x) .
\]
Donc $d_n(z) = A_n(z) - b\,A_{n-1}(z) = A_{n+1} - b^n A_1 -
b\,A_n + b^n A_1 = d_{n+1}(x)$. Ainsi la partie fractionnaire de
$bx$ porte les chiffres décalés~; en itérant $m$ fois, la partie
fractionnaire de $b^m x$ a pour chiffres $(d_{n+m})_{n \geq 1}$.

\textbf{14.} Division euclidienne~: $b^n p = q\,Q_n + r_n$ avec $0
\leq r_n < q$. En divisant par $q$~: $b^n x = Q_n + \frac{r_n}{q}$
avec $0 \leq \frac{r_n}{q} < 1$, donc $Q_n = \lfloor b^n x \rfloor
= A_n$, ce qui donne $A_n = \frac{b^n p - r_n}{q}$. Pour la
récurrence~: $b^n p = b(q\,A_{n-1} + r_{n-1}) = q\,(b\,A_{n-1}) +
b\,r_{n-1}$, donc $b^n p$ et $b\,r_{n-1}$ diffèrent d'un multiple
de $q$~: $r_n = (b\,r_{n-1}) \bmod q$.

\textbf{15.} Divisons $b\,r_{n-1}$ par $q$~: $b\,r_{n-1} = q\,c +
r_n$ avec $c = \lfloor b\,r_{n-1}/q \rfloor$. En substituant dans
la formule affichée de la question 14~: $b^n p = q(b\,A_{n-1} + c)
+ r_n$, et l'unicité de la division euclidienne identifie $A_n =
b\,A_{n-1} + c$, c'est-à-dire $d_n = c = \lfloor b\,r_{n-1}/q
\rfloor$. Le chiffre $n$ ne dépend que de $r_{n-1}$ --- la boucle
de la division posée de la partie I, désormais certifiée.

\textbf{16.} Les $q + 1$ restes $r_0, \dots, r_q$ prennent leurs
valeurs dans l'\omterm{def:b1:logic:sets}{ensemble} à $q$ éléments $\intint{0}{q-1}$~: d'après
le \omterm{cor:b1:counting:pigeonhole}{principe des tiroirs} (\cref{cor:b1:counting:pigeonhole}) deux
coïncident, disons $r_N = r_{N+T}$ avec $0 \leq N < N + T \leq q$.
Comme $r_n$ détermine $r_{n+1}$ (question 14), une récurrence donne
$r_{n+T} = r_n$ pour tout $n \geq N$~; et comme $r_{n-1}$ détermine
$d_n$ (question 15), $d_{n+T} = d_n$ pour tout $n \geq N + 1$. Le
développement de tout rationnel est périodique à partir d'un certain
rang, avec prépériode $\leq q$ et période $\leq q$.

\textbf{17.} Les chiffres de la partie fractionnaire de $b^T y$ sont
$(d_{n+T}) = (d_n)$ (lemme de décalage, puis pure périodicité)~: la
même suite propre que $y$. D'après la question 10 les valeurs sont
égales~: $b^T y - \lfloor b^T y \rfloor = y$, donc $(b^T - 1)\,y =
\lfloor b^T y \rfloor = A_T \in \N$ et
\[
y = \frac{A_T}{b^T - 1} ,
\]
rationnel de dénominateur divisant $b^T - 1$~; le numérateur $A_T$
est l'entier dont les chiffres en base $b$ sont $d_1 \dots d_T$.
Vérification~: $0.(142857) = \frac{142857}{999999}$, et $142857
\times 7 = 999999$, donc c'est bien $\frac 17$.

\textbf{18.} Si $d_{n+T} = d_n$ pour $n > N$, la partie
fractionnaire $z$ de $b^N x$ a pour chiffres $(d_{N+n})_{n\geq1}$
(lemme de décalage), qui sont purement périodiques~; d'après la
question 17, $z \in \Q$. Alors $b^N x = A_N + z$ donne $x = (A_N +
z)/b^N \in \Q$. Avec la question 16~: $x$ rationnel $\iff$
développement périodique à partir d'un certain rang. Le membre de
droite mentionne la base, celui de gauche non~: la périodicité dans
une base équivaut à la rationalité, donc à la périodicité dans
toutes les bases.

\textbf{19.} Pour $x = \frac 1q$, $r_n = b^n \bmod q$. Si
$\gcd(b, q) = 1$, alors $r_T = r_0 = 1$ si et seulement si $b^T
\equiv 1 \pmod q$~; un tel $T$ existe (les tiroirs donnent $b^i
\equiv b^j$, $i < j$, et $b$ est inversible modulo $q$, donc
$b^{j-i} \equiv 1$), et le plus petit --- l'ordre multiplicatif ---
rend les restes, donc les chiffres, purement périodiques de période
$T$. Aucune période plus petite n'est possible~: une période $T'$
donnerait $(b^{T'} - 1)\frac1q \in \N$ (question 17), c'est-à-dire
$q \mid b^{T'} - 1$. Pour $q = 7$, $b = 10$~: $10 \equiv 3$, $10^2
\equiv 2$, $10^3 \equiv 6$, $10^4 \equiv 4$, $10^5 \equiv 5$, $10^6
\equiv 1 \pmod 7$~: ordre $6$, et en effet $\frac 17$ est de
période six.

\textbf{20.} La suite comporte une infinité de $0$ (les chiffres de
$x^*$ sont presque tous nuls), donc elle est propre, et $x^*$ est
bien défini (question 9). Supposons les chiffres périodiques de
période $T$ au-delà de $N$. Une infinité de chiffres valent $1$ (un
par nombre triangulaire), donc un $1$ se trouve à une position $j >
N$~; la périodicité place alors un $1$ à toute position $j + kT$~:
à partir de $j$, les écarts entre $1$ consécutifs valent au plus
$T$. Mais les $1$ se trouvent exactement aux nombres triangulaires,
dont les écarts consécutifs $\frac{(j+1)(j+2)}{2} -
\frac{j(j+1)}{2} = j + 1$ finissent par dépasser $T$~:
contradiction. La suite n'est pas périodique à partir d'un certain
rang, donc d'après la question 18, $x^* \notin \Q$ ---
l'irrationalité se lit dans le seul rythme des chiffres.

\textbf{21.} Étant donné $x < y$, la question 7 fournit $n$ avec
$b^{-n} < y - x$~; posons $m = \lfloor b^n x \rfloor + 1$. Alors
$b^n x < m \leq b^n x + 1 < b^n y$, donc $x < \frac{m}{b^n} < y$~:
densité, et pour toutes les bases à la fois ($b = 2$ redonne
l'\cref{exo:b1:reals:8}). Pour $\frac pq \in \intoo{0}{1}$ en base
$b = q$~: le premier chiffre est $\lfloor q \cdot \frac pq \rfloor
= p$ et la partie fractionnaire de $q \cdot \frac pq = p$ est
$0$~: tous les chiffres suivants sont nuls, développement fini
$\frac pq = (0.p)_q$. Être fini dépend de la base~; la périodicité
--- la rationalité --- non (question 18).

\textbf{22.} Chaque $e_k \in \{1, 2\}$ est un chiffre de la base
$10$, et la suite ne finit jamais par une infinité de $9$~: elle est
propre. Sa valeur $y$ appartient à $\intco{0}{1}$ et a pour chiffres
exactement les $(e_k)$ (question 9). Fixons $k$~: le $k$-ième
chiffre de $y$ est $e_k$, choisi $\neq$ du $k$-ième chiffre de
$x_k$, donc les suites propres de $y$ et de $x_k$ diffèrent, donc $y
\neq x_k$ (question 10~: le codage est \omterm{def:b1:logic:inj}{injectif}). Ainsi $y$ ne
figure dans aucune liste~: aucune \omterm{def:b1:logic:map}{application} $\N^* \to
\intco{0}{1}$ n'est \omterm{def:b1:logic:inj}{surjective}. Les réels, contrairement aux
rationnels, ne s'énumèrent pas --- la non-dénombrabilité, dont le
\cref{ch:b1:topology} développe la théorie.

\textbf{23.} Si les développements coïncident jusqu'à $n$, alors $x$
et $y$ ont la même troncature $s_n$, et la question 6 les place tous
deux dans $\intco{s_n}{s_n + b^{-n}}$, \omterm{prop:b1:reals:intervals}{intervalle} de longueur
$b^{-n}$~: $\abs{x - y} < b^{-n}$. Réciproque~: $x = 0.1$ et $y =
0.0999$ (développements finis, donc propres) vérifient $\abs{x - y}
= 10^{-4} < 10^{-3}$, et pourtant leurs développements diffèrent dès
le tout premier chiffre. Les coupables sont les fractions
$b$-adiques de la question 11~: à leur voisinage, un déplacement
minuscule retourne tous les chiffres affichés ($0.0999 \to
0.1000$), car ce sont précisément les réels où rôde le jumeau
impropre.

\textbf{24.} $p = 1$, $q = 10$, $b = 2$, $r_0 = 1$~: $2 = 10 \cdot
0 + 2$, $4 = 10 \cdot 0 + 4$, $8 = 10 \cdot 0 + 8$, $16 = 10
\cdot 1 + 6$, $12 = 10 \cdot 1 + 2$ --- et $r_5 = 2 = r_1$~: les
restes parcourent le cycle $(2, 4, 8, 6)$ à partir de l'indice $1$.
Chiffres~: $d_1 = 0$, puis le bloc répété $d_2 d_3 d_4 d_5 = 0, 0,
1, 1$~:
\[
\tfrac{1}{10} = (0.0\overline{0011})_2 ,
\]
prépériode $1$, période $4$. D'après la question 3, un développement
fini en base $2$ exigerait que tout premier de $10$ \omterm{def:b1:arith:divides}{divise} $2$~; le
premier $5$ s'y refuse. Donc $0.1$ n'est \emph{pas} représentable par
une chaîne binaire finie --- un ordinateur qui ne stocke qu'un nombre
fini de bits n'en garde qu'une troncature, et les erreurs de
troncature accumulées expliquent pourquoi le $0.1 + 0.2$ en virgule
flottante diffère de $0.3$ dans les derniers bits.

\textbf{25.} (i) La complétude a produit les valeurs~: $x = \sup s_n$
et $y = \sup t_n$ (questions 7 et 9) --- dans $\Q$ seul, la suite
propre de $\sqrt 2$ ne nommerait rien. (ii) La \omterm{thm:b1:reals:archimedes}{propriété d'Archimède}
a rendu $b^{-n}$ finalement plus petit que tout $\varepsilon$, ce qui
force les troncatures à se resserrer sur leur \omterm{def:b1:reals:bounds}{borne supérieure}
(questions 7 et 21). (iii) La clause d'unicité de la \omterm{thm:b1:reals:floor}{partie entière} a
identifié $Q_n = A_n$ à la question 14 et légitimé chaque extraction
de chiffre $\lfloor u - K \rfloor = \lfloor u \rfloor - K$
(question 13). (iv) Le \omterm{cor:b1:counting:pigeonhole}{principe des tiroirs}, appliqué à un nombre
fini de restes, est le seul moteur de la périodicité (question 16).
Morale~: les suites propres codent fidèlement $\intco{0}{1}$ et
transforment la rationalité en un rythme visible~; mais additionner
des suites de chiffres exige des retenues venues d'infiniment loin à
droite, si bien qu'aucune règle à étapes finies ne calcule même le
premier chiffre d'une somme --- alors que l'interface «~\omterm{def:b1:reals:bounds}{borne
supérieure}~» du \cref{thm:b1:reals:sup} traite toute l'analyse avec
un unique axiome. Les chiffres sont une magnifique \emph{image} de
$\R$~; la \omterm{def:b1:reals:bounds}{borne supérieure} en est le \emph{moteur}.
\end{solution}
