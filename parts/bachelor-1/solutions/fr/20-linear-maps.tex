\section*{Chapitre \ref{ch:b1:linmaps} --- Applications linéaires}

\begin{solution}{exo:b1:linmaps:1}
(1) \omterm{def:b1:linmaps:def}{Linéaire}~: les \omterm{prop:b1:vspaces:coordinates}{coordonnées} sont des expressions \omterm{def:b1:linmaps:def}{linéaires}. (2) Non \omterm{def:b1:linmaps:def}{linéaire}~:
$u(2(1,1)) = 4 \neq 2 = 2u(1,1)$. (3) \omterm{def:b1:linmaps:def}{Linéaire}~: la dérivation et la
multiplication par $X$ le sont, et les sommes d'\omterm{def:b1:linmaps:def}{applications linéaires} le sont. (4) \omterm{def:b1:linmaps:def}{Linéaire}~:
l'évaluation respecte les opérations ponctuelles.
\end{solution}

\begin{solution}{exo:b1:linmaps:2}
\omterm{def:b1:linmaps:kerim}{Noyau}~: résolvons $x + y - z = 0$, $2x + y + z = 0$, $3x + 2y = 0$.
La troisième donne $y = -\frac{3x}{2}$~; la première donne $z = x + y =
-\frac x2$~; vérifions dans la deuxième~: $2x - \frac{3x}{2} - \frac x2 = 0$~:
c'est satisfait. Donc $\ker u = \operatorname{Vect}\bigl((2, -3, -1)\bigr)$
(en prenant $x = 2$), de dimension $1$.

\omterm{thm:b1:linmaps:ranknullity}{Théorème du rang}~: $\operatorname{rk} u = 3 - 1 = 2$. \omterm{def:b1:linmaps:kerim}{Image}~: engendrée par
les \omterm{def:b1:linmaps:kerim}{images} de la \omterm{def:b1:vspaces:free}{base} canonique, $u(e_1) = (1,2,3)$, $u(e_2) =
(1,1,2)$, $u(e_3) = (-1,1,0)$~; les deux premières sont \omterm{def:b1:vspaces:free}{libres}, et le \omterm{def:b1:linmaps:rank}{rang}
vaut $2$~: \omterm{def:b1:vspaces:free}{base} $\bigl((1,2,3), (1,1,2)\bigr)$.

Non \omterm{def:b1:logic:inj}{injective} ($\ker \neq \{0\}$), non \omterm{def:b1:logic:inj}{surjective} (\omterm{def:b1:linmaps:rank}{rang} $2 < 3$)~:
cohérent avec le \cref{cor:b1:linmaps:samedim}.
\end{solution}

\begin{solution}{exo:b1:linmaps:3}
\emph{\omterm{def:b1:linmaps:kerim}{Noyau}~:} $P = P'$ force $\deg P = \deg P'$ sauf si $P = 0$~;
or $\deg P' < \deg P$ pour $P \neq 0$~: donc $\ker u = \{0\}$, et $u$,
endomorphisme \omterm{def:b1:logic:inj}{injectif} de $\R_n[X]$ qui est \omterm{def:b1:findim:def}{de dimension finie}, est un
isomorphisme (\cref{cor:b1:linmaps:samedim}).

\emph{Réciproque~:} posons $v(P) = P + P' + P'' + \dots + P^{(n)}$ (somme
finie sur $\R_n[X]$). Alors
\[
v\bigl(u(P)\bigr) = \sum_{k=0}^{n} (P - P')^{(k)}
= \sum_{k=0}^{n} P^{(k)} - \sum_{k=0}^{n} P^{(k+1)}
= P - P^{(n+1)} = P ,
\]
par télescopage, puisque $P^{(n+1)} = 0$. Donc $v = u^{-1}$.
\end{solution}

\begin{solution}{exo:b1:linmaps:4}
$\operatorname{im}(v \circ u) = v(\operatorname{im} u) \subseteq
\operatorname{im} v$~: le \omterm{def:b1:linmaps:rank}{rang} est $\leq \operatorname{rk} v$. Et $v$
restreinte à $\operatorname{im} u$ a pour \omterm{def:b1:linmaps:kerim}{image}
$\operatorname{im}(vu)$~; le \omterm{thm:b1:linmaps:ranknullity}{théorème du rang} appliqué dans
$\operatorname{im} u$ donne~: $\operatorname{rk}(vu) \leq
\dim\operatorname{im} u = \operatorname{rk} u$.
\end{solution}

\begin{solution}{exo:b1:linmaps:5}
$u^2 = 0$ signifie $u(u(x)) = 0$ pour tout $x$~: tout $u(x)$ est dans
$\ker u$, c'est-à-dire $\operatorname{im} u \subseteq \ker u$. Puis le
\omterm{thm:b1:linmaps:ranknullity}{théorème du rang}~:
\[
\dim E = \dim\ker u + \operatorname{rk} u \geq 2\operatorname{rk} u .
\]
Exemple d'égalité dans $\R^2$~: $u(x, y) = (y, 0)$~: $u^2 = 0$,
$\operatorname{rk} u = 1 = \frac{\dim E}{2}$.
\end{solution}

\begin{solution}{exo:b1:linmaps:6}
$(pq)^2 = pqpq = ppqq = pq$ (commutation)~: c'est une \omterm{def:b1:linmaps:projection}{projection}
(\cref{thm:b1:linmaps:projchar}).

\omterm{def:b1:linmaps:kerim}{Image}~: $\operatorname{im}(pq) \subseteq \operatorname{im} p$, et
$= \operatorname{im}(qp) \subseteq \operatorname{im} q$~: elle est contenue
dans l'intersection. Réciproquement, si $x \in \operatorname{im} p \cap
\operatorname{im} q$, alors $p(x) = x$ et $q(x) = x$ (les points fixes
caractérisent l'\omterm{def:b1:linmaps:kerim}{image} d'une \omterm{def:b1:linmaps:projection}{projection}), donc $pq(x) = x$~: $x \in
\operatorname{im}(pq)$.

\omterm{def:b1:linmaps:kerim}{Noyau}~: $\ker p \subseteq \ker(qp) = \ker(pq)$ et de même $\ker q
\subseteq \ker(pq)$~: la somme est contenue. Réciproquement, soit $pq(x) =
0$, et écrivons
\[
x = \underbrace{q(x)}_{\in\, \ker p} +
\underbrace{(x - q(x))}_{\in\, \ker q} :
\]
le premier terme vérifie $p(q(x)) = 0$, donc il est dans $\ker p$~; le
second est dans $\ker q$ puisque $q(x - q(x)) = q(x) - q^2(x) = 0$. D'où
$x \in \ker p + \ker q$.
\end{solution}

\begin{solution}{exo:b1:linmaps:7}
Remarquons d'abord les inclusions générales $\ker u \subseteq \ker u^2$ et
$\operatorname{im} u^2 \subseteq \operatorname{im} u$, et, par le
\omterm{thm:b1:linmaps:ranknullity}{théorème du rang}, (2) $\iff$ (3) (noyaux égaux $\iff$ \omterm{def:b1:linmaps:rank}{rangs} égaux
$\iff$ \omterm{def:b1:linmaps:kerim}{images} égales, compte tenu des inclusions).

(1 $\Rightarrow$ 2)~: soit $u^2(x) = 0$~; alors $u(x) \in \ker u \cap
\operatorname{im} u = \{0\}$, donc $x \in \ker u$.

(2 $\Rightarrow$ 1)~: par Grassmann et le \omterm{thm:b1:linmaps:ranknullity}{théorème du rang}, $\dim(\ker u +
\operatorname{im} u) = \dim\ker u + \operatorname{rk} u -
\dim(\ker u \cap \operatorname{im} u) = \dim E - \dim(\ker u \cap
\operatorname{im} u)$~: la somme vaut $E$ si et seulement si l'intersection est
$\{0\}$. Soit $y \in \ker u \cap \operatorname{im} u$~: $y = u(x)$ et
$u(y) = 0$, donc $u^2(x) = 0$, donc (par (2)) $u(x) = 0$~: $y = 0$. D'où
$E = \ker u \oplus \operatorname{im} u$.
\end{solution}

\begin{solution}{exo:b1:linmaps:8}
Si $\varphi = 0$~: alors $\ker\varphi = E \subseteq \ker\psi$ force
$\psi = 0 = 0\cdot\varphi$. Si $\varphi \neq 0$~: $\ker\varphi$ est un
\omterm{def:b1:linmaps:forms}{hyperplan}~; choisissons $a \notin \ker\varphi$. Posons $\lambda =
\frac{\psi(a)}{\varphi(a)}$. La forme $\psi - \lambda\varphi$
s'annule sur $\ker\varphi$ (les deux formes s'y annulent, par l'inclusion) et en $a$~:
elle s'annule sur $\ker\varphi \oplus Ka = E$. Donc $\psi =
\lambda\varphi$.
\end{solution}

\begin{solution}{exo:b1:linmaps:9}
Supposons $\lambda_0 x + \lambda_1 u(x) + \dots + \lambda_{n-1}
u^{n-1}(x) = 0$. Appliquons $u^{n-1}$~: tous les termes portant un facteur $u^{\geq
n}$ meurent, ne laissant que $\lambda_0 u^{n-1}(x) = 0$, donc $\lambda_0 = 0$.
Appliquons $u^{n-2}$ à la relation restante~: $\lambda_1 u^{n-1}(x) =
0$, donc $\lambda_1 = 0$~; et ainsi de suite. La famille est \omterm{def:b1:vspaces:free}{libre}~; étant de \omterm{def:b1:counting:card}{cardinal}
$n = \dim E$, c'est une \omterm{def:b1:vspaces:free}{base} (\cref{prop:b1:findim:twoofthree}). (Dans
cette \omterm{def:b1:vspaces:free}{base}, $u$ agit comme un décalage --- le modèle de la nilpotence
maximale.)
\end{solution}

\begin{solution}{exo:b1:linmaps:10}
\begin{enumerate}
  \item $f \circ f = -\mathrm{id}$ est \omterm{def:b1:logic:inj}{bijective}, donc $f$ l'est
        (\cref{prop:b1:logic:comp} adaptée~: $f$ admet l'inverse à droite et
        à gauche $-f$). Si $f(x) = \lambda x$ avec $x \neq 0$~:
        en appliquant $f$, $-x = \lambda^2 x$, donc $\lambda^2 = -1$~:
        impossible dans $\R$.
  \item Construisons la famille de proche en proche. Prenons $x_1 \neq 0$~: $(x_1,
        f(x_1))$ est \omterm{def:b1:vspaces:free}{libre} d'après (1). Si $\operatorname{Vect}$ de la
        famille courante $\bigl(x_1, f(x_1), \dots, x_k,
        f(x_k)\bigr)$, notons-le $V_k$ --- un \omterm{def:b1:vspaces:subspace}{sous-espace} stable par $f$
        (chaque générateur est envoyé sur un autre générateur ou son opposé~:
        $f(f(x_i)) = -x_i$) --- n'est pas $E$ tout entier, choisissons $x_{k+1}
        \notin V_k$. \emph{Affirmation~: la famille agrandie est \omterm{def:b1:vspaces:free}{libre}.}
        Supposons $\alpha x_{k+1} + \beta f(x_{k+1}) + v = 0$ avec $v
        \in V_k$ et $(\alpha, \beta) \neq (0,0)$. Appliquons $f$~:
        $\alpha f(x_{k+1}) - \beta x_{k+1} + f(v) = 0$ avec $f(v)
        \in V_k$. Éliminons $f(x_{k+1})$ entre les deux relations
        (multiplions la première par $\alpha$, la seconde par $-\beta$, et
        ajoutons)~:
        \[
        (\alpha^2 + \beta^2)\, x_{k+1} \in V_k ,
        \]
        et $\alpha^2 + \beta^2 \neq 0$ force $x_{k+1} \in V_k$~:
        contradiction. La construction se poursuit donc, en ajoutant des vecteurs
        \emph{deux par deux}, jusqu'à ce que $V_k = E$~: la famille finale est une
        \omterm{def:b1:vspaces:free}{base} de \omterm{def:b1:counting:card}{cardinal} pair, et $n$ est pair.
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:linmaps:11}
Majoration~: pour tout $x$, $(u + v)(x) = u(x) + v(x) \in
\operatorname{im} u + \operatorname{im} v$, donc
\[
\operatorname{rk}(u + v)
\leq \dim(\operatorname{im} u + \operatorname{im} v)
\leq \operatorname{rk} u + \operatorname{rk} v
\]
(Grassmann, \cref{thm:b1:findim:grassmann}). Minoration~: appliquons
la majoration au couple $(u + v, -v)$, dont la somme est $u$~:
\[
\operatorname{rk} u \leq \operatorname{rk}(u + v) +
\operatorname{rk}(-v) = \operatorname{rk}(u + v) +
\operatorname{rk} v,
\]
donc $\operatorname{rk} u - \operatorname{rk} v \leq
\operatorname{rk}(u+v)$~; en échangeant $u$ et $v$ on obtient la valeur
absolue.
\end{solution}

\begin{solution}{exo:b1:linmaps:12}
\emph{Formule exacte.} Soit $v'$ la restriction de $v$ au
\omterm{def:b1:vspaces:subspace}{sous-espace} $\operatorname{im} w$. Son \omterm{def:b1:linmaps:kerim}{image} est $v(w(F)) =
\operatorname{im}(v \circ w)$, et son \omterm{def:b1:linmaps:kerim}{noyau} est $\ker v \cap
\operatorname{im} w$. Le \omterm{thm:b1:linmaps:ranknullity}{théorème du rang} pour $v'$ sur l'espace
$\operatorname{im} w$~:
\[
\operatorname{rk} w = \dim\operatorname{im} w
= \operatorname{rk}(v \circ w) + \dim(\ker v \cap
\operatorname{im} w) .
\]

\emph{Frobenius.} Appliquons la formule exacte deux fois, à $w$ et à
$w \circ u$~:
\[
\operatorname{rk} w - \operatorname{rk}(vw)
= \dim\bigl(\ker v \cap \operatorname{im} w\bigr),
\qquad
\operatorname{rk}(wu) - \operatorname{rk}(vwu)
= \dim\bigl(\ker v \cap \operatorname{im}(wu)\bigr) .
\]
Comme $\operatorname{im}(w \circ u) \subseteq \operatorname{im}
w$, la seconde intersection est contenue dans la première, et sa
dimension n'est pas plus grande~:
\[
\operatorname{rk}(wu) - \operatorname{rk}(vwu)
\;\leq\; \operatorname{rk} w - \operatorname{rk}(vw) ,
\]
ce qui se réarrange en l'inégalité de Frobenius. Avec $w =
\mathrm{id}_F$ (de \omterm{def:b1:linmaps:rank}{rang} $\dim F$, et $\operatorname{im}\,
\mathrm{id}_F = F$)~: $\operatorname{rk} v + \operatorname{rk} u
\leq \dim F + \operatorname{rk}(vu)$, l'inégalité de Sylvester ---
démontrée à nouveau, sous forme matricielle, dans l'\cref{exo:b1:matrices:10}.
\end{solution}

\begin{solution}{pb:b1:linmaps:1}
\textbf{1.} $(\mathrm{id} - p)^2 = \mathrm{id} - 2p + p^2 =
\mathrm{id} - p$~: c'est un projecteur. Si $y = x - p(x)$, alors $p(y) =
p(x) - p^2(x) = 0$, et réciproquement $x \in \ker p$ donne $x =
(\mathrm{id} - p)(x)$~: $\operatorname{im}(\mathrm{id} - p) = \ker
p$. Et $(\mathrm{id} - p)(x) = 0 \iff p(x) = x \iff x \in
\operatorname{im} p$ (points fixes,
\cref{thm:b1:linmaps:projchar})~: $\ker(\mathrm{id} - p) =
\operatorname{im} p$.

\textbf{2.} $(\lambda\,\mathrm{id} + \mu p)^2 =
\lambda^2\,\mathrm{id} + (2\lambda\mu + \mu^2)\,p$. Le couple
$(\mathrm{id}, p)$ est \omterm{def:b1:vspaces:free}{libre} dans $\mathcal{L}(E)$~: $p = c\,
\mathrm{id}$ donnerait $c^2 = c$, donc $p = 0$ ou $\mathrm{id}$,
ce qui est exclu. En identifiant les coefficients, l'\omterm{def:b1:logic:map}{application} est un projecteur si et seulement si
$\lambda^2 = \lambda$ et $2\lambda\mu + \mu^2 = \mu$. Pour
$\lambda = 0$~: $\mu \in \{0, 1\}$. Pour $\lambda = 1$~: $\mu^2 +
\mu = 0$, $\mu \in \{0, -1\}$. Exactement quatre projecteurs dans le
plan~: $0$, $p$, $\mathrm{id}$, $\mathrm{id} - p$.

\textbf{3.} $(\lambda\,\mathrm{id} + \mu p)(\lambda'\,\mathrm{id}
+ \mu' p) = \lambda\lambda'\,\mathrm{id} + (\lambda\mu' +
\mu\lambda' + \mu\mu')\,p$~: le plan est stable par
composition. Puisque $p^k = p$ pour tout $k \geq 1$, pour $Q =
\sum_k a_k X^k$~:
\[
Q(p) = a_0\,\mathrm{id} + \Bigl(\sum_{k \geq 1} a_k\Bigr) p
= Q(0)\,\mathrm{id} + \bigl(Q(1) - Q(0)\bigr)\,p .
\]

\textbf{4.} Sur $\operatorname{im} p$ (où $p$ agit comme
l'identité), $\lambda\,\mathrm{id} + \mu p$ multiplie par $\lambda
+ \mu$~; sur $\ker p$, par $\lambda$. Comme $E = \operatorname{im} p
\oplus \ker p$, l'\omterm{def:b1:logic:map}{application} est \omterm{def:b1:logic:inj}{bijective} si et seulement si $\lambda \neq 0$ et
$\lambda + \mu \neq 0$. En résolvant $\lambda\alpha = 1$,
$\lambda\beta + \mu\alpha + \mu\beta = 0$ dans la règle de composition
de la question 3~:
\[
(\lambda\,\mathrm{id} + \mu p)^{-1}
= \frac1\lambda\,\mathrm{id} -
\frac{\mu}{\lambda(\lambda + \mu)}\,p ,
\]
dont l'action est la multiplication par $1/\lambda$ sur $\ker p$ et par $1/(\lambda +
\mu)$ sur $\operatorname{im} p$, comme il se doit.

\textbf{5.} Écrivons $F = \operatorname{im} p = \operatorname{im}
p'$. Pour tout $x$, $p'(x) \in F$ et $p$ fixe $F$ point par point~:
$p(p'(x)) = p'(x)$, c'est-à-dire $p\,p' = p'$~; symétriquement $p'\,p =
p$. Lorsque deux \omterm{def:b1:linmaps:projection}{projections} partagent leur \omterm{def:b1:linmaps:kerim}{image}, c'est celle qui est appliquée
\emph{en premier} qui décide~: son résultat est déjà dans $F$, où la
\omterm{def:b1:linmaps:projection}{projection} extérieure agit comme l'identité et ne change rien.

\textbf{6.} $(p + q)^2 = p^2 + pq + qp + q^2 = (p + q) + pq +
qp$, donc $p + q$ projecteur force $pq + qp = 0$. Composons à
gauche avec $p$~: $pq + pqp = 0$~; à droite avec $p$~: $pqp + qp
= 0$. Par soustraction, $pq = qp$~; alors $pq + qp = 2pq = 0$ et la
caractéristique n'est pas $2$~: $pq = qp = 0$.

\textbf{7.} Avec $pq = qp = 0$, le même développement donne $(p +
q)^2 = p + q$. \omterm{def:b1:linmaps:kerim}{Image}~: $\operatorname{im}(p + q) \subseteq
\operatorname{im} p + \operatorname{im} q$ toujours. Réciproquement,
pour $x \in \operatorname{im} p$~: $q(x) = q(p(x)) = 0$, donc $(p +
q)(x) = p(x) = x$ et $x \in \operatorname{im}(p+q)$~; de même pour
$\operatorname{im} q$. \omterm{def:b1:vspaces:sum}{Somme directe}~: $x \in \operatorname{im} p
\cap \operatorname{im} q$ donne $x = p(x) = p(q(x)) = 0$.
\omterm{def:b1:linmaps:kerim}{Noyau}~: si $p(x) + q(x) = 0$, en appliquant $p$ on obtient $p(x) +
p(q(x)) = p(x) = 0$, et en appliquant $q$ on obtient $q(x) = 0$~: $\ker(p
+ q) = \ker p \cap \ker q$ (l'inclusion réciproque est claire).

\textbf{8.} $p - q$ est un projecteur si et seulement si $\mathrm{id} - (p - q) =
(\mathrm{id} - p) + q$ en est un (question 1, deux fois). D'après les questions
6 et 7 appliquées aux projecteurs $\mathrm{id} - p$ et $q$, cela
a lieu si et seulement si $(\mathrm{id} - p)q = q(\mathrm{id} - p) = 0$, c'est-à-dire
si et seulement si $pq = q$ et $qp = q$.

\textbf{9.} $pq = q$ signifie que $p$ fixe tout $q(x)$, c'est-à-dire
$\operatorname{im} q \subseteq \ker(p - \mathrm{id}) =
\operatorname{im} p$. Et $qp = q$ signifie $q\bigl((\mathrm{id} -
p)(x)\bigr) = 0$ pour tout $x$, c'est-à-dire que $q$ s'annule sur
$\operatorname{im}(\mathrm{id} - p) = \ker p$~: $\ker p \subseteq
\ker q$. Les deux étapes sont des équivalences~: l'ordre $q \leq p$ dit que
$q$ projette sur une \omterm{def:b1:linmaps:kerim}{image} plus petite, parallèlement à un \omterm{def:b1:linmaps:kerim}{noyau} plus grand.

\textbf{10.} En développant, $(\mathrm{id} - p)(\mathrm{id} - q) =
\mathrm{id} - p - q + pq = \mathrm{id} - r$. Les projecteurs
$\mathrm{id} - p$ et $\mathrm{id} - q$ commutent, donc d'après
l'\cref{exo:b1:linmaps:6} leur produit $\mathrm{id} - r$ est le
projecteur sur $\operatorname{im}(\mathrm{id} - p) \cap
\operatorname{im}(\mathrm{id} - q) = \ker p \cap \ker q$ parallèlement à
$\ker(\mathrm{id} - p) + \ker(\mathrm{id} - q) =
\operatorname{im} p + \operatorname{im} q$. D'après la question 1, $r =
\mathrm{id} - (\mathrm{id} - r)$ est alors le projecteur avec
$\operatorname{im} r = \operatorname{im} p + \operatorname{im}
q$ et $\ker r = \ker p \cap \ker q$.

\textbf{11.} $p_i$ est bien définie (unicité de la
décomposition) et \omterm{def:b1:linmaps:def}{linéaire} (la décomposition de $x + \lambda y$
est la somme des décompositions, encore par unicité). Pour $x_i
\in F_i$ la décomposition est $x_i$ lui-même, donc $p_i(x_i) = x_i$~:
$p_i^2 = p_i$, et $p_j(x_i) = 0$ pour $j \neq i$~: $p_i p_j = 0$
($p_j(x) \in F_j$). En sommant les composantes, $\sum_i p_i =
\mathrm{id}$. Enfin $\operatorname{im} p_i = F_i$ et $\ker
p_i = \bigoplus_{j \neq i} F_j$.

\textbf{12.} $p_i = p_i \circ \mathrm{id} = p_i\sum_j p_j =
p_i^2 + \sum_{j \neq i} p_i p_j = p_i^2$~: chaque $p_i$ est un
projecteur. Tout $x = \mathrm{id}(x) = \sum_i p_i(x)$ est dans
$\sum_i \operatorname{im} p_i$~: les \omterm{def:b1:linmaps:kerim}{images} somment à $E$.
\omterm{def:b1:vspaces:sum}{Somme directe}~: supposons $y_1 + \dots + y_k = 0$ avec $y_i \in
\operatorname{im} p_i$, donc $p_i(y_i) = y_i$. Appliquons $p_j$~: $p_j
(y_i) = p_j p_i (y_i) = 0$ pour $i \neq j$, donc $0 = p_j\bigl(\sum
y_i\bigr) = y_j$, pour tout $j$. D'où $E = \bigoplus_i
\operatorname{im} p_i$.

\textbf{13.} $q = \mathrm{id} - p$, et la question 1 donne $pq =
p - p^2 = 0 = qp$ directement~: pour deux projecteurs, sommer à
l'identité force déjà l'orthogonalité du couple.

\textbf{14.} $p + q = \mathrm{id} - r$ avec $r$ projecteur, et
$(\mathrm{id} - r)$ est un projecteur (question 1)~: donc $p + q$ est un
projecteur, et la question 6 donne $pq = qp = 0$. Par \omterm{def:b1:linmaps:projection}{symétrie} ($q +
r = \mathrm{id} - p$ et $p + r = \mathrm{id} - q$), tous les
produits deux à deux sont nuls, et la question 12 conclut~: $E =
\operatorname{im} p \oplus \operatorname{im} q \oplus
\operatorname{im} r$, automatiquement.

\textbf{15.} \emph{Lemme.} Par récurrence avec Grassmann
(\cref{thm:b1:findim:grassmann})~:
\[
\dim(F_1 + \dots + F_k) \leq \dim(F_1 + \dots + F_{k-1}) + \dim
F_k \leq \dots \leq \sum_i \dim F_i .
\]
Si le total est une égalité, chaque étape en est une~: $(F_1 + \dots +
F_{j-1}) \cap F_j = \{0\}$ pour tout $j$, et une relation $y_1 +
\dots + y_k = 0$ ($y_i \in F_i$) s'effondre par la droite~: $y_k
\in (F_1 + \dots + F_{k-1}) \cap F_k = \{0\}$, puis $y_{k-1} =
0$, etc.~: la somme est directe. Réciproquement, une \omterm{def:b1:vspaces:sum}{somme directe} a des
dimensions additives (concaténer les \omterm{def:b1:vspaces:free}{bases}). \emph{Application~:}
$x = \sum_i p_i(x)$ montre que $E = \sum_i \operatorname{im} p_i$,
donc $n \leq \sum_i \operatorname{rk} p_i$~; l'hypothèse donne
l'égalité, d'où le caractère direct. Produits~: fixons $j$ et $y \in
\operatorname{im} p_j$. Alors $y = \sum_i p_i(y)$ avec $p_i(y)
\in \operatorname{im} p_i$, tandis que $y = y$ est aussi une
décomposition (la composante $j$ seule)~; l'unicité force $p_i(y)
= 0$ pour $i \neq j$. Appliqué à $y = p_j(x)$~: $p_i p_j = 0$.

\textbf{16.} Si $u^k(x) = 0$ alors $u^{k+1}(x) = u(0) = 0$. Et
$\operatorname{im} u^{k+1} = u^k\bigl(u(E)\bigr) \subseteq
u^k(E) = \operatorname{im} u^k$.

\textbf{17.} Supposons $\ker u^{r} = \ker u^{r+1}$ et soit $x \in
\ker u^{r+2}$~: alors $u(x) \in \ker u^{r+1} = \ker u^{r}$, donc
$u^{r+1}(x) = 0$~: $x \in \ker u^{r+1}$. Avec la question 16,
$\ker u^{r+1} = \ker u^{r+2}$, et par récurrence tous les noyaux
ultérieurs coïncident avec $\ker u^{r}$. Pour les \omterm{def:b1:linmaps:kerim}{images}~: le \omterm{thm:b1:linmaps:ranknullity}{théorème du rang}
donne $\dim\operatorname{im} u^k = n - \dim\ker u^k$, donc les
dimensions des \omterm{def:b1:linmaps:kerim}{images} se figent exactement quand celles des noyaux le font,
et, avec les inclusions de la question 16, des dimensions égales signifient
des \omterm{def:b1:vspaces:subspace}{sous-espaces} égaux (\cref{thm:b1:findim:subspaces}).

\textbf{18.} La suite $\bigl(\dim\ker u^k\bigr)_k$ est
croissante à valeurs dans $\intint{0}{n}$~; elle ne peut pas croître
strictement $n + 1$ fois, donc $\dim\ker u^{r} = \dim\ker
u^{r+1}$ pour un certain $r \leq n$, d'où $\ker u^{r} = \ker u^{r+1}$
(inclusion et dimensions égales). Prenons $r$ minimal~; la question 17
fige tout à partir de $r$, \omterm{def:b1:linmaps:kerim}{images} comprises.

\textbf{19.} Intersection~: soit $x \in \ker u^{r} \cap
\operatorname{im} u^{r}$, disons $x = u^{r}(y)$ avec $u^{r}(x) =
0$. Alors $u^{2r}(y) = 0$, et $\ker u^{2r} = \ker u^{r}$
(question 17), donc $x = u^{r}(y) = 0$. Dimensions~: le \omterm{thm:b1:linmaps:ranknullity}{théorème du rang}
pour $u^{r}$ donne $\dim\ker u^{r} + \dim\operatorname{im} u^{r}
= n$~; avec l'intersection triviale, Grassmann fait de la somme un
\omterm{def:b1:vspaces:subspace}{sous-espace} de dimension $n$~: $E = \ker u^{r} \oplus
\operatorname{im} u^{r}$.

\textbf{20.} Stabilité~: $u^{r}(u(x)) = u(u^{r}(x)) = 0$ pour $x
\in \ker u^{r}$~; et $u(u^{r}(y)) = u^{r}(u(y)) \in
\operatorname{im} u^{r}$. Sur $N = \ker u^{r}$~: $(u|_N)^{r} = 0$
par définition de $N$~: nilpotente. Sur $I = \operatorname{im}
u^{r}$~: $\ker(u|_I) = \ker u \cap I \subseteq \ker u^{r} \cap I
= \{0\}$, donc $u|_I$ est un endomorphisme \omterm{def:b1:logic:inj}{injectif} de $I$ qui est \omterm{def:b1:findim:def}{de
dimension finie}, donc \omterm{def:b1:logic:inj}{bijectif}
(\cref{cor:b1:linmaps:samedim}).

\textbf{21.} Soit $x = a + b$ avec $a \in N$, $b \in I$. Alors
$u(x) = u(a) + u(b)$ avec $u(a) \in N$ et $u(b) \in I$
(question 20)~: c'\emph{est} là la décomposition de $u(x)$, donc
$\pi(u(x)) = u(a) = u(\pi(x))$~: $\pi u = u\pi$.

\textbf{22.} $u^2(x,y,z) = u(y, 0, z) = (0, 0, z)$ et
$u^3(x,y,z) = u(0,0,z) = (0,0,z) = u^2(x,y,z)$. Noyaux~: $\ker u
= \{y = z = 0\} = \operatorname{Vect}(e_1)$, $\ker u^2 = \{z =
0\} = \operatorname{Vect}(e_1, e_2)$, $\ker u^3 = \ker u^2$~:
stabilisation à $r = 2$. \omterm{def:b1:linmaps:kerim}{Images}~: $\operatorname{im} u =
\operatorname{Vect}(e_1, e_3)$, $\operatorname{im} u^2 =
\operatorname{Vect}(e_3)$. Fitting~: $\R^3 =
\operatorname{Vect}(e_1, e_2) \oplus \operatorname{Vect}(e_3)$,
et $\pi(x, y, z) = (x, y, 0)$. Vérification~: $\pi u(x,y,z) = \pi(y, 0,
z) = (y, 0, 0)$ et $u\pi(x,y,z) = u(x, y, 0) = (y, 0, 0)$~:
égaux. Sur le premier facteur $u(x, y, 0) = (y, 0, 0)$, dont le
carré est $0$~: nilpotente~; sur le second $u(0,0,z) = (0,0,z)$~:
l'identité, \omterm{def:b1:logic:inj}{bijective}.

\textbf{23.} Si $u^m = 0$ alors $\ker u^m = E$~; comme les noyaux sont
figés à partir de $r$, $\ker u^{r} = \ker u^{\max(m, r)} = E$.
Réciproquement $\ker u^{r} = E$ signifie $u^{r} = 0$. Et $\ker u^{r} =
E \iff$ le projecteur de Fitting est la \omterm{def:b1:linmaps:projection}{projection} sur $E$ parallèlement à $\{0\}$, c'est-à-dire
$\pi = \mathrm{id}$. Enfin $r \leq n$ (question 18) donne~:
tout endomorphisme nilpotent vérifie $u^{n} = 0$ --- l'indice
de nilpotence ne dépasse jamais la dimension.

\textbf{24.} Soit $m$ un indice de nilpotence de $u|_A$~: $A
\subseteq \ker u^{m} \subseteq \ker u^{\max(m,r)} = \ker u^{r}$.
Comme $u|_B$ est \omterm{def:b1:logic:inj}{bijective}, $B = u(B) = u^{k}(B) \subseteq
\operatorname{im} u^{k}$ pour tout $k$, en particulier $B
\subseteq \operatorname{im} u^{r}$. Alors
\[
n = \dim A + \dim B \leq \dim\ker u^{r} +
\dim\operatorname{im} u^{r} = n :
\]
les deux inclusions sont des égalités de dimensions, donc de
\omterm{def:b1:vspaces:subspace}{sous-espaces}~: $A = \ker u^{r}$, $B = \operatorname{im} u^{r}$.

\textbf{25.} (i) La partie III est un dictionnaire~: les décompositions $E = F_1
\oplus \dots \oplus F_k$ correspondent exactement aux familles de
projecteurs avec $\sum p_i = \mathrm{id}$ et $p_i p_j = 0$, les
$F_i$ étant les \omterm{def:b1:linmaps:kerim}{images}. (ii) Pour $k = 3$ les compléments
$\mathrm{id} - p_i$ sont eux-mêmes des projecteurs, ce qui a bouclé
l'argument sans hypothèse \omterm{def:b1:vspaces:sum}{supplémentaire}~; pour un $k$ quelconque il faut
$\sum_i \operatorname{rk} p_i \leq n$, inégalité que la trace de
Licence~2 donne gratuitement ($\operatorname{tr} p = \operatorname{rk}
p$ pour un projecteur, et les traces s'additionnent pour donner $\operatorname{tr}
\mathrm{id} = n$). (iii) L'\cref{exo:b1:linmaps:7} est le \omterm{pb:b1:linmaps:1}{lemme de
Fitting} dans le cas déjà stabilisé $r \leq 1$~; en général on
laisse les chaînes de noyaux et d'\omterm{def:b1:linmaps:kerim}{images} se figer, ce qui prend au plus $n$
étapes. (iv) Dans le volume de Licence~2, appliqué à $u - \lambda\,
\mathrm{id}$, le facteur nilpotent devient le \omterm{def:b1:vspaces:subspace}{sous-espace} caractéristique
associé à $\lambda$ et les projecteurs de la partie III deviennent
les projecteurs spectraux de la théorie de la réduction. Le théorème de
la partie IV est le \emph{\omterm{pb:b1:linmaps:1}{lemme de Fitting}}.
\end{solution}
