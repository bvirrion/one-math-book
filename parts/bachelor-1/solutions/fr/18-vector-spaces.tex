\section*{Chapitre \ref{ch:b1:vspaces} --- Espaces vectoriels}

\begin{solution}{exo:b1:vspaces:1}
\begin{enumerate}
  \item Oui~: contient $0$, et l'équation qui le définit est linéaire
        (stabilité par $x + \lambda y$).
  \item Non~: ne contient pas $(0,0,0)$.
  \item Non~: $(1, 0)$ et $(0, -1)$ y appartiennent ($xy = 0$), leur
        somme $(1, -1)$ non ($xy = -1 < 0$).
  \item Oui~: $0$ s'annule en $1$~; $(P + \lambda Q)(1) = P(1) +
        \lambda Q(1) = 0$.
  \item Oui~: la fonction nulle est bornée~; si $\abs f \leq M$ et
        $\abs g \leq M'$, alors $\abs{f + \lambda g} \leq M +
        \abs\lambda M'$.
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:vspaces:2}
$(1,2,1) = a(1,0,1) + b(1,1,0)$ impose $a + b = 1$, $b = 2$, $a =
1$~: incompatible ($a + b = 3 \neq 1$)~: le vecteur n'est pas dans le
\omterm{def:b1:vspaces:span}{sous-espace engendré}. $(2,1,1) =
a(1,0,1) + b(1,1,0)$~: $b = 1$, $a = 1$, $a + b = 2$~: compatible, donc
$(2,1,1) = (1,0,1) + (1,1,0)$, il y est.

Équation~: $(x, y, z) = (a + b, b, a)$ signifie $x = y + z$~: le
\omterm{def:b1:vspaces:span}{sous-espace engendré} est le plan $\{x - y - z = 0\}$.
\end{solution}

\begin{solution}{exo:b1:vspaces:3}
Première famille~: $\lambda(1,1,0) + \mu(1,0,1) + \nu(0,1,1) = 0$ donne
$\lambda + \mu = 0$, $\lambda + \nu = 0$, $\mu + \nu = 0$~: en
additionnant, $2(\lambda + \mu + \nu) = 0$, puis en soustrayant chaque
équation, $\lambda = \mu = \nu = 0$~: \omterm{def:b1:vspaces:free}{libre}.

Deuxième~: $(2,4,6) = 2(1,2,3)$~: liée.

Troisième~: quatre vecteurs dans $\R^3$ --- nécessairement liés une
fois la dimension disponible (\cref{ch:b1:findim})~; directement~:
$(0,1,1) = -(1,0,0) + 0\cdot(1,1,0) + (1,1,1)$, en effet $(-1,0,0) +
(1,1,1) = (0,1,1)$~: une relation non triviale.
\end{solution}

\begin{solution}{exo:b1:vspaces:4}
Les \omterm{def:b1:poly:def}{polynômes} $1, (X-1), (X-1)^2, (X-1)^3$ ont des degrés distincts
$0, 1, 2, 3$~: la famille est \omterm{def:b1:vspaces:free}{libre}
(\cref{prop:b1:vspaces:freecriteria}~(1))~; elle est génératrice (tout
$P \in \R_3[X]$ se développe suivant les puissances de $X - 1$, par
exemple par la \omterm{thm:b1:taylor:integral}{formule de Taylor} pour les \omterm{def:b1:poly:def}{polynômes}, cf.\ la
démonstration de la \cref{prop:b1:poly:multiplicity})~: c'est une \omterm{def:b1:vspaces:free}{base}.
Pour $X^3$, Taylor en $1$~: $P = X^3$, $P(1) = 1$, $P'(1) = 3$,
$P''(1) = 6$, $P'''(1) = 6$~:
\[
X^3 = 1 + 3(X - 1) + 3(X-1)^2 + (X-1)^3 ,
\]
\omterm{prop:b1:vspaces:coordinates}{coordonnées} $(1, 3, 3, 1)$ (une ligne du triangle de Pascal, comme le
laissait attendre $X^3 = ((X-1)+1)^3$).
\end{solution}

\begin{solution}{exo:b1:vspaces:5}
$F \cap G$~: les conditions $x = y = z$ et $x = t = 0$ donnent \omterm{def:b1:logic:sets}{ensemble}
$x = 0$, donc $y = z = 0$, et $t = 0$~: l'intersection est
$\{0\}$. Somme~: étant donné $(x,y,z,t)$, cherchons $(a,a,a,b) \in F$ et
$(0,c,d,0) \in G$ de somme ce vecteur~: $a = x$, $b = t$, $c = y - x$,
$d = z - x$~: toujours possible. Donc $\R^4 = F \oplus G$, et
\[
(1,2,3,4) = (1,1,1,4) + (0,1,2,0) .
\]
\end{solution}

\begin{solution}{exo:b1:vspaces:6}
Un élément de $G$ s'écrit $\lambda u$~; s'il converge, alors (puisque
$\lambda u_n = \lambda(-1)^n$ a les deux valeurs d'\omterm{def:b1:topology:closure}{adhérence} $\pm
\lambda$) nécessairement $\lambda = 0$~: $F \cap G = \{0\}$.

$F + G$ n'est pas l'espace tout entier~: il est formé des suites de la
forme $c_n + \lambda(-1)^n$ avec $(c_n)$ convergente. La suite $v_n =
n$ n'est pas de cette forme ($v_n - \lambda(-1)^n$ n'est pas bornée,
donc jamais convergente). Ainsi $F \oplus G \subsetneq$ (espace de
toutes les suites).
\end{solution}

\begin{solution}{exo:b1:vspaces:7}
($\supseteq$) $F \cap G$ et $F \cap H$ sont tous deux dans $F$, et leur
somme est dans $G + (F \cap H)$~: l'inclusion suit, le membre de gauche
étant un \omterm{def:b1:vspaces:subspace}{sous-espace} contenant les deux morceaux --- concrètement, un
élément $g + h$ avec $g \in F\cap G$, $h \in F \cap H$ est dans $F$
(somme de deux éléments de $F$) et dans $G + (F \cap H)$.

($\subseteq$) Soit $x \in F$ avec $x = g + h$, $g \in G$, $h \in F
\cap H$. Alors $g = x - h \in F$ (différence d'éléments de $F$), donc
$g \in F \cap G$, et $x = g + h \in (F \cap G) + (F \cap H)$.

Contre-exemple à la distributivité complète dans $\R^2$~: $F =
\operatorname{Vect}(1,1)$, $G = \operatorname{Vect}(1,0)$, $H =
\operatorname{Vect}(0,1)$. Alors $G + H = \R^2$, donc $F \cap (G+H) =
F$, tandis que $F \cap G = F \cap H = \{0\}$~: le membre de droite vaut
$\{0\} \neq F$.
\end{solution}

\begin{solution}{exo:b1:vspaces:8}
\begin{enumerate}
  \item Supposons $\sum_{i} \lambda_i \eu^{a_i x} = 0$ pour tout $x$.
        Multiplions par $\eu^{-a_p x}$~: $\lambda_p + \sum_{i < p}
        \lambda_i \eu^{(a_i - a_p)x} \to \lambda_p$ quand $x \to
        +\infty$ (chaque exposant $a_i - a_p < 0$). Le membre de
        gauche est identiquement nul, donc $\lambda_p = 0$~; on itère
        en descendant.
  \item Soit $a\cos x + b \sin x + c \cos 2x + d\sin 2x = 0$ pour tout
        $x$. Évaluons en $x = 0$~: $a + c = 0$~; en $x = \pi$~: $-a + c
        = 0$~; d'où $a = c = 0$, et la relation se réduit à $b\sin
        x + d \sin 2x = 0$. Évaluons en $x = \frac\pi2$~: $b = 0$~;
        puis en $x = \frac\pi4$~: $d = 0$.
  \item Supposons $\sum \lambda_i \abs{x - a_i} = 0$ pour tout $x$. La
        fonction $\sum_{i \neq j} \lambda_i\abs{x - a_i}$ est
        \omterm{def:b1:derivative:def}{dérivable} en $a_j$ (chaque terme l'est, loin de son propre
        point anguleux), donc $-\lambda_j \abs{x - a_j}$, leur
        différence, doit l'être aussi en $a_j$ --- ce qui force
        $\lambda_j = 0$ ($\abs{\,\cdot\,}$ a un point anguleux). Cela
        vaut pour tout $j$.
\end{enumerate}
\end{solution}

\begin{solution}{exo:b1:vspaces:9}
Soit $h \in H$. Comme $h \in H \subseteq F + H = F + G$, écrivons $h = f
+ g$ avec $f \in F$, $g \in G$. Alors $f = h - g \in H$ (les deux
termes sont dans $H$, grâce à $G \subseteq H$), donc $f \in F \cap H =
F \cap G \subseteq G$, et $h = f + g \in G$. Ainsi $H \subseteq G$, et
avec l'hypothèse $G \subseteq H$~: égalité.

Contre-exemple sans $G \subseteq H$~: dans $\R^2$, prenons $F =
\operatorname{Vect}(1,0)$, $G = \operatorname{Vect}(0,1)$, $H =
\operatorname{Vect}(1,1)$~: alors $F + G = F + H = \R^2$ et $F \cap G
= F \cap H = \{0\}$, et pourtant $G \neq H$.
\end{solution}

\begin{solution}{exo:b1:vspaces:10}
Les deux \omterm{def:b1:logic:sets}{ensembles} sont des \omterm{def:b1:vspaces:subspace}{sous-espaces} (les conditions qui les
définissent sont linéaires et valent pour $0$). Décomposition~: pour
$P \in \R[X]$,
\[
P(X) = \underbrace{\frac{P(X) + P(-X)}{2}}_{\in\,\mathcal P}
+ \underbrace{\frac{P(X) - P(-X)}{2}}_{\in\,\mathcal I},
\]
et un \omterm{def:b1:poly:def}{polynôme} à la fois pair et impair vérifie $P = -P$, donc $P = 0$~:
la somme est directe et vaut $\R[X]$.

Soit maintenant $P = \sum_k a_k X^k$ pair. Alors $P(X) - P(-X) = 2
\sum_{k \text{ impair}} a_k X^k$ est le \omterm{def:b1:poly:def}{polynôme} nul, donc tous les
coefficients de degré impair sont nuls (\cref{def:b1:poly:def})~: $P \in
\operatorname{Vect}(1, X^2, X^4, \dots)$, c'est-à-dire $P = Q(X^2)$ pour
un certain \omterm{def:b1:poly:def}{polynôme} $Q$. Réciproquement, tout \omterm{def:b1:poly:def}{polynôme} en $X^2$ est
pair.
\end{solution}

\begin{solution}{exo:b1:vspaces:11}
Supposons $a(x_1 + x_2) + b(x_2 + x_3) + c(x_3 + x_1) = 0$.
En regroupant sur la \omterm{def:b1:vspaces:free}{famille libre} $(x_1, x_2, x_3)$~:
\[
(a + c)\,x_1 + (a + b)\,x_2 + (b + c)\,x_3 = 0
\implies a + c = a + b = b + c = 0 .
\]
Soustraire les deux premières équations donne $c = b$~; la troisième
donne alors $2b = 0$, donc $b = c = 0$, puis $a = 0$~: la famille est
\omterm{def:b1:vspaces:free}{libre}.

Pour quatre vecteurs, la famille analogue est \emph{toujours} liée~:
\[
(x_1 + x_2) - (x_2 + x_3) + (x_3 + x_4) - (x_4 + x_1) = 0
\]
est une combinaison nulle non triviale (coefficients $1, -1, 1, -1$),
quels que soient $(x_1, x_2, x_3, x_4)$. C'est la parité de la longueur
du cycle qui décide.
\end{solution}

\begin{solution}{exo:b1:vspaces:12}
\begin{enumerate}
  \item Si $F_1 \subseteq F_2$ ou $F_2 \subseteq F_1$, la réunion
        est l'un des deux, donc propre. Sinon, prenons $x \in F_1
        \setminus F_2$ et $y \in F_2 \setminus F_1$, et considérons
        $x + y$. Si $x + y \in F_1$, alors $y = (x + y) - x \in
        F_1$~: contradiction. Si $x + y \in F_2$, alors $x \in F_2$~:
        contradiction. Donc $x + y \notin F_1 \cup F_2$, et $E \neq
        F_1 \cup F_2$.
  \item Supposons par l'absurde que $E = F_1 \cup \dots \cup
        F_k$, avec $k$ choisi \emph{minimal} parmi tous les
        recouvrements de ce type. La minimalité interdit $F_1 \subseteq
        F_2 \cup \dots \cup F_k$ (sinon on supprimerait $F_1$), donc il
        existe $x \in F_1$ avec $x \notin F_i$ pour tout $i \geq 2$.
        Comme $F_1$ est propre, choisissons $y \notin F_1$. Pour chaque
        scalaire $t$, le vecteur $y + t x$ appartient à un certain
        $F_i$. Il n'est jamais dans $F_1$~: sinon $y = (y + tx) - tx
        \in F_1$ (car $x \in F_1$). Le \omterm{def:b1:structures:field}{corps} est infini, choisissons
        donc $k$ scalaires distincts $t_1, \dots, t_k$~: les $k$
        vecteurs $y + t_j x$ tombent dans les $k - 1$ \omterm{def:b1:vspaces:subspace}{sous-espaces}
        $F_2, \dots, F_k$, et deux d'entre eux, disons $y + t x$ et $y
        + t' x$ avec $t \neq t'$, sont dans le même $F_i$ ($i \geq 2$).
        Alors leur différence $(t - t')x \in F_i$, donc $x \in F_i$~:
        contradiction. Il n'existe donc aucun recouvrement fini par des
        \omterm{def:b1:vspaces:subspace}{sous-espaces} propres.
\end{enumerate}
\end{solution}

\begin{solution}{pb:b1:vspaces:1}
\textbf{1.} $L_i$ est un produit de $n$ facteurs du premier degré
divisé par une constante non nulle (les $x_i$ sont distincts), donc
$\deg L_i = n$. En évaluant en $x_j$ avec $j \neq i$~: le facteur $X -
x_j$ du numérateur s'annule, donc $L_i(x_j) = 0$. En $x_i$, numérateur
et dénominateur coïncident~: $L_i(x_i) = 1$.

\textbf{2.} Supposons $\sum_i \lambda_i L_i = 0$. Évaluons en $x_j$~:
tous les termes meurent sauf $\lambda_j L_j(x_j) = \lambda_j$, donc
$\lambda_j = 0$ pour tout $j$~: la famille est \omterm{def:b1:vspaces:free}{libre}.

\textbf{3.} Posons $D = P - \sum_i P(x_i) L_i$. Alors $\deg D \leq n$
et, par la question 1, $D(x_j) = P(x_j) - P(x_j) = 0$ aux $n + 1$
points distincts $x_0, \dots, x_n$. Un \omterm{def:b1:poly:def}{polynôme} non nul de degré
$\leq n$ a au plus $n$ racines (\cref{cor:b1:poly:nroots}), donc $D
= 0$. Ainsi tout $P \in \R_n[X]$ est combinaison des $L_i$~: la
famille est génératrice, et avec la question 2, c'est une \omterm{def:b1:vspaces:free}{base}.

\textbf{4.} Étant donné $y_0, \dots, y_n$, le \omterm{def:b1:poly:def}{polynôme} $P = \sum_i
y_i L_i$ est de degré $\leq n$ et interpole. Unicité~: un tel $P$ a,
par la question 3, pour \omterm{prop:b1:vspaces:coordinates}{coordonnées} $(P(x_0), \dots,
P(x_n)) = (y_0, \dots, y_n)$ dans la \omterm{def:b1:vspaces:free}{base} $(L_i)$, et les \omterm{prop:b1:vspaces:coordinates}{coordonnées}
dans une \omterm{def:b1:vspaces:free}{base} sont uniques (\cref{prop:b1:vspaces:coordinates}). Les
\omterm{prop:b1:vspaces:coordinates}{coordonnées} de $P$ dans la \omterm{def:b1:vspaces:free}{base} de Lagrange sont ses \emph{valeurs aux
nœuds} --- c'est tout l'intérêt de cette \omterm{def:b1:vspaces:free}{base}.

\textbf{5.} Appliquons la question 3 à $P = X^k$ ($0 \leq k \leq n$)~:
\[
X^k = \sum_{i=0}^{n} x_i^{k} L_i ,
\]
et $k = 0$ donne $\sum_i L_i = 1$.

\textbf{6.} $\deg N_k = k$ exactement~: la famille $(N_0, \dots, N_n)$
est un escalier de degrés dans $\R_n[X]$, donc une \omterm{def:b1:vspaces:free}{base} d'après
l'\cref{ex:b1:vspaces:staircase} (liberté par
la \cref{prop:b1:vspaces:freecriteria}~(1), caractère générateur par
descente finie sur le degré).

\textbf{7.} D'après la récurrence,
\[
f[x_0, x_1] = \frac{f(x_1) - f(x_0)}{x_1 - x_0},
\qquad
f[x_0, x_1, x_2] = \frac{f[x_1, x_2] - f[x_0, x_1]}{x_2 - x_0}.
\]
Pour $f(x) = x^2$~:
\[
f[x_0, x_1] = \frac{x_1^2 - x_0^2}{x_1 - x_0} = x_0 + x_1,
\]
puis
\[
f[x_0, x_1, x_2]
= \frac{(x_1 + x_2) - (x_0 + x_1)}{x_2 - x_0}
= \frac{x_2 - x_0}{x_2 - x_0} = 1 .
\]

\textbf{8.} $\deg S \leq n$ puisque $Q, R$ sont de degré $\leq n - 1$.
En $x_0$~: $S(x_0) = \frac{-(x_0 - x_n) R(x_0)}{x_n - x_0} =
R(x_0) = f(x_0)$. En $x_n$~: $S(x_n) = \frac{(x_n - x_0)
Q(x_n)}{x_n - x_0} = Q(x_n) = f(x_n)$. En un nœud \omterm{def:b1:topology:closure}{intérieur} $x_i$
($1 \leq i \leq n-1$), $Q$ et $R$ prennent tous deux la valeur
$f(x_i)$, donc
\[
S(x_i) = \frac{(x_i - x_0) - (x_i - x_n)}{x_n - x_0}\, f(x_i)
= f(x_i) .
\]

\textbf{9.} Par récurrence sur le nombre de nœuds. Un nœud~:
l'interpolant est la constante $f(x_0) = f[x_0]$. Supposons
l'\omterm{def:b1:logic:statement}{assertion} vraie pour $k$ nœuds et soit $S$ interpolant en $x_0,
\dots, x_k$~; par unicité (question 4), $S$ est donné par le lemme
d'Aitken à partir de $R$ (nœuds $x_0, \dots, x_{k-1}$) et de $Q$
(nœuds $x_1, \dots, x_k$). Le coefficient de $X^{k}$ dans $S$ vaut
\[
\frac{[X^{k-1}]\,Q - [X^{k-1}]\,R}{x_k - x_0}
= \frac{f[x_1, \dots, x_k] - f[x_0, \dots, x_{k-1}]}{x_k - x_0}
= f[x_0, \dots, x_k]
\]
par l'hypothèse de récurrence et la relation de récurrence de
définition.

\textbf{10.} Soit $P_k$ l'interpolant de $f$ en $x_0, \dots, x_k$. La
différence $P_k - P_{k-1}$ est de degré $\leq k$ et s'annule en
$x_0, \dots, x_{k-1}$, donc, par le théorème de factorisation appliqué
$k$ fois (\cref{thm:b1:poly:factor}), elle vaut $c\,N_k$ pour une
constante $c$~; en comparant les coefficients de $X^{k}$ et en
utilisant la question 9, $c = f[x_0, \dots, x_k]$. Le télescopage à
partir de $P_0 = f(x_0) N_0$ donne la formule de Newton. Pour la
formule close, écrivons $P_k = \sum_{i \leq
k} f(x_i) L_i$ (Lagrange, sur les nœuds $x_0, \dots, x_k$) et lisons
le coefficient de $X^{k}$~: chaque $L_i$ contribue
$\frac{1}{\prod_{j \neq i}(x_i - x_j)}$, d'où
\[
f[x_0, \dots, x_k] = \sum_{i=0}^{k}
\frac{f(x_i)}{\prod_{j \neq i,\, j \leq k}(x_i - x_j)} .
\]
Le membre de droite est invariant par toute \omterm{def:b1:counting:objects}{permutation} des nœuds, donc
la différence divisée ne dépend pas de leur ordre.

\textbf{11.} Si $P = a X^m + (\text{degrés inférieurs})$, la formule du
binôme donne
\[
\Delta P = a\bigl((X+1)^m - X^m\bigr) + \dots
= a\,m\,X^{m-1} + (\text{degrés inférieurs}),
\]
car $(X+1)^m - X^m = m X^{m-1} + \dots$ et la partie de degré inférieur
de $P$ ne contribue, après $\Delta$, qu'en degré $\leq m - 2$ (ou en
termes de degré $\leq m-2$). Donc
$\deg \Delta P = m - 1$ de coefficient dominant $m a$. Une constante
$c$ donne $\Delta c = c - c = 0$.

\textbf{12.} $\deg B_k = k$~: escalier, donc une \omterm{def:b1:vspaces:free}{base} de
$\R_n[X]$ (\cref{ex:b1:vspaces:staircase}). Pour $\Delta B_k$
($k \geq 1$), factorisons le produit commun~:
\begin{align*}
k!\,\Delta B_k
&= (X+1)X\cdots(X-k+2) - X(X-1)\cdots(X-k+1) \\
&= X(X-1)\cdots(X-k+2)\,\bigl[(X+1) - (X-k+1)\bigr] \\
&= k\,X(X-1)\cdots(X-k+2),
\end{align*}
donc $\Delta B_k = \frac{X(X-1)\cdots(X-k+2)}{(k-1)!} = B_{k-1}$.

\textbf{13.} Écrivons $P = \sum_{k=0}^{n} c_k B_k$ (\omterm{def:b1:vspaces:free}{base}, question
12). Appliquons $\Delta^{j}$~: d'après la question 12, $\Delta^{j} P =
\sum_{k \geq j} c_k B_{k-j}$. Évaluons en $0$~: $B_0(0) = 1$ et
$B_m(0) = 0$ pour $m \geq 1$ (le facteur $X$ s'annule), donc
$\bigl(\Delta^{j}P\bigr)(0) = c_j$. C'est la formule des différences
progressives.

\textbf{14.} Par récurrence sur $k$. Pour $k = 0$, l'identité s'écrit
$P(0) = P(0)$. Supposons-la vraie au rang $k$ et appliquons-la à
$\Delta P$~:
\[
\bigl(\Delta^{k+1} P\bigr)(0)
= \sum_{j=0}^{k} (-1)^{k-j}\binom kj \bigl(P(j+1) - P(j)\bigr).
\]
Collectons le coefficient de $P(i)$~: il vaut $(-1)^{k-i+1}\binom
k{i-1}\cdot(-1)^{0}$ venant de la première somme (décalée) et
$-(-1)^{k-i}\binom ki$ venant de la seconde --- au total
\[
(-1)^{k+1-i}\Bigl(\binom k{i-1} + \binom ki\Bigr)
= (-1)^{k+1-i}\binom{k+1}i
\]
par la règle de Pascal, ce qui est l'identité au rang $k + 1$.

\textbf{15.} En itérant la question 11 depuis le degré $n$, de
coefficient dominant $a_n$~: après un $\Delta$, degré $n-1$ et
coefficient dominant $n a_n$~; après deux, $n(n-1)a_n$~; après $n$
étapes, degré $0$ et valeur $n(n-1)\cdots 1\, a_n = n!\,a_n$, une
constante. Un $\Delta$ de plus la tue~: $\Delta^{n+1}P = 0$.

\textbf{16.} Si $m \geq k$~: $B_k(m) = \binom mk \in \N$. Si $0
\leq m < k$~: l'un des facteurs de $m(m-1)\cdots(m-k+1)$ est nul, donc
$B_k(m) = 0$. Si $m = -q$ avec $q \geq 1$~:
\[
B_k(-q) = \frac{(-q)(-q-1)\cdots(-q-k+1)}{k!}
= (-1)^k\,\frac{q(q+1)\cdots(q+k-1)}{k!}
= (-1)^k \binom{q+k-1}{k},
\]
un entier. Donc chaque $B_k$ envoie $\Z$ dans $\Z$.

\textbf{17.} ($\Leftarrow$) Si $P = \sum_k c_k B_k$ avec $c_k \in
\Z$, alors pour $m \in \Z$, $P(m) = \sum_k c_k B_k(m) \in \Z$ par la
question 16. ($\Rightarrow$) Si $P$ est à valeurs entières, ses
\omterm{prop:b1:vspaces:coordinates}{coordonnées} sont $c_k = \bigl(\Delta^k P\bigr)(0) = \sum_{j=0}^k
(-1)^{k-j}\binom kj P(j)$ (questions 13 et 14), combinaison à
coefficients entiers des entiers $P(0), \dots, P(k)$. C'est la
caractérisation de Pólya des \omterm{pb:b1:vspaces:1}{polynômes à valeurs entières}.

\textbf{18.} Posons $Q(X) = P(X + a)$, \omterm{def:b1:poly:def}{polynôme} de degré $\leq
n$ avec $Q(0), Q(1), \dots, Q(n) \in \Z$. Ses \omterm{prop:b1:vspaces:coordinates}{coordonnées} dans
$(B_k)_{k \leq n}$ sont $c_k = \sum_{j \leq k}(-1)^{k-j}\binom kj
Q(j) \in \Z$ (la question 14 n'utilise que les valeurs en $0, \dots, k
\leq n$). Par la question 17 ($\Leftarrow$), $Q$ est à valeurs entières
sur $\Z$ tout entier, et donc $P(X) = Q(X - a)$ aussi.

\textbf{19.} Un produit de $k$ entiers consécutifs s'écrit $m(m-1)
\cdots(m-k+1) = k!\,B_k(m)$ pour un certain $m \in \Z$, et $B_k(m) \in
\Z$ par la question 16~: le produit est divisible par $k!$.

\textbf{20.} Valeurs de $P = \frac{X(X+1)(2X+1)}{6}$ en $0,1,2,3$~:
$0, 1, 5, 14$. Table des différences~: ligne $\Delta$~: $1, 4, 9$~;
ligne $\Delta^2$~: $3, 5$~; ligne $\Delta^3$~: $2$. Donc, par la
question 13,
\[
P = 0\cdot B_0 + 1\cdot B_1 + 3\,B_2 + 2\,B_3 ,
\]
à \omterm{prop:b1:vspaces:coordinates}{coordonnées} entières~: $P$ est à valeurs entières (question 17),
alors que ses coefficients monomiaux $\frac13, \frac12, \frac16$ ne
sont pas entiers. Calcul direct~:
\begin{align*}
\Delta P &= \frac{(X+1)(X+2)(2X+3) - X(X+1)(2X+1)}{6} \\
&= \frac{(X+1)\bigl[(X+2)(2X+3) - X(2X+1)\bigr]}{6}
= \frac{(X+1)(6X+6)}{6} = (X+1)^2 .
\end{align*}
Par télescopage $P(m) = \sum_{j=0}^{m-1}\Delta P(j) = \sum_{j=1}^{m}
j^2$ (avec $P(0) = 0$)~: la formule de la somme des carrés.

\textbf{21.} Les valeurs $2^i$ en $i = 0, \dots, n$ ont une table des
différences constamment égale à $1$ sur le bord gauche~: $\Delta^k$ de
la suite $(2^i)$ redonne $(2^i)$ (puisque $2^{i+1} - 2^i = 2^i$), donc
$\bigl(\Delta^k P\bigr)(0) = 2^0 = 1$ pour tout $k \leq n$, et $P =
B_0 + B_1 + \dots + B_n$ par la question 13. Alors
\[
P(n+1) = \sum_{k=0}^{n}\binom{n+1}{k}
= 2^{n+1} - \binom{n+1}{n+1} = 2^{n+1} - 1 \neq 2^{n+1}:
\]
le schéma se brise au premier point non contrôlé.

\textbf{22.} Par la question 12, $B_k = \Delta B_{k+1}$, donc
\[
\sum_{j=0}^{m-1} B_k(j)
= \sum_{j=0}^{m-1}\bigl(B_{k+1}(j+1) - B_{k+1}(j)\bigr)
= B_{k+1}(m) - B_{k+1}(0) = B_{k+1}(m).
\]
Pour $j < k$, les termes $B_k(j)$ sont nuls, donc la somme commence en
réalité à $j = k$~: $\sum_{j=k}^{m-1}\binom jk = \binom m{k+1}$,
l'identité de la crosse de hockey.

\textbf{23.} Les tables des différences (ou un développement direct)
donnent
\[
X^2 = B_1 + 2 B_2, \qquad X^3 = B_1 + 6 B_2 + 6 B_3
\]
(vérification~: $B_1 + 2B_2 = X + X(X-1) = X^2$~; en $X = 1, 2, 3$ la
seconde donne $1, 8, 27$). La question 22 fournit alors
\[
\sum_{j=0}^{m-1} j^2 = B_2(m) + 2B_3(m)
= \binom m2 + 2\binom m3 = \frac{m(m-1)(2m-1)}{6},
\]
\[
\sum_{j=0}^{m-1} j^3 = B_2(m) + 6B_3(m) + 6B_4(m)
= \binom m2 + 6\binom m3 + 6\binom m4 .
\]
En développant la dernière expression~: $\binom m2 + 6\binom m3 +
6\binom m4 = \frac{m(m-1)}{2}\bigl[1 + 2(m-2) +
\frac{(m-2)(m-3)}{2}\bigr] = \frac{m^2(m-1)^2}{4} = \binom m2^2$.
En remplaçant $m$ par $m + 1$~: $1^3 + \dots + m^3 =
\bigl(\frac{m(m+1)}2\bigr)^2 = (1 + \dots + m)^2$, l'identité de
Nicomaque.

\textbf{24.} Nœuds $0, 1, 2$, \omterm{def:b1:poly:def}{polynôme} $X^2$. \omterm{def:b1:vspaces:free}{Base} monomiale
$(1, X, X^2)$~: \omterm{prop:b1:vspaces:coordinates}{coordonnées} $(0, 0, 1)$. \omterm{def:b1:vspaces:free}{Base} de Lagrange~: les
\omterm{prop:b1:vspaces:coordinates}{coordonnées} sont les valeurs $(0, 1, 4)$ (question 4). \omterm{def:b1:vspaces:free}{Base} de Newton
$(1, X, X(X-1))$~: \omterm{pb:b1:vspaces:1}{différences divisées} $f[0] = 0$, $f[0,1] = 1$,
$f[0,1,2] = \frac{3 - 1}{2} = 1$ (question 9), d'où les \omterm{prop:b1:vspaces:coordinates}{coordonnées}
$(0, 1, 1)$ --- et en effet $X + X(X-1) = X^2$. Trois \omterm{def:b1:vspaces:free}{bases}, trois
vecteurs de \omterm{prop:b1:vspaces:coordinates}{coordonnées}, un seul \omterm{def:b1:poly:def}{polynôme}.

\textbf{25.} (i) La question 4 est automatique parce que $(L_i)$ est
une \emph{\omterm{def:b1:vspaces:free}{base}}~: existence et unicité de l'interpolation sont
exactement existence et unicité des \omterm{prop:b1:vspaces:coordinates}{coordonnées}. (ii) La \omterm{def:b1:vspaces:free}{base} de
Newton est un escalier, donc les \omterm{prop:b1:vspaces:coordinates}{coordonnées} se calculent par
divisions successives --- chaque nouveau nœud ajoute un terme sans
perturber les précédents --- alors que les \omterm{prop:b1:vspaces:coordinates}{coordonnées} de $P$ dans la
\omterm{def:b1:vspaces:free}{base} de Lagrange sont les valeurs $P(x_i)$, disponibles sans le moindre
calcul. (iii) Les deux \omterm{def:b1:vspaces:free}{bases} sont \omterm{def:b1:vspaces:free}{libres} par les deux mêmes critères de
la \cref{prop:b1:vspaces:freecriteria}~: degrés distincts pour Newton,
évaluation aux nœuds pour Lagrange. (iv) Le théorème de Pólya dit que
«~$P(\Z) \subseteq \Z$~», propriété des valeurs, équivaut au fait que
les \omterm{prop:b1:vspaces:coordinates}{coordonnées} dans la \omterm{def:b1:vspaces:free}{base} $(B_k)$ soient entières --- l'arithmétique
d'un \omterm{def:b1:poly:def}{polynôme} ne devient visible que dans la \omterm{def:b1:vspaces:free}{base} adaptée à la
question.
\end{solution}
