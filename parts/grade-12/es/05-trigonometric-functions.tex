\chapter{Funciones trigonométricas}\label{ch:g12:trigo}

El funciones $\cos $ y $\sin $ convertir la geometría del círculo en
análisis. Definidos enrollando la línea real alrededor del círculo unitario, son
el arquetipo de periódico funciones, y sus derivadas hazlos
soluciones de la \omterm{def:g10:algebra:equation}{ecuación} de oscilaciones $ y'' = -y $.

\section{El círculo unitario}

\begin{definition}[Coseno y seno]\label{def:g12:trigo:cossin}
Sea $\mathcal{C}$ ser el círculo de radio 1 con centro en el origen de un
\omterm{def:g10:coordgeom:system}{ortonormal} \omterm{def:g10:coordgeom:system}{sistema de coordenadas}. a cada real $ t $, asocia el punto $ M(t)$
obtenido enrollando una longitud $\abs{t}$ a lo largo de $\mathcal{C}$ desde el punto
$ I(1,0)$, en sentido antihorario si $ t \geq 0$ y en el sentido de las agujas del reloj en caso contrario. Entonces
\emph{$\cos t $ y $\sin t $ son las coordenadas de
$ M(t)$}\index{$\cos t $ y $\sin t $ son las coordenadas de
$ M(t)$}\index{seno}:
\[
M(t) = (\cos t, \sin t).
\]
\end{definition}

\begin{omfigure}
\begin{tikzpicture}[scale=2.2]
  \draw[->] (-1.25,0) -- (1.35,0) node[right] {$ x $};
  \draw[->] (0,-1.25) -- (0,1.35) node[above] {$ y $};
  \draw (0,0) circle (1);
  \coordinate (O) at (0,0);
  \coordinate (M) at (50:1);
  \draw[thick,omDef] (O) -- (M);
  \fill (M) circle (0.6pt) node[above right] {$ M(t)$};
  \fill (1,0) circle (0.6pt) node[below right] {$ I $};
  \draw[dashed] (M) -- (M |- O) node[below,pos=1.05] {$\cos t $};
  \draw[dashed] (M) -- (M -| O) node[left,pos=1.02] {$\sin t $};
  \draw[->,omThm,thick] (0.35,0) arc (0:50:0.35);
  \node[omThm] at (25:0.5) {$ t $};
\end{tikzpicture}
\end{omfigure}

Como el circulo tiene circunferencia $2\pi $, el punto $ M(t)$ no cambia cuando
$ t $ aumenta en $2\pi $; y desde $ M(t)$ se encuentra en el círculo unitario:

\begin{proposition}[Identidades fundamentales]\label{prop:g12:trigo:identities}
A pesar de $ t \in \R $ y $ k \in \Z $:
\[
\cos^2 t + \sin^2 t = 1, \qquad
\cos(t + 2k\pi) = \cos t, \qquad
\sin(t + 2k\pi) = \sin t,
\]
\[
\cos(-t) = \cos t, \qquad \sin(-t) = -\sin t,
\]
\[
\cos(\pi - t) = -\cos t, \quad \sin(\pi - t) = \sin t, \quad
\cos\left(\tfrac{\pi}{2} - t\right) = \sin t, \quad
\sin\left(\tfrac{\pi}{2} - t\right) = \cos t .
\]
\end{proposition}

\begin{proof}
La primera identidad es la \omterm{def:g10:algebra:equation}{ecuación} del círculo unitario; los demás expresan el
simetrías del círculo: $ M(-t)$ es el reflejo de $ M(t)$ en el
$ x $-eje, $ M(\pi - t)$ en el $ y $-eje, y $ M(\frac\pi2 - t)$ en la linea
$ y = x $.
\end{proof}

Los valores a saber de memoria:
\begin{center}
\begin{tabular}{c|ccccc}
$ t $ & $0$ & $\dfrac{\pi}{6}$ & $\dfrac{\pi}{4}$ & $\dfrac{\pi}{3}$ & $\dfrac{\pi}{2}$\\[6pt]
\hline\rule{0pt}{14pt}%
$\cos t $ & $1$ & $\dfrac{\sqrt3}{2}$ & $\dfrac{\sqrt2}{2}$ & $\dfrac12$ & $0$\\[6pt]
$\sin t $ & $0$ & $\dfrac12$ & $\dfrac{\sqrt2}{2}$ & $\dfrac{\sqrt3}{2}$ & $1$
\end{tabular}
\end{center}

\begin{proposition}[Fórmulas de suma]\label{prop:g12:trigo:addition}
A pesar de $ a, b \in \R $:
\begin{align*}
\cos(a+b) &= \cos a \cos b - \sin a \sin b, &
\sin(a+b) &= \sin a \cos b + \cos a \sin b,\\
\cos(a-b) &= \cos a \cos b + \sin a \sin b, &
\sin(a-b) &= \sin a \cos b - \cos a \sin b.
\end{align*}
En particular $\cos 2a = 2\cos^2 a - 1 = 1 - 2\sin^2 a $ y
$\sin 2a = 2 \sin a \cos a $.
\end{proposition}

\begin{proof}
El \omterm{def:g11:vect:vector}{vectores} $\vect{u} = (\cos a, \sin a)$ y $\vect{v} = (\cos b, \sin b)$
son unidad \omterm{def:g11:vect:vector}{vectores} haciendo un ángulo $ a - b $; su \omterm{def:g11:scal:dot}{producto escalar} es igual a ambos
$\cos a\cos b + \sin a \sin b $ (\omterm{def:g10:coordgeom:system}{coordenadas}) y
$\norm{\vect u}\,\norm{\vect v}\cos(a-b) = \cos(a - b)$ (geometría), que es
la tercera fórmula. Reemplazo $ b $ por $-b $ da el primero; el \omterm{def:g11:trigo:cossin}{señor} fórmulas
sigue usando $\sin x = \cos(\frac\pi2 - x)$. Las fórmulas de duplicación son las
caso $ b = a $ combinado con $\cos^2 + \sin^2 = 1$.
\end{proof}

\section{Estudio analítico}

\begin{lemma}[Límite fundamental]\label{lem:g12:trigo:sinxx}
\[
\lim_{t \to 0} \frac{\sin t}{t} = 1
\qquad\text{and}\qquad
\lim_{t \to 0} \frac{\cos t - 1}{t} = 0 .
\]
\end{lemma}

\begin{proof}
Para $0 < t < \frac{\pi}{2}$, compara tres áreas en el círculo unitario: la
triangulo $ OIM(t)$, el sector circular $ OIM(t)$, y el triángulo rectángulo con
base $ OI $ y altura $\tan t $:
\[
\frac{\sin t}{2} \leq \frac{t}{2} \leq \frac{\tan t}{2}.
\]

\begin{omfigure}
\begin{tikzpicture}[scale=2.2]
  \draw[->] (-0.25,0) -- (1.5,0) node[right] {$ x $};
  \draw[->] (0,-0.25) -- (0,1.45) node[above] {$ y $};
  \fill[omDef!20] (0,0) -- (1,0) -- (50:1) -- cycle;
  \draw[gray, thin] (-10:1) arc (-10:80:1);
  \draw[omProp, thick] (0,0) -- (1,0) -- (1,1.1918) -- cycle;
  \draw[omDef] (0,0) -- (1,0) -- (50:1) -- cycle;
  \draw[omThm, thick] (1,0) arc (0:50:1);
  \draw[dashed] (50:1) -- (0.643,0);
  \node[left, font=\small] at (0.68,0.35) {$\sin t $};
  \node[omProp, right, font=\small] at (1,0.62) {$\tan t $};
  \node[omThm, font=\small] at (20:0.5) {$ t $};
  \fill (50:1) circle (0.6pt);
  \node at (58:1.22) {$ M(t)$};
  \fill (1,0) circle (0.6pt) node[below right] {$ I $};
  \node[below left] at (0,0) {$ O $};
\end{tikzpicture}

{\small Las tres áreas anidadas: el triángulo $ OIM $ (azul, área
$\frac{\sin t}{2}$), el sector circular (delimitado por el arco rojo, área
$\frac{t}{2}$), y el triángulo rectángulo de altura $\tan t $ (naranja, área
$\frac{\tan t}{2}$).}
\end{omfigure}

La primera desigualdad da $\frac{\sin t}{t} \leq 1$; el segundo da
$\cos t \leq \frac{\sin t}{t}$. Como $ t \to 0^+$, $\cos t \to 1$ (continuidad),
y el teorema de compresión produce $\frac{\sin t}{t} \to 1$; la paridad extiende esto
a $ t \to 0$. Para el segundo límite,
\[
\frac{\cos t - 1}{t} = \frac{\cos^2 t - 1}{t(\cos t + 1)}
= -\frac{\sin t}{t}\cdot\frac{\sin t}{\cos t + 1}
\xrightarrow[t\to0]{} -1 \cdot \frac{0}{2} = 0. \qedhere
\]
\end{proof}

\begin{theorem}[Derivadas de seno y coseno]\label{thm:g12:trigo:derivatives}
El funciones $\sin $ y $\cos $ son \omterm{def:g12:deriv:derivative}{diferenciable} en $\R $, con
\[
(\sin)' = \cos, \qquad (\cos)' = -\sin .
\]
De manera más general, $(\sin(ax+b))' = a\cos(ax+b)$ y
$(\cos(ax+b))' = -a\sin(ax+b)$.
\end{theorem}

\begin{proof}
Por las fórmulas de suma,
\[
\frac{\sin(x+h) - \sin x}{h}
= \sin x\,\frac{\cos h - 1}{h} + \cos x\,\frac{\sin h}{h}
\xrightarrow[h\to0]{} \sin x \cdot 0 + \cos x \cdot 1 = \cos x,
\]
usando \cref{lem:g12:trigo:sinxx}. El cálculo para $\cos $ es idéntico,
y la forma general se deriva de la regla de la cadena.
\end{proof}

\begin{proposition}[Variaciones]\label{prop:g12:trigo:variations}
En $\intcc{0}{\pi}$, $\cos $ disminuye de $1$ a $-1$. En
$\intcc{-\frac\pi2}{\frac\pi2}$, $\sin $ aumenta de $-1$ a $1$. Ambos
funciones son $2\pi $-periódico y tiene \omterm{def:g10:stats:quartiles}{rangos} $\intcc{-1}{1}$; $\cos $ es
par, $\sin $ es \omterm{def:g11:func:parity}{impar}.
\end{proposition}

\begin{proof}
En $\intoo{0}{\pi}$, $(\cos)' = -\sin < 0$ desde el punto $ M(t)$ tiene
positivo ordenada allá; similarmente $(\sin)' = \cos > 0$ en
$\intoo{-\frac\pi2}{\frac\pi2}$. La periodicidad y la paridad provienen de
\cref{prop:g12:trigo:identities}.
\end{proof}

\begin{omfigure}
\begin{tikzpicture}
\begin{axis}[
  width=12cm, height=4.5cm,
  axis lines=middle,
  xmin=-7, xmax=7, ymin=-1.4, ymax=1.4,
  xtick={-6.2832,-3.1416,3.1416,6.2832},
  xticklabels={$-2\pi $,$-\pi $,$\pi $,$2\pi $},
  ytick={-1,1},
  samples=200, domain=-7:7]
  \addplot[omDef,thick] {cos(deg(x))};
  \addplot[omThm,thick] {sin(deg(x))};
\end{axis}
\end{tikzpicture}

{\small las curvas de $\cos $ (azul) y $\sin $ (rojo).}
\end{omfigure}

\begin{method}[resolviendo $\cos x = a $ y $\sin x = a $]\label{met:g12:trigo:equations}
Para $ a \in \intcc{-1}{1}$, encuentra una solución particular $\alpha $ (de la
tabla de valores o una calculadora). Entonces todas las soluciones son:
\[
\cos x = \cos\alpha \iff x = \alpha + 2k\pi \text{ or } x = -\alpha + 2k\pi,
\quad k \in \Z;
\]
\[
\sin x = \sin\alpha \iff x = \alpha + 2k\pi \text{ or } x = \pi - \alpha + 2k\pi,
\quad k \in \Z.
\]
Para desigualdades, ubique los arcos solución en el círculo unitario y lea
el \omterm{def:g10:numbers:interval}{intervalos} dentro de un período.
\end{method}

\begin{example}\label{ex:g12:trigo:equation}
Resolver $\cos x = \frac12$ en $\intoc{-\pi}{\pi}$: una solución particular es
$\frac\pi3$, entonces las soluciones son $ x = \frac\pi3$ y $ x = -\frac\pi3$.
en todos $\R $: $ x = \pm\frac\pi3 + 2k\pi $, $ k \in \Z $.
\end{example}

\section{Ejercicios}

\begin{exercise}[$\star $]\label{exo:g12:trigo:1}
Calcular $\cos\frac{2\pi}{3}$, $\sin\frac{5\pi}{6}$, $\cos\frac{7\pi}{4}$ y
$\sin\left(-\frac{\pi}{3}\right)$ utilizando las identidades de
\cref{prop:g12:trigo:identities}.
\end{exercise}

\begin{exercise}[$\star $]\label{exo:g12:trigo:2}
Diferenciar $ f(x) = \sin^2 x $, $ g(x) = \cos(3x) + x\sin x $ y
$ h(x) = \dfrac{\sin x}{2 + \cos x}$.
\end{exercise}

\begin{exercise}[$\star $]\label{exo:g12:trigo:3}
resolver en $\intoc{-\pi}{\pi}$, entonces en $\R $:
\[
\text{(a) } \sin x = \frac{\sqrt3}{2};
\qquad
\text{(b) } \cos\left(2x\right) = \frac{\sqrt2}{2} .
\]
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g12:trigo:4}
Usando las fórmulas de suma, calcule el valor exacto de
$\cos\frac{\pi}{12}$ y $\sin\frac{\pi}{12}$.
(Pista: $\frac{\pi}{12} = \frac{\pi}{3} - \frac{\pi}{4}$.)
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g12:trigo:5}
resolver en $\intcc{0}{2\pi}$ la desigualdad $2\sin^2 x - \sin x - 1 \geq 0$.
(Pista: factoriza la cuadrática en $\sin x $.)
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g12:trigo:6}
Calcular los límites
\[
\lim_{x\to0} \frac{\sin 3x}{x}, \qquad
\lim_{x\to0} \frac{1 - \cos x}{x^2}, \qquad
\lim_{x\to+\infty} x \sin\frac{1}{x}.
\]
\end{exercise}

\begin{exercise}[$\star\star $]\label{exo:g12:trigo:7}
Estudia la \omterm{def:g11:func:function}{función} $ f(x) = x + \cos x $ en $\intcc{0}{2\pi}$: variaciones y
extremos. mostrar que $ f $ es \omterm{def:g12:seq:monotonic}{creciente} en $\R $ para todo $ f'$ desaparece en
infinitos puntos.
\end{exercise}

\begin{exercise}[$\star\star\star $]\label{exo:g12:trigo:8}
Sea $ f(x) = A\cos x + B \sin x $ con $(A,B) \neq (0,0)$.
\begin{enumerate}
  \item mostrar que $ f $ se puede escribir $ f(x) = R\cos(x - \varphi)$ con
        $ R = \sqrt{A^2 + B^2}$ y un adecuado $\varphi $.
  \item deducir el \omterm{def:g10:functions:extrema}{máximo} y \omterm{def:g10:functions:extrema}{mínimo} de $ f $y resolver
        $\cos x + \sin x = 1$ en $\intoc{-\pi}{\pi}$.
\end{enumerate}
\end{exercise}

\begin{exercise}[$\star\star\star $]\label{exo:g12:trigo:9}
mostrar que $\sin $ y $\cos $ ambos satisfacen el diferencial \omterm{def:g10:algebra:equation}{ecuación}
$ y'' = -y $. Por el contrario, dejemos $ y $ ser una solución de $ y'' = -y $ con
$ y(0) = a $ y $ y'(0) = b $; \omterm{def:g10:proba:sample}{muestra} que $ y = a\cos + b\sin $.
(Pista: considere $ g = (y - a\cos - b\sin)$ y la "energía"
$ E = g^2 + (g')^2$.)
\end{exercise}

\section{Problema: el secreto del péndulo}

\begin{problem}[{Problema de fin de semana --- un límite geométrico de potencias
las derivadas del seno y el coseno, y de ellas todas
oscilación: resortes, péndulos, latidos y el metro que casi
era}]
\label{pb:g12:trigo:1}
Cada reloj que alguna vez hizo tictac, cada cuerda que alguna vez sonó,
obedece a las mismas matemáticas: una fuerza restauradora proporcional a
desplazamiento, de ahí el \omterm{def:g10:algebra:equation}{ecuación} $ y'' = -\omega^2 y $, por eso
\omterm{def:g11:trigo:cossin}{coseños}. En la base de todo esto se encuentra un límite que parece inocente,
$\frac{\sin x}{x} \to 1$ --- prometido en el noveno grado, utilizado por
\cref{thm:g12:trigo:derivatives}, y lo demuestra aquí con una foto.
Este problema sube desde ese límite hasta el balanceo de un metro
péndulo, y explica por qué el metro es, por un pelo, no
definido por él.

\medskip
\noindent\textbf{Part I --- The fundamental limit.}
\begin{enumerate}
  \item Calentar numéricamente: calcular $\frac{\sin x}{x}$ para
        $ x = 0.1$ y $ x = 0.01$ (radianes!).
  \item El sándwich, en el círculo unitario con
        $0 < x < \frac\pi2$: expresar mediante geometría elemental la
        áreas de (i) el triángulo con vértices $ O $, $ A(1,0)$
        y el punto del círculo $ M(\cos x, \sin x)$; (ii) el
        sector circular $ OAM $; (iii) el triángulo rectángulo $ OAT $
        dónde $ T(1, \tan x)$.
  \item De la zona del sándwich deducir
        $\sin x < x < \tan x $, entonces
        $\cos x < \frac{\sin x}{x} < 1$, y concluir con el
        teorema de compresión que
        $\lim_{x \to 0} \frac{\sin x}{x} = 1$ (controles de paridad
        $ x < 0$).
  \item Deducir
        $\lim_{x \to 0} \frac{1 - \cos x}{x^2} = \frac12$
        (multiplicar por el conjugado).
  \item Explique por qué este límite es el motor de
        \cref{thm:g12:trigo:derivatives} (qué es
        $\sin'(0)$?), y por qué salda también una antigua deuda: en
        que sentido tienen \emph{radianes} la única unidad angular para
        cual $\sin' = \cos $ se mantiene sin una constante?
\end{enumerate}

\medskip
\noindent\textbf{Part II --- Harmonic motion.}
\begin{enumerate}[resume]
  \item Verifique por la regla de la cadena que
        $ y(t) = R\cos(\omega t - \varphi)$ satisface
        $ y'' = -\omega^2 y $.
  \item Una masa sobre un resorte obedece $ y'' = -4y $, con
        $ y(0) = 3$ y $ y'(0) = 8$. Admitir (de
        \cref{exo:g12:trigo:9}) que todas las soluciones son
        $ A\cos 2t + B\sin 2t $, determinar $ A $ y $ B $.
  \item Reescribe la solución como $ R\cos(2t - \varphi)$
        (\cref{exo:g12:trigo:8}): da la amplitud $ R $, el
        periodo y la fase $\varphi $ (tres decimales).
  \item ¿En qué momento pasa la masa por primera vez?
        posición de equilibrio $ y = 0$?
  \item El péndulo: una pesa en una cuerda de longitud $ L $ obedece
        \emph{exactamente} $\theta'' = -\frac gL \sin\theta $ ---
        irresoluble por elemental funciones. Para columpios pequeños,
        reemplazar $\sin\theta $ por $\theta $ (¡La parte I lo justifica!)
        y deducir el periodo
        $ T = 2\pi\sqrt{\frac Lg}$. Calcular $ T $ para
        $ L = 1$ metro ($ g = 9.81$): El famoso descubrimiento de Galileo ---
        ¿Qué significa la fórmula? \emph{no} ¿depender de?
  \item El péndulo de segundos: qué longitud da
        $ T = 2$ es exactamente? La Academia Francesa de 1791 vaciló
        entre esta longitud y la diezmillonésima parte del
        cuarto de meridiano para definir el \emph{metro} --- por
        ¿En cuántos milímetros se diferencian los dos candidatos?
\end{enumerate}

\medskip
\noindent\textbf{Part III --- Addition formulas at work.}
\begin{enumerate}[resume]
  \item Mostrar con las fórmulas de suma.
        (\cref{prop:g12:trigo:addition}) eso
        $\cos\left(x + \frac\pi2\right) = -\sin x $ y
        $\sin\left(x + \frac\pi2\right) = \cos x $: circular
        la diferenciación es un cuarto de vuelta --- comprobar la coherencia
        con \cref{thm:g12:trigo:derivatives}.
  \item Deducir la identidad del producto a la suma
        $\cos a \cos b = \frac12\left(\cos(a - b) +
        \cos(a + b)\right)$ y la linealización
        $\cos^2 x = \frac{1 + \cos 2x}{2}$ (la integración
        El capítulo te lo agradecerá).
  \item Beats: derivar
        $\sin p + \sin q = 2 \sin\frac{p + q}{2}
        \cos\frac{p - q}{2}$ y aplíquelo a dos diapasones
        en $440$ y $444$ Hz: qué tono escucha el oído y
        cuantas pulsaciones por segundo? (Así es como las orquestas
        melodía.)
  \item Deducir la identidad del triple ángulo
        $\cos 3x = 4\cos^3 x - 3\cos x $
        (escribir $3x = 2x + x $). Configuración $ x = 20^\circ $, que
        cúbico \omterm{def:g10:algebra:equation}{ecuación} debe $\cos 20^\circ $ ¿satisfacer? (Que esto
        cúbico no se puede resolver sólo con raíces cuadradas es
        precisamente por qué la trisección del ángulo derrotó
        regla y compás durante dos milenios --- la historia completa
        se cuenta en los volúmenes de la universidad.)
  \item Una corriente alterna lee
        $ I(t) = 3\cos(100\pi t) + 3\sqrt3 \sin(100\pi t)$.
        Reescribirlo como $ R\cos(100\pi t - \varphi)$: pico
        corriente y fase?
\end{enumerate}

\medskip
\noindent\textbf{Part IV --- The oscillating world.}
\begin{enumerate}[resume]
  \item Períodos: ¿cuál es el período (más pequeño) de
        $\sin^2 t $ (use la pregunta 13)? De
        $\sin(2t) + \sin(3t)$?
  \item Un péndulo real amortigua:
        $ y(t) = \eu^{-t/10}\cos(2\pi t)$. Describe el movimiento,
        calcular el valor del sobre en $ t = 10$ (un viejo amigo
        aparece, \cref{pb:g12:exp:1}), y nombrar el capítulo
        donde ecuaciones con tales soluciones se resuelven.
  \item ¿Por qué \omterm{def:g12:trigo:cossin}{senos} y \omterm{def:g11:trigo:cossin}{coseños} ¿gobernar cada vibración? Dale el
        Diccionario de física a matemáticas en dos oraciones.
        (fuerza restauradora proporcional al desplazamiento
        $\rightarrow $ cual \omterm{def:g10:algebra:equation}{ecuación} $\rightarrow$ cual
        soluciones) y decir qué \cref{exo:g12:trigo:9}
        contribuye al reclamo.
  \item Final: el arco del capítulo en cuatro pasos --- uno
        límite geométrico, dos derivadas, un diferencial
        \omterm{def:g10:algebra:equation}{ecuación}, todo de oscilación. Y el asterisco honesto:
        Para grandes oscilaciones, el verdadero período del péndulo implica una
        Integral elíptica, calculada a la velocidad del rayo por\,\dots
        La aritmética de Gauss: geométrica. \omterm{def:g11:stat:mean}{media}, el postre de
        \cref{pb:g12:seq:1}. Los hilos de la serie se atan solos.
\end{enumerate}
\end{problem}
