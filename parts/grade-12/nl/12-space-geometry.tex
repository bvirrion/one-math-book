\chapter{Vectoren, lijnen en vlakken in de ruimte}\label{ch:g12:space}

Driedimensionale meetkunde wordt computationeel zodra vectoren en
coördinaten beschikbaar zijn: lijnen krijgen parametrische voorstellingen,
vlakken krijgen Cartesiaanse vergelijkingen, en het inwendig product meet
hoeken en afstanden. Dit hoofdstuk bouwt deze gereedschapskist en gebruikt
haar om de klassieke snij- en afstandsproblemen op te lossen.

\section{Vectoren in de ruimte}

Vectoren in de ruimte gehoorzamen dezelfde regels als in het vlak: ze
tellen op, ze schalen, en $\vect{AB} = \vect{CD}$ precies wanneer $ABDC$
een parallellogram is. In een coördinatenstelsel
$(O; \vec\imath, \vec\jmath, \vec k)$ heeft elke vector coördinaten
$(x, y, z)$.

\begin{definition}[Collineariteit, coplanariteit]\label{def:g12:space:collinear}
Twee vectoren zijn \emph{collineair} als de ene een veelvoud van de andere
is. Drie vectoren $\vec u, \vec v, \vec w$ zijn
\emph{coplanair}\index{coplanair} als een van hen als combinatie van de
andere twee te schrijven is, zeg $\vec w = a\vec u + b\vec v$.
\end{definition}

\begin{proposition}\label{prop:g12:space:basis}
Als $\vec u, \vec v, \vec w$ niet coplanair zijn, ontbindt elke vector van
de ruimte uniek als $x\vec u + y\vec v + z\vec w$.
\end{proposition}

\begin{proof}[Bewijs-idee]
Door de top van de te ontbinden vector, teken de lijn evenwijdig aan
$\vec w$; zij snijdt het vlak van $\vec u, \vec v$ in één punt, wat de
vector splitst in een component in dat vlak (uniek $x\vec u + y\vec v$,
vlakke meetkunde) en een component langs $\vec w$. Uniciteit: een verschil
van twee ontbindingen zou een van de vectoren in termen van de andere twee
uitdrukken, in strijd met non-coplanariteit.
\end{proof}

\section{Lijnen en vlakken}

\begin{definition}[Parametrische voorstelling van een lijn]\label{def:g12:space:line}
De lijn door $A(x_A, y_A, z_A)$ met richtingsvector
$\vec u (a, b, c) \neq \vec 0$ is de verzameling van punten
\[
M(x_A + ta,\; y_A + tb,\; z_A + tc), \qquad t \in \R .
\]
\end{definition}

\begin{definition}[Vlak]\label{def:g12:space:plane}
Het vlak door $A$ gericht door twee niet-collineaire vectoren
$\vec u, \vec v$ is de verzameling van punten $M$ met
$\vect{AM} = s\vec u + t\vec v$, $(s, t) \in \R^2$.
\end{definition}

\begin{omfigure}
\begin{tikzpicture}[scale=1.05]
  \draw (-0.5,-0.25) -- (3.4,1.7);
  \draw[-Stealth, omDef, thick] (1,0.5) -- (1.9,0.95)
    node[midway, above left, font=\small] {$\vec u$};
  \fill (0.1,0.05) circle (1.2pt) node[below right, font=\small] {$t=-1$};
  \fill (1,0.5) circle (1.2pt) node[above left, font=\small] {$A$};
  \fill (1.9,0.95) circle (1.2pt) node[below right, font=\small] {$t=1$};
  \fill (2.8,1.4) circle (1.2pt) node[below right, font=\small] {$t=2$};
\end{tikzpicture}
\qquad\quad
\begin{tikzpicture}[scale=1.05]
  \draw (0,0) -- (4,0.5) -- (5.2,1.9) -- (1.2,1.4) -- cycle;
  \node[font=\small] at (4.75,1.6) {$\mathcal P$};
  \coordinate (A) at (1.3,0.55);
  \coordinate (M) at (3.77,1.38);
  \draw[-Stealth, omDef, thick] (A) -- ++(1.31,0.16)
    node[midway, below, font=\small] {$\vec u$};
  \draw[-Stealth, omDef, thick] (A) -- ++(0.42,0.49)
    node[midway, left, font=\small] {$\vec v$};
  \draw[black, dashed, thin] (3.27,0.79) -- (M) -- (1.80,1.14);
  \fill (A) circle (1.2pt) node[below left, font=\small] {$A$};
  \fill (M) circle (1.2pt) node[above, font=\small] {$M$};
\end{tikzpicture}

{\small Links: de lijn door $A$ met richting $\vec u$, gegradueerd door de
parameter $t$. Rechts: het vlak door $A$ gericht door $\vec u$ en
$\vec v$; elk punt $M$ van het vlak wordt bereikt als
$\vect{AM} = s\vec u + t\vec v$.}
\end{omfigure}

\begin{proposition}[Relatieve posities]\label{prop:g12:space:positions}
Twee distincte vlakken zijn ofwel evenwijdig of snijden in een lijn. Een
lijn en een vlak zijn ofwel evenwijdig (eventueel bevat) of snijden in één
punt. Twee lijnen in de ruimte kunnen snijdend, strikt evenwijdig,
identiek, of \emph{kruisend} (niet coplanair) zijn.
\end{proposition}

\begin{proof}[Schets]
Dit zijn incidentie-uitspraken; elke gevalsanalyse reduceert, in
coördinaten, tot het oplossen van een lineair stelsel en het tellen van
zijn oplossingen --- zie \cref{met:g12:space:intersections}.
\end{proof}

\section{Inwendig product}

\begin{definition}[Inwendig product]\label{def:g12:space:dot}
Het \emph{inwendig product}\index{inwendig product}\index{scalair product}
van $\vec u$ en $\vec v$ is
\[
\vec u \cdot \vec v = \tfrac12\left(\norm{\vec u + \vec v}^2
- \norm{\vec u}^2 - \norm{\vec v}^2\right),
\]
waar $\norm{\vec u}$ de lengte van $\vec u$ is. Als beide vectoren
niet-nul zijn, $\vec u \cdot \vec v = \norm{\vec u}\,\norm{\vec v}\cos\theta$
waar $\theta$ de hoek ertussen is; en in een \emph{orthonormaal}
coördinatenstelsel,
\[
\vec u \cdot \vec v = xx' + yy' + zz' .
\]
Twee vectoren zijn \emph{loodrecht} (orthogonaal) als hun inwendig product
$0$ is.
\end{definition}

\begin{proposition}[Eigenschappen]\label{prop:g12:space:dotprops}
Het inwendig product is symmetrisch ($\vec u\cdot\vec v = \vec v\cdot\vec u$),
bilineair ($(a\vec u + b\vec v)\cdot \vec w = a\,\vec u\cdot\vec w +
b\,\vec v\cdot \vec w$), en
$\vec u \cdot \vec u = \norm{\vec u}^2 \geq 0$.
\end{proposition}

\begin{proof}
In een orthonormaal stelsel zijn alle drie de eigenschappen onmiddellijk op
de formule $xx' + yy' + zz'$, die zelf volgt uit de definiërende formule en
de Pythagoras-berekening van lengtes:
$\norm{\vec u}^2 = x^2 + y^2 + z^2$.
\end{proof}

\begin{definition}[Normaalvector]\label{def:g12:space:normal}
Een \emph{normaalvector}\index{normaalvector} van een vlak $\mathcal P$ is
een niet-nul vector loodrecht op elke vector die in $\mathcal P$ ligt (het
volstaat dat hij loodrecht is op twee niet-collineaire richtingen van
$\mathcal P$).
\end{definition}

\begin{omfigure}
\begin{tikzpicture}[scale=1.1]
  \draw (0,0) -- (4,0.5) -- (5.2,1.9) -- (1.2,1.4) -- cycle;
  \node[font=\small] at (4.75,1.6) {$\mathcal P$};
  \coordinate (H) at (2.6,0.95);
  \coordinate (M0) at (2.86,3.03);
  \draw[-Stealth, omThm, thick] (H) -- (2.73,1.99)
    node[midway, right=1pt, font=\small] {$\vec n$};
  \draw[black, dashed, thin] (2.73,1.99) -- (M0);
  \draw[thin] (2.79,0.974) -- (2.815,1.174) -- (2.625,1.15);
  \draw[black, dashed, thin] (H) -- (1.1,0.55);
  \fill (H) circle (1.2pt) node[below right, font=\small] {$H$};
  \fill (M0) circle (1.2pt) node[above, font=\small] {$M_0$};
  \fill (1.1,0.55) circle (1.2pt) node[below left, font=\small] {$A$};
\end{tikzpicture}

{\small Een normaalvector $\vec n$ van het vlak $\mathcal P$ is loodrecht
op elke richting van $\mathcal P$. Het punt $H$, voet van de loodlijn uit
$M_0$, realiseert de afstand van $M_0$ tot het vlak.}
\end{omfigure}

\begin{theorem}[Cartesiaanse vergelijking van een vlak]\label{thm:g12:space:planeeq}
In een orthonormaal coördinatenstelsel heeft het vlak door $A$ met
normaalvector $\vec n(a, b, c)$ de vergelijking
\[
a x + b y + c z + d = 0,
\]
waar $d = -(ax_A + by_A + cz_A)$. Omgekeerd definieert elke zulke
vergelijking met $(a,b,c) \neq (0,0,0)$ een vlak met normaalvector
$(a, b, c)$.
\end{theorem}

\begin{proof}
$M \in \mathcal P \iff \vect{AM} \perp \vec n \iff
a(x - x_A) + b(y - y_A) + c(z - z_A) = 0$, wat tot de vergelijking
ontwikkelt. Omgekeerd, kies enig oplossingspunt $A$ van de vergelijking;
dezelfde berekening achterwaarts toont dat de oplossingsverzameling
$\{M : \vect{AM}\cdot\vec n = 0\}$ is, het vlak door $A$ loodrecht op
$\vec n$.
\end{proof}

\begin{method}[Snijdingen in de praktijk]\label{met:g12:space:intersections}
\begin{itemize}
  \item \emph{Lijn $\cap$ vlak}: substitueer de parametrische coördinaten
        van de lijn in de vlakvergelijking; los op naar $t$ (één oplossing:
        één punt; $0 = $ niet-nul: evenwijdig; $0 = 0$: lijn bevat).
  \item \emph{Vlak $\cap$ vlak}: los het stelsel van de twee vergelijkingen
        op, parametriseer met één vrije coördinaat; de oplossing is een
        lijn (of de vlakken zijn evenwijdig wanneer de normaalvectoren
        collineair zijn).
  \item \emph{Orthogonaliteitscontroles}: lijn $\perp$ vlak iff haar
        richting collineair is met de normaal; twee vlakken zijn loodrecht
        iff hun normalen loodrecht zijn.
\end{itemize}
\end{method}

\begin{proposition}[Afstand van een punt tot een vlak]\label{prop:g12:space:distance}
\index{afstand!punt tot vlak}
In een orthonormaal stelsel is de afstand van $M_0(x_0, y_0, z_0)$ tot het
vlak $\mathcal P : ax + by + cz + d = 0$
\[
\operatorname{dist}(M_0, \mathcal P)
= \frac{\abs{ax_0 + by_0 + cz_0 + d}}{\sqrt{a^2 + b^2 + c^2}}.
\]
\end{proposition}

\begin{proof}
Laat $H$ de orthogonale projectie van $M_0$ op $\mathcal P$ zijn: de voet
van de loodlijn, dus $\vect{HM_0}$ is collineair met de eenheidsnormaal
$\frac{\vec n}{\norm{\vec n}}$, en de afstand is
$\abs{\vect{HM_0}\cdot \frac{\vec n}{\norm{\vec n}}}$. Voor elke
$H \in \mathcal P$,
$\vect{HM_0}\cdot\vec n = ax_0 + by_0 + cz_0 - (ax_H + by_H + cz_H)
= ax_0 + by_0 + cz_0 + d$ (met de vergelijking in $H$), waaruit de formule
na deling door $\norm{\vec n} = \sqrt{a^2+b^2+c^2}$.
\end{proof}

\begin{example}\label{ex:g12:space:distance}
Afstand van de oorsprong tot het vlak $x + 2y + 2z - 6 = 0$:
$\frac{\abs{-6}}{\sqrt{1 + 4 + 4}} = \frac63 = 2$.
\end{example}

\section{Oefeningen}

In alle oefeningen is het coördinatenstelsel orthonormaal.

\begin{exercise}[$\star$]\label{exo:g12:space:1}
Laat $A(1, 0, 2)$, $B(3, 1, 1)$ en $C(2, -1, 3)$. Zijn de punten $A$, $B$,
$C$ collineair? Geef een parametrische voorstelling van de lijn $(AB)$.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g12:space:2}
Bepaal een Cartesiaanse vergelijking van het vlak door $A(1, 1, 0)$ met
normaalvector $\vec n(2, -1, 3)$. Behoort het punt $B(0, 2, 1)$ ertoe?
\end{exercise}

\begin{exercise}[$\star$]\label{exo:g12:space:3}
Bereken de hoek in $A$ (tot op de graad) van de driehoek met hoekpunten
$A(0,0,0)$, $B(1, 1, 0)$ en $C(1, 0, 1)$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:space:4}
Beschouw de lijn $\mathcal D$ door $A(1, 2, 0)$ met richting
$\vec u(1, -1, 2)$, en het vlak $\mathcal P : 2x + y - z + 1 = 0$.
Bepaal $\mathcal D \cap \mathcal P$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:space:5}
Laat $\mathcal P : x - y + 2z = 1$ en $\mathcal Q : 2x + y + z = 4$.
Toon dat $\mathcal P$ en $\mathcal Q$ snijden in een lijn en geef een
parametrische voorstelling ervan.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:space:6}
Toon dat de lijnen
\[
\mathcal D_1 : (x, y, z) = (1 + t,\; 2t,\; 3 - t), \qquad
\mathcal D_2 : (x, y, z) = (2 + s,\; 1 + s,\; 1 + 2s)
\]
kruisend zijn (niet coplanair).
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:g12:space:7}
Laat $\mathcal P : 2x - y + 2z - 3 = 0$.
\begin{enumerate}
  \item Bereken de afstand van $M_0(3, 1, 1)$ tot $\mathcal P$.
  \item Bepaal de coördinaten van de orthogonale projectie $H$ van $M_0$
        op $\mathcal P$, en controleer de afstand $M_0H$.
\end{enumerate}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:g12:space:8}
De kubus $ABCDEFGH$ heeft zijde $1$; plaats coördinaten zodat
$A(0,0,0)$, $B(1,0,0)$, $D(0,1,0)$, $E(0,0,1)$ (met
$C = B + D$, $F = B + E$, $G = B + D + E$, $H = D + E$ als vectoren uit
$A$).
\begin{enumerate}
  \item Toon dat de diagonaal $(AG)$ loodrecht is op het vlak $(BDE)$.
  \item Bereken de afstand van $A$ tot het vlak $(BDE)$, en het punt waar
        $(AG)$ het doorboort.
\end{enumerate}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:g12:space:9}
\emph{(Volume van een tetraëder.)} Laat $A(1,1,1)$, $B(2,1,0)$, $C(0,2,1)$
en $D(2,2,2)$.
\begin{enumerate}
  \item Verifieer dat $\vect{AB}$, $\vect{AC}$, $\vect{AD}$ niet coplanair
        zijn (de punten vormen een echte tetraëder).
  \item Vind een vector $\vec n$ loodrecht op zowel $\vect{BC}$ als
        $\vect{BD}$, en leid een Cartesiaanse vergelijking van het vlak
        $(BCD)$ af.
  \item Bereken de afstand van $A$ tot het vlak $(BCD)$ en de oppervlakte
        van de driehoek $BCD$, met de formule
        $\text{Opp.} = \frac12\sqrt{\norm{\vec u}^2\norm{\vec v}^2 -
        (\vec u \cdot \vec v)^2}$ voor de driehoek opgespannen door
        $\vec u, \vec v$.
  \item Leid het volume van de tetraëder af
        ($V = \frac13 \times \text{grondvlak} \times \text{hoogte}$).
\end{enumerate}
\end{exercise}
