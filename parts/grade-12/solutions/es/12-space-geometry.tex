\section*{Capítulo \ref{ch:g12:space} --- Vectores, rectas y planos en el espacio}

\begin{solution}{exo:g12:space:1}
$\vect{AB}(2, 1, -1)$ y $\vect{AC}(1, -1, 1)$ no son proporcionales
($\frac21 \neq \frac{1}{-1}$), así que los puntos no están alineados. Recta
$(AB)$:
\[
(x, y, z) = (1 + 2t,\; t,\; 2 - t), \qquad t \in \R .
\]
\end{solution}

\begin{solution}{exo:g12:space:2}
$2(x - 1) - (y - 1) + 3(z - 0) = 0$, es decir,
\[
2x - y + 3z - 1 = 0 .
\]
Para $B(0,2,1)$: $0 - 2 + 3 - 1 = 0$: sí, $B$ está en el plano.
\end{solution}

\begin{solution}{exo:g12:space:3}
$\vect{AB}(1,1,0)$ y $\vect{AC}(1,0,1)$:
\[
\cos\widehat{A}
= \frac{\vect{AB}\cdot\vect{AC}}{\norm{\vect{AB}}\,\norm{\vect{AC}}}
= \frac{1}{\sqrt2\,\sqrt2} = \frac12,
\]
luego $\widehat A = 60^\circ$.
\end{solution}

\begin{solution}{exo:g12:space:4}
Puntos de $\mathcal D$: $(1 + t,\, 2 - t,\, 2t)$. Sustituyendo en la
\omterm{def:g10:algebra:equation}{ecuación} de $\mathcal P$:
\[
2(1+t) + (2 - t) - 2t + 1 = 5 - t = 0 \iff t = 5 .
\]
Punto de \omterm{def:g10:numbers:interunion}{intersección} único: $(6, -3, 10)$.
\end{solution}

\begin{solution}{exo:g12:space:5}
Los \omterm{def:g12:space:normal}{vectores normales} $(1, -1, 2)$ y $(2, 1, 1)$ no son \omterm{def:g12:space:collinear}{colineales}, así que
los planos se cortan en una recta. Sumando las dos ecuaciones:
$3x + 3z = 5$, luego $x = \frac53 - z$; y entonces la primera \omterm{def:g10:algebra:equation}{ecuación} da
$y = x + 2z - 1 = \frac23 + z$. Con $z = t$:
\[
(x, y, z) = \left(\frac53 - t,\; \frac23 + t,\; t\right), \qquad t \in \R,
\]
una recta de \omterm{def:g11:vect:direction}{vector director} $(-1, 1, 1)$. (Las dos ecuaciones se cumplen
idénticamente.)
\end{solution}

\begin{solution}{exo:g12:space:6}
Las direcciones $\vec u_1(1, 2, -1)$ y $\vec u_2(1, 1, 2)$ no son
\omterm{def:g12:space:collinear}{colineales}, así que las rectas no son paralelas. Que se cortaran exigiría
\[
1 + t = 2 + s, \qquad 2t = 1 + s, \qquad 3 - t = 1 + 2s .
\]
Las dos primeras dan $t = 1 + s$ y $2(1+s) = 1 + s$, luego $s = -1$ y
$t = 0$; pero entonces la tercera dice $3 = -1$, absurdo. Sin punto común y
sin ser paralelas: las rectas se cruzan.
\end{solution}

\begin{solution}{exo:g12:space:7}
\emph{1.} $\operatorname{dist} = \dfrac{\abs{2\cdot3 - 1 + 2\cdot1 - 3}}
{\sqrt{4 + 1 + 4}} = \dfrac{4}{3}$.

\emph{2.} $H = M_0 + t\,\vec n$ con $\vec n(2, -1, 2)$, eligiendo $t$ de
modo que $H \in \mathcal P$:
\[
2(3 + 2t) - (1 - t) + 2(1 + 2t) - 3 = 4 + 9t = 0 \iff t = -\frac49 .
\]
Por tanto, $H\left(\frac{19}{9}, \frac{13}{9}, \frac19\right)$, y
\[
M_0H = \norm{t\,\vec n} = \frac49 \times 3 = \frac43,
\]
que coincide con la fórmula de la distancia.
\end{solution}

\begin{solution}{exo:g12:space:8}
\omterm{def:g10:coordgeom:system}{Coordenadas}: $G(1,1,1)$, y el plano $(BDE)$ pasa por $B(1,0,0)$,
$D(0,1,0)$ y $E(0,0,1)$.

\emph{1.} El plano $(BDE)$ tiene \omterm{def:g10:algebra:equation}{ecuación} $x + y + z = 1$ (la cumplen los
tres puntos, que además no están alineados), así que $\vec n(1,1,1)$ es
normal a él. Como $\vect{AG} = (1,1,1) = \vec n$, la diagonal $(AG)$ es
ortogonal al plano $(BDE)$.

\emph{2.} Distancia desde $A(0,0,0)$:
$\frac{\abs{0 + 0 + 0 - 1}}{\sqrt3} = \frac{1}{\sqrt3} = \frac{\sqrt3}{3}$.
La recta $(AG)$ es $(t, t, t)$; corta al plano cuando $3t = 1$: en el punto
$\left(\frac13, \frac13, \frac13\right)$, el baricentro del triángulo
$BDE$.
\end{solution}

\begin{solution}{exo:g12:space:9}
\emph{1.} $\vect{AB}(1, 0, -1)$, $\vect{AC}(-1, 1, 0)$ y
$\vect{AD}(1, 1, 1)$. Si $\vect{AD} = a\vect{AB} + b\vect{AC}$, las
\omterm{def:g10:coordgeom:system}{coordenadas} dan $a - b = 1$, $b = 1$ y $-a = 1$: las dos primeras obligan a
$a = 2$, en contradicción con $a = -1$. No son \omterm{def:g12:space:collinear}{coplanarios}.

\emph{2.} $\vect{BC}(-2, 1, 1)$ y $\vect{BD}(0, 1, 2)$. Resolviendo
$\vec n \cdot \vect{BC} = \vec n \cdot \vect{BD} = 0$: de $b + 2c = 0$ sale
$b = -2c$; y entonces $-2a - 2c + c = 0$ da $c = -2a$. Con $a = 1$:
$\vec n(1, 4, -2)$. Plano por $B(2,1,0)$:
\[
x + 4y - 2z - 6 = 0 .
\]

\emph{3.} Distancia desde $A(1,1,1)$:
$\dfrac{\abs{1 + 4 - 2 - 6}}{\sqrt{21}} = \dfrac{3}{\sqrt{21}}$.
Área de $BCD$: $\norm{\vect{BC}}^2 = 6$, $\norm{\vect{BD}}^2 = 5$ y
$\vect{BC}\cdot\vect{BD} = 3$, luego
\[
\text{Área} = \frac12\sqrt{6 \times 5 - 9} = \frac{\sqrt{21}}{2}.
\]

\emph{4.}
\[
V = \frac13 \times \frac{\sqrt{21}}{2} \times \frac{3}{\sqrt{21}}
= \frac12 .
\]
\end{solution}

\begin{solution}{pb:g12:space:1}
\textbf{1.} Plano: $x + y + z = 1$. Recta: $(t, t, t)$. \omterm{def:g10:numbers:interunion}{Intersección}:
$3t = 1$: $\left(\frac13, \frac13, \frac13\right)$.

\textbf{2.} $\dfrac{\abs{0 + 0 + 0 - 1}}{\sqrt3} =
\dfrac{1}{\sqrt3} = \dfrac{\sqrt3}{3}$.

\textbf{3.} $1 - 1 + 0 = 0$: sí, son \omterm{def:g11:scal:orthogonal}{ortogonales}.

\textbf{4.} Mismo \omterm{def:g12:space:normal}{vector normal} $(1,1,1)$, pero $\frac52 \neq 1$: planos
estrictamente paralelos. La recta: $(1,1,0) \cdot (1,1,1) = 2 \neq 0$: no
es paralela al plano, así que lo atraviesa en un punto.

\textbf{5.} \omterm{prop:g10:coordgeom:midpoint}{Puntos medios}: $\left(1, \frac12, 0\right)$,
$\left(\frac12, 1, 0\right)$, $\left(0, 1, \frac12\right)$,
$\left(0, \frac12, 1\right)$, $\left(\frac12, 0, 1\right)$ y
$\left(1, 0, \frac12\right)$: en todos, la suma de \omterm{def:g10:coordgeom:system}{coordenadas} vale
$\frac32$.

\textbf{6.} El \omterm{def:g12:space:normal}{vector normal} de $P$ es $(1,1,1)$, la dirección de $(AG)$:
perpendicular. El centro $\left(\frac12,\frac12,\frac12\right)$ tiene suma
de \omterm{def:g10:coordgeom:system}{coordenadas} $\frac32$: está en $P$.

\textbf{7.} Los \omterm{prop:g10:coordgeom:midpoint}{puntos medios} consecutivos se diferencian en \omterm{def:g11:vect:vector}{vectores} como
$\left(-\frac12, \frac12, 0\right)$: longitud $\frac{\sqrt2}{2}$ cada vez;
seis lados iguales.

\textbf{8.} En $\left(\frac12, 1, 0\right)$: \omterm{def:g11:vect:vector}{vectores} de arista
$\left(\frac12, -\frac12, 0\right)$ y
$\left(-\frac12, 0, \frac12\right)$: \omterm{def:g12:space:dot}{producto escalar} $-\frac14$ y \omterm{def:g11:scal:dot}{normas}
$\frac{\sqrt2}{2}$: $\cos = -\frac12$: ángulo de $120^\circ$. Lados iguales
y ángulos iguales de $120^\circ$: un hexágono regular.

\textbf{9.} Perímetro $6 \times \frac{\sqrt2}{2} = 3\sqrt2 \approx 4.24$.
Área $6 \times \frac{\sqrt3}{4} \times \frac12 = \frac{3\sqrt3}{4}
\approx 1.30$: mayor que una cara (de área $1$); una rodaja más grande que
cualquier lado de la caja, y solo el rectángulo diagonal
($\sqrt2 \approx 1.41$) la supera.

\textbf{10.} $(t,t,t)$ corta a $P$ en $t = \frac12$: el centro, que es el
\omterm{prop:g10:coordgeom:midpoint}{punto medio} de $[AG]$ ($A$ en $t = 0$ y $G$ en $t = 1$).

\textbf{11.} Para $c = \frac12$: el plano corta las tres aristas de la
esquina de $A$ en $\left(\frac12,0,0\right)$,
$\left(0,\frac12,0\right)$ y $\left(0,0,\frac12\right)$: un triángulo
equilátero de lado $\frac{\sqrt2}{2}$. A medida que $c$ crece, el triángulo
crece, sus esquinas se van truncando hasta formar un hexágono (regular
exactamente en $c = \frac32$) y después la figura se encoge simétricamente
hasta volver a ser un triángulo en la esquina de $G$: la metamorfosis del
corte de un cristal.

\textbf{12.} Cada lado de la sección es la \omterm{def:g10:numbers:interunion}{intersección} del plano de corte
con una \emph{cara} del cubo, y un plano corta a una cara (un polígono
plano) a lo sumo en un segmento: como mucho, $6$ lados. Siete es
imposible; de tres a seis se dan todos (las preguntas 8 y 11 muestran dos
de esos casos).

\textbf{13.} Base $ABD$: triángulo rectángulo de área $\frac12$; altura
$AE = 1$: volumen $\frac13 \times \frac12 \times 1 = \frac16$.

\textbf{14.} Cada pareja de entre $B, D, E, G$ se \omterm{def:g11:seq:arithmetic}{diferencia} exactamente en
dos \omterm{def:g10:coordgeom:system}{coordenadas}, y en $1$ cada una: las seis distancias valen $\sqrt2$;
regular. El cubo se descompone en $BDEG$ más cuatro tetraedros de esquina
congruentes con $ABDE$: volumen $1 - 4 \times \frac16 = \frac13$.

\textbf{15.} Plano $(BDE)$: $x + y + z = 1$; centro
$\left(\frac12,\frac12,\frac12\right)$: distancia
$\frac{\abs{\frac32 - 1}}{\sqrt3} = \frac{1}{2\sqrt3} = \frac{\sqrt3}{6}$.

\textbf{16.} $\vect{AG} = (1,1,1)$ y $\vect{BH} = (-1,1,1)$:
$\cos\theta = \frac{-1 + 1 + 1}{3} = \frac13$: $\theta \approx 70.5^\circ$,
con suplementario $\approx 109.5^\circ$. Cuatro pares de electrones se
repelen entre sí y se separan todo lo posible alrededor del carbono: el
óptimo son los vértices del tetraedro regular, cuyas direcciones
centro-vértice se cortan exactamente con ese ángulo; el diamante es la
pregunta 14 cristalizada.

\textbf{17.} $(1, t, t)$ en $P$: $1 + 2t = \frac32$: $t = \frac14$: el
punto $\left(1, \frac14, \frac14\right)$.

\textbf{18.} $(AB)$: puntos $(t, 0, 0)$; $(EG)$: puntos $(s, s, 1)$. Que se
cortaran exigiría $0 = 1$ en la tercera coordenada: nunca. Que fueran
paralelas exigiría que $(1,0,0)$ y $(1,1,0)$ fueran \omterm{def:g12:space:collinear}{colineales}: tampoco. Ni
se cortan ni son paralelas: se cruzan; dos pasillos en plantas distintas.

\textbf{19.} $B + t(1,1,1) = (1 + t, t, t)$ en $P$: $1 + 3t = \frac32$:
$t = \frac16$: proyección $\left(\frac76, \frac16, \frac16\right)$, cuya
suma de \omterm{def:g10:coordgeom:system}{coordenadas} es $\frac32$: está en $P$, como debía ser.

\textbf{20.} Las rectas paramétricas atraviesan (preguntas 1, 10 y 17); los
\omterm{def:g12:space:normal}{vectores normales} miden ángulos y distancias (preguntas 2, 8, 15 y 16); las
ecuaciones de planos tallan secciones (la metamorfosis del triángulo al
hexágono). Y el terreno de juego pagó con creces la visita: un hexágono
regular en una caja de cuadrados, un tetraedro regular en las esquinas, el
ángulo del diamante y dos rectas que se ignoran; geometría que solo el
espacio puede ofrecer.
\end{solution}
