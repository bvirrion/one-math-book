\section*{Capítulo \ref{ch:g12:matrix} --- Matrices y grafos}

\begin{solution}{exo:g12:matrix:1}
\[
A + B = \begin{pmatrix} 3 & 2\\ 1 & 2\end{pmatrix}, \quad
AB = \begin{pmatrix} 4 & 2\\ 1 & 1\end{pmatrix}, \quad
BA = \begin{pmatrix} 2 & 4\\ 1 & 3\end{pmatrix}, \quad
A^2 = \begin{pmatrix} 1 & 4\\ 0 & 1\end{pmatrix}.
\]
Nota $AB \neq BA $.
\end{solution}

\begin{solution}{exo:g12:matrix:2}
$\det A = 6 - 5 = 1 \neq 0$:
$ A^{-1} = \begin{pmatrix} 3 & -5\\ -1 & 2 \end{pmatrix}$.
$\det B = 12 - 12 = 0$: $ B $ no es \omterm{def:g12:matrix:inverse}{reversible}.
\end{solution}

\begin{solution}{exo:g12:matrix:3}
El sistema es $ AX = Y $ con $ A $ como en \cref{exo:g12:matrix:2} y
$ Y = \begin{pmatrix} 1\\ 2\end{pmatrix}$:
\[
X = A^{-1}Y = \begin{pmatrix} 3 & -5\\ -1 & 2\end{pmatrix}
\begin{pmatrix} 1\\ 2\end{pmatrix}
= \begin{pmatrix} -7\\ 3\end{pmatrix}:
\qquad x = -7,\ y = 3 .
\]
\end{solution}

\begin{solution}{exo:g12:matrix:4}
\emph{1.} $ N^2 = \begin{pmatrix} 0&1\\0&0\end{pmatrix}
\begin{pmatrix} 0&1\\0&0\end{pmatrix} = 0$, y $ I $ viaja con cada
matriz.

\emph{2.} Dado que los dos términos se conmutan, se aplica el teorema del binomio y todos
términos que contienen $ N^2$ desaparecer:
\[
A^k = (2I + N)^k = 2^k I + k\,2^{k-1} N
= \begin{pmatrix} 2^k & k\,2^{k-1}\\ 0 & 2^k \end{pmatrix}.
\]
comprobar si $ k = 2$:
$ A^2 = \begin{pmatrix} 2&1\\0&2\end{pmatrix}^2
= \begin{pmatrix} 4&4\\0&4\end{pmatrix}$, y la fórmula da
$2^2 = 4$, $2 \times 2 = 4$. \checkmark
\end{solution}

\begin{solution}{exo:g12:matrix:5}
\emph{Inducción.} Para $ n = 1$:
$ A^1 = \begin{pmatrix} 0&1\\1&1\end{pmatrix}
= \begin{pmatrix} F_0 & F_1\\ F_1 & F_2\end{pmatrix}$. Supongamos la fórmula
para $ n $; entonces
\[
A^{n+1} = A^n A
= \begin{pmatrix} F_{n-1} & F_n\\ F_n & F_{n+1}\end{pmatrix}
\begin{pmatrix} 0 & 1\\ 1 & 1\end{pmatrix}
= \begin{pmatrix} F_n & F_{n-1} + F_n\\ F_{n+1} & F_n + F_{n+1}\end{pmatrix}
= \begin{pmatrix} F_n & F_{n+1}\\ F_{n+1} & F_{n+2}\end{pmatrix}.
\]
\emph{Identidad.} Para $2\times2$ matrices, en expansión \omterm{def:g10:proba:sample}{muestra}
$\det(MN) = \det M \det N $; por eso
$\det(A^n) = (\det A)^n = (-1)^n $, y
$\det A^n = F_{n-1}F_{n+1} - F_n^2$. (Esto es \emph{La identidad de Cassini}.)
\end{solution}

\begin{solution}{exo:g12:matrix:6}
\emph{1.} Ordenar vértices $1, 2, 3$:
\[
M = \begin{pmatrix} 0&1&1\\ 0&0&1\\ 1&0&0\end{pmatrix}, \quad
M^2 = \begin{pmatrix} 1&0&1\\ 1&0&0\\ 0&1&1\end{pmatrix}, \quad
M^3 = \begin{pmatrix} 1&1&1\\ 1&0&1\\ 1&0&1 \end{pmatrix}.
\]

\emph{2.} $\bigl(M^3\bigr)_{11} = 1$: exactamente un recorrido cerrado de longitud $3$
en el vértice $1$, es decir $1 \to 2 \to 3 \to 1$. (El paseo
$1 \to 3 \to 1$ tiene longitud $2$ sólo, y $1 \to 3$ entonces $3\to1$ entonces
$1\to3$ termina en $3$.)
\end{solution}

\begin{solution}{exo:g12:matrix:7}
\emph{1.} $ a_{n+1} = 0.8a_n + 0.3b_n $, $ b_{n+1} = 0.2a_n + 0.7b_n $:
$ M = \begin{pmatrix} 0.8 & 0.3\\ 0.2 & 0.7\end{pmatrix}$.

\emph{2.} $ MX = X $ da $0.8a + 0.3b = a $, \emph{es decir.} $0.3b = 0.2a $, entonces
$ b = \frac23 a $; con $ a + b = 1$: $ a = 0.6$, $ b = 0.4$.

\emph{3.} Usando $ b_n = 1 - a_n $:
$ a_{n+1} = 0.8a_n + 0.3(1 - a_n) = 0.5a_n + 0.3$, entonces
\[
c_{n+1} = a_{n+1} - 0.6 = 0.5a_n + 0.3 - 0.6 = 0.5(a_n - 0.6) = 0.5\,c_n .
\]
Por eso $ c_n = 0.5^n c_0 \to 0$: $ a_n \to 0.6$ y $ b_n \to 0.4$, cualquiera que sea el
inicial \omterm{def:g11:prob:rv}{distribución}.
\end{solution}

\begin{solution}{exo:g12:matrix:8}
\emph{1.} $\det P = -2$, entonces
$ P^{-1} = -\frac12\begin{pmatrix} -1 & -1\\ -1 & 1\end{pmatrix}
= \frac12\begin{pmatrix} 1 & 1\\ 1 & -1\end{pmatrix}$. Entonces
\[
AP = \begin{pmatrix} 4 & 2\\ 4 & -2 \end{pmatrix}, \qquad
D = P^{-1}AP = \frac12\begin{pmatrix} 1&1\\1&-1\end{pmatrix}
\begin{pmatrix} 4&2\\4&-2\end{pmatrix}
= \begin{pmatrix} 4 & 0\\ 0 & 2\end{pmatrix}.
\]

\emph{2.} De $ A = PDP^{-1}$, una inducción inmediata da
$ A^n = PD^nP^{-1}$ con
$ D^n = \begin{pmatrix} 4^n & 0\\ 0 & 2^n\end{pmatrix}$, entonces
\[
A^n = P D^n P^{-1}
= \begin{pmatrix} 4^n & 2^n\\ 4^n & -2^n\end{pmatrix}\cdot
\frac12\begin{pmatrix} 1&1\\1&-1\end{pmatrix}
= \frac12\begin{pmatrix} 4^n + 2^n & 4^n - 2^n\\
4^n - 2^n & 4^n + 2^n\end{pmatrix}.
\]
Aplicando $ A^n $ a $ X_0 = \begin{pmatrix} u_0\\v_0\end{pmatrix}$ reproduce
exactamente las fórmulas de \cref{ex:g12:matrix:coupled}.
\end{solution}

\begin{solution}{pb:g12:matrix:1}
\textbf{1.} $ AB = \begin{pmatrix} 2 & 1\\ 4 & 3\end{pmatrix}$
y $ BA = \begin{pmatrix} 3 & 4\\ 1 & 2\end{pmatrix}$: matriz
la multiplicación no es conmutativa --- $ B $ intercambia columnas en el
derecha, filas a la izquierda.

\textbf{2.} \omterm{def:g11:vect:det}{Determinante} $6 - 5 = 1$: inverso
$\begin{pmatrix} 3 & -1\\ -5 & 2\end{pmatrix}$. Aplicándolo a
$\binom{4}{7}$: $ x = 12 - 7 = 5$, $ y = -20 + 14 = -6$.

\textbf{3.} $ N^2 = 0$. Entonces $(I + N)^n = I + nN $ por inducción:
$(I + nN)(I + N) = I + (n+1)N + nN^2 = I + (n+1)N $.

\textbf{4.} $ A = \begin{pmatrix} 0&1&1\\ 1&0&1\\ 1&1&0
\end{pmatrix}$, y $ A^3$ tiene entradas diagonales $2$: de cada uno
vértice, exactamente dos caminos cerrados de longitud $3$ (el triangulo
caminó en el sentido de las agujas del reloj o en el sentido contrario a las agujas del reloj) --- el teorema de conteo
en acción.

\textbf{5.} $ D^n = \begin{pmatrix} 2^n & 0\\ 0 & 2^{-n}
\end{pmatrix}$: una dirección explota, la otra muere ---
Los destinos diagonales son independientes. sucesiones geométricas.

\textbf{6.} $ F^2 = \begin{pmatrix} 2 & 1\\ 1 & 1\end{pmatrix}$,
$ F^3 = \begin{pmatrix} 3 & 2\\ 2 & 1\end{pmatrix}$,
$ F^4 = \begin{pmatrix} 5 & 3\\ 3 & 2\end{pmatrix}$: Fibonacci
en todas partes; conjetura como se dijo.

\textbf{7.} Si $ F^n = \begin{pmatrix} F_{n+1} & F_n\\ F_n &
F_{n-1}\end{pmatrix}$, entonces
\[
F^{n+1} = F^n F =
\begin{pmatrix} F_{n+1} + F_n & F_{n+1}\\
F_n + F_{n-1} & F_n \end{pmatrix}
= \begin{pmatrix} F_{n+2} & F_{n+1}\\ F_{n+1} & F_n
\end{pmatrix} :
\]
herencia; el caso base $ n = 1$ es $ F $ mismo, utilizando el
convención $ F_0 = 0$ (que extiende la recurrencia hacia atrás).

\textbf{8.} $\det F = -1$, entonces
$\det(F^n) = (\det F)^n = (-1)^n $; y directamente
$\det(F^n) = F_{n+1}F_{n-1} - F_n^2$: Cassini, una línea. (El
regla del producto para $2 \times 2$ \omterm{def:g11:vect:det}{determinantes} es un agradable
expansión de cinco minutos.)

\textbf{9.} Arriba a la derecha de $ F^m F^n $:
$ F_{m+1}F_n + F_m F_{n-1}$; arriba a la derecha de $ F^{m+n}$:
$ F_{m+n}$. Para $ m = n = 3$:
$ F_4 F_3 + F_3 F_2 = 3 \times 2 + 2 \times 1 = 8 = F_6$.

\textbf{10.} Para $ k = 1$: trivial. Si $ F_n \mid F_{kn}$, el
fórmula de suma con $ m = kn $:
$ F_{(k+1)n} = F_{kn+1}F_n + F_{kn}F_{n-1}$: ambos términos son
múltiplos de $ F_n $. Entonces $ F_n \mid F_{kn}$ para todo $ k $: controlar
$ F_3 = 2$ dividir $ F_6 = 8$ y $ F_9 = 34$.

\textbf{11.} $ F^{100} = F^{64} F^{32} F^4$: siete cuadraturas
($ F^2, F^4, \dots, F^{64}$) más dos combinaciones --- nueve
multiplicaciones en lugar de noventa y nueve. es el
truco de la mesa de doblar de los escribas egipcios, tomado de
números a matrices: escribir $100$ en binario, multiplicar el
duplicaciones que necesitas.

\textbf{12.} $0.8 + 0.2 = 1$ y $0.4 + 0.6 = 1$: mañana
debe ser \emph{alguno} clima --- cada columna es una información completa
\omterm{def:g10:proba:distribution}{probabilidad} \omterm{def:g11:prob:rv}{distribución}, por lo que las \omterm{def:g10:proba:distribution}{probabilidades} se conservan.

\textbf{13.} Mañana: $\binom{0.8}{0.2}$. Día siguiente:
$ M\binom{0.8}{0.2} = \binom{0.72}{0.28}$.

\textbf{14.} $ Mv = v $ con $ v = \binom{s}{r}$, $ s + r = 1$:
$0.8s + 0.4r = s $ da $0.4r = 0.2s $, $ s = 2r $:
$ v = \binom{2/3}{1/3}$. A la larga, dos días de cada tres son
soleado --- como sea que se vea hoy.

\textbf{15.} De $\binom01$: componentes soleados $0.4$, $0.56$,
$0.624$, $0.6496$; brechas para $\frac23$: $0.267$, $0.107$,
$0.043$, $0.017$ --- cada paso multiplica la brecha exactamente por
$0.4$ (el segundo valor propio de la máquina): geométrico
convergencia al estado estacionario.

\textbf{16.} Columnas (de $ A $, $ B $, $ C $):
$ P = \begin{pmatrix} 0 & 0 & 1\\ \frac12 & 0 & 0\\
\frac12 & 1 & 0\end{pmatrix}$. Estado estable: $ v_A = v_C $,
$ v_B = \frac{v_A}{2}$, $ v_C = \frac{v_A}{2} + v_B $; sumando a
$1$: $ v = \left(\frac25, \frac15, \frac25\right)$. Categoría:
$ A $ y $ C $ empatar por primera vez, $ B $ último.

\textbf{17.} $ C $ recibe \emph{todo} de $ B $el trafico y la mitad
de $ A $'s, y canaliza todo de regreso a $ A $: el estable
medidas estatales donde el surfista \emph{pasa tiempo}, no como
muchos enlaces apuntan hacia --- un enlace de una página popular pesa más
varios de los desiertos. Esa ponderación recursiva es
precisamente la idea fundacional de Google; manijas amortiguadoras trampas para arañas
y callejones sin salida.

\textbf{18.} $ A^n = P D^n P^{-1}$ da
$ u_n = \frac{4^n + 2^n}{2}$ (y
$ v_n = \frac{4^n - 2^n}{2}$). Controlar: $ u_1 = 3$, $ u_2 = 10$,
$ u_3 = 36$; directamente: $(1,0) \to (3,1) \to (10,6) \to
(36, 28)$: coincidente.

\textbf{19.} La diagonalización cambia a \omterm{def:g10:coordgeom:system}{coordenadas} en el cual
el sistema acoplado se desmorona en independiente geométrico
sucesiones --- cada valor propio corre su propia carrera. El Markov
el estado estacionario es el \omterm{def:g11:vect:vector}{vector} propio del valor propio $1$, y el
La tasa de convergencia de la pregunta 15 es el siguiente valor propio: el
La máquina meteorológica fue una historia propia todo el tiempo.

\textbf{20.} Contabilidad: un sistema es uno matriz \omterm{def:g10:algebra:equation}{ecuación},
resuelto por una inversa. Combinatoria: potencias de la adyacencia
matriz contar paseos, enlaces, conexiones. Evolución: poderes de
la máquina lleva a los estados a sus destinos, y la
direcciones propias (la dirección dorada de Fibonacci, la dirección del tiempo).
estado estacionario, el ranking de la web \omterm{def:g11:vect:vector}{vector}) son los destinos.
El álgebra lineal, en los volúmenes universitarios, es la ciencia de
exactamente esto.
\end{solution}
