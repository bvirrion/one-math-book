\chapter{Les espaces \texorpdfstring{$L^p$}{Lp}}\label{ch:b3:lp}

L'intégrale de Lebesgue a été construite pour l'analyse~; les
espaces $L^p$ sont le lieu où cette analyse vit. Ce sont des
espaces de Banach (Riesz--Fischer) --- les \omterm{thm:b3:complete:completion}{complétés} que
l'\cref{exo:b3:complete:1} montrait manquer aux fonctions
\omterm{def:b3:topology:continuity}{continues} --- et ils supportent une technologie de
lissage, la \emph{convolution contre des \omterm{def:b3:lp:mollifier}{mollificateurs}}, qui
approxime toute fonction $L^p$ par des $\mathcal C^\infty$.
Ce chapitre prouve les versions intégrales de Hölder et
Minkowski, la complétude, les théorèmes de \omterm{ex:b3:lebesgue:gamma}{densité}, et la
machine de régularisation, pour finir par la géographie des
inclusions et de l'interpolation de l'échelle $L^p$. Tout au
long, $(X, \mathcal A, \mu)$ est un espace mesuré et les
fonctions sont à valeurs complexes~; sur $\R^d$, la \omterm{def:b3:measure:measure}{mesure} est
$\lambda_d$.

\section{Définition~; Hölder et Minkowski}

\begin{definition}\label{def:b3:lp:space}
Pour $1 \leq p < \infty$, $\mathcal L^p(\mu)$ est l'ensemble
des $f$ \omterm{def:b3:lebesgue:measurable}{mesurables} avec $\norm f_p = \bigl(\int\abs
f^p\dd\mu\bigr)^{1/p} < \infty$, et $\mathcal L^\infty(\mu)$
l'ensemble des $f$ bornées hors d'un ensemble nul, avec
$\norm f_\infty$ le \emph{sup essentiel} --- le plus petit $M$
avec $\abs f \leq M$ p.p.\ (l'inf est atteint~: intersecter les
ensembles nuls pour $M + \frac1n$). Comme $\norm f_p = 0$ ne
force $f = 0$ que \emph{p.p.}
(\cref{exo:b3:lebesgue:5}), on définit
\[
L^p(\mu) = \mathcal L^p(\mu)/\{f = 0 \text{ p.p.}\} :
\]
les éléments sont des classes de fonctions modulo les
ensembles nuls, et $\norm\cdot_p$ est une vraie norme sur
$L^p$.\index{espace Lp@espace $L^p$}
\end{definition}

\begin{theorem}[Inégalité de Hölder]\label{thm:b3:lp:holder}
Soient $1 \leq p, q \leq \infty$ avec $\frac1p + \frac1q = 1$
(exposants conjugués). Pour $f, g$ \omterm{def:b3:lebesgue:measurable}{mesurables}~:\index{inégalité
de Hölder}
\[
\norm{fg}_1 \leq \norm f_p\,\norm g_q ,
\]
avec égalité (pour $1 < p < \infty$, normes finies, $f,g\ne0$)
ssi $\abs f^p$ et $\abs g^q$ sont proportionnelles p.p.
\end{theorem}

\begin{proof}
Les cas $\{p, q\} = \{1, \infty\}$ sont directs ($\abs{fg}
\leq \norm g_\infty\abs f$ p.p.). Soit $1 < p < \infty$~;
normaliser $\norm f_p = \norm g_q = 1$ (homogénéité~; les
normes nulles ou infinies sont triviales). L'inégalité de
Young $ab \leq \frac{a^p}p + \frac{b^q}q$ ($a, b \geq 0$~;
concavité de $\ln$, comme dans le~\cref{pb:b3:banach:1})
donne ponctuellement $\abs{f g} \leq \frac{\abs f^p}p +
\frac{\abs g^q}q$~; intégrer~: $\norm{fg}_1 \leq \frac1p +
\frac1q = 1$. L'égalité force l'égalité p.p.\ dans Young,
c.-à-d.\ $\abs f^p = \abs g^q$ p.p.\ (après normalisation~; en
la défaisant, proportionnalité).
\end{proof}

\begin{theorem}[Inégalité de Minkowski]\label{thm:b3:lp:minkowski}
Pour $1 \leq p \leq \infty$~: $\norm{f + g}_p \leq \norm f_p
+ \norm g_p$.\index{inégalité de Minkowski}
\end{theorem}

\begin{proof}
$p = 1, \infty$~: inégalité triangulaire ponctuelle/p.p. Pour
$1 < p < \infty$, supposer $\norm{f+g}_p < \infty$ (sinon
utiliser $\abs{f+g}^p \leq 2^{p-1}(\abs f^p + \abs g^p)$,
conséquence de la convexité de $t^p$, pour voir que le membre
de gauche est fini lorsque celui de droite l'est). Alors
\[
\norm{f{+}g}_p^p \leq \int\abs f\,\abs{f{+}g}^{p-1} +
\int\abs g\,\abs{f{+}g}^{p-1}
\leq \bigl(\norm f_p + \norm g_p\bigr)\,
\bigl\|\abs{f{+}g}^{p-1}\bigr\|_q
\]
par Hölder, et $\norm{\abs{f+g}^{p-1}}_q =
\norm{f+g}_p^{p/q}$ car $(p-1)q = p$~; diviser par
$\norm{f+g}_p^{p/q}$ (si non nul~; sinon trivial) et utiliser
$p - \frac pq = 1$.
\end{proof}

\section{Complétude et ses compagnons}

\begin{theorem}[Riesz--Fischer]\label{thm:b3:lp:complete}
Pour $1 \leq p \leq \infty$, $L^p(\mu)$ est un espace de
Banach. De plus, toute suite convergeant dans $L^p$ a une
sous-suite convergeant \emph{\omterm{def:b3:lebesgue:l1}{presque partout}} (avec un
dominateur $L^p$ dans le cas $p < \infty$).\index{théorème de
Riesz--Fischer}
\end{theorem}

\begin{proof}
$p = \infty$~: une suite $\norm\cdot_\infty$-de Cauchy est,
hors d'un seul ensemble nul (union dénombrable),
uniformément de Cauchy~: elle converge uniformément hors de
celui-ci~; terminé. Soit $p < \infty$. Par
l'\cref{exo:b3:complete:1}(b) il suffit de sommer les séries
absolument convergentes~: soit $\sum\norm{f_k}_p = M <
\infty$. Poser $G_n = \sum_{k \leq n}\abs{f_k}$ et $G =
\sum_k\abs{f_k}$ (ponctuellement dans $[0,\infty]$)~: par
Minkowski $\norm{G_n}_p \leq M$, et le TCM ($G_n^p \nearrow
G^p$) donne $\int G^p \leq M^p$~: $G < \infty$ p.p., donc la
série $\sum f_k(x)$ converge absolument pour p.t.\ $x$~;
appeler la somme $S(x)$ (n'importe quelle valeur sur
l'ensemble nul). Alors $\abs{S - \sum_{k\leq n}f_k}^p \leq
(2G)^p \in L^1$, et le TCD donne $\norm{S - \sum_{k\leq
n}f_k}_p \to 0$~: la série converge dans $L^p$.

L'énoncé de sous-suite~: si $f_n \to f$ dans $L^p$, choisir
$n_k$ avec $\norm{f_{n_{k+1}} - f_{n_k}}_p \leq 2^{-k}$~; la
série $\sum(f_{n_{k+1}} - f_{n_k})$ tombe sous l'argument
précédent~: elle converge absolument p.p., dominée par un $G
\in L^p$, donc $f_{n_k} \to f_{n_1} + \sum(\cdots) $ p.p., et
cette limite p.p.\ doit être (un représentant de) $f$ (toutes
deux sont des limites $L^p$). Le dominateur~:
$\abs{f_{n_k}} \leq \abs{f_{n_1}} + G$.
\end{proof}

\begin{remark}\label{rem:b3:lp:modes}
La convergence $L^p$ n'implique pas la convergence p.p.\ (la
suite \emph{machine à écrire}, \cref{exo:b3:lp:3}), ni
réciproquement (bosses qui s'échappent)~: les deux modes ne
sont liés que par les sous-suites et la domination. Garder en
tête les contre-exemples de l'\cref{exo:b3:lp:3} est le
meilleur vaccin.
\end{remark}

\section{Théorèmes de densité}

\begin{theorem}\label{thm:b3:lp:density}
Soit $1 \leq p < \infty$.
\begin{enumerate}
  \item Les \omterm{def:b3:lebesgue:simple}{fonctions simples} (à supports de \omterm{def:b3:measure:measure}{mesure} finie)
        sont \omterm{def:b3:topology:interior}{denses} dans $L^p(\mu)$.
  \item Dans $L^p(\R^d)$, les fonctions \omterm{def:b3:topology:continuity}{continues} à support
        compact $\mathcal C_c(\R^d)$ sont \omterm{def:b3:topology:interior}{denses}.
  \item La translation est \omterm{def:b3:topology:continuity}{continue} sur $L^p(\R^d)$~: en
        écrivant $\tau_hf = f(\cdot - h)$, $\norm{\tau_hf -
        f}_p \to 0$ lorsque $h \to 0$.
\end{enumerate}
Aucun des trois ne tient pour $p = \infty$.
\end{theorem}

\begin{proof}
(1) Pour $f \geq 0$~: les $s_n \nearrow f$ dyadiques
du~\cref{thm:b3:lebesgue:approximation} satisfont $\abs{f -
s_n}^p \leq f^p \in L^1$~: TCD. (Chaque $s_n \leq f$ est dans
$L^p$, et ses ensembles de niveau ont \omterm{def:b3:measure:measure}{mesure} finie là où la
valeur est positive~: $\mu(s_n \geq c) \leq c^{-p}\int f^p$.)
Scinder $f$ générale en quatre parties positives.

(2) Par (1) il suffit d'approximer $\mathbf 1_A$, $A$ borélien
avec $\lambda_d(A) < \infty$. La régularité (preuve comme
au~\cref{thm:b3:measure:regularity}) donne un compact $K
\subseteq A \subseteq U$ \omterm{def:b3:topology:topology}{ouvert} avec $\lambda_d(U\setminus K)
< \varepsilon$~; la fonction d'Urysohn
\[
\varphi(x) = \frac{d(x, \R^d\setminus U)}{d(x, \R^d\setminus U)
+ d(x, K)}
\]
est \omterm{def:b3:topology:continuity}{continue}, $1$ sur $K$, $0$ hors de $U$, et peut être
prise à support compact (rétrécir d'abord $U$ en un \omterm{def:b3:topology:topology}{ouvert}
borné). Alors $\norm{\mathbf 1_A - \varphi}_p^p \leq
\lambda_d(U\setminus K) < \varepsilon$.

(3) Pour $g \in \mathcal C_c$~: la continuité uniforme donne
$\norm{\tau_hg - g}_\infty \to 0$, avec supports dans un
compact fixe pour $\abs h \leq 1$~: $\norm{\tau_hg - g}_p \to
0$. Pour $f$ générale~: choisir $g \in \mathcal C_c$ avec
$\norm{f - g}_p < \varepsilon$~; alors $\norm{\tau_hf -
f}_p \leq 2\norm{f - g}_p + \norm{\tau_hg - g}_p$
(invariance par translation de la norme).

Pour $p = \infty$~: l'approximation uniforme de $\mathbf
1_{\intoo0\infty}$ par des fonctions \omterm{def:b3:topology:continuity}{continues} est impossible
(saut), et $\norm{\tau_h\mathbf 1_{\intoo0\infty} - \mathbf
1_{\intoo0\infty}}_\infty = 1$ pour $h \neq 0$.
\end{proof}

\section{Convolution et régularisation}

\begin{theorem}[Inégalité de Young]\label{thm:b3:lp:young}
Soient $1 \leq p \leq \infty$, $f \in L^1(\R^d)$, $g \in
L^p(\R^d)$. Alors $f * g$ est définie p.p., appartient à
$L^p$, et\index{inégalité de Young}
\[
\norm{f * g}_p \leq \norm f_1\,\norm g_p .
\]
\end{theorem}

\begin{proof}
$p = \infty$~: borne directe. $p = 1$~:
\cref{thm:b3:product:convolution}. Soit $1 < p < \infty$, $q$
conjugué. Scinder $\abs{f(y)} = \abs{f(y)}^{1/q}\cdot
\abs{f(y)}^{1/p}$ et appliquer Hölder~:
\[
\int\abs{f(y)}\,\abs{g(x{-}y)}\,\dd y
\leq \Bigl(\int\abs f\Bigr)^{1/q}
\Bigl(\int\abs{f(y)}\,\abs{g(x - y)}^p\,\dd y\Bigr)^{1/p} .
\]
Élever à la puissance $p$ et intégrer en $x$~; Tonelli sur le
second facteur donne $\norm f_1^{p/q}\cdot\norm f_1\norm
g_p^p$, c.-à-d.\ $\norm{f*g}_p^p \leq \norm f_1^{1 +
p/q}\norm g_p^p = (\norm f_1\norm g_p)^p$ --- et la finitude
de l'intégrale de Tonelli justifie la convergence absolue
p.p.\ comme au~\cref{thm:b3:product:convolution}.
\end{proof}

\begin{definition}[Mollificateurs]\label{def:b3:lp:mollifier}
La fonction\index{mollificateur}\index{fonction bump}
\[
\rho(x) = \begin{cases}
c\,\exp\Bigl(-\dfrac{1}{1 - \norm x^2}\Bigr) & \norm x < 1,\\
0 & \norm x \geq 1,
\end{cases}
\]
avec $c$ normalisant $\int\rho = 1$, est $\mathcal C^\infty$
sur $\R^d$~: le point est que $t \mapsto \eu^{-1/t}\mathbf
1_{t>0}$ est $\mathcal C^\infty$ sur $\R$, toutes ses
dérivées en $0^+$ étant $0$ (chaque dérivée est
$P(1/t)\eu^{-1/t}$ pour un polynôme $P$, qui tend vers $0$~;
induction). Pour $\varepsilon > 0$ poser
$\rho_\varepsilon(x) = \varepsilon^{-d}\rho(x/\varepsilon)$~:
supportée dans $\bar B(0, \varepsilon)$, encore d'intégrale
$1$.
\end{definition}

\begin{theorem}[Régularisation]\label{thm:b3:lp:regularization}
Soient $1 \leq p < \infty$ et $f \in L^p(\R^d)$. Alors~:
\begin{enumerate}
  \item $f * \rho_\varepsilon \in \mathcal C^\infty(\R^d)$,
        avec $\partial^\alpha(f * \rho_\varepsilon) = f *
        \partial^\alpha\rho_\varepsilon$~;
  \item $\norm{f * \rho_\varepsilon - f}_p \to 0$ lorsque
        $\varepsilon \to 0$~;
  \item par conséquent $\mathcal C^\infty_c(\R^d)$ est \omterm{def:b3:topology:interior}{dense}
        dans $L^p(\R^d)$.
\end{enumerate}
\end{theorem}

\begin{proof}
(1) Dérivation sous le signe intégral
(\cref{thm:b3:lebesgue:paramdiff}) en $x$~: pour $x$ dans une
boule $B$, $\abs{\partial_{x_i}\rho_\varepsilon(x - y)} \leq
C_\varepsilon\,\mathbf 1_{K}(y)$ avec $K$ compact ($y$ à
distance $\varepsilon$ de $B$), et $\abs f\,\mathbf 1_K \in
L^1$ (Hölder contre $\mathbf 1_K$)~: le théorème s'applique~;
itérer pour les dérivées d'ordre supérieur.

(2) Comme $\int\rho_\varepsilon = 1$~:
\[
(f * \rho_\varepsilon)(x) - f(x)
= \int \bigl(f(x - y) - f(x)\bigr)\rho_\varepsilon(y)\,\dd y ,
\]
et l'inégalité de Minkowski \emph{intégrale} --- ou
directement~: Hölder/Jensen avec la \omterm{def:b3:measure:measure}{mesure} de probabilité
$\rho_\varepsilon\dd y$ et Tonelli ---
\[
\norm{f*\rho_\varepsilon - f}_p^p
\leq \int\Bigl(\int\abs{f(x-y) -
f(x)}^p\dd x\Bigr)\rho_\varepsilon(y)\,\dd y
= \int \norm{\tau_yf - f}_p^p\;\rho_\varepsilon(y)\,\dd y
\]
(l'étape du milieu~: appliquer l'inégalité de Jensen,
\cref{exo:b3:lp:10}, à l'intégrale intérieure en $y$, puis
Tonelli). L'intégrande est supportée dans $\norm y \leq
\varepsilon$ et y tend vers $0$ uniformément lorsque
$\varepsilon \to 0$ (\cref{thm:b3:lp:density}(3))~: toute
l'expression tend vers $0$.

(3) Approximer $f$ par $g \in \mathcal C_c$
(\cref{thm:b3:lp:density}(2)), puis $g$ par $g *
\rho_\varepsilon \in \mathcal C_c^\infty$ (support compact~:
somme des supports).
\end{proof}

\begin{example}[Mollifier $\abs x$, avec vitesses]
\label{ex:b3:lp:mollifyexample}
Prendre $f(x) = \abs x$ sur $\R$ (localement $L^1$~; le
théorème s'applique sur toute fenêtre bornée) et un
\omterm{def:b3:lp:mollifier}{mollificateur} symétrique $\rho_\varepsilon$. Alors
\[
f_\varepsilon(x) = (f * \rho_\varepsilon)(x)
= \int\abs{x - y}\,\rho_\varepsilon(y)\,\dd y
\]
est $\mathcal C^\infty$~; loin du coin, rien ne se passe~: pour
$\abs x \geq \varepsilon$, $\abs{x - y}$ est linéaire en $x$
sur le support de $\rho_\varepsilon$, donc $f_\varepsilon(x)
= \abs x$ \emph{exactement} (la symétrie tue la correction).
Près de $0$, le lissage coûte précisément
\[
0 \leq f_\varepsilon(0) = \int\abs
y\,\rho_\varepsilon(y)\,\dd y \leq \varepsilon,
\qquad
\norm{f_\varepsilon - f}_\infty \leq \varepsilon :
\]
l'erreur d'approximation est confinée au
$\varepsilon$-voisinage de la singularité et est de sa
taille. Par ailleurs $f_\varepsilon'' \geq 0$ partout ($f$
est convexe, et la convolution contre $\rho_\varepsilon \geq
0$ préserve la convexité), avec $\int f_\varepsilon'' =
f_\varepsilon'(\infty) - f_\varepsilon'(-\infty) = 2$~: la
dérivée seconde est une bosse de masse $2$ pressée dans une
largeur $O(\varepsilon)$, donc $\norm{f_\varepsilon''}_\infty
\gtrsim \varepsilon^{-1}$. Lisser est un échange~: erreur
uniforme $O(\varepsilon)$ contre explosion des dérivées
$O(\varepsilon^{-1})$ --- le taux d'échange exact que
l'analyse quantitative (inégalités d'interpolation, le cercle
d'idées du~\cref{pb:b3:lp:1}) formalise.
\end{example}

\begin{corollary}[Lemme fondamental du calcul des
variations]\label{cor:b3:lp:fundlemma}
Soit $f \in L^1_{\mathrm{loc}}(\R^d)$ (\omterm{def:b3:lebesgue:l1}{intégrable} sur les
compacts) avec $\int f\varphi = 0$ pour toute $\varphi \in
\mathcal C^\infty_c(\R^d)$. Alors $f = 0$
p.p.\index{lemme fondamental du calcul des variations}
\end{corollary}

\begin{proof}
Fixer une boule $B = B(0, R)$ et soit $g = f\mathbf 1_{B(0,
R+1)} \in L^1$. Pour $x \in B$ et $\varepsilon < 1$~: $(g *
\rho_\varepsilon)(x) = \int f(y)\rho_\varepsilon(x - y)\dd y =
0$, la fonction test étant $y \mapsto \rho_\varepsilon(x-y)
\in \mathcal C_c^\infty$. Mais $g * \rho_\varepsilon \to g$
dans $L^1$ (\cref{thm:b3:lp:regularization})~: $g = 0$ p.p.\
sur $B$~; épuiser $\R^d$.
\end{proof}

\section{La géographie \texorpdfstring{$L^p$}{Lp}}

\begin{proposition}\label{prop:b3:lp:inclusions}
(a) Si $\mu(X) < \infty$ et $1 \leq p \leq q \leq \infty$,
alors $L^q \subseteq L^p$ avec $\norm f_p \leq
\mu(X)^{\frac1p - \frac1q}\,\norm f_q$.
(b) Sur $\R^d$ (\omterm{def:b3:measure:measure}{mesure} infinie) il n'y a pas d'inclusions~:
pour $p \neq q$ il y a des fonctions dans $L^p\setminus L^q$.
(c) (Interpolation) Si $p < r < q$ et $\alpha \in \intoo01$
est défini par $\frac1r = \frac\alpha p + \frac{1 - \alpha}q$,
alors
\[
\norm f_r \leq \norm f_p^{\alpha}\,\norm f_q^{1 - \alpha} ;
\]
en particulier $L^p \cap L^q \subseteq L^r$.
\end{proposition}

\begin{proof}
(a) Hölder avec les exposants $\frac qp$ et son conjugué~:
$\int\abs f^p\cdot 1 \leq \norm{\abs f^p}_{q/p}\,\norm
1_{(q/p)'} = \norm f_q^p\,\mu(X)^{1 - p/q}$ ($q = \infty$
directement). (b) Près de $0$ et près de $\infty$, les
puissances $x^{-\alpha}$ calibrent~: \cref{exo:b3:lp:2}. (c)
Écrire $\abs f^r = \abs f^{r\alpha}\abs f^{r(1-\alpha)}$ et
appliquer Hölder avec la paire conjuguée $\frac p{r\alpha}$,
$\frac q{r(1-\alpha)}$ (conjugués précisément par la
définition de $\alpha$)~: $\int\abs f^r \leq
\norm f_p^{r\alpha}\norm f_q^{r(1 - \alpha)}$.
\end{proof}

\begin{method}\label{met:b3:lp:toolkit}
La boîte à outils $L^p$, telle qu'utilisée partout
ci-dessous~: pour prouver une identité ou inégalité pour tout
$f \in L^p$ --- la prouver sur une classe \omterm{def:b3:topology:interior}{dense} ($\mathcal
C_c^\infty$ via~\cref{thm:b3:lp:regularization}) et étendre
par continuité (\cref{thm:b3:complete:extension}, les deux
membres étant $L^p$-continus)~; pour prouver $f = 0$, tester
contre $\mathcal C_c^\infty$ (\cref{cor:b3:lp:fundlemma})~;
pour gagner de la régularité, convoler~; pour échanger les
exposants, Hölder et interpolation. La théorie de Fourier
du~\cref{ch:b3:fouriertransform} est une longue application de
cette méthode.
\end{method}

\section{Exercices}

\begin{exercise}[$\star$]\label{exo:b3:lp:1}
(a) Énoncer et prouver l'inégalité de Cauchy--Schwarz dans
$L^2(\mu)$ comme le cas $p = q = 2$ de Hölder.
(b) Sur un espace de \emph{probabilité}, montrer que $p
\mapsto \norm f_p$ est croissante.
(c) Quand Hölder est-elle une égalité pour $p = 1$, $q =
\infty$~?
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b3:lp:2}
Pour quels $p \in \intco1\infty$ les suivantes appartiennent-elles
à $L^p$~?
\[
x^{-1/2}\mathbf 1_{\intoo01},\qquad
x^{-1/2}\mathbf 1_{\intoo1\infty},\qquad
\frac{1}{x^{1/2}(1 + \abs{\ln x})}\ \text{sur } \intoo01,
\qquad
\frac1{1 + \abs x}\ \text{sur } \R .
\]
Conclure~: sur $\intoo01$ un petit $p$ est plus facile, sur
$\intoo1\infty$ un grand $p$ est plus facile, et aucun $L^p$
n'en contient un autre sur $\R$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:lp:3}
(La machine à écrire) Énumérer les intervalles dyadiques $I_1
= \intcc01$, $I_2 = \intcc0{\frac12}$, $I_3 =
\intcc{\frac12}1$, $I_4 = \intcc0{\frac14}$, \dots\ et soit
$f_n = \mathbf 1_{I_n}$.
(a) Montrer $f_n \to 0$ dans tout $L^p(\intcc01)$, $p <
\infty$, mais $(f_n(x))$ diverge pour \emph{tout} $x \in
\intcc01$.
(b) Exhiber la sous-suite convergente p.p.\ promise par
le~\cref{thm:b3:lp:complete}.
(c) Réciproquement donner une suite convergeant p.p.\ mais
pas dans $L^1$, et une convergeant dans $L^1$ mais dans
aucun $L^p$, $p > 1$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:lp:4}
Soient $\mu(X) < \infty$ et $f \in L^\infty(\mu)$, $f \neq
0$. Montrer que $\norm f_p \to \norm f_\infty$ lorsque $p
\to \infty$.
\emph{(Borne supérieure par (a) de
\cref{prop:b3:lp:inclusions}~; borne inférieure en intégrant
sur $\{\abs f > \norm f_\infty - \varepsilon\}$, de \omterm{def:b3:measure:measure}{mesure}
positive.)}
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:lp:5}
(a) Où exactement la preuve
du~\cref{thm:b3:lp:density}(3) utilise-t-elle $p < \infty$~?
(b) Montrer que $f \in L^\infty(\R)$ satisfait
$\norm{\tau_hf - f}_\infty \to 0$ ssi $f$ a un représentant
uniformément continu.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:lp:6}
Soient $p, q$ conjugués, $f \in L^p(\R^d)$, $g \in
L^q(\R^d)$. Montrer que $f * g$ est définie
\emph{partout}, bornée, avec $\norm{f*g}_\infty \leq \norm
f_p\norm g_q$, et \emph{uniformément \omterm{def:b3:topology:continuity}{continue}}.
\emph{(Continuité de la translation dans $L^p$~; traiter $p
\in \{1, \infty\}$ séparément --- pour $p = \infty$ utiliser
la continuité de translation sur le facteur $L^1$.)}
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:lp:7}
Soit $f \in L^1_{\mathrm{loc}}(\intoo ab)$ avec
$\int f\varphi' = 0$ pour toute $\varphi \in \mathcal
C^\infty_c(\intoo ab)$. Montrer que $f$ est p.p.\ égale à une
constante. \emph{(Fixer $\chi \in \mathcal C_c^\infty$ avec
$\int\chi = 1$~; toute $\psi \in \mathcal C_c^\infty$ avec
$\int\psi = 0$ est une $\varphi'$~; écrire une fonction test
générale comme $\psi + (\int\psi)\chi$ et appliquer
le~\cref{cor:b3:lp:fundlemma} à $f - c$ avec $c = \int
f\chi$.)}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b3:lp:8}
(Urysohn lisse) Soient $K \subseteq U \subseteq \R^d$, $K$
compact, $U$ \omterm{def:b3:topology:topology}{ouvert}. Construire $\varphi \in \mathcal
C^\infty_c(\R^d)$ avec $0 \leq \varphi \leq 1$, $\varphi =
1$ sur $K$, $\operatorname{supp}\varphi \subseteq U$.
\emph{(Mollifier l'indicateur du $\delta$-voisinage
$K_\delta$ de $K$ avec $\rho_{\delta/2}$, pour $\delta$
petit.)}
En déduire un énoncé de partition de l'unité $\mathcal
C^\infty$ pour un compact recouvert par un nombre fini
d'\omterm{def:b3:topology:topology}{ouverts}.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:lp:9}
En utilisant l'interpolation (\cref{prop:b3:lp:inclusions}(c))~:
(a) montrer que $L^1(\R)\cap L^\infty(\R) \subseteq L^p(\R)$
pour tout $p$, avec $\norm f_p \leq \norm f_1^{1/p}\norm
f_\infty^{1 - 1/p}$~;
(b) montrer que $f \mapsto \norm f_p$ est, pour $f$ fixé,
log-convexe en $\frac1p$, et donner un exemple où $f \in L^p$
exactement pour $p$ dans un intervalle donné $(p_0, p_1)$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:lp:10}
(Jensen) Soit $\mu$ une \omterm{def:b3:measure:measure}{mesure} de \emph{probabilité}, $f \in
L^1(\mu)$ réelle, et $\Phi \colon \R \to \R$ convexe. Montrer
\[
\Phi\Bigl(\int f\,\dd\mu\Bigr) \leq \int \Phi\circ
f\,\dd\mu
\]
\emph{(droite de support de $\Phi$ au point $m = \int f$)}.
En déduire l'inégalité arithmético-géométrique et la
monotonie de $p \mapsto \norm f_p$ de
l'\cref{exo:b3:lp:1}(b).\index{inégalité de Jensen}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b3:lp:11}
(Inégalité de Young pour la convolution) Soient $1 \leq p, q,
r \leq \infty$ avec $\frac1p + \frac1q = 1 + \frac1r$, et $f
\in L^p(\R^d)$, $g \in L^q(\R^d)$.
(a) Prouver $\norm{f * g}_r \leq \norm f_p\,\norm g_q$.
\emph{(Écrire, pour les exposants conjugués tirés de $p, q,
r$,
\[
\abs{f(y)g(x-y)} = \bigl(\abs f^p\abs g^q\bigr)^{1/r}
\cdot\abs f^{\,p(1/p - 1/r)}\cdot\abs g^{\,q(1/q - 1/r)},
\]
et appliquer l'inégalité de Hölder à trois facteurs avec
exposants $r$, $\frac{pr}{r - p}$, $\frac{qr}{r - q}$~; puis
intégrer en $x$ par Tonelli.)}
(b) Vérifier les trois cas spéciaux déjà connus~: $r =
\infty$ (Hölder, \cref{exo:b3:lp:6})~; $q = 1$
($L^p$-stabilité de la convolution par un noyau
\omterm{def:b3:lebesgue:l1}{intégrable})~; $p = q = 1$ ($L^1$ est une algèbre de
convolution, \cref{thm:b3:product:convolution}).
(c) Pourquoi n'y a-t-il pas d'inégalité avec $\frac1p +
\frac1q < 1 + \frac1r$~? \emph{(Tester sur les dilatations
$f_\lambda(x) = f(\lambda x)$ et comparer les échelles des
deux membres.)}
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:lp:12}
(Cas d'égalité) (a) Dans l'inégalité de Hölder
$\int\abs{fg} \leq \norm f_p\norm g_q$ ($1 < p < \infty$),
montrer que l'égalité tient ssi $\abs f^p$ et $\abs g^q$ sont
proportionnelles p.p. \emph{(Suivre le cas d'égalité de
l'inégalité de Young $ab \leq \frac{a^p}p + \frac{b^q}q$,
qui est $a^p = b^q$.)}
(b) Dans l'inégalité de Minkowski $\norm{f + g}_p \leq \norm
f_p + \norm g_p$ ($1 < p < \infty$), montrer que l'égalité
avec $f, g \neq 0$ force $g = cf$ p.p.\ avec $c > 0$.
(c) Contraster avec $p = 1$ et $p = \infty$~: décrire les
cas d'égalité (beaucoup plus larges) là, sur des exemples.
\end{exercise}

\section{Problème~: l'inégalité de Hardy}

\begin{problem}[{Problème du week-end --- l'inégalité de
Hardy et sa constante optimale}]\label{pb:b3:lp:1}
Pour $f \in L^p(\intoo0{+\infty})$, $1 < p < \infty$,
définir l'\emph{opérateur de Hardy}
\[
(Hf)(x) = \frac1x\int_0^x f(t)\,\dd t .
\]
L'inégalité de Hardy (1920) affirme\index{inégalité de Hardy}
\[
\norm{Hf}_p \;\leq\; \frac{p}{p-1}\,\norm f_p ,
\]
et la constante $\frac p{p-1}$ est optimale et non atteinte.
Ce problème prouve tout, puis s'étend aux séries.

\medskip
\noindent\textbf{Partie I --- L'inégalité.} Supposer d'abord
$f \geq 0$ \omterm{def:b3:topology:continuity}{continue} à support compact dans
$\intoo0{+\infty}$, et soit $F(x) = \int_0^xf$.
\begin{enumerate}
  \item Montrer que $Hf \in L^p$~: près de $0$, $F$ s'annule
        sur un \omterm{def:b3:topology:topology}{voisinage} de $0$~; près de $\infty$, $F$ est
        bornée, donc $(Hf)(x) = O(1/x)$, et $x \mapsto
        \frac1x$ appartient à $L^p(\intoo1{+\infty})$ pour
        $p > 1$.
  \item Intégrer par parties pour montrer
        \[
        \int_0^\infty \Bigl(\frac Fx\Bigr)^p\dd x
        = \frac{p}{p-1}\int_0^\infty\Bigl(\frac
        Fx\Bigr)^{p-1}f(x)\,\dd x .
        \]
        \emph{(Dériver $x^{1-p}F^p$~; les termes de bord
        s'annulent --- justifier les deux extrémités.)}
  \item Appliquer Hölder au membre de droite et en déduire
        $\norm{Hf}_p \leq \frac p{p-1}\norm f_p$ pour de
        telles $f$.
  \item Étendre à tout $L^p$~: pour $f \geq 0$, construire
        des $f_n$ \omterm{def:b3:topology:continuity}{continues} à support compact dans
        $\intoo0{+\infty}$, $0 \leq f_n \nearrow f$ p.p.\
        \emph{(tronquer, puis approximer de façon monotone
        --- justifier la construction)}~; alors $Hf_n
        \nearrow Hf$ ponctuellement (TCM à l'intérieur de
        la moyenne) et le TCM passe l'inégalité à la
        limite. Pour $f$ signée ou complexe, conclure avec
        $\abs{Hf} \leq H\abs f$.
\end{enumerate}

\medskip
\noindent\textbf{Partie II --- Optimalité.}
\begin{enumerate}[resume]
  \item Pour $A > 1$ soit $f_A(t) = t^{-1/p}\,\mathbf
        1_{\intcc1A}(t)$. Calculer $\norm{f_A}_p^p = \ln A$
        et, pour $1 \leq x \leq A$,
        \[
        (Hf_A)(x) = \frac p{p-1}\;x^{-1/p}\,
        \bigl(1 - x^{-(1 - 1/p)}\bigr).
        \]
  \item En déduire $\liminf_{A\to\infty} \norm{Hf_A}_p/
        \norm{f_A}_p \geq \frac{p}{p-1}$, et conclure que
        la constante est optimale.
  \item Montrer que l'égalité $\norm{Hf}_p = \frac
        p{p-1}\norm f_p$ avec $f \neq 0$ est impossible.
        \emph{(Suivre le cas d'égalité de Hölder dans la
        question 3~: il forcerait un comportement $f =
        cx^{-1/p}$, qui n'est pas dans $L^p$.)}
\end{enumerate}

\medskip
\noindent\textbf{Partie III --- L'inégalité discrète.}
\begin{enumerate}[resume]
  \item Pour un $g \geq 0$ décroissant sur $(0,\infty)$ et
        $a_n = g(n)$, comparer $\sum a_n^p$ et $\int g^p$,
        et les moyennes $H$ en conséquence, pour déduire de
        la partie I l'\emph{inégalité de Hardy discrète}~:
        pour $a_n \geq 0$,
        \[
        \sum_{n\geq1}\Bigl(\frac{a_1 + \dots +
        a_n}{n}\Bigr)^{p}
        \;\leq\;
        \Bigl(\frac{p}{p-1}\Bigr)^{p}\,\sum_{n\geq1}a_n^p
        \]
        --- la prouver d'abord pour $(a_n)$ décroissante via
        la comparaison ci-dessus, puis ramener le cas
        général au décroissant par réarrangement
        \emph{(admettre, avec une justification en une
        ligne, que trier $(a_n)$ en ordre décroissant ne
        peut qu'augmenter le membre de gauche en fixant le
        droit)}.
  \item En déduire~: si $\sum a_n^p < \infty$ alors les
        moyennes de Cesàro de $(a_n)$ sont encore
        $\ell^p$ --- et donner un exemple ($p = 2$) où
        $(a_n) \in \ell^2$ mais $a_n$ n'est pas sommable,
        pourtant Hardy contrôle encore les moyennes.
\end{enumerate}

\medskip
\noindent\textbf{Partie IV --- Épilogue.}
\begin{enumerate}[resume]
  \item Montrer que l'inégalité de Hardy échoue pour $p =
        1$~: avec $f = \mathbf 1_{\intcc01}$, calculer $Hf$
        et observer $Hf \notin L^1$. Où la preuve
        s'effondre-t-elle~?
\end{enumerate}

\medskip
\noindent\textbf{Partie V --- La fonction maximale, et le
théorème de différentiation de Lebesgue.} Hardy moyenne
depuis l'origine~; Hardy--Littlewood moyenne \emph{autour
de chaque point}. Pour $f \in L^1(\R)$ définir
\[
Mf(x) = \sup_{r>0}\ \frac1{2r}\int_{x-r}^{x+r}\abs
f\,\dd\lambda .
\]
\begin{enumerate}[resume]
  \item (Vitali, version finie) Soient $B_1, \dots, B_N$ des
        intervalles \omterm{def:b3:topology:topology}{ouverts}. Montrer qu'il existe une
        sous-famille disjointe $B_{i_1}, \dots, B_{i_k}$
        avec $\bigcup_jB_j \subseteq \bigcup_l3B_{i_l}$,
        où $3B$ désigne l'intervalle de même \omterm{ex:b3:groups:actions}{centre} et de
        triple longueur \emph{(glouton~: choisir
        répétitivement le plus long intervalle disjoint de
        ceux déjà choisis)}.
  \item (Type faible $(1,1)$) Montrer que pour tout $t >
        0$,
        \[
        \lambda\bigl(\{Mf > t\}\bigr) \;\leq\;
        \frac3t\,\norm f_1 :
        \]
        chaque $x$ avec $Mf(x) > t$ possède un intervalle
        centré $B_x$ avec $\int_{B_x}\abs f >
        t\,\lambda(B_x)$~; prendre un compact $K \subseteq
        \{Mf > t\}$ (régularité intérieure), le recouvrir
        par un nombre fini de $B_x$, appliquer la question
        11, et épuiser.
  \item Calculer $M\mathbf 1_{\intcc01}(x)$ pour $x > 1$ et
        en déduire que $Mf \notin L^1$ pour toute $f \neq 0$
        ($Mf(x) \geq \frac c{\abs x}$ à l'infini)~: en $p =
        1$, l'inégalité faible de la question 12 est le
        meilleur énoncé possible.
  \item (Type fort pour $p > 1$) Pour $f \in L^p$~: scinder
        $f = f\,\mathbf 1_{\abs f > t/2} + f\,\mathbf
        1_{\abs f \leq t/2}$, observer $Mf \leq
        M\bigl(f\mathbf 1_{\abs f > t/2}\bigr) + \frac
        t2$, et combiner la question 12 avec la formule des
        couches (\cref{prop:b3:product:layercake}) et
        Tonelli pour prouver
        \[
        \norm{Mf}_p^p \;\leq\;
        \frac{6p\,2^{p-1}}{p-1}\,\norm f_p^p .
        \]
        (L'explosion lorsque $p \downarrow 1$ est l'échec de
        la question 13, quantifié.)
  \item (Théorème de différentiation de Lebesgue) Prouver~:
        pour $f \in L^1(\R)$,
        \[
        \frac1{2r}\int_{x-r}^{x+r}f\,\dd\lambda
        \;\longrightarrow\; f(x)
        \qquad (r \to 0)\quad\text{pour p.t.\ }x .
        \]
        \emph{(Clair pour $f$ \omterm{def:b3:topology:continuity}{continue}. En général écrire
        $f = g + h$, $g$ \omterm{def:b3:topology:continuity}{continue} à support compact,
        $\norm h_1 < \varepsilon$
        (\cref{thm:b3:lp:density})~; l'ensemble où
        $\limsup_{r\to0}$ de l'oscillation moyennée dépasse
        $\delta$ est \omterm{def:b3:rings:content}{contenu} dans $\{Mh > \delta/2\} \cup
        \{\abs h > \delta/2\}$, de \omterm{def:b3:measure:measure}{mesure}
        $O(\varepsilon/\delta)$~; faire $\varepsilon \to
        0$, puis $\delta \to 0$ le long d'une suite.)}
  \item En déduire~: (a) presque tout point est un
        \emph{point de Lebesgue} de $f$~; (b) pour $f \in
        L^1$, la primitive $F(x) = \int_0^xf$ est dérivable
        p.p.\ avec $F' = f$ p.p.\ --- la moitié intégrale
        du théorème fondamental du calcul dans le monde de
        Lebesgue, fermant le cercle \omterm{def:b3:topology:topology}{ouvert} par l'escalier
        (\cref{pb:b3:measure:1}), qui montrait que la
        moitié réciproque peut échouer.
  \item (Points de \omterm{ex:b3:lebesgue:gamma}{densité}) Pour $A \subseteq \R$
        \omterm{def:b3:lebesgue:measurable}{mesurable}, montrer que presque tout $x \in A$
        satisfait
        $\frac{\lambda(A\cap\intcc{x-r}{x+r})}{2r} \to 1$~:
        les ensembles \omterm{def:b3:lebesgue:measurable}{mesurables} sont localement pleins en
        presque tous leurs points. Esquisser, en deux
        lignes, comment cela donne encore une preuve du
        théorème de Steinhaus
        (\cref{exo:b3:measure:8}).
\end{enumerate}

\medskip
\noindent\textbf{Partie VI --- Variations sur le thème de
la moyenne.}
\begin{enumerate}[resume]
  \item (Hardy pondéré) Pour $\alpha < p - 1$ et $f \geq 0$,
        montrer
        \[
        \int_0^\infty\Bigl(\frac{F(x)}x\Bigr)^{p}
        x^{\alpha}\,\dd x \;\leq\;
        \Bigl(\frac{p}{p - 1 - \alpha}\Bigr)^{p}
        \int_0^\infty f(x)^p\,x^{\alpha}\,\dd x
        \]
        par la même intégration par parties, et vérifier
        que la frontière $\alpha = p - 1$ est réellement
        interdite (adapter le contre-exemple de la question
        10).
  \item (L'adjoint) Soit $H^*f(x) =
        \int_x^{\infty}\frac{f(t)}t\,\dd t$. Montrer
        $\langle Hf, g\rangle = \langle f, H^*g\rangle$
        pour $f, g$ positives (Tonelli), et prouver
        $\norm{H^*f}_p \leq p\,\norm f_p$ \emph{(directement
        par parties, ou depuis Hardy sur l'exposant
        conjugué par dualité --- attention à quel exposant
        ramasse quelle constante)}.
  \item (Une inégalité de type Hilbert) En déduire que pour
        $f \in L^p$, $g \in L^q$ positives~:
        \[
        \int_0^\infty\!\!\int_0^\infty
        \frac{f(x)\,g(y)}{\max(x,y)}\,\dd x\,\dd y
        \;\leq\; (p + q)\,\norm f_p\,\norm g_q
        \]
        \emph{(scinder selon $y < x$ / $y \geq x$~: chaque
        moitié est un accouplement d'une fonction contre
        une transformée de Hardy de l'autre)}.
  \item (Optimalité, discret) Montrer que la constante
        $\bigl(\frac p{p-1}\bigr)^p$ de la question 8 est
        aussi optimale~: tester sur $a_n = n^{-1/p}\,\mathbf
        1_{n \leq N}$, comparer les deux membres avec des
        intégrales, et faire $N \to \infty$ (le miroir
        discret de la partie II).
  \item (Synthèse) Trois opérateurs de moyenne sont
        apparus dans ce problème~: le $H$ de Hardy, la
        moyenne de Cesàro discrète, et l'opérateur \omterm{def:b3:rings:primemaximal}{maximal}
        $M$. Énoncer en une ligne chacun ce que sa
        continuité dit, observer que les trois échouent
        exactement en $p = 1$, et expliquer pourquoi c'est
        le même échec trois fois (la queue harmonique
        $\frac1x$).
\end{enumerate}

\medskip
\noindent\textbf{Partie VII --- L'inégalité de Carleman, et
à quel point «~optimal~» est-il.}
\begin{enumerate}[resume]
  \item (Inégalité de Carleman) Soient $a_n \geq 0$ avec
        $\sum a_n < \infty$. Appliquer l'inégalité de Hardy
        discrète de la question 8 à $b_n = a_n^{1/p}$,
        utiliser l'inégalité arithmético-géométrique, et
        faire $p \to \infty$ (montrer que $p \mapsto
        \bigl(\frac p{p-1}\bigr)^p$ décroît vers $\eu$) pour
        obtenir
        \[
        \sum_{n\geq1}\bigl(a_1a_2\cdots a_n\bigr)^{1/n}
        \;\leq\; \eu\,\sum_{n\geq1}a_n :
        \]
        les moyennes géométriques d'une suite sommable sont
        sommables, à coût au plus $\eu$.
  \item (La constante $\eu$ est optimale) Tester $a_n =
        \frac1n\,\mathbf 1_{n\leq N}$~: en utilisant
        l'encadrement de Stirling
        du~\cref{pb:b3:product:1}, montrer
        $(n!)^{-1/n} = \frac\eu n\bigl(1 +
        O\bigl(\frac{\ln n}n\bigr)\bigr)$, en déduire que
        les deux membres de Carleman croissent comme
        $\eu\ln N$, et conclure qu'aucune constante plus
        petite que $\eu$ ne peut convenir. (Observer le
        schéma~: les optimiseurs de Hardy et de Carleman
        sont tous deux les suites de type harmonique qui
        échouent juste à être dans l'espace.)
  \item (À quelle vitesse «~optimal~» est-il approché~?)
        Prendre $p = 2$. Pour $f = \mathbf 1_{\intcc01}$,
        calculer $\norm{Hf}_2/\norm f_2 = \sqrt2$, contre
        la borne $2$. Pour les quasi-optimiseurs $f_A$ de
        la question 5, prouver l'identité exacte
        \[
        \norm{Hf_A}_2^2 = 4\ln A - 8 + \frac{8}{\sqrt A},
        \qquad\text{donc}\qquad
        \frac{\norm{Hf_A}_2^2}{\norm{f_A}_2^2}
        = 4 - \frac{8 - 8A^{-1/2}}{\ln A} .
        \]
        Évaluer en $A = \eu^{10}$ (rapport $\approx 1{,}790$)
        et commenter~: le supremum $2$ n'est approché qu'à
        la vitesse $1/\ln A$ --- une constante optimale
        peut être presque invisible numériquement.
\end{enumerate}
\end{problem}
