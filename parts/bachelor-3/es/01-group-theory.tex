\chapter{Teoría de grupos}\label{ch:b3:groups}

El volumen del Año 2 encontró a los grupos como dispositivos de contabilidad: Lagrange
teorema, grupos cíclicos, el grupo simétrico y su firma. esto
El capítulo convierte la teoría de grupos en un \emph{método}. El motor es el
noción de un grupo \emph{interino} en un conjunto: contando \omterm{def:b3:groups:action}{órbitas} y fijo
puntos produce el \omterm{cor:b3:groups:classeq}{ecuación de clase}, el teorema de Cauchy y los tres
Teoremas de Sylow: el principio fundamental de lo local a lo global de
Teoría de grupos finitos. Luego aprendemos a armar grupos (directos y
\omterm{def:b3:groups:semidirect}{productos semidirectos}) y desmontarlos (\omterm{thm:b3:groups:jordanholder}{serie de composición},
\omterm{def:b3:groups:derived}{grupos solucionables}), y demostramos el teorema de que, en
\cref{ch:b3:galois}, cerrará una pregunta de tres siglos sobre
ecuaciones polinómicas: \omterm{thm:b3:groups:ansimple}{grupo alterno} $A_n$ es \omterm{def:b3:groups:simple}{simple} para $n
\geq 5$.

\section{Grupos de cocientes y teoremas de isomorfismo}

En todo momento, $G$ es un grupo escrito multiplicativamente, $e$ es
identidad. Recordemos del volumen del año 2: subgrupos, clases laterales $gH$,
Teorema de Lagrange ($\abs G = [G:H]\,\abs H$ para $G$ finito), el
Orden de un elemento, grupos cíclicos y grupo simétrico $S_n$.
con su morfismo característico $\varepsilon \colon S_n \to \{\pm1\}$.

\begin{definition}\label{def:b3:groups:normal}
Un subgrupo $N$ de $G$ es \emph{normal}\index{subgrupo normal}
(escrito $N \trianglelefteq G$) cuando $gNg^{-1} = N$ para cada $g
\in G$--- de manera equivalente, cuando las clases laterales izquierda y derecha coinciden:$gN =
Ng$para todos$g$.
\end{definition}

\begin{theorem}[Grupo consciente]\label{thm:b3:groups:quotient}
Deje $N \trianglelefteq G$. El conjunto $G/N$ de clases laterales, con el
multiplicación $(gN)(hN) = ghN$, es un grupo bien definido, el
\emph{grupo cociente}\index{grupo cociente}, y el
\emph{proyección canónica} $\pi \colon G \to G/N$, $g \mapsto gN$,
es un morfismo sobreyectivo con núcleo $N$. Por el contrario, cada núcleo
de un morfismo de grupo es \omterm{def:b3:groups:normal}{normal}: \omterm{def:b3:groups:normal}{subgrupos normales} son exactamente los
granos.
\end{theorem}

\begin{proof}
La buena definición es el objetivo. Si $gN = g'N$ y $hN = h'N$,
escriba $g' = gn$, $h' = hm$ con $n, m \in N$. Entonces $g'h' = gnhm =
gh\,(h^{-1}nh)\,m \in ghN$desde$h^{-1}nh \in N$ por normalidad: el
El producto de las clases laterales no depende de los representantes.
Asociatividad, identidad $eN = N$ e inversas $(gN)^{-1} = g^{-1}N$
se heredan de $G$. Claramente $\pi$ es un morfismo sobreyectivo y
$\pi(g) = N \iff g \in N$.

Si $f \colon G \to H$ es un morfismo y $k \in \ker f$, entonces
$f(gkg^{-1}) = f(g)f(k)f(g)^{-1} = e$: los núcleos son \omterm{def:b3:groups:normal}{normal}.
\end{proof}

\begin{theorem}[Propiedad universal; primer teorema del isomorfismo]
\label{thm:b3:groups:firstiso}
Sea $f \colon G \to H$ un morfismo y $N \trianglelefteq G$ con
$N \subseteq \ker f$. Existe un morfismo único $\bar f \colon G/N
\to H$con$f = \bar f \circ \pi$. En particular, tomando$N = \ker
f$:\index{teoremas de isomorfismo}
\[
G/\ker f \;\xrightarrow{\;\sim\;}\; \operatorname{im} f,
\qquad gN \mapsto f(g).
\]
\end{theorem}

\begin{proof}
Unicidad: $\bar f(gN)$ debe ser $f(g)$. Existencia: si $gN = g'N$
luego $g^{-1}g' \in N \subseteq \ker f$, por lo que $f(g) = f(g')$ y $\bar
f(gN) = f(g)$están bien definidos; es un morfismo porque$f$ lo es. Para
$N = \ker f$: $\bar f$ es inyectivo, ya que $\bar f(gN) = e$ significa
$g \in \ker f$, es decir\ $gN = N$; su imagen es la de $f$.
\end{proof}

\begin{theorem}[Teoremas de isomorfismo segundo y tercero]
\label{thm:b3:groups:secondiso}
Sean $H \leq G$ y $N \trianglelefteq G$.
\begin{enumerate}
  \item $HN = \{hn : h \in H,\, n \in N\}$ es un subgrupo, $N
        \trianglelefteq HN$,$H \cap N \trianglelefteq H$ y
        \[
        H/(H \cap N) \;\cong\; HN/N .
        \]
  \item Si además $N \subseteq K \trianglelefteq G$, entonces $K/N
        \trianglelefteq G/N$y$(G/N)\big/(K/N) \cong G/K$.
\end{enumerate}
\end{theorem}

\begin{proof}
(1) $HN$ es un subgrupo: $(hn)(h'n') = hh'\,(h'^{-1}nh')n' \in HN$
y $(hn)^{-1} = h^{-1}(hn^{-1}h^{-1}) \in HN$, usando la normalidad de
$N$. Redactar $H \hookrightarrow HN \xrightarrow{\pi} HN/N$: esto
el morfismo es sobreyectivo ($hnN = hN$) con núcleo $\{h \in H : h \in
N\} = H \cap N$; aplicar \cref{thm:b3:groups:firstiso}.

(2) La proyección $G/N \to G/K$, $gN \mapsto gK$, está bien definida.
($N \subseteq K$), sobreyectivo, con kernel $K/N$; aplicar
\cref{thm:b3:groups:firstiso} nuevamente.
\end{proof}

\begin{theorem}[Teorema de correspondencia]\label{thm:b3:groups:correspondence}
Deje $N \trianglelefteq G$. La aplicación $H \mapsto H/N$ es una biyección.
entre subgrupos de $G$ que contienen $N$ y subgrupos de $G/N$,
preservando inclusiones, índices y normalidad (en ambas direcciones).
\end{theorem}

\begin{proof}
Su inverso es $\bar H \mapsto \pi^{-1}(\bar H)$. Ambos mapas envían
subgrupos a subgrupos, y son mutuamente inversos: $\pi^{-1}(H/N) =
HN = H$desde$N \subseteq H$y$\pi(\pi^{-1}(\bar H)) = \bar H$
por sobreyectividad de $\pi$. Las inclusiones se conservan claramente;
$[G:H] = [G/N : H/N]$ porque $gH \mapsto (gN)(H/N)$ es un
biyección bien definida entre espacios laterales; y $gHg^{-1} = H$ para
todos $g$ iff $(gN)(H/N)(gN)^{-1} = H/N$ para todos $gN$, nuevamente por
sobreyectividad de $\pi$.
\end{proof}

\begin{example}\label{ex:b3:groups:firstiso}
$\varepsilon \colon S_n \to \{\pm 1\}$ da $S_n/A_n \cong
\{\pm1\}$;$\det \colon GL_n(K) \to K^\times$ da
$GL_n(K)/SL_n(K) \cong K^\times$; $t \mapsto \eu^{2\iu\pi t}$ da
$\R/\Z \cong \mathbb U$, el grupo circular. El primer isomorfismo
El teorema es cómo los cocientes son \emph{calculado} en la práctica: encuentre un
sobreyección con el núcleo correcto.
\end{example}

\begin{method}\label{met:b3:groups:normal}
Para probar $N \trianglelefteq G$, en orden decreciente de elegancia:
exhibe $N$ como el núcleo de un morfismo definido en $G$; comprobar
$gNg^{-1} \subseteq N$ para todos los $g$ (basta con aplicarlo a
$g^{-1}$ y la conjugación da la inclusión inversa); verificar que
$N$ es una unión de \omterm{ex:b3:groups:actions}{clases de conjugación}; o tenga en cuenta que $[G:N] = 2$
(entonces se fuerza $gN = Ng$ --- \cref{exo:b3:groups:1}).
\end{method}

\section{Acciones grupales}

\begin{definition}\label{def:b3:groups:action}
Un \emph{acción}\index{acción grupal} de $G$ en un conjunto $X$ es un
morfismo $\varphi \colon G \to \mathfrak{S}(X)$ en el grupo de
biyecciones de $X$; uno escribe $g \cdot x$ para $\varphi(g)(x)$.
Equivalentemente: una aplicación $G \times X \to X$ con $e \cdot x = x$ y $g
\cdot (h \cdot x) = (gh) \cdot x$. El \emph{órbita}\index{órbita} de
$x$ es $\mathcal O_x = \{g \cdot x : g \in G\}$, es
\emph{estabilizador}\index{estabilizador} es el subgrupo $G_x = \{g :
g\cdot x = x\}$y$X^G = \{x : \forall g,\ g \cdot x = x\}$ es
el conjunto de \emph{puntos fijos}. La acción es \emph{transitivo} si
hay exactamente una órbita, \emph{fiel} si $\varphi$ es
inyectivo, \emph{libre} si todos los estabilizadores son triviales.
\end{definition}

\begin{example}\label{ex:b3:groups:actions}
Cinco \omterm{def:b3:groups:action}{comportamiento} ejecutan toda la teoría de grupos finitos:
\begin{enumerate}
  \item $G$ sobre sí mismo por \emph{traducción izquierda} $g \cdot x = gx$:
        libre y transitivo.
  \item $G$ sobre sí mismo por \emph{conjugación} $g \cdot x = gxg^{-1}$:
        \omterm{def:b3:groups:action}{órbitas} son el \emph{clases de conjugación}\index{conjugación
        clase}, \omterm{def:b3:groups:action}{estabilizadores} el \emph{centralizadores}
        \index{centralizador} $Z_G(x) = \{g : gx = xg\}$, fijo
        señala el \emph{centro}\index{centro} $Z(G)$.
  \item $G$ en el espacio de la clase lateral $G/H$ por $g \cdot xH = gxH$:
        transitivo, con \omterm{def:b3:groups:action}{estabilizador} de la clase lateral $H$ igual a $H$.
        Cada transitiva \omterm{def:b3:groups:action}{acción} tiene esta forma.
        (\cref{exo:b3:groups:8}).
  \item $G$ en su conjunto de subgrupos por conjugación: el \omterm{def:b3:groups:action}{estabilizador}
        de $H$ es el \emph{normalizador}\index{normalizador} $N_G(H) =
        \{g : gHg^{-1} = H\}$, el subgrupo más grande de$G$ en el que
        $H$ es \omterm{def:b3:groups:normal}{normal}.
  \item $S_n$ en $\intint{1}{n}$: la madre de todos los ejemplos.
\end{enumerate}
\end{example}

\begin{theorem}[Órbita--estabilizador]\label{thm:b3:groups:orbitstab}
La aplicación $gG_x \mapsto g \cdot x$ es una biyección bien definida.
$G/G_x \to \mathcal O_x$. En particular, para $G$
finito, \index{teorema del estabilizador de la órbita}
\[
\abs{\mathcal O_x} = [G : G_x] \quad\text{divides } \abs G,
\]
y, el \omterm{def:b3:groups:action}{órbitas} particionando $X$ (son las clases del
equivalencia $x \sim y \iff y \in \mathcal O_x$),
\[
\abs X = \sum_{i} \,[G : G_{x_i}]
\qquad (x_i\colon \text{one point per orbit}).
\]
\end{theorem}

\begin{proof}
Bien definido e inyectivo: $gG_x = hG_x \iff h^{-1}g \in G_x \iff
h^{-1}g \cdot x = x \iff g \cdot x = h \cdot x$; lee la cadena en
ambas direcciones. La sobreyectividad es la definición del \omterm{def:b3:groups:action}{órbita}. el
Las declaraciones de conteo se derivan del teorema de Lagrange y del
partición de $X$ en \omterm{def:b3:groups:action}{órbitas}.
\end{proof}

\begin{corollary}[Ecuación de clase]\label{cor:b3:groups:classeq}
Para un grupo finito $G$, elegir un representante $x_i$ en cada
\omterm{ex:b3:groups:actions}{clase de conjugación} con más de un elemento:\index{ecuación de clase}
\[
\abs G = \abs{Z(G)} + \sum_i \,[G : Z_G(x_i)],
\qquad\text{each } [G : Z_G(x_i)] > 1 \text{ dividing } \abs G.
\]
\end{corollary}

\begin{proof}
Aplicar \cref{thm:b3:groups:orbitstab} a la conjugación \omterm{def:b3:groups:action}{acción}:
singleton \omterm{def:b3:groups:action}{órbitas} son exactamente los elementos de $Z(G)$.
\end{proof}

\begin{theorem}[Puntos fijos de $p$-grupos]\label{thm:b3:groups:pfixed}
Sea $p$ primo. Un \emph{$p$-grupo}\index{grupo p@$p$-grupo} es un
grupo finito cuyo orden es una potencia de $p$. Si un grupo $p$ $G$ actúa
en un conjunto finito $X$, entonces
\[
\abs{X^G} \equiv \abs X \pmod p .
\]
Consecuencias: un grupo $p$ no trivial tiene \omterm{ex:b3:groups:actions}{centro} no trivial, y
todo grupo de orden $p^2$ es abeliano.
\end{theorem}

\begin{proof}
Cada \omterm{def:b3:groups:action}{órbita} tiene cardinalidad $[G:G_x]$, una potencia de $p$; este poder es
$1$ exactamente en puntos fijos y de otro modo divisible por $p$. sumando
sobre \omterm{def:b3:groups:action}{órbitas} da la congruencia. Para el \omterm{ex:b3:groups:actions}{centro}: conjugación
\omterm{def:b3:groups:action}{acción} de $G$ en sí mismo tiene $X^G = Z(G)$, por lo que $\abs{Z(G)} \equiv
\abs G \equiv 0 \pmod p$y$Z(G) \ni e$fuerzan a$\abs{Z(G)} \geq
p$. Orden$p^2$: si$Z(G) \neq G$entonces$\abs{Z(G)} = p$ y
$G/Z(G)$ es cíclico de orden $p$, lo que obliga a $G$ abeliano
(\cref{exo:b3:groups:2}) --- contradicción.
\end{proof}

\begin{theorem}[cauchy]\label{thm:b3:groups:cauchy}
Si un primo $p$ divide a $\abs G$, entonces $G$ contiene un elemento de
orden $p$.\index{teorema de cauchy}
\end{theorem}

\begin{proof}[Prueba (McKay)]
Deje $X = \{(g_1, \dots, g_p) \in G^p : g_1 g_2 \cdots g_p = e\}$.
La elección de $g_1, \dots, g_{p-1}$ determina libremente $g_p$: $\abs X =
\abs G^{p-1}$, divisible por$p$. El grupo cíclico$\Z/p\Z$ actúa sobre
$X$ por desplazamiento cíclico $(g_1, \dots, g_p) \mapsto (g_2, \dots, g_p,
g_1)$--- esto preserva$X$, desde$g_2 \cdots g_p g_1 =
g_1^{-1}(g_1 \cdots g_p)g_1 = e$. Por \cref{thm:b3:groups:pfixed},
$\abs{X^{\Z/p\Z}} \equiv \abs X \equiv 0 \pmod p$. Los puntos fijos son
las tuplas constantes $(g, \dots, g)$ con $g^p = e$; la tupla $(e,
\dots, e)$es una de ellas, por lo que hay al menos$p$ de ellas, por lo tanto
al menos un $g \neq e$ con $g^p = e$: su orden es exactamente $p$.
\end{proof}

\begin{theorem}[Cayley]\label{thm:b3:groups:cayley}
Cada grupo de orden $n$ se incrusta en $S_n$.\index{teorema de cayley}
\end{theorem}

\begin{proof}
La traducción izquierda $\varphi \colon G \to \mathfrak S(G) \cong S_n$ es
un morfismo; $\varphi(g) = \mathrm{id}$ fuerza a $g = ge = e$: es
fiel.
\end{proof}

\begin{method}\label{met:b3:groups:counting}
El conteo de punto fijo es el movimiento de apertura universal de un grupo finito
teoría. Para demostrar que algo \emph{existe} (un elemento central,
un elemento de orden $p$, un \omterm{def:b3:groups:normal}{subgrupo normal}, un punto fijo), haga un
grupo bien elegido actúa en un conjunto finito bien elegido, luego compara
$\abs{X^G}$ con $\abs X$ módulo $p$, o deje que los tamaños \omterm{def:b3:groups:action}{órbita} dividan el
orden del grupo. las pruebas de
\cref{thm:b3:groups:pfixed,thm:b3:groups:cauchy} y de los tres
Los teoremas de Sylow a continuación son cinco variaciones de esta única idea.
\end{method}

\section{Los teoremas de Sylow}

El teorema de Lagrange dice que el orden de un subgrupo divide a $\abs G$;
lo contrario falla ($A_4$, de orden $12$, no tiene subgrupo de orden
$6$ --- \cref{exo:b3:groups:1}). Los teoremas de Sylow salvan el
lo contrario para las potencias primarias, y su cláusula de conteo es la
herramienta general más afilada que tenemos para producir \omterm{def:b3:groups:normal}{subgrupos normales}.

\begin{definition}\label{def:b3:groups:sylow}
Escriba $\abs G = p^a m$ con $p \nmid m$. Un \emph{silo
$p$-subgrupo}\index{Subgrupo Sylow} de $G$ es un subgrupo de orden
$p^a$ --- un subgrupo $p$ del orden más grande imaginable. el
El número de subgrupos Sylow $p$ de $G$ se indica como $n_p$.
\end{definition}

\begin{lemma}\label{lem:b3:groups:binom}
Si $\abs G = p^a m$ con $p \nmid m$, entonces $\dbinom{p^a m}{p^a}
\equiv m \pmod p$.
\end{lemma}

\begin{proof}
En $\mathbb F_p[X]$, el sueño del estudiante de primer año $(1+X)^p = 1 + X^p$ (el
los coeficientes $\binom pk$, $0<k<p$, son divisibles por $p$: $p$
divide el numerador de $\frac{p!}{k!(p-k)!}$ pero no el
denominador) itera hasta $(1+X)^{p^a} = 1 + X^{p^a}$, de donde
\[
(1+X)^{p^a m} = \bigl(1 + X^{p^a}\bigr)^m
= \sum_{k=0}^{m} \binom mk X^{k p^a} \quad\text{in } \mathbb
F_p[X].
\]
Identifique el coeficiente de $X^{p^a}$: a la izquierda $\binom{p^a
m}{p^a} \bmod p$, a la derecha$\binom m1 = m$.
\end{proof}

\begin{theorem}[Sylow I: existencia]\label{thm:b3:groups:sylow1}
Para cada número primo $p$, existen subgrupos Sylow $p$ de $G$.
\end{theorem}

\begin{proof}[Prueba (Wielandt)]
Sea $\Omega$ el conjunto de \emph{subconjuntos} de $G$ de cardinalidad
$p^a$; $G$ actúa sobre $\Omega$ mediante la traducción izquierda $g \cdot S = gS$.
Por \cref{lem:b3:groups:binom}, $\abs\Omega = \binom{p^a m}{p^a}
\equiv m \not\equiv 0 \pmod p$, por lo que algunos \omterm{def:b3:groups:action}{órbita}$\mathcal O_S$ tienen
tamaño primo a $p$ (si $p$ dividiera cada tamaño \omterm{def:b3:groups:action}{órbita}, se dividiría
$\abs\Omega$). Sea $H = G_S$ el \omterm{def:b3:groups:action}{estabilizador} de dicho $S$.
Dado que $[G : H] = \abs{\mathcal O_S}$ es primo de $p$ y $p^a \mid
\abs G = [G:H]\,\abs H$, obtenemos$p^a \mid \abs H$. Por el contrario, arreglar
$s \in S$: la aplicación $H \to S$, $h \mapsto hs$, es inyectivo y
aterriza en $S$ porque $hS = S$; por lo tanto $\abs H \leq \abs S = p^a$.
Entonces $\abs H = p^a$.
\end{proof}

\begin{theorem}[Sylow II: dominación y conjugación]\label{thm:b3:groups:sylow2}
Sea $P$ un subgrupo $p$ de Sylow y $Q$ cualquier subgrupo $p$ de $G$.
Luego $Q \subseteq gPg^{-1}$ para algunos $g \in G$. en particular todos
Los subgrupos Sylow $p$ son conjugados y $P \trianglelefteq G \iff
n_p = 1$.
\end{theorem}

\begin{proof}
Sea $Q$ actuar sobre el espacio lateral $X = G/P$, de cardinalidad $m
\not\equiv 0 \pmod p$. Por \cref{thm:b3:groups:pfixed} aplicado a
el grupo $p$ $Q$, $\abs{X^Q} \equiv m \not\equiv 0 \pmod p$: allí
es una clase lateral fija $gP$, es decir,\ $QgP = gP$, es decir, $g^{-1}Qg \subseteq
P$. Si$Q$ es en sí mismo un \omterm{def:b3:groups:sylow}{Subgrupo Sylow}, la igualdad de órdenes se vuelve
$Q \subseteq gPg^{-1}$ en una igualdad. Finalmente $P
\trianglelefteq G$si sus conjugados$\{gPg^{-1}\}$ --- que por
los anteriores son \emph{todo} los subgrupos Sylow $p$ --- reducir a
$\{P\}$.
\end{proof}

\begin{theorem}[Sylow III: contando]\label{thm:b3:groups:sylow3}
$n_p \equiv 1 \pmod p$ y $n_p = [G : N_G(P)]$, que divide
$m$.
\end{theorem}

\begin{proof}
Sea $\mathrm{Syl}_p$ el conjunto de subgrupos Sylow $p$; $G$ actúa sobre
es transitivamente por conjugación (\cref{thm:b3:groups:sylow2}), con
\omterm{def:b3:groups:action}{estabilizador} de $P$ el \omterm{ex:b3:groups:actions}{normalizador} $N_G(P) \supseteq P$: $n_p = [G :
N_G(P)]$, y$m = [G:P] = [G:N_G(P)]\,[N_G(P):P]$muestra$n_p \mid
m$.

Ahora restrinja \omterm{def:b3:groups:action}{acción} a $P$ y cuente los puntos fijos. Si $Q \in
\mathrm{Syl}_p$está arreglado por$P$, entonces$P \subseteq N_G(Q)$; ambos
$P$ y $Q$ son subgrupos Sylow $p$ del grupo $N_G(Q)$, por lo tanto
conjugado en él (\cref{thm:b3:groups:sylow2} aplicado a $N_G(Q)$);
pero $Q \trianglelefteq N_G(Q)$, entonces $Q$ es su único conjugado allí:
$P = Q$. Por lo tanto, el único punto fijo es el propio $P$, y
\cref{thm:b3:groups:pfixed} da $n_p = \abs{\mathrm{Syl}_p}
\equiv \abs{\mathrm{Syl}_p^P} = 1 \pmod p$.
\end{proof}

\begin{method}\label{met:b3:groups:sylowmethod}
Para analizar un grupo de orden dado $n = p^a m$: enumerar los divisores
de $m$ congruente con $1$ mod $p$ --- estos son los candidatos para
$n_p$. Si el único candidato es $1$, el subgrupo Sylow $p$ es
\omterm{def:b3:groups:normal}{normal}. Si $n_p > 1$ se ve obligado a ser pequeño, actúe por conjugación en
$\mathrm{Syl}_p$ para obtener un morfismo $G \to S_{n_p}$ con pequeño
núcleo. Y contar elementos: distintos subgrupos Sylow $p$ de
\emph{principal} orden $p$ se cruzan trivialmente, por lo que llevan
$n_p(p-1)$ elementos de orden exactamente $p$; recuentos superpuestos para
diferentes primos a menudo fuerzan una contradicción
(\cref{exo:b3:groups:7}).
\end{method}

\begin{example}\label{ex:b3:groups:pq}
Sea $\abs G = pq$ con números primos $p < q$ y $p \nmid q - 1$. entonces
$n_q \mid p$ y $n_q \equiv 1 \bmod q$ fuerzan $n_q = 1$ (como $p <
q$);$n_p \mid q$y$n_p \equiv 1 \bmod p$fuerzan$n_p = 1$(como$q
\not\equiv 1 \bmod p$). Sean$P, Q$los dos Sylows \omterm{def:b3:groups:normal}{normal}:$P
\cap Q = \{e\}$(órdenes coprime), entonces$\abs{PQ} = pq$
(\cref{exo:b3:groups:4}) y $G \cong P \times Q \cong \Z/p\Z
\times \Z/q\Z \cong \Z/pq\Z$ por
\cref{prop:b3:groups:direct} a continuación. Cada grupo de orden $15$,
$33$, $35$, \dots\ es cíclico. El caso excluido $p \mid q - 1$
produce exactamente un grupo más, nonabelian --- ver el fin de semana
problema (\cref{pb:b3:groups:1}).
\end{example}

\begin{example}[Un censo completo de Sylow: $S_4$]
\label{ex:b3:groups:s4sylow}
Ejecutemos el método en $G = S_4$, $\abs G = 24 = 2^3\cdot3$.
\emph{Solo $3$:} $n_3 \mid 8$, $n_3 \equiv 1 \bmod 3$, entonces $n_3
\in \{1, 4\}$; desde$\langle(123)\rangle$ y
$\langle(124)\rangle$ son distintos, $n_3 = 4$ --- los cuatro
subgrupos $\langle(abc)\rangle$, uno para cada subconjunto de elementos $3$
$\{a, b, c\}$, que representa los tres ciclos $8$. Por Sylow~II
son conjugados, y el morfismo de conjugación $S_4 \to
S_{\mathrm{Syl}_3} \cong S_4$ es un isomorfismo aquí (su núcleo
está contenido en $N = N_G(\langle(123)\rangle)$ del orden $24/4 =
6$, y un \omterm{def:b3:groups:normal}{subgrupo normal} de$S_4$dentro de$S_3$similar a$N$ debe ser
trivial: consistiría en permutaciones pares que fijasen los cuatro
Sylows, y sólo $e$ lo hace). \emph{Solo $2$:} $n_2 \mid 3$, $n_2
\equiv 1 \bmod 2$:$n_2 \in \{1, 3\}$. El subgrupo$D =
\langle(1234), (13)\rangle$tiene orden$8$(un diédrico$D_4$:
las simetrías del cuadrado con vértices $1, 2, 3, 4$), no es
\omterm{def:b3:groups:normal}{normal} ($(12)(1234)(12) = (2134)$ genera un mensaje diferente
$4$ subgrupo de ciclos), por lo que $n_2 = 3$: las tres copias de $D_4$
corresponden a las tres formas de emparejar puntos $4$ en un
``cuadrado''. Note la moraleja del censo: $\abs{S_4} = 24$
deja espacio para que Sylow falle en la normalidad, y ambos lo hacen ---
comparar orden $12$, donde el conteo obliga a uno de ellos \omterm{def:b3:groups:normal}{normal}
(Parte ~IV de \cref{pb:b3:groups:1}).
\end{example}

\section{Productos directos y semidirectos.}

\begin{proposition}[Reconociendo un producto directo]\label{prop:b3:groups:direct}
Deje $H, K \trianglelefteq G$ con $H \cap K = \{e\}$ y $HK = G$.
Entonces $(h,k) \mapsto hk$ es un isomorfismo $H \times K \to
G$.\index{producto directo}
\end{proposition}

\begin{proof}
Para $h \in H$, $k \in K$, \omterm{def:b3:groups:derived}{conmutador} $hkh^{-1}k^{-1}$ se encuentra en
$K$ (léalo como $(hkh^{-1})k^{-1}$, usando la normalidad de $K$) y en
$H$ (léelo como $h(kh^{-1}k^{-1})$): es $e$, entonces $H$ y $K$
conmutar elemento por elemento y la aplicación es un morfismo. es sobreyectivo
desde $HK = G$, y el inyectivo desde $hk = e$ da $h = k^{-1} \in
H \cap K = \{e\}$.
\end{proof}

La normalidad de los factores \emph{ambos} es lo que falla con mayor frecuencia: en $S_3
= \langle (1\,2\,3)\rangle \,\langle(1\,2)\rangle$ ambos factores
se cruzan trivialmente y generan, aún $S_3 \not\cong \Z/3\Z \times
\Z/2\Z$. La noción correcta cuando solo un factor es \omterm{def:b3:groups:normal}{normal}:

\begin{definition}\label{def:b3:groups:semidirect}
Sean $H$, $K$ grupos y $\varphi \colon K \to
\operatorname{Aut}(H)$ un morfismo. El \emph{semidirecto
producto}\index{producto semidirecto} $H \rtimes_\varphi K$ es el
conjunto $H \times K$ equipado con
\[
(h, k)\,(h', k') = \bigl(h\,\varphi(k)(h'),\; kk'\bigr).
\]
\end{definition}

\begin{proposition}\label{prop:b3:groups:semidirect}
$H \rtimes_\varphi K$ es un grupo; $H \times \{e\}$ es un \omterm{def:b3:groups:normal}{normales
subgrupo} isomorfo a $H$, $\{e\} \times K$ es un subgrupo isomorfo
a $K$; se cruzan trivialmente y generan. Por el contrario, si $G =
NK$con$N \trianglelefteq G$,$K \leq G$y$N \cap K = \{e\}$,
luego $G \cong N \rtimes_\varphi K$ para $\varphi(k) = (n \mapsto
knk^{-1})$.
\end{proposition}

\begin{proof}
Verificación directa: la asociatividad se reduce a $\varphi(kk') =
\varphi(k)\circ\varphi(k')$y cada$\varphi(k)$ es un morfismo;
la identidad es $(e,e)$ y $(h,k)^{-1} =
\bigl(\varphi(k^{-1})(h^{-1}), k^{-1}\bigr)$. La proyección$(h,k)
\mapsto k$es un morfismo en$K$con el núcleo$H \times \{e\}$,
que por tanto es \omterm{def:b3:groups:normal}{normal}. Por el contrario: cada $g \in G$ escribe
\emph{únicamente} como $nk$ con $n \in N$, $k \in K$ (existencia: $G =
NK$; unicidad:$nk = n'k'$da$n'^{-1}n = k'k^{-1} \in N \cap
K$), y
\[
(nk)(n'k') = n\,(kn'k^{-1})\;kk'
\]
muestra que $nk \mapsto (n, k)$ transporta la ley de $G$ a ese
de $N \rtimes_\varphi K$.
\end{proof}

\begin{example}\label{ex:b3:groups:semidirectexamples}
(a) Las simetrías \emph{grupo diédrico}\index{grupo diédrico} $D_n$ ($n \geq
3$) de las$2n$de un gon$n$ regular: las rotaciones forman
un \omterm{def:b3:groups:normal}{subgrupo normal} de índice $2$, cualquier reflexión genera un
complemento, y conjugar una rotación por una reflexión la invierte:
$D_n \cong \Z/n\Z \rtimes_\varphi \Z/2\Z$ con $\varphi(1) = (x
\mapsto -x)$.
(b) El grupo afín de una línea, $\{x \mapsto ax + b : a \in
K^\times,\, b \in K\} \cong K \rtimes K^\times$: traducciones
\omterm{def:b3:groups:normal}{normal}, homotecias un complemento.
(c) $S_n \cong A_n \rtimes \Z/2\Z$ (complemento: cualquiera
transposición).
(d) El \omterm{pb:b3:groups:1}{grupo cuaternión} $Q_8$ es \emph{no} a \omterm{def:b3:groups:semidirect}{producto semidirecto}
de subgrupos adecuados: cada subgrupo no trivial contiene $-1$
(\cref{pb:b3:groups:1}), por lo que no se cruzan dos subgrupos adecuados
trivialmente.
\end{example}

\section{Grupos solubles; simplicidad de \texorpdfstring{$A_n$}{An}}

\begin{definition}\label{def:b3:groups:derived}
El \emph{conmutador}\index{conmutador} de $x, y \in G$ es $[x,y]
= xyx^{-1}y^{-1}$; el \emph{subgrupo derivado}\index{derivado
subgrupo} $D(G)$ es el subgrupo generado por todos los conmutadores. el
\emph{serie derivada} es $D^0(G) = G$, $D^{i+1}(G) = D(D^i(G))$,
y $G$ es \emph{soluble}\index{grupo solucionable} si $D^n(G) =
\{e\}$para algunos$n$.
\end{definition}

\begin{proposition}\label{prop:b3:groups:derived}
$D(G)$ es \omterm{def:b3:groups:normal}{normal} (de hecho, estable bajo cada automorfismo), $G/D(G)$
es abeliano, y para $N \trianglelefteq G$: $G/N$ abeliano $\iff
D(G) \subseteq N$. Además$G$ es \omterm{def:b3:groups:derived}{soluble} si hay una cadena.
$G = G_0 \trianglerighteq G_1 \trianglerighteq \dots
\trianglerighteq G_n = \{e\}$con cada$G_{i+1} \trianglelefteq
G_i$y cada cociente$G_i/G_{i+1}$ abeliano. Subgrupos y
los cocientes de \omterm{def:b3:groups:derived}{grupos solucionables} son \omterm{def:b3:groups:derived}{soluble}; por el contrario, si $N$ y
$G/N$ son \omterm{def:b3:groups:derived}{soluble}, también lo es $G$.
\end{proposition}

\begin{proof}
Un automorfismo $\alpha$ asigna $[x,y]$ a $[\alpha x, \alpha y]$:
permuta el \omterm{def:b3:groups:derived}{conmutadores}, por lo que conserva el subgrupo que generan;
las conjugaciones son automorfismos, de ahí la normalidad. En $G/D(G)$,
$\bar x\bar y \bar x^{-1}\bar y^{-1} = \overline{[x,y]} = \bar e$:
el cociente es abeliano. Si $G/N$ es abeliano, entonces cada $[x,y] \in
N$, entonces$D(G) \subseteq N$; por el contrario si$D(G) \subseteq N$ entonces
$G/N$, un cociente del abeliano $G/D(G)$ por el tercer isomorfismo
teorema, es abeliano.

Si $G$ es \omterm{def:b3:groups:derived}{soluble}, \omterm{def:b3:groups:derived}{serie derivada} es dicha cadena. Por el contrario,
dada una cadena, $D^i(G) \subseteq G_i$ por inducción: $G_i/G_{i+1}$
abelian da $D(G_i) \subseteq G_{i+1}$, entonces $D^{i+1}(G) = D(D^i G)
\subseteq D(G_i) \subseteq G_{i+1}$; por lo tanto$D^n(G) = \{e\}$.

Herencia: $D^i(H) \subseteq D^i(G)$ para $H \leq G$ (inducción),
y $D^i(G/N) = \pi(D^i(G))$ ya que $\pi$ asigna \omterm{def:b3:groups:derived}{conmutadores} a
\omterm{def:b3:groups:derived}{conmutadores}; esto proporciona los enunciados para subgrupos y cocientes.
Extensión: si $D^m(G/N) = \{e\}$ entonces $D^m(G) \subseteq N$, y
$D^n(N) = \{e\}$ da $D^{m+n}(G) = D^n(D^m(G)) \subseteq D^n(N)
= \{e\}$.
\end{proof}

\begin{example}\label{ex:b3:groups:solvableexamples}
Los grupos abelianos son \omterm{def:b3:groups:derived}{soluble}. Los grupos $p$ son \omterm{def:b3:groups:derived}{soluble}, por inducción.
en el pedido: $Z(G) \neq \{e\}$ y $G/Z(G)$ es más pequeño
$p$-grupo. $S_3$ y $S_4$ son \omterm{def:b3:groups:derived}{soluble}: $S_4 \trianglerighteq A_4
\trianglerighteq V \trianglerighteq \{e\}$, donde$V = \{e,
(1\,2)(3\,4), (1\,3)(2\,4), (1\,4)(2\,3)\}$ es el grupo Klein de
transposiciones dobles (\omterm{def:b3:groups:normal}{normal} en $S_4$: una unión de \omterm{ex:b3:groups:actions}{conjugación
clases}), con cocientes abelianos $\Z/2\Z$, $\Z/3\Z$, $V$. en
\cref{ch:b3:galois}, ``la ecuación general de grado $n$ es
\omterm{def:b3:groups:derived}{soluble} por radicales'' literalmente \emph{significar} ``$S_n$ es un
\omterm{def:b3:groups:derived}{grupo solucionable}''. De ahí la importancia de la siguiente definición.
\end{example}

\begin{definition}\label{def:b3:groups:simple}
Un grupo $G \neq \{e\}$ es \emph{simple}\index{grupo sencillo} si es
sólo \omterm{def:b3:groups:normal}{subgrupos normales} son $\{e\}$ y $G$. Un simple nobeliano
el grupo no es \omterm{def:b3:groups:derived}{soluble}: $D(G) \trianglelefteq G$ no es $\{e\}$
(de lo contrario, $G$ abeliano), por lo que $D(G) = G$ y \omterm{def:b3:groups:derived}{serie derivada} son
constante. Los grupos simples abelianos son exactamente los $\Z/p\Z$, $p$.
primo (un grupo abeliano es simple si no tiene un grupo no trivial adecuado)
subgrupo, si y solo es cíclico de orden primo según Lagrange).
\end{definition}

\begin{lemma}\label{lem:b3:groups:threecycles}
Para $n \geq 3$, $A_n$ se genera mediante ciclos $3$; para $n \geq 5$,
todos los ciclos $3$ son \emph{in $A_n$} conjugados.
\end{lemma}

\begin{proof}
Un elemento de $A_n$ es producto de un número par de
transposiciones; emparéjalos y úsalos (composición de derecha a izquierda)
\[
(a\,b)(c\,d) = (a\,c\,b)(a\,c\,d), \qquad
(a\,b)(b\,c) = (a\,b\,c), \qquad
(a\,b)(a\,b) = e
\]
para pares disjuntos, superpuestos e iguales respectivamente: cada par
de transposiciones es un producto de ciclos $3$.

Conjugación: $\sigma(a\,b\,c)\sigma^{-1} = (\sigma a\; \sigma b\;
\sigma c)$, por lo que dos ciclos$3$cualesquiera están conjugados por algún$\sigma \in
S_n$. Si$\sigma$es impar, reemplácelo por$\sigma' = \sigma (d\,e)$
donde $d, e$ son dos puntos fuera de $\{a, b, c\}$ --- existen
desde $n \geq 5$; entonces $\sigma'$ es par y $\sigma'(a\,b\,c)
\sigma'^{-1} = \sigma(a\,b\,c)\sigma^{-1}$, ya que$(d\,e)$ conmuta
con $(a\,b\,c)$.
\end{proof}

\begin{theorem}[Simplicidad del grupo alterno]\label{thm:b3:groups:ansimple}
$A_n$ es \omterm{def:b3:groups:simple}{simple} para $n \geq 5$.\index{grupo alterno}
\end{theorem}

\begin{proof}
Sea $N \trianglelefteq A_n$, $N \neq \{e\}$. Por
\cref{lem:b3:groups:threecycles} basta demostrar que $N$
contiene \emph{uno} $3$-ciclo: normalidad y conjugación de
$3$ ciclos en $A_n$ luego coloque todos los ciclos $3$ en $N$, por lo que $N = A_n$.

Para $\rho \in S_n$, deje que $F(\rho) = \{x : \rho(x) \neq x\}$ sea su
\emph{apoyo} y $f(\rho) = \abs{F(\rho)}$. Elija $\sigma \in N
\setminus \{e\}$con$f(\sigma)$ \emph{mínimo}. Tenga en cuenta que un
una permutación par no trivial tiene $f \geq 3$, y que $f(\sigma) =
4$es imposible para$\sigma \in A_n$a menos que$\sigma$ sea un doble
transposición (un ciclo $4$ es impar). Mostramos $\sigma$ es un
$3$-ciclo.

\emph{Case A: $\sigma$ is a product of disjoint transpositions},
diga $\sigma = (a\,b)(c\,d)\cdots$ con $f(\sigma) \geq 4$. Elija $e'
\notin \{a, b, c, d\}$(posible:$n \geq 5$), configure$\tau =
(c\,d\,e')$ y
\[
\sigma' = \tau\sigma\tau^{-1}\,\sigma^{-1} \in N
\qquad (\tau\sigma\tau^{-1} \in N \text{ by normality}).
\]
Desde $\sigma\tau^{-1}\sigma^{-1} = (\sigma c\;\sigma e'\;\sigma
d) = (d\;\sigma e'\;c)$(usando$\sigma c = d$,$\sigma d = c$), nosotros
obtener $\sigma' = (c\,d\,e')(d\;\sigma e'\;c)$.

Si $\sigma e' = e'$ (que se cumple en particular cuando $f(\sigma) =
4$, es decir $\sigma = (a\,b)(c\,d)$): entonces$(d\,e'\,c) =
(c\,d\,e')$y$\sigma' = (c\,d\,e')^2 = (c\,e'\,d)$, a
$3$-ciclo tumbado en $N$, con $f(\sigma') = 3 < 4 \leq f(\sigma)$
--- minimalismo contradictorio.

Si $\sigma e' \neq e'$: entonces $\sigma e' \notin \{a, b, c, d,
e'\}$($\sigma$intercambia$a,b$y$c,d$, y$e' \notin \{a,b,c,d\}$
con inyectivo $\sigma$), por lo que $\sigma$ mueve los seis puntos $a, b,
c, d, e', \sigma e'$:$f(\sigma) \geq 6$. Por otro lado
$\sigma'$, un producto de dos ciclos $3$ con soportes en $\{c, d,
e', \sigma e'\}$, satisface$f(\sigma') \leq 4$; y$\sigma' \neq
e$, desde$\sigma'(d) = \tau\sigma\tau^{-1}(c) = \tau\sigma(e') =
\sigma e' \neq d$($\tau$corrige$\sigma e' \notin \{c,d,e'\}$).
Entonces $\sigma' \in N \setminus\{e\}$ con $f(\sigma') \leq 4 <
f(\sigma)$: la minimalidad se contradice.

\emph{Case B: some cycle of $\sigma$ has length $\geq 3$}, digamos
$\sigma(a) = b$, $\sigma(b) = c$ con $a, b, c$ distintos. si
$\sigma$ es exactamente este ciclo $3$, hemos terminado. De lo contrario
$f(\sigma) \geq 5$ (el caso $f(\sigma) = 4$ con un ciclo $\geq3$
es el ciclo impar $4$, excluido), por lo que podemos elegir $d, e' \in
F(\sigma) \setminus \{a, b, c\}$. Configure$\tau = (c\,d\,e')$ y
$\sigma' = \tau\sigma\tau^{-1}\sigma^{-1} \in N$. Como antes
$\sigma' = (c\,d\,e')\,(\sigma c\;\sigma e'\;\sigma d)$ solo se mueve
puntos de
\[
M = \{c, d, e'\} \cup \{\sigma c, \sigma d, \sigma e'\}
\subseteq F(\sigma)
\]
(las imágenes de puntos movidos se mueven: $\sigma(x) \ne x$ implica
$\sigma(\sigma x) \neq \sigma x$, $\sigma$ son inyectivos). también
$b \notin M$: los cinco puntos $a, b, c, d, e'$ son distintos, por lo que $b
\notin \{c, d, e'\}$; y$b \in \{\sigma c, \sigma d, \sigma
e'\}$forzaría a$a \in \{c, d, e'\}$(aplicar$\sigma^{-1}$, usando
$\sigma a = b$), lo cual es falso. Por lo tanto, $\sigma'$ corrige $b$, mientras que
$\sigma$ mueve $b$; y
$F(\sigma') \subseteq F(\sigma)$. Finalmente $\sigma' \neq e$:
$\sigma^{-1}(c) = b$, $\tau^{-1}(b) = b$, $\sigma(b) = c$,
$\tau(c) = d$, entonces $\sigma'(c) = d \neq c$. Así $\sigma' \in N
\setminus \{e\}$con$f(\sigma') \leq f(\sigma) - 1$,
minimalismo contradictorio.

Siendo ambos casos imposibles, $\sigma$ es un ciclo $3$.
\end{proof}

\begin{corollary}\label{cor:b3:groups:snnotsolvable}
Para $n \geq 5$: $A_n$ y $S_n$ no son \omterm{def:b3:groups:derived}{soluble}, y el único
\omterm{def:b3:groups:normal}{subgrupos normales} de $S_n$ son $\{e\}$, $A_n$ y $S_n$.
\end{corollary}

\begin{proof}
$A_n$ es no abeliano \omterm{def:b3:groups:simple}{simple}, por lo tanto no es \omterm{def:b3:groups:derived}{soluble}
(\cref{def:b3:groups:simple}); un grupo que contiene un \omterm{def:b3:groups:derived}{no solucionable}
El subgrupo no es \omterm{def:b3:groups:derived}{soluble} (\cref{prop:b3:groups:derived}). Sea $N
\trianglelefteq S_n$: entonces$N \cap A_n \trianglelefteq A_n$ es igual
$\{e\}$ o $A_n$. Si $N \cap A_n = A_n$, entonces $A_n \subseteq N$
y $N \in \{A_n, S_n\}$ por el índice. Si $N \cap A_n = \{e\}$,
la restricción a $N$ de la proyección $S_n \to S_n/A_n \cong
\Z/2\Z$es inyectiva, por lo que$\abs N \leq 2$; si$N = \{e, \sigma\}$,
la normalidad hace que el \omterm{ex:b3:groups:actions}{clase de conjugación} de $\sigma$ sea igual a
$\{\sigma\}$, es decir $\sigma \in Z(S_n)$. Pero $Z(S_n) = \{e\}$ para
$n \geq 3$: si $\sigma \ne e$ mueve $a$ a $b \neq a$, seleccione $c
\notin \{a, b\}$; luego$(b\,c)\sigma(b\,c)^{-1}$envía$a$a$c
\ne b$, por lo que difiere de$\sigma$. Por lo tanto$N = \{e\}$.
\end{proof}

\begin{theorem}[Jordan--Hölder]\label{thm:b3:groups:jordanholder}
Todo grupo finito $G \neq \{e\}$ admite un \emph{composición
serie}\index{serie de composición}
\[
\{e\} = G_0 \trianglelefteq G_1 \trianglelefteq \cdots
\trianglelefteq G_r = G,
\qquad G_{i}/G_{i-1} \text{ simple},
\]
y el multiset de \emph{factores de composición} $G_i/G_{i-1}$, hasta
al isomorfismo, no depende de la serie elegida. un finito
grupo es \omterm{def:b3:groups:derived}{soluble} si todos sus factores de composición son cíclicos de
orden principal.\index{Jordan--Hölder theorem}
\end{theorem}

\begin{proof}
\emph{Existencia}: inducción en $\abs G$. Si $G$ es \omterm{def:b3:groups:simple}{simple}, tome
$\{e\} \trianglelefteq G$. De lo contrario, elija un \omterm{def:b3:groups:normal}{normales
subgrupo} $N$ máximo adecuado (hay un número finito de subgrupos); $G/N$ es \omterm{def:b3:groups:simple}{simple}
por el teorema de correspondencia (un \omterm{def:b3:groups:normal}{subgrupo normal} adecuado y no trivial
de $G/N$ se elevaría a un \omterm{def:b3:groups:normal}{subgrupo normal} de $G$ estrictamente entre
$N$ y $G$). Agregue $N \trianglelefteq G$ a un \omterm{thm:b3:groups:jordanholder}{serie de composición}
de $N$.

\emph{Unicidad}: inducción en $\abs G$, el caso $G$ \omterm{def:b3:groups:simple}{simple}
siendo claro. Tome dos \omterm{thm:b3:groups:jordanholder}{serie de composición}, con penúltimos términos
$M \trianglelefteq G$ y $N \trianglelefteq G$ (por lo que $G/M$, $G/N$
son \omterm{def:b3:groups:simple}{simple}). Si es $M = N$, concluir por inducción aplicada a $M$.
De lo contrario, $MN$, \omterm{def:b3:groups:normal}{normal} en $G$ y que contiene estrictamente $M$, es igual
$G$ ($M$ es \omterm{def:b3:groups:normal}{normal} máximo: cualquier \omterm{def:b3:groups:normal}{normal} $M \subsetneq L
\subsetneq G$ se asignaría a un \omterm{def:b3:groups:normal}{subgrupo normal} adecuado y no trivial de
el \omterm{def:b3:groups:simple}{simple} $G/M$). El segundo teorema del isomorfismo da
\[
G/M = MN/M \cong N/(M \cap N),
\qquad
G/N = MN/N \cong M/(M \cap N).
\]
Configure $K = M \cap N$ ($\trianglelefteq G$) y corrija un \omterm{thm:b3:groups:jordanholder}{composición
serie} de $K$. Entonces $M$ lleva dos \omterm{thm:b3:groups:jordanholder}{serie de composición}: es
el original, y la serie de $K$ seguida de $K \trianglelefteq
M$(el cociente$M/K \cong G/N$ es \omterm{def:b3:groups:simple}{simple}). Por inducción (aplicada
a $M$), los factores de la serie original de $M$ son
$\{\text{factors of } K\} \cup \{G/N\}$; lo mismo para $N$. Por lo tanto
ambas series de $G$ tienen factores
\[
\{\text{factors of } K\} \;\cup\; \{\,G/N,\; G/M\,\},
\]
el mismo conjunto múltiple.

\omterm{def:b3:groups:derived}{Solubilidad}: si todos los factores son $\Z/p_i\Z$, la serie es una cadena
con cocientes abelianos, entonces $G$ es \omterm{def:b3:groups:derived}{soluble}
(\cref{prop:b3:groups:derived}). Por el contrario, un factor de composición
de un \omterm{def:b3:groups:derived}{grupo solucionable} es \omterm{def:b3:groups:derived}{soluble} (un cociente de un subgrupo) y
\omterm{def:b3:groups:simple}{simple}; a \omterm{def:b3:groups:derived}{soluble} \omterm{def:b3:groups:simple}{grupo sencillo} es abeliano ($D(G) \ne G$ fuerzas
$D(G) = \{e\}$), de ahí algunos $\Z/p\Z$.
\end{proof}

\begin{remark}\label{rem:b3:groups:jhmeaning}
Jordan--Hölder dice que cada grupo finito se construye a partir de \omterm{def:b3:groups:simple}{sencillo
grupos}, con una lista de piezas bien definida --- una aritmética de grupos
en el que \omterm{def:b3:groups:simple}{grupos simples} son los números primos, y donde \emph{como el
las piezas están pegadas} (datos de extensión, como en \omterm{def:b3:groups:semidirect}{producto semidirecto})
reemplaza la mera multiplicación. La clasificación de lo finito.
\omterm{def:b3:groups:simple}{grupos simples} --- el $\Z/p\Z$ cíclico, el $A_{n \geq
5}$alterno, dieciséis familias de tipo Lie y los grupos esporádicos$26$ --- es
uno de los monumentos de las matemáticas del siglo XX; su prueba,
repartida en unas diez mil páginas de diario, está mucho más allá
este curso.
\end{remark}

\begin{omfigure}
\begin{tikzpicture}[xscale=1.55, yscale=1.4]
  \tikzset{sg/.style={draw, rounded corners=2pt, inner sep=3pt,
      fill=omDef!6, font=\small}}
  \node[sg, fill=omThm!10] (G)   at (0, 3)    {$D_4$};
  \node[sg] (K1)  at (-1.6, 2)   {$\langle r^2, s\rangle$};
  \node[sg] (R)   at (0, 2)      {$\langle r\rangle$};
  \node[sg] (K2)  at (1.6, 2)    {$\langle r^2, rs\rangle$};
  \node[sg] (S1)  at (-2.4, 1)   {$\langle s\rangle$};
  \node[sg] (S2)  at (-1.2, 1)   {$\langle r^2 s\rangle$};
  \node[sg, fill=omProp!12] (Z) at (0, 1) {$\langle r^2\rangle$};
  \node[sg] (S3)  at (1.2, 1)    {$\langle rs\rangle$};
  \node[sg] (S4)  at (2.4, 1)    {$\langle r^3 s\rangle$};
  \node[sg] (E)   at (0, 0)      {$\{e\}$};
  \draw (G) -- (K1); \draw (G) -- (R); \draw (G) -- (K2);
  \draw (K1) -- (S1); \draw (K1) -- (S2); \draw (K1) -- (Z);
  \draw (R) -- (Z);
  \draw (K2) -- (Z); \draw (K2) -- (S3); \draw (K2) -- (S4);
  \draw (S1) -- (E); \draw (S2) -- (E); \draw (Z) -- (E);
  \draw (S3) -- (E); \draw (S4) -- (E);
\end{tikzpicture}

{\small Los diez subgrupos del \omterm{ex:b3:groups:semidirectexamples}{grupo diédrico} $D_4 = \langle r,
s \mid r^4 = s^2 = e,\ srs^{-1} = r^{-1}\rangle$. los tres
Los subgrupos del índice $2$ (fila del medio) son \omterm{def:b3:groups:normal}{normal}, al igual que \omterm{ex:b3:groups:actions}{centro}.
$\langle r^2\rangle$ (resaltado); los cuatro subgrupos de reflexión
caer en dos \omterm{ex:b3:groups:actions}{clases de conjugación} de dos. Cadenas de abajo hacia arriba
proporcione \omterm{thm:b3:groups:jordanholder}{serie de composición}, por ejemplo\ $\{e\} \trianglelefteq \langle
r^2\rangle \trianglelefteq \langle r\rangle \trianglelefteq D_4$:
factores $\Z/2\Z, \Z/2\Z, \Z/2\Z$ --- siempre el mismo multiset, como
Jordan--Hölder.}
\end{omfigure}

\section{Ceremonias}

\begin{exercise}[$\star$]\label{exo:b3:groups:1}
(a) Demuestre que cada subgrupo del índice $2$ es \omterm{def:b3:groups:normal}{normal}.
(b) Demuestre que si $[G : H] = 2$, entonces $x^2 \in H$ por cada $x \in
G$.
(c) Deducir que $A_4$ no tiene subgrupo de orden $6$: Lagrange
la conversación falla. \emph{(Cuente los cuadrados de $3$-ciclos.)}
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b3:groups:2}
Demuestre que si $G/Z(G)$ es cíclico, entonces $G$ es abeliano. deducir de nuevo
que cada grupo de orden $p^2$ es abeliano, y exhibe, para cada
prime $p$, un grupo nobeliano de orden $p^3$. \emph{(Think of
upper triangular matrices with unit diagonal over $\mathbb F_p$.)}
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b3:groups:3}
(a) Demuestre que $\operatorname{Aut}(\Z/n\Z) \cong (\Z/n\Z)^\times$.
(b) Demuestre que los automorfismos internos $\iota_g \colon x \mapsto
gxg^{-1}$forman un \omterm{def:b3:groups:normal}{subgrupo normal}$\operatorname{Inn}(G)
\trianglelefteq \operatorname{Aut}(G)$, con
$\operatorname{Inn}(G) \cong G/Z(G)$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:groups:4}
Sean $H, K$ subgrupos de un grupo finito $G$.
(a) Comprobar el \emph{fórmula del producto} $\abs{HK}\,\abs{H\cap K} =
\abs H\, \abs K$, contando las fibras del mapa$H \times K
\to HK$,$(h,k) \mapsto hk$.
(b) Demuestre que $HK$ es un subgrupo si $HK = KH$ (automático cuando uno
de los dos es \omterm{def:b3:groups:normal}{normal}).
(c) Si $H, K \trianglelefteq G$ y $H \cap K = \{e\}$, demuestre que
$hk = kh$ para todos $h \in H$, $k \in K$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:groups:5}
(Lema de conteo de Burnside) Un grupo finito $G$ actúa sobre un grupo finito
establezca $X$. Demuestre que el número de \omterm{def:b3:groups:action}{órbitas} es el \emph{promedio
número de puntos fijos}:
\[
\#\{\text{orbits}\} = \frac{1}{\abs G}\sum_{g \in G}
\abs{\operatorname{Fix}(g)},
\qquad \operatorname{Fix}(g) = \{x \in X : g \cdot x = x\},
\]
contando el conjunto $\{(g,x) : g\cdot x = x\}$ de dos maneras.
Aplicación: $\Z/p\Z$ ($p$ prime) actúa por rotación sobre collares de
Cuentas $p$ con colores $a$ disponibles; deducir el pequeño
teorema $a^p \equiv a \pmod p$.
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b3:groups:6}
Usando los teoremas de Sylow, demuestre que cada grupo de orden $15$ es
cíclico y que todo grupo de orden $45$ es abeliano.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:groups:7}
Demuestre que ningún grupo de orden $30$, ni ninguno de orden $56$, es
\omterm{def:b3:groups:simple}{simple}. \emph{(For $30$: if $n_3 \neq 1$ and $n_5 \neq 1$, count
the elements of orders $3$ and $5$. For $56$: count the elements
of order $7$.)}
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:groups:8}
(a) Sea $H \leq G$ del índice $n$. Demuestre que el \omterm{def:b3:groups:action}{acción} de $G$ en
$G/H$ produce un morfismo $G \to S_n$ cuyo núcleo $\bigcap_{g \in
G} gHg^{-1}$es el \omterm{def:b3:groups:normal}{subgrupo normal} más grande de$G$ contenido en
$H$.
(b) Deducir: si $G$ es finito y $p$ es el primo \emph{pequeñísimo}
divisor de $\abs G$, cada subgrupo del índice $p$ es \omterm{def:b3:groups:normal}{normal}.
(c) Demuestre que cada transitiva \omterm{def:b3:groups:action}{acción} de $G$ en un conjunto $X$ es
isomorfo al \omterm{def:b3:groups:action}{acción} en un espacio lateral: hay una biyección $X
\to G/G_x$ que conmuta con el \omterm{def:b3:groups:action}{comportamiento}.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:groups:9}
(a) Demuestre que $D(G)$ es el \omterm{def:b3:groups:normal}{subgrupo normal} más pequeño de $G$ con
cociente abeliano, y que cada morfismo desde $G$ hasta un abeliano
agrupar factores de forma única a través del \emph{abelianización}
$G^{\mathrm{ab}} = G/D(G)$.
(b) Calcule $D(S_n)$ y $S_n^{\mathrm{ab}}$ para $n \geq 2$, y
$D(Q_8)$ y $Q_8^{\mathrm{ab}}$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:groups:10}
Sea $G$ un grupo $p$ y $H \subsetneq G$ un subgrupo adecuado.
Muestre que $H \subsetneq N_G(H)$ ("\omterm{ex:b3:groups:actions}{normalizadores} crece") y deduzca
que cada subgrupo máximo de un grupo $p$ es \omterm{def:b3:groups:normal}{normal} del índice $p$.
\emph{(Induction on $\abs G$, using $Z(G) \neq \{e\}$: treat
separately $Z(G) \subseteq H$ and $Z(G) \not\subseteq H$.)}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b3:groups:11}
(Simplicidad de $A_5$, práctica)
(a) Demuestre que \omterm{ex:b3:groups:actions}{clases de conjugación} de $A_5$ tienen cardinalidades
$1$, $15$, $20$, $12$, $12$. Preste atención a la división del
Clase $S_5$ de ciclos $5$: para un ciclo $5$ $\sigma$, compare el
\omterm{ex:b3:groups:actions}{centralizadores} de $\sigma$ en $S_5$ y en $A_5$.
(b) Deduzca que $A_5$ es \omterm{def:b3:groups:simple}{simple}: un \omterm{def:b3:groups:normal}{subgrupo normal} es una unión de
\omterm{ex:b3:groups:actions}{clases de conjugación}, contiene $e$ y tiene cardinalidad divisoria
$60$.
(c) Demuestre que un \omterm{def:b3:groups:simple}{grupo sencillo} de orden $60$ necesariamente tiene $n_5 =
6$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:groups:12}
(\omterm{ex:b3:groups:actions}{Normalizadores} de \omterm{def:b3:groups:sylow}{Subgrupos de Sylow} se normalizan automáticamente) Dejemos que $P$
ser un subgrupo Sylow $p$ de un grupo finito $G$ y $H =
N_G(P)$.
(a) Demuestre que $P$ es el subgrupo \emph{único} Sylow $p$ de
$H$.
(b) Deducir $N_G(H) = H$. \emph{(For $g \in N_G(H)$: $gPg^{-1}$
is a Sylow $p$-subgroup of $H$, so $gPg^{-1} = P$.)}
(c) Concluir que no hay ningún Sylow \omterm{ex:b3:groups:actions}{normalizador} contenido en un
\omterm{def:b3:groups:normal}{subgrupo normal} de $G$, y que un subgrupo máximo que contiene
$N_G(P)$ se autonormaliza.
\end{exercise}

\section{Problema: los grupos de orden como máximo 15}

\begin{problem}[{Problema de fin de semana --- clasificación de pequeños
grupos}]\label{pb:b3:groups:1}
El objetivo es una clasificación completa, con pruebas completas, de los
grupos de orden $\leq 15$ hasta isomorfismo. Las órdenes $1, 2, 3, 5,
7, 11, 13$son liquidadas por Lagrange (cíclicas), y las órdenes$4$ y
$9$ por \cref{thm:b3:groups:pfixed} más el análisis a continuación de
$p^2$: quedan $6, 8, 10, 12, 14, 15$.

\medskip
\noindent\textbf{Parte I --- Tools.}
\begin{enumerate}
  \item Demuestre que un grupo en el que cada elemento satisface $x^2 =
        e$ es abeliano; deducir que tal grupo finito tiene orden
        $2^k$ y es isomorfo a $(\Z/2\Z)^k$. \emph{(View it as
        a vector space over $\mathbb F_2$.)}
  \item Demuestre que un grupo de orden $p^2$ es isomorfo a
        $\Z/p^2\Z$ o $(\Z/p\Z)^2$. Enumera los grupos abelianos de
        ordene $8$ hasta el isomorfismo: $\Z/8\Z$, $\Z/4\Z \times
        \Z/2\Z$,$(\Z/2\Z)^3$ --- demuestre que la lista está completa y
        irredundante \emph{sin} el teorema de la estructura de
        \cref{ch:b3:modules} (discutir por el orden máximo de un
        elemento).
  \item Vamos $\varphi, \varphi' \colon K \to \operatorname{Aut}(H)$
        serán dos \omterm{def:b3:groups:action}{comportamiento}. Demuestre que si $\varphi' = \varphi \circ
        \alpha$con$\alpha \in \operatorname{Aut}(K)$, entonces$H
        \rtimes_{\varphi} K \cong H \rtimes_{\varphi'} K$.
  \item Determine $\operatorname{Aut}(\Z/n\Z)$ para $n = 3, 4, 5,
        7$explícitamente y muestre$\operatorname{Aut}\bigl((\Z/2\Z)^2
        \bigr) \cong S_3$.
\end{enumerate}

\medskip
\noindent\textbf{Parte II --- Orders $2p$ ($6$, $10$, $14$) and
$pq$.}
\begin{enumerate}[resume]
  \item Sea $\abs G = 2p$ con $p$ un primo impar. Mostrar que $G$ tiene
        a \omterm{def:b3:groups:normal}{subgrupo normal} $N = \langle r \rangle$ de orden $p$ y
        un elemento $s$ del orden $2$ fuera de $N$.
  \item Deducir $G \cong \Z/p\Z \rtimes_\varphi \Z/2\Z$, donde
        $\varphi(1) \in \operatorname{Aut}(\Z/p\Z)$ es un
        involución, y concluir: $G \cong \Z/2p\Z$ o $G \cong
        D_p$; compruebe que estos dos no sean isomórficos. Esto se resuelve
        pedidos $6$, $10$, $14$.
  \item De manera más general, sea $\abs G = pq$ con números primos $p < q$.
        Demuestre que si $p \nmid q-1$ entonces $G$ es cíclico
        (\cref{ex:b3:groups:pq}), y que si $p \mid q - 1$ hay
        es, además de $\Z/pq\Z$, \emph{exactamente uno} grupo no abeliano
        $\Z/q\Z \rtimes \Z/p\Z$ hasta isomorfismo --- usar pregunta
        3 y el hecho de que $\operatorname{Aut}(\Z/q\Z) \cong
        (\Z/q\Z)^\times$es cíclico de orden$q - 1$, admitido aquí
        y probado en \cref{ch:b3:galois} (ciclicidad de
        $\mathbb F_q^\times$). Concluir para el pedido $15$.
\end{enumerate}

\medskip
\noindent\textbf{Parte III --- Order $8$.} Sea $G$ nobeliano de
orden $8$.
\begin{enumerate}[resume]
  \item Demuestre que $G$ tiene un elemento $r$ de orden $4$ (use
        pregunta 1) y que $N = \langle r\rangle$ es \omterm{def:b3:groups:normal}{normal}.
  \item Vamos $s \notin N$. Muestra que $s^2 \in N$
        (\cref{exo:b3:groups:1}(b)), que $srs^{-1} = r^{-1}$
        (examine las posibles imágenes de $r$ bajo conjugación,
        que debe tener orden 4 y excluir $srs^{-1} = r$), y
        que $s^2 \in \{e, r^2\}$ \emph{(¿Qué pasa si $s^2 = r$
        o $r^3$? y ¿por qué $s^2$ debe conmutar con $s$?)}.
  \item En el caso $s^2 = e$, mostrar $G \cong D_4$.
  \item En el caso $s^2 = r^2$, demuestre que la multiplicación
        la mesa está completamente determinada; el grupo resultante es el
        \emph{grupo cuaternión}\index{grupo cuaternión} $Q_8 =
        \{\pm 1, \pm \mathrm i, \pm \mathrm j, \pm \mathrm k\}$,
        $\mathrm i^2 = \mathrm j^2 = \mathrm k^2 = \mathrm i\,
        \mathrm j\,\mathrm k = -1$(establecer$r = \mathrm i$,$s =
        \mathrm j$). Verifique que$Q_8$ exista, por ejemplo\ dentro
        $GL_2(\C)$ vía
        \[
        \mathrm i \mapsto \begin{pmatrix} \iu & 0\\ 0 & -\iu
        \end{pmatrix},
        \qquad
        \mathrm j \mapsto \begin{pmatrix} 0 & 1\\ -1 & 0
        \end{pmatrix}.
        \]
  \item Demuestre que cada subgrupo no trivial de $Q_8$ contiene
        $-1$; deducir que cada subgrupo de $Q_8$ es \omterm{def:b3:groups:normal}{normal}, es decir
        $D_4 \not\cong Q_8$ (contar elementos del orden $2$), y
        que $Q_8$ no es un \omterm{def:b3:groups:semidirect}{producto semidirecto} de dos adecuados
        subgrupos.
\end{enumerate}

\medskip
\noindent\textbf{Parte IV --- Order $12$.} Deje $\abs G = 12$, $P_3
\in \mathrm{Syl}_3(G)$,$P_2 \in \mathrm{Syl}_2(G)$.
\begin{enumerate}[resume]
  \item Muestra $n_3 \in \{1, 4\}$, $n_2 \in \{1, 3\}$, y que $n_3
        = 4$fuerza a$n_2 = 1$ \emph{(contar elementos de orden
        $3$)}.
  \item Supongamos que $n_3 = 4$. La conjugación \omterm{def:b3:groups:action}{acción} en
        $\mathrm{Syl}_3$ da $\rho \colon G \to S_4$. mostrar eso
        $\ker \rho$, contenido en cada $N_G(P_3)$ y por tanto de
        el orden que divide $3$, es trivial \emph{(¿Por qué no puede tener?
        pedir $3$?)}; que la imagen, un subgrupo de orden $12$ de
        $S_4$, es necesariamente $A_4$ \emph{(index 2 subgroups are
        \omterm{def:b3:groups:normal}{normal} and contain all squares ---
        \cref{exo:b3:groups:1}; count the squares in $S_4$)}; y
        concluir $G \cong A_4$.
  \item Supongamos $n_3 = 1$, entonces $G \cong \Z/3\Z \rtimes_\varphi P_2$
        con $\varphi \colon P_2 \to \operatorname{Aut}(\Z/3\Z)
        \cong \Z/2\Z$. Enumerar los casos:$\varphi$ trivial
        produce $\Z/12\Z$ y $\Z/6\Z \times \Z/2\Z$; $P_2 =
        \Z/4\Z$con$\varphi$ sobreyectiva produce el
        \emph{grupo dicíclico} $\mathrm{Dic}_3 = \Z/3\Z \rtimes
        \Z/4\Z$;$P_2 = (\Z/2\Z)^2$con$\varphi$ sobreyectiva
        arroja, hasta la equivalencia de la pregunta 3, un único
        grupo --- muestra que es $D_6$, por ejemplo\ exhibiendo un elemento
        de orden $6$ y una involución similar a un reflejo.
  \item Mostrar que $\Z/12\Z$, $\Z/6\Z \times \Z/2\Z$, $D_6$, $A_4$,
        $\mathrm{Dic}_3$ son \emph{(cuenta
        elementos de orden $2$, o usar $n_3$)} no isomórficos por pares. Esto establece el orden
        $12$.
\end{enumerate}

\medskip
\noindent\textbf{Part V --- Synthesis.}
\begin{enumerate}[resume]
  \item Armar la tabla de clasificación: para cada orden $n \leq
        15$, la lista completa de grupos hasta isomorfismo, con
        los recuentos $1, 1, 1, 2, 1, 2, 1, 5, 2, 2, 1, 5, 1, 2, 1$.
\end{enumerate}

\medskip
\noindent\textbf{Part VI --- Beyond: the groups of order $p^3$,
$p$ odd.} El análisis de la orden $8$ de la Parte III tiene una hermosa
análogo de primo impar, con un fenómeno genuinamente nuevo. Sea $p$
un primo impar y $G$ no abeliano de orden $p^3$.
\begin{enumerate}[resume]
  \item Muestre que $\abs{Z(G)} = p$, que $G/Z(G) \cong
        (\Z/p\Z)^2$ \emph{(a cyclic quotient by the \omterm{ex:b3:groups:actions}{centro} forces
        abelianity: \cref{exo:b3:groups:2})} y que $D(G) =
        Z(G)$\emph{(for$D(G) \subseteq Z(G)$, use that
        $G/Z(G)$ is abelian; for equality, $G$ is nonabelian and
        $D(G) \neq \{e\}$)}. Deduce que cada \omterm{def:b3:groups:derived}{conmutador}
        $[x, y] = xyx^{-1}y^{-1}$ es central y de orden.
        dividiendo $p$.
  \item (La identidad clave) Dejemos que $x, y \in G$ y $z = [y, x]$,
        céntrico. Demostrar por inducción en $k$:
        \[
        (xy)^k = x^k y^k z^{k(k-1)/2} .
        \]
        \emph{(Mueva cada $y$ más allá de cada $x$; cada cruce cuesta
        un factor central $z$.)}
  \item Deduzca que para $p$ impar la aplicación $\theta\colon x \mapsto
        x^p$es un morfismo de grupo de$G$a$Z(G)$ \emph{(why is
        $x^p$ central? why does $z^{p(p-1)/2} = e$ need $p$
        odd?)}, y concluya que $G$ tiene exponente $p$ o $p^2$,
        los dos casos se distinguen por si $\theta$ es
        trivial.
  \item (Exponente $p$) Supongamos que cada elemento satisface $x^p = e$.
        Elija $x, y$ cuyas clases generen $G/Z(G)$ y configure $z =
        [y, x]$. Muestre que$z \neq e$, que cada elemento de$G$
        es exclusivamente $x^ay^bz^c$ ($0 \leq a, b, c < p$), y que
        la multiplicación está enteramente determinada por la
        relaciones $x^p = y^p = z^p = e$, $z$ central, $[y, x] =
        z$. Verifique que el \emph{grupo heisenberg}
        \[
        H_p = \left\{
        \begin{pmatrix} 1 & a & c\\ 0 & 1 & b\\ 0 & 0 & 1
        \end{pmatrix} : a, b, c \in \mathbb F_p \right\}
        \subseteq GL_3(\mathbb F_p)
        \]
        realiza estas relaciones y tiene exponente $p$ (calcule
        $(I + N)^p$ con $N$ estrictamente triangular superior, utilizando
        $N^3 = 0$ y $p \geq 3$): cada exponente-$p$ no abeliano
        El grupo de orden $p^3$ es isomorfo a $H_p$.
  \item (Exponente $p^2$) Supongamos que algún $r \in G$ tiene el orden $p^2$,
        y configure $N = \langle r\rangle$, \omterm{def:b3:groups:normal}{normal} (índice $p$:
        \cref{exo:b3:groups:10}). Mostrar que hay $s \notin N$ con
        $s^p = e$ \emph{(take any $t \notin N$; using question
        20, correct it: $\theta(t) = t^p \in Z(G) \subseteq N$
        --- justify $Z(G) = \langle r^p\rangle$ --- and choose $a$
        with $s = tr^{a}$ satisfying $s^p = e$; where is $p$ odd
        used?)}. Mostrar $srs^{-1} = r^{1+p}$ hasta reemplazar $s$ por
        una potencia, y concluimos: existe exactamente \emph{uno}
        grupo nobeliano de orden $p^3$ y exponente $p^2$,
        es decir $\Z/p^2\Z \rtimes_\varphi \Z/p\Z$ con
        $\varphi(1)\colon r \mapsto r^{1+p}$ \emph{(use question
        3; $\operatorname{Aut}(\Z/p^2\Z)$ is cyclic of order
        $p(p-1)$, admitted here, so it has a unique subgroup of
        order $p$)}.
  \item Concluye el conteo: para $p$ impar hay exactamente $5$
        grupos de orden $p^3$ (tres abelianos, dos nobelianos),
        al igual que para $p = 2$ --- pero los dos no abelianos son
        ya no $D_4$ y $Q_8$. Identificar exactamente dónde
        El argumento odd-$p$ se rompe para $p = 2$: en la identidad de
        pregunta 19, $z^{k(k-1)/2}$ para $k = p = 2$ es $z^1 \neq
        e$, por lo que elevar al cuadrado no es un morfismo --- y de hecho$Q_8$
        tiene un elemento único de orden $2$ mientras que el exponente-$4$
        $D_4$ tiene cinco.
\end{enumerate}

\medskip
\noindent\textbf{Part VII --- Complements.}
\begin{enumerate}[resume]
  \item Para $p$ impar, cuente los elementos de orden $p$ en cada uno de
        los dos grupos nobelianos de orden $p^3$: demuestre que
        $H_p$ tiene exactamente $p^3 - 1$ de ellos, mientras que $M_p =
        \Z/p^2\Z \rtimes \Z/p\Z$tiene exactamente$p^2 - 1$
        \emph{(use the morphism $\theta$ of question 20:
        identify its image, then the order of its kernel)}.
        Verifique numéricamente para $p = 3$: $26$ contra $8$.
        Explique por qué no existe un argumento de morfismo cuadrado de este tipo.
        puede separar $D_4$ de $Q_8$, y qué recuento lo hace
        separarlos.
  \item Llame a un número entero $n \geq 1$ \emph{cíclico} si cada grupo
        de orden $n$ es cíclico. Demuestre que si $p^2 \mid n$ para
        algún primo $p$, o si $n$ tiene divisores primos $p < q$
        con $p \mid q - 1$, entonces $n$ no es \emph{(en
        cada caso exhibe un grupo no cíclico de orden $n$, usando
        Parte II para la segunda)} cíclico. Deducir que $n$ fuerzas cíclicas
        $\gcd(n, \varphi(n)) = 1$, donde $\varphi$ es el de Euler
        paciente y coteje con la tabla de la pregunta 17:
        entre $n \leq 15$, las órdenes que llevan un solo grupo
        son exactamente $n \in \{1, 2, 3, 5, 7, 11, 13, 15\}$,
        precisamente aquellos con $\gcd(n, \varphi(n)) = 1$.
\end{enumerate}
\end{problem}