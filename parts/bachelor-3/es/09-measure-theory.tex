\chapter{Teoría de la medida}\label{ch:b3:measure}

¿Cuánto mide un subconjunto de $\R$? La respuesta ingenua --- asignar a
cada conjunto tiene una longitud invariante en la traducción que extiende la de
intervalos --- es \emph{imposible}: construcción de Vitali, en
Al final de este capítulo, se produce un conjunto sin consistencia.
longitud. \omterm{def:b3:measure:measure}{Medida} la teoría es el retiro disciplinado: restringir
atención a una rica clase de conjuntos \emph{mensurable}, en los que
La longitud aditiva contable existe y es única. Las recompensas son
inmensa --- integral de Lebesgue (\cref{ch:b3:lebesgue}), la
$L^p$ espacios de análisis funcional, y todo el mundo moderno.
probabilidad (\cref{ch:b3:probability}) se basan en los tres
teoremas demostrados aquí: el lema de unicidad de Dynkin,
El teorema de extensión de Carathéodory y la existencia de
\omterm{def:b3:measure:lebesgueouter}{medida de lebesgue}.

\section{$\sigma$-álgebras}

\begin{definition}\label{def:b3:measure:sigmaalgebra}
A \emph{$\sigma$-algebra}\index{sigma-algebra@$\sigma$-algebra}
en un conjunto $X$ hay una familia $\mathcal A$ de subconjuntos que contienen
$\varnothing$, estable bajo complemento y bajo
Uniones \emph{contable} (por lo tanto, intersecciones contables, establecidas
diferencias, y contiene $X$). La pareja $(X, \mathcal A)$
es un \emph{espacio mensurable}; Los miembros de $\mathcal A$ son
\emph{conjuntos medibles}. Para cualquier familia $\mathcal E$ de subconjuntos,
$\sigma(\mathcal E)$ denota el álgebra $\sigma$ más pequeña
que contiene $\mathcal E$ (la intersección de todos ellos ---
una intersección de $\sigma$-álgebras es uno).
\end{definition}

\begin{definition}\label{def:b3:measure:borel}
El \emph{Borel $\sigma$-algebra}\index{Borel sigma-algebra@%
Borel $\sigma$-algebra} de un espacio \omterm{def:b3:topology:topology}{topológico} es
$\mathcal B(X) = \sigma(\{\text{open sets}\})$. En $\R$:
$\mathcal B(\R)$ también es generado por los intervalos abiertos, por
los intervalos cerrados, por los rayos $(-\infty, a]$, y por los rayos
con puntos finales racionales (\cref{exo:b3:measure:1}) --- cada uno
La familia genera las aperturas mediante operaciones contables, por ejemplo\
cada \omterm{def:b3:topology:topology}{conjunto abierto} de $\R$ es una unión contable de intervalos abiertos
con datos racionales.
\end{definition}

\begin{definition}\label{def:b3:measure:dynkin}
A \emph{$\pi$-system}\index{pi-system@$\pi$-system} es una familia
estable bajo intersecciones finitas. un
\emph{$\lambda$-system}\index{lambda-system@$\lambda$-system}
(clase Dynkin) es una familia $\mathcal D$ con: $X \in \mathcal
D$;$A, B \in \mathcal D$,$A \subseteq B$ $\Rightarrow$ $B
\setminus A \in \mathcal D$; y$A_n \uparrow A$,$A_n \in
\mathcal D$ $\Rightarrow$ $A \in \mathcal D$.
\end{definition}

\begin{theorem}[Lema $\pi$--$\lambda$ de Dynkin]
\label{thm:b3:measure:dynkin}
Si un sistema $\lambda$, $\mathcal D$ contiene un sistema $\pi$
$\mathcal P$, luego $\mathcal D \supseteq
\sigma(\mathcal P)$.\index{Lema de Dynkin}
\end{theorem}

\begin{proof}
Sea $\mathcal D_0$ el sistema $\lambda$ más pequeño que contiene
$\mathcal P$ (intersección de todos ellos); basta con mostrar
$\mathcal D_0$ es un álgebra $\sigma$, desde entonces
$\sigma(\mathcal P) \subseteq \mathcal D_0 \subseteq \mathcal
D$. Un sistema$\lambda$ estable bajo intersecciones finitas
\emph{is} a $\sigma$-álgebra: complementos ($X \setminus A = X
\setminus A$con$A \subseteq X$), uniones finitas ($A \cup B =
X \setminus ((X\setminus A)\cap(X\setminus B))$) y contables
sindicatos a través de $\bigcup_{k \leq n}A_k \uparrow \bigcup_kA_k$. entonces nosotros
Demuestre que $\mathcal D_0$ es un sistema $\pi$, en dos pasos. dejar
\[
\mathcal D_1 = \{A \in \mathcal D_0 : A \cap P \in \mathcal
D_0 \ \forall P \in \mathcal P\}.
\]
$\mathcal D_1$ es un sistema $\lambda$ (los tres axiomas son
verificado al cruzar con $P$: por ejemplo, \ $(B\setminus A)\cap P
= (B \cap P)\setminus(A \cap P)$, una diferencia adecuada dentro
$\mathcal D_0$) y contiene $\mathcal P$ (sistema $\pi$):
$\mathcal D_1 = \mathcal D_0$. Ahora deja
\[
\mathcal D_2 = \{A \in \mathcal D_0 : A \cap D \in \mathcal
D_0\ \forall D \in \mathcal D_0\}.
\]
Por el paso anterior, $\mathcal D_2 \supseteq \mathcal P$; y
$\mathcal D_2$ es un sistema $\lambda$ según la misma verificación:
$\mathcal D_2 = \mathcal D_0$, que dice exactamente eso
$\mathcal D_0$ es estable bajo las intersecciones.
\end{proof}

\section{Medidas}

\begin{definition}\label{def:b3:measure:measure}
A \emph{medida}\index{medida} en $(X, \mathcal A)$ es una aplicación
$\mu \colon \mathcal A \to [0, +\infty]$ con $\mu(\varnothing)
= 0$es decir \emph{$\sigma$-additive}: para pares disjuntos
$(A_n)_{n\in\N}$,
\[
\mu\Bigl(\bigsqcup_n A_n\Bigr) = \sum_n \mu(A_n).
\]
$(X, \mathcal A, \mu)$ es un \emph{medir el espacio}; $\mu$ es
\emph{finito} si $\mu(X) < \infty$, \emph{probabilidad
medida} si $\mu(X) = 1$, \emph{$\sigma$-finite} si $X$ es un
unión contable de conjuntos de medida finita. Ejemplos: contar
medir en $(\N, \mathcal P(\N))$; la masa de Dirac $\delta_a(A)
= \mathbf 1_{a \in A}$; y, el objeto de este capítulo,
\omterm{def:b3:measure:lebesgueouter}{medida de lebesgue}.
\end{definition}

\begin{proposition}\label{prop:b3:measure:basics}
Sea $\mu$ un \omterm{def:b3:measure:measure}{medida}. (a) Monotonicidad: $A \subseteq B
\Rightarrow \mu(A) \leq \mu(B)$. (b) Subaditividad contable:
$\mu(\bigcup A_n) \leq \sum\mu(A_n)$. (c) \omterm{def:b3:topology:continuity}{Continuidad} de
a continuación: $A_n \uparrow A \Rightarrow \mu(A_n) \to \mu(A)$. (d)
\omterm{def:b3:topology:continuity}{Continuidad} desde arriba: $A_n \downarrow A$ \emph{with
$\mu(A_1) < \infty$} $\Rightarrow \mu(A_n) \to \mu(A)$.
\end{proposition}

\begin{proof}
(a) $B = A \sqcup (B\setminus A)$. (b) Disjuntos: $B_n = A_n
\setminus \bigcup_{k<n}A_k$ son disjuntos con la misma unión,
y $\mu(B_n) \leq \mu(A_n)$. (c) $A = \bigsqcup_n (A_n
\setminus A_{n-1})$($A_0 = \varnothing$): las sumas parciales de
$\sum\mu(A_n\setminus A_{n-1})$ son $\mu(A_n)$. (d) Aplicar (c)
a $A_1 \setminus A_n \uparrow A_1 \setminus A$ y restar
de $\mu(A_1)$ --- la finitud hace la resta
legítimo. Contraejemplo sin él: $A_n = [n, \infty)$ para
\omterm{def:b3:measure:lebesgueouter}{medida de lebesgue}: $A_n \downarrow \varnothing$ pero $\mu(A_n) =
\infty$.
\end{proof}

\begin{theorem}[Singularidad]\label{thm:b3:measure:uniqueness}
Sea $\mu, \nu$ \omterm{def:b3:measure:measure}{medidas} en $\sigma(\mathcal P)$, $\mathcal
P$un sistema$\pi$, con$\mu = \nu$en$\mathcal P$. si hay
son conjuntos $P_k \in \mathcal P$ con $P_k \uparrow X$ y
$\mu(P_k) < \infty$, luego $\mu = \nu$ en todos
$\sigma(\mathcal P)$.
\end{theorem}

\begin{proof}
Fije $k$ y considere los finitos \omterm{def:b3:measure:measure}{medidas} $\mu_k(A) = \mu(A \cap
P_k)$y$\nu_k(A) = \nu(A \cap P_k)$en$\sigma(\mathcal
P)$: coinciden en$\mathcal P$, desde$P \cap P_k \in \mathcal
P$(sistema$\pi$), y asignan a$X$ el mismo valor finito
$\mu(P_k)$. La clase $\mathcal D = \{A : \mu_k(A) =
\nu_k(A)\}$es un sistema$\lambda$:$X \in \mathcal D$; adecuado
diferencias por resta (valores finitos); límites crecientes
por \omterm{def:b3:topology:continuity}{continuidad} desde abajo (\cref{prop:b3:measure:basics}(c)).
Contiene el sistema $\pi$ $\mathcal P$, por lo que Dynkin
(\cref{thm:b3:measure:dynkin}) da $\mathcal D \supseteq
\sigma(\mathcal P)$:$\mu_k = \nu_k$ por todas partes. Finalmente, para
cualquier $A \in \sigma(\mathcal P)$, \omterm{def:b3:topology:continuity}{continuidad} desde abajo a lo largo de $A
\cap P_k \uparrow A$da$\mu(A) = \lim_k\mu_k(A) =
\lim_k\nu_k(A) = \nu(A)$.
\end{proof}

\section{Medidas exteriores y teorema de Carathéodory}

\begin{definition}\label{def:b3:measure:outer}
Un \emph{medida exterior}\index{medida exterior} en $X$ es una aplicación
$\mu^* \colon \mathcal P(X) \to [0, \infty]$ con
$\mu^*(\varnothing) = 0$, monótono y subaditivo contable.
Un conjunto $A$ es \emph{$\mu^*$-measurable} (Carathéodory) si
divide cada conjunto de forma aditiva:
\[
\mu^*(E) = \mu^*(E \cap A) + \mu^*(E \setminus A)
\qquad \text{for every } E \subseteq X
\]
($\leq$ siempre se cumple por subaditividad; el contenido es $\geq$).
\end{definition}

\begin{theorem}[carathéodory]\label{thm:b3:measure:caratheodory}
Los conjuntos medibles $\mu^*$ forman un álgebra $\sigma$ $\mathcal
M$, y$\mu^*\restriction_{\mathcal M}$ es un \omterm{def:b3:measure:measure}{medida}.
Además, cada conjunto con $\mu^*(N) = 0$ pertenece a $\mathcal M$.
(el \omterm{def:b3:measure:measure}{medida} es \emph{\omterm{def:b3:complete:complete}{completo}}).\index{Carathéodory's
theorem}
\end{theorem}

\begin{proof}
$\mathcal M$ contiene $\varnothing$ y es estable bajo
complemento (la condición definitoria es simétrica en $A$,
$X\setminus A$). \emph{Uniones finitas}: dejemos que $A, B \in \mathcal
M$y$E$sean arbitrarios; dividiendo$E$por$A$, luego cada pieza por
$B$:
\[
\mu^*(E) = \mu^*(E\cap A\cap B) + \mu^*(E\cap A\setminus B) +
\mu^*(E\cap B\setminus A) + \mu^*(E\setminus(A\cup B)).
\]
Las primeras tres piezas cubren $E \cap (A \cup B)$, por lo que
la subaditividad da $\mu^*(E) \geq \mu^*(E\cap(A\cup B)) +
\mu^*(E\setminus(A\cup B))$:$A \cup B \in \mathcal M$. Por
inducción, uniones finitas; con complementos, disjunciones finitas
manipulaciones están disponibles.

\emph{Additivity on $\mathcal M$}: para $A, B \in
\mathcal M$disjunto y cualquier$E$:$\mu^*(E\cap(A\sqcup B)) = \mu^*(E\cap
A) + \mu^*(E \cap B)$(dividido por$A$); por inducción,
\[
\mu^*\Bigl(E \cap \bigsqcup_{k\leq n}A_k\Bigr)
= \sum_{k\leq n}\mu^*(E\cap A_k).
\tag{$*$}
\]

\emph{Uniones contables}: sea $(A_k) \subseteq \mathcal M$
disjunto (basta, por desunión dentro del álgebra
$\mathcal M$), $A = \bigsqcup A_k$, $E$ arbitraria. Usando
$\bigsqcup_{k\leq n}A_k \in \mathcal M$ y monotonicidad:
\[
\mu^*(E) = \mu^*\Bigl(E\cap\bigsqcup_{k\leq n}A_k\Bigr) +
\mu^*\Bigl(E\setminus\bigsqcup_{k\leq n}A_k\Bigr)
\geq \sum_{k \leq n}\mu^*(E\cap A_k) + \mu^*(E\setminus A)
\]
por ($*$). Sea $n \to \infty$ y use subaditividad contable
al revés :
\[
\mu^*(E) \geq \sum_{k}\mu^*(E\cap A_k) + \mu^*(E\setminus A)
\geq \mu^*(E \cap A) + \mu^*(E\setminus A) \geq \mu^*(E):
\]
todas las desigualdades son igualdades. Esto prueba tanto $A \in
\mathcal M$como, tomando$E = A$, la aditividad contable de
$\mu^*$ en $\mathcal M$.

\emph{Conjuntos nulos}: si $\mu^*(N) = 0$, entonces para cualquier $E$:
$\mu^*(E \cap N) + \mu^*(E\setminus N) \leq 0 + \mu^*(E)$:
$N \in \mathcal M$.
\end{proof}

\section{Medida de Lebesgue sobre \texorpdfstring{$\R$}{R}}

\begin{definition}\label{def:b3:measure:lebesgueouter}
El \emph{Medida exterior de Lebesgue}\index{medida de lebesgue} de $A
\subseteq \R$ es
\[
\lambda^*(A) = \inf\Bigl\{\sum_{n} (b_n - a_n) :
A \subseteq \bigcup_n \intoo{a_n}{b_n}\Bigr\}
\]
(tapas contables por intervalos abiertos).
\end{definition}

\begin{lemma}\label{lem:b3:measure:outerbasics}
$\lambda^*$ es un \omterm{def:b3:measure:outer}{medida exterior}, invariante bajo traducciones,
y $\lambda^*(I) = \ell(I)$ (la longitud) para cada intervalo
$I$.
\end{lemma}

\begin{proof}
\omterm{def:b3:measure:outer}{medida exterior}: $\varnothing$ está cubierto por arbitrariamente pequeños
intervalos; la monotonicidad es clara; subaditividad: cubiertas dadas
de cada $A_n$ dentro de $\varepsilon 2^{-n}$ del mínimo,
su unión cubre $\bigcup A_n$ con una longitud total $\leq \sum
\lambda^*(A_n) + \varepsilon$. Invariancia de traducción:
traducir las portadas.

Longitud: basta con tratar $I = \intcc ab$ (otros tipos
difieren según los puntos finales, que tienen \omterm{def:b3:measure:outer}{medida exterior} $0$: cubrir por
pequeños intervalos; luego apriete comparaciones de tipo $\intcc{a+\varepsilon}{b -
\varepsilon} \subseteq \intoo ab$).
$\lambda^*(\intcc ab) \leq b - a$: portada por
$\intoo{a-\varepsilon}{b+\varepsilon}$. Por el contrario, sea $\intcc
ab \subseteq \bigcup_n\intoo{a_n}{b_n}$: por
\emph{compacidad} (Borel--Lebesgue,
\cref{thm:b3:topology:metriccompact}), un número finito de intervalos
basta, digamos $I_1, \dots, I_N$. Mostramos $\sum_{k\leq N}(b_k -
a_k) \geq b - a$por inducción en$N$: elija$I_{k_1} \ni a$; si
$b_{k_1} > b$ hecho ($b_{k_1} - a_{k_1} > b - a$); de lo contrario el
el segmento $\intcc{b_{k_1}}b$ está cubierto por el restante $N - 1$
intervalos, y la inducción da $\sum_{k \neq k_1}(b_k - a_k)
\geq b - b_{k_1}$, mientras que$b_{k_1} - a_{k_1} > b_{k_1} - a$:
resumir.
\end{proof}

\begin{theorem}[Medida de Lebesgue]\label{thm:b3:measure:lebesgue}
Cada conjunto Borel de $\R$ es medible en $\lambda^*$. el
restricción $\lambda$ de $\lambda^*$ al álgebra $\sigma$
$\mathcal L = \mathcal M_{\lambda^*} \supseteq \mathcal B(\R)$
(el \emph{Lebesgue $\sigma$-algebra}) es el único \omterm{def:b3:measure:measure}{medida} en
$\mathcal B(\R)$ asignando a cada intervalo su longitud; es
invariante de traducción y $\sigma$-finito.
\end{theorem}

\begin{proof}
Por \cref{thm:b3:measure:caratheodory} basta con mostrar cada uno
el rayo $A = \intoo{-\infty}c$ es $\lambda^*$-medible
(los rayos generan $\mathcal B$,
\cref{def:b3:measure:borel}). Deje $E \subseteq \R$ con
$\lambda^*(E) < \infty$ y $\bigcup I_n \supseteq E$ una funda
con $\sum\ell(I_n) \leq \lambda^*(E) + \varepsilon$. cada uno
$I_n$ se divide en los dos intervalos $I_n' = I_n \cap A$ y
$I_n'' = I_n\setminus A$ (un intervalo menos un rayo es un
intervalo) con $\ell(I_n') + \ell(I_n'') = \ell(I_n)$; el
Cubierta $I_n'$ $E \cap A$ y cubierta $I_n''$ $E \setminus A$
(amplíe cada uno a un intervalo abierto de longitud $\ell +
\varepsilon2^{-n}$ para permanecer dentro de la definición), por lo que
\[
\lambda^*(E\cap A) + \lambda^*(E\setminus A)
\leq \sum_n\bigl(\ell(I_n') + \ell(I_n'')\bigr) + 2\varepsilon
\leq \lambda^*(E) + 3\varepsilon .
\]
Unicidad: dos \omterm{def:b3:measure:measure}{medidas} concordando con la longitud en el
$\pi$-sistema de intervalos $\intoc ab$ (finitos en ellos) acuerdan
$\sigma(\text{intervals}) = \mathcal B$ por
\cref{thm:b3:measure:uniqueness} con $P_k = \intoc{-k}k$.
$\sigma$-finitud: $\R = \bigcup(-k, k]$.
\end{proof}

\begin{theorem}[Regularidad]\label{thm:b3:measure:regularity}
Por cada $A \in \mathcal L$:\index{regularidad de Lebesgue
medida}
\[
\lambda(A) = \inf\{\lambda(U) : U \supseteq A \text{ open}\}
= \sup\{\lambda(K) : K \subseteq A \text{ compact}\}.
\]
\end{theorem}

\begin{proof}
\emph{Exterior}: una funda $\bigcup I_n$ con $\sum\ell(I_n) \leq
\lambda(A) + \varepsilon$es una \omterm{def:b3:topology:topology}{conjunto abierto}$U \supseteq A$ con
$\lambda(U) \leq \lambda(A) + \varepsilon$ (subaditividad); si
$\lambda(A) = \infty$ la afirmación es trivial. \emph{Interno}:
primero deje que $A$ esté limitado, $A \subseteq [-M, M]$. Elige un abierto
$U \supseteq ([-M,M]\setminus A)$ con $\lambda(U) \leq
\lambda([-M,M]\setminus A) + \varepsilon$; entonces$K = [-M,
M]\setminus U$es \omterm{def:b3:topology:compact}{compacto},$K \subseteq A$ y
\[
\lambda(K) = \lambda([-M,M]) - \lambda([-M,M]\cap U)
\geq \lambda([-M,M]) - \bigl(\lambda([-M,M]) - \lambda(A) +
\varepsilon\bigr) = \lambda(A) - \varepsilon .
\]
Para generales $A$: $\lambda(A) = \lim_M\lambda(A\cap[-M,M])$
(\omterm{def:b3:topology:continuity}{continuidad} desde abajo) y aplique el estuche delimitado en el interior.
\end{proof}

\begin{example}\label{ex:b3:measure:cantor}
El conjunto de Cantor (\cref{exo:b3:topology:10}) tiene $\lambda(C) = 0$:
$C \subseteq C_n$, una unión de intervalos de longitud $2^n$
$3^{-n}$, entonces $\lambda(C) \leq (2/3)^n \to 0$. un incontable
conjunto nulo --- la cardinalidad no ve \omterm{def:b3:measure:measure}{medida}. Por el contrario,
\emph{conjuntos gordos de Cantor} (\cref{exo:b3:measure:5}) no están en ninguna parte
denso con positivo \omterm{def:b3:measure:measure}{medida}: \omterm{def:b3:topology:topology}{topología} no ve \omterm{def:b3:measure:measure}{medida}
tampoco. El problema del fin de semana lleva esta interacción a su punto máximo.
sorprendente conclusión: hay conjuntos mensurables según Lebesgue que
no son Borel.
\end{example}

\begin{theorem}[vitali]\label{thm:b3:measure:vitali}
No hay \omterm{def:b3:measure:measure}{medida} en los subconjuntos \emph{todo} de $\R$, es decir
invariante de traducción y asigna a cada intervalo su longitud.
En particular $\mathcal L \neq \mathcal P(\R)$: no mensurable
existen conjuntos.\index{El conjunto no mensurable de Vitali}
\end{theorem}

\begin{proof}
Supongamos que $\mu$ fuera uno. En $\intcc01$, considere la equivalencia
$x \sim y \iff x - y \in \Q$; por el \emph{axioma de elección}
elija un representante en $\intcc01$ por clase: un conjunto $V$. Para
$q \in \Q\cap\intcc{-1}1$, las traducciones $V + q$ son por pares
disjunto (dos puntos de $V$ que difieren por un racional serían
representantes equivalentes pero distintos) y
\[
\intcc01 \subseteq \bigsqcup_{q \in \Q\cap\intcc{-1}1}(V + q)
\subseteq \intcc{-1}2 :
\]
la primera inclusión ya que cada $x \in \intcc01$ difiere de
su representante $v$ por un racional $q = x - v \in
\intcc{-1}1$. La monotonicidad y la aditividad$\sigma$ dan
\[
1 \leq \sum_{q}\mu(V + q) \leq 3,
\qquad\text{with } \mu(V + q) = \mu(V) \text{ for all } q .
\]
Una suma infinita de la constante $\mu(V)$ es $0$ o $\infty$:
ambos límites no pueden sostenerse. Entonces no existe tal $\mu$ --- y $V
\notin \mathcal L$, ya que$\lambda$en$\mathcal L$ tiene todos los
propiedades utilizadas.
\end{proof}

\begin{remark}\label{rem:b3:measure:banachtarski}
En $\R^3$ el fracaso es más dramático: el Banach--Tarski
La paradoja descompone una bola en cinco pedazos que se vuelven a ensamblar,
rotaciones y traslaciones, en \emph{dos} bolas del mismo
radio --- por lo que ni siquiera un invariante de rotación finitamente aditivo
Existe volumen en todos los subconjuntos de $\R^3$. Las piezas son, de
Por supuesto, no mensurable. La mensurabilidad no es burocrática
precaución; es el límite de la coherencia.
\end{remark}

\begin{method}\label{met:b3:measure:goodsets}
El \emph{principio de buenos conjuntos}: para demostrar que todos los conjuntos de
$\sigma(\mathcal E)$ tiene una propiedad, muestra que los conjuntos buenos
formar un álgebra $\sigma$ (o un sistema $\lambda$, si la propiedad
es teórico de medidas y $\mathcal E$ es un sistema $\pi$ ---
luego Dynkin) que contiene $\mathcal E$. Casi todas las pruebas de
Este capítulo y el siguiente son un ejemplo. Para probar dos \omterm{def:b3:measure:measure}{medidas}
igual: verifíquelos en un sistema generador $\pi$ más
$\sigma$-finitud (\cref{thm:b3:measure:uniqueness}). a
construir un \omterm{def:b3:measure:measure}{medida}: construir un \omterm{def:b3:measure:outer}{medida exterior} por portadas y cotización
Carathéodory.
\end{method}

\section{Ceremonias}

\begin{exercise}[$\star$]\label{exo:b3:measure:1}
(a) Demuestre que $\{A \subseteq X : A$ o $X\setminus A$ es
countable$\}$ es un álgebra $\sigma$: la generada por
solteros.
(b) Demuestre que $\mathcal B(\R)$ es generado por cada uno de: open
intervalos; intervalos cerrados; rayos $\intoc{-\infty}a$; rayos
con $a \in \Q$.
(c) ¿Es la familia de \emph{uniones finitas disjuntas de intervalos}?
$\intoc ab$ a $\sigma$-álgebra? Un álgebra (estable bajo
complemento y uniones finitas)?
\end{exercise}

\begin{exercise}[$\star$]\label{exo:b3:measure:2}
(a) Demuestre la inclusión-exclusión para un \omterm{def:b3:measure:measure}{medida} finito: $\mu(A\cup
B) = \mu(A) + \mu(B) - \mu(A\cap B)$, y el conjunto de tres
versión.
(b) Dé un ejemplo que muestre que \omterm{def:b3:topology:continuity}{continuidad} desde arriba
(\cref{prop:b3:measure:basics}(d)) falla sin el
supuesto de finitud.
(c) Demuestre que un conjunto contable tiene \omterm{def:b3:measure:lebesgueouter}{medida de lebesgue} cero.
Deduzca $\lambda(\Q) = 0$ y $\lambda(\intcc01\setminus\Q) =
1$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:measure:3}
Sea $\mu, \nu$ la probabilidad \omterm{def:b3:measure:measure}{medidas} de $\mathcal B(\R)$
con $\mu(\intoc{-\infty}t) = \nu(\intoc{-\infty}t)$ para todos
$t \in \R$. Mostrar $\mu = \nu$. (Esto hace que el
\emph{función de distribución} $F(t) = \mu(\intoc{-\infty}t)$
\omterm{def:b3:complete:complete}{completo} invariante --- la base de
\cref{ch:b3:probability}.)
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:measure:4}
(Borel--Cantelli, versión \omterm{def:b3:measure:measure}{medida}) Sea $(A_n)$ medible
con $\sum_n\mu(A_n) < \infty$ y $\limsup A_n =
\bigcap_N\bigcup_{n\geq N}A_n$ (los puntos pertenecientes a
infinitos $A_n$). Mostrar $\mu(\limsup A_n) = 0$.
Aplicación: para casi todos los $x \in \intcc01$, sólo de forma limitada
muchos $n$ satisfacen $\abs{x - p/q_n} \leq 4^{-n}$ para el
$n$-ésimo $p/q_n$ racional de una enumeración de
$\Q\cap\intcc01$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:measure:5}
(Conjunto Fat Cantor) Repita la construcción de Cantor en $\intcc01$,
pero en el paso $n$ elimine de cada uno de los intervalos $2^{n-1}$ un
\emph{centrado} intervalo abierto de longitud $4^{-n}$ únicamente. Mostrar
que el $K = \bigcap K_n$ resultante es \omterm{def:b3:topology:compact}{compacto}, tiene vacío
\omterm{def:b3:topology:interior}{interior} (no sobrevive ningún intervalo), y
\[
\lambda(K) = 1 - \sum_{n\geq1}2^{n-1}4^{-n} = \tfrac12 :
\]
a ninguna parte \omterm{def:b3:topology:interior}{conjunto denso} de \omterm{def:b3:measure:measure}{medida} $\frac12$. deducir un
\emph{\omterm{rem:b3:complete:meagre}{pobre}} subconjunto de $\intcc01$ del \omterm{def:b3:measure:measure}{medida} completo $1$ y un
subconjunto denso abierto de \omterm{def:b3:measure:measure}{medida} $< \varepsilon$.
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:measure:6}
Sea $\mu$ un \omterm{def:b3:measure:measure}{medida} en $\mathcal B(\R)$, invariante bajo
traducciones, con $c = \mu(\intoc01) < \infty$. Mostrar $\mu =
c\,\lambda$en$\mathcal B(\R)$. \emph{(Compute$\mu$ on dyadic intervals by dividing
$\intoc01$ into $2^n$ translates, then invoke
\cref{thm:b3:measure:uniqueness}.)}
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:measure:7}
(Aproximación) Sea $A \in \mathcal L$ con $\lambda(A) <
\infty$y$\varepsilon > 0$. Mostrar que hay un \emph{finito}
unión de intervalos $B$ con $\lambda(A\,\triangle\,B) <
\varepsilon$($\triangle$ = diferencia simétrica).
\emph{(Regularity: squeeze $K \subseteq A \subseteq U$ and use
the structure of the open $U$ as a countable union of
intervals, plus compactness of $K$.)}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b3:measure:8}
(Steinhaus) Sea $A \in \mathcal L$ con $\lambda(A) > 0$. Mostrar
que $A - A = \{x - y : x, y \in A\}$ contiene un intervalo
alrededor de $0$.
\emph{(Reduce to $\lambda(A) < \infty$; by
\cref{exo:b3:measure:7}-style regularity, find an interval $I$
with $\lambda(A \cap I) > \frac34\ell(I)$; then for $\abs t <
\frac12\ell(I)$, the sets$A\cap I$and$(A\cap I) + t$ both
sit in an interval of length $\frac32\ell(I)$ and have total
\omterm{def:b3:measure:measure}{medida} $> \frac32\ell(I)$: they must intersect.)}
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b3:measure:9}
Demuestre que cada $A \in \mathcal L$ con $\lambda(A) > 0$
contiene un subconjunto no mensurable. \emph{(Intersect $A$ with the
translates $V + q$ of Vitali's set: if all $A \cap (V+q)$ were
measurable, each would be null by the argument of
\cref{thm:b3:measure:vitali} --- Steinhaus
(\cref{exo:b3:measure:8}) helps: a measurable set of positive
\omterm{def:b3:measure:measure}{medida} inside $V + q$ would give $(V+q) - (V+q) \supseteq$
an interval, contradicting that this difference set meets $\Q$
only at $0$; conclude with subadditivity.)}
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:measure:10}
Demuestre que $A \subseteq \R$ con $\lambda^*(A) < \infty$ es
Lebesgue mensurable si por cada $\varepsilon > 0$ hay un
abra $U \supseteq A$ con $\lambda^*(U \setminus A) <
\varepsilon$, si hay un$G_\delta$, configure$G \supseteq A$
con $\lambda^*(G\setminus A) = 0$. (Así que los conjuntos de Lebesgue son
Borel establece conjuntos de módulo nulo).
\end{exercise}

\begin{exercise}[$\star\star$]\label{exo:b3:measure:11}
(\omterm{def:b3:topology:continuity}{Continuidad} a lo largo de los límites monótonos y su nitidez)
(a) Demuestre que para conjuntos medibles, $\mu(\liminf A_n) \leq
\liminf\mu(A_n)$ (Fatou para conjuntos), y que si
$\mu\bigl(\bigcup A_n\bigr) < \infty$, también
$\limsup\mu(A_n) \leq \mu(\limsup A_n)$.
(b) Exhibir, para \omterm{def:b3:measure:lebesgueouter}{medida de lebesgue} en $\R$, una secuencia con
$\mu(A_n) = 1$ para todos los $n$ aún $\mu(\limsup A_n) = 0$: el
La hipótesis de finitud en la segunda desigualdad no es
decorativo.
(c) Deducir: si $\sum\mu(A_n) < \infty$ entonces
$\mu(\limsup A_n) = 0$ (Borel--Cantelli nuevamente), y si el
$A_n$ aumenta o disminuye (con $\mu(A_1) < \infty$ en el
caso decreciente), $\mu(\lim A_n) = \lim\mu(A_n)$.
\end{exercise}

\begin{exercise}[$\star\star\star$]\label{exo:b3:measure:12}
(Teorema de Egorov) Sean $\mu(X) < \infty$ y $f_n \to f$
puntualmente, todo medible (de valor real). Para $k, N \geq 1$
conjunto
\[
E_{k,N} = \bigcap_{n \geq N}\Bigl\{x : \abs{f_n(x) - f(x)}
\leq \tfrac1k\Bigr\} .
\]
(a) Muestre que para $k$ fijo, $E_{k,N} \nearrow X$ como $N \to
\infty$y deduzca$N_k$con$\mu(X \setminus E_{k,N_k})
\leq \varepsilon2^{-k}$.
(b) Concluya \emph{teorema de egorov}: por cada $\varepsilon
> 0$hay un$A$medible con$\mu(X\setminus A) \leq
\varepsilon$tal que$f_n \to f$\emph{uniformemente en$A$}
--- la convergencia puntual es una convergencia uniforme a partir de un
conjunto arbitrariamente pequeño.
(c) Demuestre que el teorema falla en $(\R, \lambda)$: el movimiento
Los golpes $f_n = \mathbf 1_{\intcc n{n+1}}$ convergen puntualmente.
a $0$ pero uniformemente sin complemento de un \omterm{def:b3:measure:measure}{medida finita}
conjunto. ¿Dónde (a) usó $\mu(X) < \infty$?
\end{exercise}

\section{Problema: la escalera Cantor-Vitali y un
conjunto medible que no es Borel}

\begin{omfigure}
\begin{tikzpicture}[scale=5.2]
  \draw[->] (0,0) -- (1.06,0) node[right, font=\small] {$x$};
  \draw[->] (0,0) -- (0,1.06);
  \draw[black!30] (1,0) -- (1,1) -- (0,1);
  \draw[omThm, thick]
    (0,0)
    -- (0.037,0.125) -- (0.074,0.125)
    -- (0.111,0.25)  -- (0.222,0.25)
    -- (0.259,0.375) -- (0.296,0.375)
    -- (0.333,0.5)   -- (0.667,0.5)
    -- (0.704,0.625) -- (0.741,0.625)
    -- (0.778,0.75)  -- (0.889,0.75)
    -- (0.926,0.875) -- (0.963,0.875)
    -- (1,1);
  \node[font=\small, omThm] at (0.32,0.86)
    {$c$ (depth-3 approximation)};
\end{tikzpicture}

{\small La escalera Cantor--Vitali: constante en cada hueco de
el conjunto de Cantor, aún subiendo de $0$ a $1$ \omterm{def:b3:topology:continuity}{continuamente}. su
derivado desaparece casi en todas partes --- toda la escalada
sucede en un conjunto nulo.}
\end{omfigure}

\begin{problem}[{Problema del fin de semana --- la escalera del diablo,
y$\mathcal B(\R) \subsetneq \mathcal L$}]
\label{pb:b3:measure:1}
Construimos el \emph{Cantor--función Vitali} (diablo
escalera), utilizarlo para transportar \omterm{def:b3:measure:measure}{medida} patológicamente, y
concluir con un teorema que ningún argumento blando ofrece: hay
Existen conjuntos medibles según Lebesgue que no son Borel. Notación:
$C$ es el conjunto de Cantor, $C_n$ su etapa $n$ (intervalos $2^n$
de longitud $3^{-n}$), y cada $x \in C$ tiene dígitos ternarios
$x = \sum 2b_n3^{-n}$, $b_n \in \{0,1\}$
(\cref{exo:b3:topology:10}).

\medskip
\noindent\textbf{Parte I --- The staircase.} Definir $c_0(x) = x$
y $c_{n+1}$ de $c_n$ por
\[
c_{n+1}(x) = \begin{cases}
\tfrac12\,c_n(3x) & 0 \leq x \leq \tfrac13,\\[2pt]
\tfrac12 & \tfrac13 \leq x \leq \tfrac23,\\[2pt]
\tfrac12 + \tfrac12\,c_n(3x - 2) & \tfrac23 \leq x \leq 1.
\end{cases}
\]
\begin{enumerate}
  \item Demuestre que cada $c_n$ es \omterm{def:b3:topology:continuity}{continuo}, no decreciente, con
        $c_n(0) = 0$, $c_n(1) = 1$, y que
        $\norm{c_{n+1} - c_n}_\infty \leq
        \tfrac12\norm{c_n - c_{n-1}}_\infty$.
  \item Deduzca que $(c_n)$ converge uniformemente a un \omterm{def:b3:topology:continuity}{continuo}
        $c$ no decreciente con $c(0) = 0$, $c(1) = 1$
        (\emph{el} Cantor--función Vitali), que satisface
        las mismas relaciones autosemejantes que el $c_{n+1}$ anterior.
  \item Demuestre que $c$ es constante en cada \omterm{def:b3:topology:components}{componente conectado}
        de $\intcc01\setminus C$, y el de $x = \sum_n
        2b_n3^{-n} \in C$,$c(x) = \sum_n b_n2^{-n}$: el
        escalera lee dígitos de Cantor en binario
        (Función \cref{pb:b3:topology:1} $g$, hecha monótona
        y mundial).
  \item Deduzca que $c$ es diferenciable, con $c' = 0$, en
        cada punto de $\intcc01\setminus C$: $c' = 0$
        \emph{$\lambda$-almost everywhere}
        (\cref{ex:b3:measure:cantor}). Concluir que el
        El teorema fundamental del cálculo falla para $c$:
        \[
        c(1) - c(0) = 1 \neq 0 = \int_0^1 c'(t)\,\dd t
        \]
        (la integral está sobre el conjunto \omterm{def:b3:measure:measure}{medida completa} donde
        $c' = 0$; anticipando \cref{ch:b3:lebesgue}, conjuntos nulos
        no afecta a las integrales). ¿Qué hipótesis de la
        $\mathcal C^1$ ¿Se viola el teorema fundamental?
  \item Mostrar que $c(C) = \intcc01$: el conjunto nulo $C$ está mapeado
        \emph{sobre} un conjunto de \omterm{def:b3:measure:measure}{medida} completo.
\end{enumerate}

\medskip
\noindent\textbf{Parte II --- The crooked \omterm{def:b3:topology:continuity}{homeomorfismo}.} Deja
$h(x) = \frac{c(x) + x}{2}$.
\begin{enumerate}[resume]
  \item Demuestre que $h \colon \intcc01 \to \intcc01$ es un
        \omterm{def:b3:topology:continuity}{homeomorfismo} (estrictamente creciente, \omterm{def:b3:topology:continuity}{continuo},
        sobreyectivo).
  \item Demuestre que $\lambda\bigl(h(\intcc01\setminus C)\bigr) =
        \tfrac12$: en cada espacio de longitud$\ell$,$h$ actúa como
        mapa afín de la pendiente $\tfrac12$, y los espacios tienen total
        longitud $1$.
  \item Deducir $\lambda\bigl(h(C)\bigr) = \tfrac12$: el
        La imagen \omterm{def:b3:topology:continuity}{homeomórfico} de un conjunto nulo puede tener resultados positivos.
        \omterm{def:b3:measure:measure}{medida}. (¿Dónde contradice esto la intuición ingenua?
        sobre el ``tamaño''?)
\end{enumerate}

\medskip
\noindent\textbf{Parte III --- A measurable set that is not
Borel.}
\begin{enumerate}[resume]
  \item Por \cref{exo:b3:measure:9}, elija un $W
        \subseteq h(C)$no medible. Muestra que$Z = h^{-1}(W) \subseteq C$
        es medible según Lebesgue. \emph{(It is a subset of a null
        set; \omterm{def:b3:complete:complete}{lo completo}, \cref{thm:b3:measure:caratheodory}.)}
  \item Demuestre que la preimagen de un Borel colocada bajo un
        \omterm{def:b3:topology:continuity}{aplicación continua} es Borel. \emph{(Good sets principle:
        $\{B : h^{-1}(B) \in \mathcal B\}$ is a
        $\sigma$-algebra containing the \omterm{def:b3:topology:topology}{conjuntos abiertos} --- mind the
        direction of the map.)}
  \item Concluir que $Z$ es \emph{no} Borel: si lo fuera,
        $W = (h^{-1})^{-1}(Z)$ sería Borel (aplicar pregunta
        10 al \omterm{def:b3:topology:continuity}{continuo} $h^{-1}$), por lo tanto mensurable ---
        contradicción. Por lo tanto
        \[
        \boxed{\ \mathcal B(\R) \subsetneq \mathcal L\ }
        \]
        y \omterm{def:b3:complete:complete}{lo completo} realmente amplía el mundo Borel.
  \item Exhibe una función medible de Lebesgue $g$ y una
        \omterm{def:b3:topology:continuity}{continuo} función $\varphi$ tal que $g \circ
        \varphi$ no es medible según Lebesgue: mensurabilidad,
        a diferencia de \omterm{def:b3:topology:continuity}{continuidad}, no compone. \emph{(Take $g =
        \mathbf 1_Z$and$\varphi = h^{-1}$, anticipating the
        definition of measurable functions from
        \cref{ch:b3:lebesgue}: preimages of Borel sets are
        Lebesgue sets. Where must one be careful about which
        $\sigma$-algebra is used on the target?)}
\end{enumerate}

\medskip
\noindent\textbf{Parte IV --- Epilogue.}
\begin{enumerate}[resume]
  \item Ordene las siguientes clases por inclusión estricta y
        Justifique cada rigor con un ejemplo de este
        capítulo y su problema: conjuntos contables; Borel nulo
        conjuntos; Conjuntos nulos de Lebesgue; Conjuntos de Borel; Conjuntos de Lebesgue;
        conjuntos arbitrarios.
\end{enumerate}

\medskip
\noindent\textbf{Part V --- The Cantor \omterm{def:b3:measure:measure}{medida}: mass on a null
set.} La escalera es la función de distribución de una
notable \omterm{def:b3:measure:measure}{medida}, que ahora construimos con el propio contenido de este capítulo.
herramientas.
\begin{enumerate}[resume]
  \item (Lebesgue--Stieltjes, existencia) Let $F\colon\R\to\R$
        ser no decreciente, \omterm{def:b3:topology:continuity}{continuo}, acotado. En medio abierto
        Los intervalos definen $\rho\bigl(\intoc ab\bigr) = F(b) -
        F(a)$y, para$A \subseteq \R$,
        \[
        \mu_F^*(A) = \inf\Bigl\{\sum_k\bigl(F(b_k) -
        F(a_k)\bigr) : A \subseteq
        \bigcup_k\intoc{a_k}{b_k}\Bigr\} .
        \]
        Demuestre que $\mu_F^*$ es un \omterm{def:b3:measure:outer}{medida exterior} y que
        $\mu_F^*\bigl(\intoc ab\bigr) = F(b) - F(a)$
        \emph{(imitate the compactness argument of
        \cref{thm:b3:measure:lebesgue}, enlarging each
        $\intoc{a_k}{b_k}$ to an open interval at
        $F$-cost $\leq \varepsilon2^{-k}$ --- where is
        \omterm{def:b3:topology:continuity}{continuidad} of $F$ used?)}.
  \item Demuestre que cada conjunto de Borel es
        $\mu_F^*$-medible en el sentido Carathéodory
        \emph{(as in the Lebesgue case, it suffices to test
        half-lines; follow the proof of
        \cref{thm:b3:measure:caratheodory}'s application)}, entonces
        que $\mu_F = \mu_F^*$ restringido a $\mathcal B(\R)$
        es un \omterm{def:b3:measure:measure}{medida} con $\mu_F(\intoc ab) = F(b) - F(a)$:
        el \emph{Lebesgue--Stieltjes \omterm{def:b3:measure:measure}{medida}} de $F$.
  \item Aplicar esto a la escalera ($F = c$ ampliado por $0$
        en $\R_-$ y $1$ en $\intco1\infty$): el
        \emph{Cantor \omterm{def:b3:measure:measure}{medida}}$\mu$. Mostrar $\mu(\R) = 1$, que
        cada espacio del conjunto de Cantor es $\mu$-null ($c$ es
        constante allí), y concluir
        \[
        \mu(C) = 1, \qquad \lambda(C) = 0 :
        \]
        $\mu$ y $\lambda$ viven en transportistas separados ($C$ y
        su complemento). Dos \omterm{def:b3:measure:measure}{medidas} en esta posición son
        llamado \emph{mutuamente singulares}, escrito $\mu \perp
        \lambda$.
  \item Demuestre que $\mu$ no tiene átomos: $\mu(\{x\}) = 0$ para
        cada $x$ \emph{(\omterm{def:b3:topology:continuity}{continuidad} of $c$)}. Un sin átomo
        probabilidad \omterm{def:b3:measure:measure}{medida} llevada por un Lebesgue-null \omterm{def:b3:topology:compact}{compacto}:
        compárelo con el único \omterm{def:b3:measure:measure}{medidas} visto hasta ahora.
  \item (Lanzamiento de moneda disfrazado) Por una palabra $(\varepsilon_1,
        \dots, \varepsilon_m) \in \{0,1\}^m$, dejemos
        $C_{\varepsilon}$ sea el conjunto de $x \in C$ cuyo ternario
        los dígitos satisfacen $b_i(x) = \varepsilon_i$ para $i \leq m$
        (una de las piezas de Cantor $2^m$ de profundidad $m$). Mostrar
        $\mu(C_\varepsilon) = 2^{-m}$\emph{(the staircase
        climbs $2^{-m}$ across that piece: use Part I,
        question 3)}. El Cantor \omterm{def:b3:measure:measure}{medida} es la ley de un
        secuencia infinita de lanzamientos de monedas justos leídos en ternario
        --- \cref{ch:b3:probability} hará que esto sea exacto.
  \item Demuestre la autosimilitud: para cada Borel $A$,
        \[
        \mu(A) = \tfrac12\,\mu(3A) +
        \tfrac12\,\mu(3A - 2) ,
        \]
        donde $3A - 2 = \{3x - 2 : x \in A\}$ \emph{(check it
        on the generating intervals $\intoc ab$ via the
        self-similar relations of $c$, then invoke uniqueness,
        \cref{thm:b3:measure:uniqueness})}.
  \item Demuestre que el reflejo $s(x) = 1 - x$ preserva
        $\mu$: $\mu(s(A)) = \mu(A)$ \emph{(a través de $c(1 - x) = 1 -
        c(x)$, que se sigue de la simetría de la
        construcción --- pruébalo)}.
  \item Calcula los dos primeros momentos de $\mu$, es decir\ de a
        punto aleatorio $X$ con ley $\mu$ (las integrales pueden ser
        manejados como límites de sumas sobre las piezas de profundidad-$m$,
        anticipando \cref{ch:b3:lebesgue}): la simetría da
        $\int x\,\dd\mu = \frac12$, y la autosimilitud da
        \[
        \int x^2\,\dd\mu = \frac38,
        \qquad\text{hence}\qquad
        \operatorname{Var}(X) = \frac18 .
        \]
        Comparar con la ley uniforme sobre $\intcc01$ (variación
        $\frac1{12}$): la masa de Cantor, empujada hasta los bordes,
        se propaga \emph{más}.
  \item Demuestre que el (\omterm{def:b3:topology:topology}{topológico}) \emph{apoyo} de $\mu$
        --- el conjunto cerrado más pequeño de \omterm{def:b3:measure:measure}{medida} completo --- es
        exactamente $C$.
  \item (Síntesis) La escalera $c$ es \omterm{def:b3:topology:continuity}{continuo} y
        no decreciente pero no cumple el teorema fundamental de
        cálculo (Parte I); el \omterm{def:b3:measure:measure}{medida} $\mu_c$ es un
        probabilidad, sin átomo, singular con respecto a
        $\lambda$. Explica en un breve párrafo cómo se
        dos caras de un fenómeno, y enunciar el general
        moral: funciones no decrecientes corresponden a \omterm{def:b3:measure:measure}{medidas}
        ($F \leftrightarrow \mu_F$), diferenciabilidad a.e.\
        corresponde a la ``parte absolutamente \omterm{def:b3:topology:continuity}{continuo}'', y
        $c$ es el testigo estándar que un \omterm{def:b3:topology:continuity}{continuo} $F$ puede
        llevar \emph{no} absolutamente \omterm{def:b3:topology:continuity}{continuo} parte en absoluto.
  \item (El módulo agudo de \omterm{def:b3:topology:continuity}{continuidad}) Sea $s =
        \frac{\ln 2}{\ln 3}$. Mostrar que$c$ es Hölder
        \omterm{def:b3:topology:continuity}{continuo} del exponente $s$:
        \[
        \abs{c(x) - c(y)} \leq 4\,\abs{x - y}^{s}
        \qquad (x, y \in \intcc01),
        \]
        y que ningún exponente $t > s$ puede funcionar, ni siquiera localmente.
        Deduzca la forma teórica de la medida: para cada $x$ y
        cada $r \in \intoc01$,
        \[
        \mu\bigl(\intcc{x - r}{x + r}\bigr) \leq 8\,r^{s} .
        \]
        \emph{(Compare a depth-$m$ triadic grid with the scale
        of $\abs{x - y}$; question 18 gives the climb across
        each piece. The exponent $s$ is the \omterm{def:b3:topology:hausdorff}{Hausdorff} dimension
        of $C$, as later courses will say.)}
  \item (La autosimilitud caracteriza a $\mu$) Demuestre la
        recíproco de la pregunta 19: si $\nu$ es una probabilidad
        \omterm{def:b3:measure:measure}{medida} en $\mathcal B(\R)$ transportado por $\intcc01$ y
        satisfactorio
        \[
        \nu(A) = \tfrac12\,\nu(3A) + \tfrac12\,\nu(3A - 2)
        \qquad (A \in \mathcal B(\R)),
        \]
        luego $\nu = \mu$. \emph{(Iterate the relation $m$
        times to spread $\nu$ over the $2^m$ depth-$m$ Cantor
        pieces, estimate $\nu(\intoc ab)$ against the count of
        pieces inside $\intoc ab$, and let $m \to \infty$;
        finish with \cref{thm:b3:measure:uniqueness}.)}
\end{enumerate}
\end{problem}