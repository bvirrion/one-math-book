\section*{Chapitre \ref{ch:b3:topology} --- Topologie générale}

\begin{solution}{exo:b3:topology:1}
(a) Quatre \omterm{def:b3:topology:topology}{topologies} sur $\{a,b\}$~: indiscrète
$\{\varnothing, X\}$~; discrète~; les deux \omterm{def:b3:topology:topology}{topologies} de
\emph{Sierpiński} $\{\varnothing, \{a\}, X\}$ et
$\{\varnothing, \{b\}, X\}$. Les deux dernières sont
homéomorphes (échanger $a, b$)~: trois classes. \omterm{def:b3:topology:connected}{Connexes}~:
toutes sauf la discrète (seule la \omterm{def:b3:topology:topology}{topologie} discrète contient
un clopen propre non vide). \omterm{def:b3:topology:hausdorff}{Séparées}~: seule la discrète (dans
les autres, le seul \omterm{def:b3:topology:topology}{voisinage} de $b$ est $X$, ou celui de
$a$).

(b) $\Q$~: intérieur $\varnothing$ (tout intervalle contient
des irrationnels), \omterm{def:b3:topology:interior}{adhérence} $\R$ (densité), frontière $\R$.
$[0,1) \cup \{2\}$~: intérieur $(0,1)$, \omterm{def:b3:topology:interior}{adhérence}
$[0,1]\cup\{2\}$, frontière $\{0, 1, 2\}$. $\{1/n\}$~:
intérieur $\varnothing$, \omterm{def:b3:topology:interior}{adhérence} $\{1/n\}\cup\{0\}$,
frontière l'\omterm{def:b3:topology:interior}{adhérence} elle-même.
\end{solution}

\begin{solution}{exo:b3:topology:2}
(a) Tout \omterm{def:b3:topology:topology}{ouvert} $V \subseteq Y$ est une réunion $\bigcup B_j$
d'ensembles de \omterm{def:b3:topology:basis}{base}, et $f^{-1}(V) = \bigcup f^{-1}(B_j)$~: si
ces derniers sont \omterm{def:b3:topology:topology}{ouverts}, le premier l'est~; la réciproque
est triviale.

(b) $f^{-1}\bigl(\intoo{\frac12}{\frac32}\bigr) = [0, \infty)$
n'est pas \omterm{def:b3:topology:topology}{ouvert}~: $f$ n'est pas \omterm{def:b3:topology:continuity}{continue}~; la continuité à
droite en chaque point est claire ($f$ est localement
constante à droite de tout point). Les ensembles $[a, b)$
satisfont le critère de \omterm{def:b3:topology:basis}{base}
(\cref{def:b3:topology:basis})~: $[a,b)\cap[c,d) =
[\max(a,c), \min(b,d))$. La \omterm{def:b3:topology:topology}{topologie} de Sorgenfrey est plus
fine que l'usuelle, car $(a, b) = \bigcup_{a < c < b}[c,
b)$~; strictement~: $[0,1)$ est \omterm{def:b3:topology:topology}{ouvert} pour Sorgenfrey, pas
pour l'usuelle. Continuité depuis la droite de Sorgenfrey en
$x$~: un \omterm{def:b3:topology:topology}{voisinage} de Sorgenfrey de $x$ contient un ensemble
de \omterm{def:b3:topology:basis}{base} $[a, b) \ni x$, donc contient $[x, b)$ --- la
condition de continuité se lit~: pour tout $\varepsilon > 0$
il existe $\delta > 0$ avec $\abs{f(t) - f(x)} < \varepsilon$
dès que $x \leq t < x + \delta$. C'est exactement la
continuité à droite en $x$.
\end{solution}

\begin{solution}{exo:b3:topology:3}
Deux \omterm{def:b3:topology:topology}{ouverts} non vides ont des complémentaires finis, donc
leur intersection a un complémentaire fini~: non vide
(l'ensemble est infini) --- aucun couple de points n'a de
\omterm{def:b3:topology:topology}{voisinages} disjoints~: non \omterm{def:b3:topology:hausdorff}{séparé}, donc non métrisable
(\cref{def:b3:topology:hausdorff}). Soit $(x_n)$ injective et
$x$ arbitraire~: un \omterm{def:b3:topology:topology}{voisinage} de $x$ contient un \omterm{def:b3:topology:topology}{ouvert}
cofini $U$~; les points finiment nombreux de $X\setminus U$
sont atteints par au plus un nombre fini d'indices
(injectivité), donc une queue de la suite est dans $U$~:
$x_n \to x$, pour \emph{tout} $x$. La preuve d'unicité a
besoin de deux \omterm{def:b3:topology:topology}{voisinages} disjoints pour séparer deux
limites alléguées --- précisément ce qui échoue ici.
\end{solution}

\begin{solution}{exo:b3:topology:4}
(a) La continuité est par construction
(\cref{def:b3:topology:constructions}). Ouverture~: un \omterm{def:b3:topology:topology}{ouvert}
$W \subseteq X \times Y$ est réunion de pavés $U_i\times
V_i$, et $\pi_X(W) = \bigcup U_i$ (les pavés non vides se
projettent sur leurs facteurs), \omterm{def:b3:topology:topology}{ouvert}. Pas fermée~: $H =
\{(x,y) : xy = 1\}$ est fermé dans $\R^2$ (image réciproque de
$\{1\}$ sous le produit continu), mais $\pi_X(H) =
\R\setminus\{0\}$ n'est pas fermé.

(b) ($\Rightarrow$) Les projections sont \omterm{def:b3:topology:continuity}{continues}.
($\Leftarrow$) Soit $x^{(k)} \to x$ composante par composante
et $W = \prod U_n$ un \omterm{def:b3:topology:topology}{voisinage} de \omterm{def:b3:topology:basis}{base} de $x$, avec $U_n =
X_n$ sauf pour $n \in F$ fini. Pour chaque $n \in F$,
$x_n^{(k)} \in U_n$ pour $k \geq K_n$~; pour $k \geq \max_F
K_n$, $x^{(k)} \in W$. La formule $d$ définit une métrique
(chaque somme est une, à la vérification standard près que
$\min(1, d_n)$ l'est)~; ses boules~: $B_d(x, 2^{-N})$ contient
le pavé de \omterm{def:b3:topology:basis}{base} $\{y : d_n(x_n,y_n) < 2^{-N},\ n \leq N'\}$
pour $N'$ convenable (la queue $\sum_{n > N'} 2^{-n}$ est
petite), et réciproquement tout pavé de \omterm{def:b3:topology:basis}{base} contient une
$d$-boule~: les deux \omterm{def:b3:topology:topology}{topologies} ont les mêmes \omterm{def:b3:topology:topology}{voisinages} de
chaque point.

(c) Pour la \omterm{def:b3:topology:constructions}{topologie produit}, $x^{(k)} \to 0$ par (b).
L'\omterm{def:b3:topology:topology}{ouvert} boîte $U = \prod_n \intoo{-\tfrac1n}{\tfrac1n}$
contient $0$, mais $x^{(k)} \notin U$ pour tout $k$ (la
$n$-ième coordonnée $1/k \geq 1/n$ quand $n \geq k$)~: aucune
queue n'entre dans $U$. La \omterm{def:b3:topology:topology}{topologie} boîte est strictement
plus fine et n'est pas une \omterm{def:b3:topology:topology}{topologie} de type produit pour la
convergence.
\end{solution}

\begin{solution}{exo:b3:topology:5}
$\varphi \colon \R \to S^1$, $x \mapsto \eu^{2\iu\pi x}$, est
\omterm{def:b3:topology:continuity}{continue}, surjective, et constante sur les classes modulo
$\Z$~: elle induit une bijection \omterm{def:b3:topology:continuity}{continue} $\bar\varphi \colon
\R/\Z \to S^1$ (\cref{prop:b3:topology:universal}(b)). La
projection $\pi$ est \emph{ouverte}~: pour $U \subseteq \R$
\omterm{def:b3:topology:topology}{ouvert}, $\pi^{-1}(\pi(U)) = \bigcup_{n}(U + n)$ est \omterm{def:b3:topology:topology}{ouvert},
donc $\pi(U)$ est \omterm{def:b3:topology:topology}{ouvert}. \omterm{def:b3:topology:hausdorff}{Séparé}~: soit $\bar x \neq \bar
y$~; choisir des représentants avec $\delta =
\min_{n}\abs{x - y - n} > 0$~; les images des intervalles de
rayon $\delta/3$ autour de $x$ et $y$ sont \omterm{def:b3:topology:topology}{ouvertes}
(ouverture de $\pi$), contiennent $\bar x, \bar y$, et sont
disjoints (deux points antécédents seraient à moins de
$2\delta/3$ l'un de l'autre mod $\Z$). \omterm{def:b3:topology:compact}{Compact}~: $\R/\Z =
\pi([0,1])$, image \omterm{def:b3:topology:continuity}{continue} d'un \omterm{def:b3:topology:compact}{espace compact}
(\cref{thm:b3:topology:compactprops}(2)). Maintenant
$\bar\varphi$ est une bijection \omterm{def:b3:topology:continuity}{continue} d'un \omterm{def:b3:topology:compact}{espace compact}
vers le \omterm{def:b3:topology:hausdorff}{séparé} $S^1$~: un \omterm{def:b3:topology:continuity}{homéomorphisme}
(\cref{cor:b3:topology:compacthomeo}).

Pour (iii)~: le composé $[0,1] \hookrightarrow \R
\xrightarrow{\pi} \R/\Z$ est continu, surjectif, et identifie
exactement $0 \sim 1$~: il induit une bijection \omterm{def:b3:topology:continuity}{continue}
$[0,1]/(0\sim1) \to \R/\Z$ d'un \omterm{def:b3:topology:compact}{espace compact} (image \omterm{def:b3:topology:continuity}{continue}
de $[0,1]$ sous la projection quotient) vers un \omterm{def:b3:topology:hausdorff}{espace
séparé}~: un \omterm{def:b3:topology:continuity}{homéomorphisme}. En composant~: les trois espaces
sont homéomorphes.
\end{solution}

\begin{solution}{exo:b3:topology:6}
(a) Un recouvrement de $K_1 \cup \dots \cup K_m$ se restreint
en un recouvrement de chaque $K_j$~: un nombre fini d'\omterm{def:b3:topology:topology}{ouverts}
par morceau suffit. Une intersection $\bigcap K_i$ est fermée
dans le \omterm{def:b3:topology:compact}{compact} $K_{i_0}$ (les \omterm{def:b3:topology:compact}{compacts} sont fermés dans
l'espace métrique ambiant), donc compacte.

(b) $x \mapsto d(x, F)$ est \omterm{def:b3:topology:continuity}{continue} ($1$-lipschitzienne) et
strictement positive sur $K$ ($d(x, F) = 0$ signifie $x \in
\bar F = F$)~; sur le \omterm{def:b3:topology:compact}{compact} $K$ elle atteint un minimum $m >
0$~: $d(K, F) \geq m$. Contre-exemple sans compacité~: $F_1 =
\{n : n \geq 2\}$ et $F_2 = \{n + \frac1n : n \geq 2\}$ sont
fermés, disjoints, à distance $\inf_n \frac1n = 0$.

(c) Si $X$ est \omterm{def:b3:topology:compact}{compact}, toute $f$ \omterm{def:b3:topology:continuity}{continue} est bornée
(\cref{thm:b3:topology:compactprops}(2)). Réciproquement, si
$X$ n'est pas \omterm{def:b3:topology:compact}{compact}, prendre $(x_n)$ sans sous-suite
convergente (\cref{thm:b3:topology:metriccompact})~; en
passant à une sous-suite on peut supposer les $x_n$ deux à
deux distincts, et aucun point de $X$ n'est point
d'accumulation de la suite, donc
\[
r_n = \min\Bigl(2^{-n},\ \tfrac13\,d\bigl(x_n, \{x_m : m \neq
n\}\bigr)\Bigr) > 0,
\]
et les boules $B(x_n, r_n)$ sont deux à deux disjointes (un
point commun des $n$-ième et $m$-ième donnerait $d(x_n, x_m) <
r_n + r_m \leq \frac23 d(x_n, x_m)$). Définir
\[
f(x) = \sum_{n} n\,\max\Bigl(0,\ 1 - \frac{d(x,
x_n)}{r_n}\Bigr):
\]
en chaque $x$ au plus un terme est non nul, et $f(x_n) = n$.
Continuité en $x$~: soit $y_k \to x$~; chaque valeur $f(y_k)$
est soit $0$ soit une valeur de bosse unique
$\mathrm{bump}_{n_k}(y_k)$. Si quelque indice $n$ apparaît
infiniment souvent, le long de cette sous-suite $f(y_k) =
\mathrm{bump}_n(y_k) \to \mathrm{bump}_n(x)$, qui égale
$f(x)$ (si $x \in B(x_n, r_n)$, toute la queue est dans
cette boule ouverte et aucun autre indice n'apparaît~; si $x
\notin B(x_n, r_n)$ alors $\mathrm{bump}_n(x) = 0 = f(x)$,
puisque $x$, adhérent à la $n$-ième boule, n'est dans aucune
autre boule ouverte). Si $n_k \to \infty$~: $d(y_k, x_{n_k})
< r_{n_k} \leq 2^{-n_k} \to 0$, donc $x_{n_k} \to x$, faisant
de $x$ un point d'accumulation de $(x_n)$ --- exclu~; ce cas
ne concerne donc qu'un nombre fini de $k$. Dans tous les cas
$f(y_k) \to f(x)$~: $f$ est \omterm{def:b3:topology:continuity}{continue}, et non bornée.
\end{solution}

\begin{solution}{exo:b3:topology:7}
Supposons qu'aucun $\delta$ ne marche~: pour chaque $n$ il
existe $A_n$ de diamètre $< 1/n$ \omterm{def:b3:rings:content}{contenu} dans aucun $U_i$~;
choisir $x_n \in A_n$. Par compacité
(\cref{thm:b3:topology:metriccompact}), une sous-suite
$x_{n_k} \to x$~; choisir $i$ et $r > 0$ avec $B(x, r)
\subseteq U_i$. Pour $k$ grand~: $d(x_{n_k}, x) < r/2$ et
$1/n_k < r/2$, donc $A_{n_k} \subseteq B(x_{n_k}, 1/n_k)
\subseteq B(x, r) \subseteq U_i$ --- contradiction. Heine~:
étant donné $\varepsilon$, recouvrir $Y$ par des boules
$B(y, \varepsilon/2)$~; les images réciproques forment un
recouvrement \omterm{def:b3:topology:topology}{ouvert} de $X$~; soit $\delta$ un nombre de
Lebesgue~: si $d(x, x') < \delta$, la paire $\{x, x'\}$ a un
diamètre $< \delta$, est dans une image réciproque, et
$d(f(x), f(x')) < \varepsilon$.
\end{solution}

\begin{solution}{exo:b3:topology:8}
(a) $GL_n(\R) = \det^{-1}(\R\setminus\{0\})$ est \omterm{def:b3:topology:topology}{ouvert}
($\det$ est polynomiale, \omterm{def:b3:topology:continuity}{continue}). Disconnexe~: $\det$
l'envoie sur $\R\setminus\{0\}$, et un \omterm{def:b3:topology:connected}{espace connexe} a des
images \omterm{def:b3:topology:continuity}{continues} \omterm{def:b3:topology:connected}{connexes}
(\cref{thm:b3:topology:connectedbasics}(2))~;
$\R\setminus\{0\}$ n'est pas un intervalle. $GL_n(\C)$~: pour
$A, B$ inversibles, $p(\lambda) = \det\bigl((1-\lambda)A +
\lambda B\bigr)$ est un polynôme en $\lambda$ avec $p(0),
p(1) \neq 0$, donc non identiquement nul~: il a un nombre
fini de racines dans $\C$. Le plan privé d'un nombre fini de
points est \omterm{def:b3:topology:pathconnected}{connexe par arcs} (contourner les points), donc il
existe un chemin $\lambda(t)$ de $0$ à $1$ avec
$p(\lambda(t)) \neq 0$~: $t \mapsto (1-\lambda(t))A +
\lambda(t)B$ est un chemin dans $GL_n(\C)$.

(b) $\det(A + \varepsilon I) = \chi_A(-\varepsilon)\cdot(\pm1)$
s'annule pour au plus $n$ valeurs de $\varepsilon$~: des
matrices inversibles $A + \varepsilon I \to A$ quand
$\varepsilon \to 0$~: densité.
\end{solution}

\begin{solution}{exo:b3:topology:9}
(a) Soit $A \subseteq \Q$ contenant $p < q$ et choisir un
irrationnel $\alpha \in (p, q)$~: $A = (A \cap (-\infty,
\alpha)) \sqcup (A\cap(\alpha, +\infty))$ scinde $A$ en deux
morceaux non vides relativement \omterm{def:b3:topology:topology}{ouverts}~: $A$ est disconnexe.
Les composantes sont des singletons. La compacité locale
échoue~: un \omterm{def:b3:topology:topology}{voisinage} \omterm{def:b3:topology:compact}{compact} $V$ de $0$ dans $\Q$ contient
$[-r, r]\cap\Q$ pour quelque $r > 0$, qui est fermé dans $V$,
donc \omterm{def:b3:topology:compact}{compact}~; mais une suite de rationnels dans $[-r,r]$
convergeant (dans $\R$) vers un irrationnel n'a pas de
sous-suite convergeant \emph{dans $\Q$}~: contradiction avec
\cref{thm:b3:topology:metriccompact}.

(b) Soit $\gamma = (\gamma_1, \gamma_2) \colon [0,1] \to S$ un
chemin avec $\gamma(0) = (0,0)$ et $\gamma(1) \in \Gamma$.
L'ensemble $\{t : \gamma_1(t) = 0\}$ est fermé et ne contient
pas $1$~; soit $t^*$ son supremum, donc $\gamma_1(t^*) = 0$ et
$\gamma_1 > 0$ sur $(t^*, 1]$. Par continuité de $\gamma_2$ en
$t^*$, choisir $\delta$ avec $\abs{\gamma_2(t) -
\gamma_2(t^*)} < \tfrac12$ pour $t \in [t^*, t^* + \delta]$.
Fixer $t_0 = t^* + \delta$ (on peut supposer $\leq 1$)~:
$\gamma_1(t_0) > 0$, et $\gamma_1$ prend toute valeur de $(0,
\gamma_1(t_0)]$ sur $[t^*, t_0]$ (théorème des valeurs
intermédiaires, \cref{thm:b3:topology:connectedbasics}).
Choisir $k$ grand avec $a_k = \frac1{\pi/2 + 2k\pi} <
\gamma_1(t_0)$ et $b_k = \frac1{3\pi/2 + 2k\pi} <
\gamma_1(t_0)$~: il existe $s, s' \in (t^*, t_0]$ avec
$\gamma_1(s) = a_k$, $\gamma_1(s') = b_k$~; comme les points
sont sur le graphe, $\gamma_2(s) = \sin(1/a_k) = 1$ et
$\gamma_2(s') = -1$. Les deux ne peuvent être à moins de
$\frac12$ de $\gamma_2(t^*)$~: contradiction. $S$ est \omterm{def:b3:topology:connected}{connexe}
mais non \omterm{def:b3:topology:pathconnected}{connexe par arcs}.
\end{solution}

\begin{solution}{exo:b3:topology:10}
(a) $C_n$ consiste exactement en les $x$ admettant un
développement ternaire avec chiffres dans $\{0, 2\}$ jusqu'au
rang $n$ (induction~: retirer les tiers médians retire le
premier chiffre $1$, etc.~; les extrémités ont deux
développements, dont un sans $1$), donc $C = \bigcap C_n$ est
l'ensemble des sommes $\sum a_n3^{-n}$, $a_n \in \{0,2\}$.
\omterm{def:b3:topology:compact}{Compact}~: chaque $C_n$ est une réunion finie d'intervalles
fermés~; $C$ est fermé dans $[0,1]$. Intérieur vide~: $C
\subseteq C_n$, une réunion d'intervalles de longueur
$3^{-n}$~; un intervalle intérieur de longueur $\ell > 0$
tiendrait dans l'un d'eux pour tout $n$. \omterm{prop:b3:galois:perfect}{Parfait}~: étant donné
$x \in C$ et $m$, retourner le chiffre $a_m$~: le nouveau
point est dans $C$, distinct, à moins de $2\cdot3^{-m}$ de
$x$.

(b) Si $x \neq y \in C$, leurs développements diffèrent d'abord
à quelque rang $k$~; entre eux se trouve un intervalle de
tiers médian retiré (le trou au rang $k$ séparant le chiffre
$0$ du chiffre $2$), fournissant un point $c \notin C$, $x <
c < y$ (disons)~: $C = (C \cap (-\infty, c)) \sqcup (C \cap
(c, \infty))$ scinde toute partie contenant les deux points.
Les composantes sont des singletons.

(c) L'application des chiffres $\beta\colon x \mapsto
(a_n(x))$ est une bijection sur $\{0,2\}^\N$ (existence et
unicité des développements $\{0,2\}$~: des suites de chiffres
distinctes donnent des points à distance $\geq 3^{-k} > 0$ où
elles diffèrent d'abord, comme dans
\cref{pb:b3:topology:1}, question~1). Elle est \omterm{def:b3:topology:continuity}{continue}~:
$\abs{x - y} < 3^{-m}$ force l'accord des $m$ premiers
chiffres (même calcul), donc $\beta$ envoie de petites boules
dans des pavés de \omterm{def:b3:topology:basis}{base}. Une bijection \omterm{def:b3:topology:continuity}{continue} du \omterm{def:b3:topology:compact}{compact} $C$
vers le produit \omterm{def:b3:topology:hausdorff}{séparé} est un \omterm{def:b3:topology:continuity}{homéomorphisme}
(\cref{cor:b3:topology:compacthomeo}~; le produit est \omterm{def:b3:topology:hausdorff}{séparé}~:
séparer en une coordonnée différente). Non dénombrabilité~:
diagonale de Cantor sur $\{0,2\}^\N$. Enfin $\{0,2\}^\N
\times \{0,2\}^\N \cong \{0,2\}^\N$ par entrelacement des
chiffres (un \omterm{def:b3:topology:continuity}{homéomorphisme}~: continuité composante par
composante dans les deux sens), donc $C \times C \cong C$.
\end{solution}

\begin{solution}{exo:b3:topology:11}
Pour $y \in \R^n$, poser
\[
\tau(y) = \Bigl(\frac{2y}{\norm y^2 + 1},\ \frac{\norm y^2 -
1}{\norm y^2 + 1}\Bigr) \in \R^n\times\R = \R^{n+1}.
\]
On vérifie $\norm{\tau(y)} = 1$, $\tau(y) \neq N$, et
$\sigma(\tau(y)) = y$, $\tau(\sigma(x)) = x$~: $\sigma$ et
$\tau$ sont mutuellement inverses, toutes deux \omterm{def:b3:topology:continuity}{continues}
(formules rationnelles à dénominateurs non nuls)~: $S^n
\setminus\{N\} \cong \R^n$. Étendre en $\hat\sigma \colon S^n
\to \widehat{\R^n}$ par $N \mapsto \infty$~: une bijection,
\omterm{def:b3:topology:continuity}{continue} en tout point de $S^n\setminus\{N\}$, et en $N$~: un
\omterm{def:b3:topology:topology}{voisinage} de \omterm{def:b3:topology:basis}{base} de $\infty$ est $\widehat{\R^n}\setminus
K$, $K$ \omterm{def:b3:topology:compact}{compact}, $K \subseteq \bar B(0, R)$~; comme
$\norm{\sigma(x)}^2 = \frac{1 + x_{n+1}}{1 - x_{n+1}} \to
\infty$ quand $x_{n+1} \to 1$, l'ensemble $\{x \in S^n :
x_{n+1} > 1 - \eta\}$ s'envoie hors de $\bar B(0,R)$ pour
$\eta$ petit~: continuité. Une bijection \omterm{def:b3:topology:continuity}{continue} du \omterm{def:b3:topology:compact}{compact}
$S^n$ vers le \omterm{def:b3:topology:hausdorff}{séparé} $\widehat{\R^n}$ est un \omterm{def:b3:topology:continuity}{homéomorphisme}.
Retirer un autre point $p$~: une rotation de $S^n$ envoie $p$
sur $N$ (les rotations sont des \omterm{def:b3:topology:continuity}{homéomorphismes}), se ramenant
au cas calculé~: $S^n \setminus \{p\} \cong \R^n$.
\end{solution}

\begin{solution}{exo:b3:topology:12}
(a) $T$ est borné, et fermé~: une limite de points de $T$
d'abscisses $\to x_0 > 0$ reste sur le graphe (localement
fermé) par continuité de $\sin\frac1x$ sur $\intoc01$~; une
limite d'abscisses $\to 0$ a une ordonnée dans
$\intcc{-1}1$, donc est dans le segment. \omterm{def:b3:topology:compact}{Compact} par
Heine--Borel (\cref{cor:b3:topology:heineborel}). \omterm{def:b3:topology:interior}{Adhérence} du
graphe $\Gamma$~: tout point $(0, y)$, $y \in \intcc{-1}1$,
est limite de points du graphe --- résoudre $\sin\frac1x = y$
près de $0$ (la fonction balaye $\intcc{-1}1$ sur chaque
intervalle $[\frac1{(k+1)\pi}, \frac1{k\pi}]$)~: $\bar\Gamma =
T$.

(b) $\Gamma$ est l'image \omterm{def:b3:topology:continuity}{continue} de la \omterm{def:b3:topology:connected}{connexe} $\intoc01$
sous $x \mapsto (x, \sin\frac1x)$~: \omterm{def:b3:topology:connected}{connexe}~; et l'\omterm{def:b3:topology:interior}{adhérence}
d'un ensemble \omterm{def:b3:topology:connected}{connexe} est \omterm{def:b3:topology:connected}{connexe}
(\cref{thm:b3:topology:connectedbasics})~: $T = \bar\Gamma$
est \omterm{def:b3:topology:connected}{connexe}.

(c) Supposer $\gamma\colon\intcc01\to T$ \omterm{def:b3:topology:continuity}{continue} avec
$\gamma(0) = (0,0)$, $\gamma(1) = (1, \sin1)$, et soit $t^*
= \sup\{t : \gamma_1(t) = 0\}$~: par continuité
$\gamma_1(t^*) = 0$ et $\gamma_1 > 0$ sur $\intoc{t^*}1$.
Pour tout $\delta > 0$, l'intervalle $\intoc{t^*}{t^*+\delta}$
a $\gamma_1$ prenant toutes les valeurs de quelque
$\intoc0\eta$ (théorème des valeurs intermédiaires,
$\gamma_1(t^* + \delta) > 0$)~; en particulier il contient
des abscisses de la forme $\frac1{\pi/2 + 2k\pi}$ et
$\frac1{-\pi/2 + 2k\pi}$ pour $k$ arbitrairement grand, où
$\gamma_2 = \pm1$. Donc sur tout \omterm{def:b3:topology:topology}{voisinage} droit de $t^*$,
$\gamma_2$ prend les deux valeurs $1$ et $-1$~: $\gamma_2$
n'a pas de limite en $t^{*+}$, contredisant la continuité.
Aucun chemin n'existe.

(d) $T$ est \omterm{def:b3:topology:connected}{connexe} mais non \omterm{def:b3:topology:pathconnected}{connexe par arcs}~: les deux
notions diffèrent. Pour $U \subseteq \R^n$ \omterm{def:b3:topology:topology}{ouvert} \omterm{def:b3:topology:connected}{connexe} et
$a \in U$, soit $V$ l'ensemble des points de $U$ joignables à
$a$ par un chemin dans $U$. $V$ est \omterm{def:b3:topology:topology}{ouvert}~: autour de $x \in
V$ une boule $B(x, r) \subseteq U$ est étoilée, et en
concaténant le chemin vers $x$ avec un segment on atteint
tout point de la boule. $V$ est fermé dans $U$~: si $x \in U
\setminus V$, le même argument de boule montre $B(x, r) \cap
V = \varnothing$ (un point de $V$ dans la boule joindrait $a$
à $x$). Non vide ($a \in V$), \omterm{def:b3:topology:topology}{ouvert} et fermé dans le \omterm{def:b3:topology:connected}{connexe}
$U$~: $V = U$. La sinusoïde échappe à cela en étant fermée
d'intérieur vide~: son «~mauvais~» point $(0,0)$ n'a aucune
boule dans $T$ pour pontifier les oscillations.

La \omterm{ex:b3:topology:sinecurve}{sinusoïde du topologue} reste l'exemple canonique séparant
connexité et connexité par arcs~: fermée d'intérieur vide, elle
échappe au critère \omterm{def:b3:topology:topology}{ouvert} qui force les deux notions à
coïncider dans $\R^n$.

\end{solution}
\begin{solution}{pb:b3:topology:1}
\textbf{1.} Supposons que les développements de $x, y \in C$
diffèrent d'abord au rang $k \leq m$, disons $b_k(x) = 1$,
$b_k(y) = 0$. Alors
\[
x - y \;\geq\; \frac{2}{3^k} - \sum_{j > k}\frac{2}{3^j}
= \frac{2}{3^k} - \frac{1}{3^k} = \frac1{3^k} \geq \frac1{3^m}:
\]
contraposée~: $\abs{x - y} < 3^{-m}$ force l'accord jusqu'au
rang $m$.

\textbf{2.} Par la question~1, $b_n$ est constante sur $C \cap
(x - 3^{-n}, x + 3^{-n})$~: localement constante, donc
\omterm{def:b3:topology:continuity}{continue}. L'application $x \mapsto (b_n(x))_n \in \{0,1\}^\N$
est \omterm{def:b3:topology:continuity}{continue} (composante par composante,
\cref{prop:b3:topology:universal}(a)) et bijective
(unicité des développements $\{0,2\}$)~; du \omterm{def:b3:topology:compact}{compact} $C$ vers
un \omterm{def:b3:topology:hausdorff}{espace séparé}, c'est un \omterm{def:b3:topology:continuity}{homéomorphisme}
(\cref{cor:b3:topology:compacthomeo}).

\textbf{3.} Continuité~: si $\abs{x - y} < 3^{-m}$, les $m$
premiers chiffres coïncident, donc $\abs{g(x) - g(y)} \leq
\sum_{n > m}2^{-n} = 2^{-m}$. Surjectivité~: tout $t \in
[0,1]$ a un développement binaire $t = \sum b_n2^{-n}$, et $t
= g\bigl(\sum 2b_n3^{-n}\bigr)$. Non injective~: $g$ identifie
les deux points de Cantor de chiffres $(0,1,1,1,\dots)$ et
$(1,0,0,\dots)$ --- tous deux d'image $\frac12$ (l'ambiguïté
dyadique $0.0111\ldots_2 = 0.1000\ldots_2$).

\textbf{4.} Chaque composante de $\Phi$ est \omterm{def:b3:topology:continuity}{continue} par la
même estimation (ses chiffres sont une sous-suite des $b_n$).
Surjectivité~: étant donné $(s, t) \in [0,1]^2$, choisir des
chiffres binaires $(c_n)$ de $s$ et $(d_n)$ de $t$, et
entrelacer~: le point $x \in C$ avec $b_{2n-1}(x) = c_n$,
$b_{2n}(x) = d_n$ satisfait $\Phi(x) = (s,t)$.

\textbf{5.} Les trous sont deux à deux disjoints d'extrémités
dans $C$, donc la formule définit $f$ sans ambiguïté sur
$[0,1]$~; aux extrémités d'un trou la formule affine renvoie
$\Phi(u)$, $\Phi(v)$~: cohérent avec $f = \Phi$ sur $C$.
Surjectivité~: déjà $f(C) = \Phi(C) = [0,1]^2$.
\textbf{6.} En $t_0 \notin C$~: $t_0$ est dans un trou \omterm{def:b3:topology:topology}{ouvert}
où $f$ est affine~: \omterm{def:b3:topology:continuity}{continue}. En $x \in C$~: on enregistre
d'abord deux estimations.

\emph{(i) Sur $C$}~: si $\abs{y - x} \leq 3^{-M}$, $y \in C$,
alors les chiffres coïncident jusqu'à $M$, donc chaque
composante de $\Phi(y) - \Phi(x)$ est au plus $\sum_{n >
\lfloor M/2\rfloor} 2^{-n} = 2^{-\lfloor M/2\rfloor}$.

\emph{(ii) À travers un trou}~: un trou $(u,v)$ retiré à
l'étape $k$ a longueur $3^{-k}$, et ses extrémités ont des
chiffres coïncidant jusqu'au rang $k - 1$, donc
$\norm{\Phi(v) - \Phi(u)}_\infty \leq 2^{-\lfloor
(k-1)/2\rfloor}$.

Maintenant soit $\abs{t - x} < 3^{-M}$ avec $t$ dans un trou
$(u, v)$ d'étape $k$, et disons $u$ l'extrémité du côté de
$x$. Alors
\[
\norm{f(t) - \Phi(x)}_\infty
\leq \norm{\Phi(u) - \Phi(x)}_\infty
+ \frac{t - u}{v - u}\,\norm{\Phi(v) - \Phi(u)}_\infty
\leq 2^{-\lfloor M/2\rfloor} + \min\bigl(1,\ 3^{k - M}\bigr)\,
2^{-\lfloor(k-1)/2\rfloor} .
\]
Pour $k \geq M$ le dernier terme est $\leq 2^{-\lfloor(M-1)/2
\rfloor}$~; pour $k < M$, comme $2^{-\lfloor(k-1)/2\rfloor}
\leq \sqrt2\,\cdot 2^{-k/2}$,
\[
3^{k-M}\,2^{-\lfloor(k-1)/2\rfloor}
\leq \sqrt2\,\Bigl(\frac{3}{\sqrt2}\Bigr)^{k} 3^{-M}
\leq \sqrt2\,\Bigl(\frac{3}{\sqrt2}\Bigr)^{M} 3^{-M}
= \sqrt2\; 2^{-M/2}.
\]
Dans tous les cas $\norm{f(t) - \Phi(x)}_\infty \leq
C\,2^{-M/2}$~: $M \to \infty$ prouve la continuité en $x$.
Donc $f$ est une surjection \omterm{def:b3:topology:continuity}{continue} $[0,1] \to [0,1]^2$.

\textbf{7.} Pour $[0,1]^n$~: scinder les chiffres de $x \in C$
en $n$ sous-suites entrelacées et répéter les questions~4--6.
Pour $\R \to \R^2$~: soit $h \colon [0,1] \to [-1,1]^2$ une
surjection \omterm{def:b3:topology:continuity}{continue} (redimensionner $f$). Définir $F$ sur
$[m, m + \frac12]$ ($m \in \Z$) comme une copie de $h$
redimensionnée pour remplir $[-(m+1), m+1]^2$ pour $m \geq 0$
(et symétriquement pour $m < 0$), et sur $[m + \frac12, m +
1]$ comme le segment affine joignant les valeurs d'extrémité~:
$F$ est \omterm{def:b3:topology:continuity}{continue} (recollement,
\cref{prop:b3:topology:gluing}(b)) et son image contient
$\bigcup_m [-(m+1), m+1]^2 = \R^2$.

\textbf{8.} $[0,1]$ est \omterm{def:b3:topology:compact}{compact} et $[0,1]^2$ \omterm{def:b3:topology:hausdorff}{séparé}~: une
injection \omterm{def:b3:topology:continuity}{continue} est un \omterm{def:b3:topology:continuity}{homéomorphisme} sur son image
(\cref{cor:b3:topology:compacthomeo}).

\textbf{9.} Retirer $\frac12$~: $[0,1]\setminus\{\frac12\}$
est disconnexe. Si $h \colon [0,1] \to [0,1]^2$ était un
\omterm{def:b3:topology:continuity}{homéomorphisme}, $[0,1]^2\setminus\{h(\frac12)\}$ serait
disconnexe aussi~; mais le carré moins un point est \omterm{def:b3:topology:pathconnected}{connexe
par arcs}. Contradiction~: $[0,1] \not\cong [0,1]^2$.

\textbf{10.} Par la question~8, une surjection \omterm{def:b3:topology:continuity}{continue}
injective serait un \omterm{def:b3:topology:continuity}{homéomorphisme}, contredisant la
question~9. La compacité est entrée dans
\cref{cor:b3:topology:compacthomeo}. Sans compacité~: $[0,
2\pi) \to S^1$ est une bijection \omterm{def:b3:topology:continuity}{continue} non \omterm{def:b3:topology:continuity}{homéomorphisme}.

\textbf{11.} Prouvé ici~: \emph{il existe une surjection
\omterm{def:b3:topology:continuity}{continue}, mais pas de bijection \omterm{def:b3:topology:continuity}{continue}, de $[0,1]$ sur
$[0,1]^2$~; en particulier $[0,1] \not\cong [0,1]^2$.} La
dimension est un invariant d'\omterm{def:b3:topology:continuity}{homéomorphisme} (au moins $1$ vs
$2$), non de surjection \omterm{def:b3:topology:continuity}{continue}. L'invariance du domaine
demande la \omterm{def:b3:topology:topology}{topologie} \omterm{def:b3:galois:algebraic}{algébrique}, hors de ce cours.
\textbf{12.} En ternaire, $\frac C2 = \{\sum_na_n3^{-n} :
a_n \in \{0, 1\}\}$ (diviser les chiffres $\{0,2\}$ donne
$\{0,1\}$). Pour $t \in [0,1]$, prendre un développement
ternaire $t = \sum_nd_n3^{-n}$, $d_n \in \{0,1,2\}$, et
scinder chaque chiffre en $d_n = a_n + b_n$ avec $a_n, b_n
\in \{0,1\}$~: alors $t \in \frac C2 + \frac C2$. Aucune
retenue ne se propage. L'inclusion réciproque est claire.
Redimensionner par $2$~: $C + C = [0,2]$.

\textbf{13.} $(x, y) \mapsto x + y$ envoie $C \times C$ sur
$C + C = [0,2]$~: la poussière (intérieur vide,
\cref{exo:b3:topology:10}) projette une ombre pleine.
Auto-similarité~: $C = \frac C3 \cup \bigl(\frac C3 +
\frac23\bigr)$.

\textbf{14.} Via $C \cong \{0,1\}^\N$ (question~2),
l'entrelacement des chiffres est une bijection \omterm{def:b3:topology:continuity}{continue} dans
les deux sens $\{0,1\}^\N\times\{0,1\}^\N \to
\{0,1\}^\N$. Donc $C\times C \cong C$ et $C^n \cong C$.
Les espaces familiers ne partagent pas cela ($\R^2 \not\cong
\R$)~; les produits infinis oui.

\textbf{15.} $\sum_{k\geq1}2\cdot3^{-2k} = \frac14$~: le
développement ternaire de $\frac14$ est $0.\overline{02}$,
donc $\frac14 \in C$ sans être extrémité d'un intervalle
retiré. Les extrémités sont dénombrables, $C$ non dénombrable
(question~19).

\textbf{16.} $g$ est au plus $2$-vers-$1$~: l'ambiguïté
dyadique $0.w01^\infty = 0.w10^\infty$ donne exactement deux
antécédents aux rationnels dyadiques de $(0,1)$. Squelette
combinatoire de l'escalier de Cantor du~\cref{ch:b3:measure}.

\textbf{17.} $x$ non isolé donne $y_0 \neq x$ dans $B(x,
r/2)$~; $y_0$ non isolé donne $y_1$. Des $\rho \leq
\min\bigl(\frac{d(y_0,y_1)}3, \frac r4\bigr)$ rendent
$\bar B(y_0, \rho)$ et $\bar B(y_1, \rho)$ disjointes dans
$B(x, r)$. La perfection maintient la récursion indéfiniment.

\textbf{18.} Construire $F_w$ par induction~: $F_\varnothing
= P$, scinder chaque boule en deux boules fermées disjointes
de diamètre $\leq 2^{-\abs w}\operatorname{diam}P$. Pour
$\varepsilon$ infini, l'intersection emboîtée est un
singleton $h(\varepsilon)$ (propriété d'intersection finie
du \omterm{def:b3:topology:compact}{compact} $P$).

\textbf{19.} $h$ est injective (mots différant se séparent en
$F_{w0}, F_{w1}$) et \omterm{def:b3:topology:continuity}{continue} (diamètres $\to 0$)~: tout
\omterm{def:b3:topology:compact}{compact} métrique \omterm{prop:b3:galois:perfect}{parfait} est non dénombrable --- $[0,1]$ et
$C$ parmi eux.

\textbf{20.} $h$ est une injection \omterm{def:b3:topology:continuity}{continue} du \omterm{def:b3:topology:compact}{compact}
$\{0,1\}^\N$ vers $P$~: \omterm{def:b3:topology:continuity}{homéomorphisme} sur l'image
(\cref{cor:b3:topology:compacthomeo}). Tout \omterm{def:b3:topology:compact}{compact} métrique
\omterm{prop:b3:galois:perfect}{parfait} contient une copie de Cantor.

\textbf{21.} Un \omterm{def:b3:topology:compact}{compact} métrique dénombrable a un point
isolé (question~19). Dans $K = \{0\}\cup\{\frac1n : n
\geq 1\}$ les $\frac1n$ sont isolés et \omterm{def:b3:topology:interior}{denses}, $0$ ne l'est
pas.

\textbf{22.} Les non-points de condensation se recouvrent par
dénombrablement d'intervalles rationnels à trace dénombrable
sur $F$~: $F \setminus P$ dénombrable. $P$ est \omterm{prop:b3:galois:perfect}{parfait} fermé
non vide si $F$ est non dénombrable. L'arbre des
questions~17--19 dans $P$ donne $\abs P \geq \mathfrak c$~:
tout fermé non dénombrable de $\R$ a cardinalité $\mathfrak
c$.

\textbf{23.} Si $3^{-(m+1)} \leq \abs{x-y} < 3^{-m}$, les
chiffres coïncident jusqu'à $m$, donc chaque coordonnée de
$\Phi$ partage $\lfloor m/2\rfloor$ chiffres binaires de
tête~: $\norm{\Phi(x)-\Phi(y)}_\infty \leq
2\abs{x-y}^\alpha$ avec $\alpha = \frac{\ln2}{2\ln3}$
(car $3^\alpha = \sqrt2$). Propager aux trous affines
($\norm{f(t)-f(s)} \leq 2\abs{t-s}^\alpha$) et aux paires
générales via les extrémités de $C \cap [t,s]$~:
$\norm{f(t)-f(s)}_\infty \leq 6\abs{t-s}^\alpha$.

\textbf{24.} Si $F$ est $\alpha$-Hölder surjective avec
$\alpha > \frac12$, découper $[0,1]$ en $N$ intervalles~:
chaque image a diamètre $O(N^{-\alpha})$. Choisir une grille
de pas $M \sim N^\alpha$~: chaque cellule de grille tient
dans au plus une image, d'où $(M+1)^2 \leq N$, impossible
pour $N$ grand. Notre courbe ($\alpha \approx 0.3155$) est
sous le mur~; Hilbert atteint $\frac12$.

\textbf{25.} Les amas $\frac1m + \frac1n$ ($n > m^2$)
occupent des intervalles disjoints $(\frac1m,
\frac1{m-1})$. $K' = \{0\} \cup \{\frac1m\}$, $K'' =
\{0\}$, $K''' = \varnothing$~; points isolés \omterm{def:b3:topology:interior}{denses}.
Induction~: coller des copies réduites de $K_j$ le long de
$\frac1m$ pour obtenir $K_{j+1}$ de rang $j+1$. Un \omterm{prop:b3:galois:perfect}{parfait}
est l'opposé~: $P' = P$, et Cantor--Bendixson (question~22)
scinde tout fermé en un noyau \omterm{prop:b3:galois:perfect}{parfait} et un reste
dénombrable.


En résumé pour les questions~17--22~: la perfection d'un
\omterm{def:b3:topology:compact}{compact} métrique $P$ permet de scinder récursivement toute
boule en deux boules fermées disjointes centrées dans $P$,
d'où une injection \omterm{def:b3:topology:continuity}{continue} $h \colon \{0,1\}^\N \to P$
qui, par compacité, est un \omterm{def:b3:topology:continuity}{homéomorphisme} sur l'image ---
toute perfection compacte contient Cantor, et en particulier
est non dénombrable. Cantor--Bendixson décompose alors tout
fermé de $\R$ en un noyau \omterm{prop:b3:galois:perfect}{parfait} (de cardinalité du
continu s'il est non vide) et un reste dénombrable, de sorte
que l'hypothèse du continu vaut pour les fermés~: aucun
fermé de $\R$ ne peut avoir une cardinalité strictement
entre $\aleph_0$ et $\mathfrak c$.

Pour la régularité de Hölder (questions~23--24)~: l'exposant
$\alpha = \frac{\ln 2}{2\ln 3}$ apparaît naturellement de
l'identité $3^\alpha = \sqrt2$, qui couple le pas ternaire
de $C$ au pas binaire des coordonnées de $\Phi$. La borne
$2\abs{x-y}^\alpha$ sur $C$ se propage aux interpolations
affines des trous (où le rapport de longueurs élevé à la
puissance $\alpha \leq 1$ diminue) et aux paires générales
via un découpage en trois (côté gauche, cœur dans $C$, côté
droit), d'où le facteur $6$. Le mur $\alpha = \frac12$ est
une obstruction de dimension pure~: une application
$\alpha$-Hölder envoie $N$ intervalles sur des ensembles de
diamètre $O(N^{-\alpha})$, qui ne peuvent couvrir une grille
de pas $N^{-\alpha}$ en dimension $2$ que si $N^{2\alpha}
\lesssim N$, c'est-à-dire $\alpha \leq \frac12$.


Pour les questions~6--10 (continuité de la courbe de Peano et
absence d'injectivité)~: le contrôle local des chiffres sur
$C$ (question~1) donne la continuité de $\Phi$ et de
l'interpolation affine sur les trous~; le facteur $2^{-M/2}$
domine uniformément. L'injectivité est bloquée par la
compacité~: une injection \omterm{def:b3:topology:continuity}{continue} $[0,1] \to [0,1]^2$ serait
un \omterm{def:b3:topology:continuity}{homéomorphisme} sur l'image
(\cref{cor:b3:topology:compacthomeo}), mais retirer un point
intérieur de $[0,1]$ déconnecte la source sans déconnecter
le carré. La compacité de $[0,1]$ est donc l'ingrédient
critique qui transforme une bijection \omterm{def:b3:topology:continuity}{continue} en
\omterm{def:b3:topology:continuity}{homéomorphisme} --- sans elle, $[0, 2\pi) \to S^1$ offre un
contre-exemple classique.


Ainsi la «~dimension~» se révèle être un invariant topologique
fin (\omterm{def:b3:topology:continuity}{homéomorphismes}) mais non un invariant de surjection
\omterm{def:b3:topology:continuity}{continue}~: Peano construit le pont, Cantor--Bendixson et
l'obstruction de Hölder en marquent les rives.

\end{solution}
