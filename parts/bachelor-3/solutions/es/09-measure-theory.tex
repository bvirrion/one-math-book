\section*{Capítulo \ref{ch:b3:measure} --- Measure Theory}

\begin{solution}{exo:b3:measure:1}
(a) La complementación intercambia los dos casos definitorios. un contable
la unión de conjuntos contables es contable; si un miembro es
co-contable, la unión es co-contable: la estabilidad se mantiene. eso
contiene los singletons y cualquier álgebra $\sigma$ que contenga
contiene todos los conjuntos contables (uniones contables) y sus
complementos: es $\sigma(\{\text{singletons}\})$.

(b) Escriba $\mathcal B = \sigma(\text{opens})$. cada abierto
subconjunto de $\R$ es una unión contable de intervalos abiertos con
puntos finales racionales (alrededor de cada punto racional del \omterm{def:b3:topology:topology}{conjunto abierto},
un intervalo de radio racional dentro de él), por lo que se abre $\in
\sigma(\text{open intervals}) \subseteq \sigma(\text{rational
data})$. Conversiones:$\intoo ab = \bigcup_n\intcc{a +
\frac1n}{b - \frac1n}$;$\intcc ab = \bigcap_n \intoo{a -
\frac1n}{b + \frac1n}$;$\intoc{-\infty}a = \bigcap_n
\intoo{-\infty}{a + \frac1n}$y viceversa$\intoo ab =
\intoo{-\infty}b \setminus \intoc{-\infty}a$; rayos racionales:
$\intoc{-\infty}a =
\bigcap_{q \in \Q,\, q > a}\intoc{-\infty}q$. cada familia
genera los demás mediante operaciones contables: los cuatro generan
$\mathcal B$.

(c) Con criterios de valoración finitos únicamente, la familia ni siquiera es una
álgebra: el complemento de $\intoc01$ contiene rayos ilimitados.
Al permitir puntos finales infinitos ($\intoc{-\infty}b$, $\intoo
a{+\infty}$) se convierte en un álgebra (complementarios y finitos).
las uniones de tales uniones son tales), pero no un álgebra $\sigma$:
$\{0\} = \bigcap_n\intoc{-\frac1n}0$ no es una unión finita de
intervalos no degenerados.
\end{solution}

\begin{solution}{exo:b3:measure:2}
(a) $A \cup B = A \sqcup (B \setminus (A\cap B))$, entonces
$\mu(A\cup B) = \mu(A) + \mu(B) - \mu(A\cap B)$ (finitud
permite la resta). Tres conjuntos: aplica la fórmula de dos conjuntos
dos veces,
\[
\mu(A\cup B\cup C) = \sum\mu(A) - \sum\mu(A\cap B) +
\mu(A\cap B\cap C)
\]
(sumas de los conjuntos de índices obvios).

(b) Para \omterm{def:b3:measure:lebesgueouter}{medida de lebesgue}, $A_n = [n, +\infty) \downarrow
\varnothing$, pero$\lambda(A_n) = \infty \not\to 0$.

(c) Un punto se encuentra en un intervalo de longitud $\varepsilon$:
$\lambda(\{x\}) = 0$; la subaditividad contable mata a los contables
conjuntos. Por tanto $\lambda(\Q \cap \intcc01) = 0$ y, por aditividad,
$\lambda(\intcc01\setminus\Q) = 1$: los irracionales lo llevan todo
la longitud.
\end{solution}

\begin{solution}{exo:b3:measure:3}
Los rayos $\intoc{-\infty}t$ forman un sistema $\pi$ (el
la intersección de dos es la más pequeña) generando $\mathcal B(\R)$
(\cref{exo:b3:measure:1}). Los conjuntos $P_k = \intoc{-\infty}k$
aumentar a $\R$ con $\mu(P_k) \leq 1 < \infty$:
Se aplica \cref{thm:b3:measure:uniqueness} y $\mu = \nu$ en
$\mathcal B(\R)$. Así, la función de distribución determina la
\omterm{def:b3:measure:measure}{medida}.
\end{solution}

\begin{solution}{exo:b3:measure:4}
Por cada $N$, $\limsup A_n \subseteq \bigcup_{n \geq N}A_n$,
entonces $\mu(\limsup A_n) \leq \sum_{n\geq N}\mu(A_n)$, la cola de
una serie convergente: sea $N \to \infty$. Aplicación: con
$A_n = \{x \in \intcc01 : \abs{x - r_n} \leq 4^{-n}\}$
($r_n$ el $n$-ésimo racional), $\lambda(A_n) \leq 2\cdot4^{-n}$
es sumable: $\lambda(\limsup A_n) = 0$, es decir, casi todos los $x$
pertenece sólo a un número finito de $A_n$. (Sin embargo, cada $x$ es un límite
de racionales: el punto es el \emph{velocidad} $4^{-n}$.)
\end{solution}

\begin{solution}{exo:b3:measure:5}
$K = \bigcap K_n$ es una intersección de uniones finitas de cerrado
intervalos: \omterm{def:b3:topology:compact}{compacto}. En la etapa $n$ quedan intervalos $2^n$
de longitud común $\ell_n \leq 2^{-n}$ (cada etapa se divide por la mitad y
se encoge); un intervalo $I \subseteq K$ estaría dentro de un solo
stage-$n$ intervalo para cada $n$, forzando $\ell(I) = 0$: vacío
\omterm{def:b3:topology:interior}{interior}. El \omterm{def:b3:measure:measure}{medida} eliminado es $\sum_{n\geq1}2^{n-1}\cdot
4^{-n} = \frac12\sum_{n\geq1}2^{-n} = \frac12$, todas las eliminaciones
siendo intervalos abiertos disjuntos: $\lambda(K) = \frac12$.

Variante: eliminar los intervalos centrales de la longitud $\varepsilon
4^{-n}$deja un \omterm{def:b3:topology:compact}{compacto}$K^{(\varepsilon)}$ denso en ninguna parte
\omterm{def:b3:measure:measure}{medida}$1 - \frac\varepsilon2$. Entonces $\bigcup_m
K^{(1/m)}$ es \omterm{rem:b3:complete:meagre}{pobre} (unión contable de la nada \omterm{def:b3:topology:interior}{conjuntos densos})
de \omterm{def:b3:measure:measure}{medida} $\geq \sup_m(1 - \frac1{2m}) = 1$: un \omterm{rem:b3:complete:meagre}{conjunto escaso} de
\omterm{def:b3:measure:measure}{medida} completo --- y su complemento en $\intoo01$ es un denso
$G_\delta$ de \omterm{def:b3:measure:measure}{medida} $0$ (\omterm{def:b3:topology:topology}{topológicamente} grasa, métricamente
nulo). El complemento de $K^{(\varepsilon)}$ en $\intoo01$ es
abierto, denso, de \omterm{def:b3:measure:measure}{medida} $\frac\varepsilon2 < \varepsilon$.
\end{solution}

\begin{solution}{exo:b3:measure:6}
Cortar $\intoc01$ en $2^n$ se traduce de $\intoc0{2^{-n}}$:
$c = 2^n\,\mu(\intoc0{2^{-n}})$, entonces $\mu(\intoc0{2^{-n}}) =
c\,2^{-n} = c\,\lambda(\intoc0{2^{-n}})$. Por traducción
invariancia y aditividad, $\mu = c\lambda$ en cada intervalo
$\intoc ab$ con $b - a$ un racional diádico y cualquier $a$; un
general $\intoc ab$ es una unión cada vez mayor de tales
($b_k \uparrow b$ pasos diádicos de $a$) y \omterm{def:b3:topology:continuity}{continuidad} de
abajo se extiende la igualdad. Los intervalos $\intoc ab$ forman un
$\pi$: sistema que genera $\mathcal B(\R)$, con $\intoc{-k}k
\uparrow \R$de \omterm{def:b3:measure:measure}{medida} finito ($\mu(\intoc{-k}k) = 2kc$):
\cref{thm:b3:measure:uniqueness} le da $\mu = c\lambda$ en
$\mathcal B(\R)$.
\end{solution}

\begin{solution}{exo:b3:measure:7}
Por regularidad (\cref{thm:b3:measure:regularity}) elija $K
\subseteq A \subseteq U$con$K$\omterm{def:b3:topology:compact}{compacto},$U$ abierto,
$\lambda(U\setminus K) < \varepsilon$ (ambas aproximaciones
dentro de $\varepsilon/2$ y $\lambda(U \setminus K) =
\lambda(U\setminus A) + \lambda(A \setminus K)$). Escribe$U$ como
una unión disjunta contable de intervalos abiertos $(I_n)$ (el
componentes del \omterm{def:b3:topology:topology}{conjunto abierto}); el \omterm{def:b3:topology:compact}{compacto} $K$ está cubierto por
finitos, $K \subseteq B = I_1\cup\dots\cup I_N \subseteq
U$. Luego$A \setminus B \subseteq A\setminus K \subseteq
U\setminus K$y$B \setminus A \subseteq U \setminus A
\subseteq U\setminus K$:
$\lambda(A\,\triangle\,B) \leq 2\lambda(U\setminus K)$ ---
comience desde $\varepsilon/2$ hasta aterrizar debajo de $\varepsilon$.
\end{solution}

\begin{solution}{exo:b3:measure:8}
Reemplazando $A$ por $A \cap [-M, M]$ de \omterm{def:b3:measure:measure}{medida} positivo (algunos $M$
funciona, por \omterm{def:b3:topology:continuity}{continuidad} desde abajo), asume $0 < \lambda(A) <
\infty$. Tome$U \supseteq A$, abra con$\lambda(U) <
\frac43\lambda(A)$y descomponga$U = \bigsqcup_nI_n$ en
intervalos abiertos disjuntos: $\lambda(A) = \sum_n\lambda(A\cap
I_n)$. Si cada$n$tuviera$\lambda(A\cap I_n) \leq
\frac34\ell(I_n)$, la suma daría$\lambda(A) \leq
\frac34\lambda(U) < \lambda(A)$: algún intervalo$I$ satisface
$\lambda(A\cap I) > \frac34\ell(I)$. Configure $B = A \cap I$ y deje
$\abs t < \frac12\ell(I)$: tanto $B$ como $B + t$ se encuentran en
intervalo $I \cup (I + t)$, de longitud $< \frac32\ell(I)$. si
estaban disjuntos: $\lambda(B) + \lambda(B + t) = 2\lambda(B)
> \frac32\ell(I)$ excedería el intervalo contenedor
\omterm{def:b3:measure:measure}{medida} --- imposible. Entonces $B \cap (B + t) \neq \varnothing$:
algunos $x \in B$ escriben $x = y + t$ con $y \in B$ y $t = x -
y \in A - A$.
Por lo tanto $\intoo{-\frac{\ell(I)}2}{\frac{\ell(I)}2} \subseteq A -
A$.
\end{solution}

\begin{solution}{exo:b3:measure:9}
El Vitali traduce la partición $(V + q)_{q\in\Q}$ $\R$ (cada
real equivale exactamente a un representante). Supongamos que todos
los conjuntos $B_q = A \cap (V + q)$ eran medibles. Cualquier dos
Los elementos de $V + q$ se diferencian por un irracional o cero (dos
distintos representantes no son equivalentes), por lo que $B_q - B_q$
cumple con $\Q$ solo en $\{0\}$: no contiene intervalo, y
Steinhaus (\cref{exo:b3:measure:8}) fuerza a $\lambda(B_q) = 0$.
Entonces $\lambda(A) \leq \sum_{q}\lambda(B_q) = 0$, contradiciendo
$\lambda(A) > 0$. Entonces algo de $B_q \subseteq A$ no es mensurable.
\end{solution}

\begin{solution}{exo:b3:measure:10}
\emph{Measurable $\Rightarrow$ $\varepsilon$-approximation}: por
regularidad exterior (\cref{thm:b3:measure:regularity}) selección abierta
$U \supseteq A$ con $\lambda(U) \leq \lambda(A) +
\varepsilon$; la mensurabilidad permite la resta
$\lambda(U\setminus A) = \lambda(U) - \lambda(A) \leq
\varepsilon$. \emph{$\varepsilon$-version$\Rightarrow$
$G_\delta$-version}: tome $U_n$ con $\lambda^*(U_n\setminus
A) < \frac1n$y$G = \bigcap U_n$: un$G_\delta$ con
$\lambda^*(G\setminus A) \leq \lambda^*(U_n\setminus A) \to 0$.
\emph{$G_\delta$-version $\Rightarrow$ measurable}: $G\setminus
A$es$\lambda^*$-nulo, por lo que se puede medir mediante \omterm{def:b3:complete:complete}{lo completo}
(\cref{thm:b3:measure:caratheodory}) y $A = G \setminus
(G\setminus A)$es medible ($G$ es Borel). Entonces Lebesgue establece
son exactamente ``Borel módulo nulo''.
\end{solution}

\begin{solution}{exo:b3:measure:11}
(a) $\liminf A_n = \bigcup_N\bigcap_{n\geq N}A_n$ es un
unión creciente de los conjuntos $B_N = \bigcap_{n\geq N}A_n$,
entonces $\mu(\liminf A_n) = \lim_N\mu(B_N)$ (\omterm{def:b3:topology:continuity}{continuidad} de
abajo); y $\mu(B_N) \leq \inf_{n \geq N}\mu(A_n)$, cuyo
El límite es $\liminf\mu(A_n)$. Para el $\limsup$: aplique el
Lo mismo que los complementos dentro del ambiente \omterm{def:b3:measure:measure}{medida finita}.
$U = \bigcup A_n$ --- \omterm{def:b3:topology:continuity}{continuidad} desde arriba en el
disminuir $C_N = \bigcup_{n\geq N}A_n$ requiere $\mu(C_1)
\leq \mu(U) < \infty$y da$\mu(\limsup A_n) =
\lim\mu(C_N) \geq \limsup\mu(A_n)$.

(b) El intervalo móvil $A_n = \intcc n{n+1}$: cada punto
pertenece como máximo a dos de los $A_n$ y eventualmente a ninguno,
entonces $\limsup A_n = \varnothing$; todavía $\mu(A_n) = 1$. Así
$\limsup\mu(A_n) = 1 > 0 = \mu(\limsup A_n)$: sin
\omterm{def:b3:measure:measure}{medida finita} sobre, la segunda desigualdad de (a) falla
--- la masa escapa al infinito, donde ningún conjunto fijo puede
atraparlo.

(c) Si $\sum\mu(A_n) < \infty$: $\mu(C_N) \leq
\sum_{n\geq N}\mu(A_n) \to 0$y$\limsup A_n =
\bigcap C_N$tiene \omterm{def:b3:measure:measure}{medida}$\leq \inf\mu(C_N) = 0$. monótono
casos: el aumento es \omterm{def:b3:topology:continuity}{continuidad} desde abajo; disminuyendo con
$\mu(A_1) < \infty$ es \omterm{def:b3:topology:continuity}{continuidad} desde arriba ---
ambos probados en las propiedades básicas de \cref{ch:b3:measure}; el
contraejemplo $A_n = \intco n\infty$ (disminuyendo a
$\varnothing$ con $\mu \equiv \infty$) muestra la finitud
vuelve a ser esencial.
\end{solution}

\begin{solution}{exo:b3:measure:12}
(a) Los conjuntos $E_{k,N}$ aumentan con $N$ (menos
restricciones), y cada $x$ eventualmente satisface $\abs{f_n(x)
- f(x)} \leq \frac1k$para todos los$n \geq N(x)$ (puntualmente
convergencia): $\bigcup_NE_{k,N} = X$. \omterm{def:b3:topology:continuity}{Continuidad} desde abajo:
$\mu(E_{k,N}) \to \mu(X) < \infty$, entonces $\mu(X\setminus
E_{k,N}) \to 0$; elija$N_k$ en consecuencia.

(b) Sea $A = \bigcap_kE_{k,N_k}$: $\mu(X\setminus A) \leq
\sum_k\varepsilon2^{-k} = \varepsilon$. En$A$: por cada
$k$, todos los $n \geq N_k$ satisfacen $\sup_A\abs{f_n - f} \leq
\frac1k$--- convergencia exactamente uniforme en$A$.

(c) Para el tope en movimiento, la convergencia uniforme en $A$ requiere
$A$ para eventualmente evitar cada $\intcc n{n+1}$ --- más
precisamente $\sup_A\abs{f_n} < \frac12$ obliga a $A \cap \intcc
n{n+1}$a estar vacío para$n$grande, por lo que$X \setminus A$
contiene una cola $\bigcup_{n\geq n_0}\intcc n{n+1}$, de
infinito \omterm{def:b3:measure:measure}{medida}. En (a), la finitud convertida
``$E_{k,N}\nearrow X$'' en ``los complementos' \omterm{def:b3:measure:measure}{medidas}
tiende a $0$'': \omterm{def:b3:topology:continuity}{continuidad} desde arriba necesita un inicio finito,
y en espacios \omterm{def:b3:measure:measure}{medida infinita} el escape al infinito es
precisamente lo que no puede ver.
\end{solution}

\begin{solution}{pb:b3:measure:1}
\textbf{1.} Inducción. \omterm{def:b3:topology:continuity}{Continuidad}: las tres fórmulas coinciden en
los cruces ($\frac12c_n(1) = \frac12$ y $\frac12 +
\frac12c_n(0) = \frac12$); cada pieza es \omterm{def:b3:topology:continuity}{continuo}.
La monotonicidad y los valores límite se heredan. Para el
estimación de contracción: en $[0,\frac13]$, $\abs{c_{n+1} - c_n}(x)
= \frac12\abs{c_n - c_{n-1}}(3x) \leq \frac12\norm{c_n -
c_{n-1}}_\infty$; en el tercio medio la diferencia es$0$; en
el tercio derecho, igual que el izquierdo.

\textbf{2.} $\norm{c_{n+1} - c_n}_\infty \leq
2^{-n}\norm{c_1 - c_0}_\infty$: la serie de incrementos
converge uniformemente, por lo que $c_n \to c$ uniformemente; $c$ es
\omterm{def:b3:topology:continuity}{continuo}, no decreciente, $c(0) = 0$, $c(1) = 1$ (todos
preservado por límites uniformes), y pasando al límite en el
La definición de recursividad muestra que $c$ en sí satisface los tres.
identidades autosemejantes.

\textbf{3.} Por la identidad intermedia, $c \equiv \frac12$ en
$\intcc{\frac13}{\frac23}$, el primer hueco. Cada espacio de $C$ es
la imagen del primer hueco bajo una composición de los dos
contracciones afines $x \mapsto \frac x3$, $x\mapsto\frac{x +
2}3$; las identidades transportan constancia en consecuencia (con
valora los racionales diádicos). Para la fórmula de dígitos, tome $x =
\sum_n 2b_n3^{-n} \in C$: si$b_1 = 0$entonces$x \in
[0,\frac13]$y$c(x) = \frac12c(3x)$con$3x$ teniendo dígitos
$(b_2, b_3, \dots)$; si $b_1 = 1$ entonces $x \in [\frac23, 1]$
y $c(x) = \frac12 + \frac12c(3x - 2)$, mismo turno. Por
Por inducción, los primeros dígitos binarios $N$ de $c(x)$ son $b_1,
\dots, b_N$para cada$N$:$c(x) = \sum_nb_n2^{-n}$.

\textbf{4.} Apagado $C$, $c$ es localmente constante: diferenciable
con derivado $0$. Desde $\lambda(C) = 0$
(\cref{ex:b3:measure:cantor}), $c' = 0$ en casi todas partes. todavía
$c(1) - c(0) = 1$: el teorema fundamental en su forma $\mathcal
C^1$requiere que$c$ sea diferenciable \emph{en todos lados}
con \omterm{def:b3:topology:continuity}{continuo} (o al menos integrable, más absoluto
\omterm{def:b3:topology:continuity}{continuidad} --- ver \cref{ch:b3:lebesgue}) derivado; $c$ es
no diferenciable en puntos de $C$, y más fundamentalmente $c$
falla \emph{absolute \omterm{def:b3:topology:continuity}{continuidad}}: sube en un conjunto nulo.

\textbf{5.} Dado $y = \sum_n\beta_n2^{-n} \in \intcc01$
($\beta_n \in \{0,1\}$), el punto $x = \sum_n2\beta_n3^{-n}
\in C$tiene$c(x) = y$por la pregunta 3:$c(C) = \intcc01$, un conjunto
de \omterm{def:b3:measure:measure}{medida} $1$ --- el conjunto nulo $C$ lleva, a través de $c$, el
todo el intervalo.

\textbf{6.} $h$ es \omterm{def:b3:topology:continuity}{continuo}, y estrictamente creciente ($x$ es,
$c$ no es decreciente); $h(0) = 0$, $h(1) = 1$, así que por el
El teorema del valor intermedio $h$ es una biyección \omterm{def:b3:topology:continuity}{continuo} de
$\intcc01$; una biyección \omterm{def:b3:topology:continuity}{continuo} de una \omterm{def:b3:topology:compact}{espacio compacto} a una
\omterm{def:b3:topology:hausdorff}{Hausdorff} uno es un \omterm{def:b3:topology:continuity}{homeomorfismo}
(\cref{cor:b3:topology:compacthomeo}).

\textbf{7.} En un espacio $(u, v)$ (longitud $\ell$), $c$ es constante,
entonces $h$ es afín con la pendiente $\frac12$: $h((u,v))$ es un
intervalo de longitud $\frac\ell2$. Los espacios son disjuntos y $h$
es inyectivo: las imágenes son disjuntas, del total \omterm{def:b3:measure:measure}{medida}
$\frac12\sum\ell = \frac12(1 - \lambda(C)) = \frac12$.

\textbf{8.} $h(\intcc01) = \intcc01$ y $h(C)$ es \omterm{def:b3:topology:compact}{compacto}
(imagen \omterm{def:b3:topology:continuity}{continuo}), por lo tanto medible, con
\[
\lambda\bigl(h(C)\bigr) = 1 -
\lambda\bigl(h(\intcc01\setminus C)\bigr) = 1 - \tfrac12 =
\tfrac12 .
\]
Un \omterm{def:b3:topology:continuity}{homeomorfismo} puede inflar un conjunto nulo a \omterm{def:b3:measure:measure}{medida} $\frac12$:
``\omterm{def:b3:topology:topology}{topológico} tamaño'' (categoría, dimensión) y ``\omterm{def:b3:measure:measure}{medida}'' son
transportado por \omterm{def:b3:topology:continuity}{homeomorfismos} de manera muy diferente --- sólo el
El primero es un invariante \omterm{def:b3:topology:topology}{topológico}.

\textbf{9.} $Z = h^{-1}(W) \subseteq h^{-1}(h(C)) = C$ tiene
$\lambda^*(Z) \leq \lambda(C) = 0$: un conjunto nulo, por lo tanto
Lebesgue-medible por \omterm{def:b3:complete:complete}{lo completo}
(\cref{thm:b3:measure:caratheodory}).

\textbf{10.} Sea $\varphi$ \omterm{def:b3:topology:continuity}{continuo} y $\mathcal D = \{B
: \varphi^{-1}(B) \in \mathcal B\}$. Las preimágenes conmutan con
complementos y uniones contables, por lo que $\mathcal D$ es un
$\sigma$-álgebra; contiene el \omterm{def:b3:topology:topology}{conjuntos abiertos} (\omterm{def:b3:topology:continuity}{continuidad}):
$\mathcal D \supseteq \mathcal B$ --- preimágenes de conjuntos de Borel
bajo \omterm{def:b3:topology:continuity}{mapas continuos} son Borel.

\textbf{11.} Si $Z$ fuera Borel, aplique la pregunta 10 a la
\omterm{def:b3:topology:continuity}{continuo} $\varphi = h^{-1}$: $\varphi^{-1}(Z) = h(Z) = W$
sería Borel, por lo tanto mensurable según Lebesgue --- contradiciendo la
elección de $W$. Entonces $Z \in \mathcal L \setminus \mathcal B$:
El álgebra $\sigma$ de Lebesgue contiene estrictamente la de Borel.

\textbf{12.} $g = \mathbf 1_Z$ es medible según Lebesgue ($Z \in
\mathcal L$) y$\varphi = h^{-1}$ es \omterm{def:b3:topology:continuity}{continuo}, pero
$(g\circ\varphi)^{-1}(\{1\}) = \varphi^{-1}(Z) = W$ no es
medible: $g \circ \varphi$ no es medible según Lebesgue. el
cuidado necesario: ``función mensurable de Lebesgue'' significa preimágenes
de \emph{Borel} fija tierra en $\mathcal L$; componer requiere
preimágenes de \emph{Lebesgue} se establece como Lebesgue, que
\omterm{def:b3:topology:continuity}{continuidad} no concede (aquí $\varphi^{-1}(Z) \notin
\mathcal L$aunque$\varphi$ es \omterm{def:b3:topology:continuity}{homeomorfismo}).

\textbf{13.} Las cadenas, con testigos del rigor:
\[
\{\text{countable}\} \subsetneq \{\text{Borel null}\}
\subsetneq \{\text{Lebesgue null}\} \subsetneq \mathcal L
\subsetneq \mathcal P(\R),
\qquad
\{\text{Borel null}\} \subsetneq \mathcal B \subsetneq
\mathcal L .
\]
Testigos: $C$ es Borel, nulo, incontable (primer espacio); $Z$ es
Lebesgue nulo pero no Borel (segundo y, dentro de $\mathcal B
\subsetneq \mathcal L$, sexto); un conjunto gordo de Cantor es Borel,
en ninguna parte denso, de \omterm{def:b3:measure:measure}{medida} positivo (que separa conjuntos nulos de
conjuntos de Borel); El $V$ de Vitali no está en $\mathcal L$ (último espacio).
\omterm{def:b3:measure:measure}{Medida}, \omterm{def:b3:topology:topology}{topología} y corte de cardinalidad $\mathcal P(\R)$ a lo largo
líneas realmente diferentes.

\textbf{14.} \omterm{def:b3:measure:outer}{medida exterior}: $\mu_F^*(\varnothing) = 0$ (cubierta
por un intervalo de fuga), la monotonicidad es clara y contable
la subaditividad sigue mediante el empalme $\varepsilon2^{-k}$-óptimo
cubiertas, exactamente como para $\lambda^*$. La portada de un intervalo
da $\mu_F^*(\intoc ab) \leq F(b) - F(a)$. por el contrario dejar
$\intoc ab \subseteq \bigcup_k\intoc{a_k}{b_k}$. Por \omterm{def:b3:topology:continuity}{continuidad}
de $F$, elija $b_k' > b_k$ con $F(b_k') \leq F(b_k) +
\varepsilon2^{-k}$y$a' \in \intoo ab$con$F(a') \leq
F(a) + \varepsilon$. El \omterm{def:b3:topology:compact}{compacto}$\intcc{a'}b$ está cubierto por
el abierto $\intoo{a_k}{b_k'}$: un número finito es suficiente, y el
argumento de encadenamiento de \cref{thm:b3:measure:lebesgue} (caminar desde
$a'$ a $b$ mediante intervalos superpuestos, telescópicos
$F$ (incrementos, superposiciones que absorben monotonicidad) produce $F(b)
- F(a') \leq \sum_k(F(b_k') - F(a_k)) \leq \sum_k(F(b_k) -
F(a_k)) + \varepsilon$. Sea$\varepsilon \to 0$: igualdad.
(\omterm{def:b3:topology:continuity}{Continuidad} de $F$ es lo que permitió abrir los intervalos en
costo $F$ arbitrariamente pequeño.)

\textbf{15.} Es suficiente probar cada media línea $H_t =
\intoc{-\infty}t$ Carathéodory-mensurable, ya que el
conjuntos medibles forman un álgebra $\sigma$
(\cref{thm:b3:measure:caratheodory}) y medias líneas generan
$\mathcal B$. Dado $A$ y una cobertura óptima $\varepsilon$
$(\intoc{a_k}{b_k})$ de $A$: cada intervalo se divide como
$\intoc{a_k}{t\wedge b_k} \cup \intoc{t \vee a_k}{b_k}$ (uno
pieza posiblemente vacía), con costos $F$ sumando exactamente
$F(b_k) - F(a_k)$; las primeras piezas cubren $A \cap H_t$, las
segundo $A \setminus H_t$. Por lo tanto $\mu_F^*(A\cap H_t) +
\mu_F^*(A\setminus H_t) \leq \mu_F^*(A) + \varepsilon$, y
la desigualdad inversa es la subaditividad. Restringir el
resultante \omterm{def:b3:measure:measure}{medida} a $\mathcal B$: el Lebesgue--Stieltjes
\omterm{def:b3:measure:measure}{medida} $\mu_F$.

\textbf{16.}$\mu(\R) = \lim_n(F(n) - F(-n)) = 1 - 0 = 1$
(\omterm{def:b3:topology:continuity}{continuidad} del \omterm{def:b3:measure:measure}{medida} junto con $\intoc{-n}n$). en una brecha
$\intoo uv$ de $C$, $c$ es constante, por lo que cada medio abierto
subintervalo es $\mu$-nulo y también lo es la brecha (contable
unión); fuera de $\intcc01$, $c$ también es constante. Por lo tanto
$\mu(\R\setminus C) = 0$, $\mu(C) = 1$, mientras que $\lambda(C) =
0$(\cref{ex:b3:measure:cantor}): cada uno de$\mu, \lambda$ es
llevado por un conjunto que el otro declara nulo --- mutuamente
singular.

\textbf{17.} $\mu(\{x\}) = \lim_{\delta\downarrow0}
\mu(\intoc{x-\delta}x) = \lim(c(x) - c(x-\delta)) = 0$ por
\omterm{def:b3:topology:continuity}{continuidad} de $c$: sin átomos. Entonces $\mu$ es un átomo
probabilidad \omterm{def:b3:measure:measure}{medida} llevada por un Lebesgue-null \omterm{def:b3:topology:compact}{compacto} ---
ni difuso con densidad como las restricciones de $\lambda$,
ni atómico como contar \omterm{def:b3:measure:measure}{medidas}: una tercera especie.

\textbf{18.} La pieza $C_\varepsilon$ abarca un ternario
intervalo $I_\varepsilon$ de longitud $3^{-m}$, y Parte I
La pregunta 3 muestra que al otro lado de $I_\varepsilon$ la escalera
sube exactamente $2^{-m}$ (los primeros dígitos binarios $m$ de $c$
están congelados en $\varepsilon$, el resto barre todo).
Por lo tanto $\mu(C_\varepsilon) = \mu(I_\varepsilon) =
c(\text{right end}) - c(\text{left end}) = 2^{-m}$: profundidad-$m$
Todos los cilindros de dígitos tienen masa $2^{-m}$, la ley de $m$ es justa.
monedas.

\textbf{19.} El lado derecho define el Borel \omterm{def:b3:measure:measure}{medida}
$\nu = \frac12\,(x \mapsto \tfrac x3)_*\mu + \frac12\,(x
\mapsto \tfrac{x+2}3)_*\mu$evaluado en$A$ --- probabilidad
\omterm{def:b3:measure:measure}{medida}. Puntualmente, con $c$ extendido globalmente, se verifica
caso por caso ($x \leq 0$; los tres tercios; $x \geq 1$) la
identidad
\[
c(x) = \tfrac12\,c(3x) + \tfrac12\,c(3x - 2),
\]
por ejemplo\ en $\intcc{1/3}{2/3}$: $\frac12\cdot1 + \frac12\cdot0 =
\frac12 = c(x)$. Evaluando$\nu$en$\intoc ab$ por lo tanto
da $c(b) - c(a) = \mu(\intoc ab)$, y dos finitos \omterm{def:b3:measure:measure}{medidas}
acordar el sistema $\pi$ de intervalos semiabiertos coinciden
en $\mathcal B$ (\cref{thm:b3:measure:uniqueness}): $\nu =
\mu$.

\textbf{20.} Por inducción en $n$: $c_0(1-x) = 1 - c_0(x)$,
y si $c_n$ tiene la simetría, entonces para $x \in
\intcc0{1/3}$:$c_{n+1}(1 - x) = \frac12 + \frac12c_n(3(1-x)
- 2) = \frac12 + \frac12c_n(1 - 3x) = \frac12 + \frac12(1 -
c_n(3x)) = 1 - c_{n+1}(x)$; el tercio medio refleja alrededor
$\frac12$; el tercio derecho es el caso izquierdo reflejado. en el
límite $c(1-x) = 1 - c(x)$. Pushforward: $(s_*\mu)(\intoc ab)
= \mu(\intco{1-b}{1-a}) = c(1-a) - c(1-b)$($\mu$ sin átomos,
pregunta 17, por lo que las convenciones de límites no cuestan nada) $= (1 -
c(a)) - (1 - c(b)) = \mu(\intoc ab)$:$s_*\mu = \mu$ por
singularidad.

\textbf{21.} Deje que $X \sim \mu$ (integrales de \omterm{def:b3:topology:continuity}{continuo}
Las funciones contra $\mu$ existen como límites de sumas de tipo Riemann.
sobre la profundidad-$m$ piezas, cada una de masa $2^{-m}$, con
error de muestreo $\leq \operatorname{osc} \leq
\norm{f'}_\infty3^{-m}$; \cref{ch:b3:lebesgue} lo hará
sistematizar esto). Simetría: $1 - X \sim X$, entonces $\E X =
\frac12$. Auto-semejanza:$X$tiene la ley de$\frac Y3$ con
probabilidad $\frac12$ y de $\frac{Y+2}3$ con probabilidad
$\frac12$, $Y \sim \mu$, entonces
\[
\E X^2 = \frac12\,\frac{\E Y^2}9 + \frac12\,
\frac{\E Y^2 + 4\E Y + 4}{9} = \frac{2\E X^2 + 6}{18},
\]
de donde $\E X^2 = \frac38$ y $\operatorname{Var}X = \frac38
- \frac14 = \frac18$. La ley uniforme tiene variación.
$\frac1{12} < \frac18$: La masa de Cantor abraza los puntos finales.

\textbf{22.} $C$ está cerrado y $\mu(C) = 1$. Si hay un $I$ abierto
cumple con $C$ en $x$, las piezas de profundidad $m$ que contienen $x$ se contraen
a $x$, por lo que algunos $I_\varepsilon \subseteq I$ y $\mu(I) \geq
2^{-m} > 0$: ningún conjunto cerrado más pequeño puede transportar$\mu$. Puntos
de $C$ tiene espacio \omterm{def:b3:topology:topology}{barrios} de $\mu$-medida $0$. Por lo tanto
$\operatorname{supp}\mu = C$ exactamente.

\textbf{23.} Un fenómeno, dos dialectos. La parte I dice
el crecimiento de $c$ es invisible para su derivado: $c' = 0$ a.e.,
toda la escalada se concentró en el conjunto nulo $C$. Parte V
dice el \omterm{def:b3:measure:measure}{medida} $\mu_c$ asociado pone toda su masa en eso
mismo conjunto nulo: $\mu_c \perp \lambda$, por lo que ninguna densidad $f \geq
0$puede satisfacer$\mu_c(A) = \int_Af\,\dd\lambda$ --- una densidad
fuerzas que desaparecen en conjuntos nulos $\lambda$. El diccionario:
acotado no decreciente $F \leftrightarrow$ finito \omterm{def:b3:measure:measure}{medida}
$\mu_F$ (preguntas 14--15); $F$ una integral de su derivada
$\leftrightarrow$ $\mu_F$ tiene una densidad (la ``absolutamente
caso \omterm{def:b3:topology:continuity}{continuo}''); y en general $F'$, que existe a.e.\
para monótono $F$ (teorema de diferenciación de Lebesgue, más allá
este capítulo), recupera sólo la parte de densidad. la escalera
es el extremo: \omterm{def:b3:topology:continuity}{continuo}, con derivada $0$ a.e.\ ---
su \omterm{def:b3:measure:measure}{medida} es \emph{puramente singular}, y el fundamental
El teorema del cálculo, lejos de fallar por accidente, falla por
la cantidad exacta $\mu_c(\R) = 1$ de masa singular.

\textbf{24.} Reparar $x < y$ en $\intcc01$ y elegir $m \geq 0$
con $3^{-(m+1)} < y - x \leq 3^{-m}$. A través de cualquier triádica
intervalo $\intcc{k3^{-m}}{(k+1)3^{-m}}$ la escalera sube
como máximo $2^{-m}$: dicho intervalo es uno de los $2^m$
piezas de $C_m$, donde la subida es exactamente $2^{-m}$ (pregunta
18), o está contenido en el \omterm{def:b3:topology:interior}{cierre} de un único hueco de algunos
etapa $\leq m$, donde $c$ es constante. Desde $y - x \leq
3^{-m}$, el intervalo$\intcc xy$ cumple como máximo dos
intervalos triádicos consecutivos de profundidad-$m$, por lo que
\[
c(y) - c(x) \leq 2\cdot2^{-m}
= 2\bigl(3^{-m}\bigr)^{s}
< 2\bigl(3(y - x)\bigr)^{s}
= 4\,(y - x)^{s},
\]
utilizando $3^{s} = 2$ y $3^{-m} < 3(y - x)$. Optimidad: la
los puntos finales $u < v$ de una pieza de Cantor de profundidad-$m$ satisfacen $v - u =
3^{-m}$y$c(v) - c(u) = 2^{-m} = (v - u)^{s}$; un Hölder
enlazado $\abs{c(v) - c(u)} \leq K(v - u)^{t}$ con $t > s$ sería
fuerza $K \geq 2^{-m}3^{mt} = (3^{t}/2)^{m} \to \infty$ (desde
$3^{t} > 3^{s} = 2$), y cada subintervalo de $\intcc01$
contiene tales piezas, por lo que la falla también es local.
Formulario \omterm{def:b3:measure:measure}{Medida}: $\mu\bigl(\intcc{x-r}{x+r}\bigr) \leq
c(x + r) - c(x - r) \leq 4(2r)^{s} = 4\cdot2^{s}r^{s} \leq
8r^{s}$(valores de$c$extendidos a$\R$ como en la pregunta 16;
$\mu$ no tiene átomos, pregunta 17).

\textbf{25.} Escriba $S_0(x) = \frac x3$ y $S_1(x) =
\frac{x+2}3$; la hipótesis dice$\nu = \frac12(S_0)_*\nu +
\frac12(S_1)_*\nu$. Iterando$m$ veces,
\[
\nu = 2^{-m}\sum_{w \in \{0,1\}^m}(S_w)_*\nu,
\qquad S_w = S_{w_1}\circ\dots\circ S_{w_m}.
\]
Dado que $\nu$ es transportado por $\intcc01$ y $S_w(\intcc01) =
I_w$, la pieza de Cantor de profundidad$m$indexada por$w$, cada una
$(S_w)_*\nu$ es una probabilidad \omterm{def:b3:measure:measure}{medida} transportada por $I_w$; el
$2^m$ las piezas son intervalos cerrados de longitud separados por pares
$3^{-m}$. Corrige $\intoc ab$ y deja que $N_m$ sea el número de
piezas $I_w \subseteq \intoc ab$. Una reunión de piezas $\intoc ab$
sin estar contenido en el mismo debe contener $a$ o $b$, y un
El punto se encuentra como máximo en una pieza, por lo que
\[
2^{-m}N_m \leq \nu(\intoc ab) \leq 2^{-m}N_m + 2\cdot2^{-m}.
\]
La misma doble desigualdad se aplica a $\mu$ (la pregunta 19 da
la iteración idéntica), con el mismo recuento $N_m$. Por lo tanto
$\abs{\nu(\intoc ab) - \mu(\intoc ab)} \leq 2^{-m+1} \to 0$:
$\nu$ y $\mu$ coinciden en el sistema $\pi$ de semiabierto
intervalos, y ambos son probabilidad \omterm{def:b3:measure:measure}{medidas}, por lo que
\cref{thm:b3:measure:uniqueness} proporciona $\nu = \mu$. el
escalera \omterm{def:b3:measure:measure}{medida} es, por tanto, \emph{el} punto fijo de la
esquema de promedio de dos mapas --- la declaración a nivel de medida de
la autosimilitud de $C$.
\end{solution}