\section*{Capítulo \ref{ch:b3:complete} --- Complete Spaces:
Baire, Ascoli, Stone--Weierstrass}

\begin{solution}{exo:b3:complete:1}
(a) Para $m \geq n$, $f_n - f_m$ desaparece fuera de un intervalo de
de longitud $\frac1n$ y está delimitado por $1$: $\norm{f_n - f_m}_1
\leq \frac1n \to 0$: Cauchy. Si$f_n \to f$ en
$\norm\cdot_1$ con $f$ \omterm{def:b3:topology:continuity}{continuo}: para fijo $\alpha <
\frac12$,$\int_0^{\alpha}\abs f = \lim \int_0^\alpha\abs{f -
f_n + f_n} \leq \lim\bigl(\norm{f - f_n}_1 + 0\bigr) = 0$
($f_n \equiv 0$ existe para $n$ grande), por lo que $f \equiv 0$ en
$[0, \frac12)$ (\omterm{def:b3:topology:continuity}{continuidad}); igualmente $f \equiv 1$ en
$(\frac12, 1]$: ninguna función \omterm{def:b3:topology:continuity}{continuo} hace ambas cosas. entonces el espacio
está incompleto --- el \omterm{thm:b3:complete:completion}{terminación} es $L^1$, integrado
\cref{ch:b3:lp}.

(b) Sea $(x_n)$ Cauchy; elija $n_1 < n_2 < \cdots$ con
$\norm{x_{n_{k+1}} - x_{n_k}} \leq 2^{-k}$. la serie
$\sum_k (x_{n_{k+1}} - x_{n_k})$ converge absolutamente, por lo tanto
converge; sus sumas parciales son $x_{n_{k+1}} - x_{n_1}$, entonces
$(x_{n_k})$ converge y una secuencia de Cauchy con un convergente
la subsecuencia converge.
\end{solution}

\begin{solution}{exo:b3:complete:2}
(a) Supongamos $E$ \omterm{def:b3:complete:complete}{completo} con base \omterm{def:b3:galois:algebraic}{algebraico} $(e_n)_{n\in\N}$
y dejamos $F_n = \operatorname{Vect}(e_1, \dots, e_n)$: cerrado
(los subespacios de dimensión finita son \omterm{def:b3:complete:complete}{completo}, por lo tanto cerrados ---
Año 2), con \omterm{def:b3:topology:interior}{interior} vacío: si $B(x, r) \subseteq F_n$, tomar
$v \notin F_n$; luego $x + \frac{r}{2\norm v}v \in B(x,r)
\setminus F_n$, absurdo. Pero$E = \bigcup F_n$ (cada vector es
una combinación finita): contradice a Baire
(\cref{thm:b3:complete:baire}). El espacio $\R[X]$ tiene el
base contable $(X^n)$, por lo que ninguna norma lo convierte en \omterm{def:b3:complete:complete}{completo}.

(b) Fijar $\delta > 0$ y una bola abierta no vacía $B$; encontramos en
$B$ un punto de $\Omega_\delta = \{x : \operatorname{osc}_xf <
\delta\}$, donde$\operatorname{osc}_xf = \inf_{V \ni
x}\operatorname{diam} f(V)$. ($\Omega_\delta$ está abierto: si
$\operatorname{diam}f(V) < \delta$ para un $V \ni x$ abierto, cada
$y \in V$ tiene oscilación $< \delta$.) Los conjuntos
\[
F_N = \bigl\{x : \abs{f_n(x) - f_m(x)} \leq \tfrac\delta3\
\ \forall\, n, m \geq N \bigr\}
\]
son cerrados (intersecciones de preimágenes de conjuntos cerrados) y
cubrir $\R$ (la convergencia puntual hace que $(f_n(x))$ Cauchy).
Aplicando Baire dentro del \omterm{def:b3:complete:complete}{completo} $\bar B$: algún $F_N \cap
\bar B$contiene una bola$B' = B(x_0, \rho)$. Dejando$m \to
\infty$:$\abs{f_N - f} \leq \frac\delta3$en$B'$. Por
\omterm{def:b3:topology:continuity}{continuidad} de $f_N$ en $x_0$, reducir a $B'' \ni x_0$ donde
$\abs{f_N - f_N(x_0)} \leq \frac\delta3$; luego para $x \in B''$,
\[
\abs{f(x) - f(x_0)} \leq \abs{f - f_N}(x) + \abs{f_N(x) -
f_N(x_0)} + \abs{f_N - f}(x_0) \leq \delta:
\]
$\operatorname{diam} f(B'') \leq 2\delta$, entonces $x_0 \in
\Omega_{3\delta} \cap B$. Por lo tanto, cada$\Omega_\delta$ está abierto y
denso; $\bigcap_k\Omega_{1/k}$ --- el conjunto de puntos \omterm{def:b3:topology:continuity}{continuidad}
--- es denso por Baire.
\end{solution}

\begin{solution}{exo:b3:complete:3}
(a) $f(x) = \frac12(x + \frac ax)$ asigna $[\sqrt a, \infty)$ a
mismo (AM--GM: $f(x) \geq \sqrt{x \cdot \frac ax} = \sqrt a$),
y $f'(x) = \frac12(1 - \frac a{x^2}) \in [0, \frac12)$ ahí:
una contracción $\frac12$-Lipschitz de un conjunto cerrado (\omterm{def:b3:complete:complete}{completo}).
Punto fijo: $x = f(x) \iff x^2 = a$: $x^* = \sqrt a$. Tarifa
(\cref{thm:b3:complete:banach}): $d(x_n, \sqrt a) \leq
2^{-n+1}\,d(x_1, x_0)$. Para$a = 2$,$x_0 = 2$:$d(x_1, x_0) =
\frac12$, por lo que$n = 40$garantiza$2^{-40} < 10^{-12}$. (En
realidad el método de Newton converge cuadráticamente: un puñado de
las iteraciones son suficientes; la estimación de contracción es pesimista pero
gratis.)

(b) $f_m(x) = m + e\sin x$ es $e$-Lipschitz con $e < 1$ en el
\omterm{def:b3:complete:complete}{completo} $\R$: punto fijo único $x(m)$. Para dos parámetros:
\[
\abs{x(m) - x(m')} = \abs{f_m(x(m)) - f_{m'}(x(m'))}
\leq \abs{m - m'} + e\abs{x(m) - x(m')},
\]
entonces $\abs{x(m) - x(m')} \leq \frac{\abs{m - m'}}{1 - e}$: incluso
Lipschitz en $m$.
\end{solution}

\begin{solution}{exo:b3:complete:4}
(a) Sea $x^*$ el único punto fijo de $f^p$. entonces
$f^p(f(x^*)) = f(f^p(x^*)) = f(x^*)$: $f(x^*)$ es un punto fijo
de $f^p$, entonces $f(x^*) = x^*$. Un punto fijo de $f$ es uno de
$f^p$: transferencias de unicidad. Para $T$: por inducción $\abs{T^pf(x)}
\leq \norm f_\infty x^p/p!$ (cada integración suma un factor
$\frac xk$), entonces $\norm{T^p} \leq a^p/p!$. La aplicación $S(u) = g +
\lambda Tu$satisface$S^p(u) - S^p(v) = \lambda^pT^p(u - v)$,
de norma $\leq \abs\lambda^pa^p/p!\,\norm{u - v} \to 0$: algunos
$S^p$ es una contracción y $S$ tiene un punto fijo único: el
La ecuación de Volterra $u = g + \lambda Tu$ es exclusivamente \omterm{def:b3:groups:derived}{soluble} para
\emph{cada} $\lambda$.

(b) $\varphi(x) = d(x, f(x))$ es \omterm{def:b3:topology:continuity}{continuo} en \omterm{def:b3:topology:compact}{compacto} $K$:
alcanza su mínimo en aproximadamente $x_0$. Si $f(x_0) \neq x_0$,
entonces $\varphi(f(x_0)) = d(f(x_0), f^2(x_0)) < d(x_0, f(x_0)) =
\min\varphi$: absurdo. Unicidad: dos puntos fijos$x \ne y$
daría $d(x,y) = d(f(x), f(y)) < d(x,y)$. sin
compacidad: $f(x) = x + \frac1x$ en $[1, \infty)$ satisface,
para $x < y$, $f(y) - f(x) = (y - x)\bigl(1 - \frac1{xy}\bigr) <
y - x$, pero$f(x) > x$ siempre: sin punto fijo --- estricto
La disminución de la distancia es más débil que un factor de contracción uniforme.
\end{solution}

\begin{solution}{exo:b3:complete:5}
(a) El conjunto $A = \{g = h\}$ es la preimagen de la diagonal
$\Delta_Y$ bajo $x \mapsto (g(x), h(x))$, \omterm{def:b3:topology:continuity}{continuo}; $\Delta_Y$
está cerrado porque $Y$ es \omterm{def:b3:topology:hausdorff}{Hausdorff} (para $y_1 \neq y_2$,
disjunto abierto \omterm{def:b3:topology:topology}{barrios} da un cuadro abierto alrededor de $(y_1,
y_2)$ disjunto de la diagonal, por lo que el complemento de
$\Delta_Y$ está abierto): entonces $A$ está
cerrado, contiene un \omterm{def:b3:topology:interior}{conjunto denso}, es igual a $X$. Usado: singularidad en
\cref{thm:b3:complete:extension}, de ahí la unicidad de
\omterm{thm:b3:complete:completion}{terminaciones}.

(b) $f$, una isometría, es uniformemente \omterm{def:b3:topology:continuity}{continuo}: se extiende a
$F\colon X \to Y$ (\cref{thm:b3:complete:extension}), todavía
isométrica (la relación $d(F(x), F(x')) = d(x, x')$ se mantiene en un
\omterm{def:b3:topology:interior}{conjunto denso} de pares y ambos lados son \omterm{def:b3:topology:continuity}{continuo}). igualmente
$f^{-1}\colon f(D) \to X$ se extiende a $G \colon Y \to X$. el
compuesto $G \circ F$ es \omterm{def:b3:topology:continuity}{continuo} y corrige el denso $D$:
es $\mathrm{id}_X$ (parte (a)); simétricamente $F \circ G =
\mathrm{id}_Y$. Entonces$F$ es una biyección isométrica. Unicidad de
\omterm{thm:b3:complete:completion}{terminaciones}: aplica esto a $D = X$ sentado densamente en dos
\omterm{thm:b3:complete:completion}{terminaciones}.
\end{solution}

\begin{solution}{exo:b3:complete:6}
$\{\sin(nx)\}$: delimitado puntualmente por $1$; \emph{no}
\omterm{def:b3:complete:equicontinuous}{equicontinuo}: en $x = 0$, $\sin(n\cdot\frac{\pi}{2n}) = 1$
con $\frac\pi{2n} \to 0$, violando cualquier $\delta$ común para
$\varepsilon = \frac12$. No relativamente \omterm{def:b3:topology:compact}{compacto} (Ascoli's
necesidad, \cref{thm:b3:complete:ascoli}).

$\{x^n\}$: acotado; no \omterm{def:b3:complete:equicontinuous}{equicontinuo} en $1$: $1 - (1 -
\delta)^n \to 1$como$n \to \infty$para$\delta$ fijo. no
relativamente \omterm{def:b3:topology:compact}{compacto} --- consistentemente, su límite puntual es
discontinuo, por lo que ninguna subsecuencia converge uniformemente.

$\{\norm f_\infty \leq 1, \norm{f'}_\infty \leq 1\}$: la media
La desigualdad de valores hace que la familia $1$-Lipschitz, por lo tanto
\omterm{def:b3:complete:equicontinuous}{equicontinuo}; delimitado: relativamente \omterm{def:b3:topology:compact}{compacto} por Ascoli. (No
\omterm{def:b3:topology:compact}{compacto}: no está cerrado --- no es necesario establecer límites uniformes
$\mathcal C^1$; su \omterm{def:b3:topology:interior}{cierre} es la función $1$-Lipschitz de
norma $\leq 1$.)

$\{\operatorname{Lip}(f) \leq 1, f(0) = 0\}$: \omterm{def:b3:complete:equicontinuous}{equicontinuo};
acotado puntualmente ($\abs{f(x)} \leq x \leq 1$); y cerrado
bajo límites uniformes (la desigualdad de Lipschitz y el valor en
$0$ pase a límites): \omterm{def:b3:topology:compact}{compacto}.
\end{solution}

\begin{solution}{exo:b3:complete:7}
(a) Para $\norm f_\infty \leq 1$: $\abs{Tf(x)} \leq
\norm k_\infty$, y
\[
\abs{Tf(x) - Tf(x')} \leq \int_0^1\abs{k(x,y) -
k(x',y)}\,\dd y \leq \omega_k\bigl(\abs{x - x'}\bigr),
\]
donde $\omega_k$ es un módulo de \omterm{def:b3:topology:continuity}{continuidad} uniforme de $k$ en
el cuadrado \omterm{def:b3:topology:compact}{compacto} (Heine): la imagen de la bola unitaria es
uniformemente delimitado y \omterm{def:b3:complete:equicontinuous}{equicontinuo}, por lo tanto relativamente \omterm{def:b3:topology:compact}{compacto}
(Ascoli). Por linealidad todo conjunto acotado tiene relativamente \omterm{def:b3:topology:compact}{compacto}
Imagen: $T$ es un operador \omterm{def:b3:topology:compact}{compacto}.

(b) La declaración de subsecuencia es la definición de relativa
compacidad aplicada a $\{Tf_n\}$. Si $T$ fuera biyectivo con
\omterm{def:b3:topology:continuity}{continuo} inversa, luego $T(B(0,1)) \supseteq B(0,
\varepsilon)$para algunos$\varepsilon > 0$($T^{-1}$ \omterm{def:b3:topology:continuity}{continuo}
en $0$); la bola cerrada $\bar B(0,\varepsilon)$, una cerrada
subconjunto de \omterm{def:b3:topology:compact}{compacto} $\overline{T(B(0,1))}$, sería \omterm{def:b3:topology:compact}{compacto}
--- imposible en el $\mathcal
C(\intcc01)$ de dimensión infinita según el teorema de Riesz (Año 2).
\end{solution}

\begin{solution}{exo:b3:complete:8}
(a) Dini: ver \cref{lem:b3:complete:dini} --- la cubierta
argumento. (b) Falso: el tope en movimiento $f_n(x) = \max(0, 1 -
\abs{nx - 1})$tiende a$0$puntualmente (para$x > 0$,$f_n(x) =
0$una vez$n > 2/x$;$f_n(0) = 0$) pero$\norm{f_n}_\infty = 1$.
(c) Falso: $f_n(x) = x^n$ disminuye a lo discontinuo
$\mathbf 1_{\{1\}}$; $\sup_{[0,1]}\abs{f_n - f} = \sup_{[0,1)}
x^n = 1$. (d) Falso:$x^n \downarrow 0$ puntualmente en el
$\intoo01$ no compacto, con $\sup_{(0,1)}x^n = 1$. cada uno
Se necesita la hipótesis de Dini.
\end{solution}

\begin{solution}{exo:b3:complete:9}
(a) Por linealidad $\int_0^1 fP = 0$ para cada polinomio $P$.
Elija $P_n \to f$ uniformemente
(\cref{cor:b3:complete:weierstrass}): $\int_0^1 f^2 =
\lim\int_0^1 fP_n = 0$, y el \omterm{def:b3:topology:continuity}{continuo}$f^2 \geq 0$ con
La integral cero desaparece de manera idéntica.

(b) En $\intcc01$: los polinomios en $x^2$ forman un álgebra
con constantes, separando puntos ($x \mapsto x^2$ es inyectivo
en $\intcc01$): densa junto a Stone--Weierstrass. En $\intcc{-1}1$:
$x^2$ toma valores iguales en $\pm x$, y también lo hace cada
polinomio en $x^2$: un límite uniforme de tal es un par
función. Si las funciones pares $g_n$ convergieran uniformemente a la
identidad, luego $x = \lim g_n(x) = \lim g_n(-x) = -x$ para todos
$x$: absurdo --- no denso. La hipótesis de la separación falla en
los pares $\{x, -x\}$.

(c) No. Para $f$ en el álgebra $\mathcal A$ generada por
constantes y $\eu^{\iu t}$ --- combinaciones lineales de
$\eu^{\iu nt}$, $n \geq 0$ --- uno tiene $\Lambda(f) =
\int_0^{2\pi}f(t)\,\eu^{\iu t}\,\dd t = 0$ (cada uno
$\int_0^{2\pi}\eu^{\iu(n+1)t}\dd t = 0$). $\Lambda$ es
\omterm{def:b3:topology:continuity}{continuo} para $\norm\cdot_\infty$ ($\abs{\Lambda(f)}\leq
2\pi\norm f_\infty$), por lo que$\Lambda$desaparece en$\bar{\mathcal
A}$; pero$\Lambda(\eu^{-\iu t}) = 2\pi \neq 0$:$\eu^{-\iu t}
\notin \bar{\mathcal A}$. (Stone--Weierstrass no se aplica:
$\mathcal A$ no es estable bajo conjugación --- y el
obstrucción es precisamente la que función holomorfa
La teoría sistematizará en \cref{ch:b3:holomorphic}.)
\end{solution}

\begin{solution}{exo:b3:complete:10}
$F_n = \bigcap_{f \in \mathcal F}\{x : \abs{f(x)} \leq n\}$ es
una intersección de conjuntos cerrados: cerrado. Limitación puntual
da $X = \bigcup_n F_n$. Baire (\cref{thm:b3:complete:baire})
proporciona $n_0$ con $U = \mathring F_{n_0} \neq \varnothing$:
en $U$, $\abs f \leq n_0$ por cada $f \in \mathcal F$
simultáneamente.
\end{solution}

\begin{solution}{exo:b3:complete:11}
(a) Una secuencia de Cauchy para $\norm\cdot_{\mathcal C^1}$ es
uniformemente Cauchy junto con sus derivados: $f_n \to f$
y $f_n' \to g$ de manera uniforme, con $f, g$ \omterm{def:b3:topology:continuity}{continuo}. pasando
al límite (la convergencia uniforme lo permite bajo el
integral) en $f_n(x) = f_n(0) + \int_0^xf_n'(t)\,\dd t$
da $f(x) = f(0) + \int_0^xg$: $f$ es $\mathcal C^1$ con
$f' = g$ y $\norm{f_n - f}_{\mathcal C^1} \to 0$.
\omterm{def:b3:complete:complete}{Completo}.

(b) $h(x) = \abs{x - \frac12}$ es un límite uniforme de
Funciones $\mathcal C^1$, por ejemplo\ $h_n(x) = \sqrt{(x -
\frac12)^2 + \frac1n}$($\abs{h_n - h} \leq \frac1{\sqrt n}$
por el límite de cantidad conjugada), pero $h \notin E$: el
La norma $\sup$ en $E$ no es \omterm{def:b3:complete:complete}{completo} --- su \omterm{thm:b3:complete:completion}{terminación} es
$\mathcal C(\intcc01)$.

(c) $f_n(x) = \sin(2\pi nx)$ tiene $\norm{f_n}_\infty = 1$ y
$\norm{f_n'}_\infty = 2\pi n \to \infty$: no existe $C$.
(Conceptualmente: si la diferenciación estuviera limitada para el sup
norma, las dos normas de (a)--(b) serían equivalentes, haciendo
$(E, \norm\cdot_\infty)$ \omterm{def:b3:complete:complete}{completo} --- contradiciendo (b).
Esta ilimitación es exactamente la razón por la que el genérico \omterm{def:b3:topology:continuity}{continuo}
las funciones pueden no ser diferenciables en ninguna parte,
\cref{thm:b3:complete:nowherediff}.)
\end{solution}

\begin{solution}{exo:b3:complete:12}
Reparar $\varepsilon > 0$. Cada $F_N$ es una intersección sobre $n
\geq N$de preimágenes del$\intcc{-\varepsilon}
\varepsilon$cerrado bajo el \omterm{def:b3:topology:continuity}{continuo}$x \mapsto f(nx)$: cerrado.
La hipótesis dice que cada $x > 0$ se encuentra en algún $F_N$. Por
Baire aplicado dentro del \omterm{def:b3:complete:complete}{completo} $\intcc ab$ (cualquier $0 < a <
b$), algún$F_N$ es denso en un subintervalo; al estar cerrado,
contiene un intervalo $\intcc{a'}{b'}$ con $0 < a' < b'$.
Luego, por cada $n \geq \max(N, \frac{a'}{b' - a'})$, el
intervalos $\intcc{na'}{nb'}$ y $\intcc{(n+1)a'}{(n+1)b'}$
superposición ($nb' \geq (n+1)a'$), por lo que
\[
\bigcup_{n \geq n_0}\intcc{na'}{nb'} \supseteq
\intco{n_0a'}{+\infty},
\]
y cada $t \geq n_0a'$ es $t = nx$ con $n \geq N$, $x \in
\intcc{a'}{b'} \subseteq F_N$:$\abs{f(t)} \leq \varepsilon$.
Por lo tanto $\limsup_{t\to\infty}\abs f \leq \varepsilon$ para todos
$\varepsilon$: $f \to 0$. Se necesita la hipótesis completa
porque $F_N$ debe \emph{cubrir} un valor de intervalo de $x$
--- con solo $x$ racional la unión del $F_N$ es
contable y Baire no da nada; de hecho hay
\omterm{def:b3:topology:continuity}{continuo} contraejemplos que desaparecen a lo largo de todo lo racional
rayos pero no en el infinito.
\end{solution}

\begin{solution}{pb:b3:complete:1}
\textbf{1.} Inducción sobre $k$: si $\norm{\varphi_n(t_k) - x_0}
\leq M(t_k - t_0) \leq MT \leq b$, el punto$(t_k,
\varphi_n(t_k))$se encuentra en$R$, por lo que se define la pendiente$f(t_k,
\varphi_n(t_k))$, de norma$\leq M$; luego para$t \in
[t_k, t_{k+1}]$,$\norm{\varphi_n(t) - x_0} \leq
\norm{\varphi_n(t_k) - x_0} + M(t - t_k) \leq M(t - t_0) \leq
MT \leq b$.

\textbf{2.} Cada pieza afín tiene pendiente de norma $\leq M$; un
La función afín por partes con pendientes delimitadas por $M$ es
$M$-Lipschitz (encadenar los puntos de la cuadrícula).

\textbf{3.} La familia $(\varphi_n)$ está acotada puntualmente.
(valores en $\bar B(x_0, b)$) y \omterm{def:b3:complete:equicontinuous}{equicontinuo} (común
Constante de Lipschitz $M$): Ascoli
(\cref{thm:b3:complete:ascoli}) extrae $\varphi_{n_j} \to
\varphi$de manera uniforme en$[t_0, t_0 + T]$. Los límites pasan al
límite: $\varphi$ es $M$-Lipschitz, $\varphi(t_0) = x_0$.

\textbf{4.} $R$ es \omterm{def:b3:topology:compact}{compacto} y $f$ \omterm{def:b3:topology:continuity}{continuo}: uniformemente
\omterm{def:b3:topology:continuity}{continuo} (Heine, \cref{cor:b3:topology:heineborel}); dejar
$\delta(\varepsilon)$ sea un módulo. Para $t \in (t_k,
t_{k+1})$sin red:$\varphi_n'(t) = f(t_k, \varphi_n(t_k))$ y el
dos puntos de evaluación de $f$ difieren en $\abs{t - t_k} \leq T/n$
en el tiempo y $\norm{\varphi_n(t) - \varphi_n(t_k)} \leq MT/n$ en
espacio. Para $n \geq n_0$ con $(1 + M)T/n_0 < \delta(\varepsilon)$:
$\norm{\Delta_n(t)} \leq \varepsilon$.

\textbf{5.} En cada $[t_k, t_{k+1}]$, $\varphi_n$ es afín, por lo que
$\varphi_n(t_{k+1}) - \varphi_n(t_k) = \int_{t_k}^{t_{k+1}}
\varphi_n'(s)\dd s$, siendo la derivada la pendiente constante;
sumando piezas (y cortando la última en $t$):
$\varphi_n(t) = x_0 + \int_{t_0}^t\varphi_n'(s)\,\dd s$.
Escritura $\varphi_n' = f(s, \varphi_n(s)) + \Delta_n(s)$
(integrandos \omterm{def:b3:topology:continuity}{continuo} por partes, un número finito de saltos) da
la pantalla.

\textbf{6.} Dado $\varepsilon$: para $j$ grande,
$\norm{\varphi_{n_j} - \varphi}_\infty < \delta(\varepsilon)$,
entonces $\norm{f(s, \varphi_{n_j}(s)) - f(s, \varphi(s))} \leq
\varepsilon$para todos$s$: convergencia uniforme de
integrandos, y $\int_{t_0}^t f(s,\varphi_{n_j}(s))\dd s \to
\int_{t_0}^tf(s, \varphi(s))\dd s$uniformemente en$t$. también
$\norm{\int_{t_0}^t\Delta_{n_j}} \leq T\sup\norm{\Delta_{n_j}}
\to 0$ (pregunta 4). Pasando al límite en la pregunta 5
identidad: $\varphi(t) = x_0 + \int_{t_0}^tf(s,
\varphi(s))\,\dd s$.

\textbf{7.} El integrando $s \mapsto f(s, \varphi(s))$ es
\omterm{def:b3:topology:continuity}{continuo}, por lo que el lado derecho es $\mathcal C^1$ en $t$ con
derivada $f(t, \varphi(t))$: $\varphi$ resuelve el Cauchy
problema en $[t_0, t_0 + T]$. Para la mitad izquierda, configure $g(t, x) =
-f(2t_0 - t, x)$, \omterm{def:b3:topology:continuity}{continuo} en el rectángulo reflejado con
el mismo cota $M$; una solución $\psi$ de $y' = g(t,y)$,
$y(t_0) = x_0$ en $[t_0, t_0 + T]$ produce $\varphi(t) =
\psi(2t_0 - t)$resolviendo la ecuación original en$[t_0 - T,
t_0]$; las dos mitades se pegan a una solución$\mathcal C^1$ (ambas
derivados unilaterales en $t_0$ igual a $f(t_0, x_0)$). ---
\emph{teorema de peano}: a \omterm{def:b3:topology:continuity}{continuo} $f$ admite local
solución a través de cada condición inicial.

\textbf{8.} \omterm{def:b3:topology:continuity}{Continuidad} está claro. Lipschitz cerca de $0$ falla:
$\abs{f(h) - f(0)} = 2\sqrt h$ y $2\sqrt h \leq L h$ son
falso para $h < 4/L^2$.

\textbf{9.} Para $t \leq c$: $x_c \equiv 0$ resuelve. Para $t \geq
c$:$x_c'(t) = 2(t - c) = 2\sqrt{(t-c)^2} = 2\sqrt{\abs{x_c}}$.
En $t = c$ las derivadas unilaterales son $0$: $x_c$ es
$\mathcal C^1$ y resuelve globalmente, con $x_c(0) = 0$ para cada
$c \geq 0$ --- junto con $x \equiv 0$, un continuo de
soluciones a través del origen.

\textbf{10.} La iteración de Picard configura $\Phi(x)(t) = x_0 +
\int_0^t f(x(s))\dd s$y necesita$\norm{\Phi(x) - \Phi(y)}
\leq k\norm{x - y}$con$k < 1$ en una bola adecuada --- lo cual
se desprende de un Lipschitz cota en $f$, transferido bajo el
integral. Aquí $\sqrt\cdot$ no admite ningún Lipschitz atado cerca de $0$,
y ninguna elección de intervalo o bola lo repara. Y de hecho no
la prueba de unicidad podría tener éxito: la unicidad es \emph{FALSO}
(pregunta 9).

\textbf{11.} En $c_0$, los vectores unitarios $e_n = (0, \dots, 0, 1,
0, \dots)$satisfacen$\norm{e_n - e_m}_\infty = 1$para$n \neq
m$: ninguna subsecuencia es Cauchy, por lo que la bola unitaria cerrada no es
\omterm{def:b3:topology:compact}{compacto}. El paso que rompe es la extracción (pregunta 3):
Ascoli para $\mathcal C([t_0, t_0+T], E)$ requiere que los valores
vivir en un espacio donde los conjuntos acotados son relativamente \omterm{def:b3:topology:compact}{compacto} ---
verdadero en $\R^d$ (Bolzano--Weierstrass), falso en $c_0$; puntualmente
la extracción ya no está disponible (de hecho, el ejemplo de Dieudonné
no tiene ninguna solución local).

\textbf{12.} \emph{peano}: $f$ \omterm{def:b3:topology:continuity}{continuo} en un \omterm{def:b3:topology:topology}{entorno} de
$(t_0, x_0)$ en $\R\times\R^d$ $\Rightarrow$ existe un
$\mathcal C^1$ solución de $x' = f(t,x)$, $x(t_0) = x_0$, en
algunos $[t_0 - T, t_0 + T]$. \emph{Cauchy--Lipschitz}
(\cref{ch:b3:ode}): si además $f$ es localmente Lipschitz en el
Variable $x$, la solución es \emph{único} (cualesquiera dos están de acuerdo
su intervalo común) --- existencia y unicidad. la pareja
$(x' = 2\sqrt{\abs x},\ x(0) = 0)$ separa los dos teoremas.

\textbf{13.} $\omega(r) = Lr$: $\int_0^1\frac{\dd r}{Lr} =
+\infty$: califica.$\omega(r) = r\log\frac1r$(cerca de$0$):
$\int\frac{\dd r}{r\log(1/r)} = \bigl[-\log\log\frac1r\bigr]
\to +\infty$como$r \to 0$: califica --- pero$\omega(r)/r =
\log\frac1r \to \infty$: no Lipschitz.$\omega(r) = 2\sqrt
r$:$\int_0^1\frac{\dd r}{2\sqrt r} = \bigl[\sqrt r\bigr]_0^1
= 1 < \infty$: falla.

\textbf{14.} Resta las dos formas integrales:
\[
x_1(t) - x_2(t) = x_1(s) - x_2(s) + \int_s^t\bigl(f(v,
x_1(v)) - f(v, x_2(v))\bigr)\dd v,
\]
tomar normas y utilizar el módulo de Osgood: $\delta(t) \leq
\delta(s) + \int_s^t\omega(\delta(v))\,\dd v$.

\textbf{15.} $\tau$ está bien definido ($\delta(t_0) = 0$) con
$\delta(\tau) = 0$ (\omterm{def:b3:topology:continuity}{continuidad}) y $\delta > 0$ en
$\intoc\tau{t_1}$. Reparar $s \in \intoo\tau{t_1}$. Entonces $u$ es
$\mathcal C^1$, $u(s) = \delta(s) > 0$, $u \geq \delta$ en
$[s, t_1]$ (pregunta 14) y $u' = \omega(\delta) \leq
\omega(u)$($\omega$no decreciente,$u > 0$). dividir y
integrar:
\[
\int_{\delta(s)}^{u(t_1)}\frac{\dd r}{\omega(r)}
= \int_s^{t_1}\frac{u'(v)}{\omega(u(v))}\,\dd v \leq t_1 - s
\leq t_1 - \tau .
\]
Aunque $u$ depende de $s$, el límite $u(t_1) \geq
\delta(t_1)$se cumple para cada$s$, por lo que el lado izquierdo está en
menos $\int_{\delta(s)}^{\delta(t_1)}\frac{\dd r}{\omega(r)}$,
que tiende a $+\infty$ como $s \downarrow \tau$ (luego
$\delta(s) \to \delta(\tau) = 0$, y la integral diverge
en $0$): el lado derecho delimitado se contradice. Por lo tanto
$\delta \equiv 0$: unicidad.

\textbf{16.} Lipschitz es $\omega = Lr$: unicidad recuperada.
Para $x' = x\log\frac1{\abs x}$: el lado derecho satisface las
Módulo de Osgood $\omega(r) = r\log\frac1r$ cerca de $0$ (valor medio
desigualdad en $x \mapsto x\log\frac1x$, cuya derivada
$\log\frac1x - 1$ es ilimitado --- Lipschitz falla, Osgood
sostiene): soluciones únicas; nota $x \equiv 0$ es uno, así que no
Otra solución puede tocar $0$. Para $\omega(r) = 2\sqrt r$:
$\int_0^{h^2}\frac{\dd r}{2\sqrt r} = h$ --- el ``Osgood
presupuesto'' para subir desde $0$ a la altura $h^2$ es exactamente el
tiempo $h$, y de hecho $x_c(c + h) = h^2$: la solución pasa
tiempo $h$ haciendo precisamente lo que la integral convergente
permisos. La divergencia de la integral es la imposibilidad de
dejando $0$ en tiempo finito; la convergencia es la vía de escape.

\textbf{17.} $G(t) = \int_{t_0}^t(Le(s) + \eta(s))\dd s$ es
$\mathcal C^1$ con $G' = Le + \eta \leq LG + \sup\eta$
(hipótesis $e \leq G$). Luego $\bigl(\eu^{-Lt}(G +
\tfrac{\sup\eta}L)\bigr)' = \eu^{-Lt}(G' - LG - \sup\eta)
\leq 0$: el corchete disminuye, por lo que$G(t) + \frac{\sup\eta}L
\leq \eu^{L(t - t_0)}\bigl(G(t_0) + \frac{\sup\eta}L\bigr) =
\eu^{L(t-t_0)}\frac{\sup\eta}L$, es decir, $e(t) \leq G(t) \leq
\frac{\sup\eta}L(\eu^{L(t-t_0)} - 1)$.

\textbf{18.} Defecto cuantitativo: en $(t_k, t_{k+1})$,
$\Delta_n(t) = f(t_k, \varphi_n(t_k)) - f(t, \varphi_n(t))$
con $\abs{t - t_k} \leq T/n$ y $\norm{\varphi_n(t) -
\varphi_n(t_k)} \leq MT/n$, entonces$\norm{\Delta_n} \leq L'T/n +
LMT/n$. Restando las identidades integrales para$\varphi_n$
(pregunta 5) y $\varphi$ y usando el límite de Lipschitz:
\[
\norm{\varphi_n(t) - \varphi(t)} \leq
\int_{t_0}^t L\,\norm{\varphi_n - \varphi}(s)\,\dd s +
\int_{t_0}^t\norm{\Delta_n(s)}\,\dd s,
\]
y la pregunta 17 con $\eta = \norm{\Delta_n}$ da la
declarado $O(1/n)$ cota, siendo $\varphi$ único por
Cauchy--Lipschitz (u Osgood). Incluso sin tarifas: cada
subsecuencia del equilímite, equi-Lipschitz $(\varphi_n)$
tiene una subsubsecuencia convergente (Ascoli + Parte II) a
Solución \emph{a}, cuya unicidad obliga a ser $\varphi$:
una secuencia cuyas subsecuencias tienen subsecuencias
con el mismo límite converge.

\textbf{19.} De $\varphi_n(t_k) = 0$: la pendiente $2\sqrt0 =
0$da$\varphi_n(t_{k+1}) = 0$; inducción:$\varphi_n
\equiv 0$, convergiendo a la solución cero. De
$\varepsilon > 0$: en $[\varepsilon', \infty)$ con
$\varepsilon' < \varepsilon$ la función $2\sqrt x$ es
Lipschitz, por lo que se aplica la pregunta 18 y Euler converge a la
solución única a través de $(0, \varepsilon)$, a saber, $x(t) =
(t + \sqrt\varepsilon)^2$(verificar:$x' = 2(t + \sqrt
\varepsilon) = 2\sqrt x$). As$\varepsilon \to 0$,$(t +
\sqrt\varepsilon)^2 \to t^2$uniformly on$[0,1]$: the double
limite las tierras en $x_0(t) = t^2$, no en $0$. Un arbitrariamente
una pequeña perturbación del dato inicial redirige el esquema
de una solución a otra: no unicidad leída como
inestabilidad numérica.

\textbf{20.} No vacío: Parte II. Cada solución satisface
$\norm{x'} = \norm{f(t, x)} \leq M$: $\mathcal S$ es
uniformemente $M$-Lipschitz y uniformemente acotado (valores en
$\bar B(x_0, b)$). Cerrado: si $x_n \in \mathcal S \to x$
uniformemente, pasar al límite en $x_n(t) = x_0 +
\int_{t_0}^tf(s, x_n(s))\dd s$ (los integrandos convergen
uniformemente por el uniforme \omterm{def:b3:topology:continuity}{continuidad} de $f$ sobre el \omterm{def:b3:topology:compact}{compacto} $R$):
$x \in \mathcal S$. Ascoli: $\mathcal S$ es un establecimiento cerrado,
acotado, \omterm{def:b3:complete:equicontinuous}{equicontinuo} subconjunto de $\mathcal C([t_0, t_0+T],
\R^d)$: \omterm{def:b3:topology:compact}{compacto}.

\textbf{21.} Cualquier solución es no decreciente ($x' = 2\sqrt{\abs
x} \geq 0$) con$x(0) = 0$, por lo tanto$x \geq 0$. Deje que$c =
\sup\{t \in [0,1] : x(t) = 0\}$(posiblemente$c = \infty$ si
$x \equiv 0$, en cuyo caso $x = x_\infty$). Para $t > c$: $x
> 0$(monotonicidad más definición de$c$), y allí$(\sqrt
x)' = \frac{x'}{2\sqrt x} = 1$, entonces$\sqrt{x(t)} = t - c$
(\omterm{def:b3:topology:continuity}{continuidad} en $c$): $x = x_c$. \omterm{def:b3:topology:continuity}{Continuidad} de $c \mapsto
x_c$: para$c, c' \in [0, 1]$,$\sup_{[0,1]}\abs{(t - c)_+^2 -
(t - c')_+^2} \leq 2\abs{c - c'}$(la aplicación$c \mapsto
(t-c)_+^2$es$2$-Lipschitz uniformemente en$t \in [0,1]$), y
desde $x_c \equiv 0$ en $[0,1]$ por cada $c \geq 1$, el
familia se reduce a $\mathcal S = \{x_c : c \in [0,1]\}$ (con
$x_1 = 0 = x_\infty$). Entonces $\mathcal S$ es la imagen del
\omterm{def:b3:topology:compact}{compacto} \omterm{def:b3:topology:connected}{conectado} $[0,1]$ bajo \omterm{def:b3:topology:continuity}{aplicación continua} $c
\mapsto x_c$: \omterm{def:b3:topology:compact}{compacto} y \omterm{def:b3:topology:connected}{conectado}. El embudo de soluciones
es un segmento \omterm{def:b3:topology:continuity}{continuo} que se ejecuta desde $t^2$ (inmediato
escape) hasta $0$ (descanso eterno).

\textbf{22.} La evaluación $\operatorname{ev}_t\colon
\mathcal C([0,1]) \to \R$,$x \mapsto x(t)$, es \omterm{def:b3:topology:continuity}{continuo}
($\abs{x(t) - y(t)} \leq \norm{x - y}_\infty$), por lo que $\mathcal
S(t) = \operatorname{ev}_t(\mathcal S)$ es una imagen \omterm{def:b3:topology:continuity}{continuo}
de un \omterm{def:b3:topology:compact}{compacto}: \omterm{def:b3:topology:compact}{compacto} --- y de un conjunto \omterm{def:b3:topology:connected}{conectado}: \omterm{def:b3:topology:connected}{conectado}.
Por ejemplo: $x_c(t) = (t - c)_+^2$ barre, mientras se ejecuta $c$
hasta $[0, 1]$, todos los valores desde $t^2$ (en $c = 0$) hasta
$0$ (en $c \geq t$), \omterm{def:b3:topology:continuity}{continuamente}: $\mathcal S(t) =
\intcc0{t^2}$. En cada instante, la sección transversal del embudo es
un segmento completo: entre el descanso y el escape máximo, cada
el compromiso se logra mediante una solución real.

\textbf{23.} Restando las formas integrales $x(t) = x_0 +
\int_{t_0}^tf(s, x(s))\dd s$y su análogo para$y$, y
configuración $e(t) = \norm{x(t) - y(t)}$, $d = \norm{x_0 - y_0}$:
\[
e(t) \leq d + \int_{t_0}^tL\,e(s)\dd s = G(t).
\]
Luego $G(t_0) = d$ y $G' = Le \leq LG$, entonces $\bigl(\eu^{-L(t
- t_0)}G\bigr)' \leq 0$y$G(t) \leq d\,\eu^{L(t - t_0)}$;
por lo tanto $e(t) \leq d\,\eu^{L(t-t_0)}$. Nitidez: para $x' =
Lx$, las soluciones hasta$x_0$y$y_0$son$x_0\eu^{L(t -
t_0)}$y$y_0\eu^{L(t-t_0)}$, cuya distancia es exactamente
$d\,\eu^{L(t-t_0)}$. Con $d = 0$, $e \equiv 0$:
Unicidad de Cauchy-Lipschitz, rederivada en dos líneas. y
para $t \in [t_0, t_0 + T]$, $\norm{x(t; x_0) - x(t;
y_0)} \leq \eu^{LT}\norm{x_0 - y_0}$ fijo: el flujo es Lipschitz
en la condición inicial --- dependencia determinista, en un
precio exponencial controlado.

\textbf{24.} En $\intoc01$, $\Omega$ está bien definido (el
integral converge en $0$ por hipótesis), $\mathcal C^1$ con
$\Omega' = \frac1\omega > 0$: una biyección creciente sobre
$\intoc0{\Omega(1)}$, con $\Omega(x) \to 0$ como $x \to 0^+$.
Su inversa $g \colon \intoc0{\Omega(1)} \to \intoc01$ es
$\mathcal C^1$ con
\[
g'(t) = \frac1{\Omega'(g(t))} = \omega\bigl(g(t)\bigr) > 0,
\qquad g(t) \xrightarrow[t \to 0^+]{} 0 .
\]
Ampliar $g$ por $0$ en $t \leq 0$: \omterm{def:b3:topology:continuity}{continuidad} está claro, y en
$t = 0$, para $t > 0$,
\[
\frac{g(t)}t = \frac1t\int_0^tg'(s)\dd s
= \frac1t\int_0^t\omega\bigl(g(s)\bigr)\dd s
\leq \omega\bigl(g(t)\bigr) \xrightarrow[t\to0^+]{} 0
\]
($\omega$ no decreciente, $g$ creciente, $\omega(0^+) = 0$):
$g'(0) = 0 = f(g(0))$ y $g' = f(g)$ se sujetan en ambos lados
de $0$. Entonces, $x \equiv 0$ y $g$ son dos soluciones distintas de $\mathcal
C^1$hasta$(0, 0)$: cuando$\int_0\frac{\dd r}
{\omega(r)}$ converge, la unicidad falla --- pregunta 15
la divergencia es exactamente la frontera. Para $\omega(r) = 2\sqrt
r$:$\Omega(x) = \int_0^x\frac{\dd r}{2\sqrt r} = \sqrt x$,
$g(t) = t^2$, y las traducciones del tiempo le dan a toda la familia
$x_c$ de la Parte III.

\textbf{25.} Con el paso $h = \frac1n$: $\varphi_n(t_{k+1}) =
\varphi_n(t_k) + h\,\varphi_n(t_k) = (1 + h)\varphi_n(t_k)$,
entonces $\varphi_n(1) = (1 + \frac1n)^n$ después de los pasos $n$.
Expansión:
\[
n\log\Bigl(1 + \frac1n\Bigr)
= n\Bigl(\frac1n - \frac1{2n^2} + O\Bigl(\frac1{n^3}\Bigr)
\Bigr) = 1 - \frac1{2n} + O\Bigl(\frac1{n^2}\Bigr),
\]
y exponencial, $(1 + \frac1n)^n = \eu\,\eu^{-1/(2n) +
O(n^{-2})} = \eu\bigl(1 - \frac1{2n} + O(n^{-2})\bigr)$. el
el error en $t = 1$ es por lo tanto $\eu - \varphi_n(1) =
\frac{\eu}{2n} + O(n^{-2})$: el$O(\frac1n)$ de la pregunta 18,
aquí con su constante exacta $\frac\eu2$. Numéricamente, $n =
10$:$1.1^{10} = 2.5937424601$($1.1^2 = 1.21$,$1.1^4 =
1.4641$,$1.1^8 = 2.14358881$, multiplicado por$1.21$) y$\eu -
2.59374 \approx 0.12454$, frente a la predicción asintótica
$\frac{\eu}{20} \approx 0.13591$: acuerdo dentro del
Corrección $O(n^{-2})$, cuyo término principal aquí reduce el
predicción hacia el valor observado.
\end{solution}