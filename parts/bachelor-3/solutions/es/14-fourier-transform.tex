\section*{Capítulo \ref{ch:b3:fouriertransform} --- The Fourier
Transform}

\begin{solution}{exo:b3:fouriertransform:1}
$\widehat{\mathbf 1_{\intcc{-a}a}}(\xi) =
\int_{-a}^a\eu^{-\iu\xi x}\dd x = \frac{2\sin(a\xi)}{\xi}$
(valor $2a$ en $0$).
$\widehat{\eu^{-a\abs x}}(\xi) = \int_0^\infty\eu^{-(a +
\iu\xi)x} + \eu^{-(a - \iu\xi)x}\,\dd x = \frac1{a + \iu\xi} +
\frac1{a - \iu\xi} = \frac{2a}{a^2 + \xi^2}$.
Tienda: $\max(0, 1 - \abs x) = \mathbf 1_{\intcc{-1/2}{1/2}} *
\mathbf 1_{\intcc{-1/2}{1/2}}$, por lo que su transformación es
$\bigl(\frac{2\sin(\xi/2)}\xi\bigr)^2 =
\bigl(\frac{\sin(\xi/2)}{\xi/2}\bigr)^2$.
Último: $\frac{2a}{a^2+\xi^2} \in L^1$, entonces inversión
(\cref{thm:b3:fouriertransform:inversion}(2)) aplicado a
$\eu^{-a\abs x}$ da, después de cambiar el nombre de las variables,
\[
\widehat{\Bigl(\frac1{x^2 + a^2}\Bigr)}(\xi) =
\frac{\pi}{a}\,\eu^{-a\abs\xi} .
\]
\end{solution}

\begin{solution}{exo:b3:fouriertransform:2}
De \cref{prop:b3:fouriertransform:basic}:
$\widehat{f(\cdot - a)} = \eu^{-\iu a\xi}\hat f(\xi)$;
$\widehat{f\cos(b\cdot)} = \frac12\bigl(\hat f(\xi - b) + \hat
f(\xi + b)\bigr)$;
$\widehat{f(a\cdot + b)}(\xi) = \frac1a\,\eu^{\iu
b\xi/a}\,\hat f(\xi/a)$($a > 0$);
$\widehat{\overline{f(-\cdot)}} = \overline{\hat f}$;
$\widehat{f * f} = \hat f^2$. Sobre el gaussiano
($\widehat{\eu^{-x^2}} = \sqrt\pi\eu^{-\xi^2/4}$) cada regla es
una verificación de una línea --- por ejemplo, \ $\eu^{-(x-a)^2}$ tiene transformación
$\sqrt\pi\,\eu^{-\iu a\xi}\eu^{-\xi^2/4}$, que el directo
El cálculo (\omterm{def:b3:complete:complete}{completo} el cuadrado) lo confirma.
\end{solution}

\begin{solution}{exo:b3:fouriertransform:3}
(a) $h = \mathbf 1_{\intcc{-1}1}*\mathbf 1_{\intcc{-1}1}$ tiene
$\hat h = \bigl(\frac{2\sin\xi}\xi\bigr)^2 \in L^1$; inversión
en $x = 0$, donde $h(0) = \lambda(\intcc{-1}1\cap\intcc{-1}1)
= 2$:
\[
2 = \frac1{2\pi}\int_\R\Bigl(\frac{2\sin\xi}\xi\Bigr)^2
\dd\xi
\ \Longrightarrow\
\int_\R\Bigl(\frac{\sin\xi}\xi\Bigr)^2\dd\xi = \pi ,
\]
consistente con $\int_0^\infty\frac{\sin^2}{\xi^2} =
\frac\pi2$ (\cref{pb:b3:lebesgue:1}).

(b) Plancherel para $f = \eu^{-\abs x}$: $\int\abs{\hat f}^2 =
2\pi\int\abs f^2$lee$\int\frac{4\,\dd\xi}{(1 + \xi^2)^2} =
2\pi\int\eu^{-2\abs x}\dd x = 2\pi$:
$\int_\R\frac{\dd\xi}{(1+\xi^2)^2} = \frac\pi2$.
\end{solution}

\begin{solution}{exo:b3:fouriertransform:4}
(a) $g_t(x) = \frac1{2\sqrt{\pi t}}\eu^{-x^2/4t}$: por
\cref{ex:b3:fouriertransform:gaussian} con $a = \frac1{4t}$,
$\hat g_t(\xi) = \frac1{2\sqrt{\pi t}}\sqrt{4\pi
t}\,\eu^{-t\xi^2} = \eu^{-t\xi^2}$.
(b) $\widehat{g_t * g_s} = \eu^{-t\xi^2}\eu^{-s\xi^2} =
\widehat{g_{t+s}}$, y la transformación es inyectiva en$L^1$
(\cref{thm:b3:fouriertransform:inversion}(3)): $g_t * g_s =
g_{t+s}$.
(c) $\norm{g_t}_1 = 1$ (integral gaussiana);
$\norm{g_t}_2^2 = \frac1{4\pi t}\int\eu^{-x^2/2t}\dd x =
\frac{\sqrt{2\pi t}}{4\pi t} = \frac1{\sqrt{8\pi t}}$.
\end{solution}

\begin{solution}{exo:b3:fouriertransform:5}
De verdad incluso $f$: $\hat f(\xi) = \int f\cos(\xi x)\dd x$ (el
la parte sinusoidal se cancela): real y par. Impar: $\hat f(\xi) =
-\iu\int f\sin(\xi x)$: impar, puramente imaginario.$\hat f(0) =
\int f$: la masa total. Si$f \geq 0$:$\abs{\hat f(\xi)}
\leq \int\abs f = \int f = \hat f(0)$, entonces se alcanza la sup.
en $0$. Para una probabilidad \omterm{ex:b3:lebesgue:gamma}{densidad}, $\hat f(-\xi)$ es la
función característica de \cref{ch:b3:clt}: módulo $\leq 1$
en todas partes, $= 1$ en el origen.
\end{solution}

\begin{solution}{exo:b3:fouriertransform:6}
(i) La inyectividad es
\cref{thm:b3:fouriertransform:inversion}(3); \omterm{def:b3:topology:continuity}{continuidad} es
$\norm{\hat f}_\infty \leq \norm f_1$ (con valores en
$\mathcal C_0$ de Riemann--Lebesgue); $\mathcal C_0$ está cerrado
en la norma sup (límites uniformes de desaparición en el infinito)
funciones desaparecen en el infinito): Banach.

(ii) Se ha producido una biyección \omterm{def:b3:topology:continuity}{continuo} entre espacios de Banach.
\omterm{def:b3:topology:continuity}{continuo} inversa (\cref{thm:b3:banach:openmapping}): allí
sería $C$ con $\norm f_1 \leq C\norm{\hat f}_\infty$.

(iii) Sea $f_n(x) = \frac{\sin x}x\cdot\frac{\sin(x/n)}{x/n}$:
un producto de dos funciones $L^2$ y $O(x^{-2})$ en
infinito, entonces $f_n \in L^1\cap L^2$. desde
$\bigl(\frac{\sin(ax)}{ax}\bigr)$ tiene transformación $L^2$
$\frac\pi a\mathbf 1_{\intcc{-a}a}$, la fórmula del producto
$\widehat{gh} = \frac1{2\pi}\hat g * \hat h$ (válido para $g, h
\in L^2$con$gh \in L^1$; compruébalo en funciones de Schwartz mediante
Fubini y extender por $L^2$-continuidad de ambos lados vía
Plancherel) da
\[
\hat f_n = \frac1{2\pi}\,\bigl(\pi\mathbf
1_{\intcc{-1}1}\bigr) * \bigl(\pi n\,\mathbf
1_{\intcc{-1/n}{1/n}}\bigr):
\]
un trapezoide de altura $\frac{\pi n}2\cdot\frac2n = \pi$:
$\norm{\hat f_n}_\infty = \pi$ por cada $n$. Pero en $[1,
n]$,$\frac{\sin(x/n)}{x/n} \geq \sin 1 > 0$, entonces
\[
\norm{f_n}_1 \geq \sin 1\int_1^n\frac{\abs{\sin x}}x\dd x
\geq c\ln n
\]
(conteo de arcos, como en \cref{thm:b3:banach:fourierdiverge}).
El $\norm{f_n}_1 \leq C\pi$ enlazado falla para $n$ grande: no
sobreyectivo. (La imagen es densa --- por
Stone, argumentos tipo Weierstrass, pero el subespacio adecuado de
$\mathcal C_0$.)
\end{solution}

\begin{solution}{exo:b3:fouriertransform:7}
Si $\hat f(\xi) = O(\abs\xi^{-k-1-\delta})$: entonces $\xi^j\hat f
\in L^1$para$0 \leq j \leq k$ (\omterm{def:b3:lebesgue:l1}{integrable} al infinito por el
decaimiento, localmente por \omterm{def:b3:topology:continuity}{continuidad} de $\hat f$). inversión
(\cref{thm:b3:fouriertransform:inversion}(2)) representa $f$
a.e.\ por $x \mapsto \frac1{2\pi}\int\hat f(\xi)\eu^{\iu
x\xi}\dd\xi$, y diferenciación bajo la integral
(dominators $\abs{\xi^j\hat f}$) hace que este representante
$\mathcal C^k$. Por el contrario, para $f \in \mathcal C_c^k$:
iterando \cref{prop:b3:fouriertransform:basic}(4),
$(\iu\xi)^k\hat f = \widehat{f^{(k)}}$, entonces $\abs{\hat f} \leq
\norm{f^{(k)}}_1\abs\xi^{-k}$. Función de tienda: \omterm{def:b3:topology:continuity}{continuo} con
Soporte \omterm{def:b3:topology:compact}{compacto} ($k = 0$: transformación limitada) y su
transformar $\sim \xi^{-2} = O(\abs\xi^{-0-1-1})$ devuelve, por
la primera dirección, un representante $\mathcal C^0$ --- ambos
agudo: la tienda no es $\mathcal C^1$, y su transformación
no decae más rápido que $\xi^{-2}$.
\end{solution}

\begin{solution}{exo:b3:fouriertransform:8}
Integración por partes ($f \in \mathcal S$; términos límite
desaparecer):
\[
1 = \int f^2 = \bigl[xf^2\bigr]_{-\infty}^{\infty} - \int
x\,(f^2)' = -2\int xff' \leq 2\,\norm{xf}_2\,\norm{f'}_2 .
\]
Plancherel y $\widehat{f'} = \iu\xi\hat f$:
$\norm{f'}_2^2 = \frac1{2\pi}\int\xi^2\abs{\hat f}^2$. cuadratura
la pantalla:
\[
\frac14 \leq \norm{xf}_2^2\cdot\frac1{2\pi}
\int\xi^2\abs{\hat f}^2\dd\xi .
\]
La igualdad requiere igualdad en Cauchy--Schwarz: $f' =
\lambda xf$con$\lambda < 0$(\omterm{def:b3:lebesgue:l1}{integrabilidad}), es decir $f(x) =
c\,\eu^{\lambda x^2/2}$: gaussianos. Una señal concentrada en
$x$ (pequeño $\norm{xf}_2$) debe tener espectro extendido, y
por el contrario: el principio de incertidumbre.
\end{solution}

\begin{solution}{exo:b3:fouriertransform:9}
El gaussiano $f(x) = \eu^{-\pi tx^2}$ es Schwartz, por lo que
Se aplica \cref{thm:b3:fouriertransform:poisson}, y
$\hat f(\xi) = t^{-1/2}\eu^{-\xi^2/4\pi t}$; en $\xi = 2\pi
k$el lado derecho se convierte en$t^{-1/2}\eu^{-\pi k^2/t}$: el
identidad theta. En $t = 1$:
\[
\sum_{n\in\Z}\eu^{-\pi n^2} = 1 + 2\eu^{-\pi} + 2\eu^{-4\pi} +
\cdots \approx 1 + 0.0864278 + 0.0000070 = 1.0864348,
\]
con precisión de $6$ decimales con tres términos ($\eu^{-9\pi}
\approx 5\cdot10^{-13}$). En$t = 10^{-2}$: la definición
La serie necesita $\eu^{-\pi n^2/100} < 10^{-7}$, es decir \ $n \gtrsim
23$--- aproximadamente términos$47$ --- mientras que la serie transformada es
$10\sum_k\eu^{-100\pi k^2}$, donde ya está el término $k = 1$
$\sim 10^{-136}$: un término es suficiente.
\end{solution}

\begin{solution}{exo:b3:fouriertransform:10}
$\mathcal Ff \in L^2(\intcc{-\pi}\pi) \subseteq
L^1(\intcc{-\pi}\pi)$ (finito \omterm{def:b3:measure:measure}{medida}), por lo que la inversión da el
\omterm{def:b3:topology:continuity}{continuo} representante $f(x) =
\frac1{2\pi}\int_{-\pi}^\pi\mathcal Ff(\xi)\eu^{\iu
x\xi}\dd\xi$, con
\[
f(n) = \frac1{2\pi}\int_{-\pi}^{\pi}\mathcal
Ff(\xi)\,\eu^{\iu n\xi}\dd\xi = \langle e_{-n}, \mathcal
Ff\rangle
\]
en la notación de \cref{thm:b3:hilbert:fourier}. Expandiendo en esa
\omterm{def:b3:hilbert:onb}{base de Hilbert}:
\[
\mathcal Ff = \sum_n f(n)\,\eu^{-\iu n\xi}
\quad\text{en } L^2(\intcc{-\pi}\pi).
\]
Aplíquese el operador $L^2$-continuo $\mathcal F^{-1}$ término a
término:
\[
\mathcal F^{-1}\bigl(\mathbf
1_{\intcc{-\pi}\pi}\eu^{-\iu n\xi}\bigr)(x)
= \frac1{2\pi}\int_{-\pi}^{\pi}\eu^{\iu\xi(x - n)}\dd\xi
= \frac{\sin\bigl(\pi(x-n)\bigr)}{\pi(x - n)} ,
\]
dando $f = \sum_nf(n)\operatorname{sinc}(\cdot - n)$ en
$L^2$: una señal de banda limitada está determinada por su número entero
muestras --- Teorema de muestreo de Shannon.
\end{solution}

\begin{solution}{exo:b3:fouriertransform:11}
(a) Inversión en $\mathcal S$: $f(x) =
\frac1{2\pi}\int\hat f(\xi)\eu^{\iu x\xi}\dd\xi =
\frac1{2\pi}(\mathcal F\hat f)(-x)$, es decir, $(\mathcal
F^2f)(x) = 2\pi f(-x)$. Aplicando dos veces:$\mathcal F^4f =
2\pi\,\mathcal F^2f(-\cdot) = (2\pi)^2f$.

(b) Si $\mathcal Ff = \lambda f$ con $f \neq 0$:
$(2\pi)^2f = \mathcal F^4f = \lambda^4f$, entonces $\lambda^4 =
(2\pi)^2$:$\lambda \in \{\pm\sqrt{2\pi},
\pm\iu\sqrt{2\pi}\}$. El gaussiano$g(x) = \eu^{-x^2/2}$ tiene
$\hat g = \sqrt{2\pi}\,g$
(\cref{ex:b3:fouriertransform:gaussian} y $a = \frac12$):
función propia para $+\sqrt{2\pi}$.

(c) $\mathcal F^2f = 2\pi f(-\cdot)$ es igual a $\pm2\pi f$
según la paridad. Para $h(x) = x\eu^{-x^2/2}$:
diferenciando $\hat g(\xi) = \sqrt{2\pi}\eu^{-\xi^2/2}$
con la regla $\widehat{xf} = \iu\frac{\dd}{\dd\xi}\hat f$:
\[
\hat h(\xi) = \iu\,\frac{\dd}{\dd\xi}\bigl(\sqrt{2\pi}
\eu^{-\xi^2/2}\bigr) = -\iu\sqrt{2\pi}\,\xi\eu^{-\xi^2/2}
= -\iu\sqrt{2\pi}\,h(\xi) :
\]
una función propia para $-\iu\sqrt{2\pi}$. (El ermita
funciones continúan el patrón, pasando por las cuatro
valores propios --- el reloj de Fourier discreto.)

\end{solution}

\begin{solution}{exo:b3:fouriertransform:12}
(a) $\tilde f \in L^2$ con $\norm{\tilde f}_2 = \norm f_2$;
\cref{exo:b3:lp:6} (exponentes conjugados $p = q = 2$) hace
$A_f = f * \tilde f$ acotado y uniformemente \omterm{def:b3:topology:continuity}{continuo}, con
\[
A_f(x) = \int f(y)\,\overline{f(y - x)}\,\dd y,
\qquad A_f(0) = \norm f_2^2,
\qquad \abs{A_f(x)} \leq \norm f_2\,\norm{f(\cdot -
x)}_2 = A_f(0)
\]
por Cauchy--Schwarz.

(b) Para $f \in L^1\cap L^2$: $\tilde f \in L^1$ también, y el
el teorema de convolución da $\widehat{A_f} = \hat f\,
\widehat{\tilde f}$; informática,$\widehat{\tilde f}(\xi) =
\int\overline{f(-x)}\eu^{-\iu\xi x}\dd x =
\overline{\int f(u)\eu^{-\iu\xi u}\dd u} =
\overline{\hat f(\xi)}$:$\widehat{A_f} = \abs{\hat f}^2
\geq 0$.

(c) Cuando $\abs{\hat f}^2 \in L^1$, se aplica la inversión al
\omterm{def:b3:topology:continuity}{continuo} $A_f$ (su transformación es \omterm{def:b3:lebesgue:l1}{integrable};
\cref{thm:b3:fouriertransform:inversion}):
\[
A_f(x) = \frac1{2\pi}\int\abs{\hat f(\xi)}^2
\eu^{\iu x\xi}\,\dd\xi .
\]
Para $f = \mathbf 1_{\intcc{-1/2}{1/2}}$: $\hat f(\xi) =
\frac{2\sin(\xi/2)}\xi = \frac{\sin(\xi/2)}{\xi/2}$, y en
$x = 0$:
\[
1 = \norm f_2^2 = \frac1{2\pi}\int_\R
\Bigl(\frac{\sin(\xi/2)}{\xi/2}\Bigr)^2\dd\xi
= \frac1{2\pi}\cdot2\int_\R\Bigl(\frac{\sin u}u\Bigr)^2\dd u
\]
($\xi = 2u$), es decir\ $\int_\R\bigl(\frac{\sin u}u\bigr)^2\dd
u = \pi$ --- Integral favorita de Plancherel, recuperada por
autocorrelación.
\end{solution}

\begin{solution}{pb:b3:fouriertransform:1}
\textbf{1.} Transformando la ecuación en $x$ (formalmente):
$\partial_t\hat u(t,\xi) = \widehat{\partial^2_{xx}u} =
(\iu\xi)^2\hat u = -\xi^2\hat u$, una EDO en$t$ para cada
frecuencia: $\hat u(t,\xi) = \eu^{-t\xi^2}\hat f(\xi)$. desde
$\eu^{-t\xi^2} = \hat g_t$
(\cref{exo:b3:fouriertransform:4}), el producto es el
transformación de $g_t * f$.

\textbf{2.} $\abs{u(t,x)} \leq \norm f_\infty\int g_t = \norm
f_\infty$: bien definido. En$[t_0, T]\times[-A, A]$: cada uno
derivado mixto $\partial^m_t\partial^n_xg_t(x - y)$ es un
polinomio en $(x - y)$ y $t^{-1}$ veces
$\eu^{-(x-y)^2/4t}$, delimitado por $\abs y \geq 2A$ por
$C\,(1 + y^2)^N\eu^{-(\abs y - A)^2/4T}$, y \omterm{def:b3:lebesgue:l1}{integrable}
dominador independiente de $(t, x)$ en la ventana (y acotado
para $\abs y \leq 2A$): diferenciación repetida bajo el
Se aplica la integral (\cref{thm:b3:lebesgue:paramdiff}): $u \in
\mathcal C^\infty(\intoo0\infty\times\R)$.

\textbf{3.} Con $g_t(x) = \frac1{2\sqrt{\pi
t}}\eu^{-x^2/4t}$:
\[
\partial_tg_t = g_t\Bigl(\frac{x^2}{4t^2} - \frac1{2t}\Bigr)
= \partial^2_{xx}g_t
\]
(diferenciar dos veces en $x$: $\partial_xg_t = -\frac
x{2t}g_t$,$\partial^2_{xx}g_t = \bigl(\frac{x^2}{4t^2} -
\frac1{2t}\bigr)g_t$). Por la pregunta 2 pasan las derivadas
bajo la integral: $\partial_tu = \partial^2_{xx}u$.

\textbf{4.}$u(t,x) - f(x) = \int g_t(y)\bigl(f(x - y) -
f(x)\bigr)\dd y$. Dado un \omterm{def:b3:topology:compact}{compacto}$K$y$\varepsilon$:
uniforme \omterm{def:b3:topology:continuity}{continuidad} de $f$ en un \omterm{def:b3:topology:topology}{entorno} de $K$ da
$\delta$ con $\abs{f(x-y) - f(x)} < \varepsilon$ para $x \in
K$,$\abs y \leq \delta$; la cola contribuye con masa$\leq 2\norm
f_\infty\int_{\abs y > \delta}g_t(y)\dd y = 2\norm
f_\infty\,\P$más allá de$\delta$, que es
$\frac2{\sqrt\pi}\int_{\delta/2\sqrt t}^\infty\eu^{-z^2}\dd z
\to 0$como$t \to 0$. Para$f \in L^p$:$\norm{u(t) - f}_p \leq
\int g_t(y)\norm{\tau_yf - f}_p\dd y$ (Minkowski/Jensen como en
\cref{thm:b3:lp:regularization}), divida de la misma manera usando
\cref{thm:b3:lp:density}(3).

\textbf{5.} El suavizado instantáneo es la pregunta 2 ($u(t)$ es
$\mathcal C^\infty$ para $t > 0$ sin suavidad de $f$
usado). Para $f = \mathbf 1_{\intoo0\infty}$:
\[
u(t, x) = \int_0^\infty g_t(x - y)\dd y
= \frac1{\sqrt\pi}\int_{-x/2\sqrt t}^{\infty}\eu^{-z^2}\dd z
= \frac12\Bigl(1 +
\operatorname{erf}\Bigl(\frac{x}{2\sqrt t}\Bigr)\Bigr),
\qquad \operatorname{erf}(s) =
\frac2{\sqrt\pi}\int_0^s\eu^{-z^2}\dd z :
\]
un escalón suavizado cuya zona de transición se ensancha como $\sqrt t$
(perfiles en $t_1 < t_2 < t_3$: rampas cada vez más planas
$(0, \frac12)$).

\textbf{6.} El integrando $g_t(x-y)f(y)$ es $\geq 0$ y el
El kernel es estrictamente positivo: $u(t,x) = 0$ forzaría a $f = 0$.
e.e. La monotonicidad en $f$ es la monotonicidad de la integral. un
lugar donde $f = 0$ en un intervalo todavía tiene $u(t, \cdot) > 0$
ahí para cada $t > 0$: el calor se propaga a velocidad infinita
(cualquier positividad en cualquier lugar se siente en todas partes al instante).

\textbf{7.} Tonelli ($g_t(x-y)\abs{f(y)}$ es \omterm{def:b3:lebesgue:l1}{integrable} en
$\R^2$): $\int u(t,x)\dd x = \int f(y)\bigl(\int g_t(x -
y)\dd x\bigr)\dd y = \int f$.

\textbf{8.} Plancherel: $2\pi\norm{u(t)}_2^2 =
\int\eu^{-2t\xi^2}\abs{\hat f(\xi)}^2\dd\xi$, sin aumento en
$t$ (puntualmente), estrictamente a menos que $\hat f = 0$ a.e.\ ($= f =
0$), con límite$0$como$t\to\infty$ por DCT. y
$\norm{u(t)}_\infty \leq \norm{g_t}_\infty\norm f_1 =
\frac{\norm f_1}{2\sqrt{\pi t}} \to 0$.

\textbf{9.} Para $\varphi \in \mathcal C_c^\infty$, $t \mapsto
\langle\varphi, \hat u(t)\rangle$es$\mathcal C^1$ con
derivado $\langle\varphi, \partial_t\hat u\rangle =
\langle\varphi, \widehat{\partial_{xx}u}\rangle =
\langle\xi^2\varphi\dots\rangle$ --- precisamente,
$\widehat{\partial^2_{xx}u} = -\xi^2\hat u$ transfiere el
ecuación. Entonces para a.e.\ $\xi$, el absolutamente \omterm{def:b3:topology:continuity}{continuo}
la función $t \mapsto \eu^{t\xi^2}\hat u(t,\xi)$ tiene derivada
$\eu^{t\xi^2}(\xi^2\hat u + \partial_t\hat u) = 0$ en el
sentido integrado: es constante, y deja $t \to 0$
($\hat u(t) \to \hat f$ en $L^2$, a.e.\ a lo largo de una subsecuencia):
$\hat u(t, \xi) = \eu^{-t\xi^2}\hat f(\xi)$ a.e. dos
Las soluciones de la clase tienen la misma transformación: son
igual.

\textbf{10.} $f = g_s * h$ con $h \in L^2$ fuerza a $\hat f =
\eu^{-s\xi^2}\hat h$, es decir, $\hat h = \eu^{s\xi^2}\hat f \in
L^2$. Tome$\hat f(\xi) = \eu^{-\abs\xi}$: luego$f(x) =
\frac1\pi\cdot\frac1{1 + x^2}$
(\cref{exo:b3:fouriertransform:1}, inversión), un perfectamente
función $L^2$ suave; pero $\eu^{2s\xi^2 - 2\abs\xi} \to
\infty$:$\eu^{s\xi^2}\hat f \notin L^2$por cada$s > 0$.
El perfil de Cauchy es \emph{nunca} el resultado de investigaciones anteriores.
difusión.

\textbf{11.} El semigrupo de calor multiplica transforma por
$\eu^{-t\xi^2}$, que no desaparece en ninguna parte: inyectivo ---
Formalmente, no se destruye ninguna información. Pero su gama consiste
de funciones cuyas transformadas decaen como $\eu^{-t\xi^2}$: a
subespacio pequeño, denso pero adecuado de $L^2$ (la pregunta 10 muestra
incluso funciones excelentes se encuentran en el exterior). invertir sería
amplificar la frecuencia $\xi$ por $\eu^{t\xi^2}$: ilimitada,
por lo tanto inestable frente a cualquier perturbación. La difusión es
irreversible no porque la aplicación se olvide, sino porque su
la inversa no puede ser \omterm{def:b3:topology:continuity}{continuo} --- una flecha del tiempo hecha de
análisis funcional.

\textbf{12.} $\hat f \in L^2(\intcc{-\Omega}\Omega)
\subseteq L^1$ (Cauchy--Schwarz en un intervalo acotado), por lo que
$F(x) = \frac1{2\pi}\int_{-\Omega}^\Omega\hat
f(\xi)\eu^{\iu x\xi}\dd\xi$ está definido en todas partes y
diferenciación bajo la integral (dominada por
$\Omega^k\abs{\hat f} \in L^1$ en la banda) lo hace
$\mathcal C^\infty$ con $\abs{F^{(k)}} \leq
\frac{\Omega^k}{2\pi}\norm{\hat f}_{L^1}$en todas partes. Y$F
= f$ a.e.: ambos lados tienen la misma transformada, y el
la transformación es inyectiva en $L^2$
(\cref{thm:b3:fouriertransform:plancherel} y sus
extensión $L^2$).

\textbf{13.} Los exponenciales $\xi \mapsto \eu^{-\iu
n\pi\xi/\Omega}$,$n \in \Z$, forman un \omterm{def:b3:hilbert:onb}{base de hilabert} de
$L^2(\intcc{-\Omega}\Omega)$
(\cref{thm:b3:hilbert:fourier}, reescalado). el coeficiente
de $\hat f$ a lo largo del $n$-ésimo es
\[
\frac1{2\Omega}\int_{-\Omega}^\Omega\hat f(\xi)\,
\eu^{\iu n\pi\xi/\Omega}\dd\xi
= \frac{2\pi}{2\Omega}\cdot
\frac1{2\pi}\int_{-\Omega}^{\Omega}\hat f(\xi)\,
\eu^{\iu(n\pi/\Omega)\xi}\dd\xi
= \frac\pi\Omega\,f\Bigl(\frac{n\pi}\Omega\Bigr),
\]
por la fórmula de la pregunta 12 en $x = \frac{n\pi}\Omega$: el
La expansión indicada se mantiene en $L^2$ de la banda.

\textbf{14.} Insertar la expansión en la fórmula de inversión.
de la pregunta 12; el intercambio de suma e integral es el
\omterm{def:b3:topology:continuity}{continuidad} del emparejamiento $L^2$ contra $\frac1{2\pi}
\eu^{\iu x\xi}\mathbf 1_{\abs\xi\leq\Omega}$(de la norma$L^2$
$\frac{\sqrt{2\Omega}}{2\pi}$, independiente de $x$ ---
de donde uniformidad):
\[
f(x) = \sum_nf\Bigl(\frac{n\pi}\Omega\Bigr)\cdot
\frac1{2\Omega}\int_{-\Omega}^\Omega
\eu^{\iu(x - n\pi/\Omega)\xi}\dd\xi
= \sum_nf\Bigl(\frac{n\pi}\Omega\Bigr)
\operatorname{sinc}(\Omega x - n\pi),
\]
desde $\frac1{2\Omega}\int_{-\Omega}^\Omega\eu^{\iu
u\xi}\dd\xi = \frac{\sin(\Omega u)}{\Omega u}$.

\textbf{15.} Leyendo el cálculo de la pregunta 14 al revés,
la transformación de $s_n = \operatorname{sinc}(\Omega\cdot -
n\pi)$es$\hat s_n = \frac\pi\Omega\,\eu^{-\iu
n\pi\xi/\Omega}\,\mathbf 1_{\intcc{-\Omega}\Omega}$.
Plancherel:
\[
\langle s_n, s_m\rangle = \frac1{2\pi}
\Bigl(\frac\pi\Omega\Bigr)^2\int_{-\Omega}^\Omega
\eu^{\iu(n-m)\pi\xi/\Omega}\dd\xi =
\frac\pi\Omega\,\delta_{nm} :
\]
una familia ortogonal de norma constante
$\sqrt{\pi/\Omega}$. Tomando normas en la pregunta 14
expansión: $\norm f_2^2 =
\frac\pi\Omega\sum_n\abs{f(n\pi/\Omega)}^2$.

\textbf{16.} $g(x) = \sin(\Omega x)\operatorname{sinc}
(\Omega x) = \frac{\sin^2(\Omega x)}{\Omega x}$ desaparece en
cada punto de la cuadrícula $\frac{n\pi}\Omega$ (incluido $0$, por el
límite) y no es idénticamente cero. Su banda: escriba $g =
\frac1{2\iu}\bigl(\eu^{\iu\Omega x} - \eu^{-\iu\Omega
x}\bigr)\operatorname{sinc}(\Omega x)$; modulación por
$\eu^{\pm\iu\Omega x}$ cambia la transformación en
$\mp\Omega$, por lo que $\hat g$ es compatible con
$\intcc{-2\Omega}{2\Omega}$ (efectivamente en la unión de dos
bandas desplazadas): $g \in PW_{2\Omega}$, invisible para
$\Omega$ muestreo de velocidad --- aliasing encarnado.

\textbf{17.} Por la pregunta 15 las muestras llevan la energía
democráticamente: $\norm f^2 = \frac\pi\Omega\sum
\abs{f(n\pi/\Omega)}^2$. Si la energía de la señal fuera del
La ventana de tiempo $\intcc{-T/2}{T/2}$ es $\leq \varepsilon^2\norm
f^2$, las muestras fuera de la ventana satisfacen (hasta el límite).
términos controlados por el límite uniforme de la pregunta 12)
$\frac\pi\Omega\sum_{\abs{n\pi/\Omega} > T/2}
\abs{f(n\pi/\Omega)}^2 \approx \norm{f\,\mathbf 1_{\abs x >
T/2}}^2 \leq \varepsilon^2\norm f^2$: truncando el
serie de muestreo al $\approx \frac{\Omega T}\pi$
Los índices en ventana reconstruyen $f$ hasta un error relativo.
$\approx\varepsilon$. De ahí el producto tiempo-ancho de banda
$\frac{\Omega T}{\pi}$ cuenta los grados reales efectivos de
libertad de la señal --- la regla detrás de cada audio
formato.

\textbf{18.} (a) $\operatorname{sinc}(\Omega x)$ tiene
muestras $f(n\pi/\Omega) = \operatorname{sinc}(n\pi) =
\delta_{n0}$: la serie se reduce a su término$n = 0$,
$\operatorname{sinc}(\Omega x)$ --- se reproduce el teorema
su propio núcleo. (b) Si $\hat f$ es compatible con
$\intcc{-\Omega'}{\Omega'} \subseteq
\intcc{-\Omega}\Omega$, se ejecuta cada paso de las preguntas 13--14
textualmente con la banda más grande $\Omega$ (la expansión de
$\hat f$ en el intervalo mayor sigue siendo legítimo):
El muestreo más rápido que la propia frecuencia Nyquist no cambia nada.
en la reconstrucción --- el sobremuestreo es inofensivo, y en
práctica beneficiosa (núcleos de reconstrucción que se descomponen más rápido
luego se puede utilizar).

\textbf{19.} Expande $\eu^{-\iu\xi x} =
\sum_k\frac{(-\iu\xi x)^k}{k!}$ dentro de la integral; en
$\intcc{-A}A$ la serie converge normalmente
($\sum_k\frac{\abs{\xi}^kA^k}{k!}\abs f \in L^1$), entonces
La integración término a término es legítima:
\[
\hat f(\xi) = \sum_{k\geq0}\frac{(-\iu\xi)^k}{k!}m_k,
\qquad \abs{m_k} \leq A^k\norm f_1 .
\]
El límite hace que la serie converja para cada complejo $\xi$;
alrededor de cualquier punto $\xi_0$, reagrupación (convergencia absoluta)
da una serie de potencias en $\xi - \xi_0$: $\hat f$ es
Real-analítico con radio infinito en todas partes.

\textbf{20.} Dejemos que $g$ sea analítico real en $\R$ (serie Taylor
convergiendo a $g$ cerca de cada punto) y $Z = \{\xi :
g^{(k)}(\xi) = 0\ \forall k\}$.$Z$ está cerrado (intersección
de conjuntos cerrados); está abierto, ya que en $\xi_0 \in Z$ el local
La expansión de Taylor de $g$ es la serie cero, por lo que $g$ desaparece
idénticamente cerca de $\xi_0$, junto con todos los derivados. si
$g$ desaparece en un intervalo, $Z \neq \varnothing$; por
\omterm{def:b3:topology:connected}{conectividad} de $\R$, $Z = \R$: $g \equiv 0$. Ahora si $f
\neq 0$tuviera soporte \omterm{def:b3:topology:compact}{compacto} junto con$\hat f$: pregunta
19 hace que $\hat f$ sea real-analítico, desapareciendo fuera de un \omterm{def:b3:topology:compact}{compacto},
por lo tanto en intervalos: $\hat f \equiv 0$, entonces $f = 0$ a.e.\ por
inyectividad --- contradicción. Del mismo modo, un $f \in
PW_\Omega$ distinto de cero no puede admitirse de forma compacta (intercambie los roles de
$f$ y $\hat f$ mediante inversión): las señales de banda limitada nunca
Dado que las señales de tiempo limitado ocupan un espectro ilimitado.

\textbf{21.} Para $f = \eu^{-ax^2}$: $\norm f_2^2 =
\sqrt{\frac\pi{2a}}$y$\int x^2\abs f^2 =
\frac1{4a}\sqrt{\frac{\pi}{2a}}$ (segundo momento gaussiano);
$\hat f = \sqrt{\frac\pi a}\,\eu^{-\xi^2/4a}$
(\cref{ex:b3:fouriertransform:gaussian}) y
\[
\frac1{2\pi}\int\xi^2\abs{\hat f}^2\dd\xi =
\frac1{2\pi}\cdot\frac\pi a\int\xi^2
\eu^{-\xi^2/2a}\dd\xi = \frac1{2a}\cdot a\sqrt{2\pi a}
= \frac{\sqrt{2\pi a}}2 .
\]
Producto normalizado: $\frac1{4a}\sqrt{\frac\pi{2a}}\cdot
\frac{\sqrt{2\pi a}}2\big/\frac{\pi}{2a} = \frac14$,
independiente de $a$. El escalado explica la constancia: reemplazar
$f$ por $f(\lambda\cdot)$ multiplica $\int x^2\abs f^2/\norm
f^2$por$\lambda^{-2}$y$\frac1{2\pi}\int\xi^2\abs{\hat
f}^2/\norm f^2$por$\lambda^{2}$: el producto es una dilatación
invariante, y los gaussianos forman una dilatación \omterm{def:b3:groups:action}{órbita}.

\textbf{22.} Con $\abs f^2$ la posición \omterm{ex:b3:lebesgue:gamma}{densidad} y
$\frac1{2\pi}\abs{\hat f}^2$ el impulso \omterm{ex:b3:lebesgue:gamma}{densidad} de un
estado cuántico (unidades físicas inserte $\hbar$),
\cref{exo:b3:fouriertransform:8} lee $\sigma_x\sigma_p
\geq \frac\hbar2$: ningún estado es nítido en ambos observables.
A lo largo del capítulo, una ley usa cinco trajes: calienta al instante
Suaviza porque $\eu^{-t\xi^2}$ aniquila las altas frecuencias.
(Parte II); el flujo no puede retroceder porque restaurar
ellos es ilimitado (Parte IV); una señal de banda limitada es rígida
lo suficiente para vivir en una red contable (Parte V); sin función
supera el piso de Heisenberg; y ninguna función es compacta
apoyado en ambos lados de la transformación (preguntas 19-20).
Lo que hace $\hat f$ en el infinito rige lo que puede hacer $f$
en cualquier lugar.

\textbf{23.} Tanto $g_t$ como $g_s$ están en $L^1$ con
$\widehat{g_t}(\xi) = \eu^{-t\xi^2}$ (preguntas 1
cálculo), por lo que el teorema de convolución da
$\widehat{g_t * g_s} = \eu^{-t\xi^2}\eu^{-s\xi^2} =
\eu^{-(t+s)\xi^2} = \widehat{g_{t+s}}$; dos funciones$L^1$
con la misma transformada conviene a.e.\ (inyectividad, a través de la
teorema de inversión --- aquí ambos lados son \omterm{def:b3:topology:continuity}{continuo}, entonces
coinciden en todos lados): $g_t * g_s = g_{t+s}$. En consecuencia
$u(t + s) = g_{t+s} * f = g_s * (g_t * f) = g_s * u(t)$
(asociatividad de convolución, Tonelli). Log-convexidad: let
$N(t) = \norm{u(t)}_2^2 = \frac1{2\pi}\int
\eu^{-2t\xi^2}\abs{\hat f}^2\dd\xi$ (Plancherel, pregunta
8). Para $t = \frac{t_1 + t_2}2$, escriba
\[
\eu^{-2t\xi^2}\abs{\hat f}^2
= \Bigl(\eu^{-2t_1\xi^2}\abs{\hat f}^2\Bigr)^{1/2}
\Bigl(\eu^{-2t_2\xi^2}\abs{\hat f}^2\Bigr)^{1/2},
\]
y Cauchy-Schwarz da $N\bigl(\frac{t_1+t_2}2\bigr) \leq
\sqrt{N(t_1)\,N(t_2)}$:$\ln N$ es convexo en el punto medio, y
siendo \omterm{def:b3:topology:continuity}{continuo} (convergencia dominada en $t$), convexo; entonces
es $\ln\norm{u(t)}_2 = \frac12\ln N(t)$. Decaimiento con un convexo.
logaritmo: el flujo de calor no puede perder energía en una ráfaga y
luego detenerse.

\textbf{24.} Los momentos del núcleo: $\int g_t = 1$ (pregunta
7 con $f = g_s$, o directamente la integral gaussiana), $\int
x\,g_t(x)\dd x = 0$(integrando impar) y, sustituyendo$x =
2\sqrt t\,v$,
\[
\int_\R x^2g_t(x)\,\dd x
= \frac{4t}{\sqrt\pi}\int_\R v^2\eu^{-v^2}\dd v = 2t .
\]
Sustituyendo $x = z + y$ en la convolución y observando
$\iint(\abs z + \abs y)^2g_t(z)f(y)\,\dd z\,\dd y < \infty$
(cada uno de $\int\abs z^kg_t$, $\int\abs y^kf$ es finito para $k
\leq 2$, usando$\abs y \leq \frac{1 + y^2}2$), Fubini y
Tonelli se aplica a las integrales de momento siguientes:
\[
\int x\,u(t,x)\dd x = \iint (z + y)\,g_t(z)f(y)\,\dd z\,\dd
y = 0\cdot\!\int\! f + 1\cdot\!\int\! yf(y)\dd y,
\]
cuál es el primer reclamo; y
\[
\iint (z+y)^2g_t(z)f(y)\,\dd z\,\dd y
= 2t\int f + 2\cdot0\cdot\!\int\! yf + \int y^2f(y)\dd y ,
\]
el segundo. Significa agregar, agregar variaciones y el núcleo.
contribuye con la media $0$ y la varianza $2t$: después del tiempo $t$ el
el calor se ha extendido sobre un ancho de orden $\sqrt{2t}$ ---
la distancia crece como la raíz cuadrada del tiempo, la firma
de difusión (y del browniano \omterm{def:b3:topology:pathconnected}{caminos} de
\cref{ch:b3:probability}).

\textbf{25.} Lado de transformación: $\hat f(\xi) =
\sqrt\pi\,\eu^{-\xi^2/4}$, entonces$\hat u(t,\xi) =
\sqrt\pi\,\eu^{-(t + \frac14)\xi^2}$, que es la transformación
de $(1 + 4t)^{-1/2}\exp\bigl(-x^2/(1+4t)\bigr)$ (el
Diccionario gaussiano $\eu^{-ax^2} \mapsto
\sqrt{\pi/a}\,\eu^{-\xi^2/4a}$con$a = \frac1{1+4t}$):
la forma cerrada. Verificación directa, con $\sigma = 1 + 4t$:
\[
\partial_tu = \sigma^{-1/2}\eu^{-x^2/\sigma}
\Bigl(-\frac2\sigma + \frac{4x^2}{\sigma^2}\Bigr)
= \partial^2_{xx}u ,
\]
ambos lados calculados a partir de $\partial_xu =
-\frac{2x}\sigma\,u$. Conservación:$\int u(t) =
\sigma^{-1/2}\sqrt{\pi\sigma} = \sqrt\pi$para todos los$t$.
Disipación:
\[
\norm{u(t)}_2^2 = \frac1\sigma\int\eu^{-2x^2/\sigma}\dd x
= \frac1\sigma\sqrt{\frac{\pi\sigma}2}
= \sqrt{\frac\pi2}\,(1+4t)^{-1/2},
\]
entonces $\norm{u(t)}_2 = (\pi/2)^{1/4}(1+4t)^{-1/4}$,
no creciente con logaritmo convexo (pregunta 23); el
La norma $L^2$ decae como $t^{-1/4}$, exactamente la mitad de
$t^{-1/2}$ exponente de $\norm{u(t)}_\infty$ --- consistente
con $\norm u_2^2 \leq \norm u_\infty\norm u_1$ y
conservación de $\norm u_1$. Variación: $\int x^2u(t) =
\frac1\sigma\cdot\frac{\sigma^{3/2}\sqrt\pi}2 =
\frac{\sqrt\pi}2\,(1 + 4t) = \int x^2f + 2t\sqrt\pi$, como
la pregunta 24 predice ($\int x^2f = \frac{\sqrt\pi}2$, $\int
f = \sqrt\pi$). En$t = 6$:$\sigma = 25$, altura máxima
$u(6, 0) = \frac15$ versus $u(0,0) = 1$, escala de ancho
$\sqrt\sigma = 5$ veces la inicial, y $\int u =
\sqrt\pi \approx 1.7725$ en todas partes: la mancha es cinco veces
más bajo, cinco veces más ancho y no le falta ni una caloría.
\end{solution}