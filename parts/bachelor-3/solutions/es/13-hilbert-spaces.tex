\section*{Capítulo \ref{ch:b3:hilbert} --- Hilbert Spaces}

\begin{solution}{exo:b3:hilbert:1}
(a) Real: expandir $\norm{x \pm y}^2 = \norm x^2 \pm 2\langle
x,y\rangle + \norm y^2$ y restar. Complejo (\omterm{def:b3:hilbert:inner}{producto interno}
lineal en la segunda ranura): expandiéndose como arriba,
\[
\langle x, y\rangle = \frac14\sum_{k=0}^{3}
\iu^k\,\bigl\|\iu^kx + y\bigr\|^2,
\]
cada término aporta $\iu^k\cdot2\operatorname{Re}\bigl(
(-\iu)^k\langle x,y\rangle\bigr)$, cuya suma es$4\langle
x,y\rangle$(verifique los cuatro valores de$k$; el$\sum\iu^k
(\norm x^2 + \norm y^2) = 0$).

(b) $L^1$: $f = \mathbf 1_{\intcc0{1/2}}$, $g = \mathbf
1_{\intcc{1/2}1}$:$\norm{f\pm g}_1^2 = 1$cada uno, suma$2$;
$2\norm f_1^2 + 2\norm g_1^2 = 1 \neq 2$. Norma Sup: $f =
\mathbf 1$,$g(t) = t$en$\intcc01$:$\norm{f + g}_\infty^2 +
\norm{f-g}_\infty^2 = 4 + 1 = 5 \neq 4 = 2 + 2$. Fallando el
ley del paralelogramo, estas normas no son inducidas por ningún \omterm{def:b3:hilbert:inner}{producto interno}
(lo que lo forzaría por expansión directa).
\end{solution}

\begin{solution}{exo:b3:hilbert:2}
(a) $p(f) = \bigl(\int_0^1f\bigr)\mathbf 1$: de hecho, constantes $f - \int f
\perp$($\int(f - \int f)c = 0$). la mejor constante
la aproximación de $f$ en medio cuadrático es su \emph{promedio} ---
la primera instancia de expectativa condicional
(\cref{ch:b3:probability}).

(b) $p(f) = f\,\mathbf 1_{\intcc{1/2}1}$: la diferencia
$f\mathbf 1_{\intcc0{1/2}}$ es ortogonal a cada $g$
Desapareciendo en $\intcc0{1/2}$.

(c) $d^2 = \bigl\|x - \tfrac12\bigr\|_2^2 = \int_0^1(x -
\tfrac12)^2\dd x = \tfrac1{12}$:$d = \frac1{2\sqrt3}$.
\end{solution}

\begin{solution}{exo:b3:hilbert:3}
(a) La equivalencia es \cref{thm:b3:hilbert:decomposition}
($\bar F = (F^\perp)^\perp$ y $\bar F = H \iff F^\perp =
\{0\}$). Ejemplo: el espacio$F$ de secuencias finitas es denso
en $\ell^2$ (truncamiento) y adecuado: $F^\perp = \{0\}$ pero $F
\neq \ell^2$--- para un subespacio no cerrado,$H = F \oplus
F^\perp$falla descaradamente ($F \oplus \{0\} \neq H$).

(b) $\abs{\langle x_n, y_n\rangle - \langle x, y\rangle} \leq
\abs{\langle x_n - x, y_n\rangle} + \abs{\langle x, y_n -
y\rangle} \leq \norm{x_n - x}\sup_n\norm{y_n} + \norm
x\,\norm{y_n - y} \to 0$ (las secuencias convergentes están limitadas).
Usado: en \cref{thm:b3:hilbert:parseval}(1) para ver $x - \lim
S_N \perp e_k$, y en \cref{thm:b3:hilbert:decomposition}
para ver $F^\perp = \bar F^{\,\perp}$.
\end{solution}

\begin{solution}{exo:b3:hilbert:4}
$e_0 = \frac1{\sqrt2}$. Siguiente, $x \perp \mathbf 1$ ya
($\int_{-1}^1x = 0$) y $\int_{-1}^1x^2 = \frac23$: $e_1 =
\sqrt{\tfrac32}\,x$. Luego$x^2 - \langle e_0, x^2\rangle e_0 =
x^2 - \frac13$(y$\perp e_1$ por paridad), con
\[
\int_{-1}^1\Bigl(x^2 - \frac13\Bigr)^2\dd x = \frac25 -
\frac49 + \frac29 = \frac{8}{45}:
\qquad e_2 = \sqrt{\tfrac{45}8}\,\Bigl(x^2 - \frac13\Bigr).
\]
Comparación: $P_0 = 1$, $P_1 = x$, $P_2 = \frac{3x^2 - 1}2$ y
$\sqrt{n + \tfrac12}\,P_n$ da $\frac1{\sqrt2}$,
$\sqrt{\frac32}x$, $\sqrt{\frac52}\,\frac{3x^2-1}2 =
\sqrt{\frac{45}8}\bigl(x^2 - \frac13\bigr)$: exactamente$e_0, e_1,
e_2$.
\end{solution}

\begin{solution}{exo:b3:hilbert:5}
Para $f(t) = t$: $c_0 = 0$ y, integrando por partes, $c_n =
\frac{\iu(-1)^n}{n}$para$n \neq 0$:$\abs{c_n}^2 =
\frac1{n^2}$. \omterm{thm:b3:hilbert:parseval}{Parseval}:
\[
\frac1{2\pi}\int_{-\pi}^\pi t^2\dd t = \frac{\pi^2}3
= \sum_{n\neq0}\frac1{n^2} = 2\sum_{n\geq1}\frac1{n^2}
\ \Longrightarrow\ \sum_{n\geq1}\frac1{n^2} = \frac{\pi^2}6 .
\]
Para $f(t) = t^2$: $c_0 = \frac{\pi^2}3$, $c_n =
\frac{2(-1)^n}{n^2}$($n \ne 0$). \omterm{thm:b3:hilbert:parseval}{Parseval}:
\[
\frac1{2\pi}\int_{-\pi}^{\pi}t^4\dd t = \frac{\pi^4}5
= \frac{\pi^4}9 + \sum_{n\neq0}\frac4{n^4}
\ \Longrightarrow\
\sum_{n\geq1}\frac1{n^4} = \frac18\Bigl(\frac{\pi^4}5 -
\frac{\pi^4}9\Bigr) = \frac{\pi^4}{90} .
\]
No se necesitan advertencias por partes-$\mathcal C^1$:
\cref{thm:b3:hilbert:fourier} cubre todas las funciones de $L^2$.
\end{solution}

\begin{solution}{exo:b3:hilbert:6}
(a) $\varphi(f) = \int_0^{1/2}f = \langle\mathbf
1_{\intcc0{1/2}},\ f\rangle$: el representante es$a =
\mathbf 1_{\intcc0{1/2}}$, y$\norm\varphi = \norm a_2 =
\frac1{\sqrt2}$ (\cref{thm:b3:hilbert:riesz}).

(b) Tome las funciones de tienda $f_n$ con el pico $1$ en
$\frac12$ y soporte de ancho $\frac2n$: $f_n(\tfrac12) = 1$
mientras que $\norm{f_n}_2^2 \leq \frac2n \to 0$: sin constante $C$
puede dar $\abs{f(\frac12)} \leq C\norm f_2$. Evaluación de puntos
no tiene sentido en $L^2$ --- los elementos son clases módulo nulo
conjuntos --- y este cálculo es la razón cuantitativa.
\end{solution}

\begin{solution}{exo:b3:hilbert:7}
(a) Sea $M = \sup_k\norm{x_k}$. Las secuencias escalares
$(\langle e_n, x_k\rangle)_k$ están delimitados por $M$: una diagonal
la extracción produce $x_{k_j}$ con $\langle e_n, x_{k_j}\rangle
\to \gamma_n$por cada$n$. Para cada$N$:$\sum_{n\leq
N}\abs{\gamma_n}^2 = \lim_j\sum_{n\leq N}\abs{\langle e_n,
x_{k_j}\rangle}^2 \leq M^2$(Bessel), entonces$(\gamma_n) \in
\ell^2$y$x = \sum_n\gamma_ne_n \in H$
(\cref{thm:b3:hilbert:parseval}(3)). Para $y \in H$:
\[
\abs{\langle y, x_{k_j} - x\rangle}
\leq \Bigl|\sum_{n\leq N}\overline{c_n(y)}\bigl(\langle e_n,
x_{k_j}\rangle - \gamma_n\bigr)\Bigr|
+ 2M\Bigl(\sum_{n>N}\abs{c_n(y)}^2\Bigr)^{1/2},
\]
usando la expansión $\langle y, z\rangle =
\sum\overline{c_n(y)}c_n(z)$ y Cauchy--Schwarz en la cola;
elija $N$ y luego $j$: convergencia débil a $x$.

(b) $\langle y, e_n\rangle = c_n(y) \to 0$ por cada $y$
($\ell^2$ colas): $e_n \rightharpoonup 0$, pero $\norm{e_n} =
1$: la norma no es débilmente \omterm{def:b3:topology:continuity}{continuo}. En (a):$\norm x^2 =
\sum\abs{\gamma_n}^2 \leq \liminf_j\norm{x_{k_j}}^2$ (finito
secciones y Bessel de nuevo): los límites débiles sólo pueden perder la norma.
\end{solution}

\begin{solution}{exo:b3:hilbert:8}
Para $y$ fijo, $x \mapsto \langle y, Tx\rangle$ es un
\omterm{def:b3:topology:continuity}{continuo} funcional lineal; Riesz ofrece un $T^*y$ único con
$\langle y, Tx\rangle = \langle T^*y, x\rangle$ para todos $x$
--- conjugando, $\langle Tx, y\rangle = \langle x,
T^*y\rangle$. La unicidad hace que$T^*$ sea lineal;
\[
\norm{T^*y} = \sup_{\norm x = 1}\abs{\langle T^*y, x\rangle}
= \sup_{\norm x=1}\abs{\langle y, Tx\rangle}
\leq \vertiii T\,\norm y,
\]
entonces $\vertiii{T^*} \leq \vertiii T$ y $T^{**} = T$ dan
igualdad. Shift: $\langle Sx, y\rangle = \sum_{n\geq1}
x_n\bar y_{n+1} = \langle x, S^*y\rangle$con$(S^*y)_n =
y_{n+1}$: el cambio hacia atrás. Kernel--imagen:$T^*y = 0$ iff
$\langle x, T^*y\rangle = 0$ para todos $x$ si $\langle Tx,
y\rangle = 0$para todos$x$si$y \perp \operatorname{im}T$:
$\ker T^* = (\operatorname{im}T)^\perp$; tomando $\perp$ y
usando \cref{thm:b3:hilbert:decomposition},
$\overline{\operatorname{im}T} = (\ker T^*)^\perp$.
\end{solution}

\begin{solution}{exo:b3:hilbert:9}
Si $a(u, \cdot) = \varphi$: para cualquier $w$,
\[
J(u + w) - J(u) = a(u, w) - \varphi(w) + \tfrac12a(w,w)
= \tfrac12a(w,w) \geq \tfrac\alpha2\norm w^2,
\]
estrictamente positivo para $w \neq 0$: $u$ es el minimizador único.
Por el contrario, en un minimizador la función $t \mapsto J(u + tw)$
(un polinomio cuadrático en $t$) tiene derivada evanescente en
$0$: $a(u, w) - \varphi(w) = 0$ por cada $w$. Proyección
redirigido: para un subespacio cerrado $F$, aplique Lax--Milgram en
el \omterm{def:b3:hilbert:inner}{Espacio de Hilbert} $F$ con $a(u,v) = \langle u, v\rangle$
($M = \alpha = 1$) y $\varphi(v) = \langle x, v\rangle$: un
$p \in F$ único con $\langle p, v\rangle = \langle x,
v\rangle$para todos los$v \in F$, es decir $x - p \perp F$ --- y por
En el caso simétrico, $p$ minimiza $\frac12\norm v^2 - \langle
x, v\rangle = \frac12\norm{v - x}^2 - \frac12\norm x^2$ sobre
$F$: la proyección.
\end{solution}

\begin{solution}{exo:b3:hilbert:10}
Normalización: $\int h_n^2 = 2^j\cdot 2^{-j} = 1$.
Ortogonalidad: dos funciones de Haar distintas tienen
soportes disjuntos (\omterm{def:b3:topology:interior}{interiores} de) (producto cero a.e.), o el
El apoyo del más fino está contenido en un medio intervalo donde el
más grueso es constante --- entonces la integral del producto es
esa constante multiplicada por $\int h_{\text{finer}} = 0$; contra $h_0
= \mathbf 1$, nuevamente$\int h_n = 0$. Totalidad: el lapso de
$\{h_0, \dots, h_{2^J-1}\}$ consta de funciones escalonadas en el
cuadrícula diádica del paso $2^{-J}$; ambos espacios tienen dimensión
$2^J$ y las funciones de Haar son independientes (ortonormales):
el lapso es \emph{todo} tales funciones de paso. paso diádico
Las funciones son densas en $L^2(\intcc01)$: \omterm{def:b3:lebesgue:simple}{funciones simples} son
conjuntos densos (\cref{thm:b3:lp:density}(1)), \omterm{def:b3:lebesgue:measurable}{mensurable} son
aproximado por uniones finitas de intervalos
(\cref{exo:b3:measure:7}), e intervalos por diádicos
(los puntos finales se mueven por $\leq 2^{-J}$). Por
\cref{thm:b3:hilbert:parseval}, el sistema Haar es un \omterm{def:b3:hilbert:onb}{Hilbert
base}.
\end{solution}

\begin{solution}{exo:b3:hilbert:11}
(i) $\Rightarrow$ (ii): para la proyección ortogonal,
$\langle Px, y\rangle = \langle Px, Py\rangle = \langle x,
Py\rangle$(insertar las descomposiciones$x = Px + (x - Px)$
etc.\ y eliminar términos cruzados). (ii) $\Rightarrow$ (iii):
$\norm{Px}^2 = \langle P^2x, x\rangle = \langle Px, x\rangle
\leq \norm{Px}\norm x$, entonces$\vertiii P \leq 1$y$Pu = u$
en la imagen distinta de cero: $= 1$. (iii) $\Rightarrow$ (i): $H =
\operatorname{im}P \oplus \ker P$(\omterm{def:b3:galois:algebraic}{algebraicamente}, de$P^2
= P$); tomar$u = Pu \in \operatorname{im}P$,$v \in \ker P$,
$t \in \R$: $\norm{P(u + tv)}^2 = \norm u^2$ debe ser $\leq
\norm{u + tv}^2 = \norm u^2 + 2t\operatorname{Re}\langle u,
v\rangle + t^2\norm v^2$por cada$t$, forzando
$\operatorname{Re}\langle u, v\rangle = 0$ (comparar el
términos lineales como $t \to 0^\pm$); reemplazando $v$ por $\iu v$
mata también la parte imaginaria: $\operatorname{im}P \perp
\ker P$, que es exactamente la ortogonalidad de la proyección.
Ejemplo: $P(x, y) = (x + y, 0)$ en $\R^2$: $P^2 = P$,
Imagen del eje $x$, núcleo de la línea $y = -x$ y
$\vertiii P = \sup\frac{\abs{x+y}}{\norm{(x,y)}} = \sqrt2$
(alcanzado en $(1,1)/\sqrt2$): una proyección oblicua tiene norma
$> 1$. (Para que conste, (ii) también proporciona (i) directamente:
$\ker P = \ker P^* = (\operatorname{im}P)^\perp$ por
\cref{exo:b3:hilbert:8}.)
\end{solution}

\begin{solution}{exo:b3:hilbert:12}
(a) Para unitarios $U$: $\norm{Ux - x}^2 = 2\norm x^2 -
2\operatorname{Re}\langle Ux, x\rangle$y$\norm{U^*x -
x}^2 = 2\norm x^2 - 2\operatorname{Re}\langle x, Ux\rangle$:
los dos desaparecen juntos, entonces $\ker(U - I) = \ker(U^* - I)$.
Luego, usando $\ker T^* = (\operatorname{im}T)^\perp$
(\cref{exo:b3:hilbert:8}) con $T = U - I$ y $T^* = U^* -
I$:
\[
\overline{\operatorname{im}(U - I)} = \bigl(\ker(U^* -
I)\bigr)^\perp = F^\perp .
\]

(b) En $F$: $U^kx = x$, entonces $A_nx = x$. Para $x = (U - I)y$:
$A_nx = \frac1n(U^ny - y)$, de norma $\leq \frac2n\norm y \to
0$. Para$x$en \omterm{def:b3:topology:interior}{cierre}: dado$\varepsilon$, elija$x' =
(U - I)y$con$\norm{x - x'} < \varepsilon$; desde
$\vertiii{A_n} \leq \frac1n\sum\vertiii{U^k} = 1$,
$\norm{A_nx} \leq \norm{A_n(x - x')} + \norm{A_nx'} \leq
\varepsilon + o(1)$.

(c) Descomponer $x = Px + (x - Px)$ con $Px \in F$ y $x -
Px \in F^\perp = \overline{\operatorname{im}(U - I)}$ (parte
(a)): $A_nx = Px + A_n(x - Px) \to Px + 0$.

(d) En la base de Fourier $e_m(x) = \eu^{2\iu\pi mx}$: $Ue_m
= \eu^{2\iu\pi m\alpha}e_m$, por lo que$Ue_m = e_m$iff$m\alpha
\in \Z$iff$m = 0$($\alpha$irracional):$F = \C\mathbf 1$
y $Pf = \langle\mathbf 1, f\rangle\mathbf 1 = \int_0^1f$.
El teorema se lee $\frac1n\sum_{k<n}f(\cdot + k\alpha) \to
\int_0^1f$en$L^2(\R/\Z)$: los promedios \omterm{def:b3:groups:action}{órbita} de un
rotación irracional equidistribuida --- la sombra $L^2$ de
Teorema de equidistribución de Weyl, obtenido por Hilbert puro
geometría.
\end{solution}

\begin{solution}{pb:b3:hilbert:1}
\textbf{1.} Gram--Schmidt garantiza
$\operatorname{Vect}(p_0, \dots, p_n) =
\operatorname{Vect}(1, \dots, t^n) = \R_n[t]$y$p_n \perp
p_k$($k < n$), por lo tanto$p_n \perp \R_{n-1}[t]$. El$p_k$, de
grados estrictamente crecientes, son independientes: una base.

\textbf{2.} $t\,p_n$ es mónico de grado $n + 1$: ampliar
$t\,p_n = p_{n+1} + \sum_{k\leq n}c_kp_k$ con $c_k =
\langle p_k, tp_n\rangle/\norm{p_k}^2$. Para$k \leq n - 2$:
$\langle p_k, tp_n\rangle = \langle tp_k, p_n\rangle = 0$
(grado $k + 1 < n$). Entonces $tp_n = p_{n+1} + a_np_n +
b_np_{n-1}$, la recurrencia indicada, con
\[
b_n = \frac{\langle p_{n-1}, tp_n\rangle}{\norm{p_{n-1}}^2}
= \frac{\langle tp_{n-1}, p_n\rangle}{\norm{p_{n-1}}^2}
= \frac{\langle p_n + (\text{lower}),\
p_n\rangle}{\norm{p_{n-1}}^2}
= \frac{\norm{p_n}^2}{\norm{p_{n-1}}^2} > 0 .
\]

\textbf{3.} Sean $t_1 < \dots < t_m$ los puntos \omterm{def:b3:topology:interior}{interior} a
$I$ donde $p_n$ cambia de signo, y $q = \prod_{i\leq m}(t -
t_i)$(con$q = 1$si$m = 0$). Entonces$p_nq$ tiene signo constante
en $I$ y no es a.e.\ cero: $\int_Ip_nq\,w \neq 0$. Si $m <
n$, esto contradice$p_n \perp \R_{n-1}[t]$. Entonces$m = n$:
$p_n$ tiene $n$ raíces \omterm{def:b3:topology:interior}{interior} distintas (tiene como máximo $n$
raíces en todos).

\textbf{4.} $(t^2 - 1)^n$ tiene grado $2n$; $n$ derivados
grado de licencia $n$, con coeficiente principal
$\frac{(2n)(2n-1)\cdots(n+1)}{2^nn!} =
\frac{(2n)!}{2^n(n!)^2}$. Para$\deg Q < n$, integrar por partes
$n$ veces: todos los términos de frontera contienen una derivada de orden
$< n$ de $(t^2-1)^n$, que desaparece en $\pm1$ (raíz del orden
$n$); después de los pasos $n$, el integrando lleva $Q^{(n)} = 0$.

\textbf{5.} Con $u = (t^2 - 1)^n$:
\[
(2^nn!)^2\norm{P_n}^2 = \int_{-1}^1(u^{(n)})^2
= (-1)^n\int_{-1}^1 u\,u^{(2n)}
= (2n)!\int_{-1}^1(1 - t^2)^n\dd t ,
\]
($u^{(2n)} = (2n)!$; los términos límite desaparecen como en la pregunta 4).
Y $\int_{-1}^1(1-t^2)^n\dd t = B(\tfrac12, n+1) =
\frac{\Gamma(\frac12)\Gamma(n+1)}{\Gamma(n + \frac32)} =
\frac{2\cdot4^n(n!)^2}{(2n+1)!}$
(\cref{exo:b3:product:8}). Combinando: $\norm{P_n}^2 =
\frac{2}{2n + 1}$.

\textbf{6.} Los polinomios son $\norm\cdot_\infty$ densos en
$\mathcal C(\intcc{-1}1)$ (Weierstrass,
\cref{cor:b3:complete:weierstrass}), \omterm{def:b3:topology:continuity}{continuo} están
$L^2$-denso (\cref{thm:b3:lp:density}), y
$\norm\cdot_2 \leq \sqrt2\norm\cdot_\infty$: tramos polinomiales
son totales, por lo que el $P_n$ normalizado forma un \omterm{def:b3:hilbert:onb}{base de hilabert}.
Ampliación de $\abs t$: el coeficiente frente a $P_0$ es
$\frac{\langle P_0, \abs t\rangle}{\norm{P_0}^2} = \frac12$;
contra $P_1$: $0$ (paridad); contra $P_2$:
$\frac{\int_{-1}^1\abs t\,\frac{3t^2-1}2\dd t}{2/5} =
\frac{1/4}{2/5} = \frac58$. Mejor aproximación cuadrática:
\[
\abs t \approx \frac12 + \frac58\,P_2(t) = \frac{3}{16} +
\frac{15}{16}\,t^2 .
\]

\textbf{7.} De $\frac{\dd^{n+1}}{\dd t^{n+1}}\eu^{-t^2} =
\frac{\dd^n}{\dd t^n}(-2t\,\eu^{-t^2})$y Leibniz,$H_{n+1}
= 2tH_n - H_n'$; inducción da grado$n$ y líder
coeficiente $2^n$. Para $m < n$, integrar por partes $n$ veces
en $\int H_m H_n\eu^{-t^2} = (-1)^n\int H_m\,\bigl(\eu^{-t^2}
\bigr)^{(n)}$: términos de frontera (polinomio$\times$
$\eu^{-t^2}$) desaparecen en $\pm\infty$, dejando $\int
H_m^{(n)}\,\eu^{-t^2} = 0$. Para$m = n$:$H_n^{(n)} = 2^nn!$,
entonces $\norm{H_n}_w^2 = 2^nn!\int\eu^{-t^2} = 2^nn!\sqrt\pi$.

\textbf{8.} Sea $f \in L^2(\R, \eu^{-t^2}\dd t)$ ortogonal
a cada polinomio, y $g = f\eu^{-t^2}$. Entonces $g \in
L^1$:$\int\abs f\eu^{-t^2} \leq \bigl(\int\abs
f^2\eu^{-t^2}\bigr)^{1/2}\bigl(\int\eu^{-t^2}\bigr)^{1/2}$
(Cauchy--Schwarz). Para $\xi \in \R$, expanda $\eu^{-\iu\xi t}$:
las sumas parciales están dominadas ya que
\[
\sum_k\frac{\abs\xi^k}{k!}\int\abs f\,\abs t^k\eu^{-t^2}\dd t
\leq \Bigl(\int \abs f^2\eu^{-t^2}\Bigr)^{1/2}
\sum_k\frac{\abs\xi^k}{k!}\Bigl(\int
t^{2k}\eu^{-t^2}\Bigr)^{1/2} < \infty
\]
(la última serie converge: $\int t^{2k}\eu^{-t^2} =
\Gamma(k+\frac12) \leq k!\,\sqrt\pi$, por lo que los términos son
$O(\abs\xi^k/\sqrt{k!})$). Integración término por término
(\cref{cor:b3:lebesgue:additivity} aplicado al absoluto
serie, luego Fubini para la serie) da
\[
\int_\R g(t)\,\eu^{-\iu\xi t}\dd t
= \sum_k\frac{(-\iu\xi)^k}{k!}\int f(t)\,t^k\,\eu^{-t^2}\dd t
= 0 ,
\]
siendo cada integral $\langle t^k, f\rangle_w$ del tipo $= 0$. Por
la inyectividad admitida de la transformada de Fourier
(\cref{ch:b3:fouriertransform}), $g = 0$ a.e., entonces $f = 0$
a.e.: la familia Hermite (cuyos tramos son los polinomios) es
Total.

\textbf{9.} Exactitud al grado $n - 1$: para tal $P$, $P =
\sum_iP(t_i)\ell_i$exactamente, entonces$\int Pw = \sum_iP(t_i)\int
\ell_iw = Q(P)$. Grado$\leq 2n - 1$: dividir$P = qp_n + r$,
$\deg q \leq n - 1$, $\deg r \leq n-1$; luego $\int Pw = \int
qp_nw + \int rw = 0 + Q(r)$($p_n \perp \R_{n-1}[t]$), mientras
$Q(P) = \sum_iw_i\bigl(q(t_i)\,p_n(t_i) + r(t_i)\bigr) = Q(r)$
ya que los nodos son las raíces de $p_n$. Igual.

\textbf{10.} $\ell_i^2$ tiene grado $2n - 2 \leq 2n - 1$ y
$\ell_i^2(t_j) = \delta_{ij}$: $0 < \int\ell_i^2w = Q(\ell_i^2)
= w_i$. Polya (\cref{exo:b3:banach:9}, transportada a$I$ con
peso): la condición (i) se cumple --- cada polinomio está integrado
exactamente una vez $2n - 1 \geq$ su grado; condición (ii):
$\sum_i\abs{w_{i}} = \sum_iw_i = Q(\mathbf 1) = \int_Iw$,
acotado: $Q_n(f) \to \int fw$ por cada $f \in \mathcal C(I)$,
$I$\omterm{def:b3:topology:compact}{compacto}.

\textbf{11.} Monic $p_2 = t^2 - \frac13$ (de
\cref{exo:b3:hilbert:4}): nodos $\pm\frac1{\sqrt3}$. Pesos:
$\ell_1(t) = \frac{t - \frac1{\sqrt3}}{-\frac2{\sqrt3}}$, y
$w_1 = \int_{-1}^1\ell_1 = 1$; por simetría $w_2 = 1$.
Exactitud: $\int 1 = 2 = 1 + 1$; $\int t = 0 =
-\frac1{\sqrt3} + \frac1{\sqrt3}$;$\int t^2 = \frac23 =
\frac13 + \frac13$;$\int t^3 = 0$. El trapezoide de dos puntos
La regla (nodos $\pm1$, pesos $1, 1$) es exacta solo hasta el grado.
$1$: en $t^2$ devuelve $2$ en lugar de $\frac23$. Mismo costo,
dos grados extra de exactitud: el beneficio de la ortogonalidad
nodos.

\textbf{12.} De $\cos(n{+}1)\theta + \cos(n{-}1)\theta =
2\cos\theta\cos n\theta$:$T_{n+1} = 2tT_n - T_{n-1}$ con
$T_0 = 1$, $T_1 = t$; la inducción da polinomios de grado
$n$ con coeficiente principal $2^{n-1}$ ($n \geq 1$).
Sustituyendo $t = \cos\theta$ ($w(t)\dd t \mapsto
\dd\theta$):$\langle T_m, T_n\rangle_w =
\int_0^\pi\cos m\theta\cos n\theta\,\dd\theta = 0$por$m
\neq n$,$= \pi$por$m = n = 0$,$= \frac\pi2$ en caso contrario
(producto a suma). Grados y ortogonalidad por pares
identificar el $T_n$ con la salida Gram-Schmidt hasta
escalares; una expansión de Chebyshev de $f$ es exactamente la
Serie de cosenos de Fourier de $\theta \mapsto f(\cos\theta)$.

\textbf{13.} $T_n(t) = 0$ si y si $\cos n\theta = 0$ si y si
$\theta = \frac{(2k-1)\pi}{2n}$: las raíces distintas $n$
$t_k = \cos\frac{(2k-1)\pi}{2n} \in \intoo{-1}1$. extremos:
$\abs{T_n} \leq 1$ en $\intcc{-1}1$, con $T_n(s_j) =
(-1)^j$en los puntos$n + 1$ $s_j = \cos\frac{j\pi}n$:
\omterm{prop:b3:galois:perfect}{perfecto} equioscilación.

\textbf{14.} $2^{1-n}T_n$ es monic con sup-norm $2^{1-n}$.
Si un mónico $P$ de grado $n$ tuviera $\sup\abs P < 2^{1-n}$, el
diferencia $D = 2^{1-n}T_n - P$ tendría grado $\leq n -
1$(los términos principales se cancelan) pero se alternaría en el signo en$s_0 >
\dots > s_n$(allí domina$2^{1-n}T_n = \pm2^{1-n}$
$P$): al menos $n$ ceros --- $D \equiv 0$, contradicción.
Para unicidad en igualdad, el mismo $D$ satisface
$(-1)^jD(s_j) \geq 0$; un polinomio distinto de cero de grado $\leq
n-1$no puede tener$n$ extremal débilmente alternante
restricciones sin raíces $n$ contadas correctamente (si
$D(s_j) = 0$ para algunos \omterm{def:b3:topology:interior}{interior} $s_j$, ese cero es doble
en el conteo ya que $D$ mantiene un signo localmente): nuevamente $D
\equiv 0$.

\textbf{15.} La fórmula del error de Lagrange (Rolle, Año~2)
da $f - L_nf = \frac{f^{(n)}(\xi_t)}{n!}\,\omega(t)$, entonces
el error uniforme es como máximo $\frac{\norm{f^{(n)}}_\infty}
{n!}\,\sup\abs\omega$, y$\omega$es mónico de grado$n$:
por pregunta 14, $\sup_{\intcc{-1}1}\abs\omega \geq 2^{1-n}$
con igualdad si los nodos son las raíces de Chebyshev. Por lo tanto
el límite óptimo $\norm{f - L_nf}_\infty \leq
\frac{\norm{f^{(n)}}_\infty}{2^{n-1}n!}$. Con igual
nodos espaciados, $\sup\abs\omega$ es exponencialmente más grande cerca
los puntos finales, e interpolando incluso $\frac1{1 + 25t^2}$
diverge allí como $n \to \infty$ (fenómeno de Runge);
Los nódulos de Chebyshev son la cura.

\textbf{16.} Diferenciando $T_n(\cos\theta) = \cos
n\theta$:$T_n'(\cos\theta) = \frac{n\sin n\theta}
{\sin\theta}$, que tiende a$n^2$como$\theta \to 0$ y a
$(-1)^{n+1}n^2$ como $\theta \to \pi$: $\abs{T_n'(\pm1)} =
n^2$. En los puntos \omterm{def:b3:topology:interior}{interior},$\abs{T_n'(t)} \leq
\frac{n}{\sqrt{1 - t^2}} = O(n)$: la explosión cuadrática
vive sólo en los bordes (el \omterm{def:b3:topology:interior}{interior} de Bernstein versus
el global de Markov).

\textbf{17.} Deje $\theta_k = \frac{(2k-1)\pi}{2n}$ y $S_j
= \sum_{k=1}^n\cos(j\theta_k)$para$1 \leq j \leq n-1$. entonces
\[
S_j = \operatorname{Re}\Bigl[\eu^{\iu j\pi/2n}
\sum_{k=0}^{n-1}\eu^{\iu jk\pi/n}\Bigr]
= \operatorname{Re}\Bigl[\eu^{\iu j\pi/2n}\,
\frac{\eu^{\iu j\pi} - 1}{\eu^{\iu j\pi/n} - 1}\Bigr] .
\]
Para $j$ incluso el numerador desaparece: $S_j = 0$. Para $j$ impar
el numerador es $-2$ y $\eu^{\iu j\pi/n} - 1 =
\eu^{\iu j\pi/2n}\cdot2\iu\sin\frac{j\pi}{2n}$, por lo que el total
la expresión es $\frac{-2}{2\iu\sin(j\pi/2n)} =
\frac{\iu}{\sin(j\pi/2n)}$: puramente imaginaria,$S_j = 0$
otra vez. De ahí la regla de igual peso $\frac\pi n\sum_kf(t_k)$
integra $T_0$ ($\sum w_i = \pi = \int w$) y mata a $T_1,
\dots, T_{n-1}$exactamente como lo hace$\int T_jw = 0$: es exacto
al grado $n - 1$. Pesos exactos al grado $n-1$ en el momento indicado.
Los nodos son únicos (base de Lagrange): todos los pesos de Gauss son
$\frac\pi n$. Para $n = 3$: nodos $\pm\frac{\sqrt3}2, 0$ y
\[
\int_{-1}^1\frac{f(t)}{\sqrt{1 - t^2}}\,\dd t \approx
\frac\pi3\Bigl[f\Bigl(\tfrac{\sqrt3}2\Bigr) + f(0) +
f\Bigl(-\tfrac{\sqrt3}2\Bigr)\Bigr],
\]
exacto hasta el grado $5$.

\textbf{18.} Para monic $P$ de grado $n$: $P = p_n + r$
con $r \in \R_{n-1}[t]$ y $p_n \perp \R_{n-1}[t]$
(pregunta 1), entonces $\norm P^2 = \norm{p_n}^2 + \norm r^2 \geq
\norm{p_n}^2$, con igualdad si$r = 0$:$p_n$ es el
residuo de proyección ortogonal de $t^n$ sobre
$\R_{n-1}[t]^\perp$, es decir,\ el polinomio mónico más cercano a
el subespacio que debe evitar. Respuestas $2^{1-n}T_n$ de Chebyshev
la misma pregunta para la norma superior: menor desviación de
cero, una vez en $L^2(w)$, una vez en $L^\infty$.

\textbf{19.} Escribe $K_n(x, y) =
\sum_{k=0}^n\frac{p_k(x)p_k(y)}{h_k}$. Base$n = 0$:$(x -
y)\frac1{h_0} = \frac{p_1(x)\cdot1 - 1\cdot p_1(y)}{h_0}$
desde $p_1 = t - a_0$. Paso: asumiendo la identidad de $n -
1$,
\[
(x - y)\,K_n(x,y) = \frac{p_n(x)p_{n-1}(y) -
p_{n-1}(x)p_n(y)}{h_{n-1}} +
\frac{(x - y)\,p_n(x)p_n(y)}{h_n} ;
\]
sustituir $x\,p_n(x) = p_{n+1}(x) + a_np_n(x) +
b_np_{n-1}(x)$y$y\,p_n(y) = p_{n+1}(y) + a_np_n(y) +
b_np_{n-1}(y)$en el segundo término: las contribuciones$a_n$
cancelar, y las aportaciones $b_n = \frac{h_n}{h_{n-1}}$
cancelar el plazo de inducción; lo que sobrevive es
$\frac{p_{n+1}(x)p_n(y) - p_n(x)p_{n+1}(y)}{h_n}$. el
La forma confluente sigue dejando $y \to x$ (ambos lados están
polinomios en $y$).

\textbf{20.} La forma confluente da $p_{n+1}'p_n -
p_n'p_{n+1} = h_n\sum_{k\leq n}\frac{p_k^2}{h_k} \geq
\frac{h_n}{h_0} > 0$en todas partes. En la raíz$x_0$ de
$p_{n+1}$: $p_{n+1}'(x_0)\,p_n(x_0) > 0$, entonces $p_n(x_0) \neq
0$(sin raíces comunes). Entre raíces consecutivas$x_0 < x_1$
de $p_{n+1}$ (todo simple, Parte I), $p_{n+1}'$ tiene opuesto
signos, por lo tanto también lo hace $p_n$: una raíz de $p_n$ se encuentra en cada uno de
los huecos $n$ --- y que agota sus raíces $n$:
entrelazado.

\textbf{21.} Expandiendo $D_n(t) = \det(tI_n - J_n)$ a lo largo del
última fila: $D_n = (t - a_{n-1})D_{n-1} - b_{n-1}D_{n-2}$,
con $D_0 = 1$, $D_1 = t - a_0$: la recurrencia y las semillas de
el mónico $p_n$, entonces $D_n = p_n$. Raíces de $p_n$ =
Valores propios del $J_n$ simétrico: real y simple por
pregunta 19 --- La cuadratura de Gauss es la teoría espectral de un
matriz tridiagonal disfrazada, la de dimensión finita
sombra de \cref{ch:b3:spectral}.

Diccionario \textbf{22.}:
\begin{center}
\begin{tabular}{l|c|c|c}
 & Legendre & Hermite & Chebyshev\\
\hline
interval & $\intcc{-1}1$ & $\R$ & $\intcc{-1}1$\\
weight & $1$ & $\eu^{-t^2}$ & $(1-t^2)^{-1/2}$\\
formula & Rodrigues & $(-1)^n\eu^{t^2}
\frac{\dd^n}{\dd t^n}\eu^{-t^2}$&$\cos(n\arccos t)$\\
norm$^2$ & $\frac2{2n+1}$ & $2^nn!\sqrt\pi$ & $\pi,
\frac\pi2$\\
habitat & quadrature & Gaussian calculus & minimax\\
\end{tabular}
\end{center}
(cada uno con su recurrencia de tres términos: forma general para
Legendre, $H_{n+1} = 2tH_n - 2nH_{n-1}$, $T_{n+1} = 2tT_n -
T_{n-1}$). La teoría general proporcionó lo que ninguna familia
muestra: realidad y entrelazamiento de raíces, positividad de
pesos de cuadratura, la mera existencia de la recurrencia y
de Christoffel--Darboux --- consecuencias de la ortogonalidad
solo, uniforme en el peso.

\textbf{23.} Existencia: la aplicación lineal $\R_{2n-1}[t] \to
\R^{2n}$,$P \mapsto (P(t_1), P'(t_1), \dots, P(t_n),
P'(t_n))$, es inyectivo (un$P$en el kernel tiene$n$ doble
raíces y grado $\leq 2n - 1$, entonces $P = 0$) entre espacios de
igual dimensión $2n$: biyectiva. Error puntual: arreglar $t$
no es un nodo y elija $K$ para que $g(s) = f(s) - Hf(s) -
K\,p_n(s)^2$desaparezca en$s = t$. Entonces$g$ desaparece en el
$n + 1$ puntos distintos $t, t_1, \dots, t_n$ y $g'$
también desaparece en cada $t_i$ (tanto $f - Hf$ como $p_n^2$ tienen
dobles ceros allí). Rolle le da a $n$ ceros de $g'$ estrictamente
entre ceros consecutivos de $g$ --- distintos de los nodos
--- entonces $g'$ tiene ceros distintos $2n$; aplicando Rolle $2n - 1$
más veces produce $\xi_t$ con $g^{(2n)}(\xi_t) = 0$.
Ya que $\deg Hf \leq 2n - 1$ y $p_n^2$ es mónico de grado
$2n$, $g^{(2n)} = f^{(2n)} - K\,(2n)!$, de donde $K =
f^{(2n)}(\xi_t)/(2n)!$ --- y la identidad es trivial en
los nodos. Integración: $Q_n(f) = Q_n(Hf)$ ($Hf$ coincide con $f$
en los nodos) y $Q_n(Hf) = \int Hf\,w$ con exactitud hasta
grado $2n - 1$ (pregunta 9), por lo que el error de cuadratura es
$\int(f - Hf)\,w$. Con $m, M$ los extremos de $f^{(2n)}$ en
$I$, la identidad puntual aprieta
\[
\frac{m\,h_n}{(2n)!} \;\leq\; \int_I(f - Hf)\,w
\;\leq\; \frac{M\,h_n}{(2n)!} ,
\]
y el teorema del valor intermedio aplicado al \omterm{def:b3:topology:continuity}{continuo}
$f^{(2n)}$ entrega $\xi$. (Para Legendre con $n = 2$: $h_2
= \int_{-1}^1(t^2 - \frac13)^2\dd t = \frac8{45}$, entonces el
El error es $f^{(4)}(\xi)/135$.)

\textbf{24.} El kernel reproduce $\R_{n-1}[t]$: expandiendo
$q = \sum_k\frac{\langle p_k, q\rangle}{h_k}p_k$ da
$\int_I K_n(t_i, t)\,q(t)\,w(t)\dd t = q(t_i)$ por cada
$q$ de grado $\leq n - 1$. Tome $q = \ell_i$: el lado izquierdo
es igual a $\ell_i(t_i) = 1$. Pero $t \mapsto K_n(t_i,
t)\,\ell_i(t)$es un polinomio de grado$\leq (n - 1) + (n
- 1) = 2n - 2$, en el cual$Q_n$ es exacto (pregunta 9), y
desaparece en cada nodo $t_j \neq t_i$ (factor $\ell_i$), por lo que
\[
1 = \int_I K_n(t_i, t)\,\ell_i(t)\,w(t)\dd t
= w_i\,K_n(t_i, t_i)
= w_i\sum_{k=0}^{n-1}\frac{p_k(t_i)^2}{h_k} .
\]
La suma es $> 0$ (su término $k = 0$ es $1/h_0 > 0$): el
fórmula declarada, y positividad nuevamente. Verificar ($n = 2$,
Leyenda): $p_0 = 1$, $h_0 = 2$, $p_1 = t$, $h_1 = \frac23$;
en $t_i = \pm\frac1{\sqrt3}$,
\[
K_2(t_i, t_i) = \frac12 + \frac{1/3}{2/3} = 1,
\qquad w_i = 1,
\]
como se encontró en la pregunta 11.

\textbf{25.} Sustituyendo $t = \cos\theta$, la integral es
$\int_0^\pi\cos^6\theta\,\dd\theta =
\pi\,\frac{5\cdot3\cdot1}{6\cdot4\cdot2} = \frac{5\pi}{16}$
(Wallis, \cref{exo:b3:product:8}). El $n = 3$
Chebyshev: la regla de Gauss (pregunta 17) tiene nodos
$\cos\frac\pi6 = \frac{\sqrt3}2$, $\cos\frac\pi2 = 0$,
$\cos\frac{5\pi}6 = -\frac{\sqrt3}2$ y pesos iguales
$\frac\pi3$:
\[
Q_3(t^6) = \frac\pi3\Bigl(2\cdot\Bigl(\frac{\sqrt3}2
\Bigr)^{6}\Bigr) = \frac\pi3\cdot\frac{54}{64}
= \frac{9\pi}{32},
\qquad
\frac{5\pi}{16} - \frac{9\pi}{32} = \frac\pi{32} .
\]
Predicción: el polinomio ortogonal de grado mónico-$3$ es
$2^{-2}T_3 = t^3 - \frac34t$, con $h_3 =
\frac1{16}\norm{T_3}_w^2 = \frac1{16}\cdot\frac\pi2 =
\frac\pi{32}$; y$f = t^6$tiene$f^{(6)} = 720 =
6!$constante, por lo que la pregunta 23 da el error$\frac{6!}{6!}\,h_3 =
\frac\pi{32}$--- sin dependencia de$\xi$ izquierda, el
La fórmula está obligada a ser exacta, y lo es.
\end{solution}